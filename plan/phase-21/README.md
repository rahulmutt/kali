# Phase 21 — Semantic Completeness and Conformance

## Goal

Close remaining language gaps by either implementing faithful semantics with evidence or preserving explicit, tested availability gates.

## Owning specs

- `specs/02-lexer-parser.md`
- `specs/03-ast.md`
- `specs/04-type-system.md`
- `specs/05-ir.md`
- `specs/10-runtime.md`
- `specs/16-testing.md`
- `specs/19-feature-maturity.md`

## Work packets

### 21.1 Generators and async generators

- Implement resumable generator and async-generator state-machine lowering.
- Cover `yield`, `yield*`, `return`, `throw`, `try/finally`, iterator close/finalization, and async interaction.
- Preserve generator/function-kind metadata through parser, AST, HIR, MIR, LIR, codegen, and export analysis.
- Async class methods now ride the shared async-function lowering path; generator class methods remain gated, and the direct runtime-entrypoint gate now also covers async class expressions in JSX/TSX input, generator class expressions in JS/TS/JSX/TSX input, plus async-generator default-export class expressions in JS/TS/JSX/TSX input. Generator codegen plus the type-resolution, CLI build-preflight, and class-method runtime gates now emit distinct generator vs async-generator wording while preserving the same canonical E5506 path for function declarations and expressions, and runtime smoke now also covers those same class-expression gates for direct `run` / `test` JSON and text output, including the TS async default-export class-expression slice.
- Current coverage also exercises async class methods through build smoke in TS, JS, JSX, and TSX input on both the Deno and browser artifact paths.
- The parser now accepts class-expression forms with preserved async/generator metadata, and direct `run` / `test` entrypoints now gate those async class expressions too; generator class expressions now also hit the direct runtime E5506 gate before lowering, including the direct runtime JS/TS coverage path. The browser API surface smoke now mirrors generator class-expression and async-generator default-export class-expression rejection across JS, TS, JSX, and TSX input.
- The Node `process.kill(0)` liveness-probe slice now also keeps transparent parenthesized and unary-plus wrapper forms covered alongside the bracketed Node aliases across JS and TS smoke input, the Node package-corpus evidence now also mirrors the bracketed and `globalThis` alias forms, and the dedicated Node API-surface support regression now mirrors that same wrapper/alias matrix in its `run` and `test` inputs, including wrapped `process` and wrapped `process.kill` call-target forms plus const aliases and sequence-callable-target forms of the callable target itself, TS/TSX satisfies-wrapped zero literals on the checker and CLI surfaces and bracketed `globalThis.process["kill"]` / `globalThis["process"]["kill"]` / `globalThis.process["kill"](0)` / `globalThis["process"]["kill"](0)` zero-probe spellings. Static aliases of the zero-probe argument now also feed that slice on the Node surface, and the zero-probe wrapper regression now also covers `(0 as number)` assertions in TS input, while the type-resolution regression set now also mirrors that Node zero-probe matrix directly on the checker path and now also pins the exact `process["kill"](0)` / `globalThis.process["kill"](0)` / `globalThis["process"]["kill"](0)` call-target variants, including the `globalThis["process"].kill(0)` / `globalThis.process["kill"](0)` / `((globalThis["process"].kill))(0)` / `((globalThis.process["kill"]))(0)` alias spellings. The current Node smoke now also includes wrapped `globalThis.process.kill` and wrapped bracketed `globalThis.process["kill"]` call-target forms, and the browser late-compat smoke now mirrors that wrapped alias in JS, TS, JSX, and TSX input, including the wrapped `((globalThis.process.kill))(0)`, `((globalThis.process["kill"]))(0)`, `((globalThis["process"].kill))(0)`, and `((globalThis["process"]["kill"]))(0)` zero-probe aliases. The standalone JS late-compat corpus now also carries the fully bracketed `((globalThis["process"]["kill"]))(0)` alias.
- The default-standalone late process-control rejection corpus now also carries `process.kill(0)` and wrapper spellings so the unsupported-path evidence stays aligned with the Node zero-probe slice.
- The codegen regression set keeps the transparent-wrapper `process.kill(0)` coverage and now also pins transparent type-wrapper variants so the packet stays buildable while preserving the same zero-probe coverage.
- HIR, MIR, LIR, and function-plan tests now also preserve class-expression generator metadata alongside the existing class-method coverage.
- Direct `run` / `test` execution for async class methods now has an explicit E5506 gate; keep generator-class lowering gated until the dedicated packet lands.
- Keep all unsupported forms behind canonical `E5506` gates until the full runtime path exists.

### 21.2 Iterator and async-iterator protocols

- Expand `for...of`, `for await...of`, spreads, and iterable consumption beyond current bounded static slices; current smoke now also covers the direct string-concatenation slice in browser bundle JS, TS, JSX, and TSX input, standalone TS run smoke for the `for await...of` string-concatenation slice, browser-bundle/browser-harness smoke for the `for...of` template-literal slice over statically known string operands in JS, TS, JSX, and TSX input, standalone run/test smoke for that template-literal slice in TS input with JSON-output coverage, and browser-requested run/test browser-harness smoke now also covers that same string-concatenation slice in JS, TS, JSX, and TSX input with JSON-output coverage.
- Implement protocol lookup, `next` result handling, abrupt completion, iterator close, and async iterator finalization.
- Current smoke now also covers the single-quoted bracketed `Reflect` alias over frozen object literals in the direct and browser-harness paths, plus sequence-expression wrappers around the supported static `Reflect.ownKeys(...)` slice in JS input, and browser-requested run/test coverage now also exercises the frozen-object `Reflect.ownKeys(...)` slice in JS, TS, JSX, and TSX input with JSON-output coverage, alongside the browser-bundle string-concatenation smoke in JS, TS, JSX, and TSX input and the matching browser-harness string-concatenation smoke in the same input matrix. The type checker now also accepts the bracketed `globalThis["Reflect"]["ownKeys"]` alias spelling on that supported `Reflect.ownKeys(...)` slice. The browser-harness `Object.values([...Object.fromEntries(...)])` spread smoke now also covers frozen operands in JS, TS, JSX, and TSX input with JSON-output coverage, and standalone run/test smoke now also covers the `for await` spread-of-`Object.values` / `Object.keys` / `Object.entries` slice over `Object.fromEntries(...)` operands in JS and TS input with JSON-output coverage. Static `new Set(...)` constructor slices now also type-check, build, and execute in standalone run/test smoke over the supported iterable inputs, including `Object.freeze`-wrapped array operands, and static `new Map(...)` constructor slices now also share that supported path. Browser bundle smoke now also covers the static `new Set(...)` constructor slices across JS, TS, JSX, and TSX input, plus the static `new Map(...)` constructor slices over supported entry tuples across the same matrix, while alias-chain and transparent-wrapper coverage now also pins `Set`/`Map` constructor references through the same bounded root-resolution path, including const-alias and parenthesized-alias `Set`/`Map` constructor coverage in runtime, browser-harness, codegen, and type-resolution smoke, and the current repository snapshot now also covers the root `globalThis.Set` / `globalThis["Set"]` and `globalThis.Map` / `globalThis["Map"]` constructor spellings in that same supported slice. Runtime smoke now also carries focused JS-input Set/Map constructor regressions for the supported standalone and browser-requested run/test paths, including the frozen-array wrapper form.
- Add conformance fixtures for supported built-ins and negative diagnostics for unimplemented protocol edges.

### 21.3 Dynamic language and built-in semantics

- Widen object-model, Math, BigInt, dynamic import, reflection, and operator semantics only when observable JavaScript behavior is pinned; current coverage also includes standalone `run` / `test` smoke for the `Number.isFinite` / `Number.isNaN` / `Number.isInteger` / `Number.isSafeInteger` primitive-value slice across JS and TS input with JSON-output coverage, plus browser-bundle smoke for that same slice across JS, TS, JSX, and TSX input, including mixed bracket/dot spellings such as `globalThis["Number"].isNaN`, and the fully bracketed `globalThis["Math"]["sqrt"]` / `globalThis["Math"]["cbrt"]` math slice now also covers JS, TS, JSX, and TSX input on the browser bundle/harness paths. Promise.allSettled now also has standalone build/runtime and browser-requested browser-harness coverage across JS, TS, JSX, and TSX input. Nullish coalescing smoke now also covers `void 0 ??` and `undefined ??` fallback operands on the standalone run/test paths and the browser-requested browser-harness paths in JS input. The same object-identity slice now also has type-system regression coverage for the `Object.is` alias spellings used by the browser/runtime smoke paths, including parser-backed bracketed `Object.is` and `Number.is*` alias spellings in JS input, and browser-harness `Object.is` same-reference alias-chain smoke now also exercises the bracketed, mixed, root, and dot alias spellings alongside the frozen-object variant; dedicated browser-harness alias-chain smoke now also covers object/array same-reference, frozen-reference, and fresh-literal comparisons across JS, TS, JSX, and TSX input, with browser-requested run/test JSON smoke now mirroring the object/array/frozen-reference matrix across that same input set, and the browser-requested browser-harness/browser-bundle smoke now also accepts const aliases that resolve to the supported `Object.is` and `Number.is*` callables. The browser-requested `Math.atan2` smoke now also covers the single-quoted bracketed zero slice in JS, TS, JSX, and TSX input with JSON-output coverage, and browser-harness/browser-bundle smoke now also covers the same single-quoted bracketed zero slice in TS and `.js` input; the unsupported-math browser-harness rejection smoke now also covers the bracketed `Math["sqrt"]` / `Math["atan2"]` and `globalThis["Math"].sqrt` / `globalThis["Math"].atan2` spellings in JSX and TSX input, and the browser `Math.round` smoke now also includes the fully bracketed `globalThis["Math"]["round"]` spelling on the bundle and harness paths. The browser-harness `Math.atan2` trailing-argument evaluation smoke now also covers TS, JSX, and TSX input with JSON-output coverage, and the browser-bundle smoke now also covers that same trailing-argument evaluation slice in JS, TS, JSX, and TSX input with JSON-output coverage. The Node `process.kill(0)` liveness-probe slice is now also available on the documented Node surface across `check` / `build` / `run` / `test`, while broader kill/signal semantics remain gated, and the current Node smoke now also pins the `globalThis.process["kill"](0)` and `globalThis["process"].kill(0)` aliases. Wrapped call targets with type assertions or `satisfies` expressions now resolve for direct callable references; the parser now also canonicalizes transparent wrapper forms around computed object-property keys when they still resolve to static string/number names, including unary numeric wrappers, and the computed-numeric object-key smoke now exercises that path across browser-harness and browser-bundle contexts, the type-resolution regression suite now also mirrors the `Number.isSafeInteger` bracketed alias family in JS input, and `check` / `build` smoke now also covers the Number-predicate alias slice across Deno and browser surfaces in JS, TS, JSX, and TSX input. The browser-targeted `Object.hasOwn` / `Object.prototype.hasOwnProperty.call` slice now also accepts const aliases that resolve to those callables in the browser-check/build and browser-harness/browser-bundle paths, and the same helper family now also recognizes the bracketed `Object["hasOwnProperty"].call` / `globalThis["Object"]["hasOwnProperty"].call` aliases plus the fully bracketed `Object["hasOwnProperty"]["call"]` / `globalThis["Object"]["hasOwnProperty"]["call"]` aliases on the supported static-object slice. The current repository snapshot also accepts const aliases that resolve to the supported `Object.is` and `Number.is*` callables on the standalone JS run/test path. The browser late-compat process-control corpus now also carries `process.kill(0)` invocation and wrapper forms in JS/TS/JSX/TSX rejection coverage so the negative evidence stays aligned with the Node zero-probe slice. Diagnostic wording now matches that supported primitive/reference slice across the checker and codegen paths.
- Keep non-literal dynamic import, broad reflective APIs, and eval-adjacent forms gated unless their spec rows are promoted.
- Pair each promotion with checker, lowering, runtime, browser/context, and JSON-output evidence where applicable.

### 21.4 Bounded TS/JS inference

- Grow inference inside deterministic budgets only.
- Preserve annotation-required boundaries for exported/public and cross-module surfaces when inference would exceed the bounded contract.
- Add positive and negative checker baselines for TS and first-class JS input.
- Library-export signature collection now also resolves function-binding alias chains within the bounded slice, including same-statement `const` aliases when the target signature is already known.
- Default-export aliases now resolve through the same bounded function-binding map used for local export aliases, and the regression corpus now also covers default-export alias chains and multi-hop source-graph re-export chains, including the new default-export alias-chain source-graph regression.

### 21.5 Conformance hygiene

- Maintain compact dashboards of supported vs gated semantics.
- Remove implementation-journal prose from plan files; exact coverage belongs in tests and maturity current-state notes.

## Exit gate

- Each promoted semantic slice has parser/checker/lowering/runtime evidence.
- Unsupported but recognized forms fail through the canonical diagnostic path.
- Maturity and current-state docs are updated only for evidence-backed availability changes.
