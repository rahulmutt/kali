# Phase 21 — Semantic Completeness and Conformance

## Goal

Close remaining language gaps by either implementing faithful semantics with evidence or preserving explicit, tested availability gates.

## Owning specs

- `specs/02-lexer-parser.md`
- `specs/03-ast.md`
- `specs/04-type-system.md`
- `specs/05-ir.md`
- `specs/10-runtime.md`
- `specs/16-testing.md`
- `specs/19-feature-maturity.md`

## Work packets

### 21.1 Generators and async generators

- Implement resumable generator and async-generator state-machine lowering.
- Cover `yield`, `yield*`, `return`, `throw`, `try/finally`, iterator close/finalization, and async interaction.
- Preserve generator/function-kind metadata through parser, AST, HIR, MIR, LIR, codegen, and export analysis.
- AST serde round-trips now also preserve generator and async-generator flags on function declarations, function expressions, class declarations, and class methods, keeping the typed AST metadata path exercised.
- Async class methods now ride the shared async-function lowering path; generator class methods remain gated, and the direct runtime-entrypoint gate now also covers async class expressions in JS/TS/JSX/TSX input, generator class expressions in JS/TS/JSX/TSX input, plus async-generator default-export class expressions in JS/TS/JSX/TSX input. Generator codegen plus the type-resolution, CLI build-preflight, and class-method runtime gates now emit distinct generator vs async-generator wording while preserving the same canonical E5506 path for function declarations and expressions, and runtime smoke now also covers those same class-expression gates for direct `run` / `test` JSON and text output, including the TS async default-export class-expression slice. Direct `run` / `test` generator-function rejection smoke now also covers TS input alongside the existing JS/JSX/TSX coverage, and standalone runtime smoke now also covers TS input for class generator-method rejection on both text and JSON output. The runtime-entrypoint regression set now also exercises transparent TS `as`/`satisfies` wrappers around generator and async-generator class expressions so that gate stays aligned with the TypeScript wrapper path too.
- The class-expression generator/async-generator rejection matrix now also has explicit `check` / `build` and type-resolution coverage through variable-initializer and default-export expression forms in JS/TS/JSX/TSX input, keeping the direct runtime gate aligned with the checker and build preflight paths. The type-resolution regression now also exercises that gate across JS, TS, JSX, and TSX source extensions.
- Current coverage also exercises async class methods through build smoke in TS, JS, JSX, and TSX input on both the Deno and browser artifact paths, and standalone `run` / `test` rejection smoke now also covers the same async class-method sequencing gate in JS, TS, JSX, and TSX input with both text and JSON output. The type-resolution regression for `Reflect.ownKeys(...)` now also mirrors the single-quoted `globalThis['Reflect']['ownKeys']` alias on the await-wrapped helper path, keeping the bracketed alias family aligned with the browser/runtime smoke.
- The parser now accepts class-expression forms with preserved async/generator metadata, including `yield*` delegation in the class-expression preservation smoke, and direct `run` / `test` entrypoints now gate those async class expressions too; generator class expressions now also hit the direct runtime E5506 gate before lowering, including the direct runtime JS/TS coverage path. The browser API surface smoke now mirrors generator class-expression and async-generator default-export class-expression rejection across JS, TS, JSX, and TSX input. The parser's computed object-property-name canonicalization now also accepts `await` wrappers when they still resolve to static string/number names, and the regression corpus now also covers nested await/sequence-wrapped computed property names. The shared generator/async-generator function-lowering diagnostic helper now returns a static string, so the mixed-flavor wording stays aligned without a per-call allocation. Standalone `run` / `test` generator-function rejection smoke now also has JSON-output coverage in JS input, keeping the direct runtime matrix consistent with the checker-side JSON regression. The standalone frozen `Object.hasOwn` smoke now also covers parenthesized transparent wrappers around the frozen callable aliases, and the browser harness/bundle `Object.hasOwn` sources now also mirror the bracketed frozen callable alias `Object.freeze((globalThis["Object"]["hasOwn"]))`, keeping that helper-family alias coverage aligned across the shared corpus; the shared frozen-callable inventory now also includes the dot-root `globalThis.Object.hasOwn` and `globalThis["Object"].hasOwn` aliases so the helper stays single-sourced across dot and bracket spellings. Standalone `run` / `test` smoke now also covers the await-wrapped `Object.is` / `Number.isSafeInteger` / `Object.keys` / `Reflect.ownKeys` helper slice through an async test callback that returns the probe promise, keeping the JS/TS/JSX/TSX await-wrapper matrix aligned across both execution lanes.
- The Node `process.kill(0)` liveness-probe slice now also keeps transparent parenthesized and unary-plus wrapper forms covered alongside the bracketed Node aliases across JS and TS smoke input, the Node package-corpus evidence now also mirrors the bracketed and `globalThis` alias forms, plus the zero-probe freeze aliases and fully bracketed wrapped kill alias, and the dedicated Node API-surface support regression now mirrors that same wrapper/alias matrix in its `run` and `test` inputs, including wrapped `process` and wrapped `process.kill` call-target forms plus const aliases and sequence-callable-target forms of the callable target itself, including the `globalThis.process["kill"]` const/sequence alias pair added in the current Node smoke, TS/TSX satisfies-wrapped zero literals on the checker and CLI surfaces and bracketed `globalThis.process["kill"]` / `globalThis["process"].kill(0)` / `globalThis["process"]["kill"]` / `globalThis.process["kill"](0)` / `globalThis["process"]["kill"](0)` zero-probe spellings, and the shared sequence-callable-target binding segment now lives in `kali_common` so the node-surface smoke reuses the canonical declarations instead of copying them inline. The direct call-target binding helper now also distinguishes the bracketed-dot `globalThis["process"].kill` alias from the fully bracketed `globalThis["process"]["kill"]` alias so that smoke stays explicit. Static aliases of the zero-probe argument now also feed that slice on the Node surface, and the zero-probe wrapper regression now also covers `(0 as number)` assertions in TS input, while the type-resolution regression set now also mirrors that Node zero-probe matrix directly on the checker path and now also pins the exact `process["kill"](0)` / `globalThis.process["kill"](0)` / `globalThis["process"]["kill"](0)` call-target variants, including the `globalThis["process"].kill(0)` / `globalThis.process["kill"](0)` / `((globalThis["process"].kill))(0)` / `((globalThis.process["kill"]))(0)` alias spellings, and the codegen regression set now also exercises the direct bracketed `globalThis["process"]["kill"](+0)` call-target variant on the same supported zero-probe path. The JS parser-backed regression now also keeps the bracketed `globalThis.process["kill"](0)` and `globalThis["process"].kill(0)` spellings in the same supported slice. The current Node smoke now also includes wrapped `globalThis.process.kill` and wrapped bracketed `globalThis.process["kill"]` call-target forms, and the browser late-compat smoke now mirrors that wrapped alias in JS, TS, JSX, and TSX input, including the wrapped `((globalThis.process.kill))(0)`, `((globalThis.process["kill"]))(0)`, `((globalThis["process"].kill))(0)`, and `((globalThis["process"]["kill"]))(0)` zero-probe aliases. The current packet also now covers parenthesized transparent freeze wrappers like `Object.freeze((process.kill))(0)` / `(+0)` across the Node and browser smoke matrices. The standalone JS late-compat corpus now also carries the fully bracketed `((globalThis["process"]["kill"]))(0)` alias, the transparent `Object.freeze(globalThis.process["kill"])(0)` / `Object.freeze(globalThis.process["kill"])(+0)` alias and the direct `Object.freeze(globalThis.process.kill)(0)` / `Object.freeze(globalThis.process.kill)(+0)` alias, the receiver-freeze aliases and their +0 siblings `Object.freeze(process)["kill"](0)` / `Object.freeze(globalThis.process)["kill"](0)` / `Object.freeze(globalThis["process"])["kill"](0)`, the mixed dot/bracket `Object.freeze(globalThis["process"].kill)(0)` alias, and the bracketed transparent `Object.freeze(globalThis["process"]["kill"])(0)` alias, and the browser/runtime late-compat smoke matrices now also include the bracketed `((process["kill"]))(0)` wrapper form, and the browser late-compat process-control matrix now also includes the parenthesized zero form under the bracketed `globalThis["process"]["kill"]` alias and the bracketed `process["kill"](+0)` spelling on the same browser-input matrix, while the Node API-surface regression now covers the exact bracketed `globalThis["process"]["kill"](0)` call target and the fully bracketed wrapped `((globalThis["process"]["kill"]))(0)` alias. The codegen regression set now also pins the direct `globalThis["process"].kill(0)` alias form on the same supported zero-probe path, the build-preflight rejection helper source now also includes the browser-safe receiver-freeze aliases and their +0 siblings `Object.freeze(globalThis.process)["kill"](0)` / `Object.freeze(globalThis["process"])["kill"](0)`, and the Node zero-probe support regression now also covers the transparent `Object.freeze(globalThis.process["kill"])(0)` / `Object.freeze(globalThis.process["kill"])(+0)` alias alongside the existing wrapper and const-alias cases, and the bracketed `Object.freeze(globalThis["process"]["kill"])(0)` alias in the same matrix, including the mixed dot/bracket frozen alias `Object.freeze(globalThis["process"].kill)(0)` in the current smoke set. The browser late-compatibility process-control matrix now also includes the transparent `Object.freeze(globalThis.process["kill"])(0)` / `Object.freeze(globalThis.process["kill"])(+0)` alias, the mixed dot/bracket `Object.freeze(globalThis["process"].kill)(0)` alias, and the bracketed `Object.freeze(globalThis["process"]["kill"])(0)` alias on the same browser rejection path, and the unsupported-path diagnostics now also name the exact bracketed aliases so the negative evidence stays aligned with the supported alias matrix. The supported zero-probe matrix now also includes the receiver-freeze sibling `Object.freeze(process)["kill"](+0)` on the Node/runtime/browser-covered paths, plus the direct bracketed `process["kill"](+0)` probe on the standalone runtime and Node API-surface smoke paths.
- The Node zero-probe smoke now also covers the fully bracketed `Object.freeze(globalThis["process"]["kill"])(0)` / `(+0)` aliases in the runtime and Node API-surface matrices.
- The runtime smoke now also pins the `+0` siblings for the unparenthesized receiver-freeze aliases `Object.freeze(globalThis.process)["kill"]` / `Object.freeze(globalThis["process"])["kill"]`, and the doubly parenthesized bracketed `((globalThis["process"]["kill"]))(+0)` alias, keeping the zero-probe wrapper matrix aligned with the shared helper inventory.
- The default-standalone late process-control rejection corpus now also carries `process.kill(0)`, wrapper spellings, and receiver-freeze aliases and their +0 siblings so the unsupported-path evidence stays aligned with the Node zero-probe slice.
- The codegen regression set keeps the transparent-wrapper `process.kill(0)` coverage and now also pins transparent type-wrapper variants so the packet stays buildable while preserving the same zero-probe coverage. The codegen and type-checker rejection wording now also enumerates the transparent `Object.freeze(globalThis.process["kill"])(0)` / `Object.freeze(globalThis.process["kill"])(+0)` and `Object.freeze(globalThis["process"].kill)(0)` / `Object.freeze(globalThis["process"].kill)(+0)` aliases so the canonical failure text stays aligned with the supported alias matrix, and codegen smoke now also pins the bracketed `Object.freeze(globalThis["process"]["kill"])(0/+0)` aliases plus the parenthesized bracketed `Object.freeze((globalThis["process"]["kill"]))` variants. The shared zero-probe wording now comes from one helper so those diagnostics stay in sync.
- HIR, MIR, LIR, and function-plan tests now also preserve class-expression generator metadata alongside the existing class-method coverage, and the class-expression smoke now includes `yield*` delegation preservation on the parser path.
- Direct `run` / `test` execution for async class methods now has an explicit E5506 gate; keep generator-class lowering gated until the dedicated packet lands.
- Keep all unsupported forms behind canonical `E5506` gates until the full runtime path exists.
- Browser late-compat and Node surface smoke now also pin the mixed frozen `Object.freeze(globalThis["process"].kill)(0)` alias and its `+0` zero-literal sibling, plus direct `globalThis.process["kill"](+0)` / `globalThis["process"].kill(+0)` / `globalThis["process"]["kill"](+0)` zero-probe aliases; the browser late-compat source corpus now also asserts the direct `globalThis.process["kill"](+0)` spelling across JS, TS, JSX, and TSX input, and the same browser matrix now also covers the doubly wrapped `((globalThis["process"]["kill"]))(+0)` alias with explicit regression assertions. The browser late-compat rejection fixtures now also cover the parenthesized bracketed freeze-wrapper alias family in the browser corpus.

### 21.2 Iterator and async-iterator protocols

- Expand `for...of`, `for await...of`, spreads, and iterable consumption beyond current bounded static slices; current smoke now also covers the direct string-concatenation slice in browser bundle JS, TS, JSX, and TSX input, standalone TS run smoke for the `for await...of` string-concatenation slice, browser-bundle/browser-harness smoke for the `for...of` template-literal slice over statically known string operands in JS, TS, JSX, and TSX input, standalone run/test smoke for that template-literal slice in TS input with JSON-output coverage, standalone run/test smoke for the same template-literal slice in JS input with JSON-output coverage, browser-requested run/test browser-harness smoke now also covers that same string-concatenation slice in JS, TS, JSX, and TSX input with JSON-output coverage, browser-bundle smoke now also covers the sequence-expression wrappers around the supported string-primitive `Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` slice in JS, TS, JSX, and TSX input, the browser bundle matrix now also includes direct `Object.values(...)` iteration over object-literal inputs in JS, TS, JSX, and TSX input, the `for await` array slice now also accepts a transparent `await` wrapper over a literal array in the standalone JS run path, browser-build smoke now also carries that transparent `await` wrapper slice across the browser JS, TS, JSX, and TSX inputs, browser-bundle smoke now also carries that same transparent `await` wrapper slice across the browser JS, TS, JSX, and TSX inputs, browser-harness smoke now also covers `for...of` / `for await...of` array break/continue on the supported array-iteration path in JS, TS, JSX, and TSX input with JSON-output coverage, browser-requested run/test browser-harness rejection coverage now also includes non-literal `new Set(values.filter(Boolean))` and `new Map(values.filter(Boolean))` sources in JS input, and the Set/Map constructor regression corpus now also keeps frozen `globalThis.Set` / `globalThis["Set"]` and `globalThis.Map` / `globalThis['Map']` aliases on the supported iterator path in runtime, browser-harness, and browser-bundle smoke. The shared `Set` / `Map` constructor alias inventories now also live in `kali_common`, giving the constructor smoke a common root-and-frozen vocabulary for future reuse.
- Implement protocol lookup, `next` result handling, abrupt completion, iterator close, and async iterator finalization.
- Current smoke now also covers the single-quoted bracketed `Reflect` alias over frozen object literals in the direct and browser-harness paths, plus sequence-expression wrappers around the supported static `Reflect.ownKeys(...)` slice in JS input, and browser-requested run/test coverage now also exercises the frozen-object `Reflect.ownKeys(...)` slice in JS, TS, JSX, and TSX input with JSON-output coverage, alongside the browser-bundle string-concatenation smoke in JS, TS, JSX, and TSX input and the matching browser-harness string-concatenation smoke in the same input matrix. The type checker now also accepts the bracketed `globalThis["Reflect"]["ownKeys"]` alias spelling on that supported `Reflect.ownKeys(...)` slice, and codegen smoke now also covers the mixed `globalThis["Reflect"].ownKeys` alias spelling there. The browser-harness `Object.values([...Object.fromEntries(...)])` spread smoke now also covers frozen operands in JS, TS, JSX, and TSX input with JSON-output coverage, and it now also exercises the bracketed `globalThis["Object"]["fromEntries"]` alias alongside the existing spread matrix so the alias path stays aligned with the bundle coverage. The browser-bundle `Object.values([...Object.fromEntries(...)])` spread smoke now also covers frozen `Object.fromEntries(...)` operands in JS, TS, JSX, and TSX input with JSON-output coverage. Standalone run/test smoke now also covers the `for await` spread-of-`Object.values` / `Object.keys` / `Object.entries` slice over `Object.fromEntries(...)` operands in JS and TS input with JSON-output coverage, and browser bundle/harness object-key smoke now also carries await-wrapped `Object.keys` and `Reflect.ownKeys` helper inputs. Static `new Set(...)` constructor slices now also type-check, build, and execute in standalone run/test smoke over the supported iterable inputs, including `Object.freeze`-wrapped array operands, and static `new Map(...)` constructor slices now also share that supported path. Browser bundle/browser-harness smoke now also covers frozen constructor aliases such as `Object.freeze(Set)` / `Object.freeze(Map)` alongside the static `new Set(...)` constructor slices across JS, TS, JSX, and TSX input, plus the static `new Map(...)` constructor slices over supported entry tuples across the same matrix, while alias-chain and transparent-wrapper coverage now also pins `Set`/`Map` constructor references through the same bounded root-resolution path, including const-alias and parenthesized-alias `Set`/`Map` constructor coverage in runtime, browser-harness, codegen, and type-resolution smoke, plus frozen constructor aliases such as `Object.freeze(Set)` / `Object.freeze(Map)` in the codegen/runtime smoke path, and the current repository snapshot now also covers the dotted `globalThis.Set` / `globalThis.Map` spellings alongside the bracketed `globalThis["Set"]` / `globalThis["Map"]` constructor spellings in that same supported slice. Codegen smoke now also pins the exact bracketed `globalThis["Set"]` / `globalThis['Map']` root spellings on the same Set/Map slice, and the checker/build regression corpus now also pins the bracketed `globalThis["Set"]` and `globalThis['Map']` constructor spellings on the shared supported Set/Map slice. Runtime smoke now also carries focused JS-input Set/Map constructor regressions for the supported standalone and browser-requested run/test paths, including the frozen-array wrapper form, and the browser-harness lane now also has a dedicated `Map` constructor iteration fixture mirroring the Set harness matrix in JS, TS, JSX, and TSX input; the current smoke now also exercises abrupt return/finally and throw/finally sequencing on the supported Set/Map constructor slice across the existing run/test, browser-harness, and browser-bundle coverage, and the object-enumeration break/continue slice now also has standalone, browser-harness, and browser-bundle smoke on the supported `Object.keys(...)` iteration path, while the browser-bundle object-enumeration finalization smoke now also covers the same `Object.keys(...)` / `Object.entries(...)` return/finally and throw/finally matrix across JS, TS, JSX, and TSX input.
- Add conformance fixtures for supported built-ins and negative diagnostics for unimplemented protocol edges; the current iterator corpus now also pins call-expression source edges such as `new Set(values.filter(Boolean))` and the parallel `new Map(values.filter(Boolean))` rejection, now covered across JS, TS, JSX, TSX, and inherited browser-config rejection paths, to keep the literal-array gate explicit.
- Static `Array.from(...)` wrappers around already-supported iterable slices now also lower through the same bounded array-iteration path, including JS/TS/JSX/TSX `run` and `test` coverage, giving the iterator packet one more transparent wrapper slice without widening the unsupported protocol surface.
- The default standalone `run` / `test` rejection corpus now also mirrors that non-literal `new Set(...)` / `new Map(...)` gate on JS input, keeping the standalone iterator-source matrix aligned with the browser-config rejection coverage.

- The parser now also accepts dot-member `from` property access, so direct `Array.from(...)` can flow through the supported array-iteration path alongside the existing bracketed alias smoke.

### 21.3 Dynamic language and built-in semantics

- Widen object-model, Math, BigInt, dynamic import, reflection, and operator semantics only when observable JavaScript behavior is pinned; current coverage also includes standalone `run` / `test` smoke for the `Number.isFinite` / `Number.isNaN` / `Number.isInteger` / `Number.isSafeInteger` primitive-value slice across JS and TS input with JSON-output coverage, plus browser-bundle smoke for that same slice across JS, TS, JSX, and TSX input, including mixed bracket/dot spellings such as `globalThis["Number"].isNaN`, and the fully bracketed `globalThis["Math"]["sqrt"]` / `globalThis["Math"]["cbrt"]` math slice now also covers JS, TS, JSX, and TSX input on the browser bundle/harness paths. Promise.allSettled now also has standalone build/runtime, browser-requested browser-harness, and browser-bundle coverage across JS, TS, JSX, and TSX input, and the smoke now also covers a freeze-wrapped bracketed callable alias plus mixed dot/bracket spellings such as `globalThis.Promise["allSettled"]` and `globalThis["Promise"].allSettled` on the same supported slice. Nullish coalescing smoke now also covers `void 0 ??` and `undefined ??` fallback operands on the standalone run/test paths and the browser-requested browser-harness paths in JS input. The same object-identity slice now also has type-system regression coverage for the `Object.is` alias spellings used by the browser/runtime smoke paths, including parser-backed bracketed `Object.is` and `Number.is*` alias spellings in JS input, and browser-harness `Object.is` same-reference alias-chain smoke now also exercises the bracketed, mixed, root, and dot alias spellings alongside the frozen-object variant; dedicated browser-harness alias-chain smoke now also covers object/array same-reference, frozen-reference, and fresh-literal comparisons across JS, TS, JSX, and TSX input, with browser-requested run/test JSON smoke now mirroring the object/array/frozen-reference matrix across that same input set, and the browser-requested browser-harness/browser-bundle smoke now also accepts const aliases that resolve to the supported `Object.is` and `Number.is*` callables. That same object-identity slice now also accepts no-substitution template literals on the supported primitive/reference path, and the checker now also pins the signed-zero `-0` / `+0` SameValue edge on the same supported slice. The browser-requested `Math.atan2` smoke now also covers the single-quoted bracketed zero slice in JS, TS, JSX, and TSX input with JSON-output coverage, and browser-harness/browser-bundle smoke now also covers the same single-quoted bracketed zero slice in TS, `.js`, JSX, and TSX input; the unsupported-math browser-harness rejection smoke now also covers the bracketed `Math["sqrt"]` / `Math["atan2"]` and `globalThis["Math"].sqrt` / `globalThis["Math"].atan2` spellings in JSX and TSX input, plus the single-quoted bracketed `globalThis['Math']['sqrt']` / `globalThis['Math']['atan2']` spellings on the same unsupported browser-harness path, and the browser `Math.round` smoke now also includes the fully bracketed `globalThis["Math"]["round"]` spelling on the bundle and harness paths, plus the transparent `Object.freeze(globalThis["Math"]["round"])` callable alias on the same supported slice. The browser-harness `Math.pow` smoke now also covers `Object.freeze(globalThis["Math"]["pow"])` on the same supported alias path, and the exponent-one/base-one identity smoke now also mirrors the same frozen callable aliases; the shared helper inventory now also includes the single-quoted `Math['pow']` / `globalThis.Math['pow']` / `globalThis['Math'].pow` / `globalThis['Math']['pow']` spellings so the browser smoke stays source-single-sourced across quote styles. Browser-targeted build smoke now also rejects negative `Math.pow(2, -1)` exponents in JS and TS input on both the Deno and browser surfaces, keeping the current-phase gate explicit. The negative `Math.pow(2, -1)` rejection path now also covers JSX and TSX inputs on both the Deno and browser surfaces, and the browser-requested run/test harness rejection path now also mirrors JSX and TSX inputs. The browser-harness `Math.atan2` trailing-argument evaluation smoke now also covers TS, JSX, and TSX input with JSON-output coverage, and the browser-bundle smoke now also covers that same trailing-argument evaluation slice in JS, TS, JSX, and TSX input with JSON-output coverage. The Node `process.kill(0)` liveness-probe slice is now also available on the documented Node surface across `check` / `build` / `run` / `test`, while broader kill/signal semantics remain gated, and the current Node smoke now also pins the `globalThis.process["kill"](0)` and `globalThis["process"].kill(0)` aliases. Wrapped call targets with type assertions or `satisfies` expressions now resolve for direct callable references; `Object.freeze(...)` now also behaves as a transparent callable wrapper for the supported Node zero-probe slice, so frozen `process.kill` references stay on the same supported path. The same transparent-wrapper path now also accepts `Object.freeze`-wrapped `Object.hasOwn` and `Math.floor` helper calls in standalone JS smoke, including the bracketed `globalThis["Object"]["hasOwn"]` and `globalThis["Math"]["floor"]` forms. The parser now also canonicalizes transparent wrapper forms around computed object-property keys when they still resolve to static string/number names, including unary numeric wrappers and await/sequence wrapper chains; the computed-numeric object-key smoke now exercises that path across standalone run/test and browser harness/bundle contexts, including the await-wrapped numeric-key slice now covered in runtime and browser smoke. Standalone object-enumeration smoke now also covers frozen direct `Object.entries(...)` object-literal targets in JS, TS, JSX, and TSX input, keeping that helper family aligned with the existing frozen `Object.keys(...)` / `Object.values(...)` coverage. The browser-harness and JS-input `Reflect.ownKeys(...)` smoke now also covers the single-quoted `globalThis['Reflect']['ownKeys'](...)` alias on the supported frozen-object slice, the type-resolution regression suite now also mirrors the `Number.isSafeInteger` bracketed alias family in JS input, and `check` / `build` smoke now also covers the Number-predicate alias slice across Deno and browser surfaces in JS, TS, JSX, and TSX input. Await-wrapped static helper inputs now also resolve through the same checker/runtime smoke path for `Object.is`, `Number.isSafeInteger`, `Object.keys`, and `Reflect.ownKeys` on JS input, and the checker regression now also covers the same await-wrapped helper slice across JS, JSX, TS, and TSX inputs. The browser-targeted `Object.hasOwn` / `Object.prototype.hasOwnProperty.call` slice now also accepts const aliases that resolve to those callables in the browser-check/build and browser-harness/browser-bundle paths, and the same helper family now also recognizes the bracketed `Object["hasOwnProperty"].call` / `globalThis["Object"]["hasOwnProperty"].call` aliases plus the fully bracketed `Object["hasOwnProperty"]["call"]` / `globalThis["Object"]["hasOwnProperty"]["call"]` aliases on the supported static-object slice. The current repository snapshot also accepts const aliases that resolve to the supported `Object.is` and `Number.is*` callables on the standalone JS run/test path. The browser late-compat process-control corpus now also carries `process.kill(0)` invocation, static zero aliases, and wrapper forms in JS/TS/JSX/TSX rejection coverage so the negative evidence stays aligned with the Node zero-probe slice. The default-standalone late process-control rejection corpus now also carries the bracketed zero-probe `+0` aliases for `globalThis.process["kill"]` and `globalThis["process"]["kill"]`, keeping the standalone rejection matrix aligned with the Node zero-probe slice. Browser build smoke now also rejects the same `process.kill(0)` slice on the browser API surface in JS, TS, JSX, and TSX input. Diagnostic wording now matches that supported primitive/reference slice across the checker and codegen paths, and standalone JS runtime smoke now also mirrors the fully bracketed `globalThis["Object"]["prototype"]["hasOwnProperty"]["call"]` / `globalThis.Object["prototype"]["hasOwnProperty"]["call"]` alias pair on the same static helper slice. The `Math.pow` family now also carries direct build smoke for the fully bracketed `globalThis["Math"]["pow"]` alias chain on Deno and browser `.js` / `.ts` inputs, and the browser-targeted build matrix now also covers the same slice in JSX and TSX input.
- Keep non-literal dynamic import, broad reflective APIs, and eval-adjacent forms gated unless their spec rows are promoted.
- Pair each promotion with checker, lowering, runtime, browser/context, and JSON-output evidence where applicable.

- The freeze-wrapped helper smoke now also covers the mixed dot-root spellings `Object.freeze(globalThis.Object["hasOwn"])(object, "a")` and `Object.freeze(globalThis.Math["floor"])(1.6)` alongside the existing bracketed-root variants, and the regression corpus now also mirrors the bracketed `globalThis["Object"]["hasOwn"]` and `globalThis["Math"]["floor"]` freeze aliases in standalone, browser-harness, and browser-bundle coverage; codegen smoke now also covers the same transparent-wrapper helper aliases for `Object.hasOwn` and `Math.floor`, and the Math floor/trunc/ceil smoke now also carries the parenthesized freeze-wrapper variants plus the bare `Math["floor"]` / `Math["trunc"]` / `Math["ceil"]` spellings on the browser-harness, browser-bundle, runtime, and build paths, keeping the lowering path aligned with the supported static-object-helper slice.

### 21.4 Bounded TS/JS inference

- Grow inference inside deterministic budgets only.
- Preserve annotation-required boundaries for exported/public and cross-module surfaces when inference would exceed the bounded contract.
- Add positive and negative checker baselines for TS and first-class JS input.
- Library-export signature collection now also resolves function-binding alias chains within the bounded slice, including same-statement `const` aliases when the target signature is already known.
- Template-literal function returns now infer `string` signatures through the same bounded export-signature path, keeping library exports aligned with the actual expression type.
- Default-export aliases now resolve through the same bounded function-binding map used for local export aliases, and the regression corpus now also covers default-export alias chains, direct default re-export spellings, named re-exports into `default`, and multi-hop source-graph re-export chains, including the new default-export alias-chain source-graph regression. Transparent `Object.freeze(...)` wrappers around otherwise statically known callable exports now also flow through the same export-surface inference path, including the source-graph freeze-wrapped default-export alias chain and the matching freeze-wrapped const-binding alias chain. The source-graph freeze-wrapped default-export alias regression now also carries JS, JSX, and TSX inputs alongside TS so the bounded export-surface coverage stays aligned across source classes.

### 21.5 Conformance hygiene

- Maintain compact dashboards of supported vs gated semantics.
- Remove implementation-journal prose from plan files; exact coverage belongs in tests and maturity current-state notes.
- The package-audit preview shim now uses schema-v1 command-shape wording in its rejection diagnostic, and the CLI unit coverage now pins that exact message so the legacy flag stays aligned with the CLI and maturity matrix.
- The shared Number predicate smoke now also lives in `kali_common` as a canonical preamble/body helper pair, keeping the runtime and browser-requested smoke inputs single-sourced across the supported `Number.isFinite` / `Number.isInteger` / `Number.isSafeInteger` / `Number.isNaN` slice.
- The dedicated freeze-wrapped helper smoke now also composes the shared `Object.hasOwn` and `Math.floor` / `Math.trunc` / `Math.ceil` helpers directly, so the runtime and `Kali.test` fixtures now cover the full canonical alias inventory in one place.
- The shared late-object-model source for `Proxy` / `Weak*` / `FinalizationRegistry` now lives in `kali_common`, and the runtime smoke reuses it to keep that rejection corpus single-sourced; the broad `Intl` prefix now also lives in `kali_common` for the runtime smoke assembly, so the shared object-model corpus keeps one centralized Intl prefix while the browser fixtures retain their input-class-specific concatenation.
- The browser/runtime late `process.env` mutation fixture now reuses the shared `kali_common` source helper, keeping the bracketed Node aliases synchronized across smoke paths. The shared late-process-env mutation helper now also carries the bracketed `globalThis["process"].env["KALI_BROWSER_ENV_MUTATION"] = {}` assignment alias so the mutation corpus stays symmetric across the remaining bracketed spellings.
- `Array.from(...)` wrappers around already-supported iterable slices now also flow through the same bounded array-iteration path, keeping the supported iterable slice aligned across the checker, codegen, and runtime smoke, now with JS/TS/JSX/TSX `run` and `test` matrix coverage.
- The standalone JS late-compat fixture now also pulls the shared late-process-control helper into its source assembly so the zero-probe block stays single-sourced, and the shared late-process-control assembly now uses the dedicated parenthesized receiver-freeze source helper for the `Object.freeze((process)).kill` / `Object.freeze((globalThis.process)).kill` / `Object.freeze((globalThis["process"])).kill` dot spellings.
- The alias-resolution registry also dropped one duplicate `Object.hasOwnProperty.call` spelling so builds stay warning-free.
- The shared `Object.hasOwn` frozen-callable aliases now live in `kali_common` as a canonical helper/source pair, mirroring the `process.kill(0)` inventory pattern for future reuse.
- The shared late-compat `Object.hasOwn` / `Object.prototype.hasOwnProperty.call` block now also comes from a common helper in `kali_common`, and the JS late-compat removal path reuses that helper instead of matching the long inline block directly; the shared `Object.prototype.hasOwnProperty.call` frozen-callable aliases now also include the direct root spellings in `kali_common`, plus the bracketed `globalThis["Object"].hasOwnProperty.call` and `globalThis["Object"]["hasOwnProperty"]["call"]` spellings, so the browser harness/bundle and late-compat smoke can reuse one canonical helper across the globalThis and root forms. The helper inventory now also carries the bracketed `globalThis["Object"].prototype.hasOwnProperty["call"]` alias path so that member-access form stays single-sourced too.
- The shared `Math.floor` / `Math.trunc` / `Math.ceil` frozen-callable aliases now also live in `kali_common` as a canonical helper/source pair, including the bare `Math["floor"]` / `Math["trunc"]` / `Math["ceil"]` variants, keeping the browser math helper smoke aligned with the other shared alias inventories; the runtime smoke now also derives its Math.floor / Math.trunc / Math.ceil alias chain from that same inventory.
- The shared `Math.pow` direct and frozen-callable aliases now also live in `kali_common` as canonical helper/source pairs, and the browser math pow harness/bundle smoke now reuses the canonical source helper to derive the frozen call-target variants, including the bare `Math.pow` / `Math["pow"]` freeze-wrapper spellings, so the shared alias corpus stays single-sourced. The browser harness/bundle `Math.pow` alias smoke now also derives the direct invocation lines from the shared `math_pow_source()` helper, and the exponent-one/base-one identity bundle and harness sources now also derive those direct invocation lines from the shared alias inventory helper too.
- The shared `process.kill(0)` zero-probe inventory assembly now uses an explicit ordered unique union of the direct and wrapped families, while the duplicate-free invariant remains test-backed, and the canonical inventory source now composes the dedicated direct and wrapped source helpers.
- The shared `process.kill(0)` helper family now also exposes a dedicated parenthesized frozen-callable source helper, and the common helper test now asserts that helper source directly alongside the zero-probe inventory; the helper inventory now also carries the parenthesized bracketed frozen-callable aliases `Object.freeze((process["kill"]))` and `Object.freeze((globalThis.process["kill"]))` so the zero-probe matrix stays symmetrical across callable-member wrappers.
- The shared zero-probe helper also now carries the direct frozen `globalThis["process"].kill` aliases and the parenthesized receiver-freeze `globalThis.process` / `globalThis["process"]` spellings so the canonical inventory stays aligned with the browser/runtime smoke matrix.
- The node package-corpus zero-probe slice now also mirrors the receiver-freeze `process` spellings with `0` / `+0` aliases, keeping the corpus aligned with the shared Node smoke matrix.
- The wrapped zero-probe inventory test now also asserts duplicate-free aliases, the inventory builder now uses an explicit ordered unique union of the direct and wrapped families, and the late-compat browser JS/TS/TSX fixtures were deduplicated to match.
- The browser late-compat TSX fixture now also asserts the shared zero-probe inventory source exactly once, keeping the browser TSX rejection corpus aligned with the common helper.
- The browser late-compat JS/TS/JSX fixtures now also assert the shared zero-probe inventory source exactly once, keeping the embedded alias matrix aligned with the TSX corpus and the common helper.
- The browser TSX late-compat source now also drops one duplicate `Object.freeze(globalThis["process"]["kill"])(+0)` alias so the zero-probe inventory stays duplicate-free.
- The browser late-compat JS fixture now also carries the parenthesized receiver-freeze `globalThis.process["kill"]` / `globalThis["process"]["kill"]` aliases plus the parenthesized receiver-freeze dot spellings `Object.freeze((process)).kill`, `Object.freeze((globalThis.process)).kill`, and `Object.freeze((globalThis["process"])).kill`, along with the parenthesized bare-process freeze-wrapper `Object.freeze((process)["kill"])(0/+0)` spellings so its zero-probe matrix stays aligned with the shared inventory.
- The browser/runtime late-object-model revocable smoke now also carries the mixed frozen aliases `Object.freeze(globalThis.Proxy.revocable)` and `Object.freeze(globalThis["Proxy"].revocable)` so the broader Proxy.revocable corpus stays aligned with the shared late-object-model gate.
- The shared `process.kill(0)` zero-probe wording test now also covers the plain bracketed root aliases, the direct `process["kill"](+0)` / `process.kill(+0)` aliases, the direct `globalThis.process["kill"](+0)` / `globalThis["process"].kill(+0)` / `globalThis["process"]["kill"](0)` / `globalThis["process"]["kill"](+0)` forms, the wrapped `process["kill"]((0))` / `globalThis.process.kill((0))` / `globalThis["process"].kill((0))` forms, and parenthesized freeze-wrapper aliases around `globalThis.process["kill"]`, plus wrapped zero forms, keeping the canonical diagnostic inventory aligned with the supported Node matrix.
- The build-preflight process-control smoke now also exercises the wrapped argument aliases `process.kill((0))`, `process["kill"]((0))`, `globalThis.process.kill((0))`, `globalThis.process["kill"]((0))`, `globalThis["process"].kill((0))`, and `globalThis["process"]["kill"]((0))` on the documented Deno and browser build paths, keeping build rejection evidence aligned with the runtime corpus.
- The build-preflight process-control regression now also asserts the full shared zero-probe alias inventory end-to-end instead of a representative subset, so the canonical browser/Deno rejection wording stays tied to `kali_common`.
- Generator and async-generator lowering diagnostics now share `kali_common` helpers, including the mixed codegen path, and the CLI build path now routes through the shared helper directly to keep the E5506 wording aligned with codegen. The type checker now also coalesces mixed generator/async-generator function lowering into one canonical E5506 diagnostic so source-level rejection wording stays aligned with the mixed-flavor helper.
- The codegen `yield` / `yield*` / `delegate` gate now routes through the shared generator helper directly, keeping async-generator wording aligned with the current function flavor.
- `process.kill` zero-probe diagnostics now enumerate the bracketed `process["kill"](0)` / `process["kill"](+0)` forms, `Object.freeze(process.kill)(+0)`, the frozen `Object.freeze(globalThis.process.kill)(+0)` / `Object.freeze(globalThis.process["kill"])(0)` / `Object.freeze(globalThis.process["kill"])(+0)` / `Object.freeze(globalThis["process"].kill)(0)` / `Object.freeze(globalThis["process"].kill)(+0)` aliases, the frozen `Object.freeze(globalThis["process"]["kill"])(0)` alias, the dotted frozen `Object.freeze(globalThis.process)["kill"](0)` alias, the wrapped `((globalThis.process["kill"]))` alias pair, and the receiver-freeze `+0` aliases alongside the existing Node alias matrix so canonical failure wording stays aligned with the supported slice; `kali_common` now also pins the broader freeze/transparent-wrapper alias set in a focused unit test, including the receiver-freeze `Object.freeze(globalThis.process)["kill"](0)` / `Object.freeze(globalThis["process"])["kill"](0)` siblings, and the browser late-compat and Node API-surface regressions now also pin the parenthesized receiver-freeze spellings around `globalThis.process["kill"]` / `globalThis["process"]["kill"]` so those fixture corpora stay aligned with the shared helper, while the build-preflight host-control diagnostics now also name the fully bracketed `globalThis["process"]["kill"]` alias alongside the existing `globalThis.process["kill"]` / `globalThis["process"].kill` aliases.
- Node API-surface freeze coverage now also asserts `Object.freeze(globalThis["process"].kill)(0/+0)` in the generated `Kali.test` body, keeping the zero-probe alias matrix aligned with the runtime corpus. The Node API-surface positive regression now also covers the fully bracketed `Object.freeze(globalThis["process"]["kill"])(0/+0)` alias pair on the documented Node surface.
- The Node API-surface zero-probe regression now also exercises the receiver-freeze `Object.freeze(globalThis.process)["kill"](0/+0)` and `Object.freeze(globalThis["process"])["kill"](0/+0)` spellings, plus their parenthesized receiver-freeze siblings, so the documented Node matrix stays aligned with the shared helper inventory.
- The codegen regression now also exercises the receiver-freeze `Object.freeze(process)["kill"](+0)` / `Object.freeze(globalThis.process)["kill"](+0)` / `Object.freeze(globalThis["process"])["kill"](+0)` siblings so the lowering smoke stays aligned with the shared zero-probe wording matrix.
- Type-resolution coverage for the Node zero-probe slice now also includes the receiver-freeze `Object.freeze(globalThis.process)["kill"](+0)` and `Object.freeze(globalThis["process"])["kill"](+0)` siblings on the documented Node surface.
- Browser bundle/harness coverage now also includes the fully bracketed `globalThis.Object.prototype.hasOwnProperty["call"]` spelling on the supported `Object.hasOwn` slice, and the current smoke now also mirrors the bracketed freeze aliases for `globalThis["Object"]["hasOwn"]` and `globalThis["Math"]["floor"]` on the same helper paths.
- Standalone JS smoke now also exercises frozen `Object.hasOwn` and `Math.floor` callable aliases via `Object.freeze(globalThis.Object["hasOwn"])` / `Object.freeze(globalThis["Object"]["hasOwn"])` and `Object.freeze(globalThis.Math["floor"])` / `Object.freeze(globalThis["Math"]["floor"])` in the dedicated helper regression sources.
- Browser-requested `run` / `test` browser-harness smoke now also accepts inherited browser `runtimeProfiles=["wasm-threads"]` configs in JS input alongside positive `--max-threads` requests, keeping the browser/runtime threading evidence explicit.

- The shared process.kill(0) zero-probe wording helper now also includes `globalThis.process.kill` directly in the TS-wrapped alias inventory, keeping the wrapper matrix aligned with the direct member-access form.
- The helper inventory now also includes the parenthesized bracketed freeze-wrapper aliases `Object.freeze((globalThis["process"]["kill"]))(0/+0)` plus the wrapped `((process["kill"]))` / `((globalThis.process["kill"]))` / `((globalThis["process"].kill))` / `((globalThis["process"]["kill"]))` family so the canonical wording stays aligned with the widened zero-probe matrix, and the shared zero-probe wording inventory was deduplicated to keep the canonical message stable.
- The focused `kali_common` helper test now also pins the bracketed wrapped `Object.freeze((globalThis["process"]["kill"]))` aliases, the parenthesized freeze-wrapper siblings `Object.freeze((globalThis.process["kill"]))` / `Object.freeze((globalThis["process"]["kill"]))`, the receiver-freeze siblings `Object.freeze(globalThis.process)["kill"](0/+0)` and `Object.freeze(globalThis["process"])["kill"](0/+0)`, and the new parenthesized bare-process freeze-wrapper siblings `Object.freeze((process)["kill"])(0/+0)`, keeping the canonical zero-probe inventory covered end-to-end.
- The runtime/browser/Node smoke now also mirrors the parenthesized receiver-freeze aliases `Object.freeze((process))["kill"]`, `Object.freeze((globalThis.process))["kill"]`, and `Object.freeze((globalThis["process"]))["kill"]` so the shared zero-probe inventory and fixture coverage stay in lockstep.
- The Node process-corpus regression now checks the shared zero-probe alias inventory end-to-end as well, so the corpus stays aligned with the common helper instead of relying on a small representative subset, and the corpus body keeps the `const zero = 0; const zeroAlias = zero;` preamble so the static zero-alias probe remains explicit in the package-corpus fixture.
- The shared zero-probe inventory is now split into direct and wrapped alias helpers so the canonical message can be checked against each alias family without re-copying the full matrix, and the combined helper now keeps the full inventory as a unique ordered union of those families. The late-process-control source now embeds that zero-probe inventory exactly once, so the preamble no longer duplicates the shared invocation slice. The preamble itself now comes from a dedicated helper, keeping the append-only inventory assembly isolated, and the preamble helper is now reusable from other crates for smoke assertions.
- The library-export proof regression now also covers `--capi` and `--component` selectors on the same no-static-export source path as `--lib`, keeping the `E5511` rejection evidence aligned across the whole exported-library selector family.
- The canonical `process.kill(0)` zero-probe wording helper is now aligned with the deduplicated inventory used by the shared helper, codegen, and smoke fixtures, and the helper text is now generated from that shared alias inventory; the wrapped inventory now also includes the direct `Object.freeze(process.kill)` / `Object.freeze(globalThis.process.kill)` forms plus the receiver-freeze `Object.freeze(globalThis.process)["kill"]` / `Object.freeze(globalThis["process"])["kill"]` siblings, the standalone runtime smoke late-process-control source now derives its zero-probe segment from the same shared inventory, the plain JS late-compat source now also carries the wrapped `process["kill"]((0))` / `globalThis.process["kill"]((0))` / `globalThis["process"].kill((0))` spellings, and the common crate now also exposes a canonical alias-inventory source helper used by the late-process-control assembly; that inventory source now composes the dedicated direct and wrapped source helpers, keeping the late-process-control assembly aligned with the shared zero-probe inventory, the browser late-compat JS/TS/JSX/TSX fixtures now mirror the same trimmed alias inventory, and the browser late-compat source coverage now also explicitly pins the mixed `globalThis.process["kill"]((0))` wrapped alias plus the parenthesized transparent freeze wrapper `Object.freeze((globalThis.process.kill))(0/+0)`; the runtime smoke and browser TSX fixture now also include the parenthesized callable-freeze `Object.freeze((process["kill"]))(0/+0)` pair. The alias-inventory source now derives directly from the deduplicated alias list, so the shared wording source and the diagnostics stay tied to the same ordered unique union. The wrapped inventory now also carries parenthesized receiver-freeze spellings around bare `process` and `globalThis.process` receivers, and the late-process-control assembly keeps the dedicated dot-receiver freeze helper spliced in separately for `Object.freeze((process).kill)(0/+0)`, keeping the zero-probe family aligned with the current browser/runtime corpus.
- The shared late-process-control helper now also carries the transparent parenthesized `((process)).kill(+0)` / `((globalThis.process)).kill(+0)` aliases so the helper-backed browser/runtime smoke stays aligned with the zero-probe wrapper matrix.
- The shared zero-probe helper now also exposes a dedicated parenthesized receiver-freeze alias inventory, and the common helper regression checks that list directly to keep the dot-receiver wrapper slice single-sourced.
- The shared zero-probe inventory now also carries the doubly parenthesized `((globalThis.process.kill))(+0)` sibling, keeping the Node/browser smoke and canonical helper inventory aligned on the `+0` wrapper case as well.
- The late-process-control prefix now also carries the parenthesized bracketed `globalThis["process"]` receiver-freeze aliases, keeping that wrapper family explicit in the same zero-probe smoke path.

- The sequence-callable-target binding helper now also carries the fully bracketed `globalThis["process"]["kill"]` binding pair and the bracketed-dot `globalThis["process"].kill` pair, keeping the Node surface alias matrix aligned with the shared helper source.

## Exit gate

- Each promoted semantic slice has parser/checker/lowering/runtime evidence.
- Unsupported but recognized forms fail through the canonical diagnostic path.
- Maturity and current-state docs are updated only for evidence-backed availability changes.
