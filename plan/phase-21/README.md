# Phase 21 — Semantic Completeness and Conformance

## Goal

Close remaining language gaps by either implementing faithful semantics with evidence or preserving explicit, tested availability gates.

## Owning specs

- `specs/02-lexer-parser.md`
- `specs/03-ast.md`
- `specs/04-type-system.md`
- `specs/05-ir.md`
- `specs/10-runtime.md`
- `specs/16-testing.md`
- `specs/19-feature-maturity.md`

## Work packets

### 21.1 Generators and async generators

- Implement resumable generator and async-generator state-machine lowering.
- Cover `yield`, `yield*`, `return`, `throw`, `try/finally`, iterator close/finalization, and async interaction.
- Preserve generator/function-kind metadata through parser, AST, HIR, MIR, LIR, codegen, and export analysis.
- Async class methods now ride the shared async-function lowering path; generator class methods remain gated, and the direct runtime-entrypoint gate now also covers async class expressions in JSX/TSX input, generator class expressions in JS/TS/JSX/TSX input, plus async-generator default-export class expressions in JS/TS/JSX/TSX input. Generator codegen plus the type-resolution, CLI build-preflight, and class-method runtime gates now emit distinct generator vs async-generator wording while preserving the same canonical E5506 path for function declarations and expressions, and runtime smoke now also covers those same class-expression gates for direct `run` / `test` JSON and text output, including the TS async default-export class-expression slice. Direct `run` / `test` generator-function rejection smoke now also covers TS input alongside the existing JS/JSX/TSX coverage.
- Current coverage also exercises async class methods through build smoke in TS, JS, JSX, and TSX input on both the Deno and browser artifact paths.
- The parser now accepts class-expression forms with preserved async/generator metadata, and direct `run` / `test` entrypoints now gate those async class expressions too; generator class expressions now also hit the direct runtime E5506 gate before lowering, including the direct runtime JS/TS coverage path. The browser API surface smoke now mirrors generator class-expression and async-generator default-export class-expression rejection across JS, TS, JSX, and TSX input. The parser's computed object-property-name canonicalization now also accepts `await` wrappers when they still resolve to static string/number names.
- The Node `process.kill(0)` liveness-probe slice now also keeps transparent parenthesized and unary-plus wrapper forms covered alongside the bracketed Node aliases across JS and TS smoke input, the Node package-corpus evidence now also mirrors the bracketed and `globalThis` alias forms, plus the zero-probe freeze aliases and fully bracketed wrapped kill alias, and the dedicated Node API-surface support regression now mirrors that same wrapper/alias matrix in its `run` and `test` inputs, including wrapped `process` and wrapped `process.kill` call-target forms plus const aliases and sequence-callable-target forms of the callable target itself, including the `globalThis.process["kill"]` const/sequence alias pair added in the current Node smoke, TS/TSX satisfies-wrapped zero literals on the checker and CLI surfaces and bracketed `globalThis.process["kill"]` / `globalThis["process"].kill(0)` / `globalThis["process"]["kill"]` / `globalThis.process["kill"](0)` / `globalThis["process"]["kill"](0)` zero-probe spellings. Static aliases of the zero-probe argument now also feed that slice on the Node surface, and the zero-probe wrapper regression now also covers `(0 as number)` assertions in TS input, while the type-resolution regression set now also mirrors that Node zero-probe matrix directly on the checker path and now also pins the exact `process["kill"](0)` / `globalThis.process["kill"](0)` / `globalThis["process"]["kill"](0)` call-target variants, including the `globalThis["process"].kill(0)` / `globalThis.process["kill"](0)` / `((globalThis["process"].kill))(0)` / `((globalThis.process["kill"]))(0)` alias spellings, and the codegen regression set now also exercises the direct bracketed `globalThis["process"]["kill"](+0)` call-target variant on the same supported zero-probe path. The JS parser-backed regression now also keeps the bracketed `globalThis.process["kill"](0)` and `globalThis["process"].kill(0)` spellings in the same supported slice. The current Node smoke now also includes wrapped `globalThis.process.kill` and wrapped bracketed `globalThis.process["kill"]` call-target forms, and the browser late-compat smoke now mirrors that wrapped alias in JS, TS, JSX, and TSX input, including the wrapped `((globalThis.process.kill))(0)`, `((globalThis.process["kill"]))(0)`, `((globalThis["process"].kill))(0)`, and `((globalThis["process"]["kill"]))(0)` zero-probe aliases. The current packet also now covers parenthesized transparent freeze wrappers like `Object.freeze((process.kill))(0)` / `(+0)` across the Node and browser smoke matrices. The standalone JS late-compat corpus now also carries the fully bracketed `((globalThis["process"]["kill"]))(0)` alias, the transparent `Object.freeze(globalThis.process["kill"])(0)` / `Object.freeze(globalThis.process["kill"])(+0)` alias and the direct `Object.freeze(globalThis.process.kill)(0)` / `Object.freeze(globalThis.process.kill)(+0)` alias, the receiver-freeze aliases and their +0 siblings `Object.freeze(process)["kill"](0)` / `Object.freeze(globalThis.process)["kill"](0)` / `Object.freeze(globalThis["process"])["kill"](0)`, the mixed dot/bracket `Object.freeze(globalThis["process"].kill)(0)` alias, and the bracketed transparent `Object.freeze(globalThis["process"]["kill"])(0)` alias, and the browser/runtime late-compat smoke matrices now also include the bracketed `((process["kill"]))(0)` wrapper form, and the browser late-compat process-control matrix now also includes the parenthesized zero form under the bracketed `globalThis["process"]["kill"]` alias and the bracketed `process["kill"](+0)` spelling on the same browser-input matrix, while the Node API-surface regression now covers the exact bracketed `globalThis["process"]["kill"](0)` call target and the fully bracketed wrapped `((globalThis["process"]["kill"]))(0)` alias. The codegen regression set now also pins the direct `globalThis["process"].kill(0)` alias form on the same supported zero-probe path, the build-preflight rejection helper source now also includes the browser-safe receiver-freeze aliases and their +0 siblings `Object.freeze(globalThis.process)["kill"](0)` / `Object.freeze(globalThis["process"])["kill"](0)`, and the Node zero-probe support regression now also covers the transparent `Object.freeze(globalThis.process["kill"])(0)` / `Object.freeze(globalThis.process["kill"])(+0)` alias alongside the existing wrapper and const-alias cases, and the bracketed `Object.freeze(globalThis["process"]["kill"])(0)` alias in the same matrix, including the mixed dot/bracket frozen alias `Object.freeze(globalThis["process"].kill)(0)` in the current smoke set. The browser late-compatibility process-control matrix now also includes the transparent `Object.freeze(globalThis.process["kill"])(0)` / `Object.freeze(globalThis.process["kill"])(+0)` alias, the mixed dot/bracket `Object.freeze(globalThis["process"].kill)(0)` alias, and the bracketed `Object.freeze(globalThis["process"]["kill"])(0)` alias on the same browser rejection path, and the unsupported-path diagnostics now also name the exact bracketed aliases so the negative evidence stays aligned with the supported alias matrix. The supported zero-probe matrix now also includes the receiver-freeze sibling `Object.freeze(process)["kill"](+0)` on the Node/runtime/browser-covered paths, plus the direct bracketed `process["kill"](+0)` probe on the standalone runtime and Node API-surface smoke paths.
- The Node zero-probe smoke now also covers the fully bracketed `Object.freeze(globalThis["process"]["kill"])(0)` / `(+0)` aliases in the runtime and Node API-surface matrices.
- The default-standalone late process-control rejection corpus now also carries `process.kill(0)`, wrapper spellings, and receiver-freeze aliases and their +0 siblings so the unsupported-path evidence stays aligned with the Node zero-probe slice.
- The codegen regression set keeps the transparent-wrapper `process.kill(0)` coverage and now also pins transparent type-wrapper variants so the packet stays buildable while preserving the same zero-probe coverage. The codegen and type-checker rejection wording now also enumerates the transparent `Object.freeze(globalThis.process["kill"])(0)` / `Object.freeze(globalThis.process["kill"])(+0)` and `Object.freeze(globalThis["process"].kill)(0)` / `Object.freeze(globalThis["process"].kill)(+0)` aliases so the canonical failure text stays aligned with the supported alias matrix, and the shared zero-probe wording now comes from one helper so those diagnostics stay in sync.
- HIR, MIR, LIR, and function-plan tests now also preserve class-expression generator metadata alongside the existing class-method coverage.
- Direct `run` / `test` execution for async class methods now has an explicit E5506 gate; keep generator-class lowering gated until the dedicated packet lands.
- Keep all unsupported forms behind canonical `E5506` gates until the full runtime path exists.
- Browser late-compat and Node surface smoke now also pin the mixed frozen `Object.freeze(globalThis["process"].kill)(0)` alias and its `+0` zero-literal sibling, plus direct `globalThis.process["kill"](+0)` / `globalThis["process"].kill(+0)` / `globalThis["process"]["kill"](+0)` zero-probe aliases; the browser late-compat source corpus now also asserts the direct `globalThis.process["kill"](+0)` spelling across JS, TS, JSX, and TSX input, and the same browser matrix now also covers the doubly wrapped `((globalThis["process"]["kill"]))(+0)` alias with explicit regression assertions. The browser late-compat rejection fixtures now also cover the parenthesized bracketed freeze-wrapper alias family in the browser corpus.

### 21.2 Iterator and async-iterator protocols

- Expand `for...of`, `for await...of`, spreads, and iterable consumption beyond current bounded static slices; current smoke now also covers the direct string-concatenation slice in browser bundle JS, TS, JSX, and TSX input, standalone TS run smoke for the `for await...of` string-concatenation slice, browser-bundle/browser-harness smoke for the `for...of` template-literal slice over statically known string operands in JS, TS, JSX, and TSX input, standalone run/test smoke for that template-literal slice in TS input with JSON-output coverage, standalone run/test smoke for the same template-literal slice in JS input with JSON-output coverage, browser-requested run/test browser-harness smoke now also covers that same string-concatenation slice in JS, TS, JSX, and TSX input with JSON-output coverage, browser-bundle smoke now also covers the sequence-expression wrappers around the supported string-primitive `Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` slice in JS, TS, JSX, and TSX input, and the `for await` array slice now also accepts a transparent `await` wrapper over a literal array in the standalone JS run path, while browser-harness smoke now also covers `for...of` / `for await...of` array break/continue on the supported array-iteration path in JS, TS, JSX, and TSX input with JSON-output coverage.
- Implement protocol lookup, `next` result handling, abrupt completion, iterator close, and async iterator finalization.
- Current smoke now also covers the single-quoted bracketed `Reflect` alias over frozen object literals in the direct and browser-harness paths, plus sequence-expression wrappers around the supported static `Reflect.ownKeys(...)` slice in JS input, and browser-requested run/test coverage now also exercises the frozen-object `Reflect.ownKeys(...)` slice in JS, TS, JSX, and TSX input with JSON-output coverage, alongside the browser-bundle string-concatenation smoke in JS, TS, JSX, and TSX input and the matching browser-harness string-concatenation smoke in the same input matrix. The type checker now also accepts the bracketed `globalThis["Reflect"]["ownKeys"]` alias spelling on that supported `Reflect.ownKeys(...)` slice, and codegen smoke now also covers the mixed `globalThis["Reflect"].ownKeys` alias spelling there. The browser-harness `Object.values([...Object.fromEntries(...)])` spread smoke now also covers frozen operands in JS, TS, JSX, and TSX input with JSON-output coverage, and it now also exercises the bracketed `globalThis["Object"]["fromEntries"]` alias alongside the existing spread matrix so the alias path stays aligned with the bundle coverage. Standalone run/test smoke now also covers the `for await` spread-of-`Object.values` / `Object.keys` / `Object.entries` slice over `Object.fromEntries(...)` operands in JS and TS input with JSON-output coverage. Static `new Set(...)` constructor slices now also type-check, build, and execute in standalone run/test smoke over the supported iterable inputs, including `Object.freeze`-wrapped array operands, and static `new Map(...)` constructor slices now also share that supported path. Browser bundle/browser-harness smoke now also covers frozen constructor aliases such as `Object.freeze(Set)` / `Object.freeze(Map)` alongside the static `new Set(...)` constructor slices across JS, TS, JSX, and TSX input, plus the static `new Map(...)` constructor slices over supported entry tuples across the same matrix, while alias-chain and transparent-wrapper coverage now also pins `Set`/`Map` constructor references through the same bounded root-resolution path, including const-alias and parenthesized-alias `Set`/`Map` constructor coverage in runtime, browser-harness, codegen, and type-resolution smoke, plus frozen constructor aliases such as `Object.freeze(Set)` / `Object.freeze(Map)` in the codegen/runtime smoke path, and the current repository snapshot now also covers the dotted `globalThis.Set` / `globalThis.Map` spellings alongside the bracketed `globalThis["Set"]` / `globalThis["Map"]` constructor spellings in that same supported slice. Codegen smoke now also pins the exact bracketed `globalThis["Set"]` / `globalThis['Map']` root spellings on the same Set/Map slice, and the checker/build regression corpus now also pins the bracketed `globalThis["Set"]` and `globalThis['Map']` constructor spellings on the shared supported Set/Map slice. Runtime smoke now also carries focused JS-input Set/Map constructor regressions for the supported standalone and browser-requested run/test paths, including the frozen-array wrapper form, and the browser-harness lane now also has a dedicated `Map` constructor iteration fixture mirroring the Set harness matrix in JS, TS, JSX, and TSX input; the current smoke now also exercises abrupt return/finally and throw/finally sequencing on the supported Set/Map constructor slice across the existing run/test, browser-harness, and browser-bundle coverage, and the object-enumeration break/continue slice now also has standalone, browser-harness, and browser-bundle smoke on the supported `Object.keys(...)` iteration path.
- Add conformance fixtures for supported built-ins and negative diagnostics for unimplemented protocol edges; the current iterator corpus now also pins call-expression source edges such as `new Set(values.filter(Boolean))` and the parallel `new Map(values.filter(Boolean))` rejection, now covered across JS, TS, JSX, TSX, and inherited browser-config rejection paths, to keep the literal-array gate explicit.

### 21.3 Dynamic language and built-in semantics

- Widen object-model, Math, BigInt, dynamic import, reflection, and operator semantics only when observable JavaScript behavior is pinned; current coverage also includes standalone `run` / `test` smoke for the `Number.isFinite` / `Number.isNaN` / `Number.isInteger` / `Number.isSafeInteger` primitive-value slice across JS and TS input with JSON-output coverage, plus browser-bundle smoke for that same slice across JS, TS, JSX, and TSX input, including mixed bracket/dot spellings such as `globalThis["Number"].isNaN`, and the fully bracketed `globalThis["Math"]["sqrt"]` / `globalThis["Math"]["cbrt"]` math slice now also covers JS, TS, JSX, and TSX input on the browser bundle/harness paths. Promise.allSettled now also has standalone build/runtime, browser-requested browser-harness, and browser-bundle coverage across JS, TS, JSX, and TSX input, and the smoke now also covers a freeze-wrapped bracketed callable alias on the same supported slice. Nullish coalescing smoke now also covers `void 0 ??` and `undefined ??` fallback operands on the standalone run/test paths and the browser-requested browser-harness paths in JS input. The same object-identity slice now also has type-system regression coverage for the `Object.is` alias spellings used by the browser/runtime smoke paths, including parser-backed bracketed `Object.is` and `Number.is*` alias spellings in JS input, and browser-harness `Object.is` same-reference alias-chain smoke now also exercises the bracketed, mixed, root, and dot alias spellings alongside the frozen-object variant; dedicated browser-harness alias-chain smoke now also covers object/array same-reference, frozen-reference, and fresh-literal comparisons across JS, TS, JSX, and TSX input, with browser-requested run/test JSON smoke now mirroring the object/array/frozen-reference matrix across that same input set, and the browser-requested browser-harness/browser-bundle smoke now also accepts const aliases that resolve to the supported `Object.is` and `Number.is*` callables. That same object-identity slice now also accepts no-substitution template literals on the supported primitive/reference path. The browser-requested `Math.atan2` smoke now also covers the single-quoted bracketed zero slice in JS, TS, JSX, and TSX input with JSON-output coverage, and browser-harness/browser-bundle smoke now also covers the same single-quoted bracketed zero slice in TS, `.js`, JSX, and TSX input; the unsupported-math browser-harness rejection smoke now also covers the bracketed `Math["sqrt"]` / `Math["atan2"]` and `globalThis["Math"].sqrt` / `globalThis["Math"].atan2` spellings in JSX and TSX input, and the browser `Math.round` smoke now also includes the fully bracketed `globalThis["Math"]["round"]` spelling on the bundle and harness paths, plus the transparent `Object.freeze(globalThis["Math"]["round"])` callable alias on the same supported slice. The browser-harness `Math.pow` smoke now also covers `Object.freeze(globalThis["Math"]["pow"])` on the same supported alias path. The browser-harness `Math.atan2` trailing-argument evaluation smoke now also covers TS, JSX, and TSX input with JSON-output coverage, and the browser-bundle smoke now also covers that same trailing-argument evaluation slice in JS, TS, JSX, and TSX input with JSON-output coverage. The Node `process.kill(0)` liveness-probe slice is now also available on the documented Node surface across `check` / `build` / `run` / `test`, while broader kill/signal semantics remain gated, and the current Node smoke now also pins the `globalThis.process["kill"](0)` and `globalThis["process"].kill(0)` aliases. Wrapped call targets with type assertions or `satisfies` expressions now resolve for direct callable references; `Object.freeze(...)` now also behaves as a transparent callable wrapper for the supported Node zero-probe slice, so frozen `process.kill` references stay on the same supported path. The same transparent-wrapper path now also accepts `Object.freeze`-wrapped `Object.hasOwn` and `Math.floor` helper calls in standalone JS smoke. The parser now also canonicalizes transparent wrapper forms around computed object-property keys when they still resolve to static string/number names, including unary numeric wrappers, and the computed-numeric object-key smoke now exercises that path across browser-harness and browser-bundle contexts, the browser-harness and JS-input `Reflect.ownKeys(...)` smoke now also covers the single-quoted `globalThis['Reflect']['ownKeys'](...)` alias on the supported frozen-object slice, the type-resolution regression suite now also mirrors the `Number.isSafeInteger` bracketed alias family in JS input, and `check` / `build` smoke now also covers the Number-predicate alias slice across Deno and browser surfaces in JS, TS, JSX, and TSX input. The browser-targeted `Object.hasOwn` / `Object.prototype.hasOwnProperty.call` slice now also accepts const aliases that resolve to those callables in the browser-check/build and browser-harness/browser-bundle paths, and the same helper family now also recognizes the bracketed `Object["hasOwnProperty"].call` / `globalThis["Object"]["hasOwnProperty"].call` aliases plus the fully bracketed `Object["hasOwnProperty"]["call"]` / `globalThis["Object"]["hasOwnProperty"]["call"]` aliases on the supported static-object slice. The current repository snapshot also accepts const aliases that resolve to the supported `Object.is` and `Number.is*` callables on the standalone JS run/test path. The browser late-compat process-control corpus now also carries `process.kill(0)` invocation, static zero aliases, and wrapper forms in JS/TS/JSX/TSX rejection coverage so the negative evidence stays aligned with the Node zero-probe slice. The default-standalone late process-control rejection corpus now also carries the bracketed zero-probe `+0` aliases for `globalThis.process["kill"]` and `globalThis["process"]["kill"]`, keeping the standalone rejection matrix aligned with the Node zero-probe slice. Browser build smoke now also rejects the same `process.kill(0)` slice on the browser API surface in JS, TS, JSX, and TSX input. Diagnostic wording now matches that supported primitive/reference slice across the checker and codegen paths, and standalone JS runtime smoke now also mirrors the fully bracketed `globalThis["Object"]["prototype"]["hasOwnProperty"]["call"]` / `globalThis.Object["prototype"]["hasOwnProperty"]["call"]` alias pair on the same static helper slice. The `Math.pow` family now also carries direct build smoke for the fully bracketed `globalThis["Math"]["pow"]` alias chain on Deno and browser `.js` / `.ts` inputs.
- Keep non-literal dynamic import, broad reflective APIs, and eval-adjacent forms gated unless their spec rows are promoted.
- Pair each promotion with checker, lowering, runtime, browser/context, and JSON-output evidence where applicable.

### 21.4 Bounded TS/JS inference

- Grow inference inside deterministic budgets only.
- Preserve annotation-required boundaries for exported/public and cross-module surfaces when inference would exceed the bounded contract.
- Add positive and negative checker baselines for TS and first-class JS input.
- Library-export signature collection now also resolves function-binding alias chains within the bounded slice, including same-statement `const` aliases when the target signature is already known.
- Template-literal function returns now infer `string` signatures through the same bounded export-signature path, keeping library exports aligned with the actual expression type.
- Default-export aliases now resolve through the same bounded function-binding map used for local export aliases, and the regression corpus now also covers default-export alias chains and multi-hop source-graph re-export chains, including the new default-export alias-chain source-graph regression.

### 21.5 Conformance hygiene

- Maintain compact dashboards of supported vs gated semantics.
- Remove implementation-journal prose from plan files; exact coverage belongs in tests and maturity current-state notes.
- The alias-resolution registry also dropped one duplicate `Object.hasOwnProperty.call` spelling so builds stay warning-free.
- The shared `process.kill(0)` zero-probe wording test now also covers the plain bracketed root aliases, the direct `process["kill"](+0)` / `process.kill(+0)` aliases, the direct `globalThis.process["kill"](+0)` / `globalThis["process"].kill(+0)` / `globalThis["process"]["kill"](0)` / `globalThis["process"]["kill"](+0)` forms, the wrapped `globalThis.process.kill((0))` / `globalThis["process"].kill((0))` forms, and parenthesized freeze-wrapper aliases around `globalThis.process["kill"]`, plus wrapped zero forms, keeping the canonical diagnostic inventory aligned with the supported Node matrix.
- Generator and async-generator lowering diagnostics now share `kali_common` helpers, and the CLI build path now routes through the shared helper directly to keep the E5506 wording aligned with codegen.
- `process.kill` zero-probe diagnostics now enumerate the bracketed `process["kill"](0)` / `process["kill"](+0)` forms, `Object.freeze(process.kill)(+0)`, the frozen `Object.freeze(globalThis.process.kill)(+0)` / `Object.freeze(globalThis.process["kill"])(0)` / `Object.freeze(globalThis.process["kill"])(+0)` / `Object.freeze(globalThis["process"].kill)(0)` / `Object.freeze(globalThis["process"].kill)(+0)` aliases, the frozen `Object.freeze(globalThis["process"]["kill"])(0)` alias, the dotted frozen `Object.freeze(globalThis.process)["kill"](0)` alias, the wrapped `((globalThis.process["kill"]))` alias pair, and the receiver-freeze `+0` aliases alongside the existing Node alias matrix so canonical failure wording stays aligned with the supported slice; `kali_common` now also pins the broader freeze/transparent-wrapper alias set in a focused unit test, including the receiver-freeze `Object.freeze(globalThis.process)["kill"](0)` / `Object.freeze(globalThis["process"])["kill"](0)` siblings.
- Node API-surface freeze coverage now also asserts `Object.freeze(globalThis["process"].kill)(0/+0)` in the generated `Kali.test` body, keeping the zero-probe alias matrix aligned with the runtime corpus. The Node API-surface positive regression now also covers the fully bracketed `Object.freeze(globalThis["process"]["kill"])(0/+0)` alias pair on the documented Node surface.
- The codegen regression now also exercises the receiver-freeze `Object.freeze(process)["kill"](+0)` / `Object.freeze(globalThis.process)["kill"](+0)` / `Object.freeze(globalThis["process"])["kill"](+0)` siblings so the lowering smoke stays aligned with the shared zero-probe wording matrix.
- Type-resolution coverage for the Node zero-probe slice now also includes the receiver-freeze `Object.freeze(globalThis.process)["kill"](+0)` and `Object.freeze(globalThis["process"])["kill"](+0)` siblings on the documented Node surface.
- Browser bundle/harness coverage now also includes the fully bracketed `globalThis.Object.prototype.hasOwnProperty["call"]` spelling on the supported `Object.hasOwn` slice.
- Browser-requested `run` / `test` browser-harness smoke now also accepts inherited browser `runtimeProfiles=["wasm-threads"]` configs in JS input alongside positive `--max-threads` requests, keeping the browser/runtime threading evidence explicit.

- The shared process.kill(0) zero-probe wording helper now also covers the direct `process.kill(0)` / `process.kill(+0)` call-targets, the direct `globalThis.process.kill` and bracketed `globalThis.process["kill"]` / `globalThis["process"].kill` alias pairs, plus the bracketed `((process["kill"]))` alias pair and the mixed frozen `Object.freeze(globalThis["process"].kill)` and wrapped `((globalThis["process"].kill))` aliases in the canonical rejection wording; the focused helper test also now pins the receiver-freeze siblings `Object.freeze(process)["kill"]`, `Object.freeze(globalThis.process)["kill"]`, and `Object.freeze(globalThis["process"])["kill"]`, while the Node API-surface/browser-compat smoke also pins the wrapped `((globalThis.process["kill"]))(+0)` alias and the browser late-compat smoke now also carries the wrapped `((process["kill"]))(+0)` sibling alongside the existing zero-probe matrix, and the canonical wording inventory now also includes the bracketed `process["kill"]((0))`, `globalThis.process["kill"]((0))`, and `globalThis["process"]["kill"]((0))` wrapper aliases.
- The helper inventory now also includes the parenthesized bracketed freeze-wrapper aliases `Object.freeze((globalThis["process"]["kill"]))(0/+0)` and the matching wrapped `((globalThis["process"]["kill"]))(0)` alias so the canonical wording stays aligned with the widened zero-probe matrix, and the shared zero-probe wording inventory was deduplicated to keep the canonical message stable.
- The focused `kali_common` helper test now also pins the bracketed wrapped `Object.freeze((globalThis["process"]["kill"]))` aliases, keeping the canonical zero-probe inventory covered end-to-end.
- The canonical `process.kill(0)` zero-probe wording helper is now aligned with the deduplicated inventory used by the shared helper, codegen, and smoke fixtures, including the wrapped `globalThis.process["kill"]((0))` and `globalThis["process"]["kill"]((0))` forms, and the browser late-compat JS/TS/JSX/TSX fixtures now mirror the same trimmed alias inventory.

## Exit gate

- Each promoted semantic slice has parser/checker/lowering/runtime evidence.
- Unsupported but recognized forms fail through the canonical diagnostic path.
- Maturity and current-state docs are updated only for evidence-backed availability changes.
