# Phase 21 — Semantic Completeness and Conformance

## Goal

Close remaining language gaps by either implementing faithful semantics with evidence or preserving explicit, tested availability gates.

Keep this file at the sequencing level. Exact coverage belongs in tests, maturity/current-state notes, schemas, and proof files.

## Owning specs

- `specs/02-lexer-parser.md`
- `specs/03-ast.md`
- `specs/04-type-system.md`
- `specs/05-ir.md`
- `specs/10-runtime.md`
- `specs/16-testing.md`
- `specs/19-feature-maturity.md`

## Work packets

### 21.1 Generators and async generators

- Implement resumable generator and async-generator state-machine lowering.
- Cover `yield`, `yield*`, `return`, `throw`, `try/finally`, iterator close/finalization, and async interaction.
- Preserve generator/function-kind metadata through parser, AST, HIR, MIR, LIR, codegen, and export analysis.
- Keep unsupported generator forms behind canonical `E5506` gates until the full runtime path exists.
- Current smoke now also exercises sequence-expression-wrapped generator and async-generator class expressions on the supported run/check/build paths, including the yield* delegation slice; the direct run/test smoke now also mirrors that sequence-wrapped class-expression rejection across JS, TS, JSX, and TSX input, and the browser-requested JS, TS, JSX, and TSX harness lanes now mirror that same sequence-wrapped generator/async-generator rejection on the yield* slice. Library-export collection now also keeps generator and async-generator default-export expressions rejected through transparent wrappers such as parentheses and sequence expressions, and browser-harness `check`/`run`/`test`/`build` now also rejects sequence-wrapped anonymous default-export generator and async-generator function declarations across JS, TS, JSX, and TSX input, plus sequence-wrapped default-export generator and async-generator class expressions on the same browser-harness matrix, and the browser-harness class-expression coverage now also pins the non-delegating `yield 1` variant alongside the `yield*` slice so the direct generic class-method gate stays aligned across wrappers.
- Browser check/build smoke now also rejects anonymous default-export generator and async-generator function declarations across JS, TS, JSX, and TSX inputs on the supported browser harness path, and the JS browser check/build lane now also mirrors the sequence-wrapped generator/async-generator class-expression yield* delegation slice. Browser-targeted check/build smoke now also rejects generator and async-generator function declarations across JS, TS, JSX, and TSX input, and the browser-requested run/test harness lanes now mirror that same function-declaration gate across the same matrix. The browser harness now also rejects default-export generator and async-generator class expressions across the same JS, TS, JSX, and TSX matrix.

### 21.2 Iterator and async-iterator protocols

- Expand `for...of`, `for await...of`, spreads, `Array.from`, object-enumeration helpers, `Set`/`Map` iteration, and iterable consumption beyond current bounded static slices.
- Current smoke now also pins mixed/bracketed frozen callable aliases for `Object.keys` / `Object.values` / `Object.entries` on the browser-requested `for await` helper path, plus parenthesized receiver-wrapped bracketed aliases for those helpers in js-like input; browser-harness coverage now also includes the logical-or `Object.entries` wrapper slice alongside the existing logical-and/nullish forms, and now also exercises the parenthesized receiver-wrapped bracketed helper variants for that same slice, including the single-quoted parenthesized `Object.freeze((globalThis['Object'])["entries"])` alias, with browser-bundle smoke now also covering the parenthesized receiver-wrapped bracketed `Object.entries` helper variant and the parenthesized bracket-root `Object.entries` helper variant, plus the parenthesized single-quoted bracketed `Object.entries` helper variant on that same browser-harness/browser-bundle matrix, and the browser-harness/browser-bundle matrix now also covers the parenthesized single-quoted receiver-wrapped property variant for `Object.entries`, plus the parenthesized dot-root frozen `Object.entries` alias and the parenthesized receiver-wrapped bracketed `Object.keys` / `Object.values` helper variants on the same browser-harness/browser-bundle matrix, the parenthesized single-quoted and double-quoted receiver-bracketed `Object.keys` helper variants on that same matrix, and the parenthesized bracket-root `Object.keys` callable alias in the shared wrapped-object harness slice, including the single-quoted bracket-root variant on that same slice, plus the dotted parenthesized receiver-wrapped bracket-root `Object.freeze((globalThis["Object"]).keys)` / `Object.freeze((globalThis["Object"]).values)` forms on that same matrix, plus the parenthesized single-quoted receiver-wrapped property variant for `Object.keys` on the browser-bundle object-keys slice. The JS-like type-resolution smoke now also includes the parenthesized single-quoted receiver-bracketed `Object.keys` helper variant on that same wrapped-object slice, plus the direct single-quoted bracket-root `globalThis['Object']['keys']` / `globalThis['Object']['values']` / `globalThis['Object']['entries']` aliases across the js/jsx/ts/tsx matrix, and the canonical object-enumeration helper inventory now also includes the parenthesized single-quoted bracket-root `Object.freeze((globalThis.Object)['keys'])` / `Object.freeze((globalThis.Object)['values'])` / `Object.freeze((globalThis.Object)['entries'])` aliases on that same slice. The JS-like runtime smoke now also includes the parenthesized single-quoted receiver-bracketed `Object.values` helper variant on the same wrapped-object slice. The shared canonical object-enumeration helper inventory now also includes the single-quoted parenthesized receiver-bracketed `Object.freeze((globalThis['Object'])['keys'])` / `Object.freeze((globalThis['Object'])['values'])` / `Object.freeze((globalThis['Object'])['entries'])` aliases so the helper source stays aligned with the smoke corpus, and it now also carries the matching parenthesized bracket-root `["keys"]` / `["values"]` / `["entries"]` aliases for both quote-root spellings, plus the direct mixed-quote `Object.freeze(globalThis["Object"]['keys'])` / `Object.freeze(globalThis["Object"]["values"])` / `Object.freeze(globalThis["Object"]["entries"])` and `Object.freeze(globalThis['Object']["keys"])` / `Object.freeze(globalThis['Object']["values"])` / `Object.freeze(globalThis['Object']["entries"])` frozen-callable aliases. The object-helper smoke now also includes logical `Object.keys` wrapper aliases and the single-quoted bracket-root `Object.freeze((globalThis['Object'])['keys'])` form on the same browser-harness/browser-bundle slice. Promise combinator smoke now also includes `Promise.any` on the standalone run/test and browser harness/bundle paths, including the mixed-bracket frozen-callable alias family around `globalThis["Promise"]['any']`; Promise.race smoke now also spans JS, TS, JSX, and TSX browser-harness/browser-bundle input on the shared browser body, including bracketed `globalThis["Promise"]` / `globalThis['Promise']` aliases and frozen bracketed aliases on that same slice, plus the direct `Promise["race"]` / `Promise['race']` root spellings. The browser-runtime spread fixture now matches the same `Object.entries` alias set, and browser object-values spread smoke now also carries the mixed double/single-quoted `globalThis["Object"]['values']` helper variant plus the parenthesized single-quoted receiver-bracketed `Object.values` callable alias. The same helper slice now also covers the bracket-root `Object.freeze((globalThis["Object"]))["values"]` / `Object.freeze((globalThis["Object"]))['values']` and `Object.freeze((globalThis["Object"]))["entries"]` / `Object.freeze((globalThis["Object"]))['entries']` forms. Browser-harness/browser-bundle spread smoke now also covers frozen callable `Object.keys` / `Object.entries` spread aliases, including the parenthesized receiver-wrapped bracketed and single-quoted forms, and Promise.any smoke now also includes the parenthesized receiver-wrapped dotted `Object.freeze((globalThis["Promise"]).any)` / `Object.freeze((globalThis['Promise']).any)` aliases.
- Current smoke now also covers the frozen root-object `Object.freeze((globalThis["Reflect"]))["ownKeys"]` alias on the supported `Reflect.ownKeys` slice in JS-like input, and the browser-bundle global-object `Object.freeze((globalThis['Object'])['keys'])` / `Object.freeze((globalThis['Object'])['values'])` aliases alongside the existing double-quoted root forms on the supported Object helper slices; the js-like object-enumeration helper smoke now also covers parenthesized single-quoted bracket-root `Object.values` / `Object.entries` aliases, and browser-harness/browser-bundle smoke now also covers the single-quoted parenthesized receiver-bracketed `Object.values` alias on that same helper slice, plus the parenthesized single-quoted bracket-root `Object.keys` callable alias in the shared wrapped-object harness slice. Browser-harness break/continue smoke now also covers the same parenthesized single-quoted receiver-wrapped property alias in the break/continue helper slice. The `Set` / `Map` frozen-callable inventories now also carry nullish/logical wrapper aliases around the root and `globalThis` constructor forms on the same supported constructor slices, keeping that alias surface aligned with the constructor-result smoke, and the browser-requested `test` harness path now also mirrors that frozen `Object.entries` spread stdout shape across the same JS/TS/JSX/TSX matrix. The wrapped-object object-enumeration smoke now also covers the bracket-root `Object.freeze((globalThis["Object"]))["keys"]` / `Object.freeze((globalThis["Object"]))['keys']` aliases on the same browser harness/bundle matrix, and the single-quoted `globalThis['Object']['keys']` / `globalThis['Object']['values']` / `globalThis['Object']['entries']` aliases on that same slice.
- Current smoke now also pins the canonical `E5506` gate for array callback-produced iterables such as `find` / `findIndex` / `findLast` / `findLastIndex` / `map` / `filter` / `some` / `every` / `reduce` / `reduceRight` / `flatMap` until faithful callback lowering exists, with the broader array-callback rejection matrix now covered in the lowering unit, build/check smoke, runtime smoke, CLI smoke, browser-harness coverage, and browser-requested run/test harness coverage in JS, TS, JSX, and TSX input. The shared helper inventory now also canonicalizes the parenthesized receiver-property `Object.keys` / `Object.values` / `Object.entries` alias forms for both double- and single-quoted `globalThis["Object"]` roots so the browser object-enumeration smoke stays centralized, and it now also carries the parenthesized direct dot-root bracketed `globalThis.Object["keys"|"values"|"entries"]` aliases. The browser-harness/browser-bundle for-await object-helper smoke now also carries the single-quoted bracketed frozen callable aliases for `Object.keys` / `Object.values` / `Object.entries`.
- Implement protocol lookup, `next` result handling, abrupt completion, iterator close, async iterator finalization, and error propagation.
- Add conformance fixtures for supported built-ins and negative diagnostics for unimplemented protocol edges.
- Current smoke coverage now also exercises Set/Map constructor break/continue finalization on the supported iterator slice in run/test and browser-harness paths, plus frozen `Object.entries` helper-call coverage on the object-enumeration slice and the parenthesized frozen bracket-root `Object.entries` alias in browser bundle/harness paths, and `Array.from` over frozen `globalThis["Set"]` / `globalThis['Set']` / `globalThis['Map']` / `globalThis["Map"]` constructor results in the standalone and browser harness/bundle smoke lanes, with browser bundle/harness smoke now also covering the parenthesized direct constructor aliases `new (globalThis["Set"])` and `new (globalThis['Map'])` on that same supported slice. Browser-object-values spread smoke now also includes the single-quoted parenthesized receiver-bracketed `Object.values` alias and the mixed double/single-quoted `globalThis["Object"]['values']` alias family, and browser-object-values iteration/spread smoke now also covers the bracket-root `Object.freeze((globalThis["Object"]))["values"]` alias on the same supported helper slice. The shared `Array.from` frozen-callable inventory now also carries the sequence-wrapped transparent aliases around `Array.from` and `globalThis.Array.from` so the smoke corpus stays aligned with the helper inventory.
- Mixed-quote `Object.fromEntries` aliases now also stay on the same static object-enumeration helper path in codegen and smoke.
- Current smoke now also includes direct mixed-quote `Array.from` aliases in the shared array-iteration inventory, the `Array.from` frozen-callable inventory now also includes the parenthesized frozen bracket-root aliases around `globalThis["Array"]` and `globalThis['Array']`, the new sequence-wrapped transparent `Array.from` aliases, and the standalone wrapped-object object-enumeration smoke now also mirrors the single-quoted bracket-root `globalThis['Object']['keys'|'values'|'entries']` aliases on the same supported slice.
- Keep transparent wrapper handling only where it remains deterministic and evidence-backed.

### 21.3 Dynamic language and built-in semantics

- Widen object-model, Math, BigInt, Promise, dynamic import, reflection, and operator semantics only when observable JavaScript behavior is pinned. Standalone run/test smoke now also mirrors the transparent bracketed and frozen alias slices for `Math.sin` / `Math.cos` / `Math.tan` zero identities in JS input. Current smoke now also covers the single-quoted receiver-wrapped `Math.floor` / `Math.trunc` / `Math.ceil` frozen-callable aliases on the supported helper slice, plus the parenthesized root-object aliases around `globalThis.Math`, `globalThis["Math"]`, and `globalThis['Math']` on that same floor/trunc/ceil slice, plus the parenthesized single-quoted receiver-wrapped callable variants around `globalThis['Math']` on that same slice, plus the parenthesized receiver-wrapped dot-root `Object.freeze((globalThis["Math"]).floor)` / `Object.freeze((globalThis["Math"]).trunc)` / `Object.freeze((globalThis["Math"]).ceil)` aliases, and direct build smoke now also covers the `Math.sqrt` / `Math.cbrt` literal slices in `.jsx` and `.tsx` input on both the Deno and browser surfaces; the browser-harness/browser-bundle frozen-callable smoke now also covers the parenthesized bracket-root and dot-root `Math.sqrt` aliases. The supported `Math.cbrt` frozen-callable inventory now also carries the parenthesized receiver-wrapped bracket-root spellings around `globalThis["Math"]` and `globalThis['Math']`, plus the corresponding parenthesized root-object `Object.freeze((globalThis["Math"]))["cbrt"]` / `Object.freeze((globalThis['Math']))["cbrt"]` aliases and the parenthesized dot-root `Object.freeze((globalThis.Math))["cbrt"]` / `Object.freeze((globalThis.Math))['cbrt']` aliases, with the browser-harness/bundle cbrt slice now also carrying the single-quoted parenthesized bracket-root invocation forms, keeping that helper slice aligned with the existing `Math.hypot` wrapper breadth. The CLI build corpus now also carries the matching `Math.cbrt` frozen-callable wrapper slice across Deno and browser `.js` / `.ts` inputs. The supported static-call inventory now also recognizes `Promise.any` aliases, including bracketed frozen-callable forms on the shared browser body, and the compiler build smoke now also exercises that shared `Promise.any` body across Deno and browser `ts` / `js` / `jsx` / `tsx` inputs; the browser harness/bundle path now also covers bracket-root frozen `globalThis["Promise"]["any"]` / `globalThis['Promise']['any']` aliases and the parenthesized receiver-wrapped dotted `Object.freeze((globalThis["Promise"]).any)` / `Object.freeze((globalThis['Promise']).any)` aliases, plus the parenthesized root-object `Object.freeze((globalThis["Math"]))["hypot"]` / `Object.freeze((globalThis['Math']))["hypot"]` aliases, the parenthesized dot-root `Object.freeze((globalThis.Math))["hypot"]` / `Object.freeze((globalThis.Math))['hypot']` aliases, and the corresponding freeze-then-bracket aliases on the supported `Math.hypot` slice; the browser harness/bundle frozen-callable smoke now also covers `Math.sqrt` / `Math.cbrt` aliases across `.js` / `.ts` / `.jsx` / `.tsx` input, and the `Math.cbrt` slice now also includes the single-quoted parenthesized bracket-root forms, with the supported `Math.sqrt` frozen-callable slice now also covering the parenthesized single-quoted bracket-root and dot-root variants plus the `globalThis['Math']` call/single forms. The Number predicate slice now also includes frozen `Number.isNaN` alias coverage alongside the existing finite/integer/safeInteger helpers, and the shared helper inventory now also exercises parenthesized receiver-property frozen aliases around `globalThis["Number"]` on that same slice; the static callable-name checker now also recognizes bracketed `Promise` combinator aliases on the same `Promise` support family. The object-has-own smoke now also covers the single-quoted, parenthesized single-quoted, and frozen single-quoted callable aliases on the supported `Object.fromEntries` path, and the bracketed `Object["hasOwn"]` / `globalThis["Object"]["hasOwn"]` callable aliases now also fold on the static object-literal path. The `Math.pow` browser alias inventory now also carries the parenthesized single-quoted bracket-root variants for `globalThis['Math']`, plus receiver-wrapped bracket-root aliases around `globalThis['Math']` with both bracket spellings, and the build preflight now also rejects optional-chain-wrapped `Math.pow` member-access forms such as `Math?.pow` and `globalThis.Math?.pow`, keeping the browser smoke aligned with the canonical alias inventory.
- Browser-harness `check` / build smoke now also rejects optional-chain-wrapped `Math.pow` in JS, TS, JSX, and TSX input, keeping the browser API-surface gate aligned with the build-time rejection path.
- The optional-chain `Math.pow` rejection slice now also has explicit JSON/text coverage for `Math?.pow` and `globalThis.Math?.pow` / `globalThis?.Math.pow` spellings in browser API-surface `check` and `build --bundle` smoke, plus browser-requested `run` / `test` browser-harness coverage in JS, TS, JSX, and TSX input.
- Keep non-literal dynamic import, broad reflective APIs, eval-adjacent forms, and unsupported object-model/runtime APIs gated unless their maturity rows are promoted; the current smoke also now gates direct `globalThis.Function` and bracketed aliases on the same eval-compat path.
- Pair each promotion with checker, lowering, runtime, browser/context, and JSON-output evidence where applicable.

### 21.4 Bounded TS/JS inference

- Grow inference inside deterministic budgets only.
- Preserve annotation-required boundaries for exported/public and cross-module surfaces when inference would exceed the bounded contract.
- Add positive and negative checker baselines for TS and first-class JS input.
- Current smoke now also covers default-export function expressions across JS, TS, JSX, and TSX input on the bounded lib-export signature slice, including plain and JSON build modes.
- Keep transparent wrapper handling aligned with bounded-literal/static-resolution paths only when cheap and deterministic.

### 21.5 Conformance hygiene

- Maintain compact dashboards of supported vs gated semantics.
- Remove implementation-journal prose from plan files.
- Keep diagnostic codes and wording aligned with `specs/15-errors.md`.
- Normalize late object-model diagnostics so quoted alias spellings surface the same canonical `Proxy.revocable` family in E5506 text, including the mixed-quote bracket-access family now kept in the shared smoke inventory.
- Keep the shared own-property source inventory aligned with the single-quoted `globalThis.Object['hasOwnProperty'].call` spelling so the `Object.hasOwn` / `hasOwnProperty.call` slices stay canonical across the smoke corpus.

## Exit gate

- Supported semantics have parser/checker/lowering/runtime evidence.
- Unsupported semantics fail through canonical diagnostics.
- `cargo test --workspace` passes for affected Rust paths.
- Public availability wording remains aligned with `specs/19-feature-maturity.md`.
