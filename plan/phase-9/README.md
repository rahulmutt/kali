# Phase 9 — Optimization, PGO, and Performance Evidence

## Goal

Improve generated code and performance claims while keeping outputs deterministic and semantics correct.

## Owning specs

- `specs/07-specialization.md`
- `specs/08-wasm-codegen.md`
- `specs/16-testing.md`
- `specs/18-schemas.md`
- `specs/19-feature-maturity.md`

## Work packets

### 9.1 Optimization inventory

- Document which `fast`, `release`, and `release-advanced` optimizations are currently real.
- Add regression tests for existing passes and artifact determinism.
- Progress: sandboxed `build --lib` repeated-build determinism now has coverage alongside the existing artifact-stability checks, and browser bundle `--format cjs` artifacts now also stay stable across repeated invocations, including the inherited browser apiSurface variant.
- Progress: the current-mode inventory now has a checked-in snapshot in [`optimization-inventory.md`](./optimization-inventory.md), and the fast-mode minimality path now has direct optimizer-level regression anchors for literal binary expressions and object-enumeration calls over literal shapes so the inventory is backed by both docs and tests.
- Current progress: the optimization inventory snapshot is now also pinned by the schema-doc drift net in `crates/kali_cli/tests/schema_docs.rs`, so the checked-in mode/evidence summary stays deterministic alongside the existing optimizer and benchmark coverage.
- Current progress: release-mode object-enumeration folding now has direct optimizer-level regression anchors for `Object.keys()`, `Object.entries()`, and `Object.values()` over literal object shapes, and release-advanced now also has matching regression anchors for the same literal-shape enumeration forms, so the current inventory keeps the three supported enumeration forms backed by tests in both optimization tiers rather than only by the runtime-smoke layer. `Reflect.ownKeys()` over literal object shapes now folds alongside that same object-enumeration path in fast, release, and release-advanced mode, keeping the Reflect subset aligned with the transpiler/bundler literal-enumeration contract. `Reflect.ownKeys()` now also folds through const-bound literal aliases in release and release-advanced mode, keeping the Reflect path aligned with the existing object-enumeration alias-depth coverage.
- Current progress: the same release and release-advanced object-enumeration folding path now also covers const-bound literal aliases, so `Object.keys()` / `Object.entries()` / `Object.values()` stay folded when the literal object is referenced through a `const` binding, and the alias-chain regression now also survives a second `const` alias hop before enumeration so the folding path stays honest through a deeper rebinding chain.
- Current progress: release-advanced now also has a nullish literal-argument specialization regression, keeping the higher optimization tier aligned with the literal-argument specialization family already exercised in release mode.
- Current progress: release-advanced now also has a BigInt literal-argument specialization regression, keeping the higher optimization tier aligned with the BigInt literal-argument benchmark lane too.
- Current progress: fast mode now also keeps nullish literal-argument callsites unspecialized, so the nullish specialization family remains gated to the release tiers while the fast-mode minimality baseline stays explicit.

### 9.2 Specialization depth

- Improve layout/representation fingerprinting.
- Respect `--max-specializations` exactly as an upper bound.
- Ensure fallback paths preserve JavaScript semantics.
- Progress: zero-budget tagged-parameter MIR specialization now has a regression that keeps the original call target in place and prevents speculative `add_pair$spec$...` clones.
- Progress: release-advanced now also has a cap-exactness regression that shows duplicate root-call shapes can inline away without consuming the single specialization slot, while the remaining distinct call shape still produces exactly one MIR-specialized clone once the cap is reached.
- Progress: the benchmark lane now also includes an object-literal property-order canonicalization workload (`object-literal-property-order-canonicalization-benchmark-v1`) so the object-layout specialization path has an explicit pinned workload alongside the existing object-enumeration and layout-specialization benchmarks.

### 9.3 PGO input

- Promote deterministic build-only `--profile` support only with strict schema validation.
- Keep PGO as an additive build input, not a fourth build mode.
- Add malformed/unknown-field rejection tests.
- Progress: CLI integration now rejects malformed PGO profile payloads with version, unknown-field, and top-level shape validation in both text and JSON build output modes, and equivalent profile snapshots now hash identically after normalization so the build-only PGO input stays deterministic even when sample order or duplicate samples differ. The CLI spec drift net now also pins the build-only `kali build --profile pgo-profile.json main.ts` example, keeping the documented PGO input surface aligned with the published build-only contract.

### 9.4 Benchmark lane

- Add version-pinned benchmarks and fixtures, including the new Math.clz32, Math.ceil, Math.abs/Math.sign, and Math.max/Math.min workloads.
- Require repeatability before any public performance wording changes.
- Keep package benchmark anecdotes separate from compatibility claims.
- Progress: the optimization benchmark smoke now uses checked-in, hash-validated fixture workloads (`math-benchmark-v1.ts` / `math-benchmark-v1.json`, `math-trunc-benchmark-v1.ts` / `math-trunc-benchmark-v1.json`, `math-imul-benchmark-v1.ts` / `math-imul-benchmark-v1.json`, `math-clz32-benchmark-v1.ts` / `math-clz32-benchmark-v1.json`, `math-ceil-benchmark-v1.ts` / `math-ceil-benchmark-v1.json`, `math-abs-sign-benchmark-v1.ts` / `math-abs-sign-benchmark-v1.json`, `math-max-min-benchmark-v1.ts` / `math-max-min-benchmark-v1.json`, `division-by-one-benchmark-v1.ts` / `division-by-one-benchmark-v1.json`, `dead-branch-elimination-benchmark-v1.ts` / `dead-branch-elimination-benchmark-v1.json`, `dead-inlined-function-pruning-benchmark-v1.ts` / `dead-inlined-function-pruning-benchmark-v1.json`, `call-inlining-benchmark-v1.ts` / `call-inlining-benchmark-v1.json`, `closure-inlining-benchmark-v1.ts` / `closure-inlining-benchmark-v1.json`, `object-enumeration-benchmark-v1.ts` / `object-enumeration-benchmark-v1.json`, `reflect-own-keys-benchmark-v1.ts` / `reflect-own-keys-benchmark-v1.json`, `reflect-own-keys-const-bound-literal-benchmark-v1.ts` / `reflect-own-keys-const-bound-literal-benchmark-v1.json`, `integer-like-object-enumeration-benchmark-v1.ts` / `integer-like-object-enumeration-benchmark-v1.json`, `object-enumeration-alias-chain-benchmark-v1.ts` / `object-enumeration-alias-chain-benchmark-v1.json`, `object-enumeration-delete-reinsert-benchmark-v1.ts` / `object-enumeration-delete-reinsert-benchmark-v1.json`, `identity-chain-benchmark-v1.ts` / `identity-chain-benchmark-v1.json`, `nested-wrapper-pruning-benchmark-v1.ts` / `nested-wrapper-pruning-benchmark-v1.json`, `algebraic-simplification-benchmark-v1.ts` / `algebraic-simplification-benchmark-v1.json`, `duplicate-pure-expression-elimination-benchmark-v1.ts` / `duplicate-pure-expression-elimination-benchmark-v1.json`, `nullish-specialization-repeat-benchmark-v1.ts` / `nullish-specialization-repeat-benchmark-v1.json`, `specialization-reuse-benchmark-v1.ts` / `specialization-reuse-benchmark-v1.json`, `const-array-element-access-benchmark-v1.ts` / `const-array-element-access-benchmark-v1.json`, `const-object-property-access-benchmark-v1.ts` / `const-object-property-access-benchmark-v1.json`, `math-variant-benchmark-v1.ts` / `math-variant-benchmark-v1.json`, `string-concatenation-benchmark-v1.ts` / `string-concatenation-benchmark-v1.json`, `array-literal-arguments-benchmark-v1.ts` / `array-literal-arguments-benchmark-v1.json`, `template-literal-concatenation-benchmark-v1.ts` / `template-literal-concatenation-benchmark-v1.json`, `layout-specialization-benchmark-v1.ts` / `layout-specialization-benchmark-v1.json`, `boolean-literal-arguments-benchmark-v1.ts` / `boolean-literal-arguments-benchmark-v1.json`, `branch-specialization-repeat-benchmark-v1.ts` / `branch-specialization-repeat-benchmark-v1.json`, and `nullish-benchmark-v1.ts` / `nullish-benchmark-v1.json`) so the compile-time size/speed comparison lane is pinned to deterministic inputs instead of an ad-hoc inline source string, the dead-inlined-function-pruning workload keeps the release-advanced dead-function cleanup path visible in the benchmark lane, the object-enumeration alias-chain workload keeps the rebinding path visible in the benchmark lane, and the Reflect.ownKeys workloads keep the same literal-object ordering path visible under the Reflect API too, the delete/reinsert object-enumeration workload keeps the mutation-heavy ordering path visible in the benchmark lane, the integer-like object-enumeration workload keeps the canonical key-ordering path visible in the benchmark lane, the dead-branch-elimination workload keeps constant-branch pruning visible in the benchmark lane, the new boolean-literal-argument workload keeps branch specialization visible in the benchmark lane, and the new branch-specialization-repeat workload keeps branch specialization visible in the benchmark lane, the new const-array element access workload keeps the array-specialization path visible in the benchmark lane, the new const-object property access workload keeps the property-specialization path visible in the benchmark lane, the new math-variant workload keeps the arithmetic baseline visible in the benchmark lane, the new string-concatenation workload keeps the literal-folding path visible in the benchmark lane, the new template-literal-concatenation workload keeps the same literal-folding path visible through template-string syntax, the new division-by-one workload keeps the release-advanced division-simplification path visible in the benchmark lane, the new Math.abs/Math.sign workload keeps the built-in math lowering path visible in the benchmark lane, the new Math.max/Math.min workload keeps the same built-in math lowering path visible in the benchmark lane, the new Math.trunc workload keeps the built-in math lowering path visible in the benchmark lane, the new Math.imul workload keeps the built-in math lowering path visible in the benchmark lane, the new Math.clz32 workload keeps the built-in math lowering path visible in the benchmark lane, the new Math.ceil workload keeps the built-in math lowering path visible in the benchmark lane, the new duplicate pure-expression elimination workload keeps the repeated-expression cleanup path visible in the benchmark lane, and the runtime-smoke benchmark loop now exercises the specialization-reuse workload and the string-concatenation plus template-literal-concatenation workloads too, while the new layout-specialization workload keeps the MIR layout / owner-reuse path visible in the benchmark lane. The benchmark fixture metadata now also has a dedicated schema-v1 document and schema-doc drift coverage, and the schema-doc drift net now also validates each benchmark fixture's declared source file and source hash against the checked-in source file, including the `call-inlining-benchmark-v1` workload, and now also pins the full benchmark slug/source-file set explicitly so additions or deletions in the benchmark corpus fail the drift net deterministically, keeping the pinned workload contract deterministic alongside the optimizer evidence. The benchmark metadata schema now also constrains benchmark slugs and source-file names with explicit patterns, keeping the v1 corpus naming contract deterministic too.
- Progress: the benchmark corpus now also pins a `bigint-literal-arguments-benchmark-v1` fixture, keeping literal-argument specialization evidence explicit for the BigInt lane alongside the existing boolean, nullish, and string literal-argument benchmarks, and it now also pins a `bigint-addition-chain-benchmark-v1` workload plus a `bigint-multiplication-chain-benchmark-v1` workload and a const-bound literal object enumeration workload so the chained-BigInt and alias-to-literal specialization paths stay visible in the benchmark lane.
- Current progress: the benchmark smoke now keeps the release-vs-fast footprint check explicit for the shared corpus while allowing workloads such as object-enumeration-delete-reinsert, reflect-own-keys, and nullish-specialization to remain release-neutral when the higher tier is the one that actually improves the footprint, keeping the deterministic benchmark lane honest without overclaiming release-mode wins on every fixture.

## Exit gate

- Optimizations preserve all conformance and sandbox tests.
- PGO output is deterministic.
- Performance claims name workload, build mode, baseline, and evidence.
