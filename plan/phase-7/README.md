# Phase 7 — Runtime, Host, and Platform Expansion

## Goal

Add runtime and host breadth without weakening sandbox honesty or confusing deployment targets.

## Owning specs

- `specs/09-sandboxing.md`
- `specs/10-runtime.md`
- `specs/11-standard-apis.md`
- `specs/12-cli.md`
- `specs/18-schemas.md`
- `specs/19-feature-maturity.md`

## Work packets

### 7.1 Threaded runtime profile

- Implement `--wasm-threads` / `runtimeProfiles = ["wasm-threads"]` as an explicit opt-in.
- Support positive `maxThreads` only when the threaded profile is active and supported.
- Add tests for zero-capable budgets vs positive thread budgets.
- Preserve no tracing/background GC and AOT-only compilation.
- Progress: the runtime and `run`/`test` CLI paths now accept the threaded profile on supported execution contexts, and `check` / `build` / `effects` now also accept it on the supported non-browser analysis/build paths; positive `--max-threads` values are honored when that opt-in is present. Browser-targeted and registry-analysis rows still gate the profile separately, and deterministic guest-facing thread-spawn host import plumbing is now in place. Regression coverage now also pins the browser-targeted and registry-analysis rejection paths for `--wasm-threads`, including browser-build smoke coverage in both text and JSON output modes, while fuller lowering / multi-worker execution semantics remain follow-up work. The browser-targeted `effects` lane now also has JSON-output rejection coverage for the threaded-profile gate, so the browser-analysis boundary stays explicit in both human and machine-readable output. The browser-targeted `check` / `build` rejection path for `--wasm-threads` now also mirrors `.js` input on both text and JSON output modes, keeping the browser-analysis/build boundary aligned with first-class JavaScript compilation. The zero-capable thread-budget contract is now also mirrored in JSON smoke coverage for `run` and `test`, and the zero-budget sandbox policy path now also accepts `check --sandbox` and `build --sandbox` in both text and JSON output modes with the same `resources.maxThreads = 0` / `resources.maxSpawnedProcesses = 0` baseline, so `--max-threads 0` stays accepted alongside the existing positive-budget rejection cases. The zero-capable spawned-process budget contract now also has JSON smoke coverage for `run` and `test`, so `--max-spawned-processes 0` stays accepted alongside the existing positive-budget rejection cases. The browser-requested `run` / `test` harness path now also keeps that zero-capable budget pair open on the browser-requested `.js` surface, so the browser-run/test evidence mirrors the same tightening path. The CLI run/test smoke matrix now also rejects positive `--max-spawned-processes` overrides in both text and JSON modes, keeping the later subprocess-budget gate explicit until subprocess support itself exists. `effects` JSON output now also exercises combined inherited `compat.features` + `compilerOptions.runtimeProfiles` normalization, inherited-browser `package-effects` rejection now also has a matching human-output regression for `--wasm-threads`, the browser-requested `run` / `test` sandbox gate now also stays closed when `KALI_BROWSER_BUNDLE_HARNESS_COMMAND` is configured, and the default standalone `package-effects` path now also has JSON-output rejection coverage for inherited threaded profiles; the quiet JSON path now also rejects a mixed inherited `eval` + `wasm-threads` context deterministically, keeping mixed-axis inherited analysis-context serialization deterministic.

### 7.2 Standalone browser runtime contract

- Decide whether Kali will support `run --api browser` / `test --api browser` through a real browser host contract.
- If yes, specify runtime ownership, sandbox/effect limits, test harness behavior, and JSON outputs before implementation.
- Keep browser bundle/check support separate from standalone browser execution.
- Progress: the standalone browser-requested run/test path is wired behind `KALI_BROWSER_BUNDLE_HARNESS_COMMAND` and now has regression coverage for the opt-in success path, including inherited browser `apiSurface` configs for both `run` and `test`; the default rejection path remains when the harness command is absent. The inherited-browser coverage now also exercises browser package resolution while keeping the host-contract split explicit, and the browser-harness smoke now also mirrors the package-resolution path through first-class `.js` inputs for both `run` and `test`. The browser runtime corpus now also mirrors the browser package fixtures on `.js` input for both `run` and `test`, including the browser-vs-deno condition-preference probe in `.js` input, and the basic browser-requested `run` / `test` acceptance lane now mirrors first-class `.js` inputs too, including object-enumeration smoke coverage under the configured browser harness and object-enumeration smoke coverage in `.ts` input; that lane now also exercises `Math.max()` / `Math.min()` / `Math.abs()` / `Math.sign()` / `Math.clz32()` semantics in both TS and JS input classes, and now also exercises the Web Crypto randomness subset via `crypto.getRandomValues()` on mirrored `.js` input, with the shared browser runtime formatter bounds-checking tagged string handles so negative numeric logs stay numeric. The browser runtime `@mariozechner/pi-coding-agent` package-content probe now also covers the inherited browser `apiSurface` path in `.js` input for both `run` and `test`, so the host-heavy browser runtime corpus stays explicit across direct and inherited browser context selection. The browser runtime web-baseline package slice now also has JSON-output coverage on the first-class JavaScript browser harness path, and the browser-requested `run` / `test` performance.now monotonic-ordering regression now also has mirrored `.ts` input coverage on the direct and inherited browser-harness paths, and the browser-requested `test` JSON lane now also pins the same `.js` timing baseline. The inherited browser `apiSurface` path now also has JSON-output `run` / `test` coverage for `Math.trunc()` in `.ts` and `.js` input under the same browser harness, and the inherited browser `apiSurface` path now also exercises the `Math.max()` / `Math.min()` / `Math.abs()` / `Math.sign()` / `Math.imul()` suite in `.js` input for both `run` and `test`, keeping the direct and inherited browser runtime corpus split explicit across both source classes. The generic browser package fixtures now also exercise the inherited browser `apiSurface` path in `.js` input for both `run` and `test`, keeping the direct and inherited browser runtime corpus split explicit across both source classes. The browser runtime corpus now also has JSON-output coverage for the browser-deno preference package slice on the direct and inherited browser `apiSurface` paths in `.js` input, including the inherited `run` / `test` probes now pinned in `crates/kali_cli/tests/package_corpus.rs`, keeping the browser-requested harness evidence aligned with the existing text-mode package-corpus matrix. The `kali doctor` JSON payload now also includes the declarative browser runtime contract snapshot alongside the browser harness selection, keeping the browser-contract metadata machine-readable without implying standalone browser execution. The human-readable `doctor` output now also mirrors that browser runtime contract snapshot alongside the browser-harness summary, and the unavailable-harness-executable regression now also pins the same contract snapshot in JSON, keeping the diagnostic text and JSON payload aligned. The sandbox policy tests now also cover timer scheduling denials, timeout ceilings, and active-timer ceilings, keeping the scheduled-callback policy boundary explicit alongside the lower-level negative-delay runtime check.
The same browser-requested run/test lane now also exercises browser Web Crypto `crypto.getRandomValues()` in mirrored `.ts` and `.js` input, plus `crypto.subtle.digest()` / `crypto.randomUUID()` semantics in mirrored `.js` input, boolean conjunction/disjunction semantics and strict equality / inequality semantics in both TS and `.js` input classes, and `try/finally` sequencing plus queueMicrotask ordering in both TS and `.js` inputs under the configured browser harness, plus optional chaining and console-level routing in `.js` input. The browser-requested run/test lane now also exercises literal-string dynamic imports with directory-index targets in `.js` input, keeping the browser runtime path aligned with the linked-graph loader behavior. Browser bundle crypto web API smoke now also mirrors `crypto.getRandomValues()` plus `crypto.subtle.digest()` / `crypto.randomUUID()` in both TS and `.js` inputs, including JSON-output coverage, keeping the browser-requested and browser-bundle execution surfaces aligned with first-class JavaScript compilation. Browser-targeted `check` / `build --bundle` coverage now also rejects late Deno/process host-control members on mirrored `.js` input, keeping the browser ambient surface separated from later standalone host-control APIs. The browser-requested `run` / `test` harness path now also carries JSON-output rejection coverage for late process-control members on mirrored `.js` input and late object-model members on mirrored `.js` input, and the inherited browser `apiSurface` path now mirrors the late object-model rejection on `run` / `test` in `.js` input too, and standalone `check` / `run` / `test` / `build` now also carry the same text and JSON-output rejection coverage for the late compatibility set in `.js` input, so the opt-in runtime contract stays aligned across both human-readable and machine-readable failure modes. The browser-requested `run` / `test` harness path now also rejects nullish coalescing `??` on `.js` input, including JSON-output coverage, so the browser runtime gate stays aligned with the same later-compatibility matrix. The browser-requested run/test strict-equality lane now also has mirrored TS and `.js` coverage, and the browser-requested run/test boolean-conjunction/disjunction lane now also has JSON-output coverage on `.js` input, keeping the browser runtime machine-readable path aligned with the same short-circuit regression the human-readable lane already pins. The browser bundle boolean conjunction/disjunction smoke now also has JSON-output coverage in both TS and `.js` inputs, keeping the browser bundle machine-readable path aligned with the same short-circuit regression the human-readable lane already pins. The browser-entrypoint alias smoke now also mirrors the run-side browser alias families onto the `test` command, including chromium-, Edge-, Opera-, and Vivaldi-family executables plus representative Brave, Firefox, privacy-browser, command-wrapped aliases, and the generic `msedge` / `opera` / `vivaldi` aliases, keeping the browser harness command split explicit across both runtime commands. The non-browser `check` path now also mirrors late Phase-3 Deno host-effect rejections onto `.js` input, including a JSON-output regression, so the late host-API gate stays explicit in the first-class JavaScript analysis path too. The inherited browser API surface now also rejects the browser library-oriented build shapes (`--lib`, `--capi`, and `--component`) on first-class `.js` input, with JSON-output coverage for the inherited `--lib` / `--capi` / `--component` lanes, and the explicit browser API surface now also rejects those same library-oriented build shapes on first-class `.js` input, with JSON-output coverage for the explicit browser lanes too, keeping the browser build-orthogonality gate explicit across both config paths.

- Progress: browser-requested run/test async/await sequencing now also has dedicated browser-harness coverage in both TS and `.js` input, keeping the opt-in browser runtime path aligned with the same await-order regression the standalone runtime smoke already pins.
- Progress: the browser-requested run/test harness now also exercises the shared web-baseline `structuredClone` / `AbortController` / `EventTarget` / `CustomEvent` primitives in both TS and `.js` input, including JSON-output coverage on the browser-requested `run` and `test` paths.
- Progress: the browser-requested run/test web-baseline primitives now also mirror that same `structuredClone` / `AbortController` / `EventTarget` / `CustomEvent` coverage onto the inherited browser `apiSurface` path in both TS and `.js` input, including JSON-output coverage, so the direct and inherited browser harness lanes stay aligned for the shared web-baseline contract.
- Progress: the standalone `run` / `test` smoke lane now also exercises `globalThis.Deno.pid` handling on `.js` input, and the JSON `test` lane now also covers the direct `globalThis.Deno.pid` spelling in `.js` input, keeping the core runtime baseline aligned with the documented Deno PID surface.
- Progress: the browser-harness `run` / `test` sandbox rejection path now also has plain-text coverage for both explicit and inherited browser `apiSurface` requests, so the opt-in browser execution path continues to reject `--sandbox` even when the harness command is configured.

### 7.3 Late host APIs

- Add mutable env, subprocess, socket/listener, process identity/control, cwd/chdir, and similar APIs only with explicit policy/effect/resource contracts.
- Ensure host API visibility matches the selected `apiSurface`.
- Current progress: Node API `process.argv.slice(2).length` now has `.js` runtime smoke coverage in `crates/kali_cli/tests/runtime_smoke.rs` on the documented Node `run` surface, and `crates/kali_codegen/src/tests.rs` now anchors the corresponding slice-length lowering path with a deterministic codegen assertion. The Node analysis/build smoke now also exercises `process.argv.slice(2).length` on the documented Node `check` / `build` surface in `.js` input, and the Node `test` surface now also has a direct `.js` smoke probe that registers `Kali.test(...)` under `--api node`, keeping the query-helper slice aligned across analysis, build, test, and execution paths. The explicit Node API surface smoke now also mirrors `.js` input on both `check` / `build` and `run` / `test`, now also carries JSON-output coverage for the Node `build` path in `.js` input, and now also carries JSON-output coverage for the Node `run` / `test` `.js` input path; the inherited Node API surface now mirrors the same `build` and `run` / `test` JSON paths in `.js` input too, so the documented Node-compatible subset stays aligned with first-class JavaScript compilation as well as the existing TypeScript lane. The non-browser `run` / `test` sandbox lane now also rejects the phase-three Deno host-effect slice in `.js` input, including JSON-output coverage, and the non-browser `build --sandbox` lane now also rejects the same slice in `.js` input with JSON-output coverage, so the sandbox-validation path stays mirrored across the execution and build surfaces.
- Current progress: the default standalone surface now also accepts the read-only `Deno.pid` / `Deno["pid"]` / `globalThis.Deno.pid` process-identity query, and the `check` / `build` / `run` smoke paths now also cover both spellings directly in `.js` input while the `run` / `build` JSON paths now also have explicit coverage for both `Deno.pid` and `globalThis.Deno.pid`; the `check` JSON path now also has explicit coverage for `globalThis.Deno.pid` in `.js` input, and the same lane now also exercises direct bracketed `Deno["pid"]` access and bracketed `globalThis["Deno"]["pid"]` access on the standalone `check` / `build` / `run` path, with matching `.ts` coverage now pinned across the standalone `check` / `build` / `run` / `test` paths plus JSON `run` / `build` coverage, and the late host-control rejection lane now also covers bracketed `globalThis["Deno"]["exit"]` access in the standalone and browser JS-input coverage, keeping the host-identity and host-control baselines explicit in machine-readable output too. The standalone `test` lane now also covers the bracketed `globalThis["Deno"]["pid"]` spelling in both human-readable and JSON output modes, with matching `.ts` test-input coverage now pinned alongside the existing `.js` path, and the standalone `test` JSON lane now also covers direct `globalThis.Deno.pid` in `.js` input, keeping the default standalone host-identity contract aligned across the full command set. The read-only env slice now also accepts `Deno.env.get(...)` on the standalone `check` / `build` / `run` / `test` path in `.js` input, with JSON-output coverage now also pinned for `check` / `build` / `run` / `test`, and `crates/kali_cli/src/build_tests.rs` now mirrors the same acceptance in the executable build path, while `Deno.env.toObject` remains the later object-materialization follow-up. The shared runtime_smoke lane now also mirrors that materialization gate in `.js` input with text and JSON check coverage, and the dedicated late-compatibility JS smoke now also rejects `Deno.env.toObject` (including the inherited `globalThis.Deno.env.toObject` form) so the later object-materialization gate stays explicit alongside the read-only env-read baseline.
- Current progress: the shared runtime_smoke lane now also mirrors recognized-but-unavailable permission-escalation members into `.js` `check` / `build` rejection coverage, including direct, computed, and bracketed forms with JSON-output assertions, so the sandbox-validation boundary stays explicit on the first-class JavaScript path as well. Parser integration coverage now also exercises the bracketed `globalThis["Deno"]["exit"]` form for late host-control member access, keeping the same source spelling honest before the type checker normalizes it to the canonical rejection path. The dedicated late-compatibility fixtures and shared runtime_smoke source helpers now also explicitly pin bracketed process-control and object-model spellings such as `globalThis["process"]["cwd"]`, `globalThis["Proxy"]`, and `globalThis["FinalizationRegistry"]`, so bracketed-path coverage is asserted rather than only present in source fixtures.

### 7.4 Late object/runtime APIs

- Triage `Proxy`, `WeakMap`, `WeakSet`, `WeakRef`, `FinalizationRegistry`, `SharedArrayBuffer`, `Atomics`, and broader `Intl`.
- Require conformance fixtures and memory-model review before promotion.
- Progress: late host-control, broader `Intl`, and late object-model rejection coverage now also has mirrored `.js` input fixtures across the `check` / `run` / `test` paths, and browser-targeted `check` / `build --bundle` now also carries mirrored `.js` rejection coverage for broader `Intl` (including explicit `Intl.DateTimeFormat` / `Intl.NumberFormat` assertions and bracketed `globalThis["Intl"]` / `globalThis["Intl"]["NumberFormat"]` / `globalThis["Intl"]["DateTimeFormat"]` source coverage) plus late object-model members; the browser late-process-control browser lane now also has JSON-output coverage for `check` and `build`, and standalone `build` now also rejects the same late compatibility members on `.js` input, keeping the first-class JavaScript surface aligned with the current later-compatibility gates. The standalone runtime_smoke broader-Intl lane now also mirrors `.js` input with `Intl.DateTimeFormat` alongside `Intl.NumberFormat`, and the same smoke harness now also pins bracketed `globalThis["Intl"]` / `globalThis["Intl"]["NumberFormat"]` / `globalThis["Intl"]["DateTimeFormat"]` source coverage for the broader-Intl rejection tests. The browser late-object-model fixtures now also include `Proxy.revocable(...)` and bracketed `globalThis["Proxy"]["revocable"](...)` spellings so the nested object-model regression stays pinned alongside the direct `Proxy` / `globalThis.Proxy` gate, and the same late-compatibility browser source fixtures now also pin bracketed string-literal Deno/process control forms plus the `WeakMap` / `WeakSet` / `WeakRef` / `FinalizationRegistry` family, including the bracketed `globalThis["WeakRef"]` form, so the source-shape regressions stay explicit alongside the rejection coverage. The late object-model slice now also explicitly gates `Object.hasOwn` / `Object.prototype.hasOwnProperty.call` across the shared JS-input late-compatibility lanes, and the shared runtime_smoke lane now mirrors those own-property helper forms too, keeping own-property helper forms in the same later object-model bucket as the other gated members. The browser-targeted late-object-model rejection assertions now also explicitly check `Proxy.revocable` and the own-property helper spellings in both text and JSON output, so the browser analysis/build gate stays aligned with the full late-object-model source surface rather than only the broad class labels. The default standalone late-compatibility smoke now also mirrors bracketed `globalThis["Intl"]["DateTimeFormat"]` source coverage on `.js` input, keeping the Intl bracketed-path regression mirrored with the rest of the matrix. The default standalone surface now also accepts the read-only `Deno.pid` / `globalThis.Deno.pid` process-identity query while continuing to reject cwd/chdir/exit and the process-global late host-control members, and the runtime and codegen smoke now also cover both `Deno.pid` and `globalThis.Deno.pid` end-to-end; the standalone `check` / `build` / `run` smoke paths now also cover both spellings directly in `.js` input, and the standalone `run` / `build` JSON paths now also have explicit coverage for both `Deno.pid` and `globalThis.Deno.pid`, keeping the machine-readable host-identity baseline aligned too. The browser-requested `run` / `test` harness path now also rejects late object-model members on `.js` input and now also carries matching JSON-output coverage, keeping that opt-in runtime contract aligned with the same later-compatibility gate coverage. The browser-targeted late-compatibility matrix now also rejects `eval` / `Function()` on the browser API-surface `check` / `build --bundle` path and on the browser-harness `run` / `test` path, including JSON-output coverage, keeping the Phase-4 compatibility gate explicit on the browser side too. The threaded runtime boundary now also mirrors `SharedArrayBuffer` / `Atomics` rejection coverage onto `.js` inputs for the supported `check` / `run` / `test` paths, and the browser-requested harness path now also rejects those threaded globals in both human-readable and JSON output modes.

## Exit gate

- Every newly supported host/runtime capability has sandbox, effect, resource, and integration coverage.
- Browser runtime claims are backed by real browser execution evidence if opened.
- Unsupported contexts still fail with canonical diagnostics.
