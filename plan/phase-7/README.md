# Phase 7 — Runtime, Host, and Platform Expansion

## Goal

Add runtime and host breadth without weakening sandbox honesty or confusing deployment targets.

## Owning specs

- `specs/09-sandboxing.md`
- `specs/10-runtime.md`
- `specs/11-standard-apis.md`
- `specs/12-cli.md`
- `specs/18-schemas.md`
- `specs/19-feature-maturity.md`

## Work packets

### 7.1 Threaded runtime profile

- Implement `--wasm-threads` / `runtimeProfiles = ["wasm-threads"]` as an explicit opt-in.
- Support positive `maxThreads` only when the threaded profile is active and supported.
- Add tests for zero-capable budgets vs positive thread budgets.
- Preserve no tracing/background GC and AOT-only compilation.
- Progress: the runtime and `run`/`test` CLI paths now accept the threaded profile on supported execution contexts, and `check` / `build` / `effects` now also accept it on the supported non-browser analysis/build paths; positive `--max-threads` values are honored when that opt-in is present. Browser-targeted and registry-analysis rows still gate the profile separately, and deterministic guest-facing thread-spawn host import plumbing is now in place. The runtime test suite now also pins the zero-budget thread-spawn host-import rejection path, keeping the guest-facing thread budget boundary explicit. Regression coverage now also pins the browser-targeted and registry-analysis rejection paths for `--wasm-threads`, including browser-build smoke coverage in both text and JSON output modes, while fuller lowering / multi-worker execution semantics remain follow-up work. The browser-targeted `effects` lane now also has JSON-output rejection coverage for the threaded-profile gate, so the browser-analysis boundary stays explicit in both human and machine-readable output. The browser-targeted `effects` lane now also mirrors that threaded-profile rejection into `.js` input on both the explicit and inherited browser-analysis paths, including JSON-output coverage, keeping the browser-analysis boundary aligned with first-class JavaScript compilation. The browser-targeted `check` / `build` rejection path for `--wasm-threads` now also mirrors `.js` input on both text and JSON output modes, keeping the browser-analysis/build boundary aligned with first-class JavaScript compilation. The zero-capable thread-budget contract is now also mirrored in JSON smoke coverage for `run` and `test`, and the zero-budget sandbox policy path now also accepts `check --sandbox` and `build --sandbox` in both text and JSON output modes with the same `resources.maxThreads = 0` / `resources.maxSpawnedProcesses = 0` baseline, so `--max-threads 0` stays accepted alongside the existing positive-budget rejection cases. The zero-capable spawned-process budget contract now also has JSON smoke coverage for `run` and `test`, so `--max-spawned-processes 0` stays accepted alongside the existing positive-budget rejection cases. The browser-requested `run` / `test` harness path now also keeps that zero-capable budget pair open on the browser-requested `.js` surface, so the browser-run/test evidence mirrors the same tightening path. The CLI run/test smoke matrix now also rejects positive `--max-spawned-processes` overrides in both text and JSON modes, keeping the later subprocess-budget gate explicit until subprocess support itself exists. `effects` JSON output now also exercises combined inherited `compat.features` + `compilerOptions.runtimeProfiles` normalization, inherited-browser `package-effects` rejection now also has a matching human-output regression for `--wasm-threads`, the browser-requested `run` / `test` sandbox gate now also stays closed when `KALI_BROWSER_BUNDLE_HARNESS_COMMAND` is configured, and the default standalone `package-effects` path now also has JSON-output rejection coverage for inherited threaded profiles; the quiet JSON path now also rejects a mixed inherited `eval` + `wasm-threads` context deterministically, keeping mixed-axis inherited analysis-context serialization deterministic. The browser late-compatibility source fixtures now also include bracketed `globalThis["SharedArrayBuffer"]` and `globalThis["Atomics"]` spellings so the source-shape regression stays explicit alongside the direct-form diagnostics.

### 7.2 Standalone browser runtime contract

- Decide whether Kali will support `run --api browser` / `test --api browser` through a real browser host contract.
- If yes, specify runtime ownership, sandbox/effect limits, test harness behavior, and JSON outputs before implementation.
- Keep browser bundle/check support separate from standalone browser execution.
- Progress: the standalone browser-requested run/test path is wired behind `KALI_BROWSER_BUNDLE_HARNESS_COMMAND` and now has regression coverage for the opt-in success path, including inherited browser `apiSurface` configs for both `run` and `test`; the default rejection path remains when the harness command is absent. The inherited-browser coverage now also exercises browser package resolution while keeping the host-contract split explicit, and the browser-harness smoke now also mirrors the package-resolution path through first-class `.js` inputs for both `run` and `test`. The browser runtime corpus now also mirrors the browser package fixtures on `.js` input for both `run` and `test`, including the browser-vs-deno condition-preference probe in `.js` input, and the basic browser-requested `run` / `test` acceptance lane now mirrors first-class `.js` inputs too, including object-enumeration smoke coverage under the configured browser harness and object-enumeration smoke coverage in `.ts` input; that lane now also exercises `Math.max()` / `Math.min()` / `Math.abs()` / `Math.sign()` / `Math.clz32()` semantics in both TS and JS input classes, and now also exercises the Web Crypto randomness subset via `crypto.getRandomValues()` on mirrored `.js` input, with the shared browser runtime formatter now also pinned by a dedicated negative-numeric console-log regression so signed numeric logs stay numeric. The browser runtime `@mariozechner/pi-coding-agent` package-content probe now also covers the inherited browser `apiSurface` path in `.js` input for both `run` and `test`, so the host-heavy browser runtime corpus stays explicit across direct and inherited browser context selection. The browser runtime web-baseline package slice now also has JSON-output coverage on the first-class JavaScript browser harness path, and the browser-requested `run` / `test` performance.now monotonic-ordering regression now also has mirrored `.ts` input coverage on the direct and inherited browser-harness paths, plus JSON-output coverage on the browser-requested `.js` run lane and the inherited browser-api-surface `.js` run lane, while the browser-requested `test` JSON lane now also pins the same `.js` timing baseline. The inherited browser `apiSurface` path now also has JSON-output `run` / `test` coverage for `Math.trunc()` in `.ts` and `.js` input under the same browser harness, and the inherited browser `apiSurface` path now also exercises the `Math.max()` / `Math.min()` / `Math.abs()` / `Math.sign()` / `Math.imul()` suite in `.js` input for both `run` and `test`, keeping the direct and inherited browser runtime corpus split explicit across both source classes. The generic browser package fixtures now also exercise the inherited browser `apiSurface` path in `.js` input for both `run` and `test`, keeping the direct and inherited browser runtime corpus split explicit across both source classes. The browser runtime corpus now also has JSON-output coverage for the browser-deno preference package slice on the direct and inherited browser `apiSurface` paths in `.js` input, including the inherited `run` / `test` probes now pinned in `crates/kali_cli/tests/package_corpus.rs`, keeping the browser-requested harness evidence aligned with the existing text-mode package-corpus matrix. The browser-targeted build-with-sandbox success matrix now also mirrors first-class `.js` inputs for both inherited and explicit browser `apiSurface` selections, so the browser bundle sandbox path stays aligned with the rest of the browser build evidence. The `kali doctor` JSON payload now also includes the declarative browser runtime contract snapshot alongside the browser harness selection, keeping the browser-contract metadata machine-readable without implying standalone browser execution. The human-readable `doctor` output now also mirrors that browser runtime contract snapshot alongside the browser-harness summary, and the unavailable-harness-executable regression now also pins the same contract snapshot in JSON while asserting the same contract display in human output, keeping the diagnostic text and JSON payload aligned. The sandbox policy tests now also cover timer scheduling denials, timeout ceilings, and active-timer ceilings, keeping the scheduled-callback policy boundary explicit alongside the lower-level negative-delay runtime check.
- Progress: the inherited browser `apiSurface` path now also has JSON-output coverage for `Math.clz32()` in `.js` input on browser-requested `run` / `test`, keeping the machine-readable browser-harness math baseline aligned with the existing direct-path coverage.
- Progress: the browser harness sandbox-rejection matrix in JS input now covers all `run` / `test` × direct/inherited browser-api-surface quadrants, including JSON-output coverage, so the standalone browser runtime contract stays mirrored across the configured harness's full selection matrix. The same browser+`--sandbox` rejection path now also stays explicit when the browser harness command is absent, including the direct and inherited browser-api-surface JS-input cases and their JSON-output coverage, so the no-harness browser gate remains visibly separate from the configured-harness path. The browser-runtime and browser-bundle summary parsers now also merge missing `testsFailed` data from stdout when a summary file omits it, and the browser-requested `test` JSON smoke now exercises that merge path end-to-end; the runtime crate now also has direct browser-bundle summary regressions (`browser_bundle_runtime_summary_merges_missing_tests_failed_from_stdout`, `browser_bundle_runtime_summary_falls_back_to_stdout_when_summary_file_is_unparseable`, and `browser_bundle_runtime_summary_uses_stdout_labels_when_summary_file_lacks_them`) for the same summary-file fallback path, so the harness summary contract remains deterministic even when the file-backed summary is malformed or only partially populated.
- Progress: the browser-requested run/test lane now also exercises browser Web Crypto `crypto.getRandomValues()` in mirrored `.ts` and `.js` input, plus `crypto.subtle.digest()` / `crypto.randomUUID()` semantics in mirrored `.js` input, boolean conjunction/disjunction semantics and strict equality / inequality semantics in both TS and `.js` input classes, plus inherited browser-api-surface boolean conjunction/disjunction semantics in `.js` input with JSON-output coverage, and `try/finally` sequencing plus queueMicrotask ordering in both TS and `.js` inputs under the configured browser harness, plus optional chaining and console-level routing in `.js` input. The browser-requested run/test lane now also exercises literal-string dynamic imports with directory-index targets in `.js` input, keeping the browser runtime path aligned with the linked-graph loader behavior. The browser-requested run path now also accepts the bracketed `Deno["pid"]` / `globalThis["Deno"]["pid"]` process-identity query in `.js` input with JSON-output coverage, and browser bundle crypto web API smoke now also mirrors `crypto.getRandomValues()` plus `crypto.subtle.digest()` / `crypto.randomUUID()` in both TS and `.js` inputs, including JSON-output coverage, keeping the browser-requested and browser-bundle execution surfaces aligned with first-class JavaScript compilation. Browser-targeted `check` / `build --bundle` coverage now also rejects late Deno/process host-control members on mirrored `.js` input, keeping the browser ambient surface separated from later standalone host-control APIs. The browser-requested `run` / `test` harness path now also carries JSON-output rejection coverage for late process-control members on mirrored `.js` input and late object-model members on mirrored `.js` input, and the inherited browser `apiSurface` path now mirrors the late object-model rejection on `run` / `test` in `.js` input too, and standalone `check` / `run` / `test` / `build` now also carry the same text and JSON-output rejection coverage for the late compatibility set in `.js` input, so the opt-in runtime contract stays aligned across both human-readable and machine-readable failure modes. The browser-requested `run` / `test` harness path now also rejects nullish coalescing `??` on `.js` input, including JSON-output coverage, so the browser runtime gate stays aligned with the same later-compatibility matrix. The browser-requested run/test strict-equality lane now also has mirrored TS and `.js` coverage, and the browser-requested run/test boolean-conjunction/disjunction lane now also has JSON-output coverage on `.js` input, keeping the browser runtime machine-readable path aligned with the same short-circuit regression the human-readable lane already pins. The browser bundle boolean conjunction/disjunction smoke now also has JSON-output coverage in both TS and `.js` inputs, keeping the browser bundle machine-readable path aligned with the same short-circuit regression the human-readable lane already pins. The browser-entrypoint alias smoke now also mirrors the run-side browser alias families onto the `test` command, including chromium-, chrome-, Google Chrome stable/headless-shell, Edge-, Opera-, and Vivaldi-family executables plus representative Brave, Firefox, privacy-browser, command-wrapped aliases, and the generic `msedge` / `opera` / `vivaldi` aliases, keeping the browser harness command split explicit across both runtime commands. The non-browser `check` path now also mirrors late Phase-3 Deno host-effect rejections onto `.js` input, including a JSON-output regression, so the late host-API gate stays explicit in the first-class JavaScript analysis path too. The inherited browser API surface now also rejects the browser library-oriented build shapes (`--lib`, `--capi`, and `--component`) on first-class `.js` input, with JSON-output coverage for the inherited `--lib` / `--capi` / `--component` lanes, and the explicit browser API surface now also rejects those same library-oriented build shapes on first-class `.js` input, with JSON-output coverage for the explicit browser lanes too, keeping the browser build-orthogonality gate explicit across both config paths.

- Progress: browser-requested run/test async/await sequencing now also has dedicated browser-harness coverage in both TS and `.js` input, keeping the opt-in browser runtime path aligned with the same await-order regression the standalone runtime smoke already pins.
- Progress: the browser-requested run/test harness now also exercises the shared web-baseline `structuredClone` / `AbortController` / `EventTarget` / `CustomEvent` primitives in both TS and `.js` input, including JSON-output coverage on the browser-requested `run` and `test` paths.
- Progress: the browser-requested run/test web-baseline primitives now also mirror that same `structuredClone` / `AbortController` / `EventTarget` / `CustomEvent` coverage onto the inherited browser `apiSurface` path in both TS and `.js` input, including JSON-output coverage, so the direct and inherited browser harness lanes stay aligned for the shared web-baseline contract.
- Progress: the browser-requested run/test object type / constructor semantics slice (`typeof`, `instanceof`) now also has JSON-output coverage in both TS and `.js` input, and browser bundle smoke now also exercises the same object-model baseline in both source classes, keeping the browser-requested and browser-bundle harness lanes aligned with the standalone runtime semantics.
- Progress: the standalone `run` / `test` smoke lane now also exercises `globalThis.Deno.pid` handling on `.js` input, and the JSON `test` lane now also covers the direct `globalThis.Deno.pid` spelling in `.js` input, keeping the core runtime baseline aligned with the documented Deno PID surface.
- Progress: the browser-harness `run` / `test` sandbox rejection path now also has plain-text coverage for both explicit and inherited browser `apiSurface` requests, including mirrored `.js` input coverage and JSON-output coverage on the browser-requested `run` / `test` lanes, so the opt-in browser execution path continues to reject `--sandbox` even when the harness command is configured.

### 7.3 Late host APIs

- Add mutable env, subprocess, socket/listener, process identity/control, cwd/chdir, and similar APIs only with explicit policy/effect/resource contracts.
- Ensure host API visibility matches the selected `apiSurface`.
- Current progress: Node API `process.argv.slice(2).length` now has `.js` runtime smoke coverage in `crates/kali_cli/tests/runtime_smoke.rs` on the documented Node `run` surface, and `crates/kali_codegen/src/tests.rs` now anchors the corresponding slice-length lowering path with a deterministic codegen assertion. The Node analysis/build smoke now also exercises `process.argv.slice(2).length` on the documented Node `check` / `build` surface in `.js` input, and the Node `test` surface now also has a direct `.js` smoke probe that registers `Kali.test(...)` under `--api node`, keeping the query-helper slice aligned across analysis, build, test, and execution paths. The explicit Node API surface smoke now also mirrors `.js` input on both `check` / `build` and `run` / `test`, now also carries JSON-output coverage for the Node `build` path in `.js` input, now also carries JSON-output coverage for the Node `run` / `test` `.js` input path, and now also carries JSON-output coverage for the explicit `node:timers` run/test helpers in `.js` input; `node:timers/promises` still remains unresolved on the explicit Node API surface, so the package-corpus row remains the evidence source for that subpath, and the explicit Node `check` / `build` smoke now also pins that unresolved import in both human and JSON output modes. The inherited Node API surface now mirrors the same `build` and `run` / `test` JSON paths in `.js` input too, and Node analysis-context `effects` coverage now also mirrors `.js` input on both the explicit and inherited `apiSurface=node` paths, so the documented Node-compatible subset stays aligned with first-class JavaScript compilation as well as the existing TypeScript lane. The non-browser `run` / `test` sandbox lane now also rejects the phase-three Deno host-effect slice in `.js` input, including JSON-output coverage, and the non-browser `build --sandbox` lane now also rejects the same slice in `.js` input with JSON-output coverage, so the sandbox-validation path stays mirrored across the execution and build surfaces.
- Current progress: the default standalone surface now also accepts the read-only `Deno.pid` / `Deno["pid"]` / `globalThis.Deno.pid` process-identity query, and the `check` / `build` / `run` smoke paths now also cover both spellings directly in `.js` input while the `run` / `build` JSON paths now also have explicit coverage for both `Deno.pid` and `globalThis.Deno.pid`; the `check` JSON path now also has explicit coverage for `globalThis.Deno.pid` in `.js` input, and the same lane now also exercises direct bracketed `Deno["pid"]` access and bracketed `globalThis["Deno"]["pid"]` access on the standalone `check` / `build` / `run` path, with matching `.ts` coverage now pinned across the standalone `check` / `build` / `run` / `test` paths plus JSON `run` / `build` coverage, and the late host-control rejection lane now also covers the canonical dot-form process-control path in the standalone and browser JS-input coverage, and now also mirrors direct bracketed `Deno["cwd"]` / `Deno["chdir"]` / `Deno["exit"]` spellings plus their `globalThis.Deno[...]` counterparts alongside the existing `globalThis["Deno"][...]` coverage, while parser integration coverage now also exercises the bracketed `globalThis["Deno"]["exit"]` access, and the shared runtime_smoke late-process-control helper now also pins direct bracketed `process["pid"]` / `process["cwd"]` / `process["chdir"]` / `process["exit"]` and inherited `globalThis.process[...]` spellings, keeping the host-identity and host-control baselines explicit in machine-readable output too. The standalone `test` lane now also covers the bracketed `globalThis["Deno"]["pid"]` spelling in both human-readable and JSON output modes, with matching `.ts` test-input coverage now pinned alongside the existing `.js` path, and the parser integration suite now also exercises the bracketed `globalThis["Deno"]["pid"]` source form, and the standalone `test` JSON lane now also covers direct `globalThis.Deno.pid` in `.js` input, keeping the default standalone host-identity contract aligned across the full command set. Parser integration coverage now also exercises bracketed `globalThis["Intl"]["DateTimeFormat"]`, `globalThis["process"]["cwd"]`, `process["exit"]`, and `globalThis["Proxy"]["revocable"]` source forms, keeping the late-compatibility source-shape regressions explicit before the checker normalizes them to canonical gates. The read-only env slice now also accepts `Deno.env.get(...)` on the standalone `check` / `build` / `run` / `test` path in `.js` input, with JSON-output coverage now also pinned for `check` / `build` / `run` / `test`, and `crates/kali_cli/src/build_tests.rs` now mirrors the same acceptance in the executable build path, including the bracketed `globalThis["Deno"]["env"]["get"]` form there too, while `Deno.env.toObject` remains the later object-materialization follow-up; the internal `DenoEnv::to_object` helper now also has deterministic snapshot coverage, keeping that eventual materialization path honest even before the public surface opens. The standalone `test` lane now also covers bracketed `Deno["env"]["get"]` / `globalThis["Deno"]["env"]["get"]` access in `.js` input, including JSON-output coverage, and the `effects` / `package-effects` smoke now also mirrors the same bracketed env-read source shape as computed host access with `Process.EnvRead`, so the read-only env-read source-shape coverage now matches the rest of the host-identity bracketed-path regressions. The shared runtime_smoke lane now also mirrors that materialization gate in `.js` input with text and JSON check / build / test coverage, including bracketed `Deno.env["toObject"]`, `Deno["env"]["toObject"]`, and `globalThis.Deno["env"]["toObject"]` spellings, the dedicated late-compatibility JS smoke now also rejects `Deno.env.toObject` (including the inherited `globalThis.Deno.env.toObject` form) so the later object-materialization gate stays explicit alongside the read-only env-read baseline, and the browser-targeted late-compatibility lane now also rejects the same `Deno.env.toObject` forms in `.js` input with text and JSON coverage, including the same bracketed env-materialization spellings, and now also rejects recognized-but-unavailable permission-escalation members (`Deno.permissions.request()` / `revoke()` plus bracketed forms) in `.js` input with text and JSON coverage, keeping the browser-analysis/build gate aligned with the same later host-API boundary.
- Current progress: the shared runtime_smoke lane now also mirrors recognized-but-unavailable permission-escalation members into `.js` `check` / `build` rejection coverage, including direct, computed, and bracketed forms with JSON-output assertions, so the sandbox-validation boundary stays explicit on the first-class JavaScript path as well. Parser integration coverage now also exercises the bracketed `globalThis["Deno"]["exit"]` form for late host-control member access, keeping the same source spelling honest before the type checker normalizes it to the canonical rejection path. The dedicated late-compatibility fixtures and shared runtime_smoke source helpers now also explicitly pin bracketed process-control and object-model spellings such as `globalThis["process"]["cwd"]`, `globalThis["Proxy"]`, and `globalThis["FinalizationRegistry"]`, and the dedicated late-compatibility fixtures now also pin the direct bracketed `process["pid"]` / `process["cwd"]` / `process["chdir"]` / `process["exit"]` forms too, so bracketed-path coverage is asserted rather than only present in source fixtures. The dedicated JS-input late-compatibility source assertions now also check bracketed `Deno.permissions["request"]()` / `revoke()` forms alongside the process/object spellings such as `globalThis["process"]["cwd"]`, `globalThis["Proxy"]["revocable"]`, and `globalThis["Object"]["prototype"]["hasOwnProperty"]["call"]`, and the matching JSON-output assertions now also pin bracketed `Proxy` / `Object.hasOwn` / `Weak*` spellings, so the bracketed-path regression stays pinned in the source-fixture layer too.

- Progress: the explicit `--api deno` runtime smoke lane now also round-trips `Deno.env.set(...)` / `Deno.env.delete(...)` in `.js` input on `check` / `build` / `run` / `test`, including JSON-output coverage, keeping the mutable-environment helper path visible on the documented Deno surface without widening the default standalone contract. The browser-targeted late-compatibility lane now also rejects mutable environment access (`Deno.env.set` / `Deno.env.delete` and bracketed forms) in `.js` input with canonical gating/error paths, keeping the browser host-mutation boundary explicit alongside the other late host-control checks. The parser integration suite now also exercises bracketed `globalThis["process"]["cwd"]` and `process["exit"]` source forms, keeping the direct and inherited late-process-control spellings explicit before type resolution normalizes them to the canonical rejection path. The package-effects regression lane now also mirrors computed bracketed `Deno.env.delete(...)` as `Process.EnvWrite`, and now also reports the direct `.js` network/listener slice explicitly, keeping both the mutating env-access and network/listener slices aligned with the bracketed host-access coverage. The subprocess slice now also has a dedicated `.js` `new Deno.Command('sh').spawn()` sandbox-rejection regression on `check` plus JSON output, so the `Process.Spawn` policy boundary is pinned without relying only on the combined host-effects fixture. The same dedicated spawn source shape now also has `build --sandbox` regression coverage in both human and JSON output, keeping the compile-time sandbox-validation path aligned with that specific subprocess boundary too. The sandbox effect-analysis tests now also pin direct and computed `Deno.connect` / `Deno.listen` / `Deno.serve` host-call access in `.js` input, and the `effects` command now also reports the direct `.js` network/listener slice explicitly, keeping the network/listener contract aligned across the analysis and reporting surfaces.

### 7.4 Late object/runtime APIs

- Triage `Proxy`, `WeakMap`, `WeakSet`, `WeakRef`, `FinalizationRegistry`, `SharedArrayBuffer`, `Atomics`, and broader `Intl`.
- Require conformance fixtures and memory-model review before promotion.
- Progress: late host-control, broader `Intl`, and late object-model rejection coverage now also has mirrored `.js` input fixtures across the `check` / `run` / `test` paths, and browser-targeted `check` / `build --bundle` now also carries mirrored `.js` rejection coverage for broader `Intl` (including explicit `Intl.DateTimeFormat` / `Intl.NumberFormat` / `Intl.RelativeTimeFormat` / `Intl.PluralRules` assertions and bracketed `globalThis["Intl"]` / `globalThis["Intl"]["NumberFormat"]` / `globalThis["Intl"]["DateTimeFormat"]` / `globalThis["Intl"]["RelativeTimeFormat"]` / `globalThis["Intl"]["PluralRules"]` source coverage) plus late object-model members; the browser late-process-control browser lane now also has JSON-output coverage for `check` and `build`, and standalone `build` now also rejects the same late compatibility members on `.js` input, keeping the first-class JavaScript surface aligned with the current later-compatibility gates. The standalone runtime_smoke broader-Intl lane now also mirrors `.js` input with `Intl.DateTimeFormat`, `Intl.RelativeTimeFormat`, and `Intl.PluralRules` alongside `Intl.NumberFormat`, and the same smoke harness now also pins bracketed `globalThis["Intl"]` / `globalThis["Intl"]["NumberFormat"]` / `globalThis["Intl"]["DateTimeFormat"]` / `globalThis["Intl"]["RelativeTimeFormat"]` / `globalThis["Intl"]["PluralRules"]` source coverage for the broader-Intl rejection tests. The browser late-object-model fixtures now also include `Proxy.revocable(...)` and bracketed `globalThis["Proxy"]["revocable"](...)` spellings so the nested object-model regression stays pinned alongside the direct `Proxy` / `globalThis.Proxy` gate, and the same late-compatibility browser source fixtures now also pin bracketed string-literal Deno/process control forms plus the `WeakMap` / `WeakSet` / `WeakRef` / `FinalizationRegistry` family, including the bracketed `globalThis["WeakRef"]` form, so the source-shape regressions stay explicit alongside the rejection coverage. The standalone JS-input late-compatibility smoke now also pins bracketed `globalThis["Deno"].permissions["request"]()` / `revoke()` and `globalThis["Deno"].env["toObject"]` source-shape variants in the same regression family, and it now also mirrors bracketed `globalThis["SharedArrayBuffer"]` / `globalThis["Atomics"]` source coverage for the threaded-runtime gate. The late object-model slice now also explicitly gates `Object.hasOwn` / `Object.prototype.hasOwnProperty.call` across the shared JS-input late-compatibility lanes, and the shared runtime_smoke lane now mirrors those own-property helper forms too, keeping own-property helper forms in the same later object-model bucket as the other gated members. The browser-targeted late-object-model rejection assertions now also explicitly check `Proxy.revocable` and the own-property helper spellings in both text and JSON output, so the browser analysis/build gate stays aligned with the full late-object-model source surface rather than only the broad class labels. The default standalone late-compatibility smoke now also mirrors bracketed `globalThis["Intl"]["DateTimeFormat"]` source coverage on `.js` input, keeping the Intl bracketed-path regression mirrored with the rest of the matrix. The default standalone surface now also accepts the read-only `Deno.pid` / `globalThis.Deno.pid` process-identity query while continuing to reject cwd/chdir/exit and the process-global late host-control members, and the runtime and codegen smoke now also cover both `Deno.pid` and `globalThis.Deno.pid` end-to-end; the standalone `check` / `build` / `run` smoke paths now also cover both spellings directly in `.js` input, and the standalone `run` / `build` JSON paths now also have explicit coverage for both `Deno.pid` and `globalThis.Deno.pid`, keeping the machine-readable host-identity baseline aligned too. The browser-requested `run` / `test` harness path now also rejects late object-model members on `.js` input and now also carries matching JSON-output coverage, keeping that opt-in runtime contract aligned with the same later-compatibility gate coverage. The browser-targeted late-compatibility matrix now also rejects `eval` / `Function()` on the browser API-surface `check` / `build --bundle` path and on the browser-harness `run` / `test` path, including JSON-output coverage, keeping the Phase-4 compatibility gate explicit on the browser side too. The threaded runtime boundary now also mirrors `SharedArrayBuffer` / `Atomics` rejection coverage onto `.js` inputs for the supported `check` / `run` / `test` paths, the browser-targeted browser API-surface `check` / `build --bundle` path now also rejects those threaded globals in `.js` input, and the browser-requested harness path now also rejects those threaded globals in both human-readable and JSON output modes, with the inherited browser `apiSurface` analysis/build path now covered too. The same browser-targeted and browser-harness threaded-runtime rejection lanes now also mirror `.ts` input in the explicit and inherited browser selections, keeping the later-compatibility gate aligned across both source classes.

## Exit gate

- Every newly supported host/runtime capability has sandbox, effect, resource, and integration coverage.
- Browser runtime claims are backed by real browser execution evidence if opened.
- Unsupported contexts still fail with canonical diagnostics.
