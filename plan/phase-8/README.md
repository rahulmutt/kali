# Phase 8 — Ecosystem Breadth and Package Compatibility

## Goal

Widen package compatibility with evidence by support rung instead of broad npm claims.

## Owning specs

- `specs/11-standard-apis.md`
- `specs/12-cli.md`
- `specs/14-packages.md`
- `specs/15-errors.md`
- `specs/16-testing.md`
- `specs/18-schemas.md`
- `specs/19-feature-maturity.md`

## Work packets

### 8.1 Package-corpus matrix

- Track packages by source kind, package shape, host/API fit, command, and support rung.
- Include Deno-oriented, browser-targeted, and Node contexts separately.
- Record expected failures for native/binary/bootstrap-heavy packages.
- Progress: the deterministic package-corpus matrix now lives in [`package-corpus-matrix.md`](./package-corpus-matrix.md) and groups the current corpus evidence by browser-targeted, default-standalone, Node, and Deno slices so the rung/context split stays explicit. The matrix drift test now also pins representative browser runtime corpus rows, the browser condition-preference TS and `.js` slices, the browser replacement-map, string-entry, string-export, browser-condition export, and browser dual-exports `.js` rows, the browser web-baseline TS and `.js` rows, the browser internal-browser-rewrite `.js` row, the browser-condition / browser-string / web-baseline row, the browser web-baseline primitive `.js` entrypoint row, the browser host-heavy package-content `.js` runtime row, the browser typed export branch `.js` runtime row, the browser typed export branch inherited-browser `apiSurface` row, the browser pattern-exports `.js` runtime row, the browser pattern-exports inherited-browser `apiSurface` row, the browser module-only `.js` runtime row, the browser module-entry-chain `.js` runtime row, the browser module-entry-chain inherited-browser `apiSurface` runtime row, and the browser runtime module-entry-chain direct JSON-output coverage on the browser harness path, the browser string-export inherited-browser `apiSurface` row, the browser direct and inherited semver `.js` runtime rows, the browser package-fixtures inherited-browser `apiSurface` row, the browser runtime exports-map rows (direct and inherited browser `apiSurface`), replacement-map, typed export branch, string-entry, and string-export rows, the browser and default-standalone mixed CommonJS/ESM interop `.js` rows, the browser module-only targeted rows, the default standalone `semver` `.js` row, the default standalone package-content `.js` test row, the Node runner packages with exports maps `.js` row, the Node built-in packages (`node:buffer`, `node:assert`, `node:child_process`, `node:events`, `node:stream`, `node:fs/promises`, `node:path`, `node:os`, `node:crypto`, `node:http`, `node:timers`, `node:fs`, `node:url`, `node:util`) `.js` row, representative Node/Deno rows, and the Node-assuming `.js` surface row so the checked-in snapshot stays aligned with the current evidence set. The matrix drift net now also asserts the browser replacement-map, string-entry, string-export, browser-condition export, browser-condition / browser-string / web-baseline, browser-condition / browser-deno preference, typed export branch, module-entry-chain, module-only, and scoped-package rows exactly once across the direct and inherited browser harness variants, in addition to the browser dual-exports browser-targeted row, the browser semver runtime rows plus the inherited browser-condition / browser-deno runtime row, the browser module-only targeted rows, and the browser package-content runtime rows plus the default standalone package-content test row, so accidental duplication in the checked-in snapshot stays visible immediately.

### 8.2 Node ecosystem breadth

- Expand Node built-ins and package-resolution behavior where package-corpus evidence demands it.
- Keep late Node modules and process-control APIs gated until Phase 7 contracts exist.
- Progress: the Node package corpus now also exercises the documented Node build surface for the same `node:`-based package set that already has Node `check` / `run` coverage, including `node:child_process`, `node:events`, and `node:os`, so analysis, build, and execution evidence stays aligned for the current Node compatibility slice.
- Progress: the default standalone exports-map package corpus now also covers `build` in addition to `check` and `run`, keeping the broad exports-map rung aligned with the current package-corpus matrix instead of leaving it as a check/run-only slice.
- Progress: the Node package corpus now also exercises the canonical pure-JS `semver` probe on `.js` input across the documented Node `check` / `build` / `run` / `test` lanes, and the package-corpus matrix now records that `.js`-input `semver` slice explicitly, keeping the Node-first-class-JavaScript evidence aligned with the browser and standalone corpus lanes. The package corpus now also carries explicit published-bin entrypoint probes for the semver fixture on the default standalone and Node surfaces, and the matrix drift test now pins those rows too, while the bootstrap-named `@mariozechner/pi-coding-agent` binary-entrypoint probe is now pinned separately as well, so published binaries stay separated from ordinary package materialization in the checked-in evidence.
- Progress: the Node package corpus now also exercises a `node:buffer` built-in import within the Node exports-map JS-input slice across the documented Node `check` / `build` / `run` / `test` lanes, keeping the Node package-resolution evidence aligned with first-class JavaScript compilation instead of only the TypeScript lane.
- Progress: the Node package corpus now also exercises dedicated `node:buffer`, `node:assert`, `node:child_process`, `node:events`, `node:http`, `node:timers`, `node:stream`, `node:fs/promises`, `node:path`, `node:os`, `node:crypto`, `node:fs`, `node:url`, and `node:util` built-in package slices on `.js` input across `check` / `build` / `run` / `test`, so the Node built-in breadth probe stays separate from the node-assuming package fixtures while keeping first-class JavaScript compilation in view.
- Progress: the Node package corpus now also mirrors the runner-package exports-map `.js` slice and the mixed-format-entry slice onto `.js` input for the explicit Node surface, and the Node runner exports-map `.js` row is now recorded in the package-corpus matrix too, keeping the Node package-resolution evidence aligned with first-class JavaScript compilation instead of only the TypeScript lane.
- Progress: the Node-assuming package corpus now also exercises the documented Node build/run/test surface on `.js` input, keeping the Node-only host-API slice explicit across the same package-shape evidence track.
- Progress: the canonical host-heavier `@mariozechner/pi-coding-agent` probe now also covers the package content on the default standalone surface at the check/build rung, and the same package-content slice now also mirrors `.js` input; it now also has `.js` test-corpus coverage and now also executes on the default standalone surface in `.js` input, so the corpus distinguishes package-content support from the separate published bin entrypoint probe while keeping first-class JavaScript compilation in view. The default standalone `date-fns` utility slice now also mirrors `.js` input across `check` / `build` / `run` / `test`, and now also covers `.ts` test input, the canonical semver probe now also mirrors `.js` input across `check` / `build` / `run` / `test`, and the package-corpus matrix now records the split default-standalone semver TS and JS rows explicitly, while the broader default standalone utility corpus now also mirrors the zod, p-limit, and ms slices onto `.js` input for `check` / `build` / `run` / `test`, and the pattern-exports utility slice now mirrors `.js` input across `check` / `build` / `run`, keeping the pure-JS utility ladder aligned with first-class JavaScript compilation too. The default standalone scoped-package corpus now also mirrors `.js` input across `check` / `build` / `run`, keeping the scoped utility rung aligned with first-class JavaScript compilation too. The default standalone web-baseline primitive package slice now also has `build` coverage, and the `.js` web-baseline slice now also has `build` coverage alongside `test`, keeping the baseline web-API rung aligned with the same first-class-JavaScript package evidence too. The matrix drift test now also pins the default standalone package-content `.js` test row plus the `.js` test rows for the `date-fns`, `zod`, `p-limit`, and `ms` utility slices.
- Progress: the default standalone exports-map mixed-format interop probe now also mirrors `.js` input on `check` / `build` / `run`, keeping the exports-map resolver evidence aligned with first-class JavaScript compilation instead of only the TypeScript lane. The default standalone string-export corpus now also has a dedicated `.js` `test` probe, so the pure-JS package evidence now covers the testable rung as well as the executable rung.
- Progress: the default standalone module-entry corpus now also mirrors `.js` input for both the simple module-entry and module-entry-chain slices on `check` / `build` / `run`, keeping the first-class-JavaScript package evidence aligned with the same module-resolution paths as the TypeScript lane.
- Progress: the Deno package corpus now also exercises the documented Deno build surface for the same Deno-host package set that already has Deno `check` / `run` coverage, so analysis, build, and execution evidence stays aligned for the current Deno compatibility slice. The package-corpus matrix drift test now also pins the Deno host-control and JSR `.js` rows explicitly, keeping the Deno slice deterministic alongside the browser/default/Node rows.
- Progress: the Deno package corpus now also exercises a canonical `jsr:@std/path` package fixture materialized at `node_modules/@std/path` on the Deno surface, keeping the `jsr:` registry prefix and on-disk path mapping honest in the package-resolution evidence. The same Deno host-control and JSR fixtures now also mirror `.js` input on `check` / `build` / `run`, keeping the Deno package-corpus slice aligned with first-class JavaScript compilation instead of only the TypeScript lane.

### 8.3 Browser package deployability

- Expand browser-targeted package checks and `build --bundle` smoke tests.
- Keep deployable-through-host claims distinct from standalone browser executable claims.
- Progress: browser-targeted `.js` package entrypoint coverage now includes the minimized mixed CommonJS/ESM interop slice plus the `vue/runtime-dom` browser branch, typed export branches, browser replacement-map JS-entrypoint coverage, browser dual-exports JS-entrypoint coverage, browser pattern-exports JS-entrypoint coverage, scoped package exports-map JS-entrypoint coverage, and module-entry-chain JS-entrypoint coverage, and the browser runtime corpus now also covers the browser module-entry `.js` slice and its inherited-browser `apiSurface` path, so the browser corpus keeps the first-class-JavaScript path honest across replacement-map, typed export, interop, pattern-exports, dual-exports, scoped exports-map, chained-module, and module-entry cases. The browser corpus now also exercises the host-heavier `@mariozechner/pi-coding-agent` package-content probe on `.js` input for `check` and `build --bundle`, and the browser runtime corpus now also exercises the same package-content probe on `.js` input for `run` and `test`, plus the typed export branch packages on `.js` input for both browser runtime commands, so the browser deployability lane distinguishes package content from the separate standalone bin-entrypoint rejection while the runtime harness matrix stays explicit about the same package shape too. Browser-blocked packages now also reject on `.js` input in the browser `check` / `build --bundle` lanes, including JSON-output coverage, so the browser-path exclusion contract stays explicit even when the package source class is first-class JavaScript. The browser runtime corpus now also records the browser-condition / browser-deno preference `.js` slice and now exercises that probe on the browser `run` path as well as `test`, so the runtime harness matrix stays explicit about the browser-vs-deno preference case too. The browser runtime corpus now also exercises browser replacement-map packages on `.js` input for `run` and `test`, including the inherited browser `apiSurface` path, keeping the runtime harness slice aligned with the same browser-map resolution branch too. The browser runtime corpus now also exercises browser pattern-exports packages on `.js` input for `run` and `test`, including the `hono/client` and `solid-js/web` slices, and now also exercises the inherited browser `apiSurface` path in `.js` input for `run` and `test`, keeping the runtime harness slice aligned with the same browser pattern-export branch too. The matrix drift test now also pins the browser replacement-map, string-entry, string-export, browser-condition export, browser dual-exports, browser pattern-exports, and browser scoped exports-map `.js` rows, plus the inherited-browser pattern-exports row, and it now also pins the browser runtime module-entry `.js` rows, so the browser deployability snapshot stays aligned with those first-class-JavaScript package shapes.
- Progress: browser-targeted exports-map package corpus coverage now also mirrors the same package set onto `.js` input, and the package-corpus matrix now records the browser exports-map `.js` rows explicitly, including the inherited browser `apiSurface` row, keeping the browser analysis/build lane aligned with first-class JavaScript compilation instead of only the TypeScript lane.
- Progress: the browser package corpus now also exercises the canonical pure-JS `semver` probe on `.js` input, and the browser runtime corpus now also exercises the same probe on `.js` input for `run` and `test` when the browser harness command is configured, including JSON-output coverage for both browser runtime semver lanes and the inherited browser `apiSurface` path, keeping the support-rung evidence aligned with the browser-targeted first-class-JavaScript path instead of only the TypeScript lane.
- Progress: the browser package corpus now also mirrors the browser module-entry fixture onto `.js` input, keeping the browser deployability evidence aligned across the JS and TS lanes for the module-entry shape too. The browser runtime corpus now also exercises module-only packages and module-entry-chain packages on `.js` input for both `run` and `test` when the browser harness command is configured, and the module-entry-chain slice now also exercises the inherited browser `apiSurface` path in `.js` input with JSON-output coverage, so the browser execution evidence now spans those module-only and chained-module shapes as well. The browser module-only runtime slice now also exercises the inherited browser `apiSurface` path in `.js` input for both `run` and `test`, including JSON-output coverage, so the browser harness snapshot stays aligned across direct and inherited context selection too. The browser runtime typed export branch slice now also exercises the inherited browser `apiSurface` path on `.js` input for both `run` and `test`, keeping the browser harness snapshot aligned across direct and inherited context selection for typed export branches too.
- Progress: the browser package corpus now also mirrors the browser string-entry, string-export, browser-condition export, browser dual-exports, web-baseline primitive, and internal browser-rewrite fixtures onto `.js` input, keeping the browser deployability evidence aligned across the JS and TS lanes.
- Progress: the browser package corpus now also exercises browser condition / browser-deno preference packages on the browser-targeted `check` / `build --bundle` path, including mirrored `.js` input, so the browser resolution surface stays aligned with the runtime browser-vs-deno probe.
- Progress: the browser runtime corpus now also mirrors the browser package fixtures on `.js` input for both `run` and `test`, including the browser-vs-deno condition-preference probe in both TS and `.js` input, and now also exercises browser dual-exports packages plus the browser typed export branch and browser web-baseline package slices on `.js` input for both `run` and `test`, plus the same web-baseline slice on the inherited browser `apiSurface` path. The generic browser package fixtures now also exercise the inherited browser `apiSurface` path in `.js` input for both `run` and `test`, and the browser dual-exports runtime slice now also exercises the inherited browser `apiSurface` path in `.js` input for both `run` and `test`, keeping browser deployability evidence aligned across the JS and TS lanes. The browser runtime corpus now also exercises browser replacement-map packages on `.js` input for `run` and `test`, and now also has JSON-output coverage for the direct and inherited browser `apiSurface` paths, and browser string-entry and string-export packages on `.js` input for `run` and `test`, with the browser string-entry slice now also exercising the inherited browser `apiSurface` path and the browser string-export slice now also exercising the inherited browser `apiSurface` path, keeping the browser harness slice aligned with those browser package shapes too. The browser runtime corpus now also exercises browser module-only packages and browser module-entry-chain packages on `.js` input for `run` and `test`, and now also has JSON-output coverage for the direct browser-harness browser module-only slice, while the browser runtime corpus also exercises browser module-entry-chain packages on the inherited browser `apiSurface` path in `.js` input with JSON-output coverage, and browser exports-map packages on `.js` input for both `run` and `test`, with JSON-output coverage for that exports-map slice as well, and the package-corpus matrix drift test now also pins the browser module-only inherited-browser row, the browser module-entry-chain inherited-browser row, the browser dual-exports inherited-browser row, the browser exports-map direct row, and the browser exports-map inherited-browser row, so the browser execution snapshot stays explicit for those module-resolution shapes as well. The browser runtime corpus now also exercises browser-condition / browser-deno preference packages on the inherited browser `apiSurface` path in `.js` input for `run` and `test`, keeping the browser-vs-deno preference split explicit on the inherited harness path too, and the matrix drift test now also pins that inherited-browser row. The browser runtime `@mariozechner/pi-coding-agent` package-content probe now also covers the inherited browser `apiSurface` path in `.js` input for both `run` and `test`, and the same direct and inherited browser-harness slices now also have JSON-output coverage, so the corpus keeps the direct and inherited browser context split explicit. The browser runtime corpus now also has JSON-output coverage for the browser-deno preference package slice on the direct and inherited browser `apiSurface` paths in `.js` input, keeping the browser-requested harness evidence aligned with the existing text-mode package-corpus matrix, and the matrix drift test now also pins the `@mariozechner/pi-coding-agent` binary-entrypoint row on the default standalone and Node surfaces.

### 8.4 Registry-analysis evolution

- Extend `package-effects` / `package-audit` only through explicit command/schema revisions.
- Do not add batch, raw-URL, local-path, or project-graph behavior accidentally.
- Progress: `package-effects` now also has native JSON pretty-output coverage and quiet-mode coverage, keeping the native-JSON registry-analysis payload contract explicit across presentation controls as well as the inherited-analysis lane.
- Progress: `package-effects` and `package-audit` now also reject raw URLs and local/relative path targets in both text and JSON output modes, keeping the single registry-identifier target contract explicit alongside the already-covered explicit-version rejection. The install-command lifecycle-hook lane now also rejects `--allow-scripts` for raw-URL and JSR targets in both text and JSON output modes, keeping the install-time npm hook path separate from registry-only and raw-URL install shapes.
- Progress: `package-audit` now also rejects `--api`, `--compat`, and `--wasm-threads` in both text and JSON output modes, keeping the command-shape gate aligned with the shared package-analysis-specific-flag contract. The same package-audit lane now also suppresses human output under `--quiet` even when inheriting `eval` plus `wasm-threads`, and the inherited compat/thread context now also has JSON-output quiet coverage; the direct `--wasm-threads` rejection now also has matching JSON-output coverage.

## Exit gate

- Package support statements name exact rung and context.
- Corpus results are deterministic and reproducible.
- Missing/stale dependency state remains `E6004`; non-install commands do not auto-repair.
