# Spec Gap Map

This map lists active implementation goals implied by the specs after the current repository baseline. It is not an availability matrix; use `specs/19-feature-maturity.md` for public support status.

## Semantics and frontend depth

Owners: `specs/02-lexer-parser.md`, `specs/03-ast.md`, `specs/04-type-system.md`, `specs/05-ir.md`, `specs/10-runtime.md`, `specs/16-testing.md`.

Remaining goals:

- Expand ECMA-262 conformance for supported syntax and runtime semantics; async/await sequencing now has a dedicated runtime-smoke regression, array iteration now has a minimized gated `for...of` rejection fixture plus mirrored `.js` input-class rejection coverage and `build` / `test` CLI smoke tests, including JSON-output coverage on the JS-input `check` / `build` lanes, and the browser-targeted `check` / `build --bundle` lanes now also reject the same gate in TS and `.js` input with matching JSON-output coverage. The same iterator-lowering gate now also rejects `for await...of` array iteration in `check` / `build` / `run` / `test` on TS and `.js` input, plus browser-targeted `check` / `build --bundle` and browser-requested `run` / `test` coverage in TS and `.js` input, keeping the async-iteration syntax row separate from the supported-await grammar row, minimized mixed CommonJS/ESM package interop fixtures now cover the default standalone surface — including an exports-map variant — and `.js` base-library artifact coverage now exercises the first-class JavaScript build lane with JSON-output coverage too; the default standalone `test` corpus now also has a minimized mixed-format interop fixture, browser-targeted `.js` entrypoints now have a minimized mixed CommonJS/ESM interop fixture too, concise arrow function expression parsing now has parser coverage, and generator-lowering gate coverage now also includes minimized `.js` input fixtures on the `check`/`build` smoke lanes plus mirrored `run`/`test` `.js` generator-gate fixtures, including mirrored async-generator `.js` fixtures, including JSON-output coverage for the `.js` async-generator function-expression gate on `check` / `build`, while non-literal dynamic import rejection now also has mirrored `.js` coverage on the `check` / `build` lanes. Promise.allSettled now also rejects with the canonical `E5506` availability diagnostic instead of a placeholder fallback, keeping the unsupported promise-combinator gate explicit alongside the other rejected-by-default runtime shapes, and the standalone `check` / `run` / `test` JS-input coverage now mirrors that gate with JSON-output coverage as well; the standalone build helper and runtime-smoke check lane now also reject bracketed and `globalThis`-bracketed `Promise.allSettled` source forms, keeping that gate explicit across the common property-access variants. The browser-targeted `check` / `build --bundle` lanes and browser-requested `run` / `test` harness now also mirror the same gate in `.js` input, including JSON-output coverage where applicable. The read-only `Deno.permissions.query(...)` subset now has runtime-smoke coverage for all four documented descriptor names (`read`, `write`, `env`, `net`) across both direct and computed JS input forms, and the `check`- and `build`-lane JS-input regressions now cover the same supported subset. Supported const-bound permission-query descriptors now also have dedicated JS-input `check` / `build` coverage, keeping the first-class JavaScript path explicit on the accepted side too; the dedicated JS-input permission-query corpus in `crates/kali_cli/tests/permission_query_js_input.rs` now also mirrors `run` acceptance and JSON-output coverage for the same const-bound subset, and the inherited `globalThis.Deno.permissions.query` / `globalThis.Deno.permissions["query"]` path now mirrors the same support slice too. Unsupported permission-query descriptors such as `ffi` and `sys` now also have dedicated TS and `.js` `check` / `build` rejection coverage, including JSON-output coverage and bracketed `globalThis["Deno"]["permissions"]["query"](...)` source forms plus the inherited/direct bracketed `permissions.query` forms, so the first-class JavaScript path stays explicit on the gated side too. The recognized-but-unavailable permission-escalation members now also cover statically-known bracketed forms such as `Deno.permissions["request"]()` / `Deno.permissions["revoke"]()` and their `globalThis["Deno"]["permissions"]` counterparts on the TS and `.js` smoke lanes, and the shared runtime_smoke harness now mirrors the same bracketed-form coverage in the broader smoke lane too, including direct bracketed `Deno["permissions"]["query"]` / `Deno.permissions["query"]` source forms in TS and `.js` input. The permission-escalation source-shape regressions now also cover bracketed method access on the direct and inherited Deno permission surface (`Deno.permissions["request"]()` / `revoke()` and `globalThis.Deno.permissions["request"]()` / `revoke()`), and the late-compatibility source fixtures now also keep the mixed inherited bracket/dot `globalThis["Deno"].permissions["request"]()` / `revoke()` spellings explicit alongside the existing bracketed permission-object variants; parser integration now also exercises the mixed inherited bracket/dot permission-escalation spellings directly, and the JS-input late-compatibility fixtures now also pin `globalThis["Deno"].permissions.request()` / `revoke()` alongside the bracketed variants. The late object-model slice now also mirrors `Object.hasOwn` / `Object.prototype.hasOwnProperty.call` in the same broad smoke lane, so the own-property-helper rejection path stays explicit alongside the other late object-model members. The sandbox effect-analysis tests now also cover the full documented `Deno.permissions.query(...)` descriptor subset in direct and bracketed forms across both TS and `.js` input, preserving the computed-host-access boundary alongside the runtime-smoke coverage. The shared runtime_smoke lane now also mirrors recognized-but-unavailable permission-escalation members into `.js` `check` / `build` rejection coverage, including direct, computed, and bracketed forms with JSON-output assertions, and the inherited `globalThis.Deno.permissions.query` / `globalThis.Deno.permissions["query"]` path now mirrors the same rejected-by-default slice too, keeping the first-class JavaScript sandbox-validation boundary explicit too. The shared runtime_smoke late-process-control helper now also pins direct bracketed `process["pid"]` / `process["cwd"]` / `process["chdir"]` / `process["exit"]` and inherited `globalThis.process[...]` spellings in the broad smoke lane. The default standalone late-compatibility smoke now also mirrors bracketed `globalThis["Intl"]["DateTimeFormat"]` source coverage on `.js` input, keeping the Intl bracketed-path regression mirrored with the rest of the matrix. Browser bundle runtime smoke now also exercises the dynamic-import loader for `.js` input, including directory-index targets, and now also normalizes runtime dynamic import specifiers in `.js` input, and the runtime-smoke regression set now also mirrors BigInt addition into `.js` run/test coverage, keeping the linked-graph browser bundle path mirrored across JS and TS lanes. The browser-requested run/test harness now also has JSON-output coverage for integer-like key ordering in both TS and `.js` input, and the inherited browser-api-surface path now also mirrors the same integer-like key-ordering coverage in both TS and `.js` input, keeping the machine-readable browser-requested ordering baseline aligned with the existing execution smoke. Browser bundle `Object.keys()` / `Object.entries()` / `Object.values()` smoke now also exercises array-literal-arguments in both `.ts` and `.js` inputs, keeping the browser bundle path aligned with the same basic enumeration regression the standalone runtime-smoke suite already pins. The build-discovery dynamic-import fixtures now also mirror comment/string-substring ignore coverage onto `.js`, `.jsx`, and `.tsx` input and add a parenthesized const-bound target-resolution case on `.ts`, `.js`, `.tsx`, and `.jsx` input so the scanner stays mirrored across the extended source-class matrix. Directory-index dynamic-import coverage now also has explicit `.tsx` and `.jsx` coverage in both the type-resolution and build-discovery layers, keeping the `index.*` claim honest across the extended source-class matrix. Object literal expressions with identifier, string, and numeric property names now also parse in the frontend, including shorthand identifier properties and literal computed keys that canonicalize to static property names, so ordinary object-expression syntax no longer trips the parser before the runtime and enumeration regressions can exercise it. Numeric property names now also lower as quoted string literals in the HIR layer, keeping property-key canonicalization separate from numeric value literals and integer-like ordering is now covered by the same object-enumeration smoke matrix. The optimizer still re-runs object-enumeration folding after binding specialization using collected constant bindings so more resolved literal shapes are visible to the enumeration helper, and the codegen aggregate-literal path now carries the 4-key ordering cases instead of rejecting them. Nullish coalescing now also parses as a gated binary operator token and rejects explicitly with `E5506` instead of silently falling through to the wrong runtime result, and that rejection is now mirrored in the browser-targeted `check` / `build --bundle` JS-input lane with JSON-output coverage too. The standalone `effects` lane now also accepts the same nullish coalescing JS input in the default analysis context, including JSON-output coverage, so the analysis-only acceptance path stays aligned with the browser-analysis acceptance. The type-resolution layer now also has a direct `.js` generator-function lowering gate in `crates/kali_types/src/tests.rs`, keeping the generator-lowering follow-up visible one layer earlier than the CLI smoke gates. Supported const-bound permission-query descriptors now also have dedicated JS-input `check` / `build` coverage plus `run` / `test` runtime acceptance coverage in `crates/kali_cli/tests/runtime_smoke.rs`, keeping the first-class JavaScript path explicit on the accepted side too. The browser runtime mixed-format interop rows now also carry JSON-output coverage on the direct and inherited browser-harness paths. The `check --sandbox` and `build --sandbox` lanes now also carry JS-input and JSON-output coverage for the same inferred-effect-vs-policy `fetch(...)` rejection, keeping the compile-time sandbox comparison path mirrored across both analysis commands.
- The build_source_file TS- and JS-input smoke now also mirrors direct bracketed `Deno["env"]["get"]` and `Deno["permissions"]["query"]` source forms, and now also mirrors the bracketed `globalThis["Deno"]["env"]["get"]` form plus the inherited `globalThis["Deno"]["permissions"]["query"]` read-descriptor form; the build_source_file TS-input smoke now also covers direct bracketed `Deno["env"]["get"]` plus the `globalThis["Deno"]["env"]["get"]` form, and the JS-input build path now also covers the same bracketed `globalThis["Deno"]["env"]["get"]` form plus the mixed dot/bracket `globalThis.Deno["env"]["get"]` and inherited `globalThis["Deno"].env["get"]` spellings, keeping the executable build-path mirror aligned with the runtime-smoke source-shape matrix; it now also rejects broader Intl members such as `Intl.DateTimeFormat` / `Intl.RelativeTimeFormat` / `Intl.PluralRules` / `Intl.Collator` / `Intl.DisplayNames` / `Intl.Segmenter` / `Intl.Locale` alongside bracketed `globalThis["Intl"]["DateTimeFormat"]` / `globalThis["Intl"]["RelativeTimeFormat"]` / `globalThis["Intl"]["Collator"]` / `globalThis["Intl"]["DisplayNames"]` / `globalThis["Intl"]["Segmenter"]` / `globalThis["Intl"]["Locale"]` in `.ts`, `.js`, `.jsx`, and `.tsx` input. The same helper now also rejects `Deno.env.toObject` in `.ts` and `.js` input, and still rejects `Promise.allSettled` in `.js` input, keeping the compile-time promise-combinator gate aligned with the codegen/runtime smoke. The same helper now also rejects unsupported permission-query descriptors (`ffi` and `sys`) in `.js` input on the direct and bracketed `globalThis["Deno"]["permissions"]["query"]` source forms, now also does the same in `.ts` input on those same source forms, and now also rejects the direct and inherited bracketed `permissions.query` spellings (`Deno["permissions"].query`, `Deno["permissions"]["query"]`, `globalThis.Deno["permissions"].query`, and `globalThis["Deno"].permissions.query`) so the compile-time mirror stays aligned with the runtime rejection path too. The build_source_file mirror now also rejects the mixed inherited bracket/dot `globalThis["Deno"].permissions["request"]()` / `revoke()` spellings in `.ts` and `.js` input, keeping the compile-time mirror aligned across input classes as well.
- Strengthen TypeScript/JavaScript inference within the bounded inference contract. Current progress: unresolved named exports and default exports at a module public boundary now fail with the canonical undefined-identifier diagnostic in `crates/kali_types/src/tests.rs`, including mirrored `.js` input coverage for the same public-boundary rejection, the shared type-annotation resolution fixtures now also mirror `.js` input so the annotation-required boundary stays exercised there too, and the phase-3-budgeted cross-module inference fixtures now have mirrored `.js` coverage for both the direct call-chain and the explicit specialization-cap variants in `crates/kali_cli/tests/runtime_smoke.rs`; the default standalone execution surface now also mirrors the same direct call-chain and explicit specialization-cap patterns in `.js` input there as well, including the standalone `.js` specialization-cap follow-up on the same module chain, and `crates/kali_types/src/tests.rs` now also covers an unresolved public-export alias plus missing re-export sources on `.js` input so the bounded-inference gate stays exercised at the module boundary. The same JS-input module-boundary matrix now also exercises an unresolved identifier inside a default-exported function body through both the type-resolution layer and the `check` CLI smoke lane, keeping the public-boundary body-resolution path aligned with the first-class JavaScript contract, and the same JS inference lane now also covers a deeper `Promise<Array<Promise<Foo>>>` nesting case so the bounded-inference happy path keeps a second nested-generic shape pinned.
- Keep open-ended cross-module/public-API inference gated until a later solver/evidence lane exists.
- Literal-string `import()` over the already-linked graph is implemented in the current snapshot; standalone `run` / `test` smoke now also covers literal-string dynamic imports in both TS and `.js` input, including JSON-output coverage for the `test` lane, and now also mirrors the same literal-string dynamic-import path in `.jsx` and `.tsx` input with matching human and JSON coverage, the browser-requested `run` / `test` harness now also mirrors the same directory-index lowering path in TS input with JSON-output coverage on the `test` lane, build-discovery now resolves `.js` literal dynamic import chunks, and non-literal dynamic import rejection now also has mirrored `.js` coverage on the standalone `run` / `test` smoke paths, including JSON-output coverage, so the later compatibility boundary stays explicit on the execution side too.
- Improve minimized regression fixtures for supported dynamic/object/runtime behavior. Current progress: console error/warn/info/debug routing now also has mirrored `console.assert()` false-branch reporting coverage in `crates/kali_cli/tests/runtime_smoke.rs`, the runtime-smoke regression set now also mirrors BigInt addition into `.js` run/test coverage, arithmetic precedence / array literal length now have mirrored `.js` `test`-path smoke coverage too, and function call return semantics plus relational comparison semantics now also have mirrored `.js` run/test coverage, keeping the supported console and basic-runtime baseline aligned with first-class JavaScript compilation. The same console-routing slice now also has a mirrored `.js` `test`-path regression so both runner modes keep the output routing contract explicit. Optional chaining now also has direct `.js` run/test smoke coverage, and `effects` browser-analysis coverage now also mirrors explicit and inherited browser API-surface requests onto `.js` input, so the browser analysis context itself stays aligned with the first-class JavaScript path. The dedicated `Reflect.ownKeys` JS-input `test` lane now also closes the corresponding object-enumeration edge. The browser-targeted `effects` lane now also has JSON-output rejection coverage for the threaded-profile gate, keeping the browser `--wasm-threads` boundary explicit in both text and JSON output modes. The browser-requested `run` / `test` lane now also exercises array-literal-arguments in `.js` input alongside the existing object-enumeration coverage, the direct browser-requested overwrite/delete-reinsert ordering mirrors now also exist in `.ts` input, and the lane now also exercises boolean conjunction/disjunction semantics and strict equality / inequality semantics in both TS and `.js` input classes, while the browser-requested JS-input boolean-logic lane now also has JSON-output coverage, and the browser-requested run/test math suite now also has JSON-output coverage in both TS and `.js` inputs, and the dedicated browser-requested `Math.trunc()` smoke now also has JSON-output coverage on the `run` and `test` lanes for both TS and `.js` input, closing the last browser-harness input-class gap for the documented math suite, and the browser-requested `Math.ceil()` smoke now also covers the direct `.ts` input path, including JSON-output coverage, and the browser-requested `test` lane now also has JSON-output coverage in `.ts` input, plus a dedicated JSON-output `Math.clz32()` mirror in `.js` input, and the plain-text `.ts` `Math.clz32()` `test` mirror now also exists, while the inherited browser-api-surface path now also covers `Math.ceil()` in `.ts` and `.js` input, including JSON-output coverage, and now also covers `Math.clz32()` on both `run` and `test` in `.js` input. The default standalone `run` / `test` smoke now also mirrors `Math.imul()`, `Math.trunc()`, and `Math.ceil()` in `.js` input, keeping the basic built-in lane aligned with the existing runtime smoke evidence, and the integer-only math lowering now explicitly rejects non-integer literal operands before they can fall through to literal encoding; standalone `run` / `test` JSON smoke now also pins that non-integer-literal gate in `.js` input. Unsupported `Math` member calls such as `Math.round()` and `Math.floor()` now reject with `E5506` at codegen instead of silently collapsing to a zero placeholder, `crates/kali_codegen/src/tests.rs` now also pins the `Math.round()` rejection directly, and the browser-targeted `check` and `build --bundle` lanes plus the browser-requested `run` / `test` lane now also carry matching `.js` rejection coverage for that gate, and the browser-targeted `check` lane now also has matching JSON-output rejection coverage in `.js` input; the browser-targeted `build --bundle` lane now also has matching JSON-output rejection coverage in `.js` input, and the browser-requested `run` lane now also has matching JSON-output rejection coverage in `.js` input, while the inherited browser-api-surface browser-harness lane now also carries matching `.js` rejection coverage on `run` / `test`, including JSON-output coverage on the inherited `run` and `test` paths, and the inherited browser API-surface `build --bundle` lane now also rejects the same unsupported math member calls in `.js` input, including JSON-output coverage. The browser-requested `run` / `test` lane now also exercises `try/catch` exception handling and `try/finally` sequencing in both TS and `.js` inputs under the configured browser harness, including JSON-output coverage, and now also has inherited-browser-api-surface coverage for `performance.now()` monotonic ordering in `.ts` and `.js` input; the browser bundle lane now also mirrors boolean conjunction/disjunction semantics in both TS and `.js` inputs and now also covers console-level routing and `console.assert()` false-branch reporting in both TS and `.js` inputs plus basic strict equality / inequality semantics in both TS and `.js` inputs, and now also has inherited-browser-api-surface coverage for the same `performance.now()` timing baseline in `.ts` and `.js` input plus JSON-output coverage for that inherited browser bundle lane, and the direct browser bundle JSON lane now also pins the same monotonic-ordering regression in `.js` input; the browser-requested `test` JSON lane now also pins the same `.js` timing baseline. The inherited browser `apiSurface` path now also exercises the `Math.max()` / `Math.min()` / `Math.abs()` / `Math.sign()` / `Math.imul()` suite in `.ts` and `.js` input for both `run` and `test`, keeping the browser-harness math evidence symmetric across the inherited and direct browser-requested paths, and now also has JSON-output coverage for that inherited TS/JS-input math suite on both `run` and `test`. The browser bundle queueMicrotask ordering now also has JSON-output coverage in `.js` input, keeping the browser-bundle machine-readable envelope aligned with the existing execution smoke. Object literal parsing now also handles identifier/string/number keys plus shorthand identifiers, which unblocks the existing runtime-smoke object-enumeration fixtures from failing in the parser. Standalone JSON-output coverage now also exercises overwrite/delete-reinsert ordering in both TS and `.js` input, keeping the object-enumeration baseline aligned with the existing smoke matrix. The phase-6 dashboard drift net now also pins the basic object-enumeration rows plus the browser-requested and browser-bundle object-enumeration rows explicitly, the basic optional chaining row, and the basic BigInt addition rows explicitly, so the deterministic snapshot stays aligned with the execution smoke. The same dashboard drift net now also pins the browser-bundle strict-equality row explicitly, keeping the browser comparison slice visible alongside the other browser runtime rows. The browser-requested run/test strict-equality lane now also has JSON-output coverage in both TS and `.js` inputs.
- Generator lowering still needs a dedicated implementation packet; the parser now accepts async function declarations/expressions, async arrow function expressions, async generator syntax, and generator syntax, including delegated `yield*`, but lowering remains gated at resolution/check time. Current runtime smoke coverage now includes both sync and async generator gate coverage across `check`, `build`, `run`, and `test`, including async generator function-expression coverage plus mirrored `.js` generator-gate fixtures on the `check`/`build` and `run`/`test` lanes, and the `run`/`test` `.js` generator-gate fixtures now also have matching JSON-output coverage, the generator-function lowering gate now also has matching JSON-output coverage on the `build`, `run`, and `test` JS-input lanes, the async-generator declaration gate now also has matching JSON-output coverage on the `check` / `build` / `run` / `test` JS-input lanes, and the generator-delegating `yield*` gate now also has matching JSON-output coverage on the `build`, `run`, and `test` JS-input lanes, while other regression cases remain limited to simpler examples such as arithmetic, exceptions (including try/finally sequencing), object enumeration, and built-ins. The CLI smoke matrix now also mirrors the same generator-function lowering rejection into `.jsx` / `.tsx` `check` / `build` / `run` / `test` inputs, keeping the minimized gate fixtures aligned across the extended source-class matrix. Arrow-function return-type annotations now also parse and resolve their type references, keeping the TypeScript return-annotation path explicit on the same parser/checker surface. The browser-targeted `check` / `build --bundle` smoke lane now also pins the same generator-lowering rejection in the browser analysis/build context, including mirrored `.js` input fixtures on both browser lanes plus JSON-output coverage for the browser-analysis and browser-bundle JS-input `yield*` gates. The browser-targeted `yield*` rejection is now mirrored there too in both TS and `.js` inputs. The browser bundle JSON lane now also carries the matching TS-input `yield*` regression for the same browser-bundle machine-readable gate. The type-resolution layer now also has direct `.ts` and `.js` generator-function lowering gates, and the lower-level `build_source_file` helper now mirrors that same generator gate in `.ts`, `.js`, `.jsx`, and `.tsx` input, so the compile-time mirror stays aligned with the CLI smoke matrix instead of drifting back to a parser-only check.
- Browser-targeted `check` / `build --bundle` now also reject `for...of` array iteration in TS and `.js` input with matching JSON-output coverage, keeping the browser analysis/bundle gate mirrored with the existing rejected-by-default iterator-lowering row.
- `Object.values()` is now covered alongside the already-covered `Object.keys()` / `Object.entries()` pair, and the runtime-smoke regression set now also checks overwrite/delete-reinsert ordering for that trio, including mirrored `.js` input coverage; keep the object-enumeration smoke regressions in sync while integer-like key-ordering now has positive runtime, test, and browser-bundle coverage across TS and JS input classes.
- Browser bundle runtime smoke now also exercises `Math.max()` / `Math.min()` / `Math.abs()` / `Math.sign()` / `Math.imul()` / `Math.clz32()` / `Math.trunc()` semantics in both TS and `.js` input, extending the browser bundle evidence beyond the existing dynamic-import and browser crypto smoke cases; the `Math.trunc()` and `Math.clz32()` JSON-output lanes now also cover `.ts` input alongside `.js` input.
- Browser bundle runtime smoke now also exercises async/await sequencing in both TS and `.js` input, keeping the browser bundle lane aligned with the same await-order regression the standalone runtime smoke already pins. Browser bundle crypto web API smoke now also mirrors `crypto.getRandomValues()` plus `crypto.subtle.digest()` / `crypto.randomUUID()` in both TS and `.js` inputs, including JSON-output coverage, extending the browser bundle evidence beyond the existing dynamic-import and browser crypto smoke cases. Browser bundle try/catch sequencing now also has dedicated TS and `.js` coverage, keeping browser exception handling aligned with the same first-class-JavaScript bundle path.
- `Math.sign()`, `Math.imul()`, and `Math.clz32()` now round out the existing `Math.max()` / `Math.min()` / `Math.abs()` built-in math coverage on the runtime-smoke path; keep the six-function built-in regression set in sync with the corresponding codegen import tests, including mirrored `.js` input coverage.
- Current progress: the documented `--compat eval` path now also has `check` and `build` acceptance coverage for the `Function()` constructor on `.js` input, keeping the Phase-4 compatibility gate explicit on the analysis/build side as well.

- Current progress: the package-corpus matrix drift test now also pins the default standalone exports-map, module-entry, and web-baseline primitive `.js` rows exactly once, keeping the default standalone package-corpus snapshot aligned with the active matrix alongside the browser and Node/Deno slices. The default standalone pure-JS `semver` probe now also carries JSON-output coverage on `.js` input across `check` / `build` / `run` / `test`, keeping that pure-JS package rung machine-readable on the default standalone surface too.

## Runtime, host, and platform expansion

Owners: `specs/09-sandboxing.md`, `specs/10-runtime.md`, `specs/11-standard-apis.md`, `specs/18-schemas.md`, `specs/19-feature-maturity.md`.

Remaining goals:

- Extend the threaded runtime profile beyond the supported `run`/`test` execution paths into the remaining analysis/build contexts; guest-facing thread-spawn host import plumbing is now in place, the runtime test suite now also pins the zero-budget thread-spawn host-import rejection path, and the run/test threaded-profile acceptance lane now also mirrors first-class `.js` input on both explicit and inherited forms, browser-targeted `effects` and inherited-browser-context `package-effects` now have regression coverage for the remaining `--wasm-threads` gates, including a human-output regression for the browser-context `package-effects` rejection, and browser-build smoke coverage now pins the remaining browser-targeted rejection paths in both text and JSON output modes, while fuller lowering / multi-worker execution semantics still need follow-up work. The threaded runtime profile now also reaches the `.js` `build --lib` / `build --capi` / `build --component` artifact modes, and inherited `runtimeProfiles = ["wasm-threads"]` now also reaches those JS-input artifact modes, so the remaining build-context coverage stays explicit beyond the generic `build` lane. The standalone `build --component` JSON smoke now also covers the explicit JS-input threaded-profile case, keeping the component artifact path machine-readable on the default standalone surface too. The standalone `build --lib` / `build --capi` JSON smoke now also covers explicit and inherited JS-input threaded-profile cases, keeping the other artifact-mode lanes machine-readable too. The browser bundle `--format cjs` path now also has first-class `.js` input coverage in both explicit and inherited browser API-surface modes, and the same JS-input lane now also accepts `--sandbox` with the browser policy gate, keeping the alternate browser wrapper format aligned with the same JS-input evidence lane. The zero-budget sandbox policy acceptance matrix now also mirrors those JS-input artifact modes for `build --sandbox` in both text and JSON output modes. The browser-targeted and browser-harness threaded-runtime rejection lanes now also mirror `.ts` input in the explicit and inherited browser selections, keeping the later-compatibility gate aligned across both source classes. The browser-targeted `effects` lane now also has JSON-output rejection coverage for the threaded-profile gate, so the browser-analysis boundary stays explicit in both human and machine-readable output. The browser-targeted `check` / `build` rejection path for `--wasm-threads` now also mirrors `.js` input on both text and JSON output modes, keeping the browser-analysis/build boundary aligned with first-class JavaScript compilation. The zero-capable thread-budget contract is now also mirrored in JSON smoke coverage for `run` and `test`, and the zero-budget sandbox policy path now also accepts `check --sandbox` and `build --sandbox` in both text and JSON output modes with the same `resources.maxThreads = 0` / `resources.maxSpawnedProcesses = 0` baseline, so the acceptance path for `--max-threads 0` stays explicit alongside the existing positive-budget rejections. The zero-capable spawned-process budget contract now also has JSON smoke coverage for `run` and `test`, so the acceptance path for `--max-spawned-processes 0` stays explicit alongside the existing positive-budget rejections. The runtime-smoke matrix now also rejects positive `--max-spawned-processes` overrides in both text and JSON modes, keeping the later subprocess-budget gate explicit until subprocess support itself exists. The runtime-smoke rejection set now also mirrors `SharedArrayBuffer` / `Atomics` onto `.js` input for the supported `check` / `run` / `test` paths, the browser-targeted browser API-surface `check` / `build --bundle` path now also rejects those threaded globals in `.js` input, and the browser late-compatibility source fixtures now also include bracketed `globalThis["SharedArrayBuffer"]` and `globalThis["Atomics"]` spellings so the source-shape regression stays explicit alongside the direct-form diagnostics, and the shared `runtime_smoke` source helper now also asserts those bracketed spellings in the threaded-runtime source-shape regression, the browser-requested run/test acceptance lane now also exercises overwrite-delete-reinsert ordering object enumeration and queueMicrotask ordering on `.js` input, and the browser-requested harness path now also rejects threaded globals in both text and JSON modes while the inherited browser `apiSurface` analysis/build path now also stays covered; the browser-requested `run` / `test` acceptance lane now also keeps the zero-capable `--max-threads 0` / `--max-spawned-processes 0` pair open on the browser-requested `.js` surface, so the acceptance path stays explicit alongside the existing positive-budget rejections. Late process-control members remain rejected in browser run/test harness coverage. The non-browser `check` path now also mirrors the phase-3 Deno host-effect rejection into `.js` input with JSON output, the non-browser `run` / `test` sandbox lane now also mirrors that same phase-3 Deno host-effect rejection into `.js` input with JSON output, and the non-browser `build --sandbox` path now mirrors that same phase-3 Deno host-effect rejection into `.js` input with JSON output, keeping the threaded-runtime and browser-requested execution boundaries mirrored across TS and JS inputs. The lower-level runtime timer host tests now also reject negative timer delays deterministically for both `setTimeout` and `setInterval`, and the sandbox policy tests now also cover timer scheduling denials, timeout ceilings, and active-timer ceilings, keeping the scheduler and scheduled-callback policy boundaries explicit alongside the existing timer-clearing regressions.
- Standalone `run --api browser` / `test --api browser` only after a real browser-runtime contract exists; the current browser-harness evidence now also covers inherited browser `apiSurface` configs for both commands when that harness override is present, including browser package-resolution coverage on the inherited path and mirrored `.js` package-resolution smoke for the browser harness path. The basic browser-requested `run` / `test` acceptance lane now also mirrors `.js` inputs and now also exercises object-enumeration smoke coverage in both JS and TS input classes, plus `Math.max()` / `Math.min()` / `Math.abs()` / `Math.sign()` / `Math.clz32()` semantics in both TS and JS input classes, and now also exercises the Web Crypto randomness subset via `crypto.getRandomValues()` on mirrored `.js` input, including JSON-output coverage for that slice, and browser Web Crypto `crypto.subtle.digest()` / `crypto.randomUUID()` semantics on mirrored `.js` input, while the browser-requested run/test lane now also exercises boolean conjunction/disjunction semantics and strict equality / inequality semantics in both TS and `.js` input classes, and the matching non-JSON TS-input run/test mirrors for both rows now also land, and browser-entrypoint alias smoke now also mirrors the run-side browser alias families onto the `test` command, including chromium-, Edge-, Opera-, and Vivaldi-family executables plus representative Brave, Firefox, privacy-browser, command-wrapped aliases, and the generic `msedge` / `opera` / `vivaldi` aliases, keeping the execution-surface evidence aligned with first-class JavaScript compilation. The browser-requested `run` / `test` sandbox gate now also rejects sandboxed browser requests even when the harness override is configured, keeping the standalone browser path from masquerading as a Kali-hosted sandbox. The JS/TS-input browser harness sandbox-rejection matrix now also covers all `run` / `test` direct/inherited browser-api-surface quadrants with JSON-output coverage, keeping the browser-requested runtime gate mirrored end-to-end in the active gap map. The browser-requested `run` / `test` performance.now monotonic-ordering regression now also has mirrored `.ts` input coverage on the direct and inherited browser-harness paths, keeping the browser timing baseline explicit across both source classes. The inherited browser `apiSurface` path now also has JSON-output `run` / `test` coverage for `Math.trunc()` in `.ts` and `.js` input under the same browser harness. The Node API `process.argv.slice(2).length` runtime-smoke coverage now also exercises the documented Node `run` surface on `.js` input, the documented Node `test` surface now also has a direct `.js` smoke probe for `Kali.test(...)`, and the corresponding slice-length lowering assertion is now anchored in `crates/kali_codegen/src/tests.rs`. The explicit Node API surface now also rejects unresolved `node:timers/promises` imports on `check` / `build` in `.js` input, including JSON-output coverage, so the remaining unresolved Node built-in gap stays explicit on the analysis/build side too.
- Current progress: the file/metadata API slice (`Deno.open`, `Deno.create`, `Deno.mkdir`, `Deno.remove`, `Deno.rename`, `Deno.lstat`) now also has default standalone JS-input smoke coverage on `check` / `build` / `run` / `test`, including JSON-output coverage on the `run` and `test` paths, complementing the lower-level `kali_api_deno` filesystem unit tests.
- Late process/working-directory APIs after policy/effect/schema contracts are explicit.
- Weak references (`WeakMap`, `WeakSet`, `WeakRef`, `FinalizationRegistry`), `Proxy`, broader `Intl`, and other late object-model surfaces only with conformance evidence. Current progress: browser-targeted `check` / `build --bundle` coverage now also rejects broader `Intl` (including `Intl.DateTimeFormat` / `Intl.NumberFormat` / `Intl.RelativeTimeFormat` / `Intl.PluralRules` / `Intl.Collator` / `Intl.Locale`) plus late object-model members on mirrored `.js` input, and the browser diagnostics now also spell the bracketed `globalThis["Intl"]` / `globalThis["Intl"]["NumberFormat"]` / `globalThis["Intl"]["DateTimeFormat"]` / `globalThis["Intl"]["RelativeTimeFormat"]` forms explicitly; standalone `check` / `run` / `test` now also carry mirrored `.js` input fixtures while standalone `build` now also rejects the same late compatibility members on `.js` input; standalone `check` / `run` / `test` / `build` now also carry the matching text and JSON-output rejection coverage on `.js` input, and browser-requested run/test harness coverage now also rejects late process-control members and nullish coalescing `??` in `.js` input, keeping the browser ambient surface aligned with the same later-compatibility gates as the standalone runtime lanes. The browser-requested run/test harness path now also rejects `Deno.env.toObject` in both direct and inherited browser `apiSurface` forms on mirrored `.js` input, including JSON-output coverage, so the browser runtime materialization boundary stays explicit alongside the same late host-API gates. The standalone runtime_smoke broader-Intl lane now also mirrors `.js` input with `Intl.DateTimeFormat`, `Intl.RelativeTimeFormat`, `Intl.PluralRules`, `Intl.Collator`, and `Intl.Locale` alongside `Intl.NumberFormat`. The default standalone surface now also accepts the read-only `Deno.pid` / `globalThis.Deno.pid` process-identity query, and the standalone `check` / `build` / `run` smoke paths now also cover both spellings directly in `.js` input, while the standalone `run` / `build` JSON paths now also have explicit coverage for both `Deno.pid` and `globalThis.Deno.pid`, and the standalone `check` JSON path now also has explicit coverage for `globalThis.Deno.pid` in `.js` input; the same host-identity lane now also covers bracketed `globalThis["Deno"]["pid"]` access on the standalone `check` / `build` / `run` path, and the standalone `test` lane now also covers the bracketed `globalThis["Deno"]["pid"]` spelling in both human-readable and JSON output modes, and the parser integration suite now also exercises the bracketed `globalThis["Deno"]["pid"]` source form, now also exercises bracketed `Deno["permissions"]["request"]` / `Deno["permissions"]["revoke"]` and inherited `globalThis["Deno"]["permissions"]["request"]` / `globalThis["Deno"]["permissions"]["revoke"]` source forms, and `crates/kali_codegen/src/tests.rs` now anchors that bracketed lowering form to the `process_pid` import too, keeping the host-identity baseline explicit in machine-readable output too. The read-only env slice now also accepts `Deno.env.get(...)` on the standalone `check` / `build` / `run` / `test` path in `.js` input, with JSON-output coverage now also pinned for `check` / `build` / `run` / `test`, and `crates/kali_cli/src/build_tests.rs` now mirrors the same acceptance in the executable build path, now also covering bracketed `Deno["env"]["get"](...)` in `.ts` and `.js` input plus bracketed `globalThis["Deno"]["env"]["get"](...)` in `.ts` input there too, while `Deno.env.toObject` remains the later object-materialization follow-up; the internal `DenoEnv::to_object` helper now also has deterministic snapshot coverage, keeping that eventual materialization path honest even before the public surface opens. The shared runtime_smoke lane now also mirrors that materialization gate in `.js` input with text and JSON check / build / test coverage, including bracketed `Deno.env["toObject"]`, `Deno["env"]["toObject"]`, `globalThis.Deno["env"]["toObject"]`, `globalThis["Deno"].env["toObject"]`, and `globalThis["Deno"]["env"]["toObject"]` spellings, and the shared late-process-control helper now also pins direct bracketed `process["pid"]` / `process["cwd"]` / `process["chdir"]` / `process["exit"]` and inherited `globalThis.process[...]` spellings, while the permission-escalation bracketed-source regression now also pins inherited `globalThis.Deno["permissions"]["request"]` / `revoke` spellings too, keeping the later object-materialization follow-up explicit in the broad smoke lane. The parser integration suite now also exercises the bracketed `globalThis["Deno"]["env"]["get"]`, `Deno["permissions"]["query"]`, `globalThis["Deno"]["permissions"]["query"]`, and `globalThis["Deno"]["pid"]` source forms alongside the existing `globalThis["Deno"]["exit"]` bracketed path, and now also covers bracketed `globalThis["Intl"]["DateTimeFormat"]`, `globalThis["Intl"]["DisplayNames"]`, `globalThis["Intl"]["PluralRules"]`, `globalThis["Intl"]["Locale"]`, `globalThis["WeakRef"]`, `globalThis["process"]["cwd"]`, `globalThis["process"]["exit"]`, `process["exit"]`, and `globalThis["Proxy"]["revocable"]` source forms so the late-compatibility source-shape regressions stay explicit before the checker normalizes them to canonical gates, and the browser late-compatibility source fixtures now also pin bracketed string-literal Deno/process-control and object-model forms, including the `WeakMap` / `WeakSet` / `WeakRef` / `FinalizationRegistry` family and the bracketed `globalThis["WeakRef"]` form, so the source-shape regressions stay explicit alongside the existing rejection coverage. The late-compatibility Intl regression coverage now also explicitly pins `Intl.DisplayNames` and `Intl.Locale` alongside the already-covered `Intl.NumberFormat` / `Intl.DateTimeFormat` / `Intl.RelativeTimeFormat` / `Intl.PluralRules` / `Intl.Collator` members in the JS-input late-compatibility matrices, including the bracketed `globalThis["Intl"]["DisplayNames"]` and `globalThis["Intl"]["Locale"]` spellings. The standalone late-compatibility JS smoke now also asserts the dot-form `globalThis.Object.hasOwn` / `globalThis.Object.prototype.hasOwnProperty.call` spellings alongside the bracketed own-property-helper forms, so the default standalone object-model rejection matrix stays aligned with the browser matrix.
- Alternative runtime backends only if they preserve visible contracts.

- Current progress: the explicit `--api deno` smoke lane now also round-trips `Deno.env.set(...)` / `Deno.env.delete(...)` in `.js` input across `check` / `build` / `run` / `test`, including JSON-output coverage, keeping the mutable-environment host slice visible on the documented Deno surface without widening the default standalone contract. The browser-targeted late-compatibility lane now also rejects mutable environment access (`Deno.env.set` / `Deno.env.delete` and bracketed forms) in `.js` input with canonical gating/error paths, keeping the browser host-mutation boundary explicit alongside the other late host-control checks. The Node surface now also rejects `process.env` assignment mutations and bracketed `process["env"]` / `globalThis["process"]["env"]` forms in `.js` input on the shared analysis and execution paths, keeping the mutable-env gate explicit even when the host model exposes `process` for package compatibility. The package-effects regression lane now also mirrors computed bracketed `Deno.env.delete(...)` as `Process.EnvWrite`, now also records `Process.EnvWrite` for Node-context `process.env` assignment mutations, and now also reports the direct `.js` network/listener slice explicitly, keeping both the mutating env-access and network/listener slices aligned with the bracketed host-access coverage. The subprocess slice now also has dedicated `new Deno.Command('sh').spawn()` sandbox-rejection regressions on `check` and `build` in both `.ts` and `.js` input, with JSON-output coverage on the mirrored analysis/build lanes, so the `Process.Spawn` policy boundary is pinned without relying only on the combined host-effects fixture. The sandbox effect-analysis tests now also pin direct and computed `Deno.connect` / `Deno.listen` / `Deno.serve` host-call access in `.js` input, and the `effects` command now also reports the direct `.js` network/listener slice explicitly, keeping the network/listener contract aligned across the analysis and reporting surfaces.

## Ecosystem and packages

Owners: `specs/11-standard-apis.md`, `specs/12-cli.md`, `specs/14-packages.md`, `specs/16-testing.md`, `specs/18-schemas.md`, `specs/19-feature-maturity.md`.

Remaining goals:

- Broader pure JS/TS npm/JSR package compatibility by support rung.
- Larger package corpus covering Deno, browser-targeted, and Node contexts separately.
- More Node built-ins and package-resolution cases when evidence justifies them.
- Current progress: the deterministic package-corpus matrix now records the active browser-targeted, default-standalone, Node, and Deno corpus slices in `plan/phase-8/package-corpus-matrix.md`, keeping the source-kind / host-fit / command / rung split explicit for the existing evidence set. The matrix drift test now also pins representative browser runtime corpus rows, the browser condition-preference slice, the browser replacement-map, string-entry, string-export, browser-condition export `.js` rows, the browser internal-browser-rewrite `.js` row, the browser-condition / browser-string / web-baseline row, the browser web-baseline primitive `.js` entrypoint row, the browser typed export branch `.js` runtime row, the browser pattern-exports `.js` runtime row, the browser semver direct and inherited-browser `.js` runtime rows, the browser string-export inherited-browser `apiSurface` row, the browser runtime exports-map rows (direct and inherited browser `apiSurface`), the browser-targeted semver runtime row, the default standalone pattern-exports, exports-map mixed-format interop, module-entry, module-entry-chain, web-baseline TS, string-export, pure-JS utility, pure-JS semver, package-content, and scoped rows, the default standalone package-content `.js` test row, the default standalone date-fns `.ts` test row, the default standalone semver `.js` slice, the default standalone semver bin-entrypoint row, the Node semver bin-entrypoint row, the Node runner packages with exports maps `.js` row, the Node built-in packages (`node:buffer`, `node:assert`, `node:child_process`, `node:events`, `node:stream`, `node:fs/promises`, `node:path`, `node:os`, `node:crypto`, `node:fs`, `node:url`, `node:util`, `node:timers`, `node:timers/promises`) `.js` row, representative Node/Deno rows, the Node-assuming `.js` surface row, the Deno host-control row, and the JSR row so the snapshot stays deterministic as the corpus grows. The Node package corpus now also broadens the `node:timers` probe to confirm `setInterval` alongside `setTimeout` and `clearTimeout`, keeping the timers slice honest inside that same Node built-in row, and the explicit Node API surface smoke now also accepts `node:timers` imports in `.js` input across the documented Node `check` / `build` / `run` / `test` lanes. The matrix drift test now also keeps the browser package-content runtime rows and the default standalone package-content test row pinned exactly once, and the browser package-fixtures direct and inherited runtime rows are now also pinned exactly once; it now also pins the generic browser-targeted exports-map, module-entry, module-entry-chain, browser replacement-map, browser-condition / browser-deno preference, browser-blocked, typed export branch, browser internal-browser-rewrite, and scoped-package rows exactly once, and it now also asserts the browser replacement-map, string-entry, string-export, browser-condition export, browser-condition / browser-string / web-baseline, browser-condition / browser-deno preference, typed export branch, module-entry-chain, and module-only rows exactly once across the direct and inherited browser harness variants, so the host-heavy package-corpus slices stay duplication-checked as the matrix expands. The browser binary-entrypoint probe now also rejects the `semver` package bin entrypoint on the inherited browser `apiSurface` path in `.js` input, keeping published npm CLI entrypoints distinct from ordinary browser deployability rows.
- Current progress: the host-heavier `@mariozechner/pi-coding-agent` package-content probe now also mirrors `.js` input on the default standalone `check` / `build` lane, and now also has a `.js` test-corpus probe, keeping first-class JavaScript compilation aligned with the same package-content evidence as the TypeScript lane while still separating the published bin-entrypoint probe. The broader default standalone exports-map package corpus now also covers `build` in addition to `check` and `run`, keeping the broad exports-map slice aligned with the current package-corpus matrix. The broader default standalone utility corpus now also mirrors the zod, p-limit, and ms slices onto `.js` input for `check` / `build` / `run` / `test`, and the pattern-exports utility slice now mirrors `.js` input across `check` / `build` / `run`, keeping the pure-JS utility ladder aligned with first-class JavaScript compilation too. The default standalone scoped-package corpus now also mirrors `.js` input across `check` / `run`, keeping the scoped utility rung aligned with first-class JavaScript compilation too. The default standalone web-baseline primitive package slice now also has `build` coverage, and the `.js` web-baseline slice now also has `build` coverage alongside `test`; both web-baseline package slices now also exercise `AbortSignal` via `AbortController.signal`, and the `.js` web-baseline slice now also has JSON-output coverage across `check` / `build` / `run` / `test`, keeping the baseline web-API rung aligned with the same first-class-JavaScript package evidence too.
- Current progress: browser-targeted package corpus coverage now also exercises `.js` entrypoints for browser replacement-map packages, including a scoped package case and a `vue/runtime-dom` browser replacement-map JS-entrypoint case, and now also covers typed export branches on `.js` input plus browser dual-exports `.js` entrypoints, closing one of the first-class-JavaScript evidence gaps in the browser surface. The browser runtime corpus now also exercises the typed export branch packages on `.js` input for `run` and `test`, and now also exercises browser pattern-exports packages on `.js` input for `run` and `test`, including the `hono/client` and `solid-js/web` slices, plus module-only packages and module-entry-chain packages on `.js` input for `run` and `test`, and the module-entry-chain slice now also has inherited browser `apiSurface` JSON-output coverage, so the browser deployability/execution lane now distinguishes package content from the separate standalone bin-entrypoint rejection while keeping the first-class-JavaScript runtime evidence aligned with the same package shapes. The browser corpus now also exercises the host-heavier `@mariozechner/pi-coding-agent` package-content probe on `.js` input for `check` and `build --bundle`, and the inherited browser `apiSurface` path now also covers the same package-content probe on the browser-targeted `check` / `build --bundle` lane in `.js` input, so the browser deployability lane distinguishes package content from the separate standalone bin-entrypoint rejection. Browser-blocked packages now also reject on `.js` input in the browser `check` / `build --bundle` lanes, and now also reject on the browser runtime `run` / `test` harness in both explicit and inherited browser `apiSurface` modes, including JSON-output coverage for both the static browser-analysis path and the browser runtime path, so the browser-path exclusion contract stays explicit even when the package source class is first-class JavaScript. The browser string package corpus now also has a dedicated browser-string/web-baseline variant, keeping the browser string + web-baseline row explicitly covered by code as well as by the matrix snapshot.
- Current progress: browser-targeted exports-map package corpus coverage now also mirrors the same package set onto `.js` input, and browser module-entry-chain coverage now also mirrors `.js` input, keeping the browser analysis/build lane aligned with first-class JavaScript compilation instead of only the TypeScript lane. The browser runtime corpus now also exercises browser internal-browser-rewrite packages on `.js` input for `run` and `test`, including direct and inherited browser `apiSurface` coverage, keeping the browser execution corpus aligned with the same browser rewrite-chain slice too. The browser runtime module-entry-chain slice now also has direct JSON-output coverage on the browser harness path, matching the inherited-browser JSON lane. The package-corpus matrix now also records the browser exports-map `.js` rows explicitly, including the inherited browser `apiSurface` row. The matrix drift test now also pins the browser dual-exports `.js` row alongside the existing replacement-map, string-entry, string-export, and browser-condition export entries, and browser pattern-exports now also has a `.js` entrypoint slice in the browser analysis/build lane. The browser condition / browser-deno preference package slice on the browser-targeted `check` / `build --bundle` path now also has JSON-output coverage on mirrored `.js` input, keeping the browser resolution surface aligned with the runtime browser-vs-deno probe.
- Current progress: the browser package corpus now also mirrors the browser module-entry fixture onto `.js` input, so the browser package-support evidence stays aligned across the JS and TS lanes for the module-entry shape too.
- Current progress: the browser runtime corpus now also exercises the host-heavier `@mariozechner/pi-coding-agent` package-content probe on `.js` input for `run` and `test`, and the same direct and inherited browser-harness slices now also have JSON-output coverage, so the browser deployability/execution lane distinguishes package content from the separate standalone bin-entrypoint rejection while keeping the first-class-JavaScript runtime evidence aligned with the same package shape.
- Current progress: the browser package corpus now also exercises the canonical pure-JS `semver` probe on `.js` input, and the browser runtime corpus now also exercises the same probe on `.js` input for `run` and `test` when the browser harness command is configured, including JSON-output coverage for both browser runtime semver lanes and the inherited browser `apiSurface` path; the browser-targeted `check` / `build --bundle` semver slice now also has JSON-output coverage on `.js` input, and the package-corpus matrix now records the browser direct and inherited semver slices explicitly, so the browser package-support evidence now spans the browser-targeted first-class-JavaScript path instead of only the TypeScript lane.
- Current progress: the browser package corpus now also mirrors the browser string-entry, string-export, browser-condition export, web-baseline primitive, and internal browser-rewrite fixtures onto `.js` input, keeping the browser package-support evidence aligned across the JS and TS lanes. The matrix drift test now also pins the browser replacement-map, string-entry, string-export, and browser-condition export `.js` rows, keeping those first-class-JavaScript browser package shapes deterministic in the snapshot.
- Current progress: the browser package corpus now also exercises browser condition / browser-deno preference packages on the browser-targeted `check` / `build --bundle` path, including mirrored `.js` input, so the browser resolution surface stays aligned with the runtime browser-vs-deno probe.
- Current progress: the browser runtime corpus now also mirrors the browser package fixtures on `.js` input for both `run` and `test`, including the browser-vs-deno condition-preference probe in both TS and `.js` input, and the browser-requested `Math.max()` / `Math.min()` / `Math.abs()` / `Math.sign()` / `Math.clz32()` lanes in both TS and `.js` input, plus the browser-requested `crypto.getRandomValues()` lane in `.js` input and browser-requested optional chaining and console-level routing on `.js` input, with standalone `.js` branch-specialization-repeat `run` / `test` JSON-output coverage now pinned too, and now also exercises browser dual-exports packages plus the browser typed export branch, browser string/web-baseline packages, and browser web-baseline package slices on `.js` input for both `run` and `test`, with JSON-output coverage now also pinned for that browser web-baseline runtime slice on the first-class JavaScript path and the same slice now also pinned on the inherited browser `apiSurface` path. The generic browser package fixtures now also exercise the inherited browser `apiSurface` path in `.js` input for both `run` and `test`, and the browser dual-exports runtime slice now also exercises the inherited browser `apiSurface` path in `.js` input for both `run` and `test`, keeping the browser-requested execution surface aligned with first-class JavaScript compilation. The browser runtime corpus now also exercises browser replacement-map packages on `.js` input for `run` and `test`, including the inherited browser `apiSurface` path, and now also has JSON-output coverage for the direct and inherited browser `apiSurface` paths, and browser string-entry and string-export packages on `.js` input for `run` and `test`, with the browser string-entry slice now also exercising the inherited browser `apiSurface` path and the browser string-export slice now also exercising the inherited browser `apiSurface` path, and the browser string-entry/string-export runtime rows now also carrying JSON-output coverage on the direct and inherited browser harness paths, keeping the browser harness slice aligned with those browser package shapes too. The browser runtime corpus now also exercises browser module-only packages and browser module-entry-chain packages on `.js` input for `run` and `test`, and now also has JSON-output coverage for the direct browser-harness browser module-only slice, including the new direct JSON-output coverage for that slice, while the browser runtime corpus also exercises browser module-entry-chain packages on the inherited browser `apiSurface` path in `.js` input with JSON-output coverage, and browser exports-map packages on `.js` input for both `run` and `test`, and the package-corpus matrix drift test now also pins those browser runtime rows, the browser dual-exports inherited-browser row, the browser module-entry-chain inherited-browser row, and the browser exports-map inherited-browser row, so the browser execution snapshot stays explicit for those module-resolution shapes as well. The browser runtime `@mariozechner/pi-coding-agent` package-content probe now also covers the inherited browser `apiSurface` path in `.js` input for both `run` and `test`, and the package-corpus matrix now records that inherited-browser row explicitly, so the browser runtime package corpus keeps the direct and inherited browser context split explicit. The browser runtime typed export branch slice now also exercises the inherited browser `apiSurface` path on `.js` input for both `run` and `test`, keeping the browser harness slice aligned across direct and inherited context selection for that package shape too. The browser bundle `.js` `Math.clz32()` slice now also has JSON-output coverage. The default standalone `@mariozechner/pi-coding-agent` bin-entrypoint probe remains explicitly rejected, so the browser/runtime package corpus stays separate from the standalone executable-entrypoint probe.
- Current progress: browser-targeted `check` / `build --bundle` coverage now also rejects late Deno/process host-control members on mirrored `.js` input, and the browser late-process-control lane now also has JSON-output coverage for `check` and `build`, keeping the browser ambient surface separated from later standalone host-control APIs. The browser-targeted late-object-model rejection assertions now also explicitly pin `Proxy.revocable` plus the own-property helper spellings in both text and JSON output, including mixed dot/bracket spellings for `Object.hasOwn` and `Object.prototype.hasOwnProperty.call`, so the browser-analysis/build gate stays aligned with the full late-object-model source surface rather than only the broad class labels. The browser API-surface type-resolution regression now also rejects `Object.hasOwn` under the browser context, so the same late object-model split stays explicit one layer earlier than the CLI smoke lanes too. The lower-level `build_source_file` unit path now also rejects bracketed `globalThis["Object"]["hasOwn"]` and `globalThis["Object"]["prototype"]["hasOwnProperty"]["call"]` forms in `.js` input, now also mirrors the bracketed `globalThis["Object"]["hasOwn"]` form in `.ts` input, now also rejects broader Intl members such as `Intl.DateTimeFormat` / `Intl.RelativeTimeFormat` / `Intl.PluralRules` / `Intl.Collator` / `Intl.DisplayNames` / `Intl.Locale` alongside bracketed `globalThis["Intl"]["Segmenter"]`, and now also rejects the weak-reference family (`WeakMap` / `WeakSet` / `WeakRef` / `FinalizationRegistry`) plus `globalThis["Proxy"]["revocable"]`, keeping the compile-time build helper aligned with the same late object-model gate.
- Current progress: the default standalone utility corpus now also mirrors its minimized mixed-format CommonJS/ESM interop fixture on `.js` input for `run`, `test`, and `build`, and the canonical semver corpus probe now also covers `.js` input on `check` / `build` / `run` / `test`, keeping first-class JavaScript package evidence aligned with the existing TypeScript lane instead of only covering the TS form.
- Current progress: the default standalone module-entry corpus now also mirrors `.js` input for both the simple module-entry and module-entry-chain slices on `check` / `run`, keeping the first-class-JavaScript package evidence aligned with the same module-resolution paths as the TypeScript lane.
- Current progress: browser bundle chunk smoke coverage now also exercises literal and const-bound dynamic imports from `.js` input, keeping the linked-graph lowering path aligned with first-class JavaScript compilation on the browser build lane.
- Current progress: the runtime-smoke regression set now also mirrors arithmetic precedence and array literal length handling into `.js` input, alongside async/await sequencing and queueMicrotask ordering on both the `run` and `test` paths, including JSON-output coverage in `.js` input, and now also covers string-primitive `Object.keys()` / `Object.entries()` / `Object.values()` enumeration in `.js` input on both paths, keeping those core supported semantics aligned with first-class JavaScript compilation evidence. Runtime object type / constructor semantics (`typeof`, `instanceof`) now also have dedicated standalone `run` / `test` smoke coverage in both `.ts` and `.js` input, including JSON-output coverage on both source classes, keeping the object-model baseline explicit in the same first-class-JavaScript path. The browser-requested `run` / `test` harness now also exercises the same object-model baseline in both `.ts` and `.js` input with JSON-output coverage, and browser bundle smoke now also exercises the same object-model baseline in both source classes, so the browser runtime snapshot stays aligned with the same object-model baseline too. Browser bundle smoke now also exercises basic object property deletion / `in`-operator semantics in both TS and `.js` inputs, keeping the same object-model slice aligned across the browser bundle lane as well. The standalone `run` / `test` smoke lane now also exercises `globalThis.Deno.pid` handling on `.js` input, and the standalone `check` / `build` / `run` lane now also covers direct bracketed `Deno["pid"]` access plus bracketed `globalThis["Deno"]["pid"]` access with matching `process_pid` lowering coverage, while the standalone `check` / `build` / `run` / `test` lane now also mirrors the same bracketed path on `.ts` input and the standalone JSON `run` / `build` lanes now also mirror that same `.ts` coverage too, so the core runtime baseline stays aligned with the documented Deno PID surface too. The shared web-baseline runtime smoke now also exercises `structuredClone`, `AbortController`, `AbortSignal`, `EventTarget`, and `CustomEvent` on the standalone `run` and `test` paths in both TS and `.js` input, with JSON-output coverage for both commands, so the web baseline stays aligned with the supported API contract instead of only the package-corpus path. The browser bundle runtime smoke now also exercises the same primitives in both TS and `.js` input, with JSON-output coverage for the browser bundle lane, so the browser-targeted bundle path stays aligned with the same web-baseline contract. The type-resolution layer now also accepts the remaining browser-baseline host globals (`fetch`, `Headers`, `Request`, `Response`, `Blob`, `File`, `performance`, and `crypto`) directly in `crates/kali_types/src/tests.rs`, keeping the browser ambient typing surface aligned with the same browser/standalone baseline one layer earlier than the runtime smoke. The phase-6 conformance dashboard now also tracks those shared web-baseline rows explicitly, and now also tracks the broader browser ambient typing row for the remaining browser-baseline host globals, and now also pins the browser-requested Web Crypto `crypto.subtle.digest()` / `crypto.randomUUID()` row plus the browser bundle `crypto.getRandomValues()` / `crypto.subtle.digest()` / `crypto.randomUUID()` row explicitly, and now also pins the basic arithmetic precedence / array-length rows explicitly in both the generic and `.js` variants, so the runtime snapshot stays in sync with the same standalone and browser bundle smoke coverage.
- Current progress: browser-requested run/test async/await sequencing now also has dedicated browser-harness coverage in both TS and `.js` input, keeping the opt-in browser runtime path aligned with the same await-order regression the standalone runtime smoke already pins. The phase-6 conformance dashboard now also tracks the inherited browser-api-surface `Math.max()` / `Math.min()` / `Math.abs()` / `Math.sign()` / `Math.imul()` suite, `Math.ceil()` row, and `Math.trunc()` rows explicitly, and the inherited browser-api-surface `Math.ceil()` smoke now also has matching `.ts` JSON-output coverage in `crates/kali_cli/tests/runtime_smoke.rs`, so the machine-readable browser-harness snapshot stays aligned with that opt-in path too.
- Current progress: `effects` JSON output now also exercises combined inherited `compat.features` + `compilerOptions.runtimeProfiles` normalization in one regression, keeping the mixed-axis analysis-context contract deterministic across schema-v1 payloads.
- Current progress: `DenoRuntimeProjection::new()` now also has unit coverage for the default-open projection baseline, keeping the Phase-1 host-projection contract explicit alongside the rest of the browser/runtime baseline.
- Current progress: Node package corpus coverage now also exercises the documented Node build surface for the same `node:`-based package set that already has Node `check` / run evidence, including `node:child_process`, `node:events`, `node:os`, and the `node:timers/promises` breadth probe, keeping package compatibility evidence aligned across analysis, build, and execution lanes. The explicit Node API surface smoke now also mirrors `.js` input across `check` / `build` / `run` / `test`, now also carries JSON-output coverage for the Node `build` path in `.js` input, now also carries JSON-output coverage for the Node `run` / `test` `.js` input path, and now also carries JSON-output coverage for the explicit `node:timers` run/test helpers in `.js` input; the explicit Node API surface now rejects `node:timers/promises` with the canonical `E5506` availability gate, and the inherited Node API surface now also rejects the same `node:timers/promises` subpath on `check` / `build` / `run` / `test` in `.js` input with matching JSON-output coverage, so the package-corpus row remains the evidence source for that subpath; the explicit Node API surface now also pins the plain import-binding rejection path on `check` / `run` in `.js` input, so the unsupported module stays rejected even when only its binding is observed; the same explicit and inherited Node API surface smoke now also rejects late network modules such as `node:net` on `check` / `build` / `run` / `test` in `.js` input with JSON-output coverage, keeping the socket/listener gate explicit alongside the other late Node-module rows; the same explicit and inherited Node API surface smoke now also rejects late process-control helpers such as `process.pid` / `process.cwd` / `process.chdir` / `process.exit` in `.js` input on `check` / `build` / `run` / `test`, keeping the later Node process-control row explicit instead of silently implying it from the broader Node package corpus; the same explicit and inherited Node API surface smoke now also exercises `node:buffer` in `.js` input, and the Node `run` / `test` smoke now also invokes `node:timers` helpers directly in `.js` input, while the package host-fit scanner now also treats `node:timers` as Node-only and the timers slice now confirms `setInterval` and `clearInterval` alongside `setTimeout` and `clearTimeout`, and inherited Node package resolution now consults the same package host-fit scan before accepting Node-only builtins, keeping the built-in breadth aligned with the package-corpus row that already tracks that module family and the updated matrix note. The explicit and inherited Node API surface build smoke now also covers `build --lib` on `.js` input, keeping the library-oriented Node build lane aligned with the documented Node subset. Node analysis-context `effects` coverage now also mirrors `.js` input on both the explicit and inherited `apiSurface=node` paths, and the dedicated Node API surface smoke now also exercises the `node:timers/promises` import path in `.js` input on both explicit and inherited Node selection while still reporting the supported effect slice, keeping the same first-class-JavaScript Node surface visible in the reporting lane too. The package-corpus matrix drift test now also pins the `node:timers` evidence note explicitly.
- Current progress: the Node package corpus now also exercises the canonical pure-JS `semver` probe on `.js` input across the Node `check` / `build` / `run` / `test` lanes, closing a first-class-JavaScript evidence gap on the Node surface instead of only the TypeScript form.
- Current progress: the Node package corpus now also exercises a `node:buffer` built-in import within the Node exports-map JS-input slice across the Node `check` / `build` / `run` / `test` lanes, keeping the Node package-resolution evidence aligned with first-class JavaScript compilation instead of only the TypeScript form.
- Current progress: the Node package corpus now also exercises dedicated `node:buffer`, `node:assert`, `node:child_process`, `node:events`, `node:http`, `node:stream`, `node:fs/promises`, `node:path`, `node:os`, `node:crypto`, `node:fs`, `node:url`, and `node:util` built-in package slices on `.js` input across `check` / `build` / `run` / `test`, so the Node built-in breadth probe stays separate from the node-assuming package fixtures while keeping first-class JavaScript compilation in view. The Node package corpus now also mirrors the inherited Node `apiSurface` path for representative built-in slices (`node:buffer`, `node:assert`, `node:http`, `node:events`, and `node:timers`) on `.js` input across `check` / `build` / `run` / `test`, keeping the inherited Node package-corpus evidence aligned with the explicit surface.
- Current progress: the Node package corpus now also mirrors the runner-package exports-map `.js` slice and the mixed-format-entry slice onto `.js` input for the explicit Node surface, and the package-corpus matrix now records the Node runner exports-map `.js` row explicitly, keeping the Node package-resolution evidence aligned with first-class JavaScript compilation instead of only the TypeScript form.
- Current progress: the canonical host-heavier `@mariozechner/pi-coding-agent` probe now also covers package content on the default standalone surface at the check/build rung, keeping the breadth probe separate from the published bin entrypoint probe and avoiding any accidental executable-package overclaim. The default standalone `date-fns` utility slice now also covers `test` on `.js` input and `.ts` test coverage, and now also has JSON-output coverage on the `.js` and `.ts` `test` paths, so the utility corpus now exercises one representative pure-JS package across the full `check` / `build` / `run` / `test` rung set.
- Current progress: the default standalone exports-map mixed-format interop probe now also mirrors `.js` input across `check` / `build` / `run`, keeping the exports-map resolver slice aligned with first-class JavaScript compilation instead of only the TypeScript lane. The default standalone string-export corpus now also has a dedicated `.js` `test` probe, so the pure-JS package evidence now covers the testable rung as well as the executable rung.
- Current progress: Deno package corpus coverage now also exercises the documented Deno build surface for the same Deno-host package set that already has Deno `check` / `run` / `test` evidence, keeping package compatibility evidence aligned across analysis, build, and execution lanes.
- Current progress: Deno package corpus coverage now also exercises a canonical `jsr:@std/path` package fixture materialized at `node_modules/@std/path` on the Deno surface, keeping the `jsr:` registry prefix and on-disk path mapping honest in the package-resolution evidence. The same Deno host-control and JSR fixtures now also mirror `.js` input across `check`, `build`, `run`, and `test`, keeping the Deno package-corpus slice aligned with first-class JavaScript compilation instead of only the TypeScript lane.
- Current progress: registry-analysis command-shape coverage now also rejects explicit package-version targets for `package-effects` and `package-audit` in both text and JSON output modes, keeping the single-package target contract aligned with the documented `E5508` path. The same registry-analysis lane now also rejects raw URLs and local/relative path targets in both text and JSON output modes, keeping the single registry-identifier contract explicit instead of silently accepting non-registry selectors. `package-audit` now also rejects `--api`, `--compat`, and `--wasm-threads` in both text and JSON output modes, and those rejections now short-circuit before registry lookup, keeping the context-free registry-analysis gate aligned with the package-analysis-specific-flag contract while staying network-free on invalid invocations.
- Current progress: `package-effects` now also has native JSON pretty-output coverage and quiet-mode coverage, keeping the native-JSON registry-analysis payload contract explicit across presentation controls as well as the inherited-analysis lane.
- Current progress: `package-audit` now also has pretty-JSON envelope coverage while inheriting a browser analysis context, keeping the envelope-only JSON command honest across formatting mode and inherited context at the same time.
- Current progress: `package-audit` now also suppresses human output under `--quiet` even when inheriting `eval` plus `wasm-threads`, and the same inherited compat/thread context now also has JSON-output quiet coverage, while the direct `--wasm-threads` rejection now also has matching JSON-output coverage, keeping the context-free registry-analysis row aligned across both quiet and non-quiet surfaces.
- Current progress: `package-effects` now also has JSON-output rejection coverage for an inherited threaded runtime profile in the default standalone package-analysis context, and the quiet JSON path now also rejects a mixed inherited `eval` + `wasm-threads` context deterministically, keeping the inherited-only registry-analysis gate aligned with the command's schema-v1 context model.
- Current progress: install-command integration now also rejects `kali install --allow-scripts` on a clean workspace in JSON output, and now also rejects the raw-URL-only install-work case in both human and JSON modes plus the raw-URL and JSR invalid-combination cases in JSON mode, matching the library-layer no-op rejection and keeping the empty effective npm-scriptable work gate explicit at the CLI boundary.
- Batch or richer registry-analysis workflows only after command/schema revisions.
- Keep native/binary/bootstrap-heavy packages rejected by default unless the specs deliberately change.

## Optimization and PGO

Owners: `specs/07-specialization.md`, `specs/08-wasm-codegen.md`, `specs/16-testing.md`, `specs/18-schemas.md`.

Remaining goals:

- Deterministic build-only PGO input (`--profile`) with strict schema validation if not fully promoted; CLI integration now covers version, unknown-field, and top-level malformed-shape rejection in text and JSON build modes, and equivalent profile snapshots now hash identically after normalization so the build-only input remains deterministic across reordered or duplicate samples.
- Deeper release and release-advanced passes: specialization, inlining, layout-aware lowering, peepholes, and LTO-like whole-graph cleanup.
- Version-pinned benchmarks before public performance claims.
- Current progress: the optimization benchmark smoke now uses checked-in, hash-validated fixture workloads (`math-benchmark-v1.ts` / `math-benchmark-v1.json`, `math-trunc-benchmark-v1.ts` / `math-trunc-benchmark-v1.json`, `math-imul-benchmark-v1.ts` / `math-imul-benchmark-v1.json`, `math-ceil-benchmark-v1.ts` / `math-ceil-benchmark-v1.json`, `math-abs-sign-benchmark-v1.ts` / `math-abs-sign-benchmark-v1.json`, `math-clz32-benchmark-v1.ts` / `math-clz32-benchmark-v1.json`, `division-by-one-benchmark-v1.ts` / `division-by-one-benchmark-v1.json`, `multiplication-by-one-benchmark-v1.ts` / `multiplication-by-one-benchmark-v1.json`, `dead-branch-elimination-benchmark-v1.ts` / `dead-branch-elimination-benchmark-v1.json`, `dead-inlined-function-pruning-benchmark-v1.ts` / `dead-inlined-function-pruning-benchmark-v1.json`, `call-inlining-benchmark-v1.ts` / `call-inlining-benchmark-v1.json`, `closure-inlining-benchmark-v1.ts` / `closure-inlining-benchmark-v1.json`, `object-enumeration-benchmark-v1.ts` / `object-enumeration-benchmark-v1.json`, `reflect-own-keys-benchmark-v1.ts` / `reflect-own-keys-benchmark-v1.json`, `reflect-own-keys-const-bound-literal-benchmark-v1.ts` / `reflect-own-keys-const-bound-literal-benchmark-v1.json`, `reflect-own-keys-alias-chain-benchmark-v1.ts` / `reflect-own-keys-alias-chain-benchmark-v1.json`, `integer-like-object-enumeration-benchmark-v1.ts` / `integer-like-object-enumeration-benchmark-v1.json`, `object-enumeration-alias-chain-benchmark-v1.ts` / `object-enumeration-alias-chain-benchmark-v1.json`, `object-enumeration-const-bound-literal-benchmark-v1.ts` / `object-enumeration-const-bound-literal-benchmark-v1.json`, `object-enumeration-delete-reinsert-benchmark-v1.ts` / `object-enumeration-delete-reinsert-benchmark-v1.json`, `object-literal-property-order-canonicalization-benchmark-v1.ts` / `object-literal-property-order-canonicalization-benchmark-v1.json`, `identity-chain-benchmark-v1.ts` / `identity-chain-benchmark-v1.json`, `nested-wrapper-pruning-benchmark-v1.ts` / `nested-wrapper-pruning-benchmark-v1.json`, `algebraic-simplification-benchmark-v1.ts` / `algebraic-simplification-benchmark-v1.json`, `duplicate-pure-expression-elimination-benchmark-v1.ts` / `duplicate-pure-expression-elimination-benchmark-v1.json`, `nullish-specialization-repeat-benchmark-v1.ts` / `nullish-specialization-repeat-benchmark-v1.json`, `specialization-reuse-benchmark-v1.ts` / `specialization-reuse-benchmark-v1.json`, `bigint-literal-arguments-benchmark-v1.ts` / `bigint-literal-arguments-benchmark-v1.json`, `bigint-addition-chain-benchmark-v1.ts` / `bigint-addition-chain-benchmark-v1.json`, `bigint-multiplication-chain-benchmark-v1.ts` / `bigint-multiplication-chain-benchmark-v1.json`, `numeric-literal-arguments-benchmark-v1.ts` / `numeric-literal-arguments-benchmark-v1.json`, `boolean-literal-arguments-benchmark-v1.ts` / `boolean-literal-arguments-benchmark-v1.json`, `branch-specialization-repeat-benchmark-v1.ts` / `branch-specialization-repeat-benchmark-v1.json`, `const-array-element-access-benchmark-v1.ts` / `const-array-element-access-benchmark-v1.json`, `const-object-property-access-benchmark-v1.ts` / `const-object-property-access-benchmark-v1.json`, `math-variant-benchmark-v1.ts` / `math-variant-benchmark-v1.json`, `string-concatenation-benchmark-v1.ts` / `string-concatenation-benchmark-v1.json`, `array-literal-arguments-benchmark-v1.ts` / `array-literal-arguments-benchmark-v1.json`, `template-literal-concatenation-benchmark-v1.ts` / `template-literal-concatenation-benchmark-v1.json`, `layout-specialization-benchmark-v1.ts` / `layout-specialization-benchmark-v1.json`, `call-inlining-chain-benchmark-v1.ts` / `call-inlining-chain-benchmark-v1.json`, and `nullish-benchmark-v1.ts` / `nullish-benchmark-v1.json`) so the compile-time size/speed comparison lane is pinned to deterministic inputs instead of an ad-hoc inline source string, the dead-inlined-function-pruning workload keeps the release-advanced dead-function cleanup path visible in the benchmark lane, the object-enumeration alias-chain workload keeps the rebinding path visible in the benchmark lane, the Reflect.ownKeys workloads keep the same literal-object ordering path visible under the Reflect API too, the Reflect.ownKeys alias-chain workload keeps the rebinding path visible in the benchmark lane, the const-bound literal alias workload keeps the alias-to-literal specialization path visible in the benchmark lane, the delete/reinsert object-enumeration workload keeps the mutation-heavy ordering path visible in the benchmark lane, the object-literal property-order canonicalization workload keeps the object-layout path visible in the benchmark lane, the integer-like object-enumeration workload keeps the canonical key-ordering path visible in the benchmark lane, the dead-branch-elimination workload keeps constant-branch pruning visible in the benchmark lane, the new boolean-literal-argument workload keeps branch specialization visible in the benchmark lane, and the new branch-specialization-repeat workload keeps branch specialization visible in the benchmark lane, the new const-array element access workload keeps the array-specialization path visible in the benchmark lane, the new const-object property access workload keeps the property-specialization path visible in the benchmark lane, the new math-variant workload keeps the arithmetic baseline visible in the benchmark lane, the new numeric-literal-arguments workload keeps the numeric literal-argument specialization path visible in the benchmark lane, the new string-concatenation workload keeps the literal-folding path visible in the benchmark lane, the new template-literal-concatenation workload keeps the same literal-folding path visible through template-string syntax, the new division-by-one workload keeps the release-advanced division-simplification path visible in the benchmark lane, the new multiplication-by-one workload keeps the release-advanced multiplication-simplification path visible in the benchmark lane, the new duplicate pure-expression elimination workload keeps the repeated-expression cleanup path visible in the benchmark lane, the new Math.abs/Math.sign workload keeps the built-in math lowering path visible in the benchmark lane, the new Math.trunc workload keeps the built-in math lowering path visible in the benchmark lane, the new Math.imul workload keeps the built-in math lowering path visible in the benchmark lane, the new Math.clz32 workload keeps the built-in math lowering path visible in the benchmark lane, the new Math.ceil workload keeps the built-in math lowering path visible in the benchmark lane, and the runtime-smoke benchmark loop now exercises the specialization-reuse workload too, along with the string-concatenation, array-literal-arguments, and template-literal-concatenation workloads, and the bigint-literal-arguments, bigint-addition-chain, bigint-multiplication-chain, and numeric-literal-arguments fixtures. The benchmark metadata schema now also constrains benchmark slugs and source-file names with explicit patterns, keeping the v1 corpus naming contract deterministic too.
- Current progress: the current optimization-mode inventory now has a checked-in snapshot in [`plan/phase-9/optimization-inventory.md`](./phase-9/optimization-inventory.md), and the optimizer-level regression suite now directly anchors that `fast` keeps simple binary expressions opaque while still folding literal-shape object-enumeration calls, while `release` owns the broader literal-folding reduction. The same inventory snapshot is now also pinned by the schema-doc drift net in `crates/kali_cli/tests/schema_docs.rs`, so the checked-in optimization-mode summary stays deterministic alongside the optimizer and benchmark coverage.
- Current progress: release-mode object-enumeration folding now has direct optimizer-level regression anchors for `Object.keys()`, `Object.entries()`, and `Object.values()` over literal object shapes, and release-advanced now also has matching regression anchors for the same literal-shape enumeration forms, keeping the current optimization inventory backed by tests for the three supported enumeration forms in both optimization tiers. `Reflect.ownKeys()` now also folds through const-bound literal aliases in release and release-advanced mode, keeping the Reflect path aligned with the existing alias-depth coverage.
- Current progress: the same object-enumeration folding path now also follows one-step const alias rebinding in both release tiers, so the optimizer keeps enumerations folded when the literal object is referenced through an intermediate `const` alias before being passed to `Object.keys()` / `Object.entries()` / `Object.values()`, and the alias-chain regression now also survives a second `const` alias hop so the folding path stays honest through a deeper rebinding chain.
- Current progress: release-advanced now also has a nullish literal-argument specialization regression, keeping the literal-argument specialization family visibly covered in the higher optimization tier too. Release-advanced now also has a BigInt literal-argument specialization regression, keeping the same specialization family visible on the BigInt benchmark lane as well. Fast mode now also keeps nullish literal-argument callsites unspecialized, so the nullish specialization family remains gated to the release tiers while the fast-mode minimality baseline stays explicit.
- No optimization that weakens sandbox, diagnostics, or AOT-only constraints.

## Verification and contracts

Owners: `specs/16-testing.md`, `specs/17-verification.md`, `specs/18-schemas.md`, `proofs/BOUNDARY.md`.

Remaining goals:

- Widen Lean models beyond the current published boundary only with mechanized theorem inventory.
- Connect proof-trigger policy to any implementation/spec paths newly claimed as covered.
- Add schema conformance checks for every machine-readable success and failure output.
- Current progress: `schemas/result/package-effects/v1.json` and `schemas/result/package-audit/v1.json` now have explicit contract assertions in `crates/kali_cli/tests/schema_docs.rs`, pinning the registry-analysis payload contract at the schema-document level, and the `effects` / `package-effects` payload schemas are now exact rather than reserved shells. The `effects` and `package-effects` schema titles/descriptions are now pinned there too, so the JSON-report drift net catches accidental wording drift alongside the structural contract. The CLI now also has direct JSON smoke coverage for both the application and library `init` scaffolds plus the existing failure shape, so the `init` envelope contract stays exercised at the command boundary instead of only at the schema-document layer. The `check`, `run`, and `test` JSON payloads now also validate before emission, so the common command/result envelopes stay machine-checked instead of relying only on schema files and snapshot tests. The package-audit payload validator now also rejects non-null payloads, keeping the envelope-only JSON command contract explicit in the validator path too.
- Current progress: the same schema-document test now also asserts the core result payloads for `check`, `run`, `install`, `fmt`, `lint`, `effects`, `build`, and `test`, and now also pins the supporting diagnostic, manifest, lockfile, sandbox-policy, specialized artifact-metadata projection schema documents, and the binding-package manifest schema, with the base `schemas/artifact-meta/v1.json` contract now also pinned on its required fields, enums, and export-item shape, widening the drift net across the main command/result/config envelopes without changing any public payload shape. The build result schema is now also checked variant-by-variant for the executable, lib, bundle, capi, component, and artifact-only fallbacks, so the build artifact-kind contract remains explicit as the surface grows, and the artifact-only fallback's `artifacts` field is now explicitly pinned as an array. The build result contract now also names the optional provenance extras that the CLI already emits (`profileDataHash`, `witPath`, and `bindingPackagePath`) so the JSON envelope stays aligned with the generated artifact metadata. The package-effects regression set now also pins combined `computed-host-access` + `eval` reports to the canonical lexical `dynamicReasons` order, keeping the native JSON package-analysis contract deterministic when multiple dynamic reasons coexist. The package-effects schema now also pins the `runtimeProfiles` / `compatFeatures` string-item shapes, so the registry-analysis report context stays aligned with the shared effect-report contract in JSON mode too. The `kali doctor` JSON payload schema is now also pinned in the schema-doc drift net, the README command-reference drift test now also covers the `doctor`, `init --lib`, `init --output json`, `test --coverage`, and sandboxed build variant code-block examples, runtime-smoke coverage now exercises the `doctor` JSON envelope with a deterministic browser-harness override, and the dedicated doctor corpus now also covers the auto-detected browser-harness baseline in JSON while the human-output doctor corpus now also covers the same auto-selected baseline; `doctor` now also stays machine-readable under `--quiet` and `--pretty` together, and the payload now also carries the declarative browser runtime contract snapshot, the native JSON emission path now validates the `effects`, `package-effects`, and `package-audit` payload shapes before printing, keeping the environment/debug contract aligned with the rest of the machine-readable CLI surface, and the runtime-smoke human branch now also pins the supported browser runtime commands note explicitly. The doctor schema now also pins the browser harness and browser runtime contract object shapes more tightly, including exact schema fragments for `envVar`, `source`, `override`, `command`, `hostLabel`, `hostDescription`, `supportedCommands`, `diagnosticHint`, and `diagnosticNotes`, and now also pins the string-item shapes for `supportedCommands` and `diagnosticNotes`, and now also requires `hostDescriptionNote` to be a non-empty string, so the browser harness summary stays machine-readable without drifting on the command-vector or contract-array shapes. The doctor corpus now also exercises the unavailable-harness-executable branch for both human and JSON output, and now also covers the auto-selected browser-harness human-output branch; the unavailable-harness JSON regression now also pins the browser runtime contract snapshot, and the human-output branch now mirrors the same contract section explicitly, so the `executableAvailable` probe stays explicit when the selected browser harness command is present but not invocable. `kali init` now also has a dedicated schema-v1 result payload contract and schema-doc coverage for its JSON envelope, keeping the scaffold command's machine-readable output aligned with the filesystem-scaffolding tests. The common JSON envelope emitter now also sanity-checks the schema-v1 top-level envelope shape before printing, and the emitter validation now also walks the emitted diagnostic arrays so malformed envelope assembly or malformed diagnostic JSON fails fast at the CLI boundary instead of drifting silently into the output stream. The common JSON envelope schema now also pins the optional `stdout` / `stderr` transport fields and the integer `exitCode` shape, so the shared envelope stays explicit across the ordinary CLI result payloads as well. The common JSON envelope validator now also has a direct regression for fractional `exitCode` values, so the integer-only exit-code contract stays explicit in runtime coverage too, and the transport-field regression now also covers non-string `stdout` / `stderr` values while the `timings` extension stays constrained to an array of objects, so the envelope path stays typed as well. The emitted envelope regression now also exercises simultaneous `stdout` / `stderr` string capture, keeping the transport-field pair explicit on the happy path too. The validator now also keeps schema-permitted extension fields explicit in `timings` items and diagnostic `context` metadata, so the open-ended pockets stay covered alongside the required-key checks. The same validator now also keeps the structured diagnostic context aligned with the current serializer shape by accepting the existing null-or-string `configPath` / `flag` serialization and rejecting non-string values there. It now also rejects non-object diagnostic contexts and missing `origin` fields before the serializer rehydrates them. The diagnostic `help` field now also has an explicit null-value acceptance regression, complementing the existing non-string rejection. The same machine-contract coverage now also exercises suggested-fix payloads, including nested text-edit shape checks and the schema-permitted extension path for fix metadata, so the fix contract stays explicit alongside labels and related items. Generated artifact metadata now also round-trips through its own schema-v1 validator before it is embedded or serialized, so the artifact-metadata contract is checked at generation time instead of relying only on later file reloads. The binding-package manifest loader now also validates the schema-v1 required field types for `moduleName`, `hostAbiVersion`, `minHostAbiVersion`, and the generated artifact paths in `artifacts.library`, `artifacts.metadata`, `artifacts.exportsHeader`, and `artifacts.glue`, including array-shape and element-type checks for `artifacts.glue`; the schema-doc drift net now also pins `maxSpecializations` plus the string-element shape of `artifacts.glue`, so the sidecar manifest contract stays aligned with its published schema instead of only checking field presence. The same drift net now also records the current snapshot's implementation-specific build-result artifact-kind labels (`meta-json` plus browser-bundle `chunk-*` labels), keeping the checked-in payload vocabulary explicit without promoting those labels to new canonical kinds.
- Keep proof theorem/property inventories out of plan files; `proofs/BOUNDARY.md` remains the sole theorem/property inventory owner.
