# Spec Gap Map

This map lists active implementation goals implied by the specs after the current repository baseline. It is not an availability matrix; use [`../specs/19-feature-maturity.md`](../specs/19-feature-maturity.md) for public support status.

Keep this map high-level: exact coverage belongs in tests, schemas, proof-boundary files, and maturity/current-state notes.

## Language semantics and frontend depth

Owners: `specs/02-lexer-parser.md`, `specs/03-ast.md`, `specs/04-type-system.md`, `specs/05-ir.md`, `specs/10-runtime.md`, `specs/16-testing.md`.

Remaining goals:

- Promote parser-accepted but unavailable semantics only when checker, lowering, runtime, and conformance evidence exist.
- Implement full generator and async-generator semantics, including resumable state machines, `yield` / `yield*`, async interaction, error propagation, and finalization; until then keep unsupported forms behind canonical `E5506` gates. The current codegen evidence now also preserves function-plan flavor metadata for default-export async-generator declarations, including anonymous default-export forms, but that does not promote executable lowering.
- Expand `for...of` / `for await...of` from bounded static slices toward full iterator and async-iterator protocol behavior, including close/finalization/error semantics; the browser-targeted build smoke now also covers the `for await` const-alias, const-string-alias, and const-boolean-alias browser TS slices, but broader protocol behavior remains open. The shared `Reflect.ownKeys` frozen-callable inventory now also includes the parenthesized double-quoted bracket-root `Object.freeze((globalThis["Reflect"])["ownKeys"])` alias alongside the existing bracket-root and single-quoted wrapper forms.
- Static `Array.from(new Set(...))` and `Array.from(new Map(...))` slices now reuse the existing set/map collectors, and the browser harness/bundle smoke lanes now cover that Array.from set/map slice too; the same supported iterable path now also accepts frozen constructor-result wrappers such as `Object.freeze(new Set(...))` and `Object.freeze(new Map(...))` plus the parenthesized `Object.freeze((new Set(...)))` / `Object.freeze((new Map(...)))` variants, with browser-harness/browser-bundle smoke now also covering that frozen constructor-result slice in JS, TS, JSX, and TSX input, and browser-targeted check smoke now also covering the same frozen constructor-result slice in JS, TS, JSX, and TSX input; frozen object-helper iterable slices such as `Object.freeze(Object.keys(...))` / `Object.freeze(Object.values(...))` / `Object.freeze(Object.entries(...))` now also flow through the checker/runtime smoke path; the supported object-enumeration iterator slice now also accepts frozen callable aliases around `Object.keys(...)`, `Object.values(...)`, `Object.entries(...)`, and `Reflect.ownKeys(...)`, including the single-quoted `globalThis['Reflect'].ownKeys` / `globalThis['Reflect']['ownKeys']` frozen aliases and the dot-root single-quoted `globalThis.Reflect['ownKeys']` / `Object.freeze((globalThis.Reflect['ownKeys']))` pair, and the shared helper inventory now also includes the bare `Reflect.ownKeys` / `Object.freeze((Reflect.ownKeys))` pair; the direct runtime and browser harness `for await` object-helper smoke now also covers the frozen callable `Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` aliases on that same path; the supported Array.from set/map slice now also has explicit break/continue smoke on the standalone and browser-requested harness paths, and the `Reflect.ownKeys(...)` break/continue slice now also rides the same smoke path; the browser runtime `Object.fromEntries(...)` hasOwn smoke now also covers frozen `Object.hasOwn` and `Object.prototype.hasOwnProperty.call` aliases over the same frozen object operand, and `Object.fromEntries(Object.freeze([...]))` now also flows through the same static object-model slice; the bracketed `globalThis["Array"].from` freeze alias now also shares that same smoke coverage, the fully bracketed `globalThis["Array"]["from"]` alias now also joins that coverage, the single-quoted bracketed `globalThis['Array']['from']` alias now also joins that coverage, and the mixed-quote bracketed `globalThis["Array"]['from']` / `globalThis['Array']["from"]` freeze aliases now also join that same coverage, while the dot-root `Object.freeze(globalThis.Array.from)` / `Object.freeze(globalThis.Array["from"])` aliases now also join the standalone, browser-requested, and browser-bundle smoke lanes, with the parenthesized dot-root `Object.freeze((globalThis.Array).from)` / `Object.freeze((globalThis.Array)["from"])` / `Object.freeze((globalThis.Array)['from'])` aliases now also joining that same coverage; current smoke now also covers the parenthesized bracket-root `Object.freeze((globalThis["Array"])["from"])` / `Object.freeze((globalThis['Array'])['from'])` aliases, plus `for await` over frozen constructor-result wrappers and frozen object-helper iteration targets on the same supported path, and build smoke now also mirrors the single-quoted `globalThis['Set']` / `globalThis['Map']` constructor spellings and their frozen constructor-result wrappers on the same Array.from slice; broader generator/iterator semantics remain open.
- Continue widening expression/operator, object-model, BigInt, Math, and dynamic-loading semantics where translation-safe; keep unsupported dynamic language forms explicitly gated. The optimizer now also folds numeric and BigInt remainder (`%`) expressions on constant literal inputs, the supported `Math.hypot` perfect-square-sum slice now also accepts frozen callable aliases around `Math.hypot` and `globalThis.Math.hypot` on the same browser/check/run smoke path, and the supported `Math.atan2` zero slice now also accepts frozen callable aliases such as `Object.freeze(globalThis.Math.atan2)`, `Object.freeze(globalThis["Math"]["atan2"])`, and `Object.freeze(Math.atan2)` on the same checker/browser smoke path. The `Math.imul` / `Math.clz32` slices now also accept bracketed and frozen callable aliases on the browser bundle/harness smoke path, but broader non-identity built-in work remains open. Static string-key resolution now also accepts same-branch conditional expressions, keeping `Object.hasOwn`-style helpers aligned with the other bounded-literal wrapper slices.
- The supported `Math.log2` / `Math.log10` literal slice now also accepts frozen callable aliases such as `Object.freeze(Math.log2)`, `Object.freeze(Math.log10)`, and their bracketed `globalThis` spellings on the same checker/runtime/browser smoke path, while broader non-identity built-in work remains open.
- Grow bounded TypeScript/JavaScript inference only within deterministic budget rules, especially at exported/public boundaries and cross-module analysis.
- Transparent sequence wrappers around static object-helper targets are now covered in the checker and browser/standalone smoke lanes, and frozen object-helper iterable slices now also carry through the same supported loop path; object-literal computed property names now also unwrap transparent `Object.freeze(...)` calls in the parser for the same static-name slice; the same object-enumeration loop path now also accepts parenthesized frozen-callable wrappers around `Object.keys(...)`, `Object.values(...)`, `Object.entries(...)`, and `Reflect.ownKeys(...)` in the checker, codegen, and smoke lanes, including the parenthesized bracket-root `Reflect.ownKeys` aliases; broader generator/iterator semantics remain open.
- Frozen object roots now also flow through the cheap `Math` / `Number` / object-helper predicate paths, and the `Number.is*` predicate slice now also accepts the fully bracketed `globalThis["Number"]["isFinite"]` / `globalThis["Number"]["isNaN"]` / `globalThis["Number"]["isInteger"]` / `globalThis["Number"]["isSafeInteger"]` spellings plus frozen bracketed `Number["isFinite"]` / `Number["isInteger"]` / `Number["isSafeInteger"]` aliases, but broader generator/iterator semantics remain open.
- Keep conformance dashboards concise: snapshots of supported/gated behavior, not implementation journals.
- Continue applying transparent wrapper handling to cheap numeric-literal Math slices where it stays deterministic; the frozen numeric-literal wrapper slice now joins the existing supported `Math.round` path, including the optional-chain-wrapped `Object.freeze(globalThis?.Math.round)` alias, the parenthesized optional-chain alias `Object.freeze((globalThis?.Math.round))`, plus the bracketed frozen-callable `Object.freeze(globalThis.Math["round"])` / `Object.freeze(globalThis["Math"]["round"])` aliases and the bracketed-dot `Object.freeze(globalThis["Math"].round)` alias, and the single-quoted `Object.freeze(globalThis['Math']['round'])` alias, with the runtime/browser-harness smoke lanes now also carrying the quote-variant `Object.freeze(globalThis.Math['round'])` / `Object.freeze(globalThis["Math"]['round'])` / `Object.freeze(globalThis['Math'].round)` aliases; the direct `Math.abs` / `Math.sign` aliases now also have browser bundle/harness coverage, and frozen `globalThis.Math.abs` / `globalThis.Math.sign` aliases now also share that slice, the direct `Object.freeze(Math.floor)` / `Object.freeze(Math.trunc)` / `Object.freeze(Math.ceil)` aliases now also stay on the supported floor/trunc/ceil slice in checker/codegen/runtime smoke coverage, including the single-quoted `Math['floor']` / `Math['trunc']` / `Math['ceil']` spellings; the `Math.sqrt` perfect-square slice now also accepts frozen callable aliases in the checker/lowering/build/runtime smoke path, the `Math.expm1` / `Math.log1p` zero-identity slice now also accepts frozen root wrappers around `Math`, `globalThis.Math`, and `globalThis["Math"]` in the checker plus browser bundle/harness smoke paths, the `Math.exp` / `Math.log` exact-identity slice now also accepts parenthesized callable aliases such as `((globalThis.Math.exp))` and `Object.freeze((globalThis.Math.log))` in the browser-harness smoke path, the `Math.pow` slice now also keeps frozen callable aliases aligned across js-like extensions, including the single-quoted `globalThis['Math']['pow']` and `Math['pow']` spellings, and the `Math.sin` / `Math.cos` / `Math.tan` zero-identity slice now also accepts frozen callable aliases around `Math`, `globalThis.Math`, and `globalThis["Math"]` in browser bundle and browser-harness smoke coverage. The same bounded-literal resolution layer now also resolves conditional-callable wrappers around the supported `Math.round` alias chain, keeping the type-checker, codegen, and build-smoke slices aligned; browser-harness/browser-bundle smoke now also mirrors the parenthesized direct `Object.freeze((Math.round))` alias on that same supported slice and now also asserts that identity on the same smoke path, while the parenthesized bracketed-dot `Object.freeze((globalThis["Math"].round))` alias now also shares that same slice, and the Deno/browser build smoke now also mirrors that parenthesized bracketed-dot alias plus the parenthesized direct `Object.freeze((globalThis.Math.round))` / `Object.freeze((globalThis["Math"].round))` aliases.
- Helper plumbing for conditional-expression branches now exists in the bounded-literal resolution layer, and the supported `Math.round` numeric-literal slice now also keeps the single-quoted `Object.freeze(globalThis['Math']['round'])` wrapper path localized there.

## Runtime, host, and platform expansion

Owners: `specs/09-sandboxing.md`, `specs/10-runtime.md`, `specs/11-standard-apis.md`, `specs/12-cli.md`, `specs/18-schemas.md`, `specs/19-feature-maturity.md`.

Remaining goals:

- Complete guest-facing threaded runtime semantics beyond profile acceptance, while preserving no-GC/no-JIT invariants and resource-budget honesty.
- Decide whether harness-assisted `run --api browser` / `test --api browser` graduates to a stable standalone browser runtime contract; if yes, specify host ownership, sandbox limits, summary behavior, and failure modes first.
- Add late host APIs only with explicit effect keys, policy behavior, resource budgets, and command/API-surface gating: subprocesses, sockets/listeners, workers/threads, environment materialization, process control, and late Node/Deno modules.
- Triage late object/runtime APIs (`Proxy`, weak references, finalization, broader `Intl`, `SharedArrayBuffer`, `Atomics`, and broader object helpers) against memory, threading, and optimization constraints before promotion. Await-wrapped and `Object.freeze`-wrapped `Proxy.revocable` aliases now still flow through the canonical late-object-model gate, but the broader late object/runtime surface remains open.
- Keep browser-targeted build/check support, browser harness execution, and Kali-hosted sandbox enforcement distinct.

## Ecosystem and packages

Owners: `specs/11-standard-apis.md`, `specs/14-packages.md`, `specs/16-testing.md`, `specs/18-schemas.md`, `specs/19-feature-maturity.md`.

Remaining goals:

- Expand package-corpus coverage by package shape, host/API context, source class, command, and support rung without turning individual successes into blanket npm claims.
- Grow Node package support only where required Node built-ins and process APIs are explicitly supported or deliberately gated.
- Grow browser deployability and browser-harness package evidence while keeping standalone browser runtime claims separate.
- Keep native, binary, bootstrap-heavy, host-mismatched, and published-bin-entrypoint packages rejected by default unless specs introduce a mediated path.
- Keep registry-analysis commands (`package-effects`, `package-audit`) single-package and registry-identifier based unless a future spec/schema revision defines batch/local/raw-URL behavior. The `package-effects` smoke now also covers the canonical `jsr:@std/path` label path, and the validator regressions now also pin canonical label mismatches for both jsr and npm coordinates plus leading/trailing whitespace rejection on package-coordinate fields, so remaining work stays on broader package-shape and support-rung expansion rather than on the basic registry identifier contract.

## Optimization, PGO, and performance

Owners: `specs/07-specialization.md`, `specs/08-wasm-codegen.md`, `specs/16-testing.md`, `specs/18-schemas.md`, `specs/19-feature-maturity.md`.

Remaining goals:

- Deepen `release` and `release-advanced` optimizations while preserving JavaScript-visible semantics, sandbox effects, proof boundaries, and deterministic artifacts.
- Treat `--profile` as a deterministic build-only additive input; do not create a hidden fourth build mode.
- Promote performance wording only when benchmark evidence names workload, build mode, baseline, and reproducibility constraints.
- Keep optimization inventories as concise evidence snapshots, including the `folded-arithmetic-variant` slice now also has a JS workload form (`folded-arithmetic-variant-js`).

## Verification and contracts

Owners: `specs/16-testing.md`, `specs/17-verification.md`, `specs/18-schemas.md`, `proofs/BOUNDARY.md`.

Remaining goals:

- Widen Lean models for ownership, effects, type-system, and lowering slices in small named increments.
- Update `proofs/BOUNDARY.md` before any proof-backed wording changes.
- Expand proof CI triggers only when the published proof boundary claims implementation or spec paths outside the proof tree.
- Continue hardening JSON payload, artifact-manifest, schema-drift, diagnostics, source spans, and CLI-doc contracts while respecting schema extension posture; the public package-effects validator now also rejects non-object payloads, unsupported registry names, registry-prefixed package names, and mismatched canonical `report.entryPoints[0]` labels for `npm` and `jsr` coordinates, and the install payload now also rejects empty or whitespace-only `manifestPath` / `lockPath` strings when present, so that emission slices are covered and remaining work stays on the other schema-drift edges.
- Avoid duplicating theorem inventories in plan files; `proofs/BOUNDARY.md` remains the sole proof-boundary inventory.
