# Spec Gap Map

This map lists active implementation goals implied by the specs after the current repository baseline. It is not an availability matrix; use [`../specs/19-feature-maturity.md`](../specs/19-feature-maturity.md) for public support status.

Keep this map high-level: exact coverage belongs in tests and maturity/current-state notes.

## Language semantics and frontend depth

Owners: `specs/02-lexer-parser.md`, `specs/03-ast.md`, `specs/04-type-system.md`, `specs/05-ir.md`, `specs/10-runtime.md`, `specs/16-testing.md`.

Remaining goals:

- Promote parser-accepted but unavailable semantics only when checker, lowering, runtime, and conformance evidence exist.
- Implement full generator and async-generator semantics, including resumable state machines, `yield` / `yield*`, async interaction, error propagation, and finalization; until then keep unsupported forms behind canonical `E5506` gates. The class-expression generator/async-generator rejection matrix now also has explicit `check` / `build` and type-resolution coverage through variable-initializer and default-export expression forms in JS/TS/JSX/TSX input, keeping the direct runtime gate aligned with the checker and build preflight paths, and standalone `run` / `test` generator-function rejection smoke now also has JSON-output coverage in JS and TS input. Browser-analysis build smoke now also covers the generator class-expression rejection slice in JS, JSX, and TSX input. The default-export class-expression flavor-preservation path is now also mirrored through AST, HIR, MIR, and LIR lowering for the same generator metadata slice.
- Expand `for...of` / `for await...of` from bounded static slices toward full iterator and async-iterator protocol behavior, including close/finalization/error semantics; current smoke now also includes the standalone TS `for await...of` string-concatenation slice plus browser-bundle/browser-harness coverage for the `for...of` template-literal slice over statically known string operands in JS, TS, JSX, and TSX input, in addition to the string-concatenation slice in JS, TS, JSX, and TSX input, browser-harness `Object.values([...Object.fromEntries(...)])` spread coverage over frozen operands, browser iterator rejection coverage now also includes non-literal `Object.entries(...)` sources in the browser-targeted `check` / `build --bundle` lanes and their inherited browser-config forms, and the `globalThis["Reflect"]["ownKeys"]` alias spelling now also resolves on the supported `Reflect.ownKeys(...)` slice, with the browser/runtime smoke now also covering frozen callable aliases for the bracketed `Reflect.ownKeys(...)` target, including the parenthesized mixed-bracket `Object.freeze((globalThis.Reflect["ownKeys"]))` alias and the browser-harness/browser-bundle parenthesized mixed-bracket alias. Static `new Set(...)` constructor slices now also type-check, build, and execute on the supported iterable inputs, with browser-requested run/test browser-harness coverage now also spanning JS, TS, JSX, and TSX input, and static `new Map(...)` constructor slices now also ride the same supported input matrix; the browser-harness/browser-bundle Set/Map smoke now also covers frozen constructor aliases such as `Object.freeze(Set)` / `Object.freeze(Map)` plus frozen `globalThis.Set` / `globalThis["Set"]` and `globalThis.Map` / `globalThis['Map']` aliases in runtime, browser-harness, and browser-bundle smoke, and now also includes parenthesized freeze-wrapper aliases around `Set` / `Map` constructor references on the same shared slice. Standalone run/test smoke now also covers the `for...of` template-literal slice in TS input with JSON-output coverage, and standalone run/test smoke now also covers the same template-literal slice in JS input with JSON-output coverage, and the browser bundle matrix now also includes direct `Object.values(...)` iteration over object-literal inputs in JS, TS, JSX, and TSX input, and the constructor slices now also keep alias-chain / transparent-wrapper references covered through the bounded root-resolution path, including const-alias and parenthesized-alias `Set`/`Map` constructor coverage in runtime, browser-harness, codegen, and type-resolution smoke, plus frozen constructor aliases such as `Object.freeze(Set)` / `Object.freeze(Map)` in the codegen/runtime smoke path; standalone runtime smoke now also covers the frozen `Set` / `Map` aliases with parenthesized frozen wrappers, plus exact bracketed `globalThis["Set"]` / `globalThis['Map']` root coverage in codegen smoke and the dotted `globalThis.Set` / `globalThis.Map` spellings in checker/build and type-resolution smoke. Standalone runtime smoke now also covers the single-quoted frozen constructor aliases `Object.freeze(globalThis['Set'])` / `Object.freeze((globalThis['Set']))` and `Object.freeze(globalThis['Map'])` / `Object.freeze((globalThis['Map']))` on the same supported slice. The current smoke now also exercises abrupt return/finally and throw/finally sequencing on the supported Set/Map constructor slice across the existing run/test, browser-harness, and browser-bundle coverage, and the object-enumeration break/continue slice now also has standalone, browser-harness, and browser-bundle smoke on the supported `Object.keys(...)` iteration path, while the browser-bundle object-enumeration finalization smoke now also covers the same `Object.keys(...)` / `Object.entries(...)` return/finally and throw/finally matrix across JS, TS, JSX, and TSX input, and the array break/continue slice now also has browser-harness smoke for the supported `for...of` / `for await...of` array iteration path in JS, TS, JSX, and TSX input with JSON-output coverage. Browser-requested run/test browser-harness rejection coverage now also includes non-literal `new Set(values.filter(Boolean))` and `new Map(values.filter(Boolean))` sources in JS input, and the parser now also treats `await` as an expression in the `for await` iterable position, so the supported `for await` array slice now accepts a transparent `await` wrapper over a literal array in parser, checker, codegen, runtime, build, and browser-harness smoke, and browser-requested run/test browser-harness runtime smoke now also covers direct and frozen `Array.from(...)` iteration over supported array inputs in JS, with the frozen-callable inventory now also mirroring the single-quoted `globalThis['Array']` / `Array['from']` spellings and the codegen smoke now also covering the single-quoted `globalThis['Array']['from']` / `Array['from']` spellings on that same supported path.
- Continue widening expression/operator, object-model, BigInt, Math, and dynamic-loading semantics where translation-safe; keep unsupported dynamic language forms explicitly gated. Recent work widened the browser-requested run/test browser-harness `Math.pow` slice to include the bracketed frozen-wrapper aliases around `globalThis.Math["pow"]` and `globalThis["Math"]["pow"]` in JS, TS, JSX, and TSX input with JSON-output coverage, and the shared browser Math.pow inventory now also has a dedicated alias-list helper so the browser source assembly stays duplicate-free. Recent work widened the static `Number.isInteger` / `Number.isSafeInteger` predicate slice alongside the existing `Number.isFinite` / `Number.isNaN` slice and now also keeps the mixed-bracket `globalThis.Number[...]` spellings covered in the smoke corpus. The type-resolution regression suite now also mirrors the `Number.isSafeInteger` bracketed alias family in JS input, browser-harness `Object.is` alias-chain coverage now also spans object/array same-reference and frozen-reference comparisons across the JS, TS, JSX, and TSX smoke matrix, including the bare `Object["is"]` root spelling on the browser-harness same-reference/frozen-reference path, the browser-harness `Object.values([...Object.fromEntries(...)])` spread slice now also covers frozen operands across the same input matrix, browser-requested browser-harness/browser-bundle smoke now also carries `Object.is` and `Number.isSafeInteger` inputs on the browser helper path, browser bundle/harness object-key smoke now also carries `Object.keys` and `Reflect.ownKeys` helper inputs, and standalone run/test smoke now also covers the `for await` spread-of-`Object.values` / `Object.keys` / `Object.entries` slice over `Object.fromEntries(...)` operands in JS and TS input with JSON-output coverage, the browser-requested run/test JSON smoke now mirrors that object/array/frozen-reference matrix across the same input set, the browser-requested `Math.atan2` smoke now also exercises the single-quoted bracketed zero slice across JS, TS, JSX, and TSX input with JSON-output coverage, and browser-harness/browser-bundle smoke now also covers that same single-quoted bracketed zero slice in TS, `.js`, JSX, and TSX input, while browser-requested sequence-wrapped template-literal dynamic import smoke now also covers TS, JSX, and TSX input with JSON-output coverage, and type-resolution smoke now also keeps supported `Object.hasOwn(...)` and `Math.floor(...)` helper calls resolved through the same transparent wrapper path, including the single-quoted `globalThis['Reflect']['ownKeys']` alias on the wrapper path; The same transparent-wrapper path now also accepts `Object.freeze`-wrapped `Object.hasOwn` and `Math.floor` helper calls in standalone JS smoke, including the bracketed `globalThis["Object"]["hasOwn"]` and `globalThis["Math"]["floor"]` forms, and the browser and frozen-object `Object.hasOwn` smoke now also reuse the shared frozen-callable condition helper from `kali_common` while the standalone/browser frozen-object fixtures now also reuse the shared frozen-callable source helper for the frozen alias statements themselves, keeping that corpus aligned with the same helper-source pattern used by the Node zero-probe inventory, and the frozen `Object.prototype.hasOwnProperty.call` helper family now follows the same shared-browser/direct smoke pattern, including the bracketed `globalThis["Object"].hasOwnProperty.call` and `globalThis["Object"]["hasOwnProperty"]["call"]` aliases. The direct `Object.prototype.hasOwnProperty.call` binding source now also lives in `kali_common`, so the browser harness/bundle smoke can reuse the same canonical helper line. The shared `Object.prototype.hasOwnProperty.call` helper inventory now also includes the bracketed `globalThis["Object"].prototype.hasOwnProperty["call"]` alias path so that helper remains single-sourced across the member-access forms. Codegen smoke now also covers the same transparent-wrapper helper aliases, and the codegen regression suite now also pins the frozen `globalThis.Math["pow"]` and `globalThis["Math"]["pow"]` wrapper aliases on the supported `Math.pow` alias chain. Browser-harness and JS-input `Reflect.ownKeys(...)` smoke now also covers the single-quoted `globalThis['Reflect']['ownKeys'](...)` alias on the supported frozen-object slice, and the browser runtime/browser-harness smoke now also covers the fully bracketed `globalThis["Reflect"]["ownKeys"](...)` alias on that same slice, plus the transparent `Object.freeze(globalThis.Reflect.ownKeys)` callable alias and its parenthesized freeze-wrapper sibling on the same supported slice. BigInt arithmetic smoke now also covers subtraction and division slices in both TS and JS input. The browser-harness `Math.atan2` trailing-argument evaluation smoke now also has browser-bundle coverage in JS, TS, JSX, and TSX input with JSON-output coverage. Browser-requested browser-harness/browser-bundle smoke now also accepts const aliases that resolve to the supported `Object.is` and `Number.is*` callables, and the browser-targeted `Object.hasOwn` / `Object.prototype.hasOwnProperty.call` slice now also accepts const aliases that resolve to those callables. Promise.allSettled smoke now also has browser-harness/browser-bundle coverage for the freeze-wrapped bracketed callable alias on the same supported slice, and the shared browser-body helper now also carries the parenthesized frozen bracketed alias variant for that same smoke family. Type-checker call targets wrapped in `as` or `satisfies` now resolve for direct callable references, and `Object.freeze(...)` now also behaves as a transparent callable wrapper for the supported Node zero-probe slice; the wrapped-call-target corpus now also covers sequence-callable-target aliases plus transparent `process` object wrappers on the documented Node `process.kill(0)` path, and the build-preflight process-control smoke now also mirrors the browser-safe receiver-freeze aliases and their +0 siblings `Object.freeze(globalThis.process)["kill"](0)` / `Object.freeze(globalThis["process"])["kill"](0)` on the same Node/browser rejection matrix; the Node API-surface regression now also picks up the direct `process["kill"](+0)` alias there alongside the existing `globalThis.process["kill"]` const/sequence alias variant, while broader function-reference wrappers remain gated until that later compatibility path is promoted; the TS/TSX satisfies/type-assertion zero-probe wrapper sources now also cover the bracketed `globalThis["process"].kill` and `globalThis["process"]["kill"]` call targets so the typed wrapper corpus stays aligned with the shared zero-probe inventory. The browser late-compat smoke now also carries the wrapped `((process["kill"]))(+0)` sibling on the JS/JSX/TS/TSX matrix. The browser-harness/browser-bundle `Math.pow` alias-chain smoke now also covers the transparent `Object.freeze(globalThis.Math["pow"])` alias and parenthesized freeze-wrapper spellings around `globalThis.Math["pow"]` and `globalThis["Math"]["pow"]` on the same supported slice, now also exercises the direct single-quoted `Math['pow']` / `globalThis.Math['pow']` / `globalThis['Math'].pow` / `globalThis['Math']['pow']` call-target spellings in the same smoke, and the shared helper inventory now also tracks those spellings on that same browser alias path. The browser bundle/browser-harness bracketed-root regression now also has explicit coverage for the parenthesized freeze-wrapper aliases around `globalThis.Math["pow"]` and `globalThis["Math"]["pow"]`. The browser-targeted negative `Math.pow(2, -1)` rejection path now also covers JSX and TSX inputs on both the Deno and browser surfaces, and the browser-requested run/test harness rejection path now also mirrors JSX and TSX input. The browser-harness/browser-bundle `Math.floor` / `Math.trunc` / `Math.ceil` frozen-callable smoke now also covers the direct dot-root spellings `Object.freeze(globalThis.Math.floor)` / `Object.freeze(globalThis["Math"].floor)` and their `trunc` / `ceil` counterparts, keeping the helper inventory aligned with the broader alias matrix. The `Array.from(...)` slice now also recognizes the `globalThis.Array.from(...)` alias in checker, codegen, and browser/runtime smoke, and the bracketed `globalThis["Array"].from(...)` / `globalThis["Array"]["from"](...)` spellings now also ride that same supported path, while type-resolution smoke now also recognizes the single-quoted bracketed `globalThis['Array']['from']` and `Array['from']` spellings.
- Grow bounded TypeScript/JavaScript inference only within deterministic budget rules, especially at exported/public boundaries and cross-module analysis. The current regression corpus now also pins default-export alias-chain resolution across the source graph, including `export { default as ... }` re-export syntax, and freeze-wrapped callable exports in the same bounded export slice.
- Keep conformance dashboards concise: snapshots of supported/gated behavior, not implementation journals. The runtime and browser late-compat smoke now also assert the shared zero-probe inventory source directly, so the canonical helper and the fixture stay synchronized, the late-process-control preamble now composes those shared statement segments explicitly before appending the inventory, the shared regression now also asserts the parenthesized receiver-freeze bracket source exactly once, and the browser `Math.pow` harness/bundle fixtures now also reuse a single combined alias-inventory helper for the direct and frozen callable lines, including the full shared browser alias inventory across JS/TS/JSX/TSX inputs.

## Runtime, host, and platform expansion

Owners: `specs/09-sandboxing.md`, `specs/10-runtime.md`, `specs/11-standard-apis.md`, `specs/12-cli.md`, `specs/18-schemas.md`, `specs/19-feature-maturity.md`.

Remaining goals:

- Complete guest-facing threaded runtime semantics beyond profile acceptance, while preserving no-GC/no-JIT invariants and resource-budget honesty; browser-targeted check/build rejection coverage for `--wasm-threads` now also spans JS, TS, JSX, and TSX input alongside the browser-harness and browser-bundle work, and the browser-harness zero-capable `--max-threads 0` budget path now also has direct coverage alongside the positive-override smoke, while browser-requested run/test JS, TS, JSX, and TSX input now also covers the zero-capable `--max-threads 0` / `--max-spawned-processes 0` path, including the dedicated zero-spawned-process regression in the current repository, and inherited browser `runtimeProfiles=["wasm-threads"]` browser-harness max-thread smoke now also spans JS, TS, JSX, and TSX input, including the zero-capable `--max-threads 0` budget path on that same matrix. Browser-requested run/test zero-spawned-process coverage now also spans JS, TS, JSX, and TSX input, matching the zero-thread matrix, and positive browser-requested / explicit browser-API `--max-spawned-processes` requests now also reject through the canonical `E5506` budget gate.
- Decide whether harness-assisted `run --api browser` / `test --api browser` graduates to a stable standalone browser runtime contract; if yes, specify host ownership, sandbox limits, summary behavior, and failure modes first.
- Add late host APIs only with explicit effect keys, policy behavior, resource budgets, and command/API-surface gating: subprocesses, sockets/listeners, workers/threads, environment materialization, process control, and late Node/Deno modules. Browser late-compat process-control coverage now also rejects the alias-target zero-probe spelling `globalThis["process"]["kill"](+0)` and the bracketed `process["kill"](+0)` spelling across JS, TS, JSX, and TSX input, plus the receiver-freeze aliases and their +0 siblings `Object.freeze(process)["kill"](0)` / `Object.freeze(globalThis.process)["kill"](0)` / `Object.freeze(globalThis["process"])["kill"](0)`, and the current smoke set now also keeps the mixed frozen alias `Object.freeze(globalThis["process"].kill)(0)` aligned with the documented Node zero-probe aliases. Browser-targeted build smoke now also carries the bracketed `Object.freeze(globalThis["process"]["kill"])(+0)` alias on the same rejection slice, the browser-targeted process-control rejection corpus now reuses the shared helper-generated zero-probe source string so the canonical alias inventory stays synchronized across diagnostics and negative build evidence; the browser late-compat JS fixture now also sources that shared inventory directly, now also uses the shared parenthesized receiver-freeze helper in its regression assertions, and now also carries the parenthesized receiver-freeze dot spellings `Object.freeze((process)).kill`, `Object.freeze((globalThis.process)).kill`, and `Object.freeze((globalThis["process"])).kill`, and frozen `Proxy.revocable` aliases now also reject through the same E5506 path on the runtime and browser smoke matrices; the browser/runtime frozen revocable corpus now also carries the mixed frozen aliases `Object.freeze(globalThis.Proxy.revocable)` and `Object.freeze(globalThis["Proxy"].revocable)` alongside the bracketed `Proxy.revocable` family, including parenthesized transparent wrappers around `Proxy.revocable`. The browser-analysis `effects` wasm-threads rejection matrix now also spans JS, JSX, and TSX input on both the explicit browser-API surface and the inherited browser-analysis path, and the JSON-output matrix now mirrors that same browser-profile gate across JS/TS/JSX/TSX input, keeping the shared source-graph evidence aligned with the browser runtime profile gate. The Node API-surface late-module rejection matrix now also spans `node:worker_threads` on both explicit and inherited Node surfaces across JS, JSX, and TSX input, keeping the worker-thread gate explicit. The browser late-compat fixtures now also cover the parenthesized dot-receiver freeze aliases `Object.freeze((process).kill)(0/+0)` on that same shared helper path. The phase-three host-API rejection matrix now also spans `run` / `test` in JS, TS, JSX, and TSX input so the subprocess and network gates stay aligned with the checker/build coverage.
- Triage late object/runtime APIs (`Proxy`, weak references, finalization, broader `Intl`, `SharedArrayBuffer`, `Atomics`, and broader object helpers) against memory, threading, and optimization constraints before promotion.
- Keep browser-targeted build/check support, browser harness execution, and Kali-hosted sandbox enforcement distinct.
- The Node process-control build-preflight smoke source now also includes the browser-safe receiver-freeze `process.kill(0)` aliases (`Object.freeze(globalThis.process)["kill"](0)` and `Object.freeze(globalThis["process"])["kill"](0)`) so the unsupported-path matrix stays aligned with the Node zero-probe support slice, and the supported runtime/codegen/type smoke now also carries the mixed frozen alias `Object.freeze(globalThis["process"].kill)(0)` plus the direct `Object.freeze(process.kill)(0)` / `Object.freeze(process.kill)(+0)` aliases and the fully bracketed wrapped `((globalThis["process"]["kill"]))(+0)` alias in the shared helper wording; the current packet also exercises parenthesized transparent freeze wrappers like `Object.freeze((process.kill))(0)` / `(+0)` and the parenthesized bracketed freeze-wrapper aliases `Object.freeze((globalThis["process"]["kill"]))(0/+0)` in the shared Node/browser smoke matrix. Type-resolution and codegen smoke now also cover the parenthesized receiver-freeze aliases `Object.freeze((process))["kill"]`, `Object.freeze((globalThis.process))["kill"]`, and `Object.freeze((globalThis["process"]))["kill"]` on the same zero-probe matrix, and the browser/Deno build-preflight regression now also asserts the wrapped `process.kill((0))` / `process["kill"]((0))` / `globalThis.process.kill((0))` / `globalThis.process["kill"]((0))` / `globalThis["process"].kill((0))` / `globalThis["process"]["kill"]((0))` aliases end-to-end instead of only representative spellings.

## Ecosystem and packages

Owners: `specs/11-standard-apis.md`, `specs/14-packages.md`, `specs/16-testing.md`, `specs/18-schemas.md`, `specs/19-feature-maturity.md`.

Remaining goals:

- Expand package-corpus coverage by package shape, host/API context, source class, command, and support rung without turning individual successes into blanket npm claims. The context-free `package-audit` lane and the `package-effects` lane now also have JSON-output coverage for inherited browser+threaded config combinations, and the node process package corpus now also mirrors wrapped zero-probe aliases such as `process["kill"]((0))`, `globalThis.process.kill(+0)`, and `Object.freeze(process.kill)(+0)` to stay aligned with the shared Node smoke matrix. Registry-analysis boundaries also now keep scheme-prefixed selectors such as `npm:lodash`, `file:///tmp/pkg`, and `git+https://example.com/pkg.git` on the command-shape rejection path before lookup, so remaining work stays on the supported shape/evidence boundary rather than the registry-analysis inheritance split. The package-effects wrapper regression also pins the inherited browser + wasm-threads analysis context on the native-JSON payload path. The browser build preflight also now rejects the mixed `delete globalThis.process["env"]["KALI_BROWSER_ENV_MUTATION"]` spelling directly, keeping the process-env mutation matrix aligned across the remaining bracketed forms.
- Grow Node package support only where required Node built-ins and process APIs are explicitly supported or deliberately gated.
- Grow browser deployability and browser-harness package evidence while keeping standalone browser runtime claims separate.
- Keep native, binary, bootstrap-heavy, host-mismatched, and published-bin-entrypoint packages rejected by default unless specs introduce a mediated path.
- Keep registry-analysis commands (`package-effects`, `package-audit`) single-package and registry-identifier based unless a future spec/schema revision defines batch/local/raw-URL behavior; scheme-prefixed selectors such as `npm:lodash` stay rejected before lookup.

## Optimization, PGO, and performance

Owners: `specs/07-specialization.md`, `specs/08-wasm-codegen.md`, `specs/16-testing.md`, `specs/18-schemas.md`, `specs/19-feature-maturity.md`.

Remaining goals:

- Deepen `release` and `release-advanced` optimizations while preserving JavaScript-visible semantics, sandbox effects, proof boundaries, and deterministic artifacts.
- Treat `--profile` as a deterministic build-only additive input; do not create a hidden fourth build mode. The current repository snapshot now also rejects empty and whitespace-only profile files with the canonical `E5509` parse path in both human and JSON `build` output.
- Promote performance wording only when benchmark evidence names workload, build mode, baseline, and reproducibility constraints.
- Keep optimization inventories as concise evidence snapshots.
- The benchmark fixture set now also includes dedicated `math-pow-builtin` and `math-pow-builtin-js` slices so `Math.pow` optimization evidence stays visible alongside the existing math builtin benchmarks, and it now also includes `math-floor-builtin` / `math-floor-builtin-js` so the `Math.floor` workload stays visible in both TS and JS forms too, plus `math-round-builtin` / `math-round-builtin-js` so `Math.round` stays visible in both source classes as well.

## Verification and contracts

Owners: `specs/16-testing.md`, `specs/17-verification.md`, `specs/18-schemas.md`, `proofs/BOUNDARY.md`.

Remaining goals:

- Widen Lean models for ownership, effects, type-system, and lowering slices in small named increments.
- Update `proofs/BOUNDARY.md` before any proof-backed wording changes.
- Expand proof CI triggers only when the published proof boundary claims implementation or spec paths outside the proof tree.
- Continue hardening JSON payload, artifact-manifest, schema-drift, diagnostics, source spans, and CLI-doc contracts while respecting schema extension posture; the browser runtime contract validator now also has a combined trimmed-fields regression so the contract shape stays explicit across host labels, host descriptions, note text, and command lists, including trimmed hostDescription coverage, the canonical browser runtime notes/commands are centralized in the validator helper path, the lower-level browser-runtime contract validator now also rejects whitespace-only `diagnosticHint` values on the shared contract path, and the four canonical scalar checks now flow through one shared descriptor loop, the `threadTopology.liveInstances[].scriptUrl` validator now rejects leading/trailing whitespace in addition to relative URLs, the browser-requested `run` / `test` summary merger now also accepts padded `hostContract` / `runtimeBackend` labels on the browser summary file, and the `package-audit --pretty` / legacy `--preview` rejections without the proper JSON envelope are now pinned before registry lookup, while the `package-effects` quiet JSON-output lane now also covers inherited browser+threaded config combinations. The shared `process.kill(0)` TS-wrapped call-target inventory now also has exact-order coverage, and the direct plus sequence call-target binding sources now also use the shared binding-line helper pattern, keeping that typed wrapper helper single-sourced and duplicate-free.
- Avoid duplicating theorem inventories in plan files; `proofs/BOUNDARY.md` remains the sole proof-boundary inventory.
