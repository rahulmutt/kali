# Spec Gap Map

This map lists active implementation goals implied by the specs after the current repository baseline. It is not an availability matrix; use [`../specs/19-feature-maturity.md`](../specs/19-feature-maturity.md) for public support status.

## Language semantics and frontend depth

Owners: `specs/02-lexer-parser.md`, `specs/03-ast.md`, `specs/04-type-system.md`, `specs/05-ir.md`, `specs/10-runtime.md`, `specs/16-testing.md`.

Remaining goals:

- Promote parser-accepted but unavailable semantics only when checker, lowering, runtime, and conformance evidence exist.
- Implement full generator and async-generator semantics, or keep all unsupported forms behind canonical `E5506` gates. Current plumbing now preserves function-flavor metadata through HIR/MIR lowering, including named function-expression forms in HIR and MIR tests, `check_source_file` now mirrors the generator/async-generator gate across Deno and browser TS/JS/JSX/TSX inputs, class generator and async-generator method syntax now also fails through the canonical unavailable-feature path, `check` / `build` smoke now also carries that class-method rejection in JS input plus browser-analysis-context JS input with JSON-output coverage, browser-requested run/test harness coverage now also keeps that gate explicit in TS/JS/JSX/TSX input when browser harness execution is configured, the central runtime_smoke generator helpers are now also exercised in browser-context and browser-harness JS smoke, and resumable state-machine execution still remains gated.
- Expand `for...of` / `for await...of` from bounded static slices toward full iterator and async-iterator protocol behavior, including close/finalization/error semantics. Current browser-bundle coverage now also includes `Object.entries(...)` iteration over static object literals, the bounded array-iteration slice now also accepts const-identity aliases such as boolean/null/Infinity/NaN elements, the standalone runtime path now also covers `Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` over static `Object.fromEntries(...)` operands, including duplicate-key overwrite cases, the standalone runtime path now also lowers string-primitive enumeration slices for those helpers inside `for...of` / `for await...of`, browser-requested run/test browser-harness coverage now also mirrors the `Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` spread slices over `Object.fromEntries(...)` operands in JSX and TSX input with JSON-output coverage, and browser bundle/browser-harness coverage now also includes the matching spread-of-`Object.values(...)` slice over `Object.fromEntries(...)` operands in JSX and TSX input, with browser-targeted build/check smoke now also covering that same spread-of-`Object.values(...)` slice in TS, JSX, and TSX input and the basic `for await...of` array slice in JSX and TSX input, and browser bundle/browser-harness smoke now also cover spread-of-`Reflect.ownKeys(...)` slices over static object-literal operands in JS, TS, JSX, and TSX input, and browser-requested run/test browser-harness coverage now also exercises the corresponding spread slice in JS, TS, JSX, and TSX input with JSON-output coverage, and browser-requested run/test browser-harness coverage now also exercises direct `for...of` consumption of `Reflect.ownKeys(...)` arrays over static object-literal operands in JS, TS, JSX, and TSX input with JSON-output coverage, and browser bundle/browser-harness smoke now also cover the frozen `Object.hasOwn(...)` helper slice over `Object.fromEntries(...)` operands in JS, TS, JSX, and TSX input, and browser-requested run/test browser-harness coverage now also exercises the frozen `Object.hasOwn(...)` helper slice over `Object.fromEntries(...)` operands in JS, TS, JSX, and TSX input with JSON-output coverage, and browser-requested run/test browser-harness coverage now also exercises the frozen `Object.values(...)` spread slice over `Object.fromEntries([...])` operands in JS, TS, JSX, and TSX input with JSON-output coverage. Standalone run/test smoke now also covers the frozen `Object.freeze(Object.fromEntries(...))` spread slice in JS and TS input with JSON-output coverage. Codegen unit coverage now also pins the mixed-bracket `globalThis.Object["values"]`, `globalThis["Object"].keys`, and `globalThis["Object"]["entries"]` spread aliases over `Object.fromEntries(...)` operands, and browser bundle smoke now also covers string-primitive enumeration for `Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` in JS, TS, JSX, and TSX input, while browser bundle/browser-harness smoke now also covers the corresponding string-primitive enumeration slices for those helpers in JS, TS, JSX, and TSX input, and standalone run/test smoke now also covers the same string-primitive enumeration slices for those helpers in JS, TS, JSX, and TSX input with JSON-output coverage, and the optimizer now also folds standalone `Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` calls over static string literals on the same helper family, while broader iterator-protocol handling remains gated. Browser-bundle build support for the `for await` string-primitive enumeration slice now also covers those helpers in JS, TS, JSX, and TSX input, and the browser-requested browser-harness run/test lanes plus the standalone run/test smoke now also cover the same slice. The browser-bundle/browser-harness wrapped-object enumeration slice now also stretches to spread-of-`Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` over plain wrapped object targets, and standalone run/test smoke now also covers frozen plain-object `Object.values(...)` iteration in JS/TS/JSX/TSX input. The supported static `for-of` / `for await...of` slices now also honor unlabeled break/continue through structured WASM branches, and browser-bundle smoke now also covers the corresponding break/continue slice over spread-array iterations in JS and TS input. Browser-targeted check/build smoke now also covers the frozen `Object.freeze(Object.fromEntries(...))` spread slice in JS, TS, JSX, and TSX input.
- Continue widening expression/operator and built-in semantics where translation-safe; keep unsupported `Math`, object-model, dynamic import, compound-assignment, and dynamic language forms explicitly gated. The current repository snapshot now also covers logical `&&=` / `||=` assignment, the wrapped mutable-local arithmetic compound-assignment slice on direct JS/TS, browser-harness, and browser-bundle smoke, including `**=` coverage in that wrapped slice, and the direct exponentiation `**` slice on standalone JS smoke, and browser-targeted check smoke now also covers the frozen `Object.hasOwn(...)` helper slice over `Object.fromEntries(...)` operands in JS, TS, JSX, and TSX input, and standalone check/run/test smoke now also covers the same fromEntries-sourced helper slice in JS, TS, JSX, and TSX input, and standalone check/run/test smoke now also covers the frozen `Object.freeze(Object.fromEntries(...))` helper slice in JS, TS, JSX, and TSX input, and standalone run/test smoke now also covers the same-reference alias-chain slice for `Object.is` in JS input with JSON-output coverage, plus the frozen same-reference alias-chain slice in TS, JSX, and TSX input with JSON-output coverage, plus the bracketed `globalThis.Object["is"]` / `globalThis["Object"].is` / `globalThis.Object.is` spellings for that same JS slice and the browser-harness TSX regression, plus signed-zero literal pairs in the static `Object.is` primitive-literal slice, and release optimizer coverage now also folds `Object.entries('ab')` into a concrete array-of-pairs literal alongside the existing string-primitive `Object.keys(...)` / `Object.values(...)` folds, and standalone run/test smoke now also covers the frozen `Object.freeze(Object.fromEntries(...))` spread slice in JS and TS input with JSON-output coverage.
- Grow bounded TypeScript/JavaScript inference only within deterministic budget rules, especially around exported/public surfaces and cross-module analysis. Current progress: bounded library-export inference now also preserves obvious nullish-coalesce fallback literals in the cheap signature slice when the branch choice is statically known across JS, TS, JSX, and TSX inputs, while the public export metadata remains conservative outside that slice.
- Keep conformance dashboards concise: snapshots of supported/gated behavior, not implementation journals.

## Runtime, host, and platform expansion

Owners: `specs/09-sandboxing.md`, `specs/10-runtime.md`, `specs/11-standard-apis.md`, `specs/12-cli.md`, `specs/18-schemas.md`, `specs/19-feature-maturity.md`.

Remaining goals:

- Complete guest-facing threaded runtime semantics beyond profile acceptance, while preserving no-GC/no-JIT invariants and resource-budget honesty. The browser-requested run/test harness path now also accepts positive `--max-threads` overrides when `--wasm-threads` is active in JS/TS/JSX/TSX input with JSON-output coverage.
- Decide whether harness-assisted `run --api browser` / `test --api browser` graduates to a stable standalone browser runtime contract; if yes, specify host ownership, sandbox limits, summary behavior, and failure modes first.
- Add late host APIs only with explicit effect keys, policy behavior, resource budgets, and command/API-surface gating: subprocesses, sockets/listeners, workers/threads, environment materialization, process control, and late Node/Deno modules. The browser-targeted `check` / `build --bundle` runtime-profile gate now also mirrors JSX and TSX input on both explicit and inherited browser API-surface paths when `--wasm-threads` is requested, and the Node API-surface regression suite now also rejects `process.kill` across JS/JSX/TSX input on `check` / `build` / `run` / `test`.
- Triage late object/runtime APIs (`Proxy`, weak references, finalization, broader `Intl`, `SharedArrayBuffer`, `Atomics`, and broader object helpers) against memory, threading, and optimization constraints before promotion.
- Keep browser-targeted build/check support, browser harness execution, and Kali-hosted sandbox enforcement distinct.

## Ecosystem and packages

Owners: `specs/11-standard-apis.md`, `specs/14-packages.md`, `specs/16-testing.md`, `specs/18-schemas.md`, `specs/19-feature-maturity.md`.

Remaining goals:

- Expand package-corpus coverage by package shape, host/API context, source class, command, and support rung without turning individual successes into blanket npm claims.
- Grow Node package support only where required Node built-ins and process APIs are explicitly supported or deliberately gated.
- Grow browser deployability and browser-harness package evidence while keeping standalone browser runtime claims separate.
- Keep native, binary, bootstrap-heavy, host-mismatched, and published-bin-entrypoint packages rejected by default unless specs introduce a mediated path.
- Keep registry-analysis commands (`package-effects`, `package-audit`) single-package and registry-identifier based unless a future spec/schema revision defines batch/local/raw-URL behavior.

## Optimization, PGO, and performance

Owners: `specs/07-specialization.md`, `specs/08-wasm-codegen.md`, `specs/16-testing.md`, `specs/18-schemas.md`, `specs/19-feature-maturity.md`.

Remaining goals:

- Deepen `release` and `release-advanced` optimizations while preserving JavaScript-visible semantics, sandbox effects, proof boundaries, and deterministic artifacts. The current repository snapshot now also folds `Object.freeze`-wrapped literal object shapes through the release and release-advanced optimizer paths for `Object.keys(...)`, `Object.values(...)`, `Object.entries(...)`, and `Reflect.ownKeys(...)`, keeping frozen-object folding aligned with the existing literal-object folding tests. The same optimizer normalization now also accepts bracketed alias spellings for the object-helper family once the access chain canonicalizes back to the same dotted helper name, and codegen coverage now also pins the mixed-bracket `globalThis.Object["hasOwn"]` / `globalThis["Object"].hasOwn` spellings over the same frozen `Object.fromEntries(...)` helper slice. Codegen regression coverage now also pins the single-quoted bracketed `globalThis['Object']['keys']` / `globalThis['Object']['values']` / `globalThis['Object']['entries']` spellings over frozen `Object.fromEntries(...)` operands.
- Treat `--profile` as a deterministic build-only additive input; do not create a hidden fourth build mode.
- Promote performance wording only when benchmark evidence names workload, build mode, baseline, and reproducibility constraints.
- Keep optimization inventories as concise evidence snapshots.

## Verification and contracts

Owners: `specs/16-testing.md`, `specs/17-verification.md`, `specs/18-schemas.md`, `proofs/BOUNDARY.md`.

Remaining goals:

- Widen Lean models for ownership, effects, type-system, and lowering slices in small named increments.
- Update `proofs/BOUNDARY.md` before any proof-backed wording changes.
- Expand proof CI triggers only when the published proof boundary claims implementation or spec paths outside the proof tree.
- Continue hardening JSON payload, artifact-manifest, schema-drift, diagnostics, source spans, and CLI-doc contracts while respecting schema extension posture. Suggested-fix text edits now also reject reversed ranges, duplicate zero-length insertions, and unexpected keys, source-location objects nested inside suggested-fix edits now also reject unexpected extension keys, timing objects now reject unexpected keys and now also reject non-finite or negative elapsed values, and timing phase labels now also reject empty or whitespace-only strings, with the timing unexpected-key path now having dedicated regression coverage; build-result and artifact-metadata export objects now also reject unexpected keys, with dedicated build-result and artifact-metadata export-key regression coverage, build result `artifactKind` now also rejects empty or whitespace-only strings, build result `buildMode` now also uses the canonical `fast` / `release` / `release-advanced` vocabulary, build result `profileDataHash` now also rejects empty or whitespace-only strings, effects payload `entryPoints` now also reject duplicate root labels, effects payload `dynamicReasons` now also enforce canonical deduplication/lexical ordering and the `dynamicEffects=false` empty-list rule, artifact entries now reject negative or fractional byte sizes, runtime/result provenance labels now require non-empty, non-whitespace string values when present, source-location validation now shares a common helper across text edits and mirror checks and now also rejects empty or whitespace-only `file` values on source spans and nested source locations, including empty or whitespace-only file values in nested label and related-info source spans, the source-edit machine contract stays aligned with the span ordering rules, and diagnostic-context origin coverage now explicitly pins the canonical `cli` / `config` / `default` / `source` JSON set; the regression suite now also rejects empty or whitespace-only diagnostic-context origins.
- Avoid duplicating theorem inventories in plan files; `proofs/BOUNDARY.md` remains the sole proof-boundary inventory.
