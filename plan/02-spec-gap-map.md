# Spec Gap Map

This map lists active implementation goals implied by the specs after the current repository baseline. It is not an availability matrix; use [`../specs/19-feature-maturity.md`](../specs/19-feature-maturity.md) for public support status.

## Language semantics and frontend depth

Owners: `specs/02-lexer-parser.md`, `specs/03-ast.md`, `specs/04-type-system.md`, `specs/05-ir.md`, `specs/10-runtime.md`, `specs/16-testing.md`.

Remaining goals:

- Implement or deliberately keep gated high-cost language semantics that are still unavailable despite parser/diagnostic coverage: generator lowering, broader `for...of` / iterator lowering beyond the supported literal-array slice (literal elements, const numeric alias elements, const string alias elements, simple variable bindings, and parenthesized identifier binding wrappers, plus the supported transparent parenthesized/type-assertion/satisfies/chain wrappers and TS `as const` wrapper cases, now including browser-harness/browser-bundle coverage for the `for...of` `as const` wrapper slice, browser-harness `run` / `test` JS-input coverage for the `for...of` `as const` and `satisfies` wrapper slices, and the matching `for await...of` parenthesized binding wrappers now also covered in browser-harness smoke, with the previously missing TS-run / JS-test parity now closed for that wrapper slice in the browser-harness matrix, and direct build smoke now also covers that same parenthesized-binding wrapper slice in Deno and browser TS, `.js`, `.jsx`, and `.tsx` inputs), browser-harness JSON-output coverage now also pins the parenthesized-binding wrapper slice for both loops in `.js` input, and the browser-harness JSON-output matrix now also covers that same parenthesized-binding slice in TSX input, broader `for await...of` / async-iterator semantics beyond that same literal-array slice, the remaining gated `Math` members (with `Math.floor` now also constant-folding statically-known numeric literals and resolving const numeric alias chains, `Math.trunc` / `Math.ceil` now also constant-folding statically-known numeric literals, the statically-known perfect-square integer literal path for `Math.sqrt` plus the statically-known perfect-cube integer literal path for `Math.cbrt` staying covered on the supported subset, `Math.exp` / `Math.log` now also having exact zero/one identity folds, `Math.expm1` / `Math.log1p` now also having exact zero identity folds, including const numeric alias chains, `Math.atan2` now also supporting the zero-numerator / non-negative-denominator literal slice, including const numeric alias chains, on the supported checker/codegen/build path while broader calls remain gated, and the browser-harness JSX/TSX matrix now also pins broader unsupported `Math.atan2(1, 1)` calls on the canonical `E5506` path, with browser-targeted build smoke now also rejecting that same broader `Math.atan2(1, 1)` slice in JSX and TSX input on both the explicit and inherited browser API surfaces, browser-requested run/test smoke now also covering the exact zero-identity `Math.sinh` / `Math.cosh` / `Math.tanh` slice in TS and `.js` input, browser-harness/browser-bundle smoke for that slice now also covering the `globalThis.Math.sinh` / `globalThis.Math.cosh` / `globalThis.Math.tanh` root forms, and browser-harness/browser-bundle smoke for the bracketed `globalThis["Math"]` root slice now also covering `Math.sinh` / `Math.cosh` / `Math.tanh` in JSX and TSX input, browser-harness smoke for that slice now also covering the positive-integer-exponent `Math.pow` alias-chain case and the exponent-one identity slice in TS and `.js` input, browser-harness/browser-bundle smoke now also covers the base-one `Math.pow` identity slice in TS, `.js`, `.jsx`, and `.tsx` input, and browser-harness run/test and JSON-output coverage now also covers that same base-one slice in TS, `.js`, `.jsx`, and `.tsx` input, and browser-requested run/test JSON-output coverage now also covers the `Math.asinh` / `Math.acosh` / `Math.atanh` identity slice and the `Math.sinh` / `Math.cosh` / `Math.tanh` zero-identity slice in TS and `.js` input, browser-harness/browser-bundle root smoke for `Math.expm1` / `Math.log1p` now also covers JSX and TSX input, and the mixed-bracket / fully bracketed root smoke for those same `Math.expm1` / `Math.log1p` spellings now also covers JSX and TSX input, compound assignment on non-local targets and immutable bindings (now pinned as explicit E5506 gates on the CLI smoke paths), plain `build` rejection on the same non-literal slice in `.ts` input with matching JSON-output coverage, plain build JSON-output rejection coverage now also pins that same gate in `.js`, `.jsx`, and `.tsx` input, and non-literal `import(expr)`.
- Progress note: the supported iterator slice now also accepts `Object.keys(...)` / `Object.values(...)` over static object literals with string-literal keys and const alias chains, plus the `globalThis.Object.keys(...)` root spelling on the same supported slice, in standalone `run` / `test` and browser-targeted `build --bundle` smoke; browser-targeted `check` now also rejects broader `Object.keys(...)` iterator sources on the canonical `E5506` path in JS, TS, JSX, and TSX input, browser-targeted JSON check coverage now also mirrors that same rejection in JS, TS, JSX, and TSX input, and broader non-literal iterator sources remain explicitly gated on the same canonical `E5506` path. Browser-requested run/test browser-harness coverage now also exercises that same supported `Object.keys(...)` slice in JS, TS, JSX, and TSX input with JSON-output coverage, including direct literal-object calls on the browser-requested run/test path, and browser-requested/browser-bundle smoke now also covers the `globalThis.Object.keys(...)` alias spelling in TS, `.js`, JSX, and TSX input, keeping the browser-requested iterator matrix aligned with the existing browser-bundle and standalone smoke. The same browser-requested harness matrix now also exercises `Object.values(...)` over the same static object-literal inputs, including the `globalThis.Object.values(...)` alias spelling, so the values slice stays aligned with the existing keys coverage. Progress note: wrapped TS object-enumeration operands now also cover `Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` through `as const` and `satisfies` wrappers on the browser-harness and browser-bundle smoke paths, keeping the object-enumeration wrapper family aligned with the existing `Object.fromEntries` wrapper coverage. The browser-harness and browser-bundle smoke now also cover the fully bracketed `globalThis["Object"]["keys"]` / `globalThis["Object"]["values"]` / `globalThis["Object"]["entries"]` spellings on that same supported object-enumeration slice, keeping the browser alias family aligned with the existing dotted `globalThis.Object` coverage. The browser-targeted iterator-source rejection lane still covers `.jsx` and `.tsx` inputs on the canonical `E5506` path, matching the existing `.js` and `.ts` smoke for the remaining unsupported iterator sources. The same non-literal `Object.keys(...)` rejection now also covers inherited browser-configured `check` / `build --bundle` smoke in the JS/TS/JSX/TSX matrix. The same non-literal `Object.values(...)` rejection now also covers inherited browser-configured `check` / `build --bundle` smoke in the JS/TS/JSX/TSX matrix.
- Progress note: browser-harness and browser-bundle smoke now also cover the `globalThis.Math.sqrt` / `globalThis.Math.cbrt` root slice in TS and `.js` input, keeping the supported root-access matrix aligned with the bare `Math.sqrt` / `Math.cbrt` slice.
- Progress note: browser-harness and browser-bundle smoke now also cover the `globalThis.Math` / bracketed `globalThis["Math"]` core math suite in JSX and TSX input, keeping the source-class matrix aligned with the supported TS and `.js` core-suite coverage. The fully bracketed `globalThis["Math"]["max"]` / `globalThis["Math"]["min"]` / `globalThis["Math"]["abs"]` / `globalThis["Math"]["sign"]` / `globalThis["Math"]["imul"]` / `globalThis["Math"]["clz32"]` core suite is now also covered by browser bundle smoke in JS, TS, JSX, and TSX input, plus browser-requested `run` / `test` smoke in JS, TS, JSX, and TSX input with JSON-output coverage.
- Progress note: browser-harness and browser-bundle smoke now also cover the mixed-bracket `globalThis.Math["log2"]` / `globalThis.Math["log10"]` root slice in TS, `.js`, `.jsx`, and `.tsx` input, keeping that bounded log identity matrix aligned with the base `Math.log2` / `Math.log10` slice.
- Progress note: browser bundle smoke now also covers the bracketed and fully bracketed `globalThis.Math.exp` / `globalThis.Math.log` identity slices in JSX and TSX input, closing the remaining source-class gap on those build-path math roots.
- Progress note: direct build smoke now also covers the same mixed-bracket `globalThis.Math["log2"]` / `globalThis.Math["log10"]` slice in Deno and browser `.js` / `.ts` inputs, keeping the build-path evidence aligned with the existing browser-harness/browser-bundle coverage.

- Progress note: browser-requested `run` / `test` harness coverage now also rejects non-literal `import(expr)` in `.js` input with matching JSON-output coverage, keeping the browser-requested dynamic-loading gate aligned with the existing browser-targeted `check` / `build --bundle` and standalone `run` / `test` rejection lanes.
- Progress note: browser-requested `run` / `test` harness coverage now also accepts raw backtick template-literal dynamic import targets with statically evaluable interpolations in JS, TS, JSX, and TSX input, with JSON-output coverage on the supported browser-requested lanes, keeping the linked-graph lowering contract aligned with the same deterministic import-specifier slice.
- Progress note: `Deno.permissions.query` descriptor objects now also peel transparent parenthesized, type-assertion, `satisfies`, chain, and decorated wrappers, and the JS/TS permission-query smoke now also exercises parenthesized descriptor-object wrappers on the direct check/build/run/test paths, keeping the query-only facade aligned with the existing transparent-wrapper handling.
- Expand TypeScript/JavaScript inference only inside the bounded inference contract; keep open-ended public-API and cross-module solving gated until deterministic budgets and evidence exist. Progress note: transparent decorated wrappers now also peel through the static string/numeric/object-identity/object-target helpers so the bounded literal slice stays aligned with HIR-transparent wrappers, and default-export function expressions now also keep their inferred signatures through transparent decorated wrappers. Default-export async function expressions now also keep their bounded `Promise<...>` signatures through transparent decorated wrappers, keeping the async export slice aligned with the existing async binding and await-wrapper coverage. The conservative unknown-signature fallback now also has JSX/TSX `build --lib` coverage for mixed conditional export bindings and default-export function declarations, and the alias-rejection regression now also covers JS, TS, JSX, and TSX input, keeping the bounded inference source-class matrix aligned. Progress note: `build --lib` smoke now also covers default-export async function declarations across JS, TS, JSX, and TSX input, while the default-export async function expression slice remains on the canonical `E5511` gate in CLI end-to-end smoke; the default-export async function expression gate is now also pinned across JS, TS, JSX, and TSX input on the CLI end-to-end build surface.
- Progress note: the late-host/object-model member-access naming helpers now also peel transparent parenthesized/type-assertion/satisfies/chain/decorated wrappers around member roots, keeping wrapped alias spellings aligned for env-materialization and late-object-model gates; wrapped call targets around the supported Object.hasOwn / Object.is helpers now also peel the same transparent wrappers so call-site transparency stays aligned with the member-root handling. Sequence-expression wrappers now also peel through the supported static Object.hasOwn target, keeping that bounded object-model slice aligned with the same transparent-wrapper handling.
- Progress note: the shared literal-array iterator lowering helper now also peels transparent `DecoratedExpression` wrappers around supported array iterables, array elements, and simple binding targets. It also peels sequence-expression wrappers in direct resolution tests, and the browser-harness run/test and JSON-output paths now also accept that sequence-expression slice on the supported JS, TS, JSX, and TSX lanes; browser-bundle smoke now also covers that same sequence-expression slice for both loops in TS, `.js`, `.jsx`, and `.tsx` input. The direct `kali_types` iterator-resolution coverage now also includes `for await...of` decorated-wrapper regressions in JS, JSX, and TSX input, keeping that transparent-wrapper path aligned with the existing TS-input coverage.
- Progress note: the shared literal-array iterator lowering path now also accepts spread elements whose spread targets resolve to supported literal-array slices, keeping the supported iterator subset aligned with the new spread flattening path. That spread-element slice now also accepts transparent parenthesized spread targets around const-bound literal arrays in current codegen smoke, and the shared helper now also accepts transparent decorated spread-target wrappers around const-bound literal arrays in direct resolution tests, keeping the helper aligned with the existing transparent-wrapper peelers. Browser bundle smoke now also covers the same spread-element slice in TS and `.js` input, keeping the browser iterator matrix aligned with the new spread-flattening path, and now also covers that spread-element slice in JSX and TSX input. Browser-requested run/test smoke now also covers the same spread-element slice in JS input, including the parenthesized spread-target slice for both loops, and now also covers the same spread-element slice in TS input via the new explicit runtime_smoke regressions for both loop spellings, keeping the browser-requested runtime matrix aligned with the same bounded iterator slice.
- Progress note: browser-harness run/test and JSON-output coverage now also exercises the `for...of` `satisfies` wrapper slice and the `for await...of` `as const` / `satisfies` wrapper slices in TS, JSX, and TSX input, keeping the iterator smoke aligned with the browser-bundle source-class matrix.
- Progress note: browser-harness run/test TSX smoke now also covers the `for...of` `as const` wrapper slice, keeping the source-class matrix aligned with the existing TS/JS/JSX bundle coverage.
- Progress note: browser-harness and browser-bundle `Math.floor` / `Math.trunc` / `Math.ceil` alias-chain smoke now also covers JSX and TSX input with JSON-output coverage, keeping that bounded math slice aligned across the browser source-class matrix.
- Progress note: browser build smoke now also covers the `globalThis.Math.round` root slice in JSX and TSX input on the browser API surface, keeping the browser-source-class matrix aligned with the existing TS and `.js` coverage.
- Progress note: browser build smoke now also covers the `for await...of` `as const` and `satisfies` wrapper slices on the browser API surface in `.js`, `.jsx`, and `.tsx` input.
- Progress note: browser-harness run/test and JSON-output coverage now also pins the `for...of` and `for await...of` const-alias-chain slice in TS input, keeping the aliased iterator smoke aligned with the existing bundle evidence.
- Progress note: browser bundle smoke now also covers the `for...of` const-alias-chain slice in JSX and TSX input, and the `for await...of` const-alias-chain slice now also extends to JSX and TSX input across the browser-bundle and browser-harness smoke paths.
- Progress note: browser bundle smoke now also covers the `for...of` `as const` wrapper slice in JSX and TSX input, extending the browser-bundle source-class matrix for that bounded iterator slice.
- Progress note: browser-harness `run` / `test` smoke now also covers the parenthesized const-alias wrapper slice for both `for...of` and `for await...of` in `.js` input, keeping the JS-input browser-requested iterator harness matrix aligned with the existing build/runtime slice.
- Progress note: browser-harness `run` / `test` smoke now also covers the const-alias-chain slice for both `for...of` and `for await...of` in `.js` input, and browser-harness JSON-output coverage now also pins that same JS-input const-alias-chain slice for both loops, closing the remaining browser-requested harness parity gap for that bounded iterator slice.
- Progress note: codegen/type smoke now also pins the exact `Math.sin` / `Math.cos` zero-identity slice, plus the matching non-identity rejection lane, keeping that supported trig pair aligned with the existing `Math.tan` gate; wrapped call targets around the supported `Math` helper slice now also peel the same transparent wrappers so the call-site transparency matches the member-root wrapper handling.
- Progress note: standalone `run` / `test` smoke now also covers the `Math.exp2` zero-identity const-alias slice in JS input with JSON-output coverage, and direct build smoke now also covers that zero-identity slice in Deno and browser `.js` / `.ts` inputs, plus JSX and TSX input on those same build paths, and browser-targeted build smoke now also covers the `globalThis.Math.exp2` / `globalThis.Math["exp2"]` / `globalThis["Math"].exp2` / `globalThis["Math"]["exp2"]` root spellings in TS and `.js` input, keeping the remaining build-path evidence gap closed for that supported builtin slice; browser-harness/browser-bundle JSON success paths for the `globalThis.Math.exp2` root slice now also assert empty `errors` arrays, keeping the deterministic JSON envelope explicit on the supported slice.
- Progress note: browser-harness JSON-output coverage now also pins the basic `Math.round` slice and the const-alias-chain slice in JS/TS input, matching the existing non-JSON browser-requested harness evidence for that supported builtin slice.
- Continue turning parser-only acceptance into either faithful runtime/checker support or explicit canonical gates (`E5506`) with mirrored TS, JS, JSX, TSX, browser, Node, and JSON-output regressions where applicable.
- Progress note: the mutable-local compound-assignment/update-expression slice now also peels transparent `DecoratedExpression` wrappers around the target, keeping the transparent-wrapper handling aligned with the existing parenthesized/type-assertion/satisfies update-target support.
- Maintain and simplify conformance dashboards as snapshots of supported/gated semantics rather than progress logs.

- Progress note: browser bundle and browser-harness smoke now also cover the empty-argument `Math.hypot()` identity slice in TS, `.js`, `.jsx`, and `.tsx` input, closing the browser-targeted evidence gap for that supported built-in fold, and direct build smoke now also covers that same empty-argument slice in JSX and TSX input on both the Deno and browser surfaces.

## Runtime, host, and platform expansion

Owners: `specs/09-sandboxing.md`, `specs/10-runtime.md`, `specs/11-standard-apis.md`, `specs/18-schemas.md`, `specs/19-feature-maturity.md`.

Remaining goals:

- Complete threaded-runtime semantics beyond opt-in profile acceptance and host-import plumbing, including guest-facing multi-worker/thread behavior where the spec permits it. Progress note: the threaded-topology snapshot helpers now also expose per-instance JSON-ready values, keeping the future guest-facing threaded-reporting work aligned with the deterministic report shape. The same helper family now also exposes plain `snapshot_object_value` aliases on the browser/runtime instance, shutdown-report, and topology types, keeping the object-value naming path aligned with the existing `*_json_value` helpers. The runtime execution context now also mirrors the same deterministic env snapshot view with matching `snapshot()` / `snapshot_object_value` / `snapshot_json_value` aliases, and the instance and report snapshot helpers now also expose bare `thread_topology_snapshot()` clone aliases so callers can use the same deterministic snapshot naming from either side of the helper family. The Deno and Node env projections now also expose a bare `snapshot()` object alias for that deterministic env view, keeping the helper naming symmetric ahead of the later object-materialization work. The instance and shutdown-report helpers now also expose bare `snapshot()` clone aliases alongside the existing `thread_topology_snapshot()` forms, keeping the threaded helper family aligned with the generic snapshot naming.
- Decide whether `run --api browser` / `test --api browser` should graduate from harness-assisted later compatibility to a stable standalone browser runtime contract; if yes, specify host ownership, sandbox limits, summary JSON behavior, and failure modes first. A dedicated JS-input regression file now also mirrors the unparseable-summary fallback on the browser-requested run/test paths, and now also covers invalid `testsFailed` values plus invalid `args` and `tests` array-shape fallbacks, keeping that browser runtime summary contract aligned with the existing source-class matrix. The browser-requested human-output `run` path now also covers empty summary-file fallback across JS, TS, JSX, and TSX input, and the browser-requested JSON `run` path now also covers that empty-summary fallback across JS, TS, JSX, and TSX input, keeping that fallback matrix aligned with the existing browser-requested summary contract. Progress note: browser bundle CJS smoke now also covers JS, TS, JSX, and TSX input on both the explicit and inherited browser API-surface builds, keeping the bundle-format matrix aligned with the existing browser-input coverage.
- Add late host APIs only with explicit policy/effect/resource contracts: environment materialization/mutation, process chdir/exit, subprocess spawning, socket/listener APIs, and late Node built-ins. Progress note: bare `check` / `build` / `run` / `test` now explicitly gate the phase-three Deno subprocess and socket/listener APIs (`Deno.Command`, `Deno.connect`, `Deno.listen`, `Deno.serve`) with canonical `E5506` diagnostics until the explicit sandbox/process-budget/network contracts exist. Progress note: the browser late-compatibility network matrix now also rejects the socket/listener slice on the browser API surface in JS input, including the bracketed `globalThis["Deno"]` alias spellings, keeping the remaining phase-three network gap explicit there too. The browser TSX late-compatibility smoke now also mirrors that socket/listener network slice (`Deno.connect`, `Deno.listen`, and `Deno.serve`) on the browser API surface, including the bracketed `globalThis["Deno"]` alias spellings, keeping the representative TSX rejection matrix aligned with the existing JS-input helper coverage. Progress note: the browser late-compatibility env-mutation matrix now also rejects the fully bracketed `globalThis["Deno"]["env"]["set/delete"]` spellings, keeping that rejection lane aligned with the already-covered mixed and partially bracketed forms. The browser JS and TSX source fixtures now also carry the mixed `globalThis.Deno["env"].{set/delete}` and `globalThis["Deno"].env["set/delete"]` spellings alongside the fully bracketed forms, keeping the source-class coverage aligned with the shared fixtures. Progress note: the env-object smoke now also keeps the browser late-compatibility rejection path explicit while the default standalone Deno surface now lowers `Deno.env.toObject`, keeping the alias coverage split across supported and gated contexts. Progress note: standalone runtime smoke now also covers the `Deno.chdir` alias family in `test` and JSON `test` paths on JS input, and the browser late-compatibility regression matrix now also carries a representative TSX-input slice for the same Deno/process/object-model/threaded-gate families; that TSX slice now also mirrors the bracketed process-control alias family so the browser late-compatibility alias matrix stays source-class aligned with the JS-input helpers. Progress note: that TSX slice now also carries the `Deno["env"].set/delete`, `globalThis.Deno["env"].set/delete`, and fully bracketed `globalThis["Deno"]["env"]["set/delete"]` spellings so the browser env-mutation alias matrix stays source-class aligned with the JS-input helpers. Progress note: the browser JS-input permission-escalation smoke now also includes the mixed-bracket `globalThis.Deno["permissions"]["request/revoke"]` spellings in source coverage, keeping the browser rejection matrix aligned with the existing canonical permission-escalation gate. Browser-entrypoint smoke now also covers the remaining Chromium/Google Chrome executable aliases (`chromium-for-testing`, `chromium for testing`, `google chrome beta`, `google chrome dev`, `google chrome for testing`, `google chrome unstable`) plus the remaining Vivaldi alias spelling (`vivaldi-snapshot`) across the runtime helper tests and CLI run/test smoke. The late-compatibility env-object rejection fixtures now also include the fully bracketed `globalThis["Deno"]["env"]["toObject"]` spelling in the standalone, browser JS, and browser TSX source-class matrices, keeping the alias-family gate exhaustive even though the actual object-materialization lowering remains gated. Progress note: the standalone JS late-compatibility helper now also checks the base process-control alias source directly, keeping the fully bracketed `globalThis["process"]["pid"|"cwd"|"chdir"|"exit"]` forms tied to the canonical source matrix instead of only to the mixed wrapper.
- Progress note: the browser-requested `run` / `test` smoke for the supported `Object.is` numeric-literal slice now also covers TSX input on the browser API surface, including the JSON `run` path, keeping that object-helper slice aligned across the browser harness source-class matrix.
- Triage late object/runtime APIs (`Proxy`, own-property helpers, weak references, finalization, broader `Intl`, `SharedArrayBuffer`, `Atomics`) with conformance and no-GC/no-JIT compatibility evidence before promotion. Progress note: the own-property-helper rejection matrix now also pins the mixed-bracket `Object.prototype["hasOwnProperty"].call` / `globalThis["Object"].prototype["hasOwnProperty"].call` spellings, and the late object/runtime smoke now also carries bracketed `globalThis["WeakMap"]` / `globalThis["WeakSet"]` / `globalThis["WeakRef"]` / `globalThis["FinalizationRegistry"]` value-access aliases in the JS and browser source-class helpers.
- Progress note: `Object.fromEntries` now also folds the `globalThis.Object.fromEntries` and bracketed `globalThis["Object"]["fromEntries"]` alias forms on the derived-shape object-enumeration path, and the runtime/browser smoke now pins those aliases alongside the existing bare-call coverage; browser bundle smoke now also covers the same `Object.fromEntries` slice in JSX and TSX input, the browser-harness `Kali.test` source now mirrors the TS `as const` branch and now also mirrors the TS `satisfies` wrapper slice, and the browser `Reflect.ownKeys` harness/bundle smoke now also spans JSX and TSX input, keeping the browser-bundle source-class matrix aligned with the browser-harness coverage. The browser-aligned `IndexedDb` stub now also exposes deterministic store snapshots and JSON-ready snapshot aliases, keeping the browser runtime helper vocabulary aligned with the rest of the deterministic snapshot helpers.
- Keep browser-targeted build/check support, browser harness execution, and Kali-hosted sandbox enforcement distinct.

## Ecosystem and packages

Owners: `specs/11-standard-apis.md`, `specs/14-packages.md`, `specs/16-testing.md`, `specs/18-schemas.md`, `specs/19-feature-maturity.md`.

Remaining goals:

- Expand package-corpus coverage by host/API context and support rung without turning individual package successes into blanket npm claims.
- Grow Node package support only where the required Node built-ins and process APIs are explicitly supported or deliberately gated.
- Grow browser deployability and browser-harness package evidence while keeping published binary entrypoints and native/bootstrap-heavy packages rejected by default.
- Progress note: the browser package-corpus published-bin-entrypoint probes now also reject the `@mariozechner/pi-coding-agent` browser runtime entrypoint on both the explicit browser surface and the inherited browser surface when a harness command is configured, keeping that negative evidence separate from ordinary browser-package content support. The browser runtime package corpus now also extends `spawn-tools` into JSX and TSX input on both the explicit browser surface and the inherited browser surface, with matching JSON-output coverage, and now also extends the `@mariozechner/pi-coding-agent` browser runtime run/test slice into TSX input on the explicit and inherited browser surfaces with matching JSON-output coverage, keeping the source-class matrix aligned with the existing JS and TS evidence.
- Keep registry-analysis commands (`package-effects`, `package-audit`) single-package and registry-identifier based unless a future spec revision defines batch/local/raw-URL behavior.

## Optimization, PGO, and performance

Owners: `specs/07-specialization.md`, `specs/08-wasm-codegen.md`, `specs/16-testing.md`, `specs/18-schemas.md`, `specs/19-feature-maturity.md`.

Remaining goals:

- Deepen `release` and `release-advanced` optimizations while preserving JavaScript-visible semantics, sandbox effects, and deterministic artifacts.
- Treat `--profile` as a deterministic build-only additive input; do not create a hidden fourth build mode.
- Promote performance wording only when benchmark evidence names workload, build mode, baseline, and reproducibility constraints.
- Keep optimization inventories as concise current-evidence snapshots, not implementation journals.

## Verification and contracts

Owners: `specs/16-testing.md`, `specs/17-verification.md`, `specs/18-schemas.md`, `proofs/BOUNDARY.md`.

Remaining goals:

- Widen Lean models for ownership/effects/lowering in small named slices, and update `proofs/BOUNDARY.md` before any proof-backed wording changes.
- Expand proof CI triggers only when the published proof boundary claims implementation or spec paths outside the proof tree.
- Continue hardening JSON payload, artifact-manifest, schema-drift, and CLI-doc contracts while respecting schema extension posture.
- Avoid duplicating theorem inventories in plan files; `proofs/BOUNDARY.md` remains the sole proof-boundary inventory.
