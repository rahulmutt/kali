# Phase 11 — Language Semantics and Conformance Closure

## Goal

Close high-value language gaps by either implementing faithful semantics or preserving explicit, tested availability gates.

## Owning specs

- `specs/02-lexer-parser.md`
- `specs/03-ast.md`
- `specs/04-type-system.md`
- `specs/05-ir.md`
- `specs/10-runtime.md`
- `specs/16-testing.md`
- `specs/19-feature-maturity.md`

## Work packets

### 11.1 Iterator and generator semantics

- Implement generator lowering only when `yield` / `yield*`, async-generator interactions, and runtime state machines have conformance coverage.
- Implement `for...of` and `for await...of` only with iterator protocol semantics, error/finalization behavior, and browser/Node/Deno evidence.
- Until implemented, keep parser acceptance paired with canonical `E5506` gates.
- Progress note: the limited literal-array `for...of` lowering slice is now live on the supported `check` / `build` / `run` / `test` paths, including browser-targeted smoke coverage in TS and `.js` input; the current slice covers literal arrays with literal elements, const numeric alias elements, const string alias elements, parenthesized const-alias wrappers, and simple variable bindings, including direct identifier bindings plus simple const-bound aliases and alias chains to those arrays in both TS and `.js` input, plus parenthesized identifier binding wrappers around the loop target, and the browser-harness `run` lane now mirrors the alias, alias-chain, string-alias, identifier-binding, parenthesized-binding, and parenthesized-wrapper smoke cases in both TS and `.js` input, and browser-harness `test` now also mirrors the const-alias-chain slice in TS input; `for await...of` now shares that same literal-array lowering slice for literal arrays with literal elements, const numeric alias elements, const string alias elements, parenthesized const-alias wrappers, and simple variable bindings, including browser-harness `run` / `test` smoke coverage in both TS and `.js` input, and `for await...of` parenthesized binding wrappers around the loop target are now also covered in browser-harness `run` / `test` smoke on both TS and `.js` input, while broader iterator-protocol handling remains gated. The browser-harness matrix now also includes the previously missing TS-run / JS-test parity for the parenthesized-binding wrapper slice on both `for...of` and `for await...of`, browser bundle smoke now also covers the parenthesized-binding wrapper slice for both loops in TS and `.js` input, and now also mirrors the `for await...of` const-alias-chain slice in browser-harness `run` / `test` smoke on both TS and `.js` input, with browser-harness JSON-output coverage now also pinning that `for await...of` const-alias-chain slice in both TS and `.js` input. Browser-harness JSON-output coverage now also pins the parenthesized-binding wrapper slice for both loops in `.js` input. The shared lowering helper now also peels transparent parenthesized/type-assertion/satisfies/chain wrappers and TS `as const` wrappers around the supported array iterables/elements, and browser-harness `run` / `test` coverage now also exercises the `for...of` `satisfies` wrapper slice alongside the `for...of` `as const` wrapper slice and the `for await...of` `as const` and `satisfies` wrapper slices in TS input; browser build smoke now also mirrors the const alias, const string-alias, parenthesized const-alias, and `as const` wrapper slices for `for...of` on the browser API surface in TS input, and browser bundle smoke now also covers the `for...of` `as const` wrapper slice in TS and `.js` input, browser-harness `run` / `test` now also covers the direct literal-array `for await...of` slice in both TS and `.js` input, and the browser-harness JSON-output coverage now also pins the direct literal-array `for await...of` slice in TS input, browser bundle smoke now also covers the `for await...of` const-string-alias and parenthesized-const-alias slices in both TS and `.js` input, browser bundle smoke now also covers the TS `for await...of` `as const` and `satisfies` wrapper slices, and browser build smoke now also mirrors the `for await...of` `as const` and `satisfies` wrapper slices in TS input on both the Deno and browser surfaces, with browser bundle smoke now also covering those `for await...of` `as const` and `satisfies` wrapper slices in `.js` input. Generator spellings remain on the canonical `E5506` gate: generator and async-generator forms now have explicit rejection coverage in the type-checker, codegen, and runtime smoke layers. `Math.trunc` and `Math.ceil` now also constant-fold statically-known numeric literals, including const numeric alias chains, on the supported `check` / `build` / `run` / `test` paths, and now also have direct `build` smoke coverage on const alias chains in the Deno and browser `.js` / `.ts` inputs. `Math.abs` now also constant-folds statically-known integer-valued numeric literal operands, and `Math.sign` now also constant-folds statically-known numeric literal operands, on the supported smoke paths, including const numeric alias chains. `Math.hypot` now also has an exact integer-literal perfect-square-sum slice in the same checker/codegen path, and now also has direct standalone check and build smoke coverage on that slice, plus browser-harness `run` / `test` smoke coverage and browser bundle smoke coverage on that slice in `.js` input. `Math.atan2` now also supports the zero-numerator / non-negative-denominator literal slice, including const numeric alias chains and the supported transparent parenthesized, type-assertion, satisfies, and TS as-const wrapper slices, in the same checker/codegen/build path while broader calls remain gated, and the build corpus now also mirrors that literal slice in JSX and TSX input on both the Deno and browser surfaces, and browser-harness/browser-bundle smoke now also covers the bracketed `globalThis[\"Math\"]` root plus TS as-const and satisfies wrapper slices in browser bundle and browser-requested smoke, and browser-harness `run` / `test` smoke now also covers the `Math.log2` / `Math.log10` literal slice in TS and `.js` input, browser-harness `run` / `test` smoke now also covers the exact `Math.exp` / `Math.log` identity folds in TS and `.js` input, now also covers the `Math.atan2` zero-numerator / non-negative-denominator slice in TS and `.js` input, and now also covers the positive-integer-exponent `Math.pow` slice through a const-alias chain in browser-harness TS and `.js` input plus representative negative-exponent E5506 rejection coverage in browser-harness JS input, and browser bundle smoke now also covers the same positive-integer-exponent alias-chain slice in TS and `.js` input, and the fully bracketed `globalThis[\"Math\"][\"pow\"]` member-access slice in TS and `.js` input, `Math.sinh` / `Math.cosh` / `Math.tanh` now also have exact zero identity folds in the current repo, and browser-requested `run` / `test` smoke now also covers the exact zero-identity slice in TS and `.js` input; browser-requested `run` / `test` smoke now also covers the exact `Math.asin` / `Math.acos` / `Math.atan` identity slice in TS and `.js` input with JSON-output coverage; browser-harness `run` / `test` smoke now also covers the exact `Math.tan` zero-identity slice in TS and `.js` input, and the browser-harness JSON-output paths now also pin the exact `Math.sin` / `Math.cos` zero-identity slice and the exact `Math.tan` zero-identity slice in TS and `.js` input, while the broader non-identity calls remain gated. Update expressions on mutable local bindings (`++` / `--`) now also lower through the same local-storage path, with rejection coverage staying explicit for immutable targets, and the corresponding standalone `run` / `test` smoke keeps that slice pinned in both `.ts` and `.js` inputs. The browser-requested `run` / `test` harness now also exercises that slice in `.ts` and `.js` input with JSON-output coverage. Nullish coalescing `??` remains implemented; nullish assignment `??=` now also has direct TS/JS smoke and browser-harness coverage in the current repository.
- Progress note: direct build and browser-bundle smoke now also cover the const-alias-chain slice for both `for...of` and `for await...of` in TS and `.js` inputs, alongside the existing const-string-alias coverage.
- Progress note: direct build smoke now also covers the `for await...of` parenthesized-binding wrapper slice in Deno and browser TS and `.js` inputs, matching the already-covered codegen/runtime slice.
- Progress note: direct build smoke now also covers the fully bracketed `globalThis["Math"]["atan2"]` zero-numerator / non-negative-denominator slice in Deno and browser `.js` / `.ts` inputs, keeping the browser-bundle evidence aligned with the already-supported bracketed-root path.
- Progress note: direct build smoke now also covers the single-quoted bracketed `globalThis['Math']['atan2']` slice in Deno and browser `.js` / `.ts` inputs, matching the existing browser-harness/browser-bundle coverage.
- Progress note: browser-harness and browser-bundle smoke now also cover the bracketed `globalThis["Math"]` root for the `Math.sin` / `Math.cos` / `Math.tan` zero-identity slice in TS and `.js` input, and now also cover the `Math.max` / `Math.min` / `Math.abs` / `Math.sign` / `Math.imul` / `Math.clz32` core suite in TS and `.js` input.
- Progress note: browser-harness and browser-bundle smoke now also cover the mixed-bracket `globalThis.Math["exp"]` / `globalThis.Math["log"]` spelling on the browser API surface in TS and `.js` input, alongside the existing `globalThis.Math.exp` / `globalThis.Math.log` and bracketed-root `globalThis["Math"].exp` / `globalThis["Math"].log` coverage.
- Progress note: direct build smoke now also covers the fully bracketed `globalThis["Math"]["exp"]` / `globalThis["Math"]["log"]` identity slice in Deno and browser `.js` / `.ts` inputs, keeping the build path aligned with the already-covered browser-harness/browser-bundle slice.
- Progress note: browser bundle smoke now also covers the `Math.floor` / `Math.trunc` / `Math.ceil` const-alias-chain slice in TS and `.js` input, and browser-harness/browser-bundle smoke now also covers the bracketed `globalThis["Math"]` / fully bracketed `globalThis["Math"]["floor"]`, `globalThis["Math"]["trunc"]`, and `globalThis["Math"]["ceil"]` slices in TS and `.js` input.
- Progress note: browser-harness and browser-bundle smoke now also cover the bracketed `globalThis["Math"]` root for the `Math.pow` zero-exponent non-integer-base slice in TS and `.js` input.
- Progress note: `Math.pow` now also folds the base-one identity slice in the supported checker/codegen/runtime smoke path, keeping the exponent-identity coverage honest without widening the gated negative-exponent cases.
- Progress note: browser-harness JSON-output smoke now also covers the `Math.pow` exponent-one identity slice in TS and `.js` input, including the fully bracketed `globalThis["Math"]["pow"]` root, so the browser-requested JSON envelope stays aligned with the existing bundle and harness coverage.
- Progress note: `Object.is` now also accepts the static primitive-literal slice on the supported `check` / `build` / `run` / `test` paths, including const alias chains and transparent wrapper forms for numeric, string, boolean, and null literals, plus unary-plus-wrapped numeric literals, and the browser bundle / browser-harness smoke now mirrors that slice in TS and `.js` input; build smoke now also covers the same numeric-literal slice in JSX and TSX input on both the Deno and browser surfaces. The browser bundle and browser-harness smoke now also pin the unary-plus-wrapped numeric literal slice directly in the browser Object.is bundle/harness cases.

### 11.2 Missing expression and built-in semantics

- Promote nullish coalescing `??` and currently unsupported `Math` members only with runtime/checker/codegen coverage.
- Keep unsupported built-ins rejected explicitly rather than lowering to placeholders.
- Mirror accepted and rejected cases across TS/JS and JSON-output paths where user-visible.
- Progress note: `Math.abs` now also constant-folds statically-known integer-valued numeric literal operands, including const numeric alias-chain resolution, on the supported codegen/runtime smoke paths, and `Math.sign` now also constant-folds statically-known numeric literal operands, including const numeric alias-chain resolution, on the supported codegen/runtime smoke paths; `Math.max` / `Math.min` now also constant-fold statically-known integer-valued numeric literal operands, including const alias chains, on the supported codegen smoke paths, and the browser-bundle smoke now also covers the `Math.max` / `Math.min` const-alias-chain slices in TS and `.js` input; `Math.imul` now also constant-folds statically-known integer literal operands, including const alias chains; `Math.clz32` now also constant-folds statically-known numeric literal operands, including const alias chains and a non-integer literal slice such as `1.6`; `Math.floor` now has integer-only smoke coverage and also constant-folds statically-known numeric literals such as `1.6`; that `Math.floor` path now also resolves through const numeric alias chains; `Math.round` now also has integer-only smoke coverage, statically-known numeric literal operands now constant-fold through the round path, direct `run` / `json run` smoke now also covers the round const-alias-chain slice in `.js` input, browser-harness smoke now also covers the round const-alias-chain slice in TS and `.js` input, and direct `build` smoke now also covers that slice in the Deno and browser `.js` / `.ts` inputs; browser-harness `run` / `test` smoke now also covers the `Math.floor` / `Math.trunc` / `Math.ceil` const-alias-chain slice in TS and `.js` input; `Math.sqrt` now has a statically-known perfect-square integer literal path that also follows const numeric alias chains; `Math.cbrt` now has a statically-known perfect-cube integer literal path that also follows const numeric alias chains, including negative perfect cubes; `Math.sqrt` / `Math.cbrt` now also have direct `build` smoke coverage in the Deno and browser `.js` / `.ts` inputs, browser-harness `run` / `test` smoke now also covers the same literal slices in TS and `.js` input with JSON-output coverage, and browser bundle smoke now also covers the `Math.sqrt` / `Math.cbrt` literal slices in TS and `.js` inputs; `Math.log2` now has a statically-known positive power-of-two integer literal path that also follows const numeric alias chains, and browser bundle smoke now also covers the bare `Math.log2` / `Math.log10` literal slice in TS, `.js`, `.jsx`, and `.tsx` input; `Math.log10` now has a statically-known positive power-of-ten integer literal path that also follows const numeric alias chains; `Math.log2` / `Math.log10` const alias-chain smoke is now also covered by direct `build` smoke tests in the Deno `.js` and `.ts` inputs, including both human and JSON-output paths, alongside the existing `run` regressions; `Math.pow` now has an integer-exponent path with checker/codegen/runtime smoke coverage on positive integer literals and signed integer bases, including const numeric alias-chain handling for the same supported integer path, plus zero-exponent and exponent-one identity slices, with the zero-exponent slice also accepting non-integer base literals and browser-harness/browser-bundle smoke now also covering those slices in TS and `.js` input, and the browser-harness `run` / `test` JS-input slice now also mirrors signed integer bases; the negative-exponent host-import gate now returns a canonical error instead of panicking; that negative-exponent `Math.pow` gate now also has direct JS-input `check` / `--output json check` smoke coverage; the exponent-zero / exponent-one identity slices now also fold on the supported smoke paths; the supported `globalThis.Math.*` member-access root now follows the same lowering path as the corresponding bare `Math.*` forms for the already-supported Math builtin slices, and browser-harness/browser-bundle smoke now also mirrors the bracketed `globalThis["Math"]` root for the `Math.atan2` zero slice in TS and `.js` input; `Math.exp` / `Math.log` now also have exact zero/one identity folds, and the const-alias-chain slice now also has direct build smoke coverage in Deno and browser `.js` / `.ts` inputs, with browser-harness JSON-output smoke now also covering that identity slice in `.js` input; browser-harness run/test smoke now also covers the bracketed `globalThis["Math"]` root for that identity slice in TS and `.js` input; `Math.expm1` / `Math.log1p` now also have exact zero identity folds, including const numeric alias chains, with browser-harness/browser-bundle smoke now also covering the exact zero identity slice in TS and `.js` input and the bracketed `globalThis["Math"]` root, and browser-harness JSON-output smoke now also covers that identity slice in `.js` input; browser-harness/browser-bundle smoke now also covers the `globalThis.Math.expm1` / `globalThis.Math.log1p` root spellings for that same zero-identity slice in TS and `.js` input, the mixed-bracket `globalThis.Math["expm1"]` / `globalThis.Math["log1p"]` spelling now also has browser-harness/browser-bundle smoke coverage in TS and `.js` input, and the fully bracketed `globalThis["Math"]["expm1"]` / `globalThis["Math"]["log1p"]` spellings now also have browser-harness/browser-bundle smoke coverage in TS and `.js` input; `Math.atan2` now also supports the zero-numerator / non-negative-denominator literal slice on the supported checker/codegen/build path, and the supported `globalThis.Math.atan2` / `globalThis["Math"].atan2` / `globalThis["Math"]["atan2"]` paths now also share that same slice in JS input, `Math.asin` / `Math.acos` / `Math.atan` now also have exact zero/one identity folds, `Math.asinh` / `Math.acosh` / `Math.atanh` now also have exact zero/one identity folds, and browser-harness/browser-bundle smoke now also covers the exact `Math.asinh` / `Math.acosh` / `Math.atanh` identity slice in TS and `.js` input, and the browser-requested run/test JSON-output lane now also covers that inverse-hyperbolic slice in TS and `.js` input; `Math.sinh` / `Math.cosh` / `Math.tanh` now also have exact zero identity folds, and browser-requested `run` / `test` smoke now also covers the exact zero-identity slice in TS and `.js` input, with the browser-requested run/test JSON-output lane now also covering that zero-identity slice in TS and `.js` input; `Math.sin` / `Math.cos` / `Math.tan` now also have exact zero identity folds, while representative non-identity calls still have explicit canonical E5506 rejection smoke alongside the existing `Math.sqrt`/`Math.log2`/`Math.log10` gates, and the build/check corpus now also pins those rejections across Deno and browser `.js` / `.ts` inputs, with standalone `run` / `test` plus browser-harness smoke now also pinning representative `Math.exp` / `Math.log` rejections on the JS-input math gate path, browser-harness smoke now also covering the exact `Math.exp` / `Math.log` identity folds in TS and `.js` input, and the supported `globalThis.Math.exp` / `globalThis.Math.log` roots now mirror that slice in browser-harness TS and `.js` input, and the fully bracketed `globalThis["Math"]["exp"]` / `globalThis["Math"]["log"]` slice now also shares that same smoke coverage in TS and `.js` input, browser-targeted `check` / `build` smoke in `.jsx` / `.tsx` input; `Object.is` now also has browser-bundle smoke coverage for the supported static primitive-literal slice in TS and `.js` input; nullish coalescing `??` is now implemented and covered across direct and browser-targeted smoke paths; arithmetic compound assignment expressions (`+=`, `-=`, `*=`, `/=`, `%=`, `**=`) now also lower for mutable local bindings with runtime local storage, including transparent parenthesized/type-assertion/satisfies wrapper targets in the codegen lowering path, and non-identifier compound-assignment targets / immutable bindings now reject explicitly rather than silently lowering; the CLI smoke now also pins the canonical E5506 rejection for non-local compound-assignment targets and the binding-specific E5506 rejection for immutable bindings across check/run JSON and non-JSON paths, so standalone `run` / `json run` JS-input smoke preserves simple local update cases even when the right-hand side is a runtime value; `Promise.allSettled` now has runtime/checker/codegen coverage across standalone, browser-targeted, and Node smoke paths. The parser now also preserves top-level statement boundaries after function declarations, so trailing statements are no longer absorbed into the function body before HIR/LIR lowering. Mutable `let` / `var` bindings now no longer get folded through the direct codegen binding cache, so the LIR→WASM path keeps mutable slots distinct from const-only aliases. The `effects` command now also reports the same late env-object materialization slice in `.js` input as `Process.EnvRead` with matching JSON-output coverage. Browser-requested run/test coverage on the browser API surface now also rejects generator and async-generator spellings in `.js` input with canonical E5506 errors, keeping the runtime smoke aligned with the existing generator gate. Follow-up for `Math.atan2`: broader calls remain gated until a translation-safe lowering path and validator coverage exist for the wider slice.

- Browser-harness/browser-bundle bracketed `globalThis["Math"]` root smoke now mirrors the existing `Math.pow` alias-chain slice in TS and `.js` input, and also covers the fully bracketed `globalThis["Math"]["pow"]` member-access slice in TS and `.js` input.
- Browser-harness/browser-bundle bracketed `globalThis["Math"]` root smoke now also covers the `Math.exp` / `Math.log` identity slice in TS and `.js` input.
- Browser-harness/browser-bundle bracketed `globalThis["Math"]` root smoke now also covers the `Math.sinh` / `Math.cosh` / `Math.tanh` zero-identity slice in TS and `.js` input.
- Browser bundle smoke now also covers the exact `Math.sin` / `Math.cos` / `Math.tan` zero-identity slice in TS and `.js` input.
- Direct build smoke now also covers the bracketed `globalThis["Math"]` root for the `Math.atan2` zero-numerator / non-negative-denominator slice in JS input.

- Progress note: browser-harness and browser-bundle smoke now also cover the bracketed `globalThis["Math"]` / fully bracketed `globalThis["Math"]["round"]` slice for `Math.round` identity cases in TS and `.js` input.

- Progress note: browser-harness and browser-bundle smoke now also cover the `globalThis.Math.atan2` / `globalThis.Math["atan2"]` zero slice in TS and `.js` input, complementing the existing bracketed-root coverage.
- Progress note: browser-harness/browser-bundle Math.atan2 smoke now also covers the single-quoted bracketed `globalThis['Math']['atan2']` spelling in TS and `.js` input.

### 11.3 Dynamic loading and module semantics

- Preserve literal-string `import()` as linked-graph lowering.
- Keep non-literal `import(expr)` gated until a host-mediated loading/effect model exists.
- Expand CJS/ESM interop tests only where the linked-artifact model remains deterministic.
- Progress note: the literal-string dynamic import path now also resolves raw backtick template-literal specifiers with statically evaluable interpolations, keeping `check` / `build` resolution and browser-bundle chunk discovery aligned for that narrower constant-expression slice; direct browser-bundle smoke now also covers the same raw backtick template-literal specifier slice in TS and `.js` input, and browser-targeted `check` / `build --bundle` now also pin the non-literal `import(expr)` gate in `.js` input with matching JSON-output rejection coverage.

### 11.4 Bounded inference hardening

- Improve local/intra-module inference under deterministic budgets.
- Add negative coverage for annotation-required public boundaries.
- Do not promote open-ended public-API or cross-module solving without a new solver budget and evidence lane.
- Progress note: library-export metadata now infers trivial return types for top-level function exports when the body collapses to a single literal-return or transparent wrapper, and now also covers top-level `const` function-expression / arrow-function bindings plus their export aliases, plus deterministic sequence/conditional/decorated wrappers around that same bounded slice, while preserving the conservative `unknown` fallback outside that bounded slice; function-shaped default export expressions now also participate in that bounded library-export slice, including transparent sequence/conditional/decorated wrappers, and async function declarations / expressions now get bounded `Promise<...>`-wrapped signatures when their return shape stays within the same cheap slice, including transparent `await` wrappers around the returned literal/body slice. The current coverage now also includes async `const` function-expression bindings through transparent conditional wrappers and export aliases. The CLI `build --lib` smoke now also pins the current JS-input alias-rejection path on the end-to-end build surface, and now also keeps mixed conditional public-boundary exports on the conservative `unknown` fallback instead of overpromoting them.

## Exit gate

- Supported semantics have conformance and minimized regressions.
- Unsupported parser-accepted forms have canonical gates.
- `specs/19-feature-maturity.md` and affected owning specs match the actual implementation.
