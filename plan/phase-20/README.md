# Phase 20 — Verification and Machine Contracts

## Goal

Widen proof-backed and machine-contract confidence while keeping claims exact.

## Owning specs

- `specs/16-testing.md`
- `specs/17-verification.md`
- `specs/18-schemas.md`
- `specs/19-feature-maturity.md`
- `proofs/BOUNDARY.md`

## Work packets

### 20.1 Proof-boundary hygiene

- Keep `proofs/BOUNDARY.md` as the sole theorem/property inventory.
- Remove duplicate proof inventories from plan files.
- Update boundary text whenever covered Lean paths or theorem claims change.

### 20.2 Model widening

- Widen core semantics, ownership/RC, effects, type-system, and lowering models in small named slices.
- Pair every widened proof-backed claim with mechanized theorem inventory and proof-CI evidence.
- Keep full-language/full-host proof wording out of release claims until those semantics are modeled.

### 20.3 Proof CI triggers

- Use `mise run lean-proofs` for Lean builds.
- Expand proof-trigger paths only when `proofs/BOUNDARY.md` claims implementation or spec paths outside `proofs/`.

### 20.4 Schema and CLI contract hardening

- Continue validating JSON payloads, artifact manifests, diagnostics, source spans, and schema-v1 envelopes at emission boundaries.
- Keep docs/schema drift tests aligned with README and CLI examples.
- Respect schema extension posture; do not make validators narrower than published schemas.
- Recent progress: the `kali doctor` CLI prose now explicitly names `browserHarness.override` alongside `envVar` and `executable`, keeping the command docs aligned with the schema-v1 doctor payload shape. `browserRuntimeContract.supportedCommands` items now also reject empty or whitespace-only strings before the fixed tuple check, keeping the machine contract explicit. Artifact metadata and build-result `buildMode` strings now also reject empty or whitespace-only values before enum validation, with dedicated regression coverage, so the canonical build-mode vocabulary stays explicit. Effects/package-effects reports now also reject whitespace-only `analysisContext.apiSurface`, `entryPoints`, `runtimeProfiles`, `compatFeatures`, and `dynamicReasons` items so the stable set fields stay deterministic, and the effects/package-effects regressions now also pin whitespace-only `analysisContext.runtimeProfiles` / `compatFeatures` entries directly. Diagnostic-context `configPath` and `flag` fields now also require actual strings when present, so null no longer slips through that schema-v1 path. Registry-analysis commands now also reject whitespace-only package selectors before lookup, keeping the single-package contract explicit.
- Implemented: diagnostic JSON now mirrors `span.file` into the top-level `file` convenience field, schema validation rejects any mismatch between the mirror and the canonical span file, diagnostic objects now reject unexpected top-level keys, diagnostic labels and related-info items now reject unexpected extension keys, diagnostic-context objects now reject unexpected extension keys, and the diagnostic-context unexpected-key path now has a dedicated regression test; diagnostic-context `configPath` / `flag` values now also reject empty or whitespace-only strings when present, with dedicated regression coverage; source-location objects in suggested-fix edits now reject unexpected extension keys, and the regression suite now also pins that nested source-location unexpected-key path inside suggested-fix edits, including the `end` location path, plus unexpected-key rejection in nested label and related-info source spans, including empty or whitespace-only file values in nested label and related-info source spans, text-edit objects inside suggested fixes now reject unexpected keys, suggested-fix objects now reject unexpected top-level keys, and suggested-fix edit locations now validate their `file` mirror plus their `line` / `column` fields directly before the range check, keeping the `start.file` / `end.file` mirror and position-order checks explicit; source-location validation now shares one helper across text-edit and source-location mirror checks and now also rejects empty or whitespace-only `file` values on source spans and nested source locations; suggested-fix edit ranges now reject reversed start/end ordering, overlapping edits, and duplicate zero-length insertions at the same file position, the end.file mirror path now has a dedicated regression test, and the regression suite now also pins whitespace-only `end.file` rejection in suggested-fix edits; CLI envelopes now reject unexpected top-level keys while validating optional `artifacts` entries and now also reject unexpected keys inside those artifact objects, duplicate artifact kind/path pairs, noncanonical artifact roles, out-of-order artifact arrays, negative `exitCode` values, and empty or whitespace-only `command` strings, build-result artifact roles now use the same canonical schema-v1 role set and reject noncanonical strings too, build-result artifact arrays now also reject out-of-order entries, and build-result emitters now sort artifact arrays before schema validation; build-result and artifact-metadata export objects now also reject unexpected keys and empty or whitespace-only export `name` / `signature` strings, with dedicated build-result and artifact-metadata export-key regression coverage, and the published schemas now also encode `minLength: 1` for those export fields plus `browserRuntimeContract.diagnosticNotes` items, artifact metadata now also rejects empty or whitespace-only `entrypoint` / `apiSurface` strings, build result `outputPath` and artifact reference paths now also reject empty or whitespace-only strings, build result `buildMode` now also uses the canonical `fast` / `release` / `release-advanced` vocabulary, build result `profileDataHash` now also rejects empty or whitespace-only strings, build result payloads now also mirror the same `hostContract` / `runtimeBackend` provenance labels as the generated artifact metadata, and build result `sourceHash` plus artifact-metadata `kaliVersion` / `sourceHash` now also reject empty or whitespace-only strings, keeping the generated sidecar/export metadata fixed-shape; install payloads now reject unexpected top-level keys, timing objects now reject unexpected keys and duplicate phase labels, the current repository snapshot now also rejects out-of-order timing sequences so the phase order stays canonical, and the timing unexpected-key path now has a dedicated regression test; the current repository snapshot now also rejects empty or whitespace-only timing phase labels to keep the schema-v1 contract explicit; effects payload `entryPoints` now also reject duplicate root labels to keep logical-root serialization deterministic; effects payload `dynamicReasons` now also enforce canonical deduplication/lexical ordering and the `dynamicEffects=false` empty-list rule, and `package-effects` report `analysisContext` objects now also reject unexpected extension keys with dedicated regression coverage; test coverage payloads now reject unexpected keys at the coverage root plus per-file and summary rows, the C-ABI metadata and binding-package sidecars now reject unexpected top-level and nested artifact keys, runtime/result provenance labels now require non-empty, non-whitespace string values when present, and the shared provenance-string helper now also backs the `run` / `test` payload `hostContract` / `runtimeBackend` checks so those fields stay aligned with the same non-empty-string contract; C-ABI metadata and binding-package provenance labels now also reject empty or whitespace-only `hostContract` / `runtimeBackend` values, with coverage on both parse and summary paths; the browserRuntimeContract `hostLabel` now also remains pinned to the canonical `browser-requested` label in doctor JSON, the published doctor schema now also encodes that const plus the non-empty `browserHarness.envVar` / `browserHarness.executable` / `browserHarness.command` / `browserHarness.args` / `browserHarness.override` / `browserRuntimeContract.hostDescription` string constraints, with the command, args, and override item schemas now also carrying `minLength: 1`; `browserHarness.source` now also rejects non-canonical values through dedicated regression coverage, `browserHarness.envVar` and `browserHarness.executable` now also reject empty or whitespace-only strings, `browserHarness.command` and `browserHarness.args` items now also reject empty or whitespace-only strings, `browserHarness.override` now also rejects empty or whitespace-only strings when `source` is `env`, `browserRuntimeContract.hostDescription` now also rejects empty or whitespace-only strings, `browserRuntimeContract.hostDescriptionNote` now also rejects empty or whitespace-only strings, `browserRuntimeContract.diagnosticHint` now also rejects empty or whitespace-only strings, and non-string `browserRuntimeContract.diagnosticHint` values now have dedicated regression coverage to keep the browser runtime guidance explicit; `browserRuntimeContract.diagnosticNotes` items now also reject empty or whitespace-only strings, keeping the ordered browser runtime note list explicit as well; the CLI's E5506 runtime-profile / compatibility gates now carry canonical flag/config path notes for `--compat`, `--wasm-threads`, and the related config keys, the doctor schema now also encodes the fixed `browserRuntimeContract.supportedCommands` tuple as ordered `run` / `test` prefix items, the CLI envelope `command` field now also rejects empty or whitespace-only strings, and the regression suite now also explicitly pins the canonical diagnostic-context origin set (`cli`, `config`, `default`, and `source`) in schema-v1 JSON. `package-audit`'s legacy `--preview` shim now also emits CLI diagnostic context in JSON mode so the compatibility rejection stays machine-readable, and the registry-analysis command gate now also rejects whitespace-only explicit package selectors before registry lookup.

## Exit gate

- `mise run lean-proofs` passes for proof changes.
- `proofs/BOUNDARY.md` names exactly the widened proof-backed scope.
- No README, release, or plan wording exceeds the published boundary.
