# Current Implementation Baseline

This file records the planning baseline for the active continuation roadmap. It is a current-state summary, not an active checklist and not an availability matrix.

## Evidence used for this cleanup

- `cargo run -q -p kali_cli --bin kali -- --help` exposes the current public command set.
- The workspace contains Rust crates for CLI, lexer/parser, AST/HIR/MIR/LIR, type checking, codegen, runtime, sandbox/effects, package management, optimization, Deno/Web/Node API projections, embedding/C ABI, formatting, linting, and bindings.
- The spec-owned availability and current-state nuance remain in [`../specs/19-feature-maturity.md`](../specs/19-feature-maturity.md).
- The proof-backed boundary remains owned by [`../proofs/BOUNDARY.md`](../proofs/BOUNDARY.md).

## Live surface at a glance

- The supported object-enumeration helper alias inventory now also accepts mixed-quote bracket-root `globalThis["Object"]['keys']` / `globalThis['Object']["keys"]` spellings, plus dot-root bracket/quote `globalThis["Object"].keys` / `globalThis['Object'].keys` and `globalThis["Reflect"].ownKeys` / `globalThis['Reflect'].ownKeys` / `globalThis["Reflect"]['ownKeys']` / `globalThis['Reflect']["ownKeys"]` spellings, with matching `values` / `entries` / `Reflect.ownKeys` coverage in the checker, codegen, and browser-bundle smoke lanes. The browser `Object.keys` bundle/harness smoke now also mirrors the double-quoted bracket-root `globalThis["Object"]['keys']` spelling on that same supported slice, and the browser `Object.values` / `Object.entries` bundle smoke now also mirrors the mixed-quote bracket-root values/entries spellings and the double-quoted bracket-root `globalThis["Object"]['values']` / `globalThis["Object"]['entries']` spellings on that same supported slice. The browser bundle/harness smoke now also covers the dot-root single-quoted `globalThis['Object'].keys` / `globalThis['Object'].values` / `globalThis['Object'].entries` spellings and `globalThis['Reflect'].ownKeys` on that same supported slice, and the browser runtime/browser harness `Reflect.ownKeys` smoke now also covers the direct mixed-quote bracket-root `globalThis["Reflect"]['ownKeys']` / `globalThis['Reflect']["ownKeys"]` spellings on that same supported slice. The shared `Reflect.ownKeys` source inventory now also carries the mixed-quote bracket-root `globalThis["Reflect"]['ownKeys']` / `globalThis['Reflect']["ownKeys"]` spellings and their parenthesized forms on the same supported slice.

The checked-in repository already includes:

- The browser-harness/browser-bundle `Math.round` smoke matrix now also covers the parenthesized mixed-quoted-bracketed, single-quoted-bracketed-dot, and parenthesized double-quoted bracket-root aliases, and the shared browser-harness source now also exercises nullish/logical wrappers around the direct, dot-root, and bracket-root `Math.round` aliases. The browser-harness/browser-bundle `Math.exp2` smoke matrix now also carries the missing transparent-wrapper variants around the supported non-negative-integer slice, keeping the zero-identity and `globalThis`-root aliases aligned.
- Browser-requested `run --api browser` / `test --api browser` now also treat malformed `KALI_BROWSER_BUNDLE_HARNESS_COMMAND` overrides as unavailable before launch, keeping browser runtime gating on the canonical browser-unavailable path instead of surfacing a late harness-preparation failure.
- The schema-v1 build-artifact provenance labels now also reject surrounding whitespace padding on the canonical `kaliVersion` / `sourceHash` / `hostContract` / `runtimeBackend` / `profileDataHash` machine-label slice, build-result `outputPath` and sidecar `metadataPath` / `witPath` / `headerPath` / `bindingPackagePath` labels now also reject surrounding whitespace padding, and artifact metadata now also rejects surrounding whitespace padding on `entrypoint` / `apiSurface`, keeping the artifact metadata and build-result provenance contract aligned with the stricter machine-label wording in `specs/18-schemas.md`.
- The shared `Array.from` frozen-callable inventory now also covers the parenthesized single-quoted bracket-root aliases, the parenthesized double-quoted bracket-root `Object.freeze((globalThis["Array"])["from"])` alias, and the mixed-quote bracket-root `Object.freeze((globalThis['Array'])["from"])` alias, and the parenthesized single-quoted bracket-root property-access alias `Object.freeze((globalThis['Array'])['from'])`, and the callable-resolution layer now also accepts nullish / logical wrappers around that supported slice such as `Object.freeze((null ?? Array.from))`, `Object.freeze((true && Array.from))`, and `Object.freeze((false || Array.from))`, keeping the supported `Array.from` smoke matrix aligned across the shared helper source and the build/runtime/browser harness lanes, including the explicit `globalThis.Array` dot-root alias regression suite and the repeated set/map output checks in the standalone/browser smoke lanes; the dedicated `array_from_set_map_dot_root_aliases` smoke now also mirrors that parenthesized double-quoted bracket-root variant, and the browser harness/bundle smoke now also exercises the new parenthesized bracket-root property-access spellings `Object.freeze((globalThis["Array"]))["from"]` and `Object.freeze((globalThis['Array']))["from"]`, plus the matching single-quoted property-access siblings `Object.freeze((globalThis["Array"]))['from']` and `Object.freeze((globalThis['Array']))['from']`, and now also exercises the parenthesized double-quoted bracket-root property-access alias `Object.freeze((globalThis["Array"])["from"])` on that same supported slice. The browser harness/bundle Array.from smoke now also exercises the parenthesized dot-root property-access alias `Object.freeze((globalThis.Array))["from"]` and its single-quoted sibling `Object.freeze((globalThis.Array))['from']`. The dot-root `globalThis.Array.from` alias now also carries nullish/logical wrapper coverage on the same supported slice, and the shared Array.from frozen-callable inventory now centralizes those dot-root wrappers too. The double-quoted bracket-root `globalThis.Array["from"]` alias family now also carries nullish/logical wrapper coverage, staying aligned with the existing `globalThis.Array.from`, `globalThis["Array"].from`, and `globalThis.Array['from']` wrapper slices. The type-checker regression matrix now also includes the frozen `Object.freeze((Array.from))` callable-name slice in JS input, and now also covers the parenthesized bracket-root dot-call alias pair `Object.freeze((globalThis["Array"]).from)` / `Object.freeze((globalThis['Array']).from)` in JS input. The mixed-quote bracket-root alias family now also carries parenthesized transparent-wrapper coverage on the same `Array.from` smoke path, keeping `Object.freeze((globalThis["Array"]['from']))` / `Object.freeze((globalThis['Array']["from"]))` aligned with the rest of the shared inventory. The build smoke matrix now also carries statically decidable nullish / logical wrappers around the direct `new Set(...)` and `new Map(...)` constructor targets on that same `Array.from(Object.freeze(...))` slice, keeping the constructor-target coverage aligned with the existing frozen-result and alias-family wrappers.

- The bracketed `globalThis["Array"].from` / `globalThis['Array'].from` nullish/logical wrapper smoke now also has dedicated standalone, browser-requested, and browser-bundle coverage, keeping that bracket-root alias family aligned with the shared Array.from inventory.
- The fully bracketed single-quoted `globalThis['Array']['from']` alias now also has nullish, `true &&`, and `false ||` wrapper coverage in the standalone, browser-requested, and browser-bundle smoke lanes, keeping that alias family aligned with the rest of the Array.from wrapper inventory.
- The `Array.from` regression matrix now also validates the parenthesized mixed-quote bracket-root property-access aliases `Object.freeze((globalThis["Array"])["from"])` and `Object.freeze((globalThis['Array'])['from'])` in the type-resolution and codegen smoke lanes, keeping that mixed-quote property-access slice pinned alongside the existing dot-call coverage.

- The browser-harness/browser-bundle `Math.round` smoke inventory now also covers the parenthesized single-quoted bracketed `globalThis['Math']['round']` alias.
- The shared `Set`/`Map` constructor-iteration smoke now also accepts nullish/logical wrappers around frozen constructor results such as `Object.freeze((null ?? new Set(...)))` and `Object.freeze((false || new Map(...)))`, keeping the build/lowering smoke aligned with the transparent-wrapper path.
- The browser-harness/browser-bundle frozen `Set`/`Map` constructor-result smoke now also accepts nullish/logical wrappers around the constructor targets, keeping that transparent-wrapper lane aligned with the shared Array.from constructor-result inventory.
- The shared `Reflect.ownKeys` smoke inventory now also includes the parenthesized double-quoted bracket-root `Object.freeze((globalThis["Reflect"])["ownKeys"])` alias alongside the parenthesized single-quoted bracket-root `Object.freeze((globalThis['Reflect'])['ownKeys'])` alias, and now also covers the dot-root `globalThis.Reflect["ownKeys"]` / `globalThis.Reflect['ownKeys']` aliases plus their parenthesized forms, keeping the quoted wrapper paths aligned across the common helper inventory and the runtime/build smoke lanes. The shared source inventory now also carries the mixed-quote bracket-root `globalThis["Reflect"]['ownKeys']` / `globalThis['Reflect']["ownKeys"]` spellings and their parenthesized forms on the same supported slice. The browser, standalone JS, and browser-harness/browser-bundle smoke lanes now also cover the direct `globalThis.Reflect['ownKeys']` alias and the parenthesized double-quoted bracket-root `Object.freeze((globalThis["Reflect"])["ownKeys"])` variant.
- The shared `Reflect.ownKeys` smoke inventory now also covers the direct `globalThis["Reflect"].ownKeys` alias, keeping the bracket-root path aligned with the existing frozen and mixed-root variants.
- The shared `Reflect.ownKeys` smoke inventory now also covers the parenthesized dot-root bracketed alias `Object.freeze((globalThis.Reflect)["ownKeys"])`, and the runtime smoke lanes mirror that same wrapper form.

- CLI commands: `doctor`, `init`, `install`, `fmt`, `lint`, `check`, `build`, `run`, `test`, `effects`, `package-effects`, and `package-audit`.
- The README command reference now also includes the canonical `jsr:@std/path` `package-effects` / `package-audit` examples that match the CLI chapter's registry-analysis contract.
- The envelope-only `package-audit` JSON contract now also hard-fails standalone `--pretty` before registry lookup, keeping the JSON-mode gate explicit.
- Build-result and artifact-metadata export arrays now also reject leading/trailing whitespace padding on exported `name` / `signature` strings, keeping the generated export-surface machine labels aligned with the other canonical schema-v1 labels.
- Static-literal analysis now also unwraps await-wrapped numeric literals in the bounded inference path, keeping cheap wrapper handling consistent across the checker.
- Static string-key resolution now also accepts same-branch conditional expressions in the bounded-literal path, keeping `Object.hasOwn` and related static-key helpers aligned with the other deterministic wrapper slices.
- Static dynamic-import resolution now also accepts transparent `Object.freeze(...)` wrappers plus statically decidable logical/nullish wrappers around statically known import specifiers, keeping the linked-graph import slice aligned with the existing literal and template-literal targets; the discovery regressions now also cover nullish and logical import-specifier wrappers in the JS and TS lanes, and browser-requested run/test harness smoke now also covers the frozen literal-specifier wrapper lane in JS input.
- Static reference-root resolution now also tolerates optional-chain-wrapped roots, await-wrapped static member roots, and direct same-reference member-root comparisons, keeping `Object.is` same-reference comparisons aligned with the other transparent wrapper slices.
- Object-literal computed property names now also unwrap transparent `Object.freeze(...)` calls in the parser, keeping the static-name slice aligned with the existing parenthesized, `await`, `as`, `satisfies`, and sequence-wrapper handling.
- Transparent object-root resolution now also unwraps `Object.freeze(...)` around static `Math` and `Number` roots, keeping the cheap math/number identity slices aligned with the existing callable-wrapper handling.
- The supported `Math.hypot` perfect-square-sum slice now also accepts frozen callable aliases around `Math.hypot` and `globalThis.Math.hypot` in the checker and browser harness/bundle smoke lanes.
- The static `Object.is` / `Number.is*` primitive-value slices now also treat `Object.freeze(...)` around statically-known primitive arguments as a transparent wrapper in the checker and smoke coverage.
- Late object-model gating now also unwraps await-wrapped, `Object.freeze`-wrapped, and optional-chain-wrapped `Proxy.revocable` aliases before emitting the canonical E5506 rejection, so transparent wrappers do not bypass the existing late-compatibility boundary; the browser JSX/TSX late-compat smoke now also mirrors that await-wrapped `Proxy.revocable` rejection slice, and now also pins transparent `Object.freeze(globalThis.Intl.NumberFormat)` coverage on the same late-object-model path.
- The late-object-model test matrix now also pins frozen `Proxy.revocable` aliases in the type checker, and the shared browser late-compat helper inventory now also asserts the frozen bracketed `Proxy.revocable` aliases plus the new parenthesized `Object.freeze((globalThis["Proxy"].revocable))` / `Object.freeze((globalThis.Proxy["revocable"]))` forms, keeping the rejection path explicit for wrapper-preserving diagnostics; the late weak-reference rejection coverage now also mirrors transparent `Object.freeze(...)` wrappers around the canonical `WeakMap` / `WeakSet` / `WeakRef` / `FinalizationRegistry` aliases in the build matrix.
- The supported numeric-fallthrough path now also keeps await-wrapped `Math.atan2` numeric literals cheap in build smoke, matching the existing transparent wrapper handling on related static-literal slices, and browser-harness/browser-bundle smoke now also covers the same await-wrapped `Math.atan2` zero slice across JS, TS, JSX, and TSX input. The same `Math.atan2` zero slice now also accepts frozen callable aliases such as `Object.freeze(globalThis.Math.atan2)`, `Object.freeze(globalThis["Math"]["atan2"])`, and `Object.freeze(Math.atan2)` in the checker, browser bundle, browser-harness, and standalone runtime smoke lanes.
- The supported Number predicate slice now also accepts frozen callable aliases such as `Object.freeze(Number.isFinite)`, `Object.freeze(Number.isInteger)`, and `Object.freeze(Number.isSafeInteger)` in the checker plus runtime and browser harness/bundle smoke coverage, and the checker now also accepts the fully bracketed `globalThis["Number"]["isFinite"]` / `globalThis["Number"]["isNaN"]` / `globalThis["Number"]["isInteger"]` / `globalThis["Number"]["isSafeInteger"]` spellings on the same supported slice, plus frozen bracketed `Number["isFinite"]` / `Number["isInteger"]` / `Number["isSafeInteger"]` aliases on that same slice.
- The supported `Math.round` literal slice now also treats transparent `Object.freeze(...)` wrappers around statically-known numeric literals as part of the same cheap folding path across the checker, runtime, and browser harness smoke coverage, and the browser-requested harness smoke now also mirrors the single-quoted bracketed `Object.freeze(globalThis['Math']['round'])` callable alias on that same supported slice, while standalone run/test smoke now also mirrors the bracketed `Object.freeze(globalThis.Math["round"])` callable alias on the same supported slice. The shared `Math.round` frozen-callable inventory now also includes the quote-variant `globalThis.Math['round']`, `globalThis["Math"]['round']`, `globalThis['Math']['round']`, and `globalThis['Math'].round` spellings, and the runtime/browser-harness smoke lanes now also carry those quote-variant frozen aliases, while the type checker and codegen now also resolve conditional-callable wrappers around the supported `Math.round` alias chain, with build smoke keeping that conditional-wrapper path aligned, including the parenthesized bracketed-dot `Object.freeze((globalThis["Math"].round))` alias plus the parenthesized direct `Object.freeze((globalThis.Math.round))` / `Object.freeze((globalThis["Math"].round))` / `Object.freeze((Math.round))` aliases. The shared `Math.round` frozen-callable inventory now also includes the parenthesized receiver-wrapped bracket-root `Object.freeze((globalThis["Math"]).round)` / `Object.freeze((globalThis['Math']).round)` aliases, plus the mixed-quote parenthesized bracket-root `Object.freeze((globalThis['Math'])["round"])` alias, and the shared checker regression matrix now also explicitly exercises the canonical `math_round_frozen_callable_invocation_source()` inventory across JS/JSX/TS/TSX input, keeping the `Math.round` alias family aligned at the checker boundary too. The same supported slice now also accepts statically decidable nullish/logical wrappers around the canonical `Math.round` callable aliases in browser-harness/browser-bundle smoke, including `Object.freeze((null ?? Math.round))`, `Object.freeze((true && globalThis.Math.round))`, and `Object.freeze((false || globalThis["Math"]["round"]))`.
- The supported `Math.exp` / `Math.log` exact-identity slice now also accepts frozen callable aliases such as `Object.freeze(Math.exp)` and `Object.freeze(globalThis["Math"]["log"])`, plus the parenthesized callable aliases `((globalThis.Math.exp))` / `Object.freeze((globalThis.Math.log))`, on the checker/codegen/runtime smoke path, keeping the exact-identity math wrappers aligned with the rest of the transparent `Object.freeze(...)` coverage.
- The supported `Math.expm1` / `Math.log1p` exact-identity slice now also accepts frozen root wrappers such as `Object.freeze(Math)`, `Object.freeze(globalThis.Math)`, and `Object.freeze(globalThis["Math"])` on the checker plus browser bundle/harness smoke paths.
- The supported `Math.exp2` zero-identity slice now also accepts frozen callable aliases around `Math.exp2`, `globalThis.Math.exp2`, the bracketed `globalThis.Math["exp2"]` spelling, and the bracket-dot `globalThis["Math"].exp2` spelling in the browser harness/bundle smoke lanes, including the transparent nullish/logical wrapper forms used by the new regression coverage.
- The supported `Math.sin` / `Math.cos` / `Math.tan` zero-identity slice now also accepts frozen callable aliases around `Math`, `globalThis.Math`, and `globalThis["Math"]` in the browser bundle and browser-harness smoke lanes.
- The supported `Math.imul` / `Math.clz32` slices now also accept bracketed and frozen callable aliases on the browser bundle/harness smoke path, including the mixed-bracket frozen spellings `Object.freeze(globalThis["Math"]["imul"])` and `Object.freeze(globalThis.Math["clz32"])`, keeping them aligned with the other supported host-import math slices. The browser omitted-operand smoke now also covers the matching `Math.clz32()` zero-arg path, and the new `Math.clz32()` frozen-callable omitted-operand lane now stays aligned with the same inventory.
- The supported `Math.floor` / `Math.trunc` / `Math.ceil` literal slices now also keep direct `Object.freeze(Math.floor)` / `Object.freeze(Math.trunc)` / `Object.freeze(Math.ceil)` callable aliases on the same cheap folding path in checker, codegen, and runtime smoke coverage, including the single-quoted `Math['floor']` / `Math['trunc']` / `Math['ceil']` spellings and the parenthesized bracket-root `Object.freeze((globalThis["Math"])["floor"])` / `Object.freeze((globalThis['Math'])['floor'])` / `Object.freeze((globalThis["Math"])["trunc"])` / `Object.freeze((globalThis['Math'])['trunc'])` / `Object.freeze((globalThis["Math"])["ceil"])` / `Object.freeze((globalThis['Math'])['ceil'])` spellings.
- The supported `Math.sqrt` perfect-square slice now also keeps frozen callable aliases such as `Object.freeze(globalThis.Math.sqrt)` and `Object.freeze(globalThis["Math"]["sqrt"])` on the same lowering/build/runtime smoke path.
- The supported `Math.cbrt` perfect-cube slice now also keeps frozen callable aliases such as `Object.freeze(Math.cbrt)` and `Object.freeze(globalThis["Math"]["cbrt"])` on the browser-harness smoke path.
- The supported `Math.log2` / `Math.log10` positive-literal slice now also keeps frozen callable aliases such as `Object.freeze(Math.log2)`, `Object.freeze(Math.log10)`, and the bracketed `globalThis` spellings on the same checker, build, runtime, and browser-harness/bundle smoke path.
- The supported `Math.max` / `Math.min` slice now also accepts frozen callable aliases around `Math.max`, `globalThis.Math.max`, `Math.min`, and `globalThis.Math.min` in the checker plus browser bundle/harness smoke coverage.
- Default-export generator and async-generator function declarations now preserve function-flavor metadata through the parser, AST, HIR, MIR, and LIR layers, including anonymous default-export forms; the current regression matrix now also includes anonymous default-export generator coverage across parser, AST, HIR, MIR, LIR, and codegen, plus CLI build/runtime smoke for the anonymous default-export generator path, browser-harness/browser-bundle smoke for the anonymous default-export generator and async-generator paths, and build smoke for the anonymous async-generator path, while executable lowering and library-export collection still reject the unsupported lowering path with the canonical gate. Generator rejections now also surface the delegation-specific `yield*` unavailable message when applicable, including the class-method check/build/runtime-entrypoint path, keeping the unsupported path wording aligned with the observed body shape.
- The direct runtime entrypoint now also rejects generator and async-generator function declarations and expressions before lowering, keeping the same canonical gate aligned across top-level, default-export, function-expression, and class-method forms.
- The generator/async-generator rejection matrix now also calls out `yield*` delegation explicitly on the unsupported path, keeping delegated-yield failures aligned with the same canonical `E5506` gate.
- The benchmark inventory notes now record that the new `math-round-builtin` / `math-round-builtin-js` pair now does the same for `Math.round`, that browser-harness/browser-bundle smoke now also covers the dot-root `Object.freeze(globalThis.Math.round)` callable alias, the optional-chain-wrapped `Object.freeze(globalThis?.Math.round)` alias, the parenthesized optional-chain alias `Object.freeze((globalThis?.Math.round))`, the bracketed `Object.freeze(globalThis.Math["round"])` / `Object.freeze(globalThis["Math"]["round"])` aliases, the bracketed-dot `Object.freeze(globalThis["Math"].round)` alias, the parenthesized bracketed-dot `Object.freeze((globalThis["Math"].round))` alias, the quote-variant `Object.freeze(globalThis.Math['round'])` / `Object.freeze(globalThis["Math"]['round'])` / `Object.freeze(globalThis['Math']['round'])` / `Object.freeze(globalThis['Math'].round)` aliases, the parenthesized mixed-bracket `Object.freeze((globalThis.Math["round"]))` alias, and the direct `Object.freeze(Math.round)` / `Object.freeze((Math.round))` root aliases on that supported slice, that the direct run/test smoke lane now also mirrors `Object.freeze((Math.round))` on the same alias chain, and browser-harness/browser-bundle smoke now also mirrors the parenthesized direct `Object.freeze((Math.round))` alias on the same supported slice, plus the parenthesized bracketed-dot `Object.freeze((globalThis["Math"].round))` alias on that same slice, and now also asserts that parenthesized direct alias identity on that same browser smoke path, and the current browser build smoke now also carries the parenthesized direct `Object.freeze((globalThis.Math.round))` / `Object.freeze((globalThis["Math"].round))` / `Object.freeze((Math.round))` aliases on that same supported slice, and the shared `Math.round` frozen-callable inventory now also includes the parenthesized receiver-wrapped bracket-root `Object.freeze((globalThis["Math"]).round)` / `Object.freeze((globalThis['Math']).round)` aliases plus the optional-chain-wrapped `Object.freeze(globalThis?.Math.round)` / `Object.freeze((globalThis?.Math.round))` aliases, with the shared checker regression matrix continuing to exercise the canonical `math_round_frozen_callable_invocation_source()` inventory across JS/JSX/TS/TSX input, that the frozen-callable `Math.abs` / `Math.sign` slice now also has direct `run` / JSON `run` smoke coverage in JS input plus browser bundle/harness coverage for `Object.freeze(globalThis.Math.abs)` / `Object.freeze(globalThis.Math.sign)` alongside the direct `Object.freeze(Math.abs)` / `Object.freeze(Math.sign)` aliases, and now also mirrors the bracketed `Object.freeze(globalThis["Math"]["abs"])` / `Object.freeze(globalThis["Math"]["sign"])` and `Object.freeze(Math["abs"])` / `Object.freeze(Math["sign"])` spellings on that same slice, with the shared helper inventory now also carrying the direct `Math.abs` / `Math.sign` frozen aliases, and that the browser Math max/min frozen-alias smoke now also covers the bracketed `globalThis["Math"]["max"]` / `globalThis["Math"]["min"]`, quote-variant `globalThis.Math['max']` / `globalThis.Math['min']` / `globalThis["Math"]['max']` / `globalThis["Math"]['min']` / `globalThis['Math']['max']` / `globalThis['Math']['min']` / `globalThis['Math'].max` / `globalThis['Math'].min` spellings, and `Math["max"]` / `Math["min"]` / `Math['max']` / `Math['min']` spellings on the same supported slice, and that the `folded-arithmetic-variant` slice now also has a JS workload form (`folded-arithmetic-variant-js`) and is covered in the optimization benchmark suite.
- Build/artifact lanes: executable builds, browser bundles, IR validation, library artifacts, C ABI artifacts, component artifacts, deterministic metadata, and sidecar manifests.
- Codegen function-plan evidence now also covers default-export generator and async-generator declarations through dedicated regressions, keeping generator flavor metadata visible through the lowered function-plan path even while executable lowering remains gated.
- Library-export inference now also preserves statically known function signatures through logical `&&` / `||` wrapper forms around callable bindings, aligning that export-analysis slice with the existing transparent-wrapper handling used by the other static function-shape regressions.
- Runtime/reporting lanes: `run`, `test`, `test --coverage`, source-graph effects, package effects, package audit, deterministic JSON envelopes, schema-v1 payload validation, deterministic thread-topology schema drift checks for the run/test JSON paths, the test coverage payload's empty/whitespace `files[].file` rejection, the package-effects report's whitespace-padded `entryPoints[0]` rejection before canonical-label comparison, and the expanded `Promise.allSettled` alias/freeze smoke inventory in direct runtime coverage, plus the new `Promise.all` browser harness/bundle alias/freeze smoke inventory, which now also carries frozen bracketed root spellings around `Promise["all"]` and `Promise['all']` and nullish/logical wrapper aliases around the supported direct and dotted `Promise.all` spellings, and the direct runtime execute / execute_tests lanes now mirror the same threaded-host-import `threadTopology` snapshot shape. The browser Object.hasOwn-fromEntries harness/bundle smoke now also carries same-branch conditional wrappers around `Object.hasOwn` and `Object.prototype.hasOwnProperty.call` on that same supported helper slice.
- The shared `Promise.allSettled` browser body now also carries the mixed-root frozen callable alias `Object.freeze(globalThis.Promise["allSettled"])` plus its parenthesized form, the new parenthesized dotted-root and bracketed-root frozen callable aliases `Object.freeze((globalThis.Promise)["allSettled"])` / `Object.freeze((globalThis["Promise"])["allSettled"])`, the new parenthesized bracket-root dot-call aliases `Object.freeze((globalThis["Promise"]).allSettled)` / `Object.freeze((globalThis['Promise']).allSettled)`, the single-quoted `Promise['allSettled']` / `globalThis.Promise['allSettled']` / `globalThis['Promise']['allSettled']` / `globalThis['Promise'].allSettled` aliases plus their frozen-callable variants, the mixed-bracket root frozen aliases `Object.freeze(globalThis["Promise"].allSettled)` / `Object.freeze(globalThis['Promise'].allSettled)` plus their parenthesized forms, and the direct bracketed-root frozen callable alias `Object.freeze(Promise["allSettled"])` plus its parenthesized form, including the parenthesized mixed-root dot-call aliases `Object.freeze((globalThis["Promise"]).allSettled)` / `Object.freeze((globalThis['Promise']).allSettled)`, keeping the canonical browser harness/bundle body aligned with the existing dot-root and bracket-root frozen aliases. It now also carries nullish/logical wrapper aliases around the supported root and dotted `Promise.allSettled` spellings so the browser smoke lane stays aligned with the broader transparent-wrapper inventory. The build-smoke matrix now also exercises the direct, dotted, bracketed, mixed-root, mixed-bracket-root, and fully bracketed frozen `Promise.allSettled` spellings so the standalone browser build lane stays in lockstep with the shared body source.
- The runtime BigInt arithmetic path now also accepts `%` remainder on the same supported smoke lanes as addition, subtraction, multiplication, division, and exponentiation, and the optimizer now also constant-folds numeric and BigInt remainder expressions on the supported literal paths.
- Object-enumeration finalization coverage now also includes async `Object.entries(...)` and `Reflect.ownKeys(...)` alongside the existing `Object.keys(...)` / `Object.values(...)` slices in the runtime and browser harness smoke lanes.
- The `Object.values` spread smoke now also mirrors the single-quoted `globalThis['Object'].values(...)` / `globalThis['Object']['values'](...)` aliases in the browser-targeted build/check matrix, keeping that helper family aligned with the existing `globalThis["Object"]` spellings.
- The static object-helper wrapper slice now also accepts sequence-expression wrappers around `Object.hasOwn(...)`, `Object.keys(...)`, and `Reflect.ownKeys(...)` targets in the checker and browser/standalone smoke coverage, and the Deno check smoke now also covers the static `Reflect.ownKeys(...)` slice in JS and TS input. The `Object.fromEntries(Object.freeze([...]))` entries slice now also flows through the same static object-model path, and the `Object.hasOwn` helper inventory now also carries the frozen bracketed `globalThis["Object"].prototype["hasOwnProperty"].call` alias pair on the same static-object slice.
- The browser bundle `Object.fromEntries` smoke now also covers frozen callable aliases around `Object.fromEntries`, including the dot-root and fully bracketed `globalThis` spellings, keeping that helper slice aligned with the broader transparent-wrapper inventory; the browser-harness/browser-bundle `Object.fromEntries` source now also mirrors the mixed-quote bracket-root `globalThis["Object"]['fromEntries']` / `globalThis['Object']["fromEntries"]` spellings on that same supported slice, and the browser bundle/harness smoke now also covers the same mixed-quote `fromEntries` spellings.
- The object-enumeration iterator slice now also accepts frozen callable aliases around `Object.keys(...)`, `Object.values(...)`, `Object.entries(...)`, and `Reflect.ownKeys(...)`, including the single-quoted `globalThis['Reflect'].ownKeys` / `globalThis['Reflect']['ownKeys']` spellings and the new dot-root single-quoted `globalThis.Reflect['ownKeys']` / `Object.freeze((globalThis.Reflect['ownKeys']))` pair on that same supported slice, keeping the checker, build smoke, and browser/runtime smoke aligned on the callable-wrapper path. The browser runtime `Object.fromEntries(...)` hasOwn smoke now also covers frozen `Object.hasOwn` and `Object.prototype.hasOwnProperty.call` aliases over the same frozen object operand, and the shared property-call helper inventory now also carries parenthesized `globalThis["Object"]` / `globalThis['Object']` root-wrapper variants on the same call path, and the direct runtime plus browser harness `for await` object-helper smoke now also covers frozen callable `Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` aliases, plus the dot-root `globalThis.Object.keys(...)` / `globalThis.Object.values(...)` / `globalThis.Object.entries(...)` frozen-callable aliases, and now also carries the bracketed `globalThis["Object"]["keys"](...)` / `globalThis["Object"]["values"](...)` spellings alongside the existing bracketed entries slice, and the parenthesized `Object.freeze((globalThis.Object.keys))(...)` / `Object.freeze((globalThis.Object.values))(...)` / `Object.freeze((globalThis.Object.entries))(...)` / `Object.freeze((globalThis["Object"].entries))(...)` wrappers now ride that same smoke path. The same supported object-enumeration slice now also accepts parenthesized frozen-callable wrappers around those helper spellings in the checker, codegen, and smoke lanes, including the bare `Reflect.ownKeys` / `Object.freeze((Reflect.ownKeys))` alias pair, the parenthesized bracket-root `Object.freeze((globalThis["Reflect"]).ownKeys)` / `Object.freeze((globalThis['Reflect']).ownKeys)` forms, the parenthesized bracketed-key `Object.freeze((globalThis["Reflect"]["ownKeys"]))` / `Object.freeze((globalThis['Reflect']['ownKeys']))` forms, and the parenthesized double-quoted bracket-root `Object.freeze((globalThis["Reflect"])["ownKeys"])` form, and now also carries break/continue finalization coverage for `Object.keys(...)`, `Object.values(...)`, `Object.entries(...)`, and `Reflect.ownKeys(...)` in the standalone and browser harness smoke paths. The same object-enumeration callable detection now also accepts statically decidable logical/nullish wrappers around the supported helper aliases, including `Object.freeze((null ?? Reflect.ownKeys))`, `Object.freeze((true && Reflect.ownKeys))`, and `Object.freeze((false || Reflect.ownKeys))`, aligned with the existing transparent-wrapper slices, and now also carries the matching `globalThis.Reflect.ownKeys` nullish/logical wrapper aliases in the shared helper inventory. The plain string-primitive `Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` smoke now also accepts the matching `null ??` / `true &&` / `false ||` callable-wrapper aliases in the standalone and browser bundle/harness lanes. The browser runtime/browser harness `Reflect.ownKeys` source now also includes the parenthesized double-quoted bracket-root `Object.freeze((globalThis["Reflect"])["ownKeys"])` alias, and the browser-bundle smoke lane now also includes the statically decidable nullish/logical wrapper aliases around bare `Reflect.ownKeys`. The browser-requested run/test harness smoke now also includes the nullish/logical wrapper aliases around bare `Reflect.ownKeys` in JS input. The static `Object.fromEntries(...)` entry-array slice now also accepts same-branch conditional wrappers in the checker and browser-harness/browser-bundle smoke lanes, and the browser object-values spread bundle/runtime smoke now also mirrors the mixed-quote bracket-root `globalThis["Object"]['fromEntries']` / `globalThis['Object']["fromEntries"]` spellings on that same helper path. The browser for-await object-string-enumeration smoke now also covers frozen callable `Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` aliases over the string primitive path, plus the nullish/logical wrapper aliases `Object.freeze((null ?? Object.keys))`, `Object.freeze((true && Object.values))`, and `Object.freeze((false || Object.entries))` on that same supported slice. The browser for-await object-helper smoke now also carries `Object.freeze((true && Object.keys))`, `Object.freeze((false || Object.values))`, and `Object.freeze((true && Object.entries))` on the same transparent-wrapper path. The checker-side object-helper iteration regression now also covers same-branch conditional wrappers around `Object.keys` and `Object.entries` on that same supported slice.
- The browser-targeted build smoke matrix now also covers the frozen dot-root and mixed-bracket `Object.hasOwn` aliases over the same frozen object operand, keeping that helper slice aligned with the rest of the transparent `Object.freeze(...)` coverage, and the same static-object helper smoke now also accepts the optional-chain-wrapped `Object.freeze(globalThis?.Object.prototype.hasOwnProperty.call)` / `Object.freeze((globalThis?.Object.prototype.hasOwnProperty.call))` aliases plus the optional-chain-wrapped bracketed `Object.freeze(globalThis?.Object["prototype"].hasOwnProperty.call)` / `Object.freeze((globalThis?.Object["prototype"].hasOwnProperty.call))` forms and the optional-chain-wrapped bracket-root `Object.freeze((globalThis?.Object)["prototype"].hasOwnProperty.call)` / `Object.freeze((globalThis?.Object)["prototype"].hasOwnProperty["call"])` pair, and the parenthesized double-quoted bracket-root `Object.freeze((globalThis["Object"]).prototype["hasOwnProperty"]["call"])` alias. The direct runtime browser smoke for `Object.fromEntries(...).hasOwn` now also mirrors the optional-chain-wrapped frozen `Object.hasOwn` / `Object.prototype.hasOwnProperty.call` aliases, including the bracketed property-call form, keeping the runtime lane aligned with the shared helper inventory. The same build smoke lane now also carries the parenthesized bracket-root `Object.freeze((globalThis["Object"]).hasOwn)` / `Object.freeze((globalThis["Object"])["hasOwn"])` and single-quoted `Object.freeze((globalThis['Object']).hasOwn)` / `Object.freeze((globalThis['Object'])['hasOwn'])` spellings for that helper slice, plus the parenthesized receiver-wrapped bracket-root `Object.freeze((globalThis["Object"]).prototype["hasOwnProperty"].call)` / `Object.freeze((globalThis['Object']).prototype['hasOwnProperty'].call)` pair on the same helper path, and now also covers the parenthesized dot-root bracketed `Object.freeze((globalThis.Object)["hasOwn"])` spelling there too.
- The shared `Object.hasOwn` / `Object.prototype.hasOwnProperty.call` frozen-callable inventory now also carries statically decidable nullish/logical wrappers around the canonical helper forms, keeping those helper slices aligned with the rest of the transparent-wrapper coverage.
- The `Object.is` same-reference smoke now also covers await-wrapped and optional-chain-wrapped static member roots in standalone and browser-requested harness/bundle paths, and the browser-requested alias-chain harness now also mirrors the optional-chain-wrapped same-reference spelling, keeping the transparent wrapper slice aligned with the checker's current static-reference resolution.
- The browser bundle smoke matrix now also mirrors the bracketed frozen `Math.pow` wrapper aliases around `globalThis.Math["pow"]` and `globalThis["Math"]["pow"]`, matching the existing browser-requested harness coverage for that same supported slice, and the type checker now also keeps the same frozen `Math.pow` callable aliases aligned across js-like extensions, including the single-quoted `globalThis['Math']['pow']` and `Math['pow']` spellings plus the mixed-quote bracketed `globalThis["Math"]["pow"]` alias; the `Math.pow` frozen-callable slice now also accepts nullish/logical wrappers around the supported `Math.pow`, `globalThis.Math.pow`, `globalThis["Math"]["pow"]`, and `globalThis['Math']['pow']` aliases.
- The array-iteration smoke now also accepts `Array.from(new Set(...))` and `Array.from(new Map(...))` wrappers over the same supported iterable path in JS and TS input, and the browser harness/bundle smoke lanes now mirror that same Array.from set/map slice too, keeping the existing Set/Map collectors visible through the Array.from alias path too, and the browser harness/bundle Array.from set/map smoke now also mirrors nullish/logical wrappers around the supported bracket-root aliases, and the browser harness/bundle set/map smoke now also includes the fully bracketed double-quoted `globalThis["Array"]["from"]` direct-call alias on that same supported path. The same supported path now also accepts frozen constructor-result wrappers such as `Object.freeze(new Set(...))` and `Object.freeze(new Map(...))` plus the parenthesized `Object.freeze((new Set(...)))` / `Object.freeze((new Map(...)))` variants, and standalone run/test smoke now also covers the corresponding `Array.from(Object.freeze(new Set(...)))` / `Array.from(Object.freeze(new Map(...)))` constructor-result slice in JS, TS, JSX, and TSX input, while browser-harness/browser-bundle smoke now also covers that frozen constructor-result slice in JS, TS, JSX, and TSX input, matching the browser-requested run/test harness coverage; browser-targeted check smoke now also covers that frozen constructor-result slice in JS, TS, JSX, and TSX input; the browser harness/bundle Array.from slice now also covers corresponding `Array.from(Object.freeze(new Set(...)))` / `Array.from(Object.freeze(new Map(...)))` constructor-result wrappers, including parenthesized variants, in JS, TS, JSX, and TSX input; the browser-targeted build smoke now also covers the direct `Array.from(new Set(...))` / `Array.from(new Map(...))` slice in JSX and TSX input; the supported Array.from set/map slice now also has explicit break/continue smoke on the standalone and browser-requested harness paths, and the corresponding Set return/finalization probes now also ride those same direct and browser-requested harness smoke lanes, keeping the abrupt-completion coverage aligned with the rest of the iterator smoke. The bracketed `globalThis["Array"].from` freeze alias now also has matching browser harness/bundle coverage on the same set/map slice, the fully bracketed `globalThis["Array"]["from"]` freeze alias now also joins that coverage, the single-quoted bracketed `globalThis['Array']['from']` freeze alias now also joins that coverage, and the mixed-quote bracketed `globalThis["Array"]['from']` / `globalThis['Array']["from"]` freeze aliases now also join the same smoke lanes, and the new dedicated regression keeps those spellings anchored across standalone, browser-requested, and browser-bundle coverage, while the direct double-quoted `Array["from"]` freeze alias now also joins the standalone, browser-requested, and browser-bundle smoke lanes, and the dot-root `Object.freeze(globalThis.Array.from)` / `Object.freeze(globalThis.Array["from"])` aliases now also join the standalone, browser-requested, and browser-bundle smoke lanes, with the parenthesized dot-root `Object.freeze((globalThis.Array).from)` / `Object.freeze((globalThis.Array)["from"])` / `Object.freeze((globalThis.Array)['from'])` aliases now also joining that same lane, with the parenthesized dot-root bracket alias `Object.freeze((globalThis.Array))["from"]` now also joining the same Array.from frozen-callable inventory. The same helper inventory now also carries nullish/logical wrappers around the bracketed `globalThis["Array"].from` alias, including `Object.freeze((null ?? globalThis["Array"].from))`, `Object.freeze((true && globalThis["Array"].from))`, and `Object.freeze((false || globalThis["Array"].from))`, plus the fully bracketed `globalThis["Array"]["from"]` and `globalThis['Array']['from']` wrapper families. Frozen object-helper iterable slices such as `Object.freeze(Object.keys(...))` / `Object.freeze(Object.values(...))` / `Object.freeze(Object.entries(...))` now also carry through the checker and smoke lanes; the build smoke matrix now also exercises the frozen `Set`/`Map` constructor-result wrappers in Deno and browser JS/TS input and now also mirrors the single-quoted `globalThis['Set']` / `globalThis['Map']` constructor spellings and their frozen constructor-result wrappers on the same Array.from slice; the same Array.from frozen constructor-result smoke now also covers the transparent `true &&` constructor-target wrapper on the same supported path. The `for await` array-iteration smoke now also covers the const-alias, const-string-alias, and const-boolean-alias browser TS build slices, and the same supported iterable path now also covers `for await` over frozen `new Set(...)` / `new Map(...)` constructor results plus frozen `Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` iteration targets in the standalone and browser harness/bundle smoke lanes. The shared helper inventories now also include the matching `Object.freeze(new Set(...))` / `Object.freeze(new Map(...))` and `Array.from(Object.freeze(...))` wrapper forms used by those smoke lanes.
- The frozen Set/Map constructor-result smoke now also includes bracketed `globalThis["Set"]` and `globalThis['Map']` aliases plus parenthesized variants in the standalone and browser harness/bundle paths, keeping the direct constructor-result slice aligned with the broader transparent-wrapper inventory; the parenthesized frozen-result slice now also accepts statically decidable nullish/logical wrappers around those `Set`/`Map` constructor targets.
- The shared `Array.from` frozen-callable inventory now also includes nullish/logical wrappers around the single-quoted bracket-root and mixed-bracket aliases `globalThis['Array'].from`, `globalThis["Array"]['from']`, and `globalThis.Array['from']`, keeping the transparent-wrapper coverage aligned with the existing `globalThis.Array.from` and `globalThis["Array"].from` families. The dedicated `array_from_set_map_dot_root_aliases` smoke now also covers the parenthesized dot-root `Object.freeze((globalThis.Array).from)` / `Object.freeze((globalThis.Array)["from"])` / `Object.freeze((globalThis.Array)['from'])` aliases across the standalone, browser-requested, and browser-bundle lanes.
- The browser-harness/browser-bundle `Array.from` smoke now also covers the parenthesized double-quoted bracket-root dot-call alias `Object.freeze((globalThis["Array"]).from)` on the same supported slice.
- The direct Set/Map constructor iteration smoke now also accepts statically decidable nullish/logical wrappers around the constructor target, keeping the direct iterator constructors aligned with the existing transparent-wrapper path.
- Default-export generator and async-generator function declarations now have explicit parser, AST, HIR, MIR, LIR, codegen, library-export gate, and CLI build/runtime smoke coverage in the current test matrix, including anonymous default-export forms on the generator path, browser-harness/browser-bundle smoke for the anonymous default-export generator and async-generator paths, and build coverage on the async-generator path, keeping the declaration-specific generator rejection path pinned while resumable lowering remains gated. The current regression matrix now also pins the anonymous default-export async-generator `yield*` delegation rejection path in the build/runtime smoke lanes.
- The generator library-export rejection helper now delegates to the shared common wording helper directly, so async-generator expression and binding rejections stay on the async-generator wording path instead of a recursive local call.
- Host/API lanes: default standalone, browser-targeted check/build/bundle flows, browser harness execution paths, documented Deno and Node API slices, resource budget validation, and late-host/object-model gating, including await-wrapped receiver handling on the late host-control process slice, computed `Deno["Command"]` subprocess aliases, including the mixed `globalThis.Deno["Command"]` spelling, the now-supported `Deno.args` invocation-argument length slice, and the shared frozen `SharedArrayBuffer` / `Atomics` late-threaded-runtime aliases, including the single-quoted and parenthesized frozen bracket-root spellings now carried by the shared helper inventory; the late weak-reference smoke also now exercises transparent `Object.freeze(...)` wrappers around the canonical `WeakMap` / `WeakSet` / `WeakRef` / `FinalizationRegistry` aliases, and the late object-model smoke now also covers transparent `Object.freeze(globalThis.Intl.NumberFormat)` wrappers on the same rejection path.
- The Node `process.kill(0)` zero-probe smoke now also covers optional-chain-wrapped aliases such as `process?.kill(0)`, `globalThis.process?.kill(0)`, and `globalThis["process"]?.kill(0)` alongside the existing zero-probe inventory.
- Policy validation now also rejects positive `resources.maxSpawnedProcesses` values before subprocess support exists, keeping the phase-3 budget gate aligned between attached policies and direct CLI overrides.
- Package lanes: install/lock/materialization, registry and raw-URL workflows, package-shape rejection, package-corpus probes, and single-package registry-analysis commands. The `package-effects` CLI smoke now also covers the canonical `jsr:@std/path` target spelling alongside the npm path, keeping the native-JSON package-analysis label contract explicit; the command-target parser now also explicitly rejects blank, whitespace-padded, and non-registry package identifiers before lookup; the package-analysis-specific flag gate now also short-circuits before package-target validation/registry lookup so malformed selectors do not override the outer E5508 command-shape rejection; the coordinate validator now also pins stable SemVer release strings for the analyzed package version, now rejects leading/trailing whitespace in package-coordinate fields, and now rejects internal whitespace in registry-native package names; the inherited-analysis payload validator now also accepts the explicit `default` inherited `apiSurface` label alongside the phase-owned `deno` / `node` / `browser` values, and package-effects analysis-context validation now also rejects whitespace-padded `apiSurface` spellings; and the schema hardening regressions now also pin mismatched canonical report labels for both jsr and npm coordinates.
- Verification lanes: Lean proof project and published proof-backed boundary limited by `proofs/BOUNDARY.md`.
- Schema-v1 emission hardening now also explicitly rejects non-object `package-effects` payloads, non-canonical package registry names in that payload's package coordinate, registry-prefixed package names in the coordinate payload, and mismatched canonical `report.entryPoints[0]` labels for `npm` and `jsr` coordinates, mirroring the existing package-audit payload-null boundary in the validation helpers; the public schema-validation helpers now carry matching regressions for those payload-shape checks, including the jsr canonical-label whitespace-padding path on `report.entryPoints[0]`. The install JSON payload now also rejects empty string and whitespace-only `manifestPath` / `lockPath` values when those paths are present, keeping install-output path validation aligned with the existing init payload contract. CLI envelope artifact entries and build-result artifact entries now also reject empty artifact `path` / `kind` labels, and the artifact `path` / `kind` / `role` labels now also reject surrounding whitespace padding in both payload families, while build-result and artifact-metadata `artifactKind` / `buildMode` / `bundleFormat` labels now also reject surrounding whitespace padding, and CLI envelope diagnostics are now emitted in stable file/line/column/code order before envelope validation, keeping the schema-v1 artifact and diagnostic contracts aligned with the deterministic sort rules.

## Planning consequence

The active plan should not list already-implemented command families, artifact families, smoke matrices, alias inventories, or diagnostic regressions as open work. Future tasks should be stated as remaining capability goals and should point to specs/tests for details.
