# Current Implementation Baseline

This file records the planning baseline for the active continuation roadmap. It is a current-state summary, not an active checklist and not an availability matrix.

## Repository evidence used for this cleanup

- `cargo run -q -p kali_cli --bin kali -- --help` exposes the current public command set.
- The workspace contains implementation crates for CLI, frontend, AST/HIR/MIR/LIR, codegen, runtime, sandbox/effects, package management, optimization, host APIs, embedding/C ABI, and bindings.
- The browser bundle CJS smoke now also covers JSX and TSX input on both the explicit and inherited browser API-surface builds, with deterministic JSON output.
- The proof tree exists under `proofs/`, and `proofs/BOUNDARY.md` states that Kali is proof-backed for the published boundary.
- Canonical availability and current-state nuance remain in `specs/19-feature-maturity.md`.

## Live surface at a glance

The current repository has already implemented the historical MVP and several later surfaces:

- CLI commands: `doctor`, `init`, `install`, `fmt`, `lint`, `check`, `build`, `run`, `test`, `effects`, `package-effects`, and `package-audit`; the `package-audit --preview` legacy shim stays rejected before registry lookup even when no package target is provided.
- Build modes and artifacts: executable builds, browser bundles, `--validate-ir`, `--lib`, `--capi`, `--component`, and deterministic artifact metadata/sidecars.
- Execution/reporting: `run`, `test`, `test --coverage`, stable JSON envelopes, public effect reports, registry effect/audit reports, and schema-v1 payload validation.
- Host/API slices: Deno-oriented default surface, including the read-only `Deno.cwd` / `Deno.pid` queries in the current standalone snapshot while `Deno.chdir` and `Deno.exit` are now also available there; the standalone surface now also accepts the bracketed `Deno["cwd"]` / `globalThis["Deno"]["cwd"]` spellings, plus the bracketed `Deno["chdir"]` / `Deno["exit"]` spellings, and now also mirrors the `globalThis.Deno.chdir` / `globalThis["Deno"].chdir` and `globalThis.Deno.exit` / `globalThis["Deno"].exit` alias forms, and standalone runtime smoke now also covers the `Deno.chdir` alias family in `test` and JSON `test` paths on JS input, and the Deno runtime projection now also exposes a deterministic environment snapshot helper alongside the mutable env view, and the shared Deno/Node helper projections now also expose explicit `has` convenience checks for their captured environment views; browser-entrypoint smoke now also covers the remaining Chromium/Google Chrome executable aliases (`chromium-for-testing`, `chromium for testing`, `google chrome beta`, `google chrome dev`, `google chrome for testing`, `google chrome unstable`) plus the remaining Vivaldi alias spelling (`vivaldi-snapshot`) across the runtime helper tests and the CLI run/test browser-entrypoint smoke. The env-snapshot surface now also lowers the documented mixed dot/bracket `Deno.env.toObject` alias family on the default standalone Deno path, while browser and Node compatibility surfaces still keep the rejection path explicit; `Deno.env.get` / `Deno.env.has` now share the same read-only env slice on the default standalone surface, the default standalone surface now also accepts `Deno.env.set` / `Deno.env.delete` plus the bracketed `Deno["env"]["set"]` / `Deno["env"]["delete"]` forms on that same env view, and the Rust Deno env projection now also exposes a matching `delete` alias over `remove` for parity, and the standalone run/test smoke now also mirrors the bracketed `Deno["env"]["has"]` / `globalThis["Deno"]["env"]["has"]` forms, including the fully bracketed `globalThis["Deno"]["env"]["has"]` spelling, while `build_tests.rs` now also mirrors those same bracketed `has` forms in the executable build path and `.js` input, and the standalone runtime smoke now also covers the same bracketed `has` forms in JSX and TSX input on the `check` / `build` / `run` / `test` paths, including JSON `test` coverage; executable build smoke now also covers the default standalone mutable-env set/delete slice across TS, JS, JSX, and TSX input, including the fully bracketed `globalThis["Deno"]["env"]["set/delete"]` spellings; documented Node subsets (including the read-only `process.pid` query, read-only `process.cwd` query, `process.chdir` cwd-mutation path, the `process.env.KEY = ...` / `delete process.env.KEY` property-mutation slice on the Node surface, the bracketed `process["env"]["KEY"]` / `delete process["env"]["KEY"]` forms, and the nested bracketed `process["env"]["KEY"]` / `delete process["env"]["KEY"]` spellings under `globalThis["process"]`, plus the bracketed `process["cwd"]` / `process["chdir"]` / `process["exit"]` forms and their `globalThis.process[...]` spellings); browser-targeted `check` / `build --bundle`; and browser-harness-assisted `run` / `test` paths where configured; the browser runtime summary fallback matrix now also covers unreadable summary files in TSX input on the JSON `test` and human `run` / `test` paths, and whitespace-only summary files in TS and TSX input on the JSON `test` path, and the browser-requested/browser-bundle runtime smoke now also covers whitespace-only summary-file fallback on the human-output path in JS input, with the dedicated JS-input regression file now also mirroring that whitespace-only browser-requested human-output fallback on the run/test paths, and the browser-requested summary-fallback smoke now also covers JSX input on the JSON `test` and human `run` / `test` paths, and the browser-requested summary-fallback smoke now also covers invalid `args` / `tests` array item types in JSX and TSX input, and the browser-requested summary-fallback smoke now also covers null `args` / `tests` fields as shape-invalid across JS, TS, JSX, and TSX input on the JSON `test` path, and the browser-requested missing-summary fallback smoke now also covers JSX and TSX input on the supported JSON `test` and human `run` paths, and the browser-harness dynamic-import smoke now also covers explicit `.js` file-specifier targets in JS input, and raw backtick template-literal specifiers with statically evaluable interpolations now also have browser-bundle smoke coverage in TS and `.js` input, and browser-requested `run` / `test` smoke now also accepts that same template-literal dynamic import slice in JS and TS input with JSON-output coverage, while browser-targeted `check` / `build --bundle` now also pin the non-literal `import(expr)` gate in `.js` input with matching JSON-output rejection coverage, and the browser bundle rejection lane now also mirrors that same gate in `.jsx` and `.tsx` input; the same sequence-expression peel now also applies to the static string, numeric, and object-identity helper slices, which lets sequence-wrapped dynamic-import targets and supported Math/Object literal folds resolve through the bounded path; the non-browser literal-object `Object.hasOwn` / `Object.prototype.hasOwnProperty.call` slice now folds static object literals and const alias chains in JS input, browser-targeted static-object-call slices are now supported on the documented browser command set, browser-bundle smoke now also covers the bracketed `globalThis["Object"]["hasOwn"]` / `globalThis["Object"]["prototype"]["hasOwnProperty"]["call"]` forms in TS and `.js` input and now also covers the mixed `globalThis.Object.prototype["hasOwnProperty"]["call"]` spelling in TS and `.js` input, and now also covers the mixed `globalThis["Object"].prototype["hasOwnProperty"]["call"]` spelling in TS and `.js` input, browser-harness run/test smoke now also mirrors the same static-object-call slice in TS and `.js` input, and the supported static-object slice also accepts `as const` wrappers around the object operand in JS input, dynamic-object cases remain gated, the static primitive-literal `Object.is` slice now also folds deterministically on the supported standalone/browser smoke paths, including the bracketed `globalThis["Object"]["is"]` spelling, browser bundle smoke now also covers that same static primitive-literal slice in TS, `.js`, `.jsx`, and `.tsx` input, including unary-plus-wrapped numeric literal cases in the browser Object.is bundle/harness coverage, browser-harness run/test smoke now also mirrors that same slice in TS and `.js` input, and build smoke now also covers the same numeric-literal slice in JSX and TSX input on both the Deno and browser surfaces, the default standalone run smoke now also exercises the `Deno.chdir` path, and the browser-targeted late-process-control smoke still includes the bracketed `Deno.chdir` rejection spellings while the entry-body codegen stack cleanup keeps the current void-entry validations stable. The default standalone late-compatibility smoke now also pins property-mutation rejection for `process.env.KEY = ...` / `delete process.env.KEY`-style source forms, including the bracketed and `globalThis.process[...]` spellings, and browser build smoke now also covers the nested bracketed `globalThis["process"]["env"]["KEY"]` mutation spellings on the browser API surface. The Node surface still stays outside the default-standalone `Deno.env.set` / `Deno.env.delete` slice. Late-compatibility smoke now also pins the remaining mixed dot/bracket `Deno.env.toObject` and `Deno.env.set/delete` spellings, including the newly covered `Deno["env"].toObject` / `globalThis.Deno["env"].toObject` and `Deno["env"].set/delete` / `globalThis.Deno["env"].set/delete` families, and a representative browser TSX late-compatibility slice now mirrors the same Deno/process/object-model/threaded-gate rejection families, including the fully bracketed `globalThis["Deno"]["env"]["set/delete"]` spellings. The browser TSX rejection matrix now also mirrors the `Deno.env.toObject` alias family so the object-materialization gate stays source-class aligned with the existing browser JS-input coverage, and the TSX env-mutation lane now also includes `Deno["env"].set/delete`, `globalThis.Deno["env"].set/delete`, and `globalThis["Deno"]["env"]["set/delete"]` spellings to stay aligned with the JS-input env-mutation coverage. The browser JS and TSX source fixtures now also carry the mixed `globalThis.Deno["env"].{set/delete}` and `globalThis["Deno"].env["set/delete"]` spellings alongside the fully bracketed forms, keeping the late-env mutation helper matrix aligned with the shared browser fixtures. The standalone Deno host-control smoke now also covers `globalThis.Deno["chdir"]` / `globalThis["Deno"]["chdir"]` and the `Deno.exit` / `Deno["exit"]` / `globalThis.Deno["exit"]` / `globalThis["Deno"]["exit"]` alias family on the supported standalone path. Coverage hardening now also pins the bracketed `connect` / `listen` / `serve` spellings for the phase-three Deno host-effect smoke in the default standalone runtime tests, keeping the standalone rejection lane aligned with the browser late-compatibility alias coverage; the `effects` command now also accepts that same `--wasm-threads` profile slice on `.js` input, including inherited runtime-profile coverage and the JSON effects envelope, keeping the threaded-profile evidence aligned across analysis and execution-style host surfaces. The browser late-compatibility process-control smoke now also asserts the fully bracketed `globalThis["process"]["pid"|"cwd"|"chdir"|"exit"]` spellings in the browser JS and TSX helpers. Wrapped call targets around the supported `Object.hasOwn` / `Object.is` / `Math` helper slices now also peel the same transparent wrappers, keeping call-site transparency aligned with the member-root wrapper handling, and the member-root naming helpers now also peel transparent sequence-expression wrappers so late-host/object-model alias diagnostics stay aligned with the same transparent-wrapper path. The threaded-topology snapshot helper family also exposes JSON-ready aliases on the runtime-host side, and the shared `ThreadRuntimeTopology` helper plus the shutdown report now carry `thread_topology_snapshot_value` / `thread_topology_snapshot_json_value` aliases so future callers can target the same deterministic thread-topology shape from either side of the API; the topology helper now also exposes a `thread_topology_snapshot()` alias to keep the base-name spelling aligned with the rest of the family, and the runtime host-state now also exposes a bare `snapshot()` alias for that same deterministic thread-topology view.

- Iterator slices: the shared literal-array `for...of` / `for await...of` lowering path now covers literal arrays with literal elements, const numeric alias elements, const string alias elements, and simple variable bindings in TS and `.js` input, with browser-harness smoke on the supported run/test paths; transparent parenthesized/type-assertion/satisfies/chain wrappers and TS `as const` wrappers around those supported array iterables/elements are now also covered, with browser-harness `run` / `test` now also exercising the TS `as const` and `satisfies` wrapper slices, browser-harness `test` now also mirrors the `for...of` const-alias-chain slice in TS input, and `for await...of` parenthesized binding wrappers around the loop target are now also covered in browser-harness `run` / `test` smoke on both TS and `.js` input, while browser build smoke now also mirrors the const string-alias slice for both loops on the browser API surface in TS and `.js` input, browser bundle smoke now also covers the parenthesized-binding wrapper slice for both loops in TS and `.js` input, browser-harness `run` / `test` now also covers the direct literal-array `for await...of` slice in both TS and `.js` input, and browser-harness JSON-output coverage now also pins the direct literal-array `for await...of` slice in TS input, browser-harness `run` / `test` now also exercises the `for...of` `as const` wrapper slice in TS input, browser-harness `run` / `test` now also exercises the `for...of` `as const` and `satisfies` wrapper slices in `.js` input, browser build smoke now also mirrors that `for...of` `as const` wrapper slice on the browser API surface in TS input, browser bundle smoke now also covers that `for...of` `as const` wrapper slice in TS and `.js` input, browser-harness `run` / `test` and JSON-output coverage now also exercise the `for...of` `satisfies` wrapper slice and the `for await...of` `as const` / `satisfies` wrapper slices in TSX input, browser bundle smoke now also covers the `for await...of` const-string-alias and parenthesized-const-alias slices in both TS and `.js` input, browser build smoke now also mirrors the `for await...of` `as const` and `satisfies` wrapper slices in TS input on both the Deno and browser surfaces, browser bundle smoke now also covers those `for await...of` `as const` and `satisfies` wrapper slices in `.js` input, `.jsx` input, and `.tsx` input, browser-harness run/test smoke now also covers the const-alias-chain slice for both loops in `.js` input, and browser-harness JSON-output coverage now also pins that same JS-input const-alias-chain slice for both loops; the browser analysis/bundle JSON success paths now also pin empty `errors` arrays for representative `for...of` and `for await...of` smoke slices; dedicated browser-bundle regressions now also pin the const-alias-chain slice for both loops in TS and `.js` input, and browser-harness `run` / `test` JSON-output coverage now also pins the const-alias-chain slice for both loops in TS input, and browser-harness run/test and JSON-output coverage now also covers the parenthesized const-alias wrapper slice for both loops in TS input, and browser-harness run/test and JSON-output coverage now also covers the `for await...of` parenthesized const-alias wrapper slice in JSX and TSX input. The `for...of` parenthesized const-alias wrapper slice now also has browser-harness run/test and JSON-output coverage in JSX and TSX input, and browser-bundle coverage in JSX and TSX input. Browser-harness run/test smoke now also covers the sequence-expression slice for both loops in TS, `.js`, `.jsx`, and `.tsx` input, and browser-harness run/test smoke now also covers the spread-element slice in JS input, including the parenthesized spread-target slice for both loops, and the shared helper now also accepts transparent decorated spread-target wrappers around const-bound literal arrays in direct resolution tests, while browser-harness run/test and JSON-output coverage now also cover that same spread-element slice in JSX and TSX input, and browser bundle smoke now also covers the same spread-element slice in TS and `.js` input. The supported iterator slice now also accepts `Object.keys(...)` / `Object.values(...)` over static object literals with string-literal keys and const alias chains in standalone run/test and browser-targeted build --bundle smoke.
- Compound-assignment slices: arithmetic compound assignment expressions (`+=`, `-=`, `*=`, `/=`, `%=`, `**=`) now also lower for mutable local bindings with runtime local storage, so the standalone `run` / `json run` JS-input smoke path now preserves simple local update cases even when the right-hand side is a runtime value; mutable `let` / `var` bindings now also avoid the direct codegen binding cache's stale const-folding path, so mutable slots stay distinct from const-only aliases; transparent parenthesized/type-assertion/satisfies wrapper targets around those mutable locals are now also accepted in the current codegen lowering path, while non-identifier compound-assignment targets and immutable bindings now reject explicitly instead of silently lowering, and the remaining broader operand shapes remain follow-up work.
- Nullish coalescing `??` remains covered on the supported `check` / `run` / `test` paths, and nullish assignment `??=` now also has direct TS/JS, browser-harness, and browser-bundle smoke coverage on the supported paths.
- Basic unary prefix semantics: prefix `!`, unary `-`, unary `+`, `void`, and the local-binding `++` / `--` update-expression slice are covered by runtime-smoke regression coverage in both `.ts` and `.js` inputs on the standalone `run` / `test` paths, and browser-requested `run` / `test` harness coverage now also exercises the same slice in `.ts` and `.js` input with JSON-output coverage.
- Browser-harness/browser-bundle bracketed `globalThis["Math"]` root smoke now mirrors the existing `Math.pow` alias-chain slice in TS, `.js`, `.jsx`, and `.tsx` input, and now also covers the `Math.exp` / `Math.log` identity slice in TS and `.js` input, plus the fully bracketed `globalThis["Math"]["exp"]` / `globalThis["Math"]["log"]` slice in TS and `.js` input, plus the `Math.sinh` / `Math.cosh` / `Math.tanh` zero-identity slice in TS, `.js`, `.jsx`, and `.tsx` input, and the fully bracketed `globalThis["Math"]["sin"]` / `globalThis["Math"]["cos"]` / `globalThis["Math"]["tan"]` slice in TS and `.js` input, plus browser-requested run/test smoke for that same fully bracketed slice in JSX and TSX input with JSON-output coverage, and now also covers the `Math.max` / `Math.min` / `Math.abs` / `Math.sign` / `Math.imul` / `Math.clz32` core suite in TS and `.js` input, and now also covers the bracketed `globalThis["Math"]["floor"]` / `globalThis["Math"]["trunc"]` / `globalThis["Math"]["ceil"]` slices in TS and `.js` input. The fully bracketed `globalThis["Math"]["pow"]` member-access slice now also has the same browser-harness/browser-bundle smoke coverage in TS, `.js`, `.jsx`, and `.tsx` input, and the `globalThis.Math.round` root slice now also has browser build smoke coverage in JSX and TSX input.
- Browser-harness/browser-bundle Math.atan2 smoke now also covers the single-quoted bracketed `globalThis['Math']['atan2']` spelling in TS and `.js` input.
- Browser-harness/browser-bundle Math.atan2 root smoke now also covers JSX and TSX input on the `globalThis.Math.atan2` slice, including the JSON-output build path.
- Package evidence: default standalone, browser check/bundle, browser-harness run/test, Node, Deno/JSR, registry-analysis, and published-bin-entrypoint contrast coverage; the browser runtime corpus now also rejects published bin-entrypoint probes on the browser surface and inherited browser surface when a harness command is configured, and now also covers the `spawn-tools` browser runtime run/test slice in TS input on those same browser contexts, with matching JSON-output coverage now also in JS input on the browser and inherited browser surfaces, and now also extends the `@mariozechner/pi-coding-agent` browser runtime run/test slice into JSX and TSX input on the explicit and inherited browser surfaces with matching JSON-output coverage; the Node package corpus now also exercises a `process.cwd` / `process.pid` / `process.chdir` / `process.exit` package slice on the documented Node surface.
- Runtime object-enumeration evidence: browser bundle smoke now also covers the `Object.fromEntries` slice in TS and `.js` input, including the `globalThis.Object.fromEntries`, mixed `globalThis.Object["fromEntries"]` / `globalThis["Object"].fromEntries`, and bracketed `globalThis["Object"]["fromEntries"]` alias spellings, and browser-requested run/test browser-harness coverage now also exercises the same slice and alias family in TS, `.js`, `.jsx`, and `.tsx` input with JSON-output coverage; browser-requested run/test browser-harness coverage now also accepts the `Object.fromEntries` entry-array `satisfies` wrapper in TS input; browser-requested run/test browser-harness coverage now also exercises the static `Object.entries(...)` object-literal slice in JS and TS input with JSON-output coverage, and the browser-bundle smoke now also covers the same `Object.entries(...)` alias family in JS, TS, JSX, and TSX input; browser-requested browser-harness coverage now also mirrors the `globalThis.Object.entries(...)` alias spelling in JS, TS, JSX, and TSX input with JSON-output coverage, and now also covers the mixed `globalThis.Object["entries"]` / `globalThis["Object"].entries` spellings alongside the existing fully bracketed form, keeping the entries alias aligned with the existing `Object.values(...)` harness coverage; browser-requested browser-harness coverage now also mirrors the same mixed `globalThis.Object["values"]` / `globalThis["Object"].values` and `globalThis.Object["keys"]` / `globalThis["Object"].keys` spellings in the supported object-enumeration slice, keeping the alias-family evidence aligned with the existing dotted and fully bracketed coverage; the browser-harness `Kali.test` source now also mirrors the TS `as const` branch so the `test` JSON path stays aligned with the `run` harness matrix, and the browser runtime package corpus now also extends the `spawn-tools` browser surface and inherited browser surface into JSX and TSX input with matching JSON-output coverage.
- Browser-aligned IndexedDB evidence: the deterministic `IndexedDb` stub now also exposes store snapshots plus JSON-ready snapshot aliases, keeping the browser runtime helper vocabulary aligned with the rest of the deterministic snapshot helpers.
- Browser navigator evidence: the browser `navigator()` baseline now also exposes deterministic snapshot helpers and JSON-ready aliases for its metadata view, keeping the browser helper vocabulary aligned with the rest of the deterministic snapshot family.
- Runtime built-in math evidence: `Math.max`, `Math.min`, `Math.abs`, `Math.sign`, `Math.imul`, `Math.round` (including the const-alias-chain smoke slice and direct build smoke coverage on that slice), `Math.clz32`, `Math.trunc`, `Math.ceil`, `Math.floor`, `Math.sqrt`, `Math.cbrt`, `Math.hypot`, `Math.pow`, and the new `Math.log2` exact power-of-two literal path are covered by checker/codegen/runtime smoke slices in the current repo, with browser bundle coverage on the documented subset, including bare `Math.log2` / `Math.log10` bundle smoke in TS and `.js` input; browser-harness/browser-bundle smoke now also covers the bracketed `globalThis["Math"]` / fully bracketed `globalThis["Math"]["round"]` slice for `Math.round` identity cases in TS, `.js`, `.jsx`, and `.tsx` input, and direct build smoke now also covers the `globalThis.Math.round` root slice in Deno and browser `.js` / `.ts` inputs; `Math.hypot` now also has direct `build` smoke coverage on the exact perfect-square-sum slice in the Deno `.js` / `.ts` inputs, plus browser-requested `run` / `test` smoke coverage in TS, `.js`, `.jsx`, and `.tsx` input with JSON-output coverage, and browser bundle smoke coverage in `.js` input; browser-harness/browser-bundle smoke now also covers the `globalThis.Math.hypot` / `globalThis["Math"]["hypot"]` root spellings in TS and `.js` input; `Math.abs` now also constant-folds statically-known integer-valued numeric literals, and `Math.sign` now also constant-folds statically-known numeric literals, including const numeric alias-chain resolution, the statically-known `abs` / `sign` / `floor` paths now also resolve through const numeric alias chains, `Math.imul` now also constant-folds statically-known integer literal operands, `Math.clz32` now also constant-folds statically-known numeric literal operands, including const numeric alias chains and a non-integer literal slice such as `1.6`, the statically-known `sqrt` / `cbrt` / `log2` / `log10` paths now also resolve through const numeric alias chains in the checker/runtime smoke path, `Math.hypot` now also has an exact integer-literal perfect-square-sum slice, and the `Math.pow` integer-exponent path now also honors const numeric alias chains for the supported integer-exponent smoke path and its canonical rejection cases, and its zero-exponent identity slice now also accepts non-integer base literals, with browser-harness/browser-bundle smoke now also covering that slice in TS and `.js` input, while the base-one identity slice now also folds on the supported smoke paths. Browser bundle smoke now also covers the same positive-integer-exponent alias-chain slice in TS, `.js`, `.jsx`, and `.tsx` input, browser-harness `run` / `test` smoke now also covers the signed-base `Math.pow` slice in JS input, and the browser-harness/browser-bundle `globalThis.Math.pow` path now also mirrors that alias-chain slice in TS, `.js`, `.jsx`, and `.tsx` input. The `Math.pow` exponent-one identity slice now also has browser bundle and browser-harness coverage in JSX and TSX input. The base-one identity slice now also has browser bundle coverage in TS, `.js`, `.jsx`, and `.tsx` input, plus browser-harness run/test and JSON-output coverage in TS, `.js`, `.jsx`, and `.tsx` input. The fully bracketed `globalThis["Math"]["pow"]` member-access slice now also has the same browser-harness/browser-bundle smoke coverage in TS and `.js` input. The browser-harness/browser-bundle smoke also now covers the bracketed `globalThis["Math"]` root for the `Math.pow` zero-exponent non-integer-base slice in TS and `.js` input, and browser-requested `run` / `test` smoke now also covers that same zero-exponent non-integer-base slice in JSX and TSX input; browser bundle smoke now also covers that same slice in JSX and TSX input. Direct `build` smoke now also pins the `Math.log2` / `Math.log10` const alias-chain cases in the Deno `.js` and `.ts` inputs, including both human and JSON output paths; direct `run` smoke still covers the same slice, and browser-harnessed `run` / `test` smoke now also covers the `Math.log2` / `Math.log10` literal slice in TS and `.js` input, with browser-harness/browser-bundle smoke now also covering the bracketed `globalThis["Math"]` root for that slice in TS and `.js` input, and browser bundle smoke now also covers the bare `Math.log2` / `Math.log10` literal slice in TS, `.js`, `.jsx`, and `.tsx` input, plus the exact `Math.exp` / `Math.log` identity folds in TS, `.js`, `.jsx`, and `.tsx` input, `Math.exp2` now also has the exact zero identity fold on the supported codegen/runtime smoke path, and direct build smoke now also covers that zero-identity slice in Deno and browser `.js` / `.ts` inputs; direct build smoke now also covers that same zero-identity slice in JSX and TSX input on the Deno and browser surfaces; browser-harness smoke now also covers that zero-identity slice in TS, `.js`, `.jsx`, and `.tsx` input, including JSON-output coverage for the TS input lane, and browser-harness/browser-bundle smoke now also covers the bracketed `globalThis["Math"].exp2` / fully bracketed `globalThis["Math"]["exp2"]` root in TS and `.js` input, and now also mirrors the `Math.atan2` zero slice and positive-integer-exponent `Math.pow` alias-chain slice in TS and `.js` browser-harness input. Browser-harness/browser-bundle bracketed `globalThis["Math"]` root smoke for the `Math.atan2` zero slice now also covers JSX and TSX input. The `Math.sqrt` / `Math.cbrt` literal slices now also have direct build smoke coverage in the Deno and browser `.js` / `.ts` inputs, and browser bundle smoke now also covers the `Math.sqrt` / `Math.cbrt` literal slices in TS and `.js` inputs; browser-harness and browser-bundle smoke now also cover the `globalThis.Math.sqrt` / `globalThis.Math.cbrt` root slice in TS and `.js` input, keeping the supported root-access matrix aligned with the bare `Math.sqrt` / `Math.cbrt` slice; `Math.ceil` / `Math.trunc` now also have direct build smoke coverage on const alias chains in the Deno and browser `.js` / `.ts` inputs. `Math.exp` / `Math.log` now also have exact zero/one identity folds in the current repo, `Math.expm1` / `Math.log1p` now also have exact zero identity folds, including const numeric alias chains, in the current repo, with browser-harness/browser-bundle smoke now also covering the exact zero identity slice in TS, `.js`, `.jsx`, and `.tsx` input and the bracketed `globalThis["Math"]` root, plus the fully bracketed `globalThis["Math"]["exp"]` / `globalThis["Math"]["log"]` slice in TS, `.js`, `.jsx`, and `.tsx` input, and the mixed-bracket `globalThis.Math["expm1"]` / `globalThis.Math["log1p"]` spelling now also has browser-harness/browser-bundle smoke coverage in TS, `.js`, `.jsx`, and `.tsx` input, and the fully bracketed `globalThis["Math"]["expm1"]` / `globalThis["Math"]["log1p"]` spellings now also have browser-harness/browser-bundle smoke coverage in TS, `.js`, `.jsx`, and `.tsx` input, and the mixed-bracket `globalThis.Math["exp"]` / `globalThis.Math["log"]` spellings now also have browser-harness/browser-bundle smoke coverage in TS, `.js`, `.jsx`, and `.tsx` input, `Math.atan2` now also supports the zero-numerator / non-negative-denominator literal slice, including const numeric alias chains and the supported transparent parenthesized, type-assertion, satisfies, and TS as-const wrapper slices, on the supported checker/codegen/build path, and the supported `globalThis.Math.atan2` / `globalThis["Math"].atan2` / `globalThis["Math"]["atan2"]` paths now also share that same slice in the current repository snapshot, the supported `globalThis.Math.*` member-access root now follows the same lowering path as the corresponding bare `Math.*` forms for the already-supported Math builtin slices, and the build corpus now also mirrors that literal slice in JSX and TSX input on both the Deno and browser surfaces, `Math.asin` / `Math.acos` / `Math.atan` now also have exact zero/one identity folds in the current repo, and browser-requested `run` / `test` smoke now also covers the exact `Math.asin` / `Math.acos` / `Math.atan` identity slice in TS and `.js` input with JSON-output coverage, `Math.asinh` / `Math.acosh` / `Math.atanh` now also have exact zero/one identity folds in the current repo, and browser-harness/browser-bundle smoke now also covers the exact `Math.asinh` / `Math.acosh` / `Math.atanh` identity slice in TS and `.js` input; `Math.sin` / `Math.cos` / `Math.tan` now also have exact zero identity folds in the current repo, and browser bundle smoke now also covers the exact zero-identity slice in TS, `.js`, `.jsx`, and `.tsx` input, including the bracketed `globalThis["Math"]` root, and browser-harness smoke now also covers the exact `Math.tan` zero-identity slice in TS and `.js` input, and browser-harness `run` / `test` smoke now also covers the `Math.floor` / `Math.trunc` / `Math.ceil` const-alias-chain slice in TS and `.js` input; browser-harness JSON-output coverage now also pins the exact `Math.sin` / `Math.cos` zero-identity slice and the exact `Math.tan` zero-identity slice in TS and `.js` input; browser bundle smoke now also covers the `Math.floor` / `Math.trunc` / `Math.ceil` const-alias-chain slice in TS and `.js` input; the build/check corpus also pins explicit `E5506` rejection coverage for representative unsupported `Math` members such as `Math.exp` and `Math.log` across Deno and browser `.js` / `.ts` inputs, and standalone `run` / `test` plus browser-harness smoke still pin representative `Math.exp` / `Math.log` rejections on the JS-input math gate path and browser-targeted `check` / `build` smoke in `.jsx` / `.tsx` input, and browser-harness `run` / `test` rejection coverage now also mirrors unsupported `Math.*` member-call gating in `.jsx` / `.tsx` input with canonical `E5506` coverage. The browser-bundle smoke now also covers the `Math.max` / `Math.min` const-alias-chain slices in TS and `.js` input. Direct build smoke now also covers the fully bracketed `globalThis["Math"]["atan2"]` zero-numerator / non-negative-denominator slice in Deno and browser `.js` / `.ts` inputs. Direct build smoke now also covers the single-quoted bracketed `globalThis['Math']['atan2']` slice in Deno and browser `.js` / `.ts` inputs. Direct codegen smoke now also covers the bracketed `globalThis["Math"]["floor"]` / `globalThis["Math"]["trunc"]` / `globalThis["Math"]["ceil"]` slices and the bracketed `globalThis["Math"]["pow"]` one-base identity slice. The limited literal-array `for...of` and `for await...of` slices now share the same lowering path on literal arrays with literal elements, const numeric alias elements, const string alias elements, and simple variable bindings, including browser-harness smoke coverage in TS and `.js` input, browser build smoke now also mirrors the const string-alias slice for both loops on the browser API surface in TS and `.js` input, direct build / browser-bundle smoke now also cover the const-alias-chain slice for both loops in TS and `.js` input, and browser bundle smoke now also covers the `for...of` `satisfies` wrapper slice in TS, `.js`, `.jsx`, and `.tsx` input. Browser-requested run/test smoke now also covers the spread-element slice in JS input. Browser-harness/browser-bundle JSON success paths for the `globalThis.Math.exp2` root slice now also assert empty `errors` / `warnings` arrays, keeping the deterministic JSON envelope explicit on that supported slice.
- Browser-harness/browser-bundle smoke now also covers the `globalThis.Math` / bracketed `globalThis["Math"]` core math suite in JSX and TSX input, extending the existing TS and `.js` source-class coverage for that supported math slice.
- Direct build smoke now also covers the mixed-bracket `globalThis.Math["log2"]` / `globalThis.Math["log10"]` slice in Deno and browser `.js` / `.ts` inputs, keeping the build-path evidence aligned with the existing browser-harness/browser-bundle coverage.
- Optimization evidence: real `fast`, `release`, and `release-advanced` optimization slices; deterministic PGO profile validation; version-pinned benchmark fixtures.
- Math round coverage now also includes the `globalThis.Math.round` root slice in browser-harness/browser-bundle smoke for TS, `.js`, `.jsx`, and `.tsx` input, alongside the existing bracketed-root `Math.round` coverage.
- Library-export metadata: top-level function exports now get trivial literal-return inference in the bounded slice, and top-level `const` function-expression / arrow-function bindings now also carry that inferred signature through export aliases and deterministic sequence/conditional/decorated wrappers while preserving the conservative `unknown` fallback outside the slice; default-export function declarations now also participate in that bounded library-export slice, including transparent sequence/conditional/decorated wrappers, and async function declarations / expressions now keep bounded `Promise<...>` signatures through transparent `await` wrappers around the returned literal/body slice; default-export async function expressions now also keep their bounded `Promise<...>` signatures through transparent decorated wrappers, and transparent `ChainExpression` wrappers now also peel through the function-body literal-return path so the same bounded export signature survives that transparent wrapper too. The end-to-end `build --lib` alias-rejection coverage now also spans JS, TS, JSX, and TSX input, keeping the library-export regression matrix aligned with the bounded inference summary above.
- Verification evidence: Lean proof tree and proof-backed status for the published boundary only.
- Browser runtime summary fallback evidence: the CLI now also has TS-input browser-harness smoke for stdout-authoritative fallback when the configured summary file is missing, unreadable, unparseable, blank, or shape-invalid, and a dedicated JS-input regression file now mirrors the unparseable-summary fallback on the browser-requested run/test paths; that JS-input file now also covers missing-summary fallback plus invalid `testsFailed` string and numeric values and invalid `args` / `tests` array-shape fallbacks on the browser-requested summary matrix, and the dedicated browser summary fallback regressions now also pin invalid `tests` array item types on the JSON `test` path across JS, TS, JSX, and TSX input. The TS, JSX, and TSX summary-fallback regressions now also pin invalid numeric `testsFailed` values on both JSON `test` and human `run` paths, keeping the browser-requested summary matrix aligned across the whole source-class set. The browser-requested human-output `run` path now also covers empty summary-file fallback across JS, TS, JSX, and TSX input, and the browser-requested JSON `run` path now also covers that empty-summary fallback across JS, TS, JSX, and TSX input. The JS-input JSON `run` path now also covers invalid `args` and invalid `tests` array-item summary payloads, keeping the run-mode parser matrix aligned with the existing JSON `test` regression shape.

## Planning consequence

Completed historical phase documents were removed from active planning. The active roadmap now starts at Phase 11 and focuses on remaining spec-owned work: language semantic closure, runtime/host expansion, package compatibility breadth, performance promotion, and verification/schema widening.

## Non-negotiable guardrails

Future phases must preserve:

- AOT-only guest-language compilation.
- Pure Rust implementation contract.
- No tracing/background GC.
- Sandbox-first honesty.
- Deterministic machine contracts.
- Public availability discipline from `specs/19-feature-maturity.md`.
- Proof-backed claim discipline from `proofs/BOUNDARY.md`.
- The deterministic environment snapshot helper family now also carries an explicit `env_snapshot_object_value` alias alongside the existing object/json snapshot helpers in the Deno, Node, and runtime projections, and the low-level Deno env view now also exposes an `env_snapshot()` object alias for the same helper family; the threaded-topology helper family now also carries explicit `thread_topology_snapshot_object_value` aliases alongside the existing value/json helpers in the runtime and browser snapshot/report projections; the runtime host-state also now mirrors that thread-topology snapshot naming with plain `snapshot_object_value` / `snapshot_json_value` aliases, the runtime execution context now also mirrors the same deterministic env snapshot view with `snapshot()` / `snapshot_object_value` / `snapshot_json_value` aliases and matching `env_has()` / `has()` convenience checks, and the browser/runtime instance and shutdown-report snapshot helpers now also expose bare `thread_topology_snapshot()` clone aliases. The browser navigator baseline now also exposes deterministic snapshot helpers and JSON-ready aliases for its metadata view, keeping the browser helper vocabulary aligned with the rest of the deterministic snapshot family. The browser runtime topology regression coverage now also pins the direct `snapshot_value()` alias alongside the existing JSON/object alias checks so the helper-family naming stays aligned across the browser snapshot/report side, and the cloned shutdown-report JSON/value alias path is now covered too. The Deno runtime projection now also exposes the same `has()` convenience alias alongside `env_has()`, and the instance and shutdown-report helpers now also expose bare `snapshot()` clone aliases so the threaded helper family stays aligned with the generic snapshot naming.

- Runtime built-in math evidence now also includes browser bundle and browser-harness coverage for the empty-argument `Math.hypot()` identity slice in TS, `.js`, `.jsx`, and `.tsx` input, standalone `run --output json` coverage in JS input, and the browser-targeted JSON smoke for that slice now also asserts empty `errors` arrays.
- The runtime host-state threaded-topology snapshot helper coverage now also pins the `snapshot_object_value` / `snapshot_json_value` aliases alongside the existing `snapshot_value` family in unit tests.
- Browser-targeted `check` now also rejects non-literal `Object.keys(...)` iterator sources on the canonical `E5506` path in JS, JSX, TS, and TSX input, and the browser-targeted JSON check lane now also mirrors that same rejection in JS, TS, JSX, and TSX input, keeping the browser static-analysis matrix aligned with the existing `build --bundle` rejection evidence. The same non-literal `Object.keys(...)` rejection now also covers inherited browser-configured `check` / `build --bundle` smoke in the JS/TS/JSX/TSX matrix.
