# Current Implementation Baseline

This file records the planning baseline for the active continuation roadmap. It is a current-state summary, not an active checklist and not an availability matrix.

## Evidence used for this cleanup

- `cargo run -q -p kali_cli --bin kali -- --help` exposes the current public command set.
- The workspace contains Rust crates for CLI, lexer/parser, AST/HIR/MIR/LIR, type checking, codegen, runtime, sandbox/effects, package management, optimization, Deno/Web/Node API projections, embedding/C ABI, formatting, linting, and bindings.
- The spec-owned availability and current-state nuance remain in [`../specs/19-feature-maturity.md`](../specs/19-feature-maturity.md).
- The proof-backed boundary remains owned by [`../proofs/BOUNDARY.md`](../proofs/BOUNDARY.md).

## Live surface at a glance

The checked-in repository already includes:

- CLI commands: `doctor`, `init`, `install`, `fmt`, `lint`, `check`, `build`, `run`, `test`, `effects`, `package-effects`, and `package-audit`.
- Build/artifact lanes: executable builds, browser bundles, IR validation, library artifacts, C ABI artifacts, component artifacts, metadata, deterministic sidecars, and browser-bundle integer-like `Object.keys` / `Object.values` iteration coverage.
- Browser bundle/browser-harness smoke now also covers the corresponding string-primitive enumeration slices for `Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` across JS, TS, JSX, and TSX input, including the mixed/bracketed `globalThis.Object["keys"]`, `globalThis["Object"].entries`, and `globalThis["Object"]["values"]` spellings. Browser-targeted check/build smoke now also covers the same string-primitive enumeration slices in JS, TS, JSX, and TSX input.
- Runtime object semantics: direct `Object.fromEntries([...])` operands now also participate in the static `Object.hasOwn` / `Object.prototype.hasOwnProperty.call` helper slice in JS input, and the browser-targeted `check` / `build --bundle` smoke, browser bundle smoke, and browser-requested `run` / `test` harness now also cover that same helper slice when the object operand is sourced from `Object.fromEntries([...])`, including explicit browser bundle/harness smoke coverage for the `Object.hasOwn` helper slice on JS, TS, JSX, and TSX input; standalone `check` / `run` / `test` smoke now also covers that same fromEntries-sourced helper slice in JS, TS, JSX, and TSX input; static `Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` lowering now also accepts `Object.fromEntries([...])` operands in the standalone run/test JS, TS, JSX, and TSX smoke path; `for...of` array lowering now also accepts spread elements whose targets resolve to supported `Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` slices in JS input, and now also lowers string-primitive enumeration slices for those helpers in the standalone `run` / `test` JS, TS, JSX, and TSX smoke path; the browser-targeted `check` / `build --bundle` smoke now also covers spread-of-`Object.values(...)` iterator slices in both `for...of` and `for await...of` forms on JS input plus spread-of-`Object.keys(...)` / `Object.entries(...)` iterator slices over `Object.fromEntries([...])` operands in JS input and const-bound `Object.keys(...)` iterator slices in `.js`, `.ts`, `.jsx`, and `.tsx` browser-targeted smoke plus the same const-bound `Object.keys(...)` slice in browser-requested `run` / `test` harness coverage; standalone `run` smoke now also covers the `for await` spread-of-`Object.values(...)` / `Object.keys(...)` / `Object.entries(...)` slices over `Object.fromEntries([...])` operands in JS and TS input with output and JSON-output coverage, and browser-requested `run` / `test` harness coverage now also exercises the same `for await` spread slices over `Object.fromEntries([...])` operands in JS input with JSON-output coverage, and browser-requested run/test browser-harness coverage now also exercises the same spread-of-`Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` iterator slices over `Object.fromEntries([...])` operands in JSX and TSX input with JSON-output coverage, and browser bundle/browser-harness coverage now also includes the matching spread-of-`Object.values(...)` slice over `Object.fromEntries([...])` operands in JSX and TSX input with JSON-output coverage, and browser-targeted build/check smoke now also covers that same spread-of-`Object.values(...)` slice in TS, JSX, and TSX input, and the current repository snapshot also accepts `Object.freeze`-wrapped static object targets for the same helper slices, with browser-targeted check/build and browser-requested run/test/browser-bundle smoke now also pin the frozen `Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` helper slices over `Object.fromEntries([...])` operands, and codegen unit coverage now also pinning the frozen `Object.hasOwn` helper slice, with browser bundle/browser-harness smoke now also covering the frozen `Object.hasOwn(...)` helper slice over `Object.fromEntries([...])` operands in JS input, and browser-requested run/test browser-harness smoke now also exercises that same frozen `Object.hasOwn` helper slice in JSX and TSX input; browser-targeted check smoke now also covers the basic `for await...of` array slice in JSX and TSX input, and the browser-targeted check/build smoke now also covers the matching `Object.keys(...)` / `Object.entries(...)` spread slices over `Object.fromEntries([...])` operands in TS, JSX, and TSX input; the `for...of` maturity row in `specs/19-feature-maturity.md` now mirrors that `Object.values(...)` slice explicitly. Browser-targeted check smoke now also covers the `Reflect.ownKeys` slice in JSX and TSX input. Browser bundle smoke now also covers the `Object.values(...)` spread-of-`Object.fromEntries([...])` slice in JS, TS, JSX, and TSX input with JSON-output coverage. Browser-requested run/test browser-harness coverage now also exercises `Promise.allSettled` across JS, TS, JSX, and TSX input with JSON-output coverage. Codegen coverage now also pins the `for await` spread-of-`Object.values(...)` / `Object.keys(...)` / `Object.entries(...)` slice over `Object.fromEntries([...])` operands so the async spread lowering stays aligned with the runtime smoke lanes. `Object.is` primitive-literal build smoke now also covers numeric, boolean, string, `null`, `Infinity`, `NaN`, `-Infinity`, and simple decimal BigInt slices across the Deno and browser build matrix, and the same-reference object/array alias-chain slice now also folds deterministically there too, including `Object.freeze`-wrapped same-reference aliases. Standalone run/test smoke now also covers that same primitive-literal `Object.is` slice in JS input with JSON-output coverage. The browser bundle/runtime spread-regression lanes now also cover frozen `Object.fromEntries(...)` operands for the shared `Object.keys(...)` / `Object.values(...)` / `Object.entries(...)` spread slice, and the supported iterator smoke now also covers spread-of-`Reflect.ownKeys(...)` slices over static object-literal operands in the JS/TS/JSX/TSX matrix, and standalone run smoke now also covers the matching `Reflect.ownKeys(...)` spread slice over static object-literal operands in JS and TS input with JSON-output coverage, while browser-requested run/test browser-harness coverage now also spans the wrapped object-enumeration slice in JSX and TSX input with JSON-output coverage. Browser bundle/browser-harness smoke now also cover spread-of-`Reflect.ownKeys(...)` slices over static object-literal operands in JS, TS, JSX, and TSX input, and browser-requested run/test browser-harness coverage now also exercises direct `for await...of` consumption of `Reflect.ownKeys(...)` arrays over static object-literal operands in JS, TS, JSX, and TSX input with JSON-output coverage. Browser bundle/browser-harness smoke now also cover spread-of-`Reflect.ownKeys(...)` slices over static object-literal operands in JS, TS, JSX, and TSX input.
- Browser-requested run/test browser-harness coverage now also exercises the frozen `Object.values(...)` spread slice over `Object.fromEntries([...])` operands in JS, TS, JSX, and TSX input with JSON-output coverage, and the same browser-requested harness coverage now also exercises the corresponding `Object.keys(...)` / `Object.entries(...)` spread slices over `Object.fromEntries([...])` operands in JS and TS input with JSON-output coverage, plus the frozen `Object.keys(...)` / `Object.entries(...)` spread slices over `Object.fromEntries([...])` operands in JS, TS, JSX, and TSX input with JSON-output coverage. Standalone run/test smoke now also covers the frozen `Object.freeze(Object.fromEntries(...))` spread slice in JS and TS input with JSON-output coverage. Browser-requested run/test browser-harness smoke now also exercises the primitive-literal `Object.is` slice across JS, TS, JSX, and TSX input with JSON-output coverage, and now also exercises the same-reference alias-chain slice for `Object.is` across the same matrix with JSON-output coverage.
- Browser runtime summary parsing now treats unexpected top-level keys as shape-invalid and falls back to stdout, keeping the browser harness summary contract deterministic when the file shape drifts; browser-requested JS run/test smoke now also covers that unexpected-top-level-key fallback path end-to-end, and the dedicated TS/JSX/TSX browser-harness fallback suites now mirror that same drift path in JSON test coverage.
- Runtime math semantics: `Math.pow` now also folds the zero-base / positive-integer-exponent slice on the supported checker/codegen path, and standalone `test` smoke now also covers that slice in JS input with JSON-output coverage; browser-harnessed `run` / `test` smoke now also covers the positive-integer-exponent alias-chain slice across JS, TS, JSX, and TSX input with JSON-output coverage; the direct exponentiation operator `**` now also lowers through the same integer-exponent path, and standalone `run` smoke now also pins a simple `2 ** 3` JS slice; `Math.exp2` now also folds exact non-negative integer literals within the current integer-fold range, including alias chains, on the supported checker/codegen path; the exact statically-known ±1-base negative-exponent `Math.pow` slice now also folds in the codegen path, including alias-chain inputs for both `1` and `-1` bases; `Math.imul` now also accepts omitted trailing operands by folding zero-/one-argument slices to `0` on the supported checker/codegen path, standalone `run` / `test` smoke now also pins that omitted-operand slice in JS input with JSON-output coverage, and browser bundle plus browser-requested run/test harness smoke now also covers that same omitted-operand slice across JS, TS, JSX, and TSX input with JSON-output coverage; `Math.clz32` now also constant-folds statically-known numeric literal operands, including const alias chains and a non-integer literal slice such as `1.6`, on the supported checker/codegen path, and the zero-argument `Math.clz32()` identity slice is now also pinned on the supported codegen/build path; browser-requested run/test browser-harness smoke now also covers the mixed-bracket `globalThis.Math["floor"]` / `globalThis.Math["trunc"]` / `globalThis.Math["ceil"]` forms in JSX and TSX input with JSON-output coverage.
- Runtime assignment semantics: nullish assignment and logical `&&=` / `||=` now also accept wrapped mutable-local targets in direct JS/TS smoke, browser-harness smoke, and browser-bundle smoke, with dedicated browser bundle build coverage now pinning that wrapped-target slice across JS, TS, JSX, and TSX input; the supported arithmetic compound-assignment slice (`+=`, `-=`, `*=`, `/=`, `%=`, `**=`) now also accepts the same wrapped mutable-local targets in direct JS/TS smoke, browser-harness smoke, and browser-bundle smoke; the current iteration also adds explicit `**=` coverage to that smoke matrix.
- Execution/reporting lanes: `run`, `test`, `test --coverage`, public source-graph effects, package effects, package audit, JSON envelopes, schema-v1 payload validation, top-level diagnostic `file` mirror coverage plus mirror-mismatch rejection, suggested-fix text edit file-mirror validation plus source-location extension rejection plus reversed/overlapping edit-range rejection plus unexpected text-edit keys, nested label and related-info source-span extension rejection, `timings` entries now also reject negative or non-finite elapsed values and duplicate phase labels, effects payload `dynamicReasons` now also enforce canonical deduplication/lexical ordering and the `dynamicEffects=false` empty-list rule, and CLI envelope artifacts now also reject negative or fractional byte sizes, with dedicated regression coverage for the end.file mirror path; CLI envelopes now also reject noncanonical artifact roles and out-of-order artifact arrays alongside the existing artifact-key validation.
- Library-export analysis now also rejects generator function declarations and expressions through the canonical `E5506` gate instead of misclassifying them as statically known library exports, and `build --lib` export collection now also resolves simple named and `export *` re-exports across the linked source graph when the target surface is statically known; default async function declarations now also keep their bounded `Promise<...>` inference through transparent `await` wrapper forms across JS, TS, JSX, and TSX input.
- Host/API slices: default standalone Deno-oriented APIs, browser-targeted `check` / `build --bundle`, browser-harness-assisted execution where configured, and documented Node-compatible analysis/build/runtime slices; late host-gating coverage now also mirrors the Deno `connect` / `listen` / `serve` / `Command` rejection slice in JSX and TSX build smoke, and Node process-control smoke now also mirrors `.jsx` and `.tsx` input on the supported `process.exit` / bracketed `process.*` path.
- Threaded runtime budgets: browser-requested `run` / `test` browser-harness coverage now also accepts positive `--max-threads` overrides when `--wasm-threads` is active in JS/TS/JSX/TSX input with JSON-output coverage.
- Package evidence: registry/raw-URL/install flows, package-corpus probes by context/rung, registry-analysis commands, and negative evidence for unsupported native/binary/bootstrap-heavy or published-bin-entrypoint cases.
- Embedding/evidence surfaces: public embedding artifacts and bindings metadata, optimization modes, deterministic PGO profile validation, benchmark fixtures, and Lean proof infrastructure for the published boundary.

## Planning consequence

The active plan starts from remaining spec gaps only. It does not preserve line-by-line progress notes for implemented slices; those belong in tests, specs, maturity current-state notes, and proof-boundary documents.

## Non-negotiable guardrails

Future phases must preserve:

- AOT-only guest-language compilation.
- Pure Rust implementation contract.
- No tracing/background GC.
- Sandbox-first honesty.
- Deterministic machine contracts.
- Public availability discipline from `specs/19-feature-maturity.md`.
- Proof-backed claim discipline from `proofs/BOUNDARY.md`.
