# Current Implementation Baseline

This file records the planning baseline for the active continuation roadmap. It is a current-state summary, not an active checklist and not an availability matrix.

## Repository evidence used for this cleanup

- `cargo run -q -p kali_cli --bin kali -- --help` exposes the current public command set.
- The workspace contains implementation crates for CLI, frontend, AST/HIR/MIR/LIR, codegen, runtime, sandbox/effects, package management, optimization, host APIs, embedding/C ABI, and bindings.
- The proof tree exists under `proofs/`, and `proofs/BOUNDARY.md` states that Kali is proof-backed for the published boundary.
- Canonical availability and current-state nuance remain in `specs/19-feature-maturity.md`.

## Live surface at a glance

The current repository has already implemented the historical MVP and several later surfaces:

- CLI commands: `doctor`, `init`, `install`, `fmt`, `lint`, `check`, `build`, `run`, `test`, `effects`, `package-effects`, and `package-audit`.
- Build modes and artifacts: executable builds, browser bundles, `--validate-ir`, `--lib`, `--capi`, `--component`, and deterministic artifact metadata/sidecars.
- Execution/reporting: `run`, `test`, `test --coverage`, stable JSON envelopes, public effect reports, registry effect/audit reports, and schema-v1 payload validation.
- Host/API slices: Deno-oriented default surface, including the read-only `Deno.cwd` / `Deno.pid` queries in the current standalone snapshot while `Deno.chdir` remains gated; documented Node subsets (including the read-only `process.pid` query and read-only `process.cwd` query on the Node surface); browser-targeted `check` / `build --bundle`; and browser-harness-assisted `run` / `test` paths where configured; the browser-harness dynamic-import smoke now also covers explicit `.js` file-specifier targets in JS input, and raw backtick template-literal specifiers with statically evaluable interpolations now also have browser-bundle smoke coverage in TS and `.js` input, while browser-targeted `check` / `build --bundle` now also pin the non-literal `import(expr)` gate in `.js` input with matching JSON-output rejection coverage, the non-browser literal-object `Object.hasOwn` / `Object.prototype.hasOwnProperty.call` slice now folds static object literals and const alias chains in JS input, browser-targeted static-object-call slices are now supported on the documented browser command set, dynamic-object cases remain gated, and the late-process-control smoke now includes the bracketed `Deno.chdir` spellings while the entry-body codegen stack cleanup keeps the current void-entry validations stable.
- Iterator slices: the shared literal-array `for...of` / `for await...of` lowering path now covers literal arrays with literal elements, const numeric alias elements, const string alias elements, and simple variable bindings in TS and `.js` input, with browser-harness smoke on the supported run/test paths; transparent parenthesized/type-assertion/satisfies/chain wrappers and TS `as const` wrappers around those supported array iterables/elements are now also covered.
- Package evidence: default standalone, browser, Node, Deno/JSR, registry-analysis, and published-bin-entrypoint contrast coverage.
- Runtime built-in math evidence: `Math.max`, `Math.min`, `Math.abs`, `Math.sign`, `Math.imul`, `Math.round`, `Math.clz32`, `Math.trunc`, `Math.ceil`, `Math.floor`, `Math.sqrt`, `Math.cbrt`, `Math.hypot`, `Math.pow`, and the new `Math.log2` exact power-of-two literal path are covered by checker/codegen/runtime smoke slices in the current repo, with browser bundle coverage on the documented subset; `Math.abs` now also constant-folds statically-known integer-valued numeric literals, and `Math.sign` now also constant-folds statically-known numeric literals, including const numeric alias-chain resolution, the statically-known `abs` / `sign` / `floor` paths now also resolve through const numeric alias chains, `Math.imul` now also constant-folds statically-known integer literal operands, `Math.clz32` now also constant-folds statically-known integer literal operands, the statically-known `sqrt` / `cbrt` / `log2` / `log10` paths now also resolve through const numeric alias chains in the checker/runtime smoke path, `Math.hypot` now also has an exact integer-literal perfect-square-sum slice, and the `Math.pow` integer-exponent path now also honors const numeric alias chains for the supported integer-exponent smoke path and its canonical rejection cases. Browser bundle smoke now also covers the same positive-integer-exponent alias-chain slice in TS and `.js` input. Direct `build` / `run` smoke now also pin the `Math.log2` / `Math.log10` const alias-chain cases in the Deno `.js` and `.ts` inputs, and browser-harnessed `run` / `test` smoke now also covers the `Math.log2` / `Math.log10` literal slice in TS and `.js` input, plus the exact `Math.exp` / `Math.log` identity folds in TS and `.js` input. The `Math.sqrt` / `Math.cbrt` literal slices now also have direct build smoke coverage in the Deno and browser `.js` / `.ts` inputs, and browser bundle smoke now also covers the `Math.sqrt` / `Math.cbrt` literal slices in TS and `.js` inputs; `Math.ceil` / `Math.trunc` now also have direct build smoke coverage on const alias chains in the Deno and browser `.js` / `.ts` inputs. `Math.exp` / `Math.log` now also have exact zero/one identity folds in the current repo, `Math.expm1` / `Math.log1p` now also have exact zero identity folds, including const numeric alias chains, in the current repo, `Math.asin` / `Math.acos` / `Math.atan` now also have exact zero/one identity folds in the current repo, `Math.atan2` now also supports the zero-numerator / non-negative-denominator literal slice, including const numeric alias chains, on the supported checker/codegen/build path, `Math.asinh` / `Math.acosh` / `Math.atanh` now also have exact zero/one identity folds in the current repo, while broader non-identity calls remain rejected with the canonical `E5506` gate; `Math.sin` / `Math.cos` / `Math.tan` now also have exact zero identity folds in the current repo; the build/check corpus also pins explicit `E5506` rejection coverage for representative unsupported `Math` members such as `Math.exp` and `Math.log` across Deno and browser `.js` / `.ts` inputs, and standalone `run` / `test` plus browser-harness smoke still pin representative `Math.exp` / `Math.log` rejections on the JS-input math gate path and browser-targeted `check` / `build` smoke in `.jsx` / `.tsx` input. The limited literal-array `for...of` and `for await...of` slices now share the same lowering path on literal arrays with literal elements, const numeric alias elements, const string alias elements, and simple variable bindings, including browser-harness smoke coverage in TS and `.js` input.
- Optimization evidence: real `fast`, `release`, and `release-advanced` optimization slices; deterministic PGO profile validation; version-pinned benchmark fixtures.
- Runtime built-in math evidence: `Math.max`, `Math.min`, `Math.abs`, `Math.sign`, `Math.imul`, `Math.round`, `Math.clz32`, `Math.trunc`, `Math.ceil`, and `Math.floor` are covered by the current smoke corpus, and `Math.abs` / `Math.sign` / `Math.round` / `Math.floor` / `Math.trunc` / `Math.ceil` now also constant-fold statically-known numeric literals, while `Math.imul` / `Math.clz32` now also constant-fold statically-known integer literal operands, including the documented alias-chain slices for the supported subcases; `Math.sin` / `Math.cos` / `Math.tan` now also constant-fold exact zero literals, `Math.expm1` / `Math.log1p` now also constant-fold exact zero literals, `Math.atan2` now also supports the zero-numerator / non-negative-denominator literal slice on the supported checker/codegen/build path, `Math.asinh` / `Math.acosh` / `Math.atanh` now also constant-fold exact zero/one literals, and the supported `Math.max` lowering path now also accepts `globalThis.Math.max` in JS input.
- Verification evidence: Lean proof tree and proof-backed status for the published boundary only.

## Planning consequence

Completed historical phase documents were removed from active planning. The active roadmap now starts at Phase 11 and focuses on remaining spec-owned work: language semantic closure, runtime/host expansion, package compatibility breadth, performance promotion, and verification/schema widening.

## Non-negotiable guardrails

Future phases must preserve:

- AOT-only guest-language compilation.
- Pure Rust implementation contract.
- No tracing/background GC.
- Sandbox-first honesty.
- Deterministic machine contracts.
- Public availability discipline from `specs/19-feature-maturity.md`.
- Proof-backed claim discipline from `proofs/BOUNDARY.md`.
