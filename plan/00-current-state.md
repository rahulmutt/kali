# Current Implementation Baseline

This file records the planning baseline for the active continuation roadmap. It is a current-state summary, not an active checklist and not an availability matrix.

## Repository evidence used for this cleanup

- `cargo run -q -p kali_cli --bin kali -- --help` exposes the current public command set.
- The workspace contains implementation crates for CLI, frontend, AST/HIR/MIR/LIR, codegen, runtime, sandbox/effects, package management, optimization, host APIs, embedding/C ABI, and bindings.
- The proof tree exists under `proofs/`, and `proofs/BOUNDARY.md` states that Kali is proof-backed for the published boundary.
- Canonical availability and current-state nuance remain in `specs/19-feature-maturity.md`.

## Live surface at a glance

The current repository has already implemented the historical MVP and several later surfaces:

- CLI commands: `doctor`, `init`, `install`, `fmt`, `lint`, `check`, `build`, `run`, `test`, `effects`, `package-effects`, and `package-audit`.
- Build modes and artifacts: executable builds, browser bundles, `--validate-ir`, `--lib`, `--capi`, `--component`, and deterministic artifact metadata/sidecars.
- Execution/reporting: `run`, `test`, `test --coverage`, stable JSON envelopes, public effect reports, registry effect/audit reports, and schema-v1 payload validation.
- Host/API slices: Deno-oriented default surface, including the read-only `Deno.cwd` / `Deno.pid` queries in the current standalone snapshot while `Deno.chdir` and `Deno.exit` are now also available there; the standalone surface now also accepts the bracketed `Deno["cwd"]` / `globalThis["Deno"]["cwd"]` spellings, plus the bracketed `Deno["chdir"]` / `Deno["exit"]` spellings, and the Deno runtime projection now also exposes a deterministic environment snapshot helper alongside the mutable env view; `Deno.env.get` / `Deno.env.has` now share the same read-only env slice on the default standalone surface, the default standalone surface now also accepts `Deno.env.set` / `Deno.env.delete` plus the bracketed `Deno["env"]["set"]` / `Deno["env"]["delete"]` forms on that same env view, and the standalone run/test smoke now also mirrors the bracketed `Deno["env"]["has"]` / `globalThis["Deno"]["env"]["has"]` forms while `build_tests.rs` now also mirrors those same bracketed `has` forms in the executable build path and `.js` input; documented Node subsets (including the read-only `process.pid` query, read-only `process.cwd` query, `process.chdir` cwd-mutation path, the `process.env.KEY = ...` / `delete process.env.KEY` property-mutation slice on the Node surface, the bracketed `process["env"]["KEY"]` / `delete process["env"]["KEY"]` forms, and the nested bracketed `process["env"]["KEY"]` / `delete process["env"]["KEY"]` spellings under `globalThis["process"]`, plus the bracketed `process["cwd"]` / `process["chdir"]` / `process["exit"]` forms and their `globalThis.process[...]` spellings); browser-targeted `check` / `build --bundle`; and browser-harness-assisted `run` / `test` paths where configured; the browser-harness dynamic-import smoke now also covers explicit `.js` file-specifier targets in JS input, and raw backtick template-literal specifiers with statically evaluable interpolations now also have browser-bundle smoke coverage in TS and `.js` input, while browser-targeted `check` / `build --bundle` now also pin the non-literal `import(expr)` gate in `.js` input with matching JSON-output rejection coverage, the non-browser literal-object `Object.hasOwn` / `Object.prototype.hasOwnProperty.call` slice now folds static object literals and const alias chains in JS input, browser-targeted static-object-call slices are now supported on the documented browser command set, browser-bundle smoke now also covers the bracketed `globalThis["Object"]["hasOwn"]` / `globalThis["Object"]["prototype"]["hasOwnProperty"]["call"]` forms in TS and `.js` input, and the supported static-object slice also accepts `as const` wrappers around the object operand in JS input, dynamic-object cases remain gated, the static numeric-literal `Object.is` slice now also folds deterministically on the supported standalone/browser smoke paths, including the bracketed `globalThis["Object"]["is"]` spelling, browser bundle smoke now also covers that same static numeric-literal slice in TS and `.js` input, and browser-harness run/test smoke now also mirrors that same slice in TS and `.js` input, the default standalone run smoke now also exercises the `Deno.chdir` path, and the browser-targeted late-process-control smoke still includes the bracketed `Deno.chdir` rejection spellings while the entry-body codegen stack cleanup keeps the current void-entry validations stable. The default standalone late-compatibility smoke now also pins property-mutation rejection for `process.env.KEY = ...` / `delete process.env.KEY`-style source forms, including the bracketed and `globalThis.process[...]` spellings, and browser build smoke now also covers the nested bracketed `globalThis["process"]["env"]["KEY"]` mutation spellings on the browser API surface.
- Iterator slices: the shared literal-array `for...of` / `for await...of` lowering path now covers literal arrays with literal elements, const numeric alias elements, const string alias elements, and simple variable bindings in TS and `.js` input, with browser-harness smoke on the supported run/test paths; transparent parenthesized/type-assertion/satisfies/chain wrappers and TS `as const` wrappers around those supported array iterables/elements are now also covered, with browser-harness `run` / `test` now also exercising the TS `as const` and `satisfies` wrapper slices, browser-harness `test` now also mirrors the `for...of` const-alias-chain slice in TS input, and `for await...of` parenthesized binding wrappers around the loop target are now also covered in browser-harness `run` / `test` smoke on both TS and `.js` input, while browser build smoke now also mirrors the const string-alias slice for both loops on the browser API surface in TS and `.js` input, browser bundle smoke now also covers the parenthesized-binding wrapper slice for both loops in TS and `.js` input, browser-harness `run` / `test` now also covers the direct literal-array `for await...of` slice in both TS and `.js` input, browser-harness `run` / `test` now also exercises the `for...of` `as const` wrapper slice in TS input, browser build smoke now also mirrors that `for...of` `as const` wrapper slice on the browser API surface in TS input, browser bundle smoke now also covers the `for await...of` const-string-alias and parenthesized-const-alias slices in both TS and `.js` input, browser build smoke now also mirrors the `for await...of` `as const` and `satisfies` wrapper slices in TS input on both the Deno and browser surfaces, and the browser analysis/bundle JSON success paths now also pin empty `errors` arrays for representative `for...of` and `for await...of` smoke slices.
- Compound-assignment slices: arithmetic compound assignment expressions (`+=`, `-=`, `*=`, `/=`, `%=`, `**=`) now also lower for mutable local bindings with runtime local storage, so the standalone `run` / `json run` JS-input smoke path now preserves simple local update cases even when the right-hand side is a runtime value; mutable `let` / `var` bindings now also avoid the direct codegen binding cache's stale const-folding path, so mutable slots stay distinct from const-only aliases; transparent parenthesized/type-assertion/satisfies wrapper targets around those mutable locals are now also accepted in the current codegen lowering path, while non-identifier compound-assignment targets and immutable bindings now reject explicitly instead of silently lowering, and the remaining broader operand shapes remain follow-up work.
- Nullish coalescing `??` remains covered on the supported `check` / `run` / `test` paths, and nullish assignment `??=` now also has direct TS/JS and browser-harness smoke coverage on the supported paths.
- Basic unary prefix semantics: prefix `!`, unary `-`, unary `+`, `void`, and the local-binding `++` / `--` update-expression slice are covered by runtime-smoke regression coverage in both `.ts` and `.js` inputs on the standalone `run` / `test` paths, and browser-requested `run` / `test` harness coverage now also exercises the same slice in `.ts` and `.js` input with JSON-output coverage.
- Browser-harness/browser-bundle bracketed `globalThis["Math"]` root smoke now mirrors the existing `Math.pow` alias-chain slice in TS and `.js` input, and now also covers the `Math.exp` / `Math.log` identity slice in TS and `.js` input, plus the `Math.sinh` / `Math.cosh` / `Math.tanh` zero-identity slice in TS and `.js` input, and now also covers the `Math.max` / `Math.min` / `Math.abs` / `Math.sign` / `Math.imul` / `Math.clz32` core suite in TS and `.js` input, and now also covers the bracketed `globalThis["Math"]["floor"]` / `globalThis["Math"]["trunc"]` / `globalThis["Math"]["ceil"]` slices in TS and `.js` input. The fully bracketed `globalThis["Math"]["pow"]` member-access slice now also has the same browser-harness/browser-bundle smoke coverage in TS and `.js` input.
- Package evidence: default standalone, browser, Node, Deno/JSR, registry-analysis, and published-bin-entrypoint contrast coverage.
- Runtime built-in math evidence: `Math.max`, `Math.min`, `Math.abs`, `Math.sign`, `Math.imul`, `Math.round` (including the const-alias-chain smoke slice and direct build smoke coverage on that slice), `Math.clz32`, `Math.trunc`, `Math.ceil`, `Math.floor`, `Math.sqrt`, `Math.cbrt`, `Math.hypot`, `Math.pow`, and the new `Math.log2` exact power-of-two literal path are covered by checker/codegen/runtime smoke slices in the current repo, with browser bundle coverage on the documented subset; browser-harness/browser-bundle smoke now also covers the bracketed `globalThis["Math"]` / fully bracketed `globalThis["Math"]["round"]` slice for `Math.round` identity cases in TS and `.js` input; `Math.hypot` now also has direct `build` smoke coverage on the exact perfect-square-sum slice in the Deno `.js` / `.ts` inputs, plus browser-requested `run` / `test` smoke coverage and browser bundle smoke coverage in `.js` input; `Math.abs` now also constant-folds statically-known integer-valued numeric literals, and `Math.sign` now also constant-folds statically-known numeric literals, including const numeric alias-chain resolution, the statically-known `abs` / `sign` / `floor` paths now also resolve through const numeric alias chains, `Math.imul` now also constant-folds statically-known integer literal operands, `Math.clz32` now also constant-folds statically-known numeric literal operands, including const numeric alias chains and a non-integer literal slice such as `1.6`, the statically-known `sqrt` / `cbrt` / `log2` / `log10` paths now also resolve through const numeric alias chains in the checker/runtime smoke path, `Math.hypot` now also has an exact integer-literal perfect-square-sum slice, and the `Math.pow` integer-exponent path now also honors const numeric alias chains for the supported integer-exponent smoke path and its canonical rejection cases, and its zero-exponent identity slice now also accepts non-integer base literals, with browser-harness/browser-bundle smoke now also covering that slice in TS and `.js` input. Browser bundle smoke now also covers the same positive-integer-exponent alias-chain slice in TS and `.js` input, browser-harness `run` / `test` smoke now also covers the signed-base `Math.pow` slice in JS input, and the browser-harness/browser-bundle `globalThis.Math.pow` path now also mirrors that alias-chain slice in TS and `.js` input. The fully bracketed `globalThis["Math"]["pow"]` member-access slice now also has the same browser-harness/browser-bundle smoke coverage in TS and `.js` input. The browser-harness/browser-bundle smoke also now covers the bracketed `globalThis["Math"]` root for the `Math.pow` zero-exponent non-integer-base slice in TS and `.js` input. Direct `build` smoke now also pins the `Math.log2` / `Math.log10` const alias-chain cases in the Deno `.js` and `.ts` inputs, including both human and JSON output paths; direct `run` smoke still covers the same slice, and browser-harnessed `run` / `test` smoke now also covers the `Math.log2` / `Math.log10` literal slice in TS and `.js` input, with browser-harness/browser-bundle smoke now also covering the bracketed `globalThis["Math"]` root for that slice in TS and `.js` input, plus the exact `Math.exp` / `Math.log` identity folds in TS and `.js` input, and now also mirrors the bracketed `globalThis["Math"]` root for that identity slice, and now also mirrors the `Math.atan2` zero slice and positive-integer-exponent `Math.pow` alias-chain slice in TS and `.js` browser-harness input. The `Math.sqrt` / `Math.cbrt` literal slices now also have direct build smoke coverage in the Deno and browser `.js` / `.ts` inputs, and browser bundle smoke now also covers the `Math.sqrt` / `Math.cbrt` literal slices in TS and `.js` inputs; `Math.ceil` / `Math.trunc` now also have direct build smoke coverage on const alias chains in the Deno and browser `.js` / `.ts` inputs. `Math.exp` / `Math.log` now also have exact zero/one identity folds in the current repo, `Math.expm1` / `Math.log1p` now also have exact zero identity folds, including const numeric alias chains, in the current repo, with browser-harness/browser-bundle smoke now also covering the exact zero identity slice in TS and `.js` input and the bracketed `globalThis["Math"]` root, `Math.atan2` now also supports the zero-numerator / non-negative-denominator literal slice, including const numeric alias chains and the supported transparent parenthesized, type-assertion, satisfies, and TS as-const wrapper slices, on the supported checker/codegen/build path, and the supported `globalThis.Math.atan2` / `globalThis["Math"].atan2` / `globalThis["Math"]["atan2"]` paths now also share that same slice in the current repository snapshot, the supported `globalThis.Math.*` member-access root now follows the same lowering path as the corresponding bare `Math.*` forms for the already-supported Math builtin slices, and the build corpus now also mirrors that literal slice in JSX and TSX input on both the Deno and browser surfaces, `Math.asin` / `Math.acos` / `Math.atan` now also have exact zero/one identity folds in the current repo, and browser-requested `run` / `test` smoke now also covers the exact `Math.asin` / `Math.acos` / `Math.atan` identity slice in TS and `.js` input with JSON-output coverage, `Math.asinh` / `Math.acosh` / `Math.atanh` now also have exact zero/one identity folds in the current repo, and browser-harness/browser-bundle smoke now also covers the exact `Math.asinh` / `Math.acosh` / `Math.atanh` identity slice in TS and `.js` input; `Math.sin` / `Math.cos` / `Math.tan` now also have exact zero identity folds in the current repo, and browser bundle smoke now also covers the exact zero-identity slice in TS and `.js` input, including the bracketed `globalThis["Math"]` root, and browser-harness smoke now also covers the exact `Math.tan` zero-identity slice in TS and `.js` input, and browser-harness `run` / `test` smoke now also covers the `Math.floor` / `Math.trunc` / `Math.ceil` const-alias-chain slice in TS and `.js` input; browser bundle smoke now also covers the `Math.floor` / `Math.trunc` / `Math.ceil` const-alias-chain slice in TS and `.js` input; the build/check corpus also pins explicit `E5506` rejection coverage for representative unsupported `Math` members such as `Math.exp` and `Math.log` across Deno and browser `.js` / `.ts` inputs, and standalone `run` / `test` plus browser-harness smoke still pin representative `Math.exp` / `Math.log` rejections on the JS-input math gate path and browser-targeted `check` / `build` smoke in `.jsx` / `.tsx` input. The browser-bundle smoke now also covers the `Math.max` / `Math.min` const-alias-chain slices in TS and `.js` input. The limited literal-array `for...of` and `for await...of` slices now share the same lowering path on literal arrays with literal elements, const numeric alias elements, const string alias elements, and simple variable bindings, including browser-harness smoke coverage in TS and `.js` input, browser build smoke now also mirrors the const string-alias slice for both loops on the browser API surface in TS and `.js` input, and direct build / browser-bundle smoke now also cover the const-alias-chain slice for both loops in TS and `.js` input.
- Optimization evidence: real `fast`, `release`, and `release-advanced` optimization slices; deterministic PGO profile validation; version-pinned benchmark fixtures.
- Library-export metadata: top-level function exports now get trivial literal-return inference in the bounded slice, and top-level `const` function-expression / arrow-function bindings now also carry that inferred signature through export aliases and deterministic sequence/conditional/decorated wrappers while preserving the conservative `unknown` fallback outside the slice; function-shaped default export expressions now also participate in that bounded library-export slice.
- Verification evidence: Lean proof tree and proof-backed status for the published boundary only.

## Planning consequence

Completed historical phase documents were removed from active planning. The active roadmap now starts at Phase 11 and focuses on remaining spec-owned work: language semantic closure, runtime/host expansion, package compatibility breadth, performance promotion, and verification/schema widening.

## Non-negotiable guardrails

Future phases must preserve:

- AOT-only guest-language compilation.
- Pure Rust implementation contract.
- No tracing/background GC.
- Sandbox-first honesty.
- Deterministic machine contracts.
- Public availability discipline from `specs/19-feature-maturity.md`.
- Proof-backed claim discipline from `proofs/BOUNDARY.md`.
