# Current Implementation Baseline

This file records the planning baseline for the active continuation roadmap. It is a current-state summary, not an active checklist and not an availability matrix.

## Evidence used for this cleanup

- `cargo run -q -p kali_cli --bin kali -- --help` exposes the current public command set.
- The workspace contains Rust crates for CLI, lexer/parser, AST/HIR/MIR/LIR, type checking, codegen, runtime, sandbox/effects, package management, optimization, Deno/Web/Node API projections, embedding/C ABI, formatting, linting, and bindings.
- For-await array iteration now also accepts a transparent `await` wrapper over a literal array on the parser, checker, codegen, runtime smoke path, and browser bundle/browser-harness smoke.
- AST serde round-trips now also preserve generator and async-generator flags on class declarations alongside the existing function-declaration, function-expression, and class-method coverage, keeping the typed AST metadata slice aligned across declaration forms.
- `Array.from(...)` wrappers around already-supported iterable slices now also lower through the same checker/codegen/runtime smoke path, including the JS/TS/JSX/TSX `run` and `test` matrix, keeping the bounded array-iteration slice aligned with the current iterator corpus; standalone `run` / `test` now also covers the parenthesized frozen callable alias `Object.freeze((globalThis.Array.from))`, browser-requested browser-harness smoke now also covers the direct and frozen `Array.from(...)` slices in JS, TS, JSX, and TSX input with matching JSON-output coverage, and browser bundle smoke now also covers the same direct and frozen `Array.from(...)` slices across JS, TS, JSX, and TSX input with matching JSON-output coverage. Type-resolution and codegen smoke now also cover the frozen `Object.freeze(globalThis.Array.from)` alias alongside the existing direct and `Object.freeze(Array.from)` cases, keeping the alias matrix aligned across the supported array-iteration path.
- Frozen `Array.from` call-targets now also flow through that same bounded array-iteration path, so `Object.freeze(Array.from)(...)` stays on the supported transparent-wrapper slice alongside the direct `Array.from(...)` form.
- Await-wrapped static helper inputs now also resolve through the same checker/runtime smoke path for `Object.is`, `Number.isSafeInteger`, `Object.keys`, and `Reflect.ownKeys` on JS input, and browser-requested browser-harness/browser-bundle smoke now also carries the await-wrapped `Object.is` and `Number.isSafeInteger` cases on the browser helper path, with browser object-key smoke now also covering await-wrapped `Object.keys` and `Reflect.ownKeys` helper inputs in the browser bundle/harness path, and the browser/runtime `Reflect.ownKeys` smoke now also covers the transparent `Object.freeze(globalThis.Reflect.ownKeys)` callable alias on the same supported slice.
- Parenthesized transparent wrappers around frozen callable helper targets now also ride the dedicated run/test and browser harness/bundle smoke for `Object.freeze(globalThis.Object["hasOwn"])` and `Object.freeze(globalThis.Math["floor"])`, and the shared `Math.floor` / `Math.trunc` / `Math.ceil` frozen-callable helper now also covers direct dot-root spellings such as `Object.freeze(globalThis.Math.floor)` / `Object.freeze(globalThis["Math"].floor)` alongside the existing bracketed-root variants and the bare `Math["floor"]` / `Math["trunc"]` / `Math["ceil"]` forms, keeping the helper-alias evidence aligned with the broader alias-chain corpus.
- Browser-harness and browser-bundle `Math.pow` alias-chain smoke now also covers parenthesized freeze-wrapper spellings around `globalThis.Math["pow"]` and `globalThis["Math"]["pow"]`, now also exercises the direct single-quoted `Math['pow']` / `globalThis.Math['pow']` / `globalThis['Math'].pow` / `globalThis['Math']['pow']` call-target spellings in the shared smoke, and the shared helper inventory now also tracks those spellings on the same alias path, keeping the browser math helper coverage aligned with the shared alias inventory. The exponent-one and base-one bundle/harness sources now also derive their direct invocation lines from the shared `math_pow_aliases()` helper, so that alias inventory stays single-sourced there too. The dedicated bracketed-frozen-wrapper bundle smoke now also draws its two bracketed alias lines from a shared kali_common helper so the narrower regression stays source-single-sourced too.
- The browser-harness and browser-bundle `Math.pow` alias smoke now also derive the direct invocation lines from the shared `math_pow_source()` helper, matching the frozen-callable single-sourcing already used for the wrapped aliases.
- The shared `Math.pow` invocation-line assembly for browser harness/bundle smoke now also lives in `kali_common`, keeping the direct and frozen line generation single-sourced across the Math.pow corpus. The exponent-one and base-one identity smoke now also derives its direct console-log and `return` entry lines from the same shared alias helpers, so the broader `Math.pow` source assembly stays centralized too.
- The shared `Math.pow` frozen-callable inventory now also splits into dedicated direct and parenthesized helper families in `kali_common`, mirroring the process.kill zero-probe inventory pattern for future reuse.
- Browser-targeted build smoke now also covers the fully bracketed `globalThis["Math"]["pow"]` alias-chain slice in JSX and TSX input on the browser API surface.
- The browser-bundle `mathPowExponentOneIdentity` smoke now also derives its frozen callable lines from the shared `math_pow_frozen_callable_aliases()` helper, so the bundle source stays single-sourced with the broader `Math.pow` alias inventory.
- The negative `Math.pow(2, -1)` rejection matrix now also covers JSX and TSX inputs on both the Deno and browser build surfaces, and the browser-requested run/test harness rejection path now also mirrors JSX and TSX inputs.
- The shared `Math.pow` frozen-callable aliases now also live in `kali_common`, and the browser math pow harness/bundle smoke now reuses that shared helper for the frozen call-target variants; the helper inventory now also includes bare `Object.freeze(Math.pow)` / `Object.freeze((Math.pow))` and `Math["pow"]` freeze-wrapper spellings so the alias matrix stays aligned across the shared smoke.
- Generator/async-generator function lowering diagnostics now reuse the shared `kali_common` message helper as a zero-allocation static string in the codegen path, keeping the mixed-flavor wording aligned with the type checker.
- Browser-targeted build smoke now also rejects negative `Math.pow(2, -1)` exponents in JS and TS input on both the Deno and browser surfaces, keeping the current-phase gate explicit.
- The Node `process.kill(0)` zero-probe helper inventory now also keeps the parenthesized receiver-freeze dot aliases `Object.freeze((globalThis.process).kill)` / `Object.freeze((globalThis["process"]).kill)` single-sourced through the shared helper path used by the Node, runtime, and browser late-compat smoke.
- The shared late-object-model source for `Proxy` / `Weak*` / `FinalizationRegistry` now lives in `kali_common`, now also includes the frozen `Proxy.revocable` aliases, and the runtime smoke reuses it instead of maintaining a local copy.
- The browser/runtime late-object-model revocable smoke now also includes parenthesized transparent wrappers around `Proxy.revocable`, keeping the negative object-model alias corpus aligned with the gated `Proxy` family.
- The browser-requested zero-spawned-process budget smoke now also covers the explicit `run --api browser` / `test --api browser` path under the browser harness, alongside the inherited-manifest browser-requested matrix, and positive `--max-spawned-processes` requests now also reject with the canonical `E5506` budget gate on the browser-requested and explicit browser-API paths.
- The shared `SharedArrayBuffer` / `Atomics` threaded-runtime source text now also comes from `kali_common`, keeping the browser/runtime rejection corpus on one canonical helper.
- The shared `Intl` source text now also lives in `kali_common` for the runtime smoke assembly, so the broad Intl prefix stays centralized even though the browser late-compat fixtures keep their own input-class-specific source concatenation.
- The shared `Set` / `Map` constructor alias inventories now also live in `kali_common`, giving the constructor smoke a common root-and-frozen vocabulary for future reuse.
- Runtime smoke now also exercises the frozen `Set` / `Map` constructor aliases and their parenthesized frozen wrappers, keeping the constructor slice aligned with the shared helper inventory.
- The direct call-target binding helper now also centralizes the `process.kill` / `process["kill"]` / `globalThis.process.kill` / `globalThis["process"].kill` / `globalThis.process["kill"]` / `globalThis["process"]["kill"]` aliases used by the Node API-surface smoke, so the callable-target aliases stay single-sourced alongside the existing sequence-callable-target helper.
- The sequence-callable-target binding helper now also carries the fully bracketed `globalThis["process"]["kill"]` binding pair and the bracketed-dot `globalThis["process"].kill` pair, keeping the Node surface alias matrix aligned with the shared helper source.
- The Node API-surface zero-probe smoke now also explicitly carries the parenthesized receiver-freeze dot and bracket aliases through the shared helper-backed matrix, keeping that static alias family aligned with the rest of the Node corpus.
- The canonical `process.kill(0)` alias-inventory source now literally composes the dedicated direct and wrapped helper strings, matching the shared single-source inventory note.
- Runtime smoke now also pins the `+0` siblings for the unparenthesized receiver-freeze aliases `Object.freeze(globalThis.process)["kill"]` / `Object.freeze(globalThis["process"])["kill"]`, plus the doubly parenthesized bracketed `((globalThis["process"]["kill"]))(+0)` alias, so the zero-probe wrapper corpus stays aligned with the shared helper inventory.
- The spec-owned availability and current-state nuance remain in [`../specs/19-feature-maturity.md`](../specs/19-feature-maturity.md).
- The explicit and inherited Node API-surface late-module rejection matrices now also pin `node:worker_threads` on the same canonical `E5506` path across `.js`, `.jsx`, and `.tsx` input as the explicit Node surface.
- The proof-backed boundary remains owned by [`../proofs/BOUNDARY.md`](../proofs/BOUNDARY.md).
- The shared `process.kill(0)` zero-probe helper inventory now also tracks the bare `process.kill` / `globalThis.process.kill` / bracketed reference spellings alongside the direct `Object.freeze(process.kill)` / `Object.freeze(globalThis.process.kill)` aliases, the receiver-freeze sibling spellings, the wrapped `process["kill"]((0))` alias, the parenthesized bare-process freeze-wrapper aliases `Object.freeze((process)["kill"])(0/+0)`, and the parenthesized callable-freeze aliases `Object.freeze((process["kill"]))(0/+0)` so diagnostics stay aligned with the current corpus, and the common crate now also exposes a combined full alias inventory helper plus a source-string helper for downstream checks and rejection corpora; that combined inventory now comes from an explicit ordered unique union of the direct and wrapped families, and the inventory source now composes the dedicated direct and wrapped source helpers so the source text and alias inventory stay in lockstep. Runtime and browser late-compat smoke now also assert that shared inventory source directly, so the fixture and helper stay synchronized. The checker and codegen zero-probe smoke now also pin the parenthesized bare-process freeze-wrapper aliases `Object.freeze((process)["kill"])(0/+0)`, and the shared late-process-control source now embeds that zero-probe inventory once, instead of duplicating the invocation slice in the preamble, and its host-control prefix now keeps the Deno cwd/chdir/exit aliases in the same deduplicated style. The common crate now also splits that late-process-control preamble into a dedicated prefix helper so the zero-probe inventory append stays isolated, and the prefix helper now has dedicated regression coverage so the preamble assembly and inventory append remain independently checkable; the prefix helper is now public so the browser JS late-compat fixture can assert the preamble boundary directly. The wrapped inventory now also carries parenthesized receiver-freeze spellings around bare `process` and `globalThis.process` receivers, and the runtime/browser/Node smoke now mirrors the parenthesized `Object.freeze((process))`, `Object.freeze((globalThis.process))`, and `Object.freeze((globalThis["process"]))` receiver-freeze aliases so the fixture coverage stays aligned with the shared corpus. Type-resolution and codegen smoke now also cover the same parenthesized receiver-freeze aliases on the zero-probe slice. The shared zero-probe inventory now also exposes TS-specific `satisfies` and type-assertion wrapper source helpers for the documented Node surface, so the Node zero-probe wrapper corpus can be assembled from the common helper instead of duplicated inline strings; those wrapper helpers now also include the bracketed `globalThis["process"].kill` and `globalThis["process"]["kill"]` call targets, keeping the typed wrapper matrix aligned with the broader alias inventory. The browser and frozen-object `Object.hasOwn` smoke now also reuse the shared frozen-callable condition helper from `kali_common`, keeping that corpus aligned with the same helper-source pattern used by the Node zero-probe inventory, and the frozen `Object.prototype.hasOwnProperty.call` helper family now follows the same shared-browser/direct smoke pattern. The direct `Object.prototype.hasOwnProperty.call` binding source now also lives in `kali_common`, so the browser harness/bundle smoke reuses the same canonical helper line. The shared `Object.prototype.hasOwnProperty.call` frozen-callable inventory now also covers the bracketed `globalThis["Object"].prototype.hasOwnProperty["call"]` alias path so that helper family stays single-sourced across the new member-access spelling. The new sequence-callable-target binding source now follows that same sharing pattern, and now also carries the remaining root and dot-root zero-probe binding pairs so the Node API-surface smoke can reuse the canonical `sequenceKill` declarations instead of hand-copying them inline. The late-process-control source now also sources the dedicated parenthesized receiver-freeze helper directly, keeping that dot-receiver slice single-sourced with the main zero-probe inventory.
- The browser late-compat and runtime smoke process-control bodies now source their shared late-process-control text from the common helper, keeping the `process.kill(0)` slice synchronized across the corpus and diagnostics; runtime smoke now also checks the parenthesized transparent freeze-wrapper aliases around `globalThis.process.kill` and `globalThis["process"].kill` against that shared source, the browser late-compat JS corpus now also mirrors the direct bracketed `process["kill"](0)` / `process["kill"](+0)` spellings, the standalone JS late-compat fixture now also pulls the shared late-process-control helper into its source assembly so the zero-probe block stays single-sourced, and it now also reuses the shared parenthesized receiver-freeze helper directly for that alias family, the shared zero-probe inventory now also exposes dedicated parenthesized receiver-freeze source helpers for the `Object.freeze((process)).kill` / `Object.freeze((globalThis.process)).kill` / `Object.freeze((globalThis["process"])).kill` dot spellings and the matching bracketed `Object.freeze((...)["kill"])` family, and the late process-env mutation fixture now reuses the shared `late_process_env_mutation_source()` assembly helper as well, while browser build preflight now also rejects the mixed bracket/dot `globalThis["process"].env[...]` mutation spelling directly.
- The Node package-corpus smoke now also mirrors the bracketed `process["cwd"]` / `process["chdir"]` / `process["exit"]` spellings plus their `globalThis.process[...]` and `globalThis["process"][...]` aliases on the documented Node surface, and the process-corpus body now sources the shared `process.kill(0)` zero-probe helper inventory directly so the corpus and diagnostics stay aligned; the package-corpus assertions now also check the full shared zero-probe alias inventory rather than only selected representative aliases, and the node-process corpus body now keeps the `const zero = 0; const zeroAlias = zero;` preamble so the regression still confirms the static alias source alongside the shared helper inventory.
- The package install flow now also rejects bootstrap-heavy lifecycle-script packages with the canonical E6005 package-contract diagnostic, keeping that exclusion explicit alongside the native-addon case.
- The shared runtime-entrypoint class-method gate helper now uses a name that matches the rejected generator/async-generator slice more precisely, and the class-expression generator/async-generator rejection matrix now also has explicit check/build and type-resolution coverage through variable-initializer and default-export expression forms in JS/TS/JSX/TSX input. The focused type-resolution regression now also loops over JS, TS, JSX, and TSX source extensions for that gate, and the parser's class-expression preservation smoke now also keeps `yield*` delegation intact on the generator path. The runtime-entrypoint regression set now also exercises transparent TS `as`/`satisfies` wrappers around generator and async-generator class expressions so the direct gate stays aligned with the TypeScript wrapper path too.
- Library-export collection now also resolves named re-exports into `default` across the source graph, keeping the bounded export-signature path aligned with default-alias re-export chains. Freeze-wrapped callable export bindings now also resolve through the same base-library export-surface path when the underlying callable remains statically known.
- The shared `process.kill(0)` zero-probe inventory now also covers dot-property frozen receiver spellings for `process`, `globalThis.process`, and `globalThis["process"]`, and the late-process-control source and runtime smoke pick them up through the shared helper path.

- The browser late-compat TS/JSX/TSX fixtures and runtime smoke now also assert the bare `((globalThis.process["kill"]))(+0)` zero-probe alias from the shared inventory, keeping the non-JS wrapper matrix aligned with the JS/browser helper coverage.
- Standalone run/test smoke now also covers the await-wrapped `Object.is` / `Number.isSafeInteger` / `Object.keys` / `Reflect.ownKeys` helper slice through an async test callback that returns the probe promise, keeping the shared await-wrapper evidence aligned across both execution lanes.

- The `--profile` build input now also rejects empty and whitespace-only files through the canonical `E5509` parse path in both human and JSON output, keeping the deterministic PGO input contract explicit.

## Live surface at a glance

The checked-in repository already includes:

- CLI commands: `doctor`, `init`, `install`, `fmt`, `lint`, `check`, `build`, `run`, `test`, `effects`, `package-effects`, and `package-audit`.
- Build/artifact lanes: executable builds, browser bundles, IR validation, library artifacts, C ABI artifacts, component artifacts, deterministic metadata, and sidecar manifests.
- Runtime/reporting lanes: `run`, `test`, `test --coverage`, source-graph effects, package effects, package audit, deterministic JSON envelopes, schema-v1 payload validation, bracketed `Object["keys"]` / `globalThis["Object"]["entries"]` object-enumeration lowering coverage, browser-harness sequence-wrapper coverage over the supported object-enumeration calls including the single-quoted bracketed `Object` spellings, direct `Reflect.ownKeys(...)` iterator lowering over static object-literal slices and its sequence-expression wrapper slice in direct/boundary smoke, including `Object.freeze`-wrapped frozen-object variants and the single-quoted bracketed `Reflect` alias in the browser-harness smoke path, plus browser-requested run/test smoke for the frozen-object `Reflect.ownKeys(...)` slice in JS, TS, JSX, and TSX input with JSON-output coverage, and the type checker now also accepts the bracketed `globalThis["Reflect"]["ownKeys"]` alias spelling on that supported `Reflect.ownKeys(...)` slice, including the single-quoted `globalThis['Reflect']['ownKeys']` regression on the await-wrapped helper path, while codegen smoke now also covers the mixed `globalThis["Reflect"].ownKeys` alias spelling there. The current string-concatenation iterable slice for `for...of` / `for await...of` over statically known string operands, plus dedicated browser-bundle smoke for that same string-concatenation slice in JS, TS, JSX, and TSX input and browser-requested run/test browser-harness smoke for the same string-concatenation slice in JS, TS, JSX, and TSX input with JSON-output coverage, plus browser-bundle/browser-harness smoke for the `for...of` template-literal slice over statically known string operands in JS, TS, JSX, and TSX input, matching standalone TS `for await...of` string-concatenation smoke, and the existing `for await...of` string-concatenation smoke, plus standalone run/test smoke for static `new Set([...])` constructor iteration over supported literal-array inputs in JS and TS input, including `Object.freeze`-wrapped literal-array inputs, plus standalone and browser-requested run/test/browser-harness smoke for static `new Map([...])` constructor iteration over supported entry-tuple inputs across JS, TS, JSX, and TSX input with JSON-output coverage, and browser-requested run/test browser-harness smoke for the same static `new Set(...)` constructor slice across JS, TS, JSX, and TSX input with JSON-output coverage, and browser-bundle smoke now also covers the same static `new Set(...)` constructor slice across JS, TS, JSX, and TSX input, plus static `new Map(...)` constructor slices over supported entry tuples across the same matrix, and the browser-harness/browser-bundle smoke now also covers frozen constructor aliases such as `Object.freeze(Set)` / `Object.freeze(Map)`, while alias-chain and transparent-wrapper coverage now also pins `Set`/`Map` constructor references through the same bounded root-resolution path, including const-alias and parenthesized-alias `Set`/`Map` constructor coverage in runtime, browser-harness, codegen, and type-resolution smoke, plus frozen constructor aliases such as `Object.freeze(Set)` / `Object.freeze(Map)` in the codegen/runtime smoke path; the current smoke now also exercises abrupt return/finally and throw/finally sequencing on the supported Set/Map constructor slice across the existing run/test, browser-harness, and browser-bundle coverage, the `Object.is` slice for static primitive values, same-reference object/array aliases, `Object.freeze`-wrapped same-reference aliases, no-substitution template-literal comparisons, distinct fresh object/array literal comparisons, and the BigInt literal comparison slice in standalone JS and browser-harness TSX runtime smoke, while BigInt arithmetic smoke now also covers subtraction and division slices in TS and JS input, and the static `Number.isFinite` / `Number.isNaN` / `Number.isInteger` / `Number.isSafeInteger` primitive-value slice. Type-resolution regression coverage now also explicitly exercises the shared `Object.is` and `Number.is*` alias spellings that feed those slices, including the parser-backed bracketed `Object.is` and `Number.is*` forms used by the browser/runtime smoke paths, and browser-requested run/test browser-harness smoke now also covers the same-reference alias-chain and frozen same-reference alias-chain `Object.is` slices in JS, TS, JSX, and TSX input with JSON-output coverage, plus dedicated browser-harness alias-chain smoke for the same `Object.is` object/array and frozen-object/frozen-array variants in JS, TS, JSX, and TSX input, and browser-requested run/test JSON smoke now mirrors that object/array/frozen-reference matrix in the browser harness, plus standalone `run` / `test` smoke for the Deno filesystem slice now also covering JSX and TSX input, and browser-requested run/test browser-harness smoke now also covers the `Object.values([...Object.fromEntries(...)])` spread slice, including frozen `Object.fromEntries(...)` operands, in JS, TS, JSX, and TSX input with JSON-output coverage, and the browser bundle matrix now also includes direct `Object.values(...)` iteration over object-literal inputs in JS, TS, JSX, and TSX input, while standalone run/test smoke now also covers frozen direct `Object.entries(...)` object-literal targets in JS, TS, JSX, and TSX input, and standalone run/test smoke now also covers the `for await` spread-of-`Object.values` / `Object.keys` / `Object.entries` slice over `Object.fromEntries(...)` operands in JS and TS input with JSON-output coverage, and the object-enumeration break/continue slice now also has standalone, browser-harness, and browser-bundle smoke on the supported `Object.keys(...)` iteration path, while the browser-bundle object-enumeration finalization smoke now also covers the same `Object.keys(...)` / `Object.entries(...)` return/finally and throw/finally matrix across JS, TS, JSX, and TSX input, and the array break/continue slice now also has standalone, browser-harness, and browser-bundle smoke for the supported `for...of` / `for await...of` array iteration path in JS, TS, JSX, and TSX input; the current runtime smoke also covers return/finally and throw/finally sequencing for supported `Object.keys(...)` and `Object.entries(...)` iteration in standalone and browser-requested harness paths, while the browser rejection corpus now also pins the `new Set(values.filter(Boolean))` call-expression edge across JS, TS, JSX, TSX, and inherited browser-config rejection paths so the literal-array gate stays explicit. The browser-bundle `Object.values([...Object.fromEntries(...)])` spread smoke now also covers frozen `Object.fromEntries(...)` operands in JS, TS, JSX, and TSX input with JSON-output coverage. The own-property helper smoke now also covers the bare `Object["hasOwn"]` root spelling in the browser-harness, browser-bundle, and source-input helper slices, plus the `Object["hasOwnProperty"].call` / `globalThis["Object"]["hasOwnProperty"].call` aliases now exercised in the JS late-compat and browser JS smoke sources on the same static helper slice, and type-resolution smoke now also covers sequence-wrapped call targets for supported `Object.hasOwn(...)` and `Math.floor(...)` helper calls, and the Node `process.kill(0)` smoke now also includes sequence-callable-target aliases in both the checker and node-surface regression paths, plus wrapped `globalThis.process.kill` and wrapped bracketed `globalThis.process["kill"]` call-target aliases in the node-surface positive regression, plus the bracketed `process["kill"]` / `globalThis["process"]["kill"]` wrapper forms in the Node API-surface and codegen smoke, and runtime outcomes now also carry deterministic thread-topology snapshots for spawned worker instances, and worker script URLs now trim surrounding whitespace before parsing so the direct `ThreadRuntimeTopology` helper stays aligned with the normalized runtime spawn path. Browser-requested `run` / `test` browser-harness smoke now also accepts inherited browser `runtimeProfiles=["wasm-threads"]` configs in JS, TS, JSX, and TSX input alongside positive `--max-threads` requests, keeping the browser/runtime threading evidence explicit; standalone JS runtime smoke now also mirrors the fully bracketed `globalThis["Object"]["prototype"]["hasOwnProperty"]["call"]` / `globalThis.Object["prototype"]["hasOwnProperty"]["call"]` alias pair on the same static helper slice, and the freeze-wrapped helper smoke now also covers the mixed dot-root spellings `Object.freeze(globalThis.Object["hasOwn"])(object, "a")` and `Object.freeze(globalThis.Math["floor"])(1.6)` alongside the existing bracketed-root variants; the browser-harness/browser-bundle math floor/trunc/ceil alias-chain smoke now also carries frozen callable aliases for `Math.trunc` and `Math.ceil` in the same matrix.
- Browser-harness smoke now also covers the `Math.pow` alias-chain slice through the bracketed `globalThis["Math"]["pow"]` and mixed `globalThis.Math["pow"]` spellings in JS, TS, JSX, and TSX input, keeping the browser math surface aligned with the bundle coverage; the transparent `Object.freeze(globalThis.Math["pow"])` alias is now covered there as well, and the exponent-one/base-one identity smoke now also mirrors the same frozen callable aliases.
- The package-effects report validator now enforces the single-root `entryPoints` contract from the schema docs, so envelope validation stays aligned with the one-package registry-analysis shape. The package-effects wrapper regression now also pins the inherited browser + wasm-threads analysis context on the native-JSON payload path.
- The default-standalone late process-control rejection corpus now also carries `process.kill(0)`, static zero aliases, and wrapper spellings, including transparent `process` object wrappers and wrapped `process.kill` call-target forms, plus the direct callable-freeze alias `Object.freeze(process.kill)(0)` / `Object.freeze(process.kill)(+0)` and the bracketed frozen aliases `Object.freeze(globalThis.process["kill"])(0)` / `Object.freeze(globalThis.process["kill"])(+0)`, plus the direct `Object.freeze(globalThis.process.kill)(0)` / `Object.freeze(globalThis.process.kill)(+0)` aliases, `Object.freeze(globalThis["process"].kill)(0)`, `Object.freeze(globalThis.process)["kill"](0)`, `Object.freeze(globalThis["process"])["kill"](0)`, `Object.freeze(process)["kill"](0)`, `Object.freeze(globalThis["process"]["kill"])(0)`, the parenthesized bracketed freeze-wrapper aliases `Object.freeze((globalThis["process"]["kill"]))(0)` / `Object.freeze((globalThis["process"]["kill"]))(+0)`, `Object.freeze((globalThis.process["kill"]))(0)` / `Object.freeze((globalThis.process["kill"]))(+0)`, `Object.freeze((globalThis["process"].kill))(0)` / `Object.freeze((globalThis["process"].kill))(+0)`, and the parenthesized transparent freeze wrappers `Object.freeze((process.kill))(0)` / `Object.freeze((process.kill))(+0)`, so the unsupported-path evidence stays aligned with the Node zero-probe slice, and the plain JS late-compat fixture now also carries the wrapped `process["kill"]((0))` / `globalThis.process["kill"]((0))` / `globalThis["process"].kill((0))` spellings. The browser late-compat smoke corpus now also pins the direct `globalThis.process["kill"](+0)` and `globalThis["process"].kill(+0)` spellings across JS, TS, JSX, and TSX input, and the browser/runtime late-compat matrix now also includes the doubly wrapped `((globalThis["process"]["kill"]))(+0)` sibling. The codegen and type-checker rejection wording now also enumerates the transparent `Object.freeze(globalThis.process["kill"])(0)` / `Object.freeze(globalThis.process["kill"])(+0)` and `Object.freeze(globalThis["process"].kill)(0)` aliases so the canonical failure text stays aligned with the same supported matrix, and codegen smoke now also pins the bracketed `Object.freeze(globalThis["process"]["kill"])(0/+0)` aliases.
- The object.is / Number.is* smoke corpus now also covers direct bracketed root spellings (`Object["is"]` and `Number["isFinite"]` / `Number["isInteger"]` / `Number["isSafeInteger"]` / `Number["isNaN"]`) across runtime, browser-harness, bundle, build, and type-resolution regression coverage, and the browser-harness Object.is same-reference/frozen-reference alias-chain smoke now also includes the bare `Object["is"]` root spelling, the browser-requested browser-harness/browser-bundle smoke now also accepts const aliases that resolve to the supported `Object.is` and `Number.is*` callables, the browser-targeted `Object.hasOwn` / `Object.prototype.hasOwnProperty.call` slice now also accepts const aliases that resolve to those callables, the computed-numeric object-key smoke now also covers unary-wrapped computed keys in browser-harness and browser-bundle paths across JS, TS, JSX, and TSX input, and the browser-harness `Object.values([...Object.fromEntries(...)])` spread slice now also covers frozen operands, while the unsupported-math browser-harness rejection smoke now also covers the bracketed `Math["sqrt"]` / `Math["atan2"]` and `globalThis["Math"].sqrt` / `globalThis["Math"].atan2` spellings in JSX and TSX input, plus the single-quoted bracketed `globalThis['Math']['sqrt']` / `globalThis['Math']['atan2']` spellings on the same unsupported browser-harness path. The browser-bundle `Math.atan2` trailing-argument evaluation smoke now also covers JS, TS, JSX, and TSX input with JSON-output coverage, and the single-quoted bracketed `globalThis['Math']['atan2']` zero slice now also covers JSX and TSX input on the browser-bundle path. The Set/Map constructor regression corpus now also pins bracketed root spellings like `globalThis["Set"]` and `globalThis['Map']` on the shared supported iteration slice, and codegen smoke now also covers the exact bracketed `globalThis["Set"]` / `globalThis['Map']` root spellings on that same path; the current smoke also now covers frozen `globalThis.Set` / `globalThis["Set"]` and `globalThis.Map` / `globalThis['Map']` constructor aliases on that same slice across runtime, browser-harness, and browser-bundle coverage, and now also includes parenthesized freeze-wrapper aliases around `Set` / `Map` constructor references on the same shared slice. Browser API-surface late-compat smoke now also mirrors generator class-expression and async-generator default-export class-expression rejection across JS, TS, JSX, and TSX input.
- Browser-requested sequence-wrapped template-literal dynamic import smoke now also covers TS, JSX, and TSX input with JSON-output coverage.
- Browser-bundle smoke for the `for...of` template-literal iterator slice now also covers JSX and TSX input, keeping the browser bundle matrix aligned with the shared string-concatenation coverage.
- Browser iterator rejection coverage now also includes non-literal `Object.entries(...)` sources in the browser-targeted `check` / `build --bundle` lanes and their inherited browser-config forms, and browser-requested run/test browser-harness rejection coverage now also includes non-literal `new Set(values.filter(Boolean))` and `new Map(values.filter(Boolean))` sources in JS input.
- Try/finally sequencing smoke now also covers the supported JS-input `for...of` / `for await...of` array-iteration slices, including the browser-harness JSON-output path.
- The number-predicate smoke now also exercises bracketed and mixed `globalThis["Number"]["isFinite"]` / `globalThis["Number"]["isInteger"]` / `globalThis.Number["isNaN"]` / `globalThis["Number"].isNaN` / `globalThis["Number"].isSafeInteger` spellings in the same supported slice, the static `Number.isSafeInteger` slice now joins the existing `Number.isFinite` / `Number.isNaN` / `Number.isInteger` coverage, standalone `run` / `test` smoke now also covers that same primitive-value slice in JS and TS input with JSON-output coverage, browser-harness smoke now also covers that same primitive-value slice in JSX and TSX input, browser-bundle smoke now also covers that same primitive-value slice in JS, TS, JSX, and TSX input, the type-resolution regression suite now also mirrors the `Number.isSafeInteger` bracketed alias family in JS input, and `check` / `build` smoke now also covers the same Number-predicate alias slice on the Deno and browser surfaces across JS, TS, JSX, and TSX input while the browser-requested browser-harness/browser-bundle smoke now also accepts const aliases that resolve to the supported `Number.is*` callable, including an explicit `Number.isSafeInteger` const alias in the current smoke corpus.
- Promise.allSettled smoke now also covers standalone build/runtime, browser-requested browser-harness, and browser-bundle paths across JS, TS, JSX, and TSX input.
- Nullish coalescing smoke now also covers the `void 0 ??` and `undefined ??` fallback operands on the standalone run/test paths and the browser-requested browser-harness paths in JS input, keeping the `null`/`undefined` fallback semantics explicit.
- Math bundle/harness smoke now also covers the fully bracketed `globalThis["Math"]["sqrt"]` / `globalThis["Math"]["cbrt"]` spellings in JS, TS, JSX, and TSX input.
- Frontend metadata lanes: class generator methods now preserve async/generator flags in the parser, AST, HIR, MIR, LIR, and codegen export-analysis prechecks, with direct MIR/LIR regression coverage now pinning that propagation while later phases still gate lowering with the canonical generator diagnostic. Async class methods now resolve through the shared async-function lowering path; generator class methods remain gated, build smoke now also exercises async class methods across TS, JS, JSX, and TSX input on both the Deno and browser artifact paths, and direct `run` / `test` now carry an explicit E5506 gate for the async-class-method runtime slice pending the runtime follow-up, with standalone rejection smoke now also covering the same async class-method sequencing gate in JS, TS, JSX, and TSX input with both text and JSON output. Generator codegen plus the type-resolution and CLI build-preflight gates now emit distinct generator vs async-generator gate wording while preserving the same canonical E5506 path, and runtime smoke now also pins the distinct async-generator wording on the direct `run` / `test` path. The direct runtime-entrypoint rejection helper now also catches async class expressions in JS/TS/JSX/TSX input, generator class expressions in JS/JSX/TSX input, parenthesized export-default class expressions in JS/TS input, and async-generator default-export class expressions in JS/JSX/TSX input, and the runtime smoke matrix now also covers those same class-expression gates for direct `run` / `test` JSON and text output, including the TS async default-export class-expression slice. Direct `run` / `test` generator-function rejection smoke now also covers TS input alongside the existing JS/JSX/TSX coverage. The library-export collection path now also rejects generator and async-generator declarations/expressions through the same canonical E5506 gate instead of treating them as statically known exports, and the current regression set now also covers default-export class-expression generator metadata through parser and export-analysis paths. The parser now also canonicalizes transparent wrapper forms around computed object-property keys when they still resolve to static string/number names, including unary numeric wrappers and await/sequence wrapper chains; the computed-numeric object-key smoke now exercises that path in standalone run/test and browser harness/bundle contexts, including the await-wrapped numeric-key slice now covered in runtime and browser smoke. The codegen `yield` / `yield*` / `delegate` gate now routes through the shared generator helper directly, keeping async-generator wording aligned with the current function flavor. The Node `process.kill(0)` liveness-probe smoke now also covers transparent parenthesized and unary-plus wrapper forms in addition to the bracketed `globalThis.process["kill"](0)` and `globalThis["process"].kill(0)` aliases across JS, TS, JSX, and TSX input on the documented Node surface, and the dedicated Node API-surface positive regression now mirrors that same wrapper/alias matrix in its `run` and `test` inputs, including wrapped `process` and wrapped `process.kill` call-target forms plus const aliases and sequence-callable-target forms of the callable target itself, TS/TSX satisfies-wrapped zero literals on the checker and CLI surfaces, and bracketed `globalThis.process["kill"]` / `globalThis["process"]["kill"]` / `globalThis.process["kill"](0)` / `globalThis["process"]["kill"](0)` zero-probe spellings. Static aliases of the zero-probe argument now also feed that slice on the Node surface, and the zero-probe wrapper regression now also covers `(0 as number)` assertions in TS and TSX input. The unsupported-path diagnostics now also name the exact bracketed aliases so the negative evidence stays aligned with the supported alias matrix.
The Node package-corpus evidence now also exercises the `process.kill(0)` liveness-probe slice, including bracketed and `globalThis` alias forms and the fully bracketed `globalThis["process"]["kill"](0)` invocation, plus wrapped aliases such as `process["kill"]((0))`, `globalThis.process.kill(+0)`, and `Object.freeze(process.kill)(+0)` to keep the corpus matrix aligned with the shared Node smoke set, alongside the existing `process.cwd` / `process.pid` / `process.chdir` / `process.exit` slice on the documented Node surface, and the Node API-surface regression now also covers the fully bracketed wrapped `((globalThis["process"]["kill"]))(0)` form and the mixed `globalThis["process"].kill(0)` alias, the build-preflight rejection helper source now also includes the browser-safe receiver-freeze aliases and their +0 siblings `Object.freeze(globalThis.process)["kill"](0)` / `Object.freeze(globalThis["process"])["kill"](0)`, while the late-compat corpus now also covers the bracketed frozen alias `Object.freeze(globalThis["process"]["kill"])(0)`. The Node API-surface positive regression now also covers the `Object.freeze(...)`-wrapped zero-probe aliases on the explicit and inherited Node surfaces, including receiver-freeze aliases and their +0 siblings such as `Object.freeze(process)["kill"](0)` / `Object.freeze(process)["kill"](+0)` / `Object.freeze(globalThis.process)["kill"](0)` / `Object.freeze(globalThis.process)["kill"](+0)` / `Object.freeze(globalThis["process"])["kill"](0)` / `Object.freeze(globalThis["process"])["kill"](+0)`, and the fully bracketed frozen `Object.freeze(globalThis["process"]["kill"])(0)` / `(+0)` aliases; the package-corpus assertions now check the shared inventory end-to-end, and the Node API-surface run/test smoke now reuses the canonical zero-probe source helpers for its large console-log and rejection-guard payloads.
- Browser-requested run/test browser-harness smoke now also covers the zero-thread / zero-spawn-process budget path across JS, TS, JSX, and TSX input with matching JSON-output and human-output coverage.
- Host/API slices: default standalone Deno-oriented APIs, browser-targeted `check` / `build --bundle`, configured browser-harness execution, and documented Node-compatible analysis/build/runtime slices.
- Package evidence: registry/raw-URL/install flows, package-corpus probes by context/rung, registry-analysis commands, and negative evidence for unsupported native/binary/bootstrap-heavy or published-bin-entrypoint cases. The corpus now also rejects native-addon entrypoints on the default standalone source-graph commands in JS input, keeping the excluded package contract explicit, and registry-analysis commands now also reject scheme-prefixed selectors such as `npm:lodash` before registry lookup.
- Filesystem host APIs: `Deno.open` / `create` / `mkdir` / `remove` / `rename` / `lstat` now also have standalone `check` / `build` / `run` / `test` smoke coverage in JS, TS, JSX, and TSX input with JSON-output coverage on the executable lanes.
- Embedding/evidence surfaces: public embedding artifacts and bindings metadata, including const-alias-aware function-export signature collection for same-statement and cross-statement function binding chains, template-literal return-type inference for library exports, default-export alias-chain coverage, multi-hop source-graph re-export coverage, optimization modes, deterministic PGO profile validation, freeze-wrapped callable helper peeling in builtin call folding, benchmark fixtures, and Lean proof infrastructure for the published boundary. The default-export alias-chain regression now also has explicit JSX/TSX coverage in the CLI build tests, and the run/test thread-topology JSON snapshot now keeps `liveInstances` ordered by `instanceId`, without duplicates, and with coherent `totalInstances = terminatedInstances + liveInstances.length` counts; the aggregated test payload merger now also renumbers appended live-instance ids across multi-file runs so the emitted snapshot remains schema-valid, the runtime host-state thread-spawn regression now also pins whitespace-trimmed script URLs and rejects relative spellings with the canonical absolute-URL validation, the CLI payload validator now rejects whitespace-padded `threadTopology.liveInstances[].scriptUrl` spellings as non-canonical absolute URLs, whitespace-only thread script URLs now fail before topology allocation, and the thread-topology tests now also pin monotonic instance-id assignment after termination/re-spawn so worker accounting stays deterministic.
- CLI/config diagnostics: malformed `compat.features` and `compilerOptions.runtimeProfiles` manifest entries now carry structured config-context metadata in JSON diagnostics, the browser runtime contract payload now trims its canonical host fields and canonical note-array items before comparison while still rejecting whitespace-only variants, the browser runtime contract `diagnosticHint` field now also keeps that same trimmed comparison path under regression coverage, and the browser runtime contract wording now shares its canonical supported-commands and diagnostic-notes tuples with the internal validator helpers so the contract text stays centralized; malformed browser harness command overrides now preserve the raw override text in diagnostics after the parser trims surrounding whitespace for split normalization, the doctor human-output regression now also covers whitespace-padded browser harness overrides while still showing the raw env override and trimmed command split, the doctor human-output path now also reads from the shared browser-runtime contract payload so the text summary stays aligned with the JSON fixture, and the doctor human summary now also prints the browser host description note alongside the canonical contract snapshot and now sources the browser-runtime contract fields directly from the shared JSON fixture, package-audit now also rejects `--pretty` before JSON mode is enabled and the legacy `--preview` shim is rejected before registry lookup while staying hidden from generated help, and the preview-short-circuit regression now also pins both text and JSON output paths before target validation; the canonical `process.kill(0)` zero-probe wording is now shared across the checker and codegen through a common helper so the alias matrix stays aligned, including the mixed frozen `Object.freeze(globalThis["process"].kill)(0)` / `(+0)` aliases, the bracketed frozen `Object.freeze(globalThis["process"]["kill"])(0)` / `(+0)` aliases, the parenthesized freeze-wrapper `Object.freeze((process.kill))(0)` / `(+0)` aliases, the parenthesized receiver-freeze spellings `Object.freeze((globalThis.process)["kill"])(0)` / `Object.freeze((globalThis.process)["kill"])(+0)` / `Object.freeze((globalThis["process"])["kill"])(0)` / `Object.freeze((globalThis["process"])["kill"])(+0)`, the bracketed `process["kill"]((0))` wrapper alias, and the parenthesized bracketed `Object.freeze((globalThis["process"]["kill"]))` variants; the typed wrapper helper now also includes `globalThis.process.kill` directly so the wrapped call-target matrix stays aligned with the direct member-access form.

- The shared process.kill(0) zero-probe wording helper now also enumerates the bracketed `((process["kill"]))` aliases in the supported diagnostic matrix, plus the double-parenthesized `((process.kill))` aliases, and the focused helper test now also pins the direct `process.kill(0)` / `process.kill(+0)` aliases, the receiver-freeze siblings `Object.freeze(process)["kill"]`, `Object.freeze(globalThis.process)["kill"]`, and `Object.freeze(globalThis["process"])["kill"]`, and the wrapped `process.kill` / `process["kill"]` call-target forms alongside the mixed transparent-wrapper aliases, the direct `globalThis.process["kill"](+0)` / `globalThis["process"].kill(+0)` / `globalThis["process"]["kill"](0)` / `globalThis["process"]["kill"](+0)` aliases, and the parenthesized freeze-wrapper `Object.freeze((process.kill))` / `Object.freeze((globalThis["process"].kill))` aliases, plus the parenthesized bracketed freeze-wrapper aliases `Object.freeze((globalThis["process"]["kill"]))(0/+0)`, while the browser late-compat smoke now also carries the wrapped `((process["kill"]))(+0)` sibling on the JS/JSX/TS/TSX matrix, and the canonical helper now also tracks the wrapped `globalThis.process["kill"]((0))`, `globalThis.process.kill((0))`, and `globalThis["process"].kill((0))` forms; the common crate now also exposes dedicated direct and wrapped zero-probe source helpers plus the wrapped-zero alias inventory helper for the `process.kill((0))` and `Object.freeze((...))` forms, and the browser late-compat JS/TS/JSX/TSX source fixtures now also explicitly pin the mixed `globalThis.process["kill"]((0))` wrapped alias against that shared inventory. The focused helper test now also pins the mixed frozen `globalThis["process"].kill` aliases and their parenthesized variants, matching the helper inventory and browser/runtime corpus. It now also checks the remaining wrapped `process["kill"]` / `globalThis["process"].kill` call-target aliases and their double-parenthesized forms, including the `((process["kill"]))` pair, so the helper unit test covers the full inventory matrix. The shared inventory now also carries the doubly parenthesized `((globalThis.process.kill))(+0)` sibling, and the runtime/browser process-control smoke picks it up through the shared source.
- The canonical `process.kill(0)` zero-probe helper inventory was deduplicated and remains aligned with the current Node/browser smoke corpus, the shared wording helper now derives from that inventory instead of hand-copying the alias matrix, the runtime smoke late-process-control source now derives its zero-probe segment from that same helper, and the browser late-compat JS/TS/JSX/TSX fixtures now mirror that trimmed alias inventory; the JS/TS/JSX fixtures now also assert that shared inventory exactly once, matching the TSX corpus, the runtime and browser late-compat source assertions now also pin the wrapped `process["kill"]((0))` / `globalThis.process["kill"]((0))` / `globalThis["process"].kill((0))` call-target forms explicitly, the browser late-compat fixtures now also pin `Object.freeze((globalThis.process.kill))(0/+0)` against the same shared helper inventory, the browser late-compat fixtures now also cover the parenthesized dot-receiver freeze aliases `Object.freeze((process).kill)(0/+0)` on that same shared inventory, the browser TSX late-compat source now also drops one duplicate `Object.freeze(globalThis["process"]["kill"])(+0)` alias so the trimmed inventory stays duplicate-free, and the browser JS/JSX fixtures now also pin the parenthesized dotted `Object.freeze((globalThis["process"].kill))(0/+0)` aliases alongside the existing TSX coverage; the runtime smoke and browser TSX fixture now also include the parenthesized callable-freeze `Object.freeze((process["kill"]))(0/+0)` pair, the canonical alias-inventory source now composes the dedicated direct and wrapped source helpers, the shared late-process-control preamble now assembles those statement segments explicitly before appending the shared inventory, and the parenthesized receiver-freeze dot aliases now also live behind a dedicated helper inventory so the late-process-control smoke can keep that slice single-sourced.
- The browser late-compat and Node API-surface smoke now also cover the parenthesized receiver-freeze dot spellings `Object.freeze((process)).kill`, `Object.freeze((globalThis.process)).kill`, and `Object.freeze((globalThis["process"])).kill`, keeping that zero-probe alias family aligned with the shared helper.
- The browser runtime contract validator now has a combined regression for trimmed contract fields, covering the canonical host labels, host description, host description note, supported command tuple, diagnostic hint, and diagnostic-note array items in one payload so the contract-hardening path stays explicit; the lower-level validator unit test now mirrors that same combined payload shape, and the CLI regression builds the trimmed browser-runtime payload from `kali_runtime::browser_runtime_contract_value()` directly so the helper-backed contract shape stays single-sourced across CLI and runtime tests; the CLI `doctor` payload reuses that same helper value directly.
- The browser runtime contract validator now also accepts padded `supportedCommands` and `diagnosticNotes` items in the same shared regression family, keeping the array-valued contract fields aligned with the trimmed scalar checks.
- Browser-requested `test` summary merging now also accepts padded `hostContract` / `runtimeBackend` labels in the browser summary file, so the trimmed label parser stays aligned with the browser harness fallback path.
- The browser-analysis `effects` wasm-threads rejection matrix now also spans JS, JSX, and TSX input on both the explicit browser-API surface and the inherited browser-analysis path, and the JSON-output matrix now mirrors that same browser-profile gate across JS/TS/JSX/TSX input, keeping the shared source-graph evidence aligned with the browser runtime profile gate.
- The standalone frozen `Object.hasOwn` smoke now also covers parenthesized transparent wrappers around the frozen callable aliases, keeping the helper alias matrix aligned with the browser harness/bundle coverage; the shared frozen-callable helper inventory now also includes the dot-root `globalThis.Object.hasOwn` / `globalThis["Object"].hasOwn` spellings so the browser and runtime fixtures can stay single-sourced across dot and bracket forms, and the standalone/browser frozen-object fixtures now also reuse the shared frozen-callable source helper for the frozen alias statements themselves.
- The browser Object.hasOwn harness now also relies on that shared frozen-callable helper inventory for the bracketed `Object.freeze(globalThis["Object"]["hasOwn"])` alias, removing the last local duplicate from the explicit harness source.
- The browser math floor / trunc / ceil smoke now also shares a canonical frozen-callable helper inventory in `kali_common`, mirroring the `Object.hasOwn` and `process.kill(0)` source helpers.

## Planning consequence

The active plan starts from remaining spec gaps only. It does not preserve line-by-line progress notes for implemented slices; those belong in tests, specs, maturity current-state notes, and proof-boundary documents.

## Non-negotiable guardrails

Future phases must preserve:

- AOT-only guest-language compilation.
- Pure Rust implementation contract.
- No tracing/background GC.
- Sandbox-first honesty.
- Deterministic machine contracts.
- Public availability discipline from `specs/19-feature-maturity.md`.
- Proof-backed claim discipline from `proofs/BOUNDARY.md`.
