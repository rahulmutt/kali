# Current Implementation Baseline

This file records the planning baseline observed before cleaning up the old completed plan phases.

## Repository evidence

- `cargo test --workspace` passes in the current workspace.
- `cargo run -q -p kali_cli --bin kali -- --help` exposes the current CLI command set.
- The workspace contains implementation crates for CLI, frontend, IR, codegen, runtime, sandbox/effects, package management, optimization, host API surfaces, embedding, C ABI, and bindings.
- The proof tree exists under `proofs/` and `proofs/BOUNDARY.md` states that Kali is proof-backed for the published boundary.

## Live surface described by specs and README

The current repo has already implemented the historical MVP and several later surfaces:

- `kali init`, `install`, `fmt`, `lint`, `check`, `build`, `run`, `test`.
- `kali build --validate-ir`, `--bundle`, `--lib`, `--capi`, and `--component`.
- `kali test --coverage`.
- Browser runtime summary parsing now also falls back to stdout when the summary file carries a non-integer or negative `testsFailed` field or `null`, and the browser-requested runtime harness now also covers the unparseable-summary fallback case in the checked-in runtime tests; the runtime harness and browser-bundle paths now also rehydrate `hostContract` and `runtimeBackend` from stdout when the summary file carries invalid labels; the newly covered combined invalid-label plus missing-`testsFailed` case now also keeps the summary-file `args` / `tests` payload while taking `testsFailed` from stdout, and they now also fall back to stdout when the summary file carries invalid `args` / `tests` array item types, with the checked-in runtime tests now also covering the invalid `tests` array item-type branch directly, and the CLI runtime smoke now also mirroring the invalid-label, invalid-array-item, and invalid-args browser-summary fallbacks in JSON mode on the browser-requested run path, while the browser-requested `test` path now also mirrors the missing-`testsFailed` summary fallback in first-class `.js` input, and the browser-requested `run` JSON smoke now also mirrors the unparseable-summary fallback path in `.js` input, keeping the browser-harness summary count deterministic across partially corrupted summary payloads.
- Runtime smoke coverage includes `Object.keys()`, `Object.entries()`, and `Object.values()` enumeration semantics alongside the earlier arithmetic, async/await, and built-in regressions, and the dedicated `Reflect.ownKeys` JS-input `test` lane, now including overwrite/delete-reinsert ordering plus `Math.abs()` and `Math.sign()` alongside the existing `Math.max()` / `Math.min()` coverage, now also mirrors BigInt addition into `.js` run/test coverage and browser bundle smoke coverage in both TS and `.js` inputs, now also exercises the Web baseline randomness subset via `crypto.getRandomValues()` on mirrored `.ts` and `.js` run/test coverage, now also mirrors arithmetic precedence and array literal length into `test`-path `.js` smoke coverage in addition to the existing Math built-in mirrors on both TS and `.js` inputs, now also covers optional chaining through direct `.js` run/test smoke coverage, and now also covers strict equality/inequality checks through the runtime path with mirrored `.js` run/test coverage. Browser bundle runtime smoke now also pins overwrite-delete-reinsert ordering for the `Object.keys()` / `Object.entries()` / `Object.values()` path in both TS and `.js` inputs, and the browser-bundle overwrite-delete-reinsert ordering lane now also has JSON-output coverage in both source classes. Browser bundle string-primitive enumeration now also has explicit TS/JS smoke plus JSON-output coverage in the same bundle lane. The same runtime smoke matrix now also has a `.js` `test`-path console-routing regression so the runner output split stays explicit in both modes. The frontend now also parses object literal expressions with identifier, string, and numeric keys plus shorthand identifier properties and literal computed keys that canonicalize to static property names, which is the prerequisite for those runtime-smoke object-expression fixtures to reach lowering instead of failing in the parser. Integer-like key ordering now has positive runtime, test, and browser-bundle coverage across TS and `.js` inputs; the aggregate-literal codegen path carries the 4-key ordering cases, and the browser-harness smoke matrix can exercise the supported ordering path instead of rejecting it. Browser bundle console-level routing and `console.assert()` false-branch reporting now also have TS and `.js` coverage in the runtime-smoke corpus, and the browser-requested `run` / `test` harness path now also mirrors the same console-level routing and assertion coverage in `.js` input, including the signed-numeric `console.log(-1)` regression, keeping the browser-targeted execution evidence aligned across input classes. The browser-requested `run` / `test` harness now also exercises the shared web-baseline `structuredClone` / `AbortController` / `Event` / `EventTarget` / `CustomEvent` / `URL` / `URLSearchParams` / `TextEncoder` / `TextDecoder` primitives in both TS and `.js` input, including JSON-output coverage on the browser-requested `run` and `test` paths, and the browser bundle runtime smoke mirrors the same primitives in both TS and `.js` input as part of the browser-targeted bundle path. Browser bundle runtime smoke now also covers `Math.ceil()` in both TS and `.js` inputs with JSON-output coverage in both source classes, keeping the browser-targeted bundle path aligned with the same integer-rounding subset that the standalone runtime smoke already pins, and the integer-only math pipeline now explicitly rejects non-integer literal operands before they can fall through to literal encoding; standalone `run` / `test` JSON smoke now also pins that non-integer-literal gate in `.js` input. The inherited browser `apiSurface` build/bundle path now also rejects unsupported `Math.floor()` / `Math.round()` calls in `.js` input with matching JSON-output coverage.
- The parser now accepts async function declarations/expressions, async arrow function expressions, plus optional return type annotations on arrow functions, and async generator syntax as AST forms, plus generator function syntax and `yield` expressions, while typechecking still gates generator lowering with the canonical `E5506` path until a real lowering packet lands; that rejection is now covered on the `check`, `build`, `run`, and `test` CLI smoke paths, including async generator syntax and mirrored async-generator `.js` fixtures plus mirrored async-generator function-expression `.js` fixtures, the generator-delegating `yield*` rejection now also has mirrored `.js` coverage on the `check` / `build` lanes, and the browser-targeted `check` / `build --bundle` smoke lane now also pins the same generator-lowering rejection in the browser analysis/build context. The browser-targeted `yield*` rejection is now mirrored there too in both TS and `.js` inputs, and the same generator-delegating `yield*` gate now also has matching JSON-output coverage on the `build`, `run`, and `test` JS-input lanes. Promise.allSettled now also rejects with the canonical `E5506` diagnostic on the standalone check / run / test paths, keeping the unsupported promise-combinator gate explicit across the front-end and execution smoke; the standalone build helper and runtime-smoke check lane now also reject bracketed and `globalThis`-bracketed `Promise.allSettled` source forms, keeping that gate explicit across the common property-access variants. The browser-targeted `check` / `build --bundle` lanes and browser-requested `run` / `test` harness now also mirror the same gate in `.js` input, including JSON-output coverage where applicable, and the checked-in browser-specific regressions now also cover the inherited browser-api-surface path explicitly. The plain non-async arrow return-type-annotation parser shape is now also covered directly, so that return-annotation lane stays explicit alongside the async-arrow path.
- `kali effects`, `kali package-effects`, and `kali package-audit`.
- Install integration now also rejects `kali install --allow-scripts` on clean workspaces in JSON mode, and now also rejects the raw-URL-only install-work case in both human and JSON modes plus the raw-URL and JSR invalid-combination cases in JSON mode, matching the library-layer empty-work rejection and keeping the no-op hook gate explicit at the CLI boundary.
- Deno-oriented baseline plus documented Node subsets for source-graph and effect/reporting workflows; the explicit Node API surface now also builds library-oriented artifacts on `.js` input in both explicit and inherited Node forms, with JSON-output coverage, and the Node analysis-context `effects` lane now also exercises the `node:timers/promises` import path in `.js` input on both explicit and inherited Node selection while still reporting the supported effect slice. The Node API surface now also rejects late Node modules such as `node:stream/promises` and late object-model members and broader `Intl` members in `.js` input across `check` / build / `run` / `test`, including JSON-output coverage, and the broader-Intl Node rejection coverage now explicitly includes `Intl.Collator` and `Intl.Segmenter`; the explicit Node surface now also rejects direct and bracketed `WeakMap` / `WeakSet` / `WeakRef` / `FinalizationRegistry` spellings in `.js` input across `check` / `build` / `run` / `test`, keeping the Node late-compatibility matrix aligned with the standalone/browser gates.
- The explicit `--api deno` smoke lane now also round-trips `Deno.env.set(...)` / `Deno.env.delete(...)` in `.js` input across `check` / `build` / `run` / `test`, including JSON-output coverage, keeping the mutable-environment host slice visible on the documented Deno surface without widening the default standalone contract. The browser-targeted late-compatibility lane now also rejects mutable environment access (`Deno.env.set` / `Deno.env.delete` and bracketed forms) in `.js` input with canonical gating/error paths, keeping the browser host-mutation boundary explicit alongside the other late host-control checks. The Node surface now also rejects `process.env` assignment mutations and bracketed `process["env"]` / `globalThis["process"]["env"]` forms in `.js` input on the shared analysis and execution paths, so the mutable-env gate stays explicit even when the host model exposes `process` for package compatibility, and the inherited Node package-effects lane now also records `Process.EnvWrite` for `process.env` assignment mutations.
- Read-only `Deno.permissions.query(...)` now accepts the documented descriptor subset (`read`, `write`, `env`, `net`) in both direct and computed JS input forms on the `check` / `build` lanes, including bracketed `query` access on direct and inherited `globalThis["Deno"]["permissions"]` paths, and the shared runtime_smoke lane now also mirrors direct bracketed `Deno["permissions"]["query"]` / `Deno.permissions["query"]` forms in both TS and `.js` input, complementing the existing effects and rejection coverage; parser integration coverage now also exercises the bracketed `globalThis["Deno"]["permissions"]["query"]` source form alongside the existing `globalThis["Deno"]["env"]["get"]` bracketed path, now also covers bracketed `globalThis["Object"]["hasOwn"]` and `globalThis["Object"]["prototype"]["hasOwnProperty"]["call"]` source forms, and now also covers bracketed process-control spellings such as `globalThis["process"]["cwd"]`, `globalThis["process"]["exit"]`, and `process["exit"]`.
- The shared runtime_smoke lane now also mirrors the `Deno.env.toObject` materialization gate in `.js` input with text and JSON check / build / test coverage, including bracketed `Deno.env["toObject"]`, `Deno["env"]["toObject"]`, `globalThis["Deno"].env["toObject"]`, `globalThis.Deno["env"]["toObject"]`, and `globalThis["Deno"]["env"]["toObject"]` spellings, and the shared late-process-control helper now also pins direct bracketed `process["pid"]` / `process["cwd"]` / `process["chdir"]` / `process["exit"]` and inherited `globalThis.process[...]` spellings, while the permission-escalation bracketed-source regression now also pins inherited `globalThis["Deno"]["permissions"]["request"]()` / `revoke()` spellings too, and the compile-time `build_source_file` mirror now also rejects the mixed inherited bracket/dot `globalThis["Deno"].permissions["request"]()` / `revoke()` spellings in `.js` and `.ts` input, with parser integration now also covering the mixed inherited bracket/dot permission-escalation spellings, and the dedicated permission-escalation helper coverage now also reaches `.jsx` and `.tsx` input for both the direct and mixed bracket/dot shapes, keeping the later object-materialization follow-up explicit in the broad smoke lane. The browser-targeted late-compatibility lane now also rejects the same `Deno.env.toObject` forms in `.js` input with text and JSON coverage, including the same bracketed env-materialization spellings, and now also rejects recognized-but-unavailable permission-escalation members (`Deno.permissions.request()` / `revoke()` plus bracketed forms) in `.js` input with matching text and JSON coverage, while the browser late-compatibility source fixtures now also pin the fully bracketed `globalThis["Deno"]["permissions"]["request"]()` / `revoke()` spellings alongside the existing mixed-bracket variants, keeping the browser analysis/build gate aligned with the same later host-API boundary. The standalone `test` lane now also covers bracketed `Deno["env"]["get"]` / `globalThis["Deno"]["env"]["get"]` access in `.js` input, and `build_tests.rs` now also mirrors the direct bracketed `Deno["env"]["get"]` and `Deno["permissions"]["query"]` forms alongside the existing `globalThis["Deno"]["env"]["get"]` build-path mirror; the build-path JS-input smoke now also mirrors the bracketed `globalThis["Deno"]["env"]["get"]` form too, and now also mirrors `globalThis["Deno"]["permissions"]["query"]` on the read-descriptor path, and now also mirrors the same bracketed `globalThis["Deno"]["env"]["get"]` form in `.js` input, keeping the executable build mirror aligned with the runtime-smoke source-shape matrix; the `effects` / `package-effects` smoke now also mirrors the same bracketed env-read source shape as computed host access with `Process.EnvRead`, while supported const-bound permission-query descriptor aliases on the static callee path now stay effect-free in the public effects/package-effects surface, and the same const-bound descriptor aliases now also have `run` / `test` runtime acceptance coverage in `.ts` and `.js` input, and the dedicated `crates/kali_cli/tests/permission_query_js_input.rs` corpus now mirrors that acceptance on the JS-input `run` lane in both human and JSON modes, keeping the read-only env source-shape coverage aligned with the rest of the host-identity bracketed-path regressions; parser integration coverage now also exercises the bracketed `globalThis["Deno"]["env"]["get"]`, `Deno["permissions"]["query"]`, `globalThis["Deno"]["permissions"]["query"]`, and `globalThis["Deno"]["pid"]` source forms alongside the existing `globalThis["Deno"]["exit"]` bracketed path, and now also exercises bracketed `globalThis["Intl"]["DateTimeFormat"]`, `globalThis["Intl"]["DisplayNames"]`, `globalThis["Intl"]["PluralRules"]`, `globalThis["Intl"]["Locale"]`, `globalThis["WeakRef"]`, `globalThis["process"]["cwd"]`, `process["exit"]`, and `globalThis["Proxy"]["revocable"]` source forms so the late-compatibility source-shape regressions stay explicit before the checker normalizes them to canonical gates. The lower-level `build_source_file` helper now also rejects broader Intl members such as `Intl.DateTimeFormat` / `Intl.RelativeTimeFormat` / `Intl.PluralRules` / `Intl.Collator` / `Intl.DisplayNames` / `Intl.Segmenter` / `Intl.Locale` alongside bracketed `globalThis["Intl"]["DateTimeFormat"]` / `globalThis["Intl"]["RelativeTimeFormat"]` / `globalThis["Intl"]["Collator"]` / `globalThis["Intl"]["DisplayNames"]` / `globalThis["Intl"]["Segmenter"]` / `globalThis["Intl"]["Locale"]` in `.ts`, `.js`, `.jsx`, and `.tsx` input, the lower-level type-resolution coverage now also pins `Intl.DisplayNames` and `Intl.Locale` as unavailable alongside `Intl.NumberFormat`, and the same helper now also rejects `Deno.env.toObject`, the weak-reference family (`WeakMap` / `WeakSet` / `WeakRef` / `FinalizationRegistry`), and threaded-runtime globals (`SharedArrayBuffer` / `Atomics`) in `.ts` and `.js` input, keeping the compile-time mirror aligned with the broader late-compatibility gate. The same helper now also rejects unsupported permission-query descriptors (`ffi` and `sys`) in `.ts` input on the same direct and bracketed `globalThis["Deno"]["permissions"]["query"]` forms, keeping the compile-time mirror aligned across input classes as well. The lower-level `build_source_file` helper now also accepts the same supported const-bound permission-query subset in `.jsx` and `.tsx` input, keeping the compile-time mirror aligned with the widened source-class matrix.
- Browser-targeted `check` and `build --bundle` remain distinct from standalone browser runtime support, while the standalone browser-requested `run`/`test` harness coverage now also exercises inherited browser `apiSurface` configs when `KALI_BROWSER_BUNDLE_HARNESS_COMMAND` is configured, including browser package-resolution coverage on the inherited path, and the browser-requested `test` package-resolution lane now also has JSON-output coverage on `.js` input. The browser bundle `--format cjs` path now also has first-class `.js` coverage in both explicit and inherited browser `apiSurface` modes, including sandbox-policy acceptance in those JS-input lanes. The browser runtime corpus now also mirrors the browser package fixtures on `.js` input for both `run` and `test`, and now also exercises the browser web-baseline package slice on `.js` input for both `run` and `test` plus the same slice on the inherited browser `apiSurface` path, with JSON-output coverage for that browser runtime slice now also pinned on the same first-class JavaScript path, plus the typed export branch packages on `.js` input for both browser runtime commands, and the basic browser-requested `run` / `test` acceptance lane now mirrors `.js` inputs too, so the browser-requested execution surface stays aligned with first-class JavaScript compilation and now also includes object-enumeration coverage in both JS and TS input classes, plus integer-like key ordering and string-primitive enumeration coverage in `.js` input, and the browser-requested run harness now also accepts the bracketed `Deno["pid"]` / `globalThis["Deno"]["pid"]` process-identity query in `.js` input with JSON-output coverage, plus `Math.max()` / `Math.min()` / `Math.abs()` / `Math.sign()` semantics in both TS and JS input classes, boolean conjunction/disjunction semantics and strict equality / inequality semantics in both TS and `.js` input classes, with JSON-output coverage now also pinned for the browser-requested strict-equality lane in both source classes, and the matching non-JSON TS-input browser-requested run/test mirrors for the boolean-logic and strict-equality rows now also land, and the Web Crypto randomness subset via `crypto.getRandomValues()` on mirrored `.ts` and `.js` input. The same browser-requested execution surface now also exercises browser Web Crypto `crypto.subtle.digest()` / `crypto.randomUUID()` semantics on mirrored `.js` input, and now also exercises `try/catch` exception handling and `try/finally` sequencing in both TS and `.js` inputs under the configured browser harness, including JSON-output coverage, and the inherited browser `apiSurface` path now also mirrors that same `try/catch` exception handling and `try/finally` sequencing in both TS and `.js` inputs with JSON-output coverage, plus optional chaining and console-level routing in `.js` input. Browser-targeted `check` / `build --bundle` coverage now also rejects late Deno/process host-control members on mirrored `.js` input, while the default standalone surface now accepts the read-only `Deno.pid` / `Deno["pid"]` / `globalThis.Deno.pid` process-identity query and still rejects cwd/chdir/exit plus the process-global late host-control members; the runtime and codegen smoke now also cover both `Deno.pid` and `globalThis.Deno.pid` end-to-end, the standalone `check` / `build` / `run` smoke paths now also cover both spellings directly in `.js` input, and the lower-level `build_source_file` helper now also mirrors the same read-only pid forms in `.ts` and `.js` input, including the bracketed `globalThis["Deno"]["pid"]` spelling, and the standalone `run` / `test` / `build` JSON paths now also have explicit coverage for both `Deno.pid` and `globalThis.Deno.pid`, with the standalone `check` JSON path now also having explicit coverage for `Deno.pid`, `globalThis.Deno.pid`, and bracketed `globalThis["Deno"]["pid"]` in `.js` input, while the standalone `test` JSON path now also has explicit coverage for direct `globalThis.Deno.pid` in `.js` input. Browser-targeted `check` / `build --bundle` still reject the remaining late host-control and late object-model members on mirrored `.js` input, including broader Intl with explicit `Intl.DateTimeFormat` / `Intl.NumberFormat` / `Intl.RelativeTimeFormat` / `Intl.PluralRules` assertions and bracketed `Deno["cwd"]` / `Deno["chdir"]` / `Deno["exit"]` spellings plus the corresponding `globalThis.Deno[...]` forms, keeping the browser ambient surface separated from later standalone host-control and object/runtime APIs. Standalone `check` / `run` / `test` / `build` now also reject the same late host-control and late object-model compatibility members on mirrored `.js` input with matching text and JSON-output coverage, and the inherited browser `apiSurface` path now mirrors the late object-model rejection on `run` / `test` in `.js` input too, and the browser-requested `run` / `test` harness path now also rejects `Deno.env.toObject` in both direct and inherited browser `apiSurface` forms on mirrored `.js` input, including JSON-output coverage, and the shared runtime_smoke lane now also mirrors `Object.hasOwn` / `Object.prototype.hasOwnProperty.call` in the broad smoke lane, and the standalone `run` / `test` sandbox lane now also rejects the phase-three Deno host-effect slice in `.js` input with JSON-output coverage, so the late-compatibility and sandbox-validation gates stay explicit across the full standalone command matrix. Browser bundle runtime smoke now also exercises `crypto.getRandomValues()` plus `crypto.subtle.digest()` / `crypto.randomUUID()` in both `.ts` and `.js` inputs, plus async/await sequencing in both `.ts` and `.js` inputs, keeping the browser bundle lane aligned with the same await-order and browser crypto regressions the standalone runtime smoke already pins. The standalone run/test smoke matrix now also exercises queueMicrotask ordering on `.js` input, including JSON-output coverage, keeping microtask scheduling aligned with the async/await baseline. The browser-requested queueMicrotask lane now also covers the inherited browser-api-surface path in both TS and `.js` input with JSON-output coverage. The default standalone run/test JSON smoke now also mirrors `Math.imul()` in `.js` input, keeping the basic built-in lane aligned with the existing `Math.clz32()` JSON regression. Browser bundle try/catch sequencing now also has dedicated TS and `.js` coverage, and browser bundle `Object.keys()` / `Object.entries()` / `Object.values()` smoke now also exercises string-primitive enumeration in both `.ts` and `.js` inputs, while the same browser bundle lane now also pins overwrite/delete-reinsert ordering in both TS and `.js` inputs, and now also exercises basic strict equality / inequality semantics in both TS and `.js` inputs, keeping browser exception handling and basic enumeration aligned with the same first-class-JavaScript bundle path. Browser bundle queueMicrotask ordering now also has JSON-output coverage in both `.ts` and `.js` input, keeping the machine-readable browser-bundle envelope aligned with the existing execution smoke. Browser-entrypoint alias smoke now also mirrors the run-side browser alias families onto the `test` command, including chromium-, chrome-, Google Chrome stable/headless-shell, Edge-, Opera-, and Vivaldi-family executables plus representative Brave, Firefox, privacy-browser, chromium-dev, chrome for testing, edge, command-wrapped aliases, and the generic `msedge` / `opera` / `vivaldi` aliases; `privacy-browser` is now explicitly present in the runtime alias list, and the remaining alias smoke now also pins `chromium-dev`, `chrome for testing`, `edge`, and `privacy-browser`, keeping the browser harness command split explicit across both runtime commands. The browser-harness launch-failure path is now also exercised directly, keeping resolved command and script details stable when the configured executable cannot be spawned.
- The inherited browser `apiSurface` path now also has JSON-output coverage for `Math.clz32()` in `.js` input on browser-requested `run` / `test`, keeping the machine-readable browser-harness math baseline aligned with the existing direct-path coverage.
- Standalone `check` / `run` / `test` now also carry matching JSON-output rejection coverage for late compatibility members in `.js` input, and the non-browser `build --sandbox` path now likewise rejects the phase-three Deno host-effect slice in `.js` input with JSON-output coverage, keeping the static sandbox-validation story aligned across check and build. The shared runtime_smoke lane now also mirrors recognized-but-unavailable permission-escalation members into `.js` `check` / `build` rejection coverage, including direct, computed, and bracketed forms with JSON-output assertions, and the late-compatibility object-model JSON assertions now also pin bracketed `Proxy` / `Object.hasOwn` / `Weak*` spellings, while the standalone late-compatibility JS smoke now also asserts the dot-form `globalThis.Object.hasOwn` / `globalThis.Object.prototype.hasOwnProperty.call` spellings alongside those bracketed forms, so the JavaScript sandbox-validation boundary stays explicit there too. The sandbox effect-analysis tests now also pin computed `Deno.connect` / `Deno.listen` / `Deno.serve` host-call access in `.js` input, keeping the network/listener slice explicit under the same contract. The browser late-compatibility source fixtures now also pin bracketed string-literal Deno/process-control and object-model forms, including the `WeakMap` / `WeakSet` / `WeakRef` / `FinalizationRegistry` family, the direct bracketed `process["pid"]` / `process["cwd"]` / `process["chdir"]` / `process["exit"]` forms, and the bracketed `globalThis["WeakRef"]` form, so the source-shape regression stays explicit alongside the existing rejection coverage. The lower-level `build_source_file` helper now also rejects `Promise.allSettled` in `.js` input, and now also rejects broader Intl members such as `Intl.DateTimeFormat` / `Intl.RelativeTimeFormat` / `Intl.PluralRules` / `Intl.Collator` / `Intl.DisplayNames` / `Intl.Locale` alongside bracketed `globalThis["Intl"]["Segmenter"]`, keeping the compile-time mirror aligned with the broader late-compatibility gate.
- Browser-targeted package-corpus coverage now includes `.js` entrypoints for browser module-entry packages and browser replacement-map packages, including a scoped package case and a `vue/runtime-dom` browser replacement-map JS-entrypoint case, keeping first-class JavaScript compilation honest in the browser analysis/build lane. The browser corpus now also mirrors browser string-entry, string-export, browser-condition export, dual-exports, and pattern-exports fixtures onto `.js` input, and the browser condition / browser-deno preference packages now also have JSON-output coverage on the mirrored `.js` browser-targeted `check` / `build --bundle` path, so the browser deployability evidence stays aligned across the JS and TS lanes for those package shapes too. The browser corpus now also exercises the host-heavier `@mariozechner/pi-coding-agent` package-content probe on `.js` input for `check` and `build --bundle`, and the direct and inherited browser-harness rows now also carry JSON-output coverage, keeping the browser package-content slice distinct from the separate standalone bin-entrypoint rejection. The browser runtime corpus now also exercises the typed export branch packages on `.js` input for `run` and `test`, and the canonical pure-JS `semver` probe on `.js` input for `run` and `test` when the browser harness command is configured, including JSON-output coverage for both browser runtime semver lanes, while the browser-targeted `check` / `build --bundle` semver slice now also has JSON-output coverage on `.js` input, keeping the browser execution evidence aligned with the same first-class-JavaScript package shape. The browser runtime corpus now also exercises the host-heavier `@mariozechner/pi-coding-agent` package-content probe on `.js` input for `run` and `test`, and the same direct and inherited browser-harness slices now also have JSON-output coverage, so the browser execution evidence stays aligned with the same package shape. The browser runtime corpus now also exercises minimized mixed CommonJS/ESM interop packages on `.js` input for `run` and `test`, including explicit and inherited browser `apiSurface` coverage, keeping the browser execution snapshot aligned with the browser-targeted interop slice. The browser runtime corpus now also exercises browser internal-browser-rewrite packages on `.js` input for `run` and `test`, including direct and inherited browser `apiSurface` coverage, keeping the browser execution snapshot aligned with the browser rewrite-chain slice too. The browser runtime corpus now also exercises browser pattern-exports packages on `.js` input for `run` and `test`, including the `hono/client` and `solid-js/web` slices, and browser module-only packages and browser module-entry-chain packages on `.js` input for `run` and `test`, including direct JSON-output coverage for the module-entry-chain browser-harness path, and the package-corpus matrix drift test now also pins those browser runtime rows, so the browser execution snapshot stays explicit for those module-resolution shapes too. The browser package fixtures now also have JSON-output coverage on both the direct and inherited browser harness paths. The same drift test now also pins the browser-targeted semver row exactly once, keeping the targeted semver slice duplication-checked alongside the browser runtime rows.
- The browser-targeted browser-blocked package slice now also has an inherited browser `apiSurface` companion on `.js` input for `check` / `build --bundle`, keeping the browser exclusion contract aligned with the browser runtime harness.
- Default standalone package-corpus coverage now also mirrors its minimized mixed-format CommonJS/ESM interop fixture on `.js` input for `run`, `test`, and `build`, and the canonical semver probe now also covers `.js` input on `check` / `build` / `run`, keeping first-class JavaScript package evidence aligned with the existing TypeScript lane. The broader default standalone utility corpus now also mirrors the `date-fns` slice onto `.js` input across `check` / `build` / `run` / `test`, while the zod, p-limit, and ms slices continue to mirror onto `.js` input for `check` / `build` / `run` / `test`, the pattern-exports utility slice now mirrors `.js` input across `check` / `build` / `run`, and the scoped-package utility slice now also mirrors `.js` input across `check` / `build` / `run`, keeping the pure-JS utility ladder aligned with first-class JavaScript compilation as well.
- Node package-corpus coverage now also exercises the canonical pure-JS `semver` probe on `.js` input across the documented Node `check` / `build` / `run` / `test` lanes, and now also adds dedicated `node:buffer`, `node:assert`, `node:child_process`, `node:events`, `node:stream`, `node:fs/promises`, `node:path`, `node:os`, `node:crypto`, `node:fs`, `node:url`, and `node:util` built-in package slices on `.js` input across the same Node lanes, keeping the Node compatibility slice aligned with first-class JavaScript compilation instead of only the TypeScript lane.
- Node-assuming package corpus coverage now also exercises the documented Node `check` / `build` / `run` / `test` surface on `.js` input, and the Node `test` runtime-smoke path now also has a direct `.js` smoke probe for `Kali.test(...)` under `--api node`, keeping the Node-only host-API slice distinct from the runner and pure-JS package probes.
- Node package-corpus coverage now also mirrors the runner-package exports-map and mixed-format-entry slices onto `.js` input for the explicit Node surface, keeping the Node package-resolution evidence aligned with first-class JavaScript compilation instead of only the TypeScript lane.
- The canonical host-heavier `@mariozechner/pi-coding-agent` probe now also includes package-content check/build coverage on the default standalone surface, and the same `.js` package-content slice now also executes on the default standalone surface, keeping the package-content rung distinct from the separate published bin-entrypoint probe.
- Browser bundle chunk smoke coverage now also exercises literal and const-bound dynamic imports from `.js` input, and the browser bundle runtime smoke now also exercises the dynamic-import loader for `.js` input including directory-index targets, plus `Math.max()` / `Math.min()` / `Math.abs()` / `Math.sign()` semantics, boolean conjunction/disjunction semantics, and `try/finally` sequencing in both `.ts` and `.js` inputs, plus direct JSON-output coverage for `performance.now()` monotonic ordering in `.js` input, so the browser `build --bundle` lane keeps the linked-graph chunking and builtin-lowering paths aligned with first-class JavaScript compilation.
- Deno package-corpus coverage now also exercises a canonical `jsr:@std/path` package fixture materialized at `node_modules/@std/path` on the Deno surface, keeping the `jsr:` registry prefix and on-disk path mapping honest in the package-resolution evidence. The Deno package corpus now also mirrors the host-control package fixtures and the canonical `jsr:@std/path` fixture onto `.js` input for `check`, `build`, and `run`, keeping first-class JavaScript compilation aligned with the Deno package evidence as well.
- `--compat eval` exists for the documented compatibility path, and the `Function()` constructor path now also has matching `check` / `build` `.js` acceptance coverage.
- The threaded runtime profile is now accepted on supported `run`/`test` execution paths when explicitly opted in with `--wasm-threads`, and `check` / `build` / `effects` now also accept that opt-in on the supported non-browser analysis/build paths; positive `--max-threads` values are honored only under that opt-in, and the runtime now also exposes deterministic guest-facing thread-spawn host import plumbing backed by the threaded topology model. The browser-targeted `effects` rejection now also has JSON-output coverage for the browser analysis context, keeping the browser-facing `--wasm-threads` gate aligned across text and JSON modes. The browser-targeted `check` / `build` rejection path for `--wasm-threads` now also mirrors `.js` input on both text and JSON output modes, keeping the browser-analysis/build boundary aligned with first-class JavaScript compilation. The zero-capable thread-budget contract is now also mirrored in JSON smoke coverage for `run` and `test`, and the zero-budget sandbox policy path now also accepts `check --sandbox` and `build --sandbox` in both text and JSON output modes with the same `resources.maxThreads = 0` / `resources.maxSpawnedProcesses = 0` baseline, so `--max-threads 0` stays accepted in both machine-readable and human-readable execution lanes. The browser-requested run/test zero-capable budget pair now also has matching human-output coverage in both source classes, complementing the existing JSON smoke. Positive `--max-spawned-processes` overrides on `run`/`test` now reject with the canonical availability gate in both text and JSON modes until subprocess support itself exists. The runtime-smoke suite now also mirrors `SharedArrayBuffer` / `Atomics` rejection coverage onto `.js` inputs for `check`, `run`, and `test`, keeping the threaded-runtime boundary aligned with first-class JavaScript compilation. The standalone and browser late-compatibility JS-input smoke now also pin bracketed `globalThis["Deno"]["permissions"]["request"]()` / `revoke()` and `globalThis["Deno"].env["toObject"]` source-shape variants as an explicit regression slice, and the dedicated browser helper assertion now uses the fully bracketed permission path directly. In addition, the inherited `runtimeProfiles = ["wasm-threads"]` path now also reaches the JS-input `build --lib` / `build --capi` / `build --component` artifact modes, keeping the threaded build-artifact edge explicit too.
- Schema-v1 outputs and diagnostic envelopes are first-class contracts, including combined inherited analysis-context normalization for `effects` JSON output across `compat.features` and `compilerOptions.runtimeProfiles`, and the test-result schema docs now also pin the nested function-coverage payload shape (`mode`, `files`, and `summary`) for `kali test --coverage`. The native-JSON `package-effects` path now also has pretty-output and quiet-mode coverage, and the package-effects JSON wrapper now also normalizes inherited compat features in the same way as the standalone effect report, and now also pins the `runtimeProfiles` / `compatFeatures` string-item shapes, so the registry-analysis payload contract stays explicit in both presentation modes. The `effects` and `package-effects` schemas are now exact object contracts rather than reserved shells, and the common JSON envelope validator now stays aligned with the schema-permitted extension posture by checking the canonical required keys and primitive types without rejecting future extension fields such as `timings`, and the schema-doc drift net now also pins that timing-item required-key/type contract directly, so the envelope contract stays explicit without being narrower than the published schema. The common JSON envelope schema now also pins the optional `stdout` / `stderr` transport fields and the integer `exitCode` shape, so the shared envelope stays explicit across the ordinary CLI result payloads as well. The common JSON envelope emitter now also sanity-checks the schema-v1 top-level envelope shape before printing, and the emitted diagnostic arrays are now walked during validation so malformed envelope assembly or malformed diagnostic JSON fails fast at the CLI boundary instead of drifting silently into the output stream, including malformed related-info spans nested under `related` items and non-string related-item messages. The same envelope validation now also rejects non-array `errors` and `warnings` payloads at the emission boundary, and it now also rejects inconsistent `success` / `exitCode` pairs when validating malformed envelopes, so diagnostic-array shape errors and envelope outcome mismatches fail fast before serialization. The run/test payload validators now also reject non-string `hostContract` / `runtimeBackend` provenance fields, keeping the machine-readable execution payload contract explicit when those optional fields are present. The `init` payload validator now also rejects non-string `root` / `manifestPath` / `sourcePath` fields and non-boolean `library` values, and the `install` payload validator now also rejects non-string `manifestPath` / `lockPath` values, keeping the scaffold/install machine-contract explicit beyond the happy-path schema shape. Build-result validation now also rejects non-string artifact roles in artifact arrays, and the variant coverage now positively exercises artifact-role-bearing payloads across the `lib` / `bundle` / `capi` / `component` build modes; the validator now also rejects unsupported `artifactKind` values before entering the shape-specific branch logic, and now also rejects non-string `bundleFormat` values on bundle outputs, keeping the optional role field explicit in the emitted build payload contract. The build-result artifact-array validator now also rejects duplicate `(kind, path)` pairs, and `crates/kali_cli/tests/schema_validation.rs` now has a dedicated regression for that duplicate-artifact gate, so the artifact list contract stays explicit alongside the role-based primary-artifact checks. The package-effects validator now also rejects unexpected keys at the top level and in both the nested package coordinate and nested report objects, keeping the registry-analysis payload contract aligned with the published `additionalProperties: false` schema. The CLI README command reference is now also covered by a drift test, including the `doctor` and `test --coverage` examples, so the documented public command surface stays aligned with the current shipped CLI set. The browser library-oriented build-shape contradiction examples now also pin the sandboxed `--lib` / `--capi` browser-API-surface cases in both the CLI spec and README drift net. The doctor corpus now also covers the auto-selected browser-harness branch in human output, and the unavailable-harness-executable regression now also asserts the browser runtime contract snapshot in both human and JSON output, keeping the environment/debug contract aligned across both the JSON and text lanes. The browser-harness doctor regressions now also pin the full contract note set in the auto-selected and unavailable human-output branches plus the quiet pretty-JSON auto-selected branch, including the browser runtime host-description note, and a dedicated human-output regression now also pins the browser harness summary and browser runtime contract block for the env override path, so the environment/debug contract stays explicit across presentation modes. The doctor payload validator now also rejects empty `browserRuntimeContract.hostDescriptionNote` strings, duplicate `supportedCommands` / `diagnosticNotes` entries, and has nested unexpected-key coverage for both the browser harness and browser runtime contract sub-objects, keeping the schema hardening path explicit alongside the other JSON-contract checks. The binding-package manifest loader now also validates the schema-v1 required field types for `moduleName`, `hostAbiVersion`, `minHostAbiVersion`, and the generated artifact paths in `artifacts.library`, `artifacts.metadata`, `artifacts.exportsHeader`, and `artifacts.glue`, so the sidecar manifest contract stays aligned with its published schema instead of only checking field presence.
- Schema contract checks now also pin the `schemas/result/package-effects/v1.json` object payload and `schemas/result/package-audit/v1.json` null payload shapes so the registry-analysis schema-v1 documents stay aligned with their CLI contracts, and the specialized artifact-metadata projection schemas (`lib-wit`, `capi`, `component`) now have explicit contract checks that keep them anchored to the shared base artifact metadata contract.
- The schema-document regression now also covers the supporting diagnostic, manifest, lockfile, and sandbox-policy schema documents, keeping the config/policy/result sidecar contracts aligned with the CLI surface.
- `package-audit` quiet-mode coverage now also suppresses human output while inheriting `eval` plus `wasm-threads`, and the same inherited compat/thread context now also has JSON-output quiet coverage, keeping the context-free registry-analysis command honest when `--quiet` is present. The package-audit package-analysis-specific flag rejections now also short-circuit before registry lookup, so invalid invocations stay network-free as well.
- `package-effects` now also has pretty-output and quiet-mode coverage for the native JSON payload path, plus JSON-output rejection coverage for an inherited threaded runtime profile in the default standalone package-analysis context; the quiet JSON path now also rejects a mixed inherited `eval` + `wasm-threads` context deterministically, keeping the registry-analysis gate aligned with the command's inherited-only context model.

## Planning consequence

The old phase documents were historical implementation records. Keeping them as active checklists caused stale references to completed tasks. Active planning now starts from the current baseline and focuses on remaining spec-owned work only.

## Non-negotiable guardrails

Future phases must preserve:

- AOT-only guest-language compilation.
- Pure Rust implementation contract.
- No tracing/background GC.
- Sandbox-first honesty.
- Deterministic machine contracts.
- Public availability discipline from `specs/19-feature-maturity.md`.
- Proof-backed claim discipline from `proofs/BOUNDARY.md`.
