# Phase 6 — Semantic Conformance and Frontend Depth

## Goal

Move from broad working coverage to measurable language conformance for the surfaces Kali already claims or intends to claim next.

## Owning specs

- `specs/02-lexer-parser.md`
- `specs/03-ast.md`
- `specs/04-type-system.md`
- `specs/05-ir.md`
- `specs/10-runtime.md`
- `specs/16-testing.md`
- `specs/19-feature-maturity.md`

## Work packets

### 6.1 Conformance inventory

- Status: delivered in the current repository snapshot as [`conformance-dashboard.md`](./conformance-dashboard.md).
- The dashboard groups supported, gated, and rejected ECMA/TS features and ties each row to tests or canonical diagnostic gates.
- Keep draft TC39 proposals rejected by default unless a specific opt-in is specified.

### 6.2 Checker and inference hardening

- Expand JS inference fixtures within the bounded inference contract.
- Add negative tests for annotation-required boundaries.
- Avoid open-ended cross-module/public-API solver claims until a later evidence-backed packet exists.
- Current progress: added `.js` fixture coverage for directory-index and parenthesized const-bound dynamic import resolution in `crates/kali_types/src/tests.rs`, plus matching build-discovery coverage for `.js` directory-index and parenthesized const-bound chunks in `crates/kali_cli/src/build_tests.rs`, and a `.js` base-library artifact smoke test in `crates/kali_cli/tests/runtime_smoke.rs`; the same library-artifact lane now also rejects sources without a statically known export surface on `.js` input, including JSON-output coverage, so the exact-version consumer boundary stays explicit in the first-class JavaScript path too.
- Current progress: unresolved named exports and default exports at a module public boundary now fail with the canonical undefined-identifier diagnostic in `crates/kali_types/src/tests.rs`, so the annotation-required/public-boundary negative coverage called out by the phase plan is no longer just prose. The same public-boundary rejection is now also mirrored in `.js` input coverage, including the unresolved default-export alias form, and the shared type-annotation resolution fixtures now mirror `.js` input too so the bounded-inference annotation gate stays covered on the first-class JavaScript path.
- Current progress: the phase-3-budgeted cross-module inference fixtures now include mirrored `.js` cases for both the direct call-chain and the explicit specialization-cap variants in `crates/kali_cli/tests/runtime_smoke.rs`, and `crates/kali_types/src/tests.rs` now also carries a mirrored `.js` unresolved public-export alias regression so the JS module-boundary lane stays covered by the same bounded-inference expectations as the TypeScript lanes.
- Current progress: parser now accepts async function declarations/expressions and async generator syntax as AST forms, plus concise arrow function expressions and generator function syntax with `yield` / `yield*` expressions, while the checker and CLI smoke tests still gate generator lowering with the canonical `E5506` availability path instead of letting it reach codegen; that gate is now covered on `check`, `build`, `run`, and `test` smoke paths, including delegated yield syntax, async generator syntax, async generator function-expression syntax, and minimized `.js` input coverage across the `check`/`build` lanes plus matching `run`/`test` `.js` generator-gate fixtures, now including mirrored async-generator `.js` fixtures too and mirrored async-generator function-expression `.js` fixtures on the `check`/`build` and `run`/`test` lanes, including JSON-output coverage for the `.js` async-generator function-expression gate on `check` / `build`, and the `run` / `test` `.js` generator-gate fixtures now also have matching JSON-output coverage, the generator-delegating `yield*` rejection now also has mirrored `.js` coverage on the `check` / `build` lanes, and the browser-targeted `check` / `build --bundle` smoke lane now also pins the same generator-lowering rejection in the browser analysis/build context, including mirrored `.js` input fixtures on both browser lanes plus JSON-output coverage for the browser-analysis and browser-bundle JS-input `yield*` gates. The browser-targeted `yield*` rejection is now mirrored there too in both TS and `.js` inputs, keeping the browser analysis/build generator gate aligned with the rest of the phase-6 matrix. The `for...of` array-iteration rejection now also has mirrored `.js` input coverage plus `build` / `test` CLI smoke coverage, and the `check` / `build` JS-input lanes now also have JSON-output rejection coverage, so the iterator-lowering gate stays explicit across the main command matrix. Object literal expressions with identifier, string, and numeric property names now also parse in the frontend, including shorthand identifier properties, so the existing object-enumeration runtime-smoke coverage can execute through the normal source-graph pipeline instead of tripping the parser on object literals.
- Current progress: read-only `Deno.permissions.query(...)` now accepts the documented descriptor subset (`read`, `write`, `env`, `net`) while rejecting unsupported descriptor kinds with the canonical `E5506` availability path, and runtime-smoke coverage now exercises the full documented subset in both direct and computed JS input forms plus `check`- and `build`-lane JS-input regressions for the same subset, keeping the Deno permission facade aligned with the Phase-1 schema-v1 contract instead of silently treating unknown descriptors as denied. The same facet now also has dedicated `.js` `check` / `build` regressions for unsupported descriptors such as `ffi` and `sys`, including JSON-output coverage, so the first-class JavaScript path stays explicit on the rejection side too.
- Current progress: numeric object property names now lower as quoted string literals in `crates/kali_hir/src/lib.rs`, which keeps property-key canonicalization from conflating them with numeric value literals and gives the object-enumeration path a cleaner foundation for integer-like ordering support. MIR layout inference now also canonicalizes integer-like property keys before string keys, and the optimizer re-runs object-enumeration folding after binding specialization using collected constant bindings so the literal-object helper can see more resolved shapes; the codegen aggregate-literal path now also carries the 4-key integer-like smoke fixtures instead of rejecting them, so the runtime/browser smoke matrix can cover the supported ordering path in both TS and `.js` inputs.
- Current progress: the phase-6 conformance dashboard now splits parser-supported generator syntax from the later generator-lowering gate so the supported/gated buckets mirror the actual current snapshot more precisely, and the console-routing regressions now include mirrored `.js` input coverage alongside the existing assertion checks. The same console-routing regression set now also covers the `test` path in `.js` input so the human/machine output split stays explicit in both runner modes, and the dashboard now records the browser-requested and browser-bundle console rows with their JSON-output coverage so the runtime snapshot stays aligned with the current machine-readable evidence too.
- Current progress: `effects` browser-analysis coverage now also mirrors explicit and inherited browser API-surface requests onto `.js` input, including JSON-output coverage for the nullish-coalescing browser-analysis smoke lane, keeping the browser analysis context aligned with first-class JavaScript compilation as well as the existing TS lane. The browser-targeted `effects` lane now also has JSON-output rejection coverage for the threaded-profile gate, so the `--wasm-threads` browser-analysis boundary stays explicit in both text and machine-readable output modes.
- Current progress: the browser-requested standalone run/test harness now also exercises object-enumeration semantics in both TS and JS input classes, including integer-like key ordering and string-primitive enumeration coverage in `.js` input, and the direct browser-requested overwrite-ordering mirrors are now also checked in for `.ts` input, and the browser-harness Math.max() / Math.min() / Math.abs() / Math.sign() / Math.imul() suite in both TS and JS input classes, including the combined `.js` `test`-path mirror added alongside the existing `.ts` regression, plus boolean conjunction/disjunction semantics and strict equality / inequality semantics in both TS and `.js` input classes, with JSON-output coverage now also pinned for the browser-requested JS-input boolean-logic `run` / `test` path, keeping the opt-in runtime path aligned with the same first-class-JavaScript evidence that the browser analysis and build lanes already pin. The same browser-requested run/test lane now also exercises `try/finally` sequencing and queueMicrotask ordering in both TS and `.js` inputs, and now also rejects late process-control members in browser harness coverage. The browser bundle lane now also exercises basic strict equality / inequality semantics, boolean conjunction/disjunction semantics, console-level routing, `console.assert()` false-branch reporting, and queueMicrotask ordering in both TS and JS inputs, and the browser bundle console-level-routing JS-input regression now also has JSON-output coverage, keeping the same browser-targeted execution evidence aligned with the strict-equality baseline already covered on the standalone runtime path. The browser bundle boolean conjunction/disjunction smoke now also has JSON-output coverage in both TS and `.js` inputs, so the browser bundle machine-readable path stays aligned with the same short-circuit regression the human-readable lane already pins. The browser-requested run/test strict-equality lane now also has mirrored TS and `.js` coverage, and the browser bundle integer-like key ordering row now also has JSON-output coverage on both TS and `.js` inputs so the machine-readable browser bundle contract stays aligned with the execution smoke. The same browser-requested run/test harness now also has JSON-output coverage for the TS-input object-enumeration and integer-like ordering slices, keeping the browser-requested machine-readable path aligned with the same overwrite-ordering and string-primitive coverage. The browser bundle math suite now also includes `Math.imul()` in both TS and `.js` inputs.
- Current progress: the browser-requested run/test harness now also mirrors console-level routing and `console.assert()` false-branch reporting in `.js` input, including JSON-output coverage, so the browser harness output split stays aligned with the browser bundle lane.
- Current progress: the phase-6 conformance dashboard now explicitly tracks the browser-requested strict-equality row alongside the browser-requested and browser-bundle integer-like key ordering rows for object enumeration, so the supported ordering and comparison slices stay visible in the phase snapshot alongside the broader browser runtime rows.

### 6.3 Module and dynamic-loading semantics

- Literal-string `import()` over the linked graph is implemented in the current snapshot; standalone `run` / `test` smoke now also covers literal-string dynamic imports in both TS and `.js` input, build-discovery now resolves `.js` literal dynamic import chunks, and the browser-targeted bundle path keeps the linked-graph lowering path honest with directory-style targets. Keep the one-linked-payload rule and preserve the linked-graph lowering path.
- Directory-style linked targets now resolve through `index.*` entries so `import("./dir")` can lower the same way as `import("./dir/index.ts")` when the directory is part of the linked graph.
- Current progress: added minimized package-corpus regressions for mixed CommonJS/ESM default-import interop on the default standalone surface, including an exports-map variant that keeps the interop path honest under the package resolver, and the same utility corpus now also exercises `build` alongside `run`/`test` for the TS and `.js` interop fixtures.
- Current progress: browser-targeted package-corpus regressions now also cover `.js` entrypoints for browser replacement-map packages, including a scoped package case, and now add a minimized mixed CommonJS/ESM browser interop fixture too, so first-class JavaScript compilation stays exercised in the browser analysis/build lane without dropping the mixed-format resolver path.
- Current progress: the default standalone semver corpus probe now also covers `.js` input on `check` / `build` / `run` / `test`, keeping the same pure-JS package fixture aligned with first-class JavaScript compilation on the default standalone surface instead of only the TypeScript lane.
- Current progress: browser bundle chunk smoke coverage now also exercises literal and const-bound dynamic imports from `.js` input, matching the TypeScript bundle chunk regressions and keeping the linked-graph lowering path honest for first-class JavaScript compilation.
- Current progress: browser bundle runtime smoke coverage now also exercises the dynamic-import loader for `.js` input, including directory-index targets, and now also normalizes runtime dynamic import specifiers in `.js` input, so the browser bundle path keeps its first-class JavaScript dynamic-loading behavior mirrored across the JS and TS lanes. The browser-requested run/test harness now also exercises literal-string dynamic imports with directory-index targets in `.js` input, keeping the opt-in browser runtime path aligned with the linked-graph directory-index lowering rule.
- Current progress: optional chaining now also has direct `.js` runtime-smoke coverage on both the `run` and `test` paths, keeping the supported object-access baseline aligned with the first-class JavaScript execution lane.
- Current progress: added `.js` negative coverage for directory dynamic imports without an `index.*` target in `crates/kali_types/src/tests.rs`, and mirrored the non-literal dynamic import rejection onto the `check` / `build` JS input lane in `crates/kali_cli/tests/runtime_smoke.rs`. The build-discovery fixture set now also mirrors comment/string-substring ignores onto `.js` input and adds the parenthesized const-bound target-resolution case on `.ts` input so the scanner stays mirrored across the two source classes.
- Preserve the one-linked-payload rule.
- Keep non-literal `import(expr)` on the later compatibility path and reject it with the canonical `E5506` gate.

### 6.4 Runtime semantic regressions

- Add minimized tests for exceptions, async/await, iterators/generators, built-ins, CJS/ESM interop, and object semantics.
- Current progress: `try/finally` sequencing now has a dedicated runtime-smoke regression alongside the existing try/catch case.
- Current progress: `performance.now()` monotonic ordering now has dedicated browser-requested run/test and browser-bundle `.ts` and `.js` smoke coverage, including inherited browser-api-surface coverage on the run/test and bundle paths plus JSON-output coverage on the inherited browser bundle lane, and the direct browser bundle JSON lane now also pins the same `.js` monotonic-ordering regression, keeping the browser timing baseline explicit alongside the existing microtask regression.
- Ensure unsupported dynamic features produce canonical availability diagnostics rather than silent placeholders.
- Current progress: added regression coverage for arithmetic precedence and array literal length handling, including mirrored `.js` input coverage for both semantics and new `test`-path `.js` mirrors for both semantics, plus function call return semantics and relational comparison semantics, including mirrored `.js` run/test coverage for both semantics, strict equality/inequality semantics, including mirrored `.js` run/test coverage for both the `===`/`!==` path, boolean conjunction/disjunction semantics, including mirrored `.js` run/test coverage, async/await sequencing, including mirrored `.js` input coverage on both the `run` and `test` paths, queueMicrotask ordering on mirrored `.js` `run`/`test` smoke paths, and the phase-6 conformance dashboard now also tracks standalone queueMicrotask ordering in `.js` input, try/catch exception semantics, including mirrored `.js` input coverage on both the `run` and `test` paths, try/finally sequencing, including mirrored `.js` input coverage on both the `run` and `test` paths, browser bundle try/catch sequencing in both TS and `.js` inputs, browser bundle try/finally sequencing in both TS and `.js` inputs, browser bundle async/await sequencing in both TS and `.js` inputs, browser bundle `Object.keys()` / `Object.entries()` / `Object.values()` enumeration semantics in both TS and `.js` inputs including string-primitive enumeration and overwrite ordering, BigInt addition semantics, including mirrored `.js` run/test coverage and browser bundle smoke coverage in both TS and `.js` inputs, a gated `for...of` array-iteration rejection fixture plus a mirrored `.js` input-class rejection fixture now covered by parser/typechecker and CLI smoke tests, including JSON-output coverage on the JS-input `check` / `build` lanes, generator and async generator lowering gates, including mirrored `.js` generator-gate fixtures for `check`/`build` and `run`/`test`, `Object.keys()` / `Object.entries()` / `Object.values()` object-enumeration semantics including overwrite ordering, plus mirrored `.js` coverage for the overwrite-ordering object-enumeration cases on both the `run` and `test` paths, browser-requested `run` / `test` Web Crypto `crypto.subtle.digest()` / `crypto.randomUUID()` semantics on `.ts` and `.js` input too, the Web baseline randomness subset via `crypto.getRandomValues()` on mirrored `.js` `run`/`test` paths, browser-requested run/test string-primitive enumeration in `.js` input, browser-requested console-level routing in `.js` input, browser-requested Web Crypto `crypto.subtle.digest()` / `crypto.randomUUID()` semantics on mirrored `.js` `run`/`test` harness coverage, `Math.max()` / `Math.min()` / `Math.abs()` / `Math.sign()` / `Math.imul()` built-in lowering with mirrored `.js` input coverage and new `test`-path smoke coverage for the same built-ins, browser bundle `console.assert()` false-branch reporting in `.js` input, and `console.error()` / `console.warn()` / `console.info()` / `console.debug()` routing plus `console.assert()` false-branch reporting, including a mirrored `.js` console-level routing regression and a mirrored `.js` `test`-path console-routing regression, in `crates/kali_cli/tests/runtime_smoke.rs`.
- Current progress: the default standalone package corpus now also has a minimized mixed-format interop fixture for `test`, complementing the existing `run` coverage for the same package shape, and the `.js` interop fixture now also covers `build`.
- Current progress: the basic `Object.keys()` / `Object.entries()` / `Object.values()` runtime regressions now also have mirrored `.js` input coverage on both the `run` and `test` paths, so the simpler enumeration path stays aligned with the overwrite-ordering and integer-like-key-ordering regressions already in place.
- Current progress: the standalone `run` / `test` JSON-output envelope now also covers integer-like key ordering in both `.ts` and `.js` input, keeping the machine-readable execution path aligned with the browser-bundle JSON ordering evidence.
- Current progress: browser bundle smoke coverage now also exercises `Math.abs()` semantics in both `.ts` and `.js` inputs, keeping the browser-targeted bundle lane aligned with the same built-in lowering evidence that the standalone runtime smoke already pins for the default surface.
- Current progress: browser bundle runtime smoke now also exercises `Math.max()` / `Math.min()` / `Math.abs()` / `Math.sign()` semantics in both TS and `.js` input, keeping the browser-targeted bundle lane aligned with the same built-in lowering evidence that the standalone runtime smoke already pins for the default surface.
- Current progress: browser bundle runtime smoke now also exercises `try/finally` sequencing in both TS and `.js` inputs, keeping the browser bundle lane aligned with the same exception-sequencing regression the standalone run/test paths already pin.
- Current progress: browser bundle runtime smoke now also exercises async/await sequencing in both TS and `.js` inputs, keeping the browser bundle lane aligned with the same await-order regression the standalone runtime smoke already pins. Browser bundle crypto web API smoke now also mirrors `crypto.getRandomValues()` plus `crypto.subtle.digest()` / `crypto.randomUUID()` in both TS and `.js` inputs, keeping the browser bundle lane aligned with the same Web-baseline crypto evidence the standalone runtime smoke already pins. Browser bundle `Object.keys()` / `Object.entries()` / `Object.values()` smoke now also exercises string-primitive enumeration and overwrite ordering in both TS and `.js` inputs, keeping the browser bundle lane aligned with the same basic enumeration regressions the standalone runtime smoke already pins. Browser bundle boolean conjunction/disjunction smoke now also mirrors both TS and `.js` inputs, keeping the browser bundle lane aligned with the same basic short-circuit semantics the standalone harness already pins.
- Current progress: `DenoRuntimeProjection::new()` now also has unit coverage for the default-open projection baseline, keeping the Phase-1 host-projection contract explicit alongside the rest of the browser/runtime baseline.
- Current progress: the browser-requested `run` / `test` performance.now monotonic-ordering regression now also has mirrored `.ts` input coverage on the direct and inherited browser-harness paths, keeping the browser timing baseline explicit across both source classes.
- Follow-up: generator lowering still needs a dedicated implementation packet for real lowering; the current snapshot keeps that surface gated at resolution/check time rather than miscompiling it, and the remaining coverage work is about keeping the minimized gate fixtures aligned across input classes.
- Follow-up: object-enumeration coverage should keep the overwrite-ordering and integer-like ordering regressions mirrored across TS and JS input classes and across the `run`/`test` paths, while broader object-model widening remains a later-compatibility topic. Keep the browser-requested and browser-bundle smoke lanes aligned with the same supported integer-like ordering path rather than letting them drift back to parser or codegen placeholders.
- Current progress: nullish coalescing now parses as a binary operator token but is explicitly gated with `E5506` in the checker/codegen path, avoiding the previous silent fallthrough that could miscompile `null ?? ...` into a plain `null` result. That rejection is now also mirrored in the browser-targeted `check` / `build --bundle` JS-input lane, including JSON-output coverage, while the browser-analysis `effects` lane continues to report through the same JS input class in both explicit and inherited browser contexts, keeping the rejected-by-default row explicit for compile/gate commands without conflating it with effect-report analysis.

## Exit gate

- Conformance dashboard exists and is deterministic.
- New supported semantics have tests and schema/diagnostic coverage where relevant.
- Maturity rows are updated only for surfaces with evidence.
