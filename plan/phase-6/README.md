# Phase 6 — Semantic Conformance and Frontend Depth

## Goal

Move from broad working coverage to measurable language conformance for the surfaces Kali already claims or intends to claim next.

## Owning specs

- `specs/02-lexer-parser.md`
- `specs/03-ast.md`
- `specs/04-type-system.md`
- `specs/05-ir.md`
- `specs/10-runtime.md`
- `specs/16-testing.md`
- `specs/19-feature-maturity.md`

## Work packets

### 6.1 Conformance inventory

- Status: delivered in the current repository snapshot as [`conformance-dashboard.md`](./conformance-dashboard.md).
- The dashboard groups supported, gated, and rejected ECMA/TS features and ties each row to tests or canonical diagnostic gates.
- Keep draft TC39 proposals rejected by default unless a specific opt-in is specified.

### 6.2 Checker and inference hardening

- Expand JS inference fixtures within the bounded inference contract.
- Add negative tests for annotation-required boundaries.
- Avoid open-ended cross-module/public-API solver claims until a later evidence-backed packet exists.
- Current progress: added `.js` fixture coverage for directory-index and parenthesized const-bound dynamic import resolution in `crates/kali_types/src/tests.rs`, plus matching build-discovery coverage for `.js` directory-index and parenthesized const-bound chunks in `crates/kali_cli/src/build_tests.rs`; that same directory-index slice also now has explicit `.tsx` and `.jsx` coverage in both the type-resolution and build-discovery layers, and the `.js` base-library artifact smoke test plus JSON-output coverage in `crates/kali_cli/tests/runtime_smoke.rs` keeps the exact-version consumer boundary explicit in the first-class JavaScript path too. The `build_source_file_writes_valid_wasm_artifact_in_js_input` regression now also mirrors the unannotated build baseline in `crates/kali_cli/src/build_tests.rs`, keeping the cheap local-inference build path covered on the first-class JavaScript side as well. The type-annotation resolution fixtures now also include a positive `.js` known-name acceptance row so the bounded-inference happy path stays explicit on the first-class JavaScript surface too. The same JS inference lane now also covers nested `Promise<Array<Foo>>`-style known-name resolution in `.js` input so the bounded inference fixture set keeps one deeper generic case pinned.
- Current progress: parser, type checker, and CLI smoke now accept TypeScript assertion / satisfies expressions (`as`, `satisfies`) in `.ts` input, and the checker resolves their referenced type names so the annotation-backed path stays visible in the same bounded-inference lane as the rest of the frontend depth work.
- Current progress: unresolved named exports and default exports at a module public boundary now fail with the canonical undefined-identifier diagnostic in `crates/kali_types/src/tests.rs`, so the annotation-required/public-boundary negative coverage called out by the phase plan is no longer just prose. The same public-boundary rejection is now also mirrored in `.js` input coverage, including the unresolved default-export alias form, and the shared type-annotation resolution fixtures now mirror `.js` input too so the bounded-inference annotation gate stays covered on the first-class JavaScript path. Missing re-export sources now also fail with the canonical import-resolution diagnostic in `crates/kali_types/src/tests.rs`, including mirrored `.js` coverage, so the static ESM boundary stays explicit when the re-export target itself is absent. The same module-boundary negative coverage now also mirrors `.jsx` and `.tsx` input for unresolved public exports and missing re-export sources, and the unresolved default-export / default-export-alias / default-export-function-body regressions now also have matching `.ts`, `.jsx`, and `.tsx` coverage alongside the existing `.js` cases, keeping the bounded-inference and static-ESM edges aligned across the extended source-class matrix. The same JS module-boundary matrix now also exercises an unresolved identifier inside a default-exported function body through both the type-resolution layer and the `check` CLI smoke lane, and now also pins the JSON-output `check` envelope for that JS-input body-resolution path, keeping the public-boundary diagnostics aligned with the first-class JavaScript contract.
- Current progress: the phase-3-budgeted cross-module inference fixtures now include mirrored `.js` cases for both the direct call-chain and the explicit specialization-cap variants in `crates/kali_cli/tests/runtime_smoke.rs`, and the default standalone execution surface now also mirrors the same direct call-chain and explicit specialization-cap patterns in `.js` input there as well, including the standalone `.js` specialization-cap follow-up on the same module chain; `crates/kali_types/src/tests.rs` now also carries a mirrored `.js` unresolved public-export alias regression so the JS module-boundary lane stays covered by the same bounded-inference expectations as the TypeScript lanes, and the same JS inference lane now also covers a deeper `Promise<Array<Promise<Foo>>>` nesting case so the bounded-inference happy path keeps a second nested-generic shape pinned.
- Current progress: parser now accepts async function declarations/expressions and async generator syntax as AST forms, plus concise arrow function expressions and generator function syntax with `yield` / `yield*` expressions, while the checker and CLI smoke tests still gate generator lowering with the canonical `E5506` availability path instead of letting it reach codegen; that gate is now covered on `check`, `build`, `run`, and `test` smoke paths, including delegated yield syntax, async generator syntax, async generator function-expression syntax, and minimized `.js` input coverage across the `check`/`build` lanes plus matching `run`/`test` `.js` generator-gate fixtures, now including mirrored async-generator `.js` fixtures too and mirrored async-generator function-expression `.js` fixtures on the `check`/`build` and `run`/`test` lanes, including JSON-output coverage for the `.js` async-generator function-expression gate on `check` / `build`, and the `run` / `test` `.js` generator-gate fixtures now also have matching JSON-output coverage, with the async-generator declaration gate now also carrying JSON-output coverage on the `check` / `build` / `run` / `test` JS-input lanes, the generator-delegating `yield*` rejection now also has mirrored `.js` coverage on the `check` / `build` lanes, and the browser-targeted `check` / `build --bundle` smoke lane now also pins the same generator-lowering rejection in the browser analysis/build context, including mirrored `.js` input fixtures on both browser lanes plus JSON-output coverage for the browser-analysis and browser-bundle JS-input `yield*` gates. The browser bundle JSON lane now also keeps the TS-input `yield*` rejection mirrored for the same browser-bundle machine-readable gate. The browser-targeted `yield*` rejection is now mirrored there too in both TS and `.js` inputs, keeping the browser analysis/build generator gate aligned with the rest of the phase-6 matrix. The `for...of` array-iteration rejection now also has mirrored `.js` input coverage plus `build` / `test` CLI smoke coverage, and the `check` / `build` JS-input lanes now also have JSON-output rejection coverage, so the iterator-lowering gate stays explicit across the main command matrix. Parser coverage now also accepts `await` expressions inside async functions and `for await...of` syntax in the frontend, keeping the async grammar surface aligned with the AST and checker layers while the lowering path stays gated; the same iterator-lowering gate now also rejects `for await...of` array iteration in browser-targeted `check` / `build --bundle` and browser-requested `run` / `test` `.js` input, keeping the browser-facing matrix aligned with the standalone run/test gate. Object literal expressions with identifier, string, and numeric property names now also parse in the frontend, including shorthand identifier properties, so the existing object-enumeration runtime-smoke coverage can execute through the normal source-graph pipeline instead of tripping the parser on object literals.
- Current progress: read-only `Deno.permissions.query(...)` now accepts the documented descriptor subset (`read`, `write`, `env`, `net`) while rejecting unsupported descriptor kinds with the canonical `E5506` availability path, and runtime-smoke coverage now exercises the full documented subset in both direct and computed JS input forms, including bracketed `query` access on both the direct and inherited `globalThis["Deno"]["permissions"]` paths; `check`- and `build`-lane JS-input regressions cover the same direct/computed/bracketed forms as well, and the shared runtime_smoke lane now also mirrors direct bracketed `Deno["permissions"]["query"]` / `Deno.permissions["query"]` source forms in both TS and `.js` input, while the inherited `globalThis.Deno.permissions.query` / `globalThis.Deno.permissions["query"]` path now mirrors the same supported and rejected coverage too, keeping the Deno permission facade aligned with the Phase-1 schema-v1 contract instead of silently treating unknown descriptors as denied. The type-resolution layer now also accepts const-bound descriptor aliases for that same read-only subset in `.ts` and `.js` input, and the `check` / `build` smoke now mirror those const-bound aliases in `.ts` and `.js` input too; the same const-bound descriptor aliases now also have `run` / `test` runtime acceptance coverage in `.ts` and `.js` input, the dedicated `crates/kali_cli/tests/permission_query_js_input.rs` corpus now mirrors that acceptance on the dedicated JS-input `run` lane in both human and JSON modes, and the `build_source_file` unit path now also mirrors `Deno.env.get(...)` in both `.ts` and `.js` input, now also covers bracketed `Deno["env"]["get"](...)` in `.ts` and `.js` input, now also covers bracketed `globalThis["Deno"]["env"]["get"](...)` in `.ts` input, and now also covers the same bracketed `globalThis["Deno"]["env"]["get"](...)` form in `.js` input, while also mirroring the same supported const-bound permission-query subset in `.js` input, with a matching `.ts` build-unit regression for that same permission-query alias set. The public `effects` and `package-effects` surfaces now keep the supported const-bound descriptor path effect-free on the static callee form, while computed callee forms still carry `computed-host-access`. The same facet now also has dedicated TS and `.js` `check` / `build` regressions for unsupported descriptors such as `ffi` and `sys`, including JSON-output coverage and bracketed `globalThis["Deno"]["permissions"]["query"](...)` source forms, and the inherited `globalThis.Deno.permissions.query` / `globalThis.Deno.permissions["query"]` path now carries the same rejection coverage too, so the first-class JavaScript path stays explicit on the rejection side too. The recognized-but-unavailable permission-escalation members now also cover statically-known bracketed forms such as `Deno.permissions["request"]()` / `Deno.permissions["revoke"]()` and their `globalThis["Deno"]["permissions"]` counterparts on the TS and `.js` smoke lanes. The phase-6 dashboard now also keeps the unsupported `ffi` / `sys` descriptor row pinned in the rejected bucket, now also tracks the later object-materialization `Deno.env.toObject` gate explicitly, including bracketed `Deno.env["toObject"]` and `globalThis.Deno["env"]["toObject"]` spellings in the shared runtime-smoke lane, now also tracks interactive permission escalation APIs in the rejected bucket, and the schema-doc drift net now asserts that rejected-by-default slice deterministically. The lower-level `build_source_file` helper now also rejects unsupported permission-query descriptors (`ffi` and `sys`) in `.js` input on the direct and bracketed `globalThis["Deno"]["permissions"]["query"]` source forms, keeping the compile-time mirror aligned with the runtime rejection path too.
- Current progress: numeric object property names now lower as quoted string literals in `crates/kali_hir/src/lib.rs`, which keeps property-key canonicalization from conflating them with numeric value literals and gives the object-enumeration path a cleaner foundation for integer-like ordering support. MIR layout inference now also canonicalizes integer-like property keys before string keys, and the optimizer re-runs object-enumeration folding after binding specialization using collected constant bindings so the literal-object helper can see more resolved shapes; the codegen aggregate-literal path now also carries the 4-key integer-like smoke fixtures instead of rejecting them, so the runtime/browser smoke matrix can cover the supported ordering path in both TS and `.js` inputs.
- Current progress: the phase-6 conformance dashboard now splits parser-supported generator syntax from the later generator-lowering gate so the supported/gated buckets mirror the actual current snapshot more precisely, and the console-routing regressions now include mirrored `.js` input coverage alongside the existing assertion checks. The same console-routing regression set now also covers the `test` path in `.js` input so the human/machine output split stays explicit in both runner modes, and the dashboard now records the browser-requested and browser-bundle console rows with their JSON-output coverage so the runtime snapshot stays aligned with the current machine-readable evidence too. The dashboard now also records the browser-requested queueMicrotask and inherited browser-api-surface queueMicrotask rows, plus the browser-requested math-suite rows, with JSON-output coverage explicitly, keeping the phase-6 snapshot aligned with the current browser-requested machine-readable regressions.
- Current progress: `effects` browser-analysis coverage now also mirrors explicit and inherited browser API-surface requests onto `.js` input, including JSON-output coverage for the nullish-coalescing browser-analysis smoke lane, keeping the browser analysis context aligned with first-class JavaScript compilation as well as the existing TS lane. The browser-targeted `effects` lane now also has JSON-output rejection coverage for the threaded-profile gate, so the `--wasm-threads` browser-analysis boundary stays explicit in both text and machine-readable output modes. The phase-6 conformance dashboard now also pins that browser-targeted threaded-profile rejection row explicitly, so the current snapshot stays aligned with the runtime-smoke evidence. The same browser-targeted `effects` rejection now also mirrors `.js` input on the explicit and inherited browser-analysis paths, including JSON-output coverage, keeping the browser-analysis boundary aligned with first-class JavaScript compilation. The sandbox effect-analysis tests now also cover the full documented `Deno.permissions.query(...)` descriptor subset in direct and bracketed forms across both TS and `.js` input, preserving the computed-host-access boundary alongside the runtime-smoke coverage.
- Current progress: the browser-requested standalone run/test harness now also exercises object-enumeration semantics in both TS and JS input classes, including integer-like key ordering and string-primitive enumeration coverage in `.js` input, and the inherited browser-api-surface path now mirrors the same object-enumeration and integer-like key ordering coverage in both TS and `.js` input, and now also has JSON-output coverage for the inherited object-enumeration source in both TS and `.js` input, including the string-primitive slice, while standalone run/test JSON-output coverage still pins the basic Object.keys() / Object.entries() / Object.values() path in `.js` input, and the direct browser-requested overwrite/delete-reinsert ordering mirrors are now also checked in for `.ts` input, and the browser-harness Math.max() / Math.min() / Math.abs() / Math.sign() / Math.imul() / Math.clz32() suite in both TS and JS input classes, including the combined `.js` `test`-path mirror added alongside the existing `.ts` regression, plus boolean conjunction/disjunction semantics and strict equality / inequality semantics in both TS and `.js` input classes, with JSON-output coverage now also pinned for the browser-requested JS-input boolean-logic `run` / `test` path, and the inherited browser-api-surface path now also exercises the same boolean conjunction/disjunction semantics in `.js` input with JSON-output coverage, and the browser-requested run/test math suite now also has JSON-output coverage in both TS and `.js` inputs, and the dedicated browser-requested JS-input `Math.ceil()` smoke now also has JSON-output coverage on the `run` and `test` lanes, alongside the dedicated browser-requested JS-input `Math.trunc()` smoke coverage on the same lanes, and the browser-requested `test` lane now also has JSON-output coverage in `.ts` input plus a dedicated JSON-output `Math.clz32()` mirror in `.js` input, and the plain-text `.ts` `Math.clz32()` `test` mirror now also exists, while the inherited browser-api-surface path now also covers `Math.clz32()` on both `run` and `test` in `.js` input, keeping the opt-in runtime path aligned with the same first-class-JavaScript evidence that the browser analysis and build lanes already pin. The same browser-requested run/test lane now also exercises `try/finally` sequencing and queueMicrotask ordering in both TS and `.js` inputs, and now also rejects late process-control members in browser harness coverage. The browser bundle lane now also exercises basic strict equality / inequality semantics, boolean conjunction/disjunction semantics, console-level routing, `console.assert()` false-branch reporting, and queueMicrotask ordering in both TS and JS inputs, and the browser bundle console-level-routing JS-input regression now also has JSON-output coverage, keeping the same browser-targeted execution evidence aligned with the strict-equality baseline already covered on the standalone runtime path. The browser bundle boolean conjunction/disjunction smoke now also has JSON-output coverage in both TS and `.js` inputs, so the browser bundle machine-readable path stays aligned with the same short-circuit regression the human-readable lane already pins. The browser bundle queueMicrotask ordering now also has JSON-output coverage in both TS and `.js` input, so the browser-bundle envelope path stays pinned alongside the existing execution smoke, and the phase-6 dashboard now also calls out that browser-bundle row explicitly. The browser-requested run/test strict-equality lane now also has JSON-output coverage in both TS and `.js` inputs, and the matching non-JSON TS-input run/test mirrors for boolean conjunction/disjunction and strict equality now also land, and the browser bundle integer-like key ordering row now also has JSON-output coverage on both TS and `.js` inputs so the machine-readable browser bundle contract stays aligned with the execution smoke. The same browser-requested run/test harness now also has JSON-output coverage for the TS- and `.js`-input object-enumeration and integer-like ordering slices, keeping the browser-requested machine-readable path aligned with the same overwrite-delete-reinsert ordering and string-primitive coverage. The browser-requested run/test harness now also exercises basic object property deletion / `in`-operator semantics in both TS and `.js` input, including JSON-output coverage, so the browser-requested object-model baseline stays aligned with the standalone regression set. The browser bundle math suite now also includes `Math.imul()` in both TS and `.js` inputs, and the browser bundle `.js` `Math.max()` build smoke now also has JSON-output coverage.
- Current progress: the inherited browser-api-surface math suite now also has TS-input `run` / `test` coverage, including JSON-output coverage, keeping the inherited browser-harness math baseline symmetric with the existing JS-input path. The phase-6 dashboard now also tracks that inherited browser-api-surface math row explicitly, keeping the deterministic snapshot aligned with the same supported math baseline.
- Current progress: standalone `run` / `test` now also carry JSON-output coverage for `Math.max()` / `Math.min()` / `Math.abs()` / `Math.sign()` in both TS and `.js` input, plus `Math.clz32()`, `Math.imul()`, and `Math.trunc()` in `.js` input, keeping the basic built-in lane aligned with the existing runtime smoke evidence. The same standalone `.js` math baseline now also includes `Math.ceil()` on the current integer-only numeric subset, with `run` / `test` smoke coverage and JSON-output coverage, and the integer-only math lowering now explicitly rejects non-integer literal operands before they can fall through to the literal-encoding path, and the codegen path now mirrors that same non-integer-literal gate instead of relying on the checker alone. The browser bundle lane now also exercises `Math.ceil()` in both TS and `.js` inputs, including JSON-output coverage in `.js` input, keeping the browser-targeted bundle path aligned with the same integer-rounding subset that the standalone runtime smoke already pins. Unsupported `Math` member calls such as `Math.round()` and `Math.floor()` now reject with `E5506` at codegen instead of silently collapsing to a zero placeholder, `crates/kali_codegen/src/tests.rs` now also pins the `Math.round()` rejection directly, and the browser-targeted `check` and `build --bundle` lanes plus the browser-requested `run` / `test` lane now also carry matching `.js` rejection coverage for that gate, and the browser-targeted `check` lane now also has matching JSON-output rejection coverage in `.js` input; the browser-targeted `build --bundle` lane now also has matching JSON-output rejection coverage in `.js` input, and the browser-requested `run` lane now also has matching JSON-output rejection coverage in `.js` input, while the inherited browser-api-surface browser-harness lane now also carries matching `.js` rejection coverage on `run` / `test`, including JSON-output coverage on the inherited `run` and `test` paths, the inherited browser API-surface `check` lane now also rejects the same unsupported math member calls in `.js` input with matching JSON-output coverage, and the inherited browser API-surface `build --bundle` lane now also rejects the same unsupported math member calls in `.js` input, including JSON-output coverage. The browser-targeted `check` / `build --bundle` lanes now also mirror the same rejection in `.ts` input, and the browser-requested `run` / `test` lanes now also mirror the same rejection in inherited-browser-api-surface `.ts` input with the same JSON-output coverage.
- Current progress: `Math.trunc()` now also has standalone `run` / `test` smoke coverage in `.js` input, including JSON-output coverage, keeping the basic built-in runtime regression set aligned with the current lowering path. The same standalone `.js` math baseline now also includes `Math.ceil()` on the current integer-only numeric subset, with `run` / `test` smoke coverage and JSON-output coverage. Browser-requested `Math.ceil()` smoke now also covers the direct `.ts` input path on the configured browser harness, including JSON-output coverage, keeping the browser-harness integer-rounding slice symmetric across source classes. The inherited browser-api-surface path now also covers `Math.ceil()` in `.ts` and `.js` input, including JSON-output coverage, and the new `.ts` inherited-browser smoke now mirrors the `.js` lane in `runtime_smoke.rs`; the phase-6 dashboard now also tracks the inherited browser-api-surface `Math.ceil()` row explicitly, keeping that browser-harness integer-rounding slice symmetric across the inherited path too. The phase-6 dashboard now also tracks the `Math.trunc()` JS-input row and the inherited browser-api-surface `Math.trunc()` row so the basic built-in evidence stays visible alongside the rest of the math baseline.
- Current progress: the browser-requested run/test harness now also mirrors console-level routing and `console.assert()` false-branch reporting in `.js` input, including JSON-output coverage, so the browser harness output split stays aligned with the browser bundle lane.
- Current progress: browser-requested run/test async/await sequencing now also has dedicated browser-harness coverage in both TS and `.js` input, including JSON-output coverage on both commands, keeping the opt-in browser runtime path aligned with the same await-order regression the standalone runtime smoke already pins. Standalone basic async/await sequencing now also has JSON-output coverage in both TS and `.js` input across `run` and `test`, keeping the basic await-order regression aligned with the existing non-JSON smoke. Browser-requested `try/catch` exception handling now also has dedicated browser-harness coverage in both TS and `.js` input, including JSON-output coverage, and the phase-6 conformance dashboard now also tracks that browser-harness async/await row explicitly.
- Current progress: the read-only `Deno.pid` / `Deno["pid"]` / `globalThis.Deno.pid` process-identity query now also has JSON-output coverage on the standalone `test` path for `.js` input, keeping the process-identity slice aligned with the rest of the machine-readable execution smoke.
- Current progress: the phase-6 conformance dashboard now explicitly tracks the browser-requested strict-equality row alongside the browser-requested and browser-bundle integer-like key ordering rows for object enumeration, plus the browser-bundle strict-equality row itself, so the supported ordering and comparison slices stay visible in the phase snapshot alongside the broader browser runtime rows.
- Current progress: the browser-requested `run` / `test` `Math.clz32()` `.ts` slice is now covered directly in the browser-harness JSON path, matching the existing `.js` slice coverage already carried in `crates/kali_cli/tests/runtime_smoke.rs`.
- Current progress: the phase-6 dashboard drift net now also pins the browser-requested math and queueMicrotask rows explicitly, including the `Math.max()` / `Math.min()` / `Math.abs()` / `Math.sign()` / `Math.imul()` slice, the `Math.clz32()` browser-requested TS and JS slices, the browser-requested `Math.trunc()` slice plus its explicit browser-requested `.js` input row, the browser-requested `.js` `Math.ceil()` row, the inherited browser-api-surface `Math.ceil()` row, the browser-requested and inherited browser-api-surface queueMicrotask rows, the basic nested Math call composition row in `.js` input, the basic `Object.keys()` / `Object.entries()` / `Object.values()` overwrite/delete-reinsert ordering rows, the browser-requested and browser-bundle object-enumeration rows, the basic object property deletion / `in`-operator row, the basic optional chaining row, the basic BigInt addition rows, the browser-requested zero-capable budget pair (`--max-threads 0` / `--max-spawned-processes 0`) in `.js` input, and the supported-row snapshot now also rejects accidental duplicate rows, so the runtime snapshot stays visible and unique in the deterministic docs test alongside the existing execution smoke; the deterministic docs test now also asserts that dedicated browser-requested `.js` `Math.trunc()` row directly.
- Current progress: browser-requested JSON-output coverage now also mirrors object-enumeration and integer-like key ordering in both TS and `.js` input on the browser-requested run/test harness, closing the remaining machine-readable browser-requested gap on that object-enumeration slice.
- Current progress: the phase-6 dashboard now also tracks the standalone string-primitive enumeration row for `Object.keys()` / `Object.entries()` / `Object.values()` in `.js` input, keeping the basic browser-agnostic object-enumeration slice visible alongside the existing overwrite/delete-reinsert and browser-requested/browser-bundle rows.
- Current progress: the phase-6 dashboard drift net now also pins the browser bundle basic object property deletion / `in`-operator row explicitly, so the browser-bundle object-model snapshot stays aligned with the current runtime smoke.
- Current progress: the phase-6 dashboard drift net now also pins the browser-requested try/catch / try/finally row plus the browser-bundle object-deletion, `try/finally`, and console-routing rows explicitly, so the supported browser-runtime snapshot stays aligned with the current smoke evidence.
- Current progress: the phase-6 dashboard drift net now also pins the browser bundle async/await and queueMicrotask rows explicitly, so the browser-bundle ordering/timing snapshot stays aligned with the current runtime smoke.

### 6.3 Module and dynamic-loading semantics

- Literal-string `import()` over the linked graph is implemented in the current snapshot; standalone `run` / `test` smoke now also covers literal-string dynamic imports in both TS and `.js` input, including JSON-output coverage for the `run` / `test` lanes and the `test` lane, and now also mirrors the same literal-string dynamic-import path in `.jsx` and `.tsx` input with matching human and JSON coverage, the browser-requested `run` / `test` harness now also mirrors the same directory-index lowering path in TS input with JSON-output coverage on the `test` lane, build-discovery now resolves `.js` literal dynamic import chunks, and the browser-targeted bundle path keeps the linked-graph lowering path honest with directory-style targets. Keep the one-linked-payload rule and preserve the linked-graph lowering path.
- Directory-style linked targets now resolve through `index.*` entries so `import("./dir")` can lower the same way as `import("./dir/index.ts")` when the directory is part of the linked graph, and that coverage is mirrored explicitly in the `.tsx` / `.jsx` type-resolution and build-discovery tests too.
- Current progress: added minimized package-corpus regressions for mixed CommonJS/ESM default-import interop on the default standalone surface, including an exports-map variant that keeps the interop path honest under the package resolver, and the same utility corpus now also exercises `build` alongside `run`/`test` for the TS and `.js` interop fixtures.
- Current progress: browser-targeted package-corpus regressions now also cover `.js` entrypoints for browser replacement-map packages, including a scoped package case, and now add a minimized mixed CommonJS/ESM browser interop fixture too, so first-class JavaScript compilation stays exercised in the browser analysis/build lane without dropping the mixed-format resolver path.
- Current progress: the default standalone semver corpus probe now also covers `.js` input on `check` / `build` / `run` / `test`, keeping the same pure-JS package fixture aligned with first-class JavaScript compilation on the default standalone surface instead of only the TypeScript lane.
- Current progress: browser bundle chunk smoke coverage now also exercises literal and const-bound dynamic imports from `.js` input, matching the TypeScript bundle chunk regressions and keeping the linked-graph lowering path honest for first-class JavaScript compilation.
- Current progress: browser bundle runtime smoke coverage now also exercises the dynamic-import loader for `.js` input, including directory-index targets, and now also normalizes runtime dynamic import specifiers in `.js` input, so the browser bundle path keeps its first-class JavaScript dynamic-loading behavior mirrored across the JS and TS lanes. The browser-requested run/test harness now also exercises literal-string dynamic imports with directory-index targets in `.js` input, and the standalone runtime-smoke matrix now also mirrors literal-string dynamic imports into `.jsx` and `.tsx` input with both human and JSON coverage, keeping the opt-in browser/runtime and extended source-class loader paths aligned with the linked-graph directory-index lowering rule.
- Current progress: optional chaining now also has direct `.js` runtime-smoke coverage on both the `run` and `test` paths, plus standalone JSON-output coverage on the `.js` `run` / `test` lanes, keeping the supported object-access baseline aligned with the first-class JavaScript execution lane.
- Current progress: added `.js` negative coverage for directory dynamic imports without an `index.*` target in `crates/kali_types/src/tests.rs`, and mirrored the non-literal dynamic import rejection onto the `check` / `build` JS input lane in `crates/kali_cli/tests/runtime_smoke.rs`. The build-discovery fixture set now also mirrors comment/string-substring ignores onto `.js` input and adds the parenthesized const-bound target-resolution case on `.ts`, `.js`, `.tsx`, and `.jsx` input so the scanner stays mirrored across the extended source-class matrix. The directory-index dynamic-import path now also has explicit `.tsx` and `.jsx` coverage in both the frontend resolver and build-discovery helpers, so the `index.*` claim stays honest across the extended source-class matrix.
- Preserve the one-linked-payload rule.
- Keep non-literal `import(expr)` on the later compatibility path and reject it with the canonical `E5506` gate.

### 6.4 Runtime semantic regressions

- Add minimized tests for exceptions, async/await, iterators/generators, built-ins, CJS/ESM interop, and object semantics.
- Current progress: `try/catch` exception handling and `try/finally` sequencing now have dedicated runtime-smoke regressions alongside the existing basic exception cases.
- Current progress: the shared web-baseline runtime smoke now also exercises `structuredClone`, `AbortController`, `AbortSignal`, `Event`, `EventTarget`, `CustomEvent`, `URL`, `URLSearchParams`, `TextEncoder`, and `TextDecoder` on the standalone `run` and `test` paths in both TS and `.js` input, with JSON-output coverage for both commands, so the web baseline stays aligned with the supported API contract instead of only the package-corpus path. The browser-requested `run` / `test` harness now also mirrors the same primitives, including inherited-browser-api-surface coverage and JSON-output coverage, so the browser-requested path stays aligned with the same web-baseline contract too. The browser bundle runtime smoke now also exercises the same primitives in both TS and `.js` input, with JSON-output coverage for the browser bundle lane, so the browser-targeted bundle path stays aligned with the same web-baseline contract. The type-resolution layer now also accepts the same shared web-baseline globals directly in `crates/kali_types/src/tests.rs`, and now also accepts the remaining browser-baseline host globals (`fetch`, `Headers`, `Request`, `Response`, `Blob`, `File`, `performance`, and `crypto`) in the same file, keeping the browser ambient typing surface visible one layer earlier than the runtime smoke. The phase-6 conformance dashboard now also tracks both the direct browser-requested web-baseline row and the inherited-browser-api-surface web-baseline row explicitly, including the `URL` / `URLSearchParams` / `TextEncoder` / `TextDecoder` coverage, and now also tracks the broader browser ambient typing row for the remaining browser-baseline host globals, keeping the supported bucket aligned with the browser bundle and standalone smoke evidence.
- Current progress: `performance.now()` monotonic ordering now has dedicated browser-requested run/test and browser-bundle `.ts` and `.js` smoke coverage, including inherited browser-api-surface coverage on the run/test and bundle paths plus JSON-output coverage on the inherited browser bundle lane, and the direct browser bundle JSON lane now also pins the same `.js` monotonic-ordering regression; the browser bundle `.js` lane now also has JSON-output coverage, and the browser-requested `run` and `test` JSON lanes now also pin the same `.js` timing baseline, the inherited browser-api-surface `.js` and `.ts` `test` lanes now also have JSON-output coverage, keeping the browser timing baseline explicit alongside the existing microtask regression.
- Current progress: the browser-requested `test` lane now also has JSON-output coverage for `performance.now()` monotonic ordering in `.ts` input, so the browser timing baseline stays explicit in the machine-readable browser-requested path too.
- Current progress: the inherited browser API-surface math suite on `.js` input now also has JSON-output coverage on both the browser-requested `run` and `test` paths, keeping the inherited browser-harness math baseline machine-readable alongside the existing non-JSON coverage.
- Ensure unsupported dynamic features produce canonical availability diagnostics rather than silent placeholders.
- Current progress: added regression coverage for arithmetic precedence and array literal length handling, including mirrored `.js` input coverage for both semantics and new `test`-path `.js` mirrors for both semantics, plus function call return semantics and relational comparison semantics, including mirrored `.js` run/test coverage for both semantics, strict equality/inequality semantics, including mirrored `.js` run/test coverage for both the `===`/`!==` path, boolean conjunction/disjunction semantics, including mirrored `.js` run/test coverage, async/await sequencing, including mirrored `.js` input coverage on both the `run` and `test` paths, queueMicrotask ordering on mirrored `.js` `run`/`test` smoke paths, including JSON-output coverage, and the phase-6 conformance dashboard now also tracks standalone queueMicrotask ordering in `.js` input plus browser-requested async/await sequencing, try/catch exception handling, try/finally sequencing, and Web Crypto randomness via `crypto.getRandomValues()` in the browser-requested `.ts` and `.js` lanes. The dashboard also records browser-requested try/catch/try/finally coverage in `.ts` and `.js` inputs, including JSON-output coverage, and the inherited browser-api-surface run/test path now also mirrors that same try/catch/try/finally coverage in both `.ts` and `.js` inputs with JSON-output coverage, and the phase-6 dashboard now also tracks that inherited browser-api-surface try/catch/try/finally row explicitly, try/finally sequencing, including mirrored `.js` input coverage on both the `run` and `test` paths, browser bundle try/catch sequencing in both TS and `.js` inputs, browser bundle try/finally sequencing in both TS and `.js` inputs, browser bundle async/await sequencing in both TS and `.js` inputs, browser bundle `Object.keys()` / `Object.entries()` / `Object.values()` enumeration semantics in both TS and `.js` inputs including string-primitive enumeration and overwrite/delete-reinsert ordering, BigInt addition semantics, including mirrored `.js` run/test coverage and browser bundle smoke coverage in both TS and `.js` inputs, a gated `for...of` array-iteration rejection fixture plus a mirrored `.js` input-class rejection fixture now covered by parser/typechecker and CLI smoke tests, including JSON-output coverage on the JS-input `check` / `build` lanes, and the same iterator-lowering gate now also rejects `for await...of` array iteration in `check` / `build` / `run` / `test` on TS and `.js` input, plus browser-targeted `check` / `build --bundle` and browser-requested `run` / `test` coverage in TS and `.js` input, keeping the async-iteration syntax row separate from the supported-await grammar row, generator and async generator lowering gates, including mirrored `.js` generator-gate fixtures for `check`/`build` and `run`/`test`, plus JSON-output coverage for the generator-function lowering gate on the JS-input `build` / `run` / `test` lanes, `Object.keys()` / `Object.entries()` / `Object.values()` object-enumeration semantics including overwrite/delete-reinsert ordering, plus mirrored `.js` coverage for the overwrite/delete-reinsert ordering object-enumeration cases on both the `run` and `test` paths, browser-requested `run` / `test` Web Crypto `crypto.subtle.digest()` / `crypto.randomUUID()` semantics on `.ts` and `.js` input too, the Web baseline randomness subset via `crypto.getRandomValues()` on mirrored `.js` `run`/`test` paths, including JSON-output coverage for the browser-requested slice, and the phase-6 conformance dashboard now also pins the browser-requested Web Crypto `crypto.subtle.digest()` / `crypto.randomUUID()` row plus the browser bundle `crypto.getRandomValues()` / `crypto.subtle.digest()` / `crypto.randomUUID()` row explicitly, browser-requested run/test string-primitive enumeration in `.js` input, browser-requested console-level routing in `.js` input, browser-requested Web Crypto `crypto.subtle.digest()` / `crypto.randomUUID()` semantics on mirrored `.js` `run`/`test` harness coverage, `Math.max()` / `Math.min()` / `Math.abs()` / `Math.sign()` / `Math.imul()` built-in lowering with mirrored `.js` input coverage and new `test`-path smoke coverage for the same built-ins, browser bundle `console.assert()` false-branch reporting in `.js` input, and `console.error()` / `console.warn()` / `console.info()` / `console.debug()` routing plus `console.assert()` false-branch reporting, including a mirrored `.js` console-level routing regression and a mirrored `.js` `test`-path console-routing regression, in `crates/kali_cli/tests/runtime_smoke.rs`.
- Current progress: the phase-6 conformance dashboard and schema-doc drift net now also pin browser-requested boolean conjunction/disjunction semantics in `.ts` and `.js` input, plus the inherited browser-api-surface boolean conjunction/disjunction lane in `.js` input with JSON-output coverage, keeping the browser-requested short-circuit row visible alongside the existing strict-equality and queueMicrotask evidence.
- Current progress: the phase-6 conformance dashboard now also tracks the browser-requested Web Crypto `crypto.subtle.digest()` / `crypto.randomUUID()` row explicitly in `.ts` and `.js` input, including JSON-output coverage, matching the browser-requested harness regressions.
- Current progress: the default standalone package corpus now also has a minimized mixed-format interop fixture for `test`, complementing the existing `run` coverage for the same package shape, and the `.js` interop fixture now also covers `build`.
- Current progress: the basic `Object.keys()` / `Object.entries()` / `Object.values()` runtime regressions now also have mirrored `.js` input coverage on both the `run` and `test` paths, and now also cover string-primitive enumeration in `.js` input on both paths, so the simpler enumeration path stays aligned with the overwrite/delete-reinsert ordering and integer-like-key-ordering regressions already in place.
- Current progress: basic object property deletion and `in`-operator semantics now also have mirrored `.ts` and `.js` coverage on the standalone `run` and `test` paths, with dedicated runtime_smoke regressions instead of dead helper scaffolding, and JSON-output coverage now also pins that object-property slice on both `.ts` and `.js` inputs; runtime object type / constructor semantics (`typeof`, `instanceof`) now also have mirrored `.ts` and `.js` coverage on the standalone `run` and `test` paths, with JSON-output coverage now also pinning the same object-model baseline on both source classes, and the browser-requested browser-harness lane now also mirrors that same `typeof` / `instanceof` slice in JSON-output coverage across `.ts` and `.js` inputs while browser bundle smoke now also exercises the same object-model baseline in both source classes, and browser bundle smoke now also exercises basic object property deletion / `in`-operator semantics in both TS and `.js` inputs; the phase-6 dashboard now also tracks the browser-requested and browser-bundle rows explicitly, keeping the object-model baseline aligned with first-class JavaScript compilation evidence; unary prefix expressions (`!`, unary `-`, unary `+`, and `void`) now have the same mirrored `.ts` and `.js` `run` / `test` coverage, including JSON-output coverage on the standalone lanes, after the shared smoke was expanded beyond the earlier `void`-only check, keeping the object-property baseline aligned with the existing enumeration smoke.
- Current progress: browser bundle runtime object type / constructor semantics now also have JSON-output coverage in both TS and `.js` input, keeping the browser bundle object-model baseline machine-readable alongside the existing execution smoke.
- Current progress: nested Math call composition now also has dedicated `.js` run/test smoke coverage, and the standalone `run` / `test` JSON-output lane now also pins the same nested Math composition path in `.js` input, keeping the supported built-in family explicit alongside the other low-level runtime semantics.
- Current progress: the standalone `run` / `test` JSON-output envelope now also covers integer-like key ordering in both `.ts` and `.js` input, keeping the machine-readable execution path aligned with the browser-bundle JSON ordering evidence.
- Current progress: browser bundle smoke coverage now also exercises `Math.abs()` semantics in both `.ts` and `.js` inputs, keeping the browser-targeted bundle lane aligned with the same built-in lowering evidence that the standalone runtime smoke already pins for the default surface.
- Current progress: browser bundle runtime smoke now also exercises `Math.max()` / `Math.min()` / `Math.abs()` / `Math.sign()` / `Math.clz32()` / `Math.trunc()` semantics in both TS and `.js` input, keeping the browser-targeted bundle lane aligned with the same built-in lowering evidence that the standalone runtime smoke already pins for the default surface. The browser bundle build smoke now also exercises `Math.clz32()` semantics in TS input, mirroring the existing `.js` bundle regression, and the browser bundle `Math.trunc()` / `Math.clz32()` JSON-output lanes now also cover `.ts` input alongside `.js` input. The browser bundle `Math.max()` JSON-output lane now also covers `.ts` input, and the browser bundle JSON path now also covers `Math.min()` / `Math.abs()` / `Math.sign()` in both `.ts` and `.js` input, keeping the machine-readable bundle path aligned with the existing built-in lowering suite. Browser bundle `Math.ceil()` JSON-output coverage now also covers `.ts` input alongside `.js` input, keeping the integer-rounding slice symmetric across source classes.
- Current progress: browser bundle runtime smoke now also exercises `try/finally` sequencing in both TS and `.js` inputs, keeping the browser bundle lane aligned with the same exception-sequencing regression the standalone run/test paths already pin.
- Current progress: browser bundle `try/catch` and `try/finally` smoke now also has JSON-output coverage in `.js` input, so the browser bundle machine-readable path stays aligned with the existing execution smoke.
- Current progress: the phase-6 conformance dashboard now also tracks browser bundle `try/catch` exception semantics explicitly, keeping the browser bundle runtime snapshot aligned with the runtime-smoke coverage.
- Current progress: browser bundle runtime smoke now also exercises async/await sequencing in both TS and `.js` inputs, including JSON-output coverage, keeping the browser bundle lane aligned with the same await-order regression the standalone runtime smoke already pins. Browser bundle crypto web API smoke now also mirrors `crypto.getRandomValues()` plus `crypto.subtle.digest()` / `crypto.randomUUID()` in both TS and `.js` inputs, including JSON-output coverage, keeping the browser bundle lane aligned with the same Web-baseline crypto evidence the standalone runtime smoke already pins. Browser bundle `Object.keys()` / `Object.entries()` / `Object.values()` smoke now also exercises string-primitive enumeration and overwrite/delete-reinsert ordering in both TS and `.js` inputs, and the string-primitive enumeration and overwrite/delete-reinsert ordering browser-bundle lanes now also have JSON-output coverage in both source classes, keeping the browser bundle lane aligned with the same basic enumeration regressions the standalone runtime smoke already pins. Browser bundle boolean conjunction/disjunction smoke now also mirrors both TS and `.js` inputs, keeping the browser bundle lane aligned with the same basic short-circuit semantics the standalone harness already pins.
- Current progress: `DenoRuntimeProjection::new()` now also has unit coverage for the default-open projection baseline, keeping the Phase-1 host-projection contract explicit alongside the rest of the browser/runtime baseline.
- Current progress: the browser-requested `run` / `test` performance.now monotonic-ordering regression now also has mirrored `.ts` input coverage on the direct and inherited browser-harness paths, plus JSON-output coverage on the browser-requested `.js` run lane and the inherited browser-api-surface `.js` run lane, keeping the browser timing baseline explicit across both source classes. The inherited browser `apiSurface` path now also has JSON-output `run` / `test` coverage for `Math.trunc()` in `.ts` and `.js` input under the same browser harness.
- Current progress: `build --sandbox` now mirrors the inferred-effect-vs-policy rejection coverage previously pinned on `check` for the same `fetch(...)` source, including `.js` input and JSON-output coverage, so the compile-time sandbox comparison path stays explicit on both analysis/build commands. The `check --sandbox` lane now also carries the same `.js`-input rejection plus JSON-output coverage, keeping the first-class JavaScript analysis path aligned with the build-side sandbox comparison coverage.
- Current progress: the phase-6 dashboard now also calls out the browser-requested `Math.clz32()` TS-input slice explicitly alongside the existing JS-input JSON row, and it now also records the inherited browser-api-surface `Math.clz32()` `.ts` and `.js` rows, so the browser-requested math baseline stays visible in the supported bucket.
- Follow-up: generator lowering still needs a dedicated implementation packet for real lowering; the current snapshot keeps that surface gated at resolution/check time rather than miscompiling it, and the remaining coverage work is about keeping the minimized gate fixtures aligned across input classes. The type-resolution layer now also has direct `.ts`, `.js`, `.tsx`, and `.jsx` generator-function lowering gates in `crates/kali_types/src/tests.rs`, the CLI smoke matrix now also mirrors the same generator-function lowering rejection into `.jsx` / `.tsx` `check` / `build` / `run` / `test` inputs, and the generator-function lowering gate now also has JSON-output coverage on the JS-input `check` / `build` / `run` / `test` lanes, plus the browser-analysis JS-input `check` lane, keeping the machine-readable rejection path explicit there too. The resolver now also collapses generator lowering to one canonical `E5506` diagnostic at the generator-function boundary, so the remaining follow-up stays focused on the real lowering packet instead of duplicate nested `yield` noise.
- Follow-up: object-enumeration coverage should keep the overwrite/delete-reinsert ordering and integer-like ordering regressions mirrored across TS and JS input classes and across the `run`/`test` paths, while broader object-model widening remains a later-compatibility topic. Keep the browser-requested and browser-bundle smoke lanes aligned with the same supported integer-like ordering path rather than letting them drift back to parser or codegen placeholders. Standalone JSON-output coverage now also exercises overwrite/delete-reinsert ordering in both TS and `.js` input so the machine-readable baseline stays aligned with the existing smoke matrix.
- Current progress: browser-targeted `check` / `build --bundle` now also reject `for...of` array iteration in `.js` input with matching JSON-output coverage, keeping the browser analysis/bundle gate mirrored with the existing rejected-by-default iterator-lowering row.
- Current progress: the browser-requested `run` / `test` harness now also rejects `for...of` array iteration in TS and `.js` input, including JSON-output coverage on both commands when `KALI_BROWSER_BUNDLE_HARNESS_COMMAND` is configured, keeping the opt-in browser runtime path aligned with the same rejected-by-default iterator-lowering gate.
- Current progress: nullish coalescing now parses as a binary operator token but is explicitly gated with `E5506` in the checker/codegen path, avoiding the previous silent fallthrough that could miscompile `null ?? ...` into a plain `null` result. That rejection is now also mirrored in the browser-targeted `check` / `build --bundle` JS-input lane, including JSON-output coverage, while the browser-analysis `effects` lane continues to report through the same JS input class in both explicit and inherited browser contexts, keeping the rejected-by-default row explicit for compile/gate commands without conflating it with effect-report analysis. The standalone `check` / `run` / `test` lanes now also mirror the same nullish coalescing rejection in `.js` input with JSON-output coverage, so the machine-readable standalone gate stays aligned with the text-mode smoke. The standalone `effects` lane now also accepts the same nullish coalescing JS input in the default analysis context, including JSON-output coverage, so the analysis-only acceptance path stays aligned with the browser-analysis acceptance.
- Current progress: the late host-control, late object-model, and interactive permission-escalation rejection coverage now also exercises statically-known string-literal bracketed forms in the standalone `.js` smoke lanes, including the bracketed `globalThis["Intl"]` / `globalThis["Intl"]["NumberFormat"]` / `globalThis["Intl"]["DateTimeFormat"]` / `globalThis["Intl"]["RelativeTimeFormat"]` / `globalThis["Intl"]["PluralRules"]` late-object-model forms, and the shared runtime_smoke harness now mirrors those bracketed forms too, so the recognized-but-unavailable compatibility-member split stays explicit for both dot and bracket access forms. The dedicated broader-Intl runtime_smoke JS-input lane now also exercises `Intl.DateTimeFormat`, `Intl.RelativeTimeFormat`, `Intl.PluralRules`, `Intl.Collator`, `Intl.DisplayNames`, `Intl.Segmenter`, and `Intl.Locale` alongside `Intl.NumberFormat`, and the shared runtime_smoke broader-Intl gate now also pins bracketed `globalThis["Intl"]` / `globalThis["Intl"]["NumberFormat"]` / `globalThis["Intl"]["DateTimeFormat"]` / `globalThis["Intl"]["RelativeTimeFormat"]` / `globalThis["Intl"]["PluralRules"]` / `globalThis["Intl"]["Collator"]` / `globalThis["Intl"]["DisplayNames"]` / `globalThis["Intl"]["Segmenter"]` / `globalThis["Intl"]["Locale"]` source coverage in the standalone smoke tests. The dedicated late-compatibility fixtures now also explicitly pin bracketed process-control and object-model spellings such as `globalThis["process"]["cwd"]`, `globalThis["Proxy"]`, `globalThis["FinalizationRegistry"]`, and the weak-reference `WeakRef` API, and the matching JSON-output assertions now also pin bracketed `Proxy` / `Object.hasOwn` / `Weak*` spellings, so the bracketed-path regression stays asserted rather than only present in source fixtures. The parser integration suite now also exercises bracketed `globalThis["Deno"]["env"]["get"]`, `globalThis["Deno"]["permissions"]["query"]`, and `globalThis["Deno"]["pid"]` source forms alongside the existing `globalThis["Deno"]["exit"]` bracketed path, and now also exercises bracketed `globalThis["Object"]["hasOwn"]` and `globalThis["Object"]["prototype"]["hasOwnProperty"]["call"]` source forms, and the phase-6 conformance dashboard now also calls out the later-compatibility process identity / process-control / working-directory row explicitly, keeping the browser-requested and standalone late-host baselines visible alongside the rest of the rejected-by-default matrix.
- Current progress: the documented `--compat eval` path now also has `check` and `build` acceptance coverage for the `Function()` constructor on `.js` input, including JSON-output coverage for both commands, and the browser-targeted / browser-requested rejection lanes now also cover the same `eval` / `Function()` compatibility gate under both explicit and inherited browser API-surface selection, keeping the Phase-4 compatibility gate explicit across the analysis/build and browser-harness paths as well.
- Current progress: parser coverage now also exercises bracketed late-compatibility member chains for `Intl`, `Proxy.revocable`, `Object.hasOwn` / `Object.prototype.hasOwnProperty.call`, and the bracketed `globalThis["Object"]["hasOwn"]` / `globalThis["Object"]["prototype"]["hasOwnProperty"]["call"]` source forms, and the type-resolution diagnostics keep the bracketed source rendering explicit alongside the existing dotted-form rejection messages. The phase-6 conformance dashboard now also calls out the nested `Proxy.revocable` forms explicitly in the `Proxy` row, so the rejected-by-default snapshot stays aligned with the fixture coverage.

## Exit gate

- Conformance dashboard exists and is deterministic.
- New supported semantics have tests and schema/diagnostic coverage where relevant.
- Maturity rows are updated only for surfaces with evidence.
