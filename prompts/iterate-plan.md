1. Take a look at SPEC.md and spec/*.md.

2. Generate a plan to implement the specifications
  2a. Generate a top-level PLAN.md as well plan/**/*.md that are referenced from the top-level plan.
  2b. Choose a sensible directory structure.
  2c. The plan should be such that at each stage of development the project should be in a workable state and start from simple features to more complex features. If the plan already exists, make improvements.

3. Once you're satisfied with your changes, you can commit them with a meaningful git commit message and push the changes as well.

