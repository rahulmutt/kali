From @SPEC.md, generate a top-level PLAN.md that points to plan/**/*.md choosing a sensible directory structure. The plan should be such that at each stage of development the project should be in a workable state and start from simple features to more complex features. If the plan already exists, suggest improvements and make them.

Once you're satisfied with your changes, you can commit them with a meaningful git commit message and push the changes as well.

