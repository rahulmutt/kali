# EXTRA-CLAIM DECLARATIONS (U14's `extra` direction, fix round 1 / I6).
# check_extra_claims.py compares this file's claim strings against the
# source's and fails on any that appear nowhere in the .rs. The entries
# below are the deliberate exceptions; a genuinely new one will not be
# on this list and will fail the gate.
# EXTRA-OK: '1\n0\n' -- live-captured exact `json.stdout` pin; source asserts `.contains` on a JSON leaf, which has no substring form, so ruling 3 requires an exact pin captured from the real binary
# Migrated from tests/browser_math_exp_log_mixed_root.rs.
#   SOURCE REF: 28df9ba02962143a1fc1735e418b2f67caee8fc6
#
# NOTE ON THE DESIGN SPEC. Section 5.6's worked example is titled
# `crates/kali_cli/tests/cases/browser/math_exp_log_mixed_root.toml` -- this
# file's path. That example is illustrative and incomplete: it shows two
# [[case]] entries fanned over ext(4) = 8 trials and does not account for the
# source's 9th #[test] fn, the inline-loop harness fn, or any of its 16
# invocations. Its house style (matrix axis for ext, sibling cases for the
# text/json split) is followed; its case count is not, because the real file
# has 24 invocations to cover.
#
# RULE 12 (carry every source comment verbatim): `browser_math_exp_log_mixed_root.rs` contains no
# Rust comments at all -- `grep -nE '^\s*//'` finds nothing, and the only `//`
# anywhere in the file is the `// kali-tree-shake:` marker on line 11, inside a
# JS fixture body, which is program text and is carried verbatim into [source].
# There is therefore no prose to move into any `rationale`, and no `///` doc
# comment on any helper in either call chain (checked: `grep -n '///'` finds
# nothing). comment_coverage.py is run with --allow-empty for this pair.
#
# RULE 7 / U1 -- MATRIX ARITHMETIC, closes exactly. 9 #[test] fns, 24 real
# invocations:
#   * `assert_browser_bundle_global_this_math_bracketed_exp_log`(filename,
#     json_output) -- 8 invocations from 8 one-line #[test] fns =
#     ext(js/ts/jsx/tsx) x json_output(false/true), a full cross product
#     (enumerated with tools/task-18-browser-pilot/enumerate_invocations.py).
#   * `run_and_test_supports_global_this_math_bracketed_exp_log_identities_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_input`
#     -- ONE #[test] fn with an INLINE loop body (no assert_* helper, which is
#     why enumerate_invocations.py reports it as UNPARSED rather than guessing).
#     Read by hand: it loops over EIGHT `(command, source_name, source,
#     expected_stdout)` tuples -- (run, main.js), (test, smoke.test.js),
#     (run, main.ts), (test, smoke.test.ts), (run, main.jsx),
#     (test, smoke.test.jsx), (run, main.tsx), (test, smoke.test.tsx) -- and,
#     inside that, over `for output_json in [false, true]`: 8 x 2 = 16
#     invocations. Unlike its siblings in this batch this loop DOES cover all
#     four extensions, which its fn name states correctly.
# Both groups therefore vary over ext(js/ts/jsx/tsx) uniformly and completely:
# 6 [[case]] x ext(4) = 24 trials = 24 invocations. The arithmetic closes, so
# the axis is kept.
#
# RULE 5 / RULE 6. The 16 harness invocations are NOT folded into one case: the
# inline loop runs 8 independently-written programs, and each becomes its own
# descriptively-named sibling [[case]] (4 of them, x 4 ext cells = 16 trials).
# Per rule 6 the matrix fold is stated here -- each of the 2 bundle [[case]]
# entries corresponds to 4 source #[test] fns (one per ext cell) and each of the
# 4 harness [[case]] entries corresponds to 4 iterations of the single looping
# fn; the assertion mapping stays 1:1 per trial.
#
# U2 ([source] is file-wide): safe here. All three fixtures are written
# unconditionally into a fresh temp dir, none is behind an `if`, and no case's
# point is a file's presence or absence -- every command names its entry
# explicitly on argv. The `${ext}` suffix keeps each matrix cell's three files
# distinct, so no [source] key needs U5 disambiguation.
#
# RULE 13 (transitive helper docs): every fn in both call chains was checked for
# a `///` doc comment and none of this file's own helpers carries one. The bundle
# chain reaches `kali_runtime_contract::browser_bundle_harness_script` and
# `::browser_harness_command_parts_for`, which do carry one-line `///` docs, but
# the migrated form never calls them -- the `browser_bundle_harness` step kind
# means the case RUNNER does (design spec 5.3), so those docs describe shared
# runner infrastructure, not what this case claims. Consistent with every case
# file shipped before batch 4.
#
# ASSERTION SHAPE, mirrored and nothing more. Bundle: `exit = "success"`; the
# json sibling adds schemaVersion/command/success/exitCode and payload
# artifactKind/bundleFormat -- the source asserts NO `errors` array on this
# envelope (`envelope["payload"].as_object().expect("payload object")` :64-73), so no `errors = []` is written; then the emitted
# `app/app.meta.json` apiSurface/artifactKind; then the bundle-harness
# `stdout_contains = ["1\n", "0\n"]` (:116-117, two plain `.contains` calls,
# mirrored as two needles, not strengthened to an exact pin) and the harness
# process's own `exit = "success"` (:109). Harness: argv DOES carry
# `--max-threads 0 --max-spawned-processes 0` (:229-232); env
# KALI_BROWSER_BUNDLE_HARNESS_COMMAND=node; text mode asserts
# `stdout.contains(expected_stdout)` where the loop's own literal is `1\n0`
# (`stdout.contains(expected_stdout)` :268), mirrored as `stdout_contains = ["1\n0"]`; json mode asserts
# schemaVersion/command/success/payload(hostContract, runtimeBackend),
# `exitCode` + `payload.exitCode` for run or `payload.total/passed/failed` for
# test, an exact `json.stdout` pin (see below) and `json.stderr = ""` (:265).
# No `errors` claim on the harness envelope either.
#
# U9 -- the exact `json.stdout` pin. The source's claim is
# `json["stdout"].as_str().contains(expected_stdout)` (:258-264) with
# `expected_stdout` = `1\n0`; a json leaf has no substring form, so ruling 3
# resolves it to an exact pin. Captured from the real binary for all 8
# (ext x command) cells: every one returns `1\n0\n`, so the single pin is
# correct in every matrix cell and it contains the source's literal.

[matrix]
ext = ["js", "ts", "jsx", "tsx"]

[source]
"app.${ext}" = """// kali-tree-shake: globalThisMathBracketedExpLogIdentities
function globalThisMathBracketedExpLogIdentities() {
  const zero = 0;
  const one = 1;
  console.log(globalThis.Math["exp"](zero));
  console.log(globalThis.Math["log"](one));
  return [globalThis.Math["exp"](zero), globalThis.Math["log"](one)];
}
"""
"main.${ext}" = """const zero = 0; const one = 1; console.log(globalThis.Math["exp"](zero)); console.log(globalThis.Math["log"](one));
"""
"smoke.test.${ext}" = """Kali.test('globalThis.Math bracketed exp/log identities', () => {
  const zero = 0;
  const one = 1;
  console.log(globalThis.Math["exp"](zero));
  console.log(globalThis.Math["log"](one));
});
"""

[[case]]
name = "build_emits_global_this_math_bracketed_exp_log_identity_literals"
rationale = """Migrated from browser_math_exp_log_mixed_root.rs. `assert_browser_bundle_global_this_math_bracketed_exp_log` builds a browser bundle (`kali build --bundle --api browser`), asserts the emitted `app/app.meta.json` metadata, then runs the bundle glue under the browser-bundle-harness contract. The bundled program reaches Math through a mixed dotted/bracketed root -- `globalThis.Math["exp"]` and `globalThis.Math["log"]` -- and prints exp(0) = 1 then log(1) = 0. The source's two stdout claims are plain `.contains` against a field that has a substring form, so they are mirrored as `stdout_contains` and NOT strengthened to an exact pin (controller ruling 3)."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "app.${ext}"]
exit = "success"

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.globalThisMathBracketedExpLogIdentities();
"""
exit = "success"
stdout_contains = ["1\n", "0\n"]

[[case]]
name = "json_build_emits_global_this_math_bracketed_exp_log_identity_literals"
rationale = """Migrated from browser_math_exp_log_mixed_root.rs. `assert_browser_bundle_global_this_math_bracketed_exp_log` builds a browser bundle (`kali build --bundle --api browser --output json`), asserts the emitted `app/app.meta.json` metadata, then runs the bundle glue under the browser-bundle-harness contract. The bundled program reaches Math through a mixed dotted/bracketed root -- `globalThis.Math["exp"]` and `globalThis.Math["log"]` -- and prints exp(0) = 1 then log(1) = 0. The source's two stdout claims are plain `.contains` against a field that has a substring form, so they are mirrored as `stdout_contains` and NOT strengthened to an exact pin (controller ruling 3). This sibling additionally asserts the JSON build envelope: schemaVersion/command/success/exitCode and payload artifactKind/bundleFormat -- the source makes no `errors` claim on this envelope, so none is written."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "--output", "json", "app.${ext}"]
exit = "success"
json = { schemaVersion = 1, command = "build", success = true, exitCode = 0, payload = { artifactKind = "bundle", bundleFormat = "esm" } }

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.globalThisMathBracketedExpLogIdentities();
"""
exit = "success"
stdout_contains = ["1\n", "0\n"]

[[case]]
name = "run_supports_global_this_math_bracketed_exp_log_identities_when_browser_harness_is_configured"
rationale = """Migrated from browser_math_exp_log_mixed_root.rs. This case is 4 of the 16 invocations made by the single #[test] fn `run_and_test_supports_global_this_math_bracketed_exp_log_identities_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_input`, whose body loops over eight (command, source_name, source, expected_stdout) tuples and then over `for output_json in [false, true]`; per the split-don't-fold rule each independent program becomes its own named sibling [[case]] rather than one folded case, and the four extensions the loop covers are the file's `ext` matrix axis. It runs `kali run --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`. Text mode: a clean exit and `stdout.contains("1\\n0")` -- the loop's own `expected_stdout` literal -- mirrored as `stdout_contains`."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["1\n0"]

[[case]]
name = "test_supports_global_this_math_bracketed_exp_log_identities_when_browser_harness_is_configured"
rationale = """Migrated from browser_math_exp_log_mixed_root.rs. This case is 4 of the 16 invocations made by the single #[test] fn `run_and_test_supports_global_this_math_bracketed_exp_log_identities_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_input`, whose body loops over eight (command, source_name, source, expected_stdout) tuples and then over `for output_json in [false, true]`; per the split-don't-fold rule each independent program becomes its own named sibling [[case]] rather than one folded case, and the four extensions the loop covers are the file's `ext` matrix axis. It runs `kali test --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`. Text mode: a clean exit and `stdout.contains("1\\n0")` -- the loop's own `expected_stdout` literal -- mirrored as `stdout_contains`."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["1\n0"]

[[case]]
name = "json_run_supports_global_this_math_bracketed_exp_log_identities_when_browser_harness_is_configured"
rationale = """Migrated from browser_math_exp_log_mixed_root.rs. This case is 4 of the 16 invocations made by the single #[test] fn `run_and_test_supports_global_this_math_bracketed_exp_log_identities_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_input`, whose body loops over eight (command, source_name, source, expected_stdout) tuples and then over `for output_json in [false, true]`; per the split-don't-fold rule each independent program becomes its own named sibling [[case]] rather than one folded case, and the four extensions the loop covers are the file's `ext` matrix axis. It runs `kali run --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`. JSON mode (`--output json`): the envelope's schemaVersion/command/success and payload hostContract/runtimeBackend, plus `exitCode` and `payload.exitCode` (the source's `run` branch), `stderr` exactly empty, and an exact `json.stdout` pin. The source claims `json["stdout"].as_str().contains("1\\n0")`; a json leaf has no substring form in this format, so ruling 3 resolves it to an exact pin, live-captured from the real `kali` binary and checked to contain the source's literal before being written. The source asserts no `errors` array on this envelope, so none is written."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "run", success = true, exitCode = 0, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", exitCode = 0 }, stdout = "1\n0\n", stderr = "" }

[[case]]
name = "json_test_supports_global_this_math_bracketed_exp_log_identities_when_browser_harness_is_configured"
rationale = """Migrated from browser_math_exp_log_mixed_root.rs. This case is 4 of the 16 invocations made by the single #[test] fn `run_and_test_supports_global_this_math_bracketed_exp_log_identities_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_input`, whose body loops over eight (command, source_name, source, expected_stdout) tuples and then over `for output_json in [false, true]`; per the split-don't-fold rule each independent program becomes its own named sibling [[case]] rather than one folded case, and the four extensions the loop covers are the file's `ext` matrix axis. It runs `kali test --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`. JSON mode (`--output json`): the envelope's schemaVersion/command/success and payload hostContract/runtimeBackend, plus `payload.total`/`passed`/`failed` (the source's `test` branch), `stderr` exactly empty, and an exact `json.stdout` pin. The source claims `json["stdout"].as_str().contains("1\\n0")`; a json leaf has no substring form in this format, so ruling 3 resolves it to an exact pin, live-captured from the real `kali` binary and checked to contain the source's literal before being written. The source asserts no `errors` array on this envelope, so none is written."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "test", success = true, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", total = 1, passed = 1, failed = 0 }, stdout = "1\n0\n", stderr = "" }
