# EXTRA-CLAIM DECLARATIONS (U14's `extra` direction, fix round 1 / I6).
# check_extra_claims.py compares this file's claim strings against the
# source's and fails on any that appear nowhere in the .rs. The entries
# below are the deliberate exceptions; a genuinely new one will not be
# on this list and will fail the gate.
# EXTRA-OK: '0\n0\n0\n0\n0\n0\n' -- live-captured exact `json.stdout` pin; source asserts `.contains` on a JSON leaf, which has no substring form, so ruling 3 requires an exact pin captured from the real binary
# Migrated from tests/browser_math_expm1_log1p_frozen_aliases.rs.
#
# RULE 12 (carry every source comment): `browser_math_expm1_log1p_frozen_aliases.rs` contains NO Rust
# comments. Checked with `grep -nE '//'`: the single hit is the
# `// kali-tree-shake:` marker on the first line of the bundle fixture's
# `r##"..."##` body, which is program text under test and is carried verbatim
# into [source] below, not Rust prose. There is nothing to move into a
# `rationale`, and `comment_coverage.py` is therefore run with --allow-empty for
# this pair (its batch-3 zero-line floor would otherwise report a vacuous
# green).
#
# RULE 7 / U1 -- INVOCATION ARITHMETIC. 17 #[test] fns, 20 real invocations.
# The gap is one looping fn, and the count needs care:
# `build_emits_frozen_math_expm1_log1p_identity_literals_in_jsx_and_tsx_input`
# (:224-229) loops `for filename in ["app.jsx", "app.tsx"]` and makes TWO helper
# calls inside the loop body -- `json_output` false at :226 and true at :227 --
# so it is 4 invocations, not 2. (HISTORICAL NOTE, fix round 1 / I1: this used
# to say enumerate_invocations.py undercounts this file, printing 6 bundle
# invocations where there are 8 and TOTAL 18 where there are 20. That was true
# when written; the parser bug -- taking only the first `assert_*` call in a loop
# body -- was found during this batch and fixed in the same commit. The repaired
# tool now reports TOTAL INVOCATIONS: 20, agreeing with the hand count.) The
# numbers below were derived by reading the loop body and are now also confirmed
# by the tool.
#   * `assert_browser_bundle_frozen_math_expm1_log1p(filename, json_output)` --
#     4 unlooped fns (:204, :209, :214, :219, covering app.js/app.ts x
#     false/true) + 4 from the loop = 8, a full ext(4) x json_output(2) product.
#   * `assert_browser_harness_frozen_math_expm1_log1p(command, filename, source,
#     json_output)` -- 12 invocations from 12 unlooped fns, NOT the 16 a full
#     product would give. The four absent cells are the non-json jsx and tsx
#     runs of both commands: the source has `run_supports_..._in_js_input`
#     (:232) and `..._in_ts_input` (:243) at `json_output = false`, but its jsx
#     and tsx harness fns (`json_run_supports_..._in_jsx_input` :320,
#     `..._in_tsx_input` :331, `json_test_supports_..._in_jsx_input` :342,
#     `..._in_tsx_input` :353) exist only at `json_output = true`.
# 17 fns -> 20 invocations -> 20 [[case]] entries. 4 + 4 + 12 = 20.
# The bundle group alone would close on an `ext` axis; the harness group does
# not, and an axis is file-wide.
#
# [matrix] DECLINED for the WHOLE file (rule 7 / U1). The enumeration above is
# not a uniform cross product, and `[matrix]` is file-wide: `expand()` fans
# EVERY [[case]] by the full axis product with no per-case opt-out
# (crates/kali_case_runner/src/expand.rs). An `ext` axis would therefore fan the
# cases the source never ran at that extension, inventing untested combinations
# -- a rule 7 arithmetic failure and a rule 2 violation at the same time. Every
# invocation below is its own named sibling [[case]] instead.
# The four siblings split out of the looping fn are named for the cell each one
# runs (rule 5: name descriptively, never numerically); the other 16 cases keep
# their source #[test] fn's name verbatim (rule 6, 1:1).
#
# U13 -- SHARED FIXTURE BODIES ARE NOT HOISTED INTO [constants] HERE, and that is
# deliberate rather than an oversight. Without a [matrix] the same bundle/run/test
# program has to be written once per entry filename, so several [source] values
# below are byte-identical. U13 would normally hoist them. Two gates say
# otherwise for program text specifically:
#   * `check_fixtures.py` (the rule 9 gate) searches only [source] values and
#     step `body` fields. A hoisted body would live in [constants] and the
#     reference would be a `${...}` placeholder, so the gate that exists to catch
#     a corrupted fixture would go red on a correct file -- and, worse, could be
#     silenced by relaxing it.
#   * U13 records the counter-hazard itself: [constants] IS a surface
#     `audit-case-migration.py` searches for assertion strings, so hoisting
#     program text moves a whole JS program onto the surface a claim can be
#     satisfied from, weakening the rule 3 gate for the entire file.
# The identity of the duplicated bodies is asserted mechanically rather than
# eyeballed: this file is generated, every duplicate is the SAME Python string
# object returned by ONE extraction call in
# tools/task-18-browser-pilot/gen_batch4_group_a.py, and the generator asserts
# that object identity for every [source] value before emitting. Matches the shipped browser/ precedent for a declined matrix
# (`math_atan2_global_this_root.toml`, `for_of_array_iteration_alias_chain.toml`).
#
# U2 -- [source] is file-wide, and that is safe here. Every fixture is written
# unconditionally into a fresh temp dir by the source, none sits behind an `if`,
# and no case's point is a file's presence or absence. Each command names its
# entry explicitly on argv, so the sibling fixtures in a trial dir are inert;
# verified against the real binary that a `test` run in a directory containing
# all four `smoke.test.<ext>` files still reports total/passed/failed = 1/1/0 for
# the one it was given, and that a `build --bundle` of `app.<ext>` in a directory
# containing all four `app.*` still emits `app/app.meta.json`.
#
# RULE 13 (transitive helper docs): `grep -c '///' browser_math_expm1_log1p_frozen_aliases.rs` returns 0 --
# no fn in any call chain of this file carries a doc comment, so there is none to
# carry. The chain does reach `kali_runtime_contract`'s
# `browser_bundle_harness_script` and `browser_harness_command_parts_for`, which
# carry one-line `///` docs, but the migrated case file never calls them: the
# `browser_bundle_harness` step kind means the case RUNNER does, so those docs
# describe shared runner infrastructure (design spec 5.3) rather than what this
# case claims. Same disposition as the already-shipped `math_log2_log10.toml`
# header, flagged there as a standing question rather than decided per file.
#
# ASSERTION SHAPE, mirrored from the source and nothing more.
# Bundle helper: `exit = "success"` on the build (:76) and on the harness process
# (:128); in json mode schemaVersion/command/success/exitCode and payload
# artifactKind/bundleFormat (:85-91) and NO `errors` claim; the emitted
# `app/app.meta.json` metadata (:99-100), read outside the `if json_output`;
# then the bundle-harness `stdout_contains = ["0\n"]` (:135).
# Harness helper: `exit = "success"` (:167); non-json `stdout_contains = ["0\n"]`
# (:199); json mode schemaVersion/command/success/payload(hostContract,
# runtimeBackend) (:176-180), `exitCode` plus `payload.exitCode` for run
# (:182-183) or payload total/passed/failed for test (:185-187), the exact
# `json.stdout` pin resolving :189-195, and `stderr = ""` (:196). No `errors`
# assertion on either envelope anywhere in this file, so `errors = []` appears
# nowhere below.
# The source DOES pass `--max-threads 0 --max-spawned-processes 0` on the harness
# argv (:159-162); the build argv has neither. The env var is spelled as a
# literal (:151) and resolves to KALI_BROWSER_BUNDLE_HARNESS_COMMAND. No
# `.matches(...).count()` claim exists in this file.
#
# The `json` branch's stdout claim is `json["stdout"].as_str().contains(...)`.
# That leaf has no substring form: design spec 5.4's twelve keys include no
# JSON-substring key at all (only exact `json` paths, `json_null` and
# `json_count`). So per controller ruling 3 -- a plain `.contains` against a
# field with NO substring form becomes an exact pin -- it is written as an exact
# `json.stdout` pin, and per U9 the pinned value was captured by running the real
# `kali` binary with `node` as the browser harness backend, never hand-computed. Captured value for both commands and all four extensions:
# "0\n0\n0\n0\n0\n0\n" -- six zeros, because the program takes expm1 and log1p
# through three separately frozen Math roots.

[source]
"app.js" = """// kali-tree-shake: frozenMathExpm1Log1pIdentities
function frozenMathExpm1Log1pIdentities() {
  const zero = 0;
  const frozenDotRoot = Object.freeze(globalThis.Math);
  const frozenMixedRoot = Object.freeze(globalThis["Math"]);
  const frozenDirectRoot = Object.freeze(Math);
  console.log(frozenDotRoot.expm1(zero));
  console.log(frozenMixedRoot.expm1(zero));
  console.log(frozenDirectRoot.expm1(zero));
  console.log(frozenDotRoot.log1p(zero));
  console.log(frozenMixedRoot.log1p(zero));
  console.log(frozenDirectRoot.log1p(zero));
  return [
    frozenDotRoot.expm1(zero),
    frozenMixedRoot.expm1(zero),
    frozenDirectRoot.expm1(zero),
    frozenDotRoot.log1p(zero),
    frozenMixedRoot.log1p(zero),
    frozenDirectRoot.log1p(zero),
  ];
}
"""
"app.ts" = """// kali-tree-shake: frozenMathExpm1Log1pIdentities
function frozenMathExpm1Log1pIdentities() {
  const zero = 0;
  const frozenDotRoot = Object.freeze(globalThis.Math);
  const frozenMixedRoot = Object.freeze(globalThis["Math"]);
  const frozenDirectRoot = Object.freeze(Math);
  console.log(frozenDotRoot.expm1(zero));
  console.log(frozenMixedRoot.expm1(zero));
  console.log(frozenDirectRoot.expm1(zero));
  console.log(frozenDotRoot.log1p(zero));
  console.log(frozenMixedRoot.log1p(zero));
  console.log(frozenDirectRoot.log1p(zero));
  return [
    frozenDotRoot.expm1(zero),
    frozenMixedRoot.expm1(zero),
    frozenDirectRoot.expm1(zero),
    frozenDotRoot.log1p(zero),
    frozenMixedRoot.log1p(zero),
    frozenDirectRoot.log1p(zero),
  ];
}
"""
"app.jsx" = """// kali-tree-shake: frozenMathExpm1Log1pIdentities
function frozenMathExpm1Log1pIdentities() {
  const zero = 0;
  const frozenDotRoot = Object.freeze(globalThis.Math);
  const frozenMixedRoot = Object.freeze(globalThis["Math"]);
  const frozenDirectRoot = Object.freeze(Math);
  console.log(frozenDotRoot.expm1(zero));
  console.log(frozenMixedRoot.expm1(zero));
  console.log(frozenDirectRoot.expm1(zero));
  console.log(frozenDotRoot.log1p(zero));
  console.log(frozenMixedRoot.log1p(zero));
  console.log(frozenDirectRoot.log1p(zero));
  return [
    frozenDotRoot.expm1(zero),
    frozenMixedRoot.expm1(zero),
    frozenDirectRoot.expm1(zero),
    frozenDotRoot.log1p(zero),
    frozenMixedRoot.log1p(zero),
    frozenDirectRoot.log1p(zero),
  ];
}
"""
"app.tsx" = """// kali-tree-shake: frozenMathExpm1Log1pIdentities
function frozenMathExpm1Log1pIdentities() {
  const zero = 0;
  const frozenDotRoot = Object.freeze(globalThis.Math);
  const frozenMixedRoot = Object.freeze(globalThis["Math"]);
  const frozenDirectRoot = Object.freeze(Math);
  console.log(frozenDotRoot.expm1(zero));
  console.log(frozenMixedRoot.expm1(zero));
  console.log(frozenDirectRoot.expm1(zero));
  console.log(frozenDotRoot.log1p(zero));
  console.log(frozenMixedRoot.log1p(zero));
  console.log(frozenDirectRoot.log1p(zero));
  return [
    frozenDotRoot.expm1(zero),
    frozenMixedRoot.expm1(zero),
    frozenDirectRoot.expm1(zero),
    frozenDotRoot.log1p(zero),
    frozenMixedRoot.log1p(zero),
    frozenDirectRoot.log1p(zero),
  ];
}
"""
"main.js" = """const zero = 0; const frozenDotRoot = Object.freeze(globalThis.Math); const frozenMixedRoot = Object.freeze(globalThis["Math"]); const frozenDirectRoot = Object.freeze(Math); console.log(frozenDotRoot.expm1(zero)); console.log(frozenMixedRoot.expm1(zero)); console.log(frozenDirectRoot.expm1(zero)); console.log(frozenDotRoot.log1p(zero)); console.log(frozenMixedRoot.log1p(zero)); console.log(frozenDirectRoot.log1p(zero));
"""
"main.ts" = """const zero = 0; const frozenDotRoot = Object.freeze(globalThis.Math); const frozenMixedRoot = Object.freeze(globalThis["Math"]); const frozenDirectRoot = Object.freeze(Math); console.log(frozenDotRoot.expm1(zero)); console.log(frozenMixedRoot.expm1(zero)); console.log(frozenDirectRoot.expm1(zero)); console.log(frozenDotRoot.log1p(zero)); console.log(frozenMixedRoot.log1p(zero)); console.log(frozenDirectRoot.log1p(zero));
"""
"main.jsx" = """const zero = 0; const frozenDotRoot = Object.freeze(globalThis.Math); const frozenMixedRoot = Object.freeze(globalThis["Math"]); const frozenDirectRoot = Object.freeze(Math); console.log(frozenDotRoot.expm1(zero)); console.log(frozenMixedRoot.expm1(zero)); console.log(frozenDirectRoot.expm1(zero)); console.log(frozenDotRoot.log1p(zero)); console.log(frozenMixedRoot.log1p(zero)); console.log(frozenDirectRoot.log1p(zero));
"""
"main.tsx" = """const zero = 0; const frozenDotRoot = Object.freeze(globalThis.Math); const frozenMixedRoot = Object.freeze(globalThis["Math"]); const frozenDirectRoot = Object.freeze(Math); console.log(frozenDotRoot.expm1(zero)); console.log(frozenMixedRoot.expm1(zero)); console.log(frozenDirectRoot.expm1(zero)); console.log(frozenDotRoot.log1p(zero)); console.log(frozenMixedRoot.log1p(zero)); console.log(frozenDirectRoot.log1p(zero));
"""
"smoke.test.js" = """Kali.test('frozen math expm1/log1p identities', () => {
  const zero = 0;
  const frozenDotRoot = Object.freeze(globalThis.Math);
  const frozenMixedRoot = Object.freeze(globalThis["Math"]);
  const frozenDirectRoot = Object.freeze(Math);
  console.log(frozenDotRoot.expm1(zero));
  console.log(frozenMixedRoot.expm1(zero));
  console.log(frozenDirectRoot.expm1(zero));
  console.log(frozenDotRoot.log1p(zero));
  console.log(frozenMixedRoot.log1p(zero));
  console.log(frozenDirectRoot.log1p(zero));
});
"""
"smoke.test.ts" = """Kali.test('frozen math expm1/log1p identities', () => {
  const zero = 0;
  const frozenDotRoot = Object.freeze(globalThis.Math);
  const frozenMixedRoot = Object.freeze(globalThis["Math"]);
  const frozenDirectRoot = Object.freeze(Math);
  console.log(frozenDotRoot.expm1(zero));
  console.log(frozenMixedRoot.expm1(zero));
  console.log(frozenDirectRoot.expm1(zero));
  console.log(frozenDotRoot.log1p(zero));
  console.log(frozenMixedRoot.log1p(zero));
  console.log(frozenDirectRoot.log1p(zero));
});
"""
"smoke.test.jsx" = """Kali.test('frozen math expm1/log1p identities', () => {
  const zero = 0;
  const frozenDotRoot = Object.freeze(globalThis.Math);
  const frozenMixedRoot = Object.freeze(globalThis["Math"]);
  const frozenDirectRoot = Object.freeze(Math);
  console.log(frozenDotRoot.expm1(zero));
  console.log(frozenMixedRoot.expm1(zero));
  console.log(frozenDirectRoot.expm1(zero));
  console.log(frozenDotRoot.log1p(zero));
  console.log(frozenMixedRoot.log1p(zero));
  console.log(frozenDirectRoot.log1p(zero));
});
"""
"smoke.test.tsx" = """Kali.test('frozen math expm1/log1p identities', () => {
  const zero = 0;
  const frozenDotRoot = Object.freeze(globalThis.Math);
  const frozenMixedRoot = Object.freeze(globalThis["Math"]);
  const frozenDirectRoot = Object.freeze(Math);
  console.log(frozenDotRoot.expm1(zero));
  console.log(frozenMixedRoot.expm1(zero));
  console.log(frozenDirectRoot.expm1(zero));
  console.log(frozenDotRoot.log1p(zero));
  console.log(frozenMixedRoot.log1p(zero));
  console.log(frozenDirectRoot.log1p(zero));
});
"""

[[case]]
name = "build_emits_frozen_math_expm1_log1p_identity_literals_in_js_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_bundle_frozen_math_expm1_log1p` builds a browser bundle (`kali build --bundle --api browser`), asserts the emitted `app/app.meta.json` metadata, then runs the bundle glue under the browser-bundle-harness contract. The bundled program freezes three spellings of the Math root with `Object.freeze` -- a dotted `globalThis.Math`, a bracketed `globalThis["Math"]` and a bare `Math` -- and calls expm1/log1p at zero through each, so six zero lines are printed. The source asserts only `stdout.contains("0\\n")` (:135), carried as `stdout_contains` at that strength; it is NOT strengthened to the six-line output that actually appears, because the source never claims it (controller ruling 3)."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "app.js"]
exit = "success"

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.frozenMathExpm1Log1pIdentities();
"""
exit = "success"
stdout_contains = ["0\n"]

[[case]]
name = "build_emits_frozen_math_expm1_log1p_identity_literals_in_ts_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_bundle_frozen_math_expm1_log1p` builds a browser bundle (`kali build --bundle --api browser`), asserts the emitted `app/app.meta.json` metadata, then runs the bundle glue under the browser-bundle-harness contract. The bundled program freezes three spellings of the Math root with `Object.freeze` -- a dotted `globalThis.Math`, a bracketed `globalThis["Math"]` and a bare `Math` -- and calls expm1/log1p at zero through each, so six zero lines are printed. The source asserts only `stdout.contains("0\\n")` (:135), carried as `stdout_contains` at that strength; it is NOT strengthened to the six-line output that actually appears, because the source never claims it (controller ruling 3)."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "app.ts"]
exit = "success"

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.frozenMathExpm1Log1pIdentities();
"""
exit = "success"
stdout_contains = ["0\n"]

[[case]]
name = "json_build_emits_frozen_math_expm1_log1p_identity_literals_in_js_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_bundle_frozen_math_expm1_log1p` builds a browser bundle (`kali build --bundle --api browser`), asserts the emitted `app/app.meta.json` metadata, then runs the bundle glue under the browser-bundle-harness contract. The bundled program freezes three spellings of the Math root with `Object.freeze` -- a dotted `globalThis.Math`, a bracketed `globalThis["Math"]` and a bare `Math` -- and calls expm1/log1p at zero through each, so six zero lines are printed. The source asserts only `stdout.contains("0\\n")` (:135), carried as `stdout_contains` at that strength; it is NOT strengthened to the six-line output that actually appears, because the source never claims it (controller ruling 3). This sibling asserts the JSON build envelope (schemaVersion/command/success/exitCode and payload artifactKind/bundleFormat, :85-91) instead of plain text; the source makes no `errors` claim on it, so none is written."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "--output", "json", "app.js"]
exit = "success"
json = { schemaVersion = 1, command = "build", success = true, exitCode = 0, payload = { artifactKind = "bundle", bundleFormat = "esm" } }

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.frozenMathExpm1Log1pIdentities();
"""
exit = "success"
stdout_contains = ["0\n"]

[[case]]
name = "json_build_emits_frozen_math_expm1_log1p_identity_literals_in_ts_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_bundle_frozen_math_expm1_log1p` builds a browser bundle (`kali build --bundle --api browser`), asserts the emitted `app/app.meta.json` metadata, then runs the bundle glue under the browser-bundle-harness contract. The bundled program freezes three spellings of the Math root with `Object.freeze` -- a dotted `globalThis.Math`, a bracketed `globalThis["Math"]` and a bare `Math` -- and calls expm1/log1p at zero through each, so six zero lines are printed. The source asserts only `stdout.contains("0\\n")` (:135), carried as `stdout_contains` at that strength; it is NOT strengthened to the six-line output that actually appears, because the source never claims it (controller ruling 3). This sibling asserts the JSON build envelope (schemaVersion/command/success/exitCode and payload artifactKind/bundleFormat, :85-91) instead of plain text; the source makes no `errors` claim on it, so none is written."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "--output", "json", "app.ts"]
exit = "success"
json = { schemaVersion = 1, command = "build", success = true, exitCode = 0, payload = { artifactKind = "bundle", bundleFormat = "esm" } }

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.frozenMathExpm1Log1pIdentities();
"""
exit = "success"
stdout_contains = ["0\n"]

[[case]]
name = "build_emits_frozen_math_expm1_log1p_identity_literals_in_jsx_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_bundle_frozen_math_expm1_log1p` builds a browser bundle (`kali build --bundle --api browser`), asserts the emitted `app/app.meta.json` metadata, then runs the bundle glue under the browser-bundle-harness contract. The bundled program freezes three spellings of the Math root with `Object.freeze` -- a dotted `globalThis.Math`, a bracketed `globalThis["Math"]` and a bare `Math` -- and calls expm1/log1p at zero through each, so six zero lines are printed. The source asserts only `stdout.contains("0\\n")` (:135), carried as `stdout_contains` at that strength; it is NOT strengthened to the six-line output that actually appears, because the source never claims it (controller ruling 3). This case is one of the four rule-5 siblings split out of the single #[test] fn `build_emits_frozen_math_expm1_log1p_identity_literals_in_jsx_and_tsx_input` (:224-229), which loops over ["app.jsx", "app.tsx"] and calls the helper twice per iteration (json_output false then true) -- four independent invocations, four siblings, no folding."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "app.jsx"]
exit = "success"

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.frozenMathExpm1Log1pIdentities();
"""
exit = "success"
stdout_contains = ["0\n"]

[[case]]
name = "json_build_emits_frozen_math_expm1_log1p_identity_literals_in_jsx_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_bundle_frozen_math_expm1_log1p` builds a browser bundle (`kali build --bundle --api browser`), asserts the emitted `app/app.meta.json` metadata, then runs the bundle glue under the browser-bundle-harness contract. The bundled program freezes three spellings of the Math root with `Object.freeze` -- a dotted `globalThis.Math`, a bracketed `globalThis["Math"]` and a bare `Math` -- and calls expm1/log1p at zero through each, so six zero lines are printed. The source asserts only `stdout.contains("0\\n")` (:135), carried as `stdout_contains` at that strength; it is NOT strengthened to the six-line output that actually appears, because the source never claims it (controller ruling 3). This case is one of the four rule-5 siblings split out of the single #[test] fn `build_emits_frozen_math_expm1_log1p_identity_literals_in_jsx_and_tsx_input` (:224-229), which loops over ["app.jsx", "app.tsx"] and calls the helper twice per iteration (json_output false then true) -- four independent invocations, four siblings, no folding. This sibling asserts the JSON build envelope (schemaVersion/command/success/exitCode and payload artifactKind/bundleFormat, :85-91) instead of plain text; the source makes no `errors` claim on it, so none is written."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "--output", "json", "app.jsx"]
exit = "success"
json = { schemaVersion = 1, command = "build", success = true, exitCode = 0, payload = { artifactKind = "bundle", bundleFormat = "esm" } }

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.frozenMathExpm1Log1pIdentities();
"""
exit = "success"
stdout_contains = ["0\n"]

[[case]]
name = "build_emits_frozen_math_expm1_log1p_identity_literals_in_tsx_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_bundle_frozen_math_expm1_log1p` builds a browser bundle (`kali build --bundle --api browser`), asserts the emitted `app/app.meta.json` metadata, then runs the bundle glue under the browser-bundle-harness contract. The bundled program freezes three spellings of the Math root with `Object.freeze` -- a dotted `globalThis.Math`, a bracketed `globalThis["Math"]` and a bare `Math` -- and calls expm1/log1p at zero through each, so six zero lines are printed. The source asserts only `stdout.contains("0\\n")` (:135), carried as `stdout_contains` at that strength; it is NOT strengthened to the six-line output that actually appears, because the source never claims it (controller ruling 3). This case is one of the four rule-5 siblings split out of the single #[test] fn `build_emits_frozen_math_expm1_log1p_identity_literals_in_jsx_and_tsx_input` (:224-229), which loops over ["app.jsx", "app.tsx"] and calls the helper twice per iteration (json_output false then true) -- four independent invocations, four siblings, no folding."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "app.tsx"]
exit = "success"

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.frozenMathExpm1Log1pIdentities();
"""
exit = "success"
stdout_contains = ["0\n"]

[[case]]
name = "json_build_emits_frozen_math_expm1_log1p_identity_literals_in_tsx_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_bundle_frozen_math_expm1_log1p` builds a browser bundle (`kali build --bundle --api browser`), asserts the emitted `app/app.meta.json` metadata, then runs the bundle glue under the browser-bundle-harness contract. The bundled program freezes three spellings of the Math root with `Object.freeze` -- a dotted `globalThis.Math`, a bracketed `globalThis["Math"]` and a bare `Math` -- and calls expm1/log1p at zero through each, so six zero lines are printed. The source asserts only `stdout.contains("0\\n")` (:135), carried as `stdout_contains` at that strength; it is NOT strengthened to the six-line output that actually appears, because the source never claims it (controller ruling 3). This case is one of the four rule-5 siblings split out of the single #[test] fn `build_emits_frozen_math_expm1_log1p_identity_literals_in_jsx_and_tsx_input` (:224-229), which loops over ["app.jsx", "app.tsx"] and calls the helper twice per iteration (json_output false then true) -- four independent invocations, four siblings, no folding. This sibling asserts the JSON build envelope (schemaVersion/command/success/exitCode and payload artifactKind/bundleFormat, :85-91) instead of plain text; the source makes no `errors` claim on it, so none is written."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "--output", "json", "app.tsx"]
exit = "success"
json = { schemaVersion = 1, command = "build", success = true, exitCode = 0, payload = { artifactKind = "bundle", bundleFormat = "esm" } }

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.frozenMathExpm1Log1pIdentities();
"""
exit = "success"
stdout_contains = ["0\n"]

[[case]]
name = "run_supports_frozen_math_expm1_log1p_identity_literals_when_browser_harness_is_configured_in_js_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_harness_frozen_math_expm1_log1p` runs `kali run --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program that freezes three spellings of the Math root and calls expm1/log1p at zero through each. This is the non-json branch (:197-200): the only output claim is `stdout.contains("0\\n")`, carried as `stdout_contains` at the same strength."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.js"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["0\n"]

[[case]]
name = "run_supports_frozen_math_expm1_log1p_identity_literals_when_browser_harness_is_configured_in_ts_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_harness_frozen_math_expm1_log1p` runs `kali run --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program that freezes three spellings of the Math root and calls expm1/log1p at zero through each. This is the non-json branch (:197-200): the only output claim is `stdout.contains("0\\n")`, carried as `stdout_contains` at the same strength."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.ts"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["0\n"]

[[case]]
name = "test_supports_frozen_math_expm1_log1p_identity_literals_when_browser_harness_is_configured_in_js_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_harness_frozen_math_expm1_log1p` runs `kali test --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program that freezes three spellings of the Math root and calls expm1/log1p at zero through each. This is the non-json branch (:197-200): the only output claim is `stdout.contains("0\\n")`, carried as `stdout_contains` at the same strength."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.js"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["0\n"]

[[case]]
name = "test_supports_frozen_math_expm1_log1p_identity_literals_when_browser_harness_is_configured_in_ts_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_harness_frozen_math_expm1_log1p` runs `kali test --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program that freezes three spellings of the Math root and calls expm1/log1p at zero through each. This is the non-json branch (:197-200): the only output claim is `stdout.contains("0\\n")`, carried as `stdout_contains` at the same strength."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.ts"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["0\n"]

[[case]]
name = "run_supports_frozen_math_expm1_log1p_identity_literals_when_browser_harness_is_configured_in_json_js_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_harness_frozen_math_expm1_log1p` runs `kali --output json run --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program that freezes three spellings of the Math root and calls expm1/log1p at zero through each. This is the json branch (:174-196). The `json` branch's stdout claim is `json["stdout"].as_str().contains(...)`. That leaf has no substring form: design spec 5.4's twelve keys include no JSON-substring key at all (only exact `json` paths, `json_null` and `json_count`). So per controller ruling 3 -- a plain `.contains` against a field with NO substring form becomes an exact pin -- it is written as an exact `json.stdout` pin, and per U9 the pinned value was captured by running the real `kali` binary with `node` as the browser harness backend, never hand-computed. The captured value is "0\\n0\\n0\\n0\\n0\\n0\\n". `stderr` is asserted exactly empty (:196); the source makes NO `errors` claim on this envelope, so none is written (rule 2)."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.js"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "run", success = true, exitCode = 0, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", exitCode = 0 }, stdout = "0\n0\n0\n0\n0\n0\n", stderr = "" }

[[case]]
name = "test_supports_frozen_math_expm1_log1p_identity_literals_when_browser_harness_is_configured_in_json_js_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_harness_frozen_math_expm1_log1p` runs `kali --output json test --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program that freezes three spellings of the Math root and calls expm1/log1p at zero through each. This is the json branch (:174-196). The `json` branch's stdout claim is `json["stdout"].as_str().contains(...)`. That leaf has no substring form: design spec 5.4's twelve keys include no JSON-substring key at all (only exact `json` paths, `json_null` and `json_count`). So per controller ruling 3 -- a plain `.contains` against a field with NO substring form becomes an exact pin -- it is written as an exact `json.stdout` pin, and per U9 the pinned value was captured by running the real `kali` binary with `node` as the browser harness backend, never hand-computed. The captured value is "0\\n0\\n0\\n0\\n0\\n0\\n". `stderr` is asserted exactly empty (:196); the source makes NO `errors` claim on this envelope, so none is written (rule 2)."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.js"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "test", success = true, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", total = 1, passed = 1, failed = 0 }, stdout = "0\n0\n0\n0\n0\n0\n", stderr = "" }

[[case]]
name = "run_supports_frozen_math_expm1_log1p_identity_literals_when_browser_harness_is_configured_in_json_ts_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_harness_frozen_math_expm1_log1p` runs `kali --output json run --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program that freezes three spellings of the Math root and calls expm1/log1p at zero through each. This is the json branch (:174-196). The `json` branch's stdout claim is `json["stdout"].as_str().contains(...)`. That leaf has no substring form: design spec 5.4's twelve keys include no JSON-substring key at all (only exact `json` paths, `json_null` and `json_count`). So per controller ruling 3 -- a plain `.contains` against a field with NO substring form becomes an exact pin -- it is written as an exact `json.stdout` pin, and per U9 the pinned value was captured by running the real `kali` binary with `node` as the browser harness backend, never hand-computed. The captured value is "0\\n0\\n0\\n0\\n0\\n0\\n". `stderr` is asserted exactly empty (:196); the source makes NO `errors` claim on this envelope, so none is written (rule 2)."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.ts"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "run", success = true, exitCode = 0, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", exitCode = 0 }, stdout = "0\n0\n0\n0\n0\n0\n", stderr = "" }

[[case]]
name = "test_supports_frozen_math_expm1_log1p_identity_literals_when_browser_harness_is_configured_in_json_ts_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_harness_frozen_math_expm1_log1p` runs `kali --output json test --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program that freezes three spellings of the Math root and calls expm1/log1p at zero through each. This is the json branch (:174-196). The `json` branch's stdout claim is `json["stdout"].as_str().contains(...)`. That leaf has no substring form: design spec 5.4's twelve keys include no JSON-substring key at all (only exact `json` paths, `json_null` and `json_count`). So per controller ruling 3 -- a plain `.contains` against a field with NO substring form becomes an exact pin -- it is written as an exact `json.stdout` pin, and per U9 the pinned value was captured by running the real `kali` binary with `node` as the browser harness backend, never hand-computed. The captured value is "0\\n0\\n0\\n0\\n0\\n0\\n". `stderr` is asserted exactly empty (:196); the source makes NO `errors` claim on this envelope, so none is written (rule 2)."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.ts"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "test", success = true, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", total = 1, passed = 1, failed = 0 }, stdout = "0\n0\n0\n0\n0\n0\n", stderr = "" }

[[case]]
name = "json_run_supports_frozen_math_expm1_log1p_identity_literals_when_browser_harness_is_configured_in_jsx_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_harness_frozen_math_expm1_log1p` runs `kali --output json run --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program that freezes three spellings of the Math root and calls expm1/log1p at zero through each. This is the json branch (:174-196). The `json` branch's stdout claim is `json["stdout"].as_str().contains(...)`. That leaf has no substring form: design spec 5.4's twelve keys include no JSON-substring key at all (only exact `json` paths, `json_null` and `json_count`). So per controller ruling 3 -- a plain `.contains` against a field with NO substring form becomes an exact pin -- it is written as an exact `json.stdout` pin, and per U9 the pinned value was captured by running the real `kali` binary with `node` as the browser harness backend, never hand-computed. The captured value is "0\\n0\\n0\\n0\\n0\\n0\\n". `stderr` is asserted exactly empty (:196); the source makes NO `errors` claim on this envelope, so none is written (rule 2)."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.jsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "run", success = true, exitCode = 0, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", exitCode = 0 }, stdout = "0\n0\n0\n0\n0\n0\n", stderr = "" }

[[case]]
name = "json_run_supports_frozen_math_expm1_log1p_identity_literals_when_browser_harness_is_configured_in_tsx_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_harness_frozen_math_expm1_log1p` runs `kali --output json run --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program that freezes three spellings of the Math root and calls expm1/log1p at zero through each. This is the json branch (:174-196). The `json` branch's stdout claim is `json["stdout"].as_str().contains(...)`. That leaf has no substring form: design spec 5.4's twelve keys include no JSON-substring key at all (only exact `json` paths, `json_null` and `json_count`). So per controller ruling 3 -- a plain `.contains` against a field with NO substring form becomes an exact pin -- it is written as an exact `json.stdout` pin, and per U9 the pinned value was captured by running the real `kali` binary with `node` as the browser harness backend, never hand-computed. The captured value is "0\\n0\\n0\\n0\\n0\\n0\\n". `stderr` is asserted exactly empty (:196); the source makes NO `errors` claim on this envelope, so none is written (rule 2)."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.tsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "run", success = true, exitCode = 0, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", exitCode = 0 }, stdout = "0\n0\n0\n0\n0\n0\n", stderr = "" }

[[case]]
name = "json_test_supports_frozen_math_expm1_log1p_identity_literals_when_browser_harness_is_configured_in_jsx_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_harness_frozen_math_expm1_log1p` runs `kali --output json test --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program that freezes three spellings of the Math root and calls expm1/log1p at zero through each. This is the json branch (:174-196). The `json` branch's stdout claim is `json["stdout"].as_str().contains(...)`. That leaf has no substring form: design spec 5.4's twelve keys include no JSON-substring key at all (only exact `json` paths, `json_null` and `json_count`). So per controller ruling 3 -- a plain `.contains` against a field with NO substring form becomes an exact pin -- it is written as an exact `json.stdout` pin, and per U9 the pinned value was captured by running the real `kali` binary with `node` as the browser harness backend, never hand-computed. The captured value is "0\\n0\\n0\\n0\\n0\\n0\\n". `stderr` is asserted exactly empty (:196); the source makes NO `errors` claim on this envelope, so none is written (rule 2)."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.jsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "test", success = true, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", total = 1, passed = 1, failed = 0 }, stdout = "0\n0\n0\n0\n0\n0\n", stderr = "" }

[[case]]
name = "json_test_supports_frozen_math_expm1_log1p_identity_literals_when_browser_harness_is_configured_in_tsx_input"
rationale = """Migrated from browser_math_expm1_log1p_frozen_aliases.rs. `assert_browser_harness_frozen_math_expm1_log1p` runs `kali --output json test --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program that freezes three spellings of the Math root and calls expm1/log1p at zero through each. This is the json branch (:174-196). The `json` branch's stdout claim is `json["stdout"].as_str().contains(...)`. That leaf has no substring form: design spec 5.4's twelve keys include no JSON-substring key at all (only exact `json` paths, `json_null` and `json_count`). So per controller ruling 3 -- a plain `.contains` against a field with NO substring form becomes an exact pin -- it is written as an exact `json.stdout` pin, and per U9 the pinned value was captured by running the real `kali` binary with `node` as the browser harness backend, never hand-computed. The captured value is "0\\n0\\n0\\n0\\n0\\n0\\n". `stderr` is asserted exactly empty (:196); the source makes NO `errors` claim on this envelope, so none is written (rule 2)."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.tsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "test", success = true, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", total = 1, passed = 1, failed = 0 }, stdout = "0\n0\n0\n0\n0\n0\n", stderr = "" }
