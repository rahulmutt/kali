# EXTRA-CLAIM DECLARATIONS (U14's `extra` direction, fix round 1 / I6).
# check_extra_claims.py compares this file's claim strings against the
# source's and fails on any that appear nowhere in the .rs. The entries
# below are the deliberate exceptions; a genuinely new one will not be
# on this list and will fail the gate.
# EXTRA-OK: '3\n3\n3\n3\n' -- live-captured exact `json.stdout` pin; source asserts `.contains` on a JSON leaf, which has no substring form, so ruling 3 requires an exact pin captured from the real binary
# Migrated from tests/browser_math_log2_log10_fully_bracketed_root.rs.
#
# RULE 12 (carry every source comment verbatim): `browser_math_log2_log10_fully_bracketed_root.rs` contains no
# Rust comments at all -- `grep -nE '^\s*//'` finds nothing, and the only `//`
# anywhere in the file is the `// kali-tree-shake:` marker on line 11, inside a
# JS fixture body, which is program text and is carried verbatim into [source].
# There is therefore no prose to move into any `rationale`, and no `///` doc
# comment on any helper in either call chain (checked: `grep -n '///'` finds
# nothing). comment_coverage.py is run with --allow-empty for this pair.
#
# RULE 7 / U1 -- MATRIX ARITHMETIC, closes exactly, over a TWO-VALUE axis.
# 5 #[test] fns, 12 real invocations (enumerated with
# tools/task-18-browser-pilot/enumerate_invocations.py, which parses this file
# completely because both groups call a named assert_* helper):
#   * `assert_browser_bundle_fully_bracketed_global_this_math_log2_log10`(
#     filename, json_output) -- 4 invocations from 4 one-line #[test] fns =
#     ext(js/ts) x json_output(false/true). This file has NO jsx/tsx tests.
#   * `assert_browser_harness_fully_bracketed_global_this_math_log2_log10`(
#     command, filename, source, json_output) -- 8 invocations from the single
#     #[test] fn
#     `run_and_test_supports_fully_bracketed_global_this_math_log2_log10_identities_when_browser_harness_is_configured_in_js_and_ts_input`,
#     which loops over four `(command, source_name, source)` tuples --
#     (run, main.js), (test, smoke.test.js), (run, main.ts),
#     (test, smoke.test.ts) -- and then over `for output_json in [false, true]`.
#     The fn name is accurate here: js and ts only.
# Both groups vary over ext(js/ts) uniformly and completely, and NEITHER covers
# jsx/tsx, so the axis is `["js", "ts"]`: 6 [[case]] x ext(2) = 12 trials = 12
# invocations. Adding jsx/tsx would fan every case over cells the source never
# runs (rule 2) and break the arithmetic (rule 7); `[matrix]` is file-wide with
# no per-case opt-out (U1), so the axis values are exactly what is exercised.
#
# RULE 5 / RULE 6. The 8 harness invocations run 4 independently-written
# programs, so they become 4 descriptively-named sibling [[case]] entries
# (x 2 ext cells), never one folded case. Per rule 6 the matrix fold is stated
# here: each bundle [[case]] corresponds to 2 source #[test] fns (one per ext
# cell) and each harness [[case]] to 2 iterations of the single looping fn; the
# assertion mapping stays 1:1 per trial.
#
# U2 ([source] is file-wide): safe here. All three fixtures are written
# unconditionally into a fresh temp dir, none is behind an `if`, and no case's
# point is a file's presence or absence -- every command names its entry
# explicitly on argv. The `${ext}` suffix keeps each matrix cell's three files
# distinct, so no [source] key needs U5 disambiguation.
#
# RULE 13 (transitive helper docs): every fn in both call chains was checked for
# a `///` doc comment and none of this file's own helpers carries one. The bundle
# chain reaches `kali_runtime_contract::browser_bundle_harness_script` and
# `::browser_harness_command_parts_for`, which do carry one-line `///` docs, but
# the migrated form never calls them -- the `browser_bundle_harness` step kind
# means the case RUNNER does (design spec 5.3), so those docs describe shared
# runner infrastructure, not what this case claims. Consistent with every case
# file shipped before batch 4.
#
# THE COUNT CLAIM (ruling 3 -- mirror the source). ONE site: `:132`,
# `assert!(stdout.matches("3\n").count() >= 2)`, on the RAW stdout of the
# bundle harness process, carried as
# `stdout_count = [{ needle = "3\n", at_least = 2 }]`. It is not weakened to
# a `*_contains` (one occurrence would satisfy that, and this program's whole
# point is that four independent evaluations each print 3) and it is not
# strengthened to `exact` even though the live output is observably `3\n` four
# times: the source states a lower bound, and pinning equality would assert
# something it never did (rule 2). The immediately preceding line `:131`,
# `assert!(stdout.contains("3\n"))`, is a SEPARATE source claim about the same
# needle and is carried as well, as `stdout_contains = ["3\n"]` -- both are
# kept, per the rule that a `.contains` and a `.count()` about one needle are
# two claims. There is NO `json_count` anywhere in this file: the harness
# helper's `if json_output` branch (:186-192) makes only a plain
# `.contains("3\n")` against `json["stdout"].as_str()`, never a count, so
# inventing a `json_count` there would be a rule 2 violation.
#
# ASSERTION SHAPE, mirrored and nothing more. Bundle: `exit = "success"`; the
# json sibling adds schemaVersion/command/success/exitCode, payload
# artifactKind/bundleFormat, AND `errors = []` -- this file DOES assert the
# build envelope's errors array is empty (:84-87), unlike the four other
# root-shape files migrated alongside it, which do not; then the emitted
# `app/app.meta.json` apiSurface/artifactKind; then the bundle-harness
# `stdout_contains` + `stdout_count` above and the harness process's own
# `exit = "success"` (:124). Harness: argv DOES carry `--max-threads 0
# --max-spawned-processes 0` (:156-159); env
# KALI_BROWSER_BUNDLE_HARNESS_COMMAND=node; text mode asserts
# `stdout.contains("3\n")` (:196); json mode asserts
# schemaVersion/command/success/payload(hostContract, runtimeBackend),
# `exitCode` + `payload.exitCode` for run or `payload.total/passed/failed` for
# test, an exact `json.stdout` pin and `json.stderr = ""` (:193). The harness
# envelope gets NO `errors` claim -- the source makes none there, only on the
# build envelope.
#
# U9 -- the exact `json.stdout` pin. The source's claim is
# `json["stdout"].as_str().contains("3\n")` (:186-192); a json leaf has no
# substring form, so ruling 3 resolves it to an exact pin. Captured from the
# real binary for all 4 (ext x command) cells: every one returns
# `3\n3\n3\n3\n` -- log2(8), the frozen alias of log2 applied to 8,
# log10(1000) and the frozen alias of log10 applied to 1000 all print 3 -- so
# the single pin holds in both matrix cells and it contains the source's
# literal.

[matrix]
ext = ["js", "ts"]

[source]
"app.${ext}" = """// kali-tree-shake: fullyBracketedGlobalThisMathLog2Log10Identities
function fullyBracketedGlobalThisMathLog2Log10Identities() {
  const log2Value = 8;
  const log10Value = 1000;
  const frozenLog2 = Object.freeze(globalThis["Math"]["log2"]);
  const frozenLog10 = Object.freeze(globalThis["Math"]["log10"]);
  console.log(globalThis["Math"]["log2"](log2Value));
  console.log(frozenLog2(log2Value));
  console.log(globalThis["Math"]["log10"](log10Value));
  console.log(frozenLog10(log10Value));
  return [globalThis["Math"]["log2"](log2Value), globalThis["Math"]["log10"](log10Value), frozenLog2(log2Value), frozenLog10(log10Value)];
}
"""
"main.${ext}" = """const frozenLog2 = Object.freeze(globalThis["Math"]["log2"]); const frozenLog10 = Object.freeze(globalThis["Math"]["log10"]); const log2Value = 8; const log10Value = 1000; console.log(globalThis["Math"]["log2"](log2Value)); console.log(frozenLog2(log2Value)); console.log(globalThis["Math"]["log10"](log10Value)); console.log(frozenLog10(log10Value));
"""
"smoke.test.${ext}" = """Kali.test('fully bracketed log2/log10 identities', () => {
  const frozenLog2 = Object.freeze(globalThis["Math"]["log2"]);
  const frozenLog10 = Object.freeze(globalThis["Math"]["log10"]);
  const log2Value = 8;
  const log10Value = 1000;
  console.log(globalThis["Math"]["log2"](log2Value));
  console.log(frozenLog2(log2Value));
  console.log(globalThis["Math"]["log10"](log10Value));
  console.log(frozenLog10(log10Value));
});
"""

[[case]]
name = "build_emits_fully_bracketed_global_this_math_log2_log10_identity_literals"
rationale = """Migrated from browser_math_log2_log10_fully_bracketed_root.rs. `assert_browser_bundle_fully_bracketed_global_this_math_log2_log10` builds a browser bundle (`kali build --bundle --api browser`), asserts the emitted `app/app.meta.json` metadata, then runs the bundle glue under the browser-bundle-harness contract. The bundled program reaches Math through a fully bracketed root -- `globalThis["Math"]["log2"]` and `globalThis["Math"]["log10"]` -- and calls each both directly and through an `Object.freeze`d reference, printing 3 four times. The source makes TWO separate claims about the needle `3\\n` on this output and both are carried: a plain `.contains` (:131), mirrored as `stdout_contains`, and `stdout.matches("3\\n").count() >= 2` (:132), mirrored as `stdout_count` with `at_least = 2`. The count is not weakened to a `*_contains` (one occurrence would satisfy that) and not strengthened to `exact` (the source states a lower bound), per controller ruling 3 -- mirror the source."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "app.${ext}"]
exit = "success"

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.fullyBracketedGlobalThisMathLog2Log10Identities();
"""
exit = "success"
stdout_contains = ["3\n"]
stdout_count = [{ needle = "3\n", at_least = 2 }]

[[case]]
name = "json_build_emits_fully_bracketed_global_this_math_log2_log10_identity_literals"
rationale = """Migrated from browser_math_log2_log10_fully_bracketed_root.rs. `assert_browser_bundle_fully_bracketed_global_this_math_log2_log10` builds a browser bundle (`kali build --bundle --api browser --output json`), asserts the emitted `app/app.meta.json` metadata, then runs the bundle glue under the browser-bundle-harness contract. The bundled program reaches Math through a fully bracketed root -- `globalThis["Math"]["log2"]` and `globalThis["Math"]["log10"]` -- and calls each both directly and through an `Object.freeze`d reference, printing 3 four times. The source makes TWO separate claims about the needle `3\\n` on this output and both are carried: a plain `.contains` (:131), mirrored as `stdout_contains`, and `stdout.matches("3\\n").count() >= 2` (:132), mirrored as `stdout_count` with `at_least = 2`. The count is not weakened to a `*_contains` (one occurrence would satisfy that) and not strengthened to `exact` (the source states a lower bound), per controller ruling 3 -- mirror the source. This sibling additionally asserts the JSON build envelope: schemaVersion/command/success/exitCode and payload artifactKind/bundleFormat, and that `errors` is the empty array."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "--output", "json", "app.${ext}"]
exit = "success"
json = { schemaVersion = 1, command = "build", success = true, exitCode = 0, payload = { artifactKind = "bundle", bundleFormat = "esm" }, errors = [] }

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.fullyBracketedGlobalThisMathLog2Log10Identities();
"""
exit = "success"
stdout_contains = ["3\n"]
stdout_count = [{ needle = "3\n", at_least = 2 }]

[[case]]
name = "run_supports_fully_bracketed_global_this_math_log2_log10_identities_when_browser_harness_is_configured"
rationale = """Migrated from browser_math_log2_log10_fully_bracketed_root.rs. `assert_browser_harness_fully_bracketed_global_this_math_log2_log10` runs `kali run --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program that computes log2(8) and log10(1000) through a fully bracketed `globalThis["Math"][...]` root, both directly and through `Object.freeze`d references, printing 3 four times. This case is 2 of the 8 invocations the single #[test] fn `run_and_test_supports_fully_bracketed_global_this_math_log2_log10_identities_when_browser_harness_is_configured_in_js_and_ts_input` makes by looping over four (command, source_name, source) tuples and then over `for output_json in [false, true]`; per the split-don't-fold rule each independent program becomes its own named sibling [[case]], and the two extensions the loop covers are the file's `ext` matrix axis -- js and ts only, since this file has no jsx/tsx tests in either group. Text mode: a clean exit and `stdout.contains("3\\n")`, mirrored as `stdout_contains`. NOTE that the harness helper makes no count claim -- the `.matches("3\\n").count() >= 2` assertion lives only on the bundle-harness path, so no `stdout_count` is written here (inventing one would be a rule 2 violation)."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["3\n"]

[[case]]
name = "test_supports_fully_bracketed_global_this_math_log2_log10_identities_when_browser_harness_is_configured"
rationale = """Migrated from browser_math_log2_log10_fully_bracketed_root.rs. `assert_browser_harness_fully_bracketed_global_this_math_log2_log10` runs `kali test --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program that computes log2(8) and log10(1000) through a fully bracketed `globalThis["Math"][...]` root, both directly and through `Object.freeze`d references, printing 3 four times. This case is 2 of the 8 invocations the single #[test] fn `run_and_test_supports_fully_bracketed_global_this_math_log2_log10_identities_when_browser_harness_is_configured_in_js_and_ts_input` makes by looping over four (command, source_name, source) tuples and then over `for output_json in [false, true]`; per the split-don't-fold rule each independent program becomes its own named sibling [[case]], and the two extensions the loop covers are the file's `ext` matrix axis -- js and ts only, since this file has no jsx/tsx tests in either group. Text mode: a clean exit and `stdout.contains("3\\n")`, mirrored as `stdout_contains`. NOTE that the harness helper makes no count claim -- the `.matches("3\\n").count() >= 2` assertion lives only on the bundle-harness path, so no `stdout_count` is written here (inventing one would be a rule 2 violation)."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["3\n"]

[[case]]
name = "json_run_supports_fully_bracketed_global_this_math_log2_log10_identities_when_browser_harness_is_configured"
rationale = """Migrated from browser_math_log2_log10_fully_bracketed_root.rs. `assert_browser_harness_fully_bracketed_global_this_math_log2_log10` runs `kali run --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program that computes log2(8) and log10(1000) through a fully bracketed `globalThis["Math"][...]` root, both directly and through `Object.freeze`d references, printing 3 four times. This case is 2 of the 8 invocations the single #[test] fn `run_and_test_supports_fully_bracketed_global_this_math_log2_log10_identities_when_browser_harness_is_configured_in_js_and_ts_input` makes by looping over four (command, source_name, source) tuples and then over `for output_json in [false, true]`; per the split-don't-fold rule each independent program becomes its own named sibling [[case]], and the two extensions the loop covers are the file's `ext` matrix axis -- js and ts only, since this file has no jsx/tsx tests in either group. JSON mode (`--output json`): the envelope's schemaVersion/command/success and payload hostContract/runtimeBackend, plus `exitCode` and `payload.exitCode` (the source's `run` branch), `stderr` exactly empty, and an exact `json.stdout` pin. The source claims `json["stdout"].as_str().contains("3\\n")` -- a plain `.contains`, NOT a count, so no `json_count` is written; a json leaf has no substring form in this format, so ruling 3 resolves the claim to an exact pin, live-captured from the real `kali` binary and checked to contain the source's literal before being written. The source asserts `errors` empty only on the build envelope, never on this one, so no `errors` claim is written here."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "run", success = true, exitCode = 0, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", exitCode = 0 }, stdout = "3\n3\n3\n3\n", stderr = "" }

[[case]]
name = "json_test_supports_fully_bracketed_global_this_math_log2_log10_identities_when_browser_harness_is_configured"
rationale = """Migrated from browser_math_log2_log10_fully_bracketed_root.rs. `assert_browser_harness_fully_bracketed_global_this_math_log2_log10` runs `kali test --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program that computes log2(8) and log10(1000) through a fully bracketed `globalThis["Math"][...]` root, both directly and through `Object.freeze`d references, printing 3 four times. This case is 2 of the 8 invocations the single #[test] fn `run_and_test_supports_fully_bracketed_global_this_math_log2_log10_identities_when_browser_harness_is_configured_in_js_and_ts_input` makes by looping over four (command, source_name, source) tuples and then over `for output_json in [false, true]`; per the split-don't-fold rule each independent program becomes its own named sibling [[case]], and the two extensions the loop covers are the file's `ext` matrix axis -- js and ts only, since this file has no jsx/tsx tests in either group. JSON mode (`--output json`): the envelope's schemaVersion/command/success and payload hostContract/runtimeBackend, plus `payload.total`/`passed`/`failed` (the source's `test` branch), `stderr` exactly empty, and an exact `json.stdout` pin. The source claims `json["stdout"].as_str().contains("3\\n")` -- a plain `.contains`, NOT a count, so no `json_count` is written; a json leaf has no substring form in this format, so ruling 3 resolves the claim to an exact pin, live-captured from the real `kali` binary and checked to contain the source's literal before being written. The source asserts `errors` empty only on the build envelope, never on this one, so no `errors` claim is written here."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "test", success = true, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", total = 1, passed = 1, failed = 0 }, stdout = "3\n3\n3\n3\n", stderr = "" }
