# EXTRA-CLAIM DECLARATIONS (U14's `extra` direction).
# check_extra_claims.py compares this file's claim strings against the source's and fails
# on any that appear nowhere in the .rs. The entries below are the deliberate exceptions;
# a genuinely new one will not be on this list and will fail the gate.
# EXTRA-OK: 'browser object hasOwn fromEntries ok\n' -- live-captured exact `json.stdout` pin; source asserts `.contains` on a JSON leaf, which has no substring form, so ruling 3 requires an exact pin captured from the real binary
# EXTRA-OK: 'app_frozen.js' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'app_frozen.ts' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'app_frozen.jsx' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'app_frozen.tsx' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'main_frozen.js' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'main_frozen.ts' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'main_frozen.jsx' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'main_frozen.tsx' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'smoke_frozen.test.js' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'smoke_frozen.test.ts' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'smoke_frozen.test.jsx' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'smoke_frozen.test.tsx' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'app_frozen/app_frozen.meta.json' -- the U5-renamed bundle output path; `kali build --bundle` names its output directory after the input STEM, so the renamed entry moves the emitted meta file with it (U5's own requirement that the `file_json` path tracks the rename)
# EXTRA-OK: 'app_frozen' -- the U5-renamed bundle directory, passed as the `browser_bundle_harness` step's `entry` -- same reason as the meta path above
# Migrated from tests/browser_object_has_own_from_entries.rs.
#   SOURCE REF: 28df9ba02962143a1fc1735e418b2f67caee8fc6
#
# RULE 12 (carry every source comment verbatim): `grep -nE '^\s*//'` over
# tests/browser_object_has_own_from_entries.rs returns NOTHING -- the file has no Rust comment lines at
# all. The generator re-derives that on every run and RAISES rather than emitting this
# block if the source ever grows one, so the `--allow-empty` discharge below cannot
# become a vacuous green by drift.
# The file's only other `//` sequences are kali-tree-shake markers inside its
# JS fixture bodies -- program text carried verbatim into [source], not Rust
# prose. The generator checks that every one of them is such a marker:
#   * // kali-tree-shake: browserObjectHasOwnFromEntries
# There is therefore no prose to move into any `rationale`, and
# comment_coverage.py is run with --allow-empty for this pair.
#
# RULE 7 / U1 -- MATRIX DECLINED, and why.
# 33 `#[test]` fns, 53 real helper invocations with every loop
# expanded.
# `ext` is NOT a uniform axis in this source, and the arithmetic proves it. The
# PLAIN cells do form a complete cross (4 extensions x 2 output modes for the
# bundle helper, and likewise for run and for test), but the FROZEN cells do
# not: `app.js` / `main.js` / `smoke.test.js` are exercised in TEXT MODE ONLY,
# while `ts`, `jsx` and `tsx` are exercised in both. Declaring `ext` an axis
# would fan a `--output json` frozen trial for `js` that the source never ran,
# which manufactures an untested combination and is a rule-2 invention as well
# as a rule-7 arithmetic failure.
# The check helper's own cells DO form a complete cross, but U1 is explicit
# that `[matrix]` is FILE-WIDE with no per-case opt-out, so one non-uniform
# helper drops the axis for the whole file.
# Per U1 a `[matrix]` axis is FILE-WIDE -- `expand()` fans EVERY `[[case]]` by the full
# cross-product whether or not that case references the axis, and there is no per-case
# opt-out -- so an axis that any one case does not vary over would manufacture trials
# the source never ran (also a rule-2 invention). Rule 7's own remedy applies: drop
# `[matrix]` for the whole file and write named siblings.
# 53 invocations -> 53 named `[[case]]` entries, no `[matrix]`.
#
# RULE 6 -- 1:1 MAPPING. With no `[matrix]` there is no fold: every `[[case]]` below is
# exactly one real helper invocation of the source, named after the `#[test]` fn that made
# it. Two source fns are never folded into one case even when their invocations are
# literally identical, because the case is the only remaining trace of the fn.
# Four of the 33 source fns are LOOPS (`for filename in [...]`), and each of the four
# loops over three filenames AND both output modes, making six independent invocations
# against six independent programs. Rule 5 splits every one of them into named siblings
# -- suffixed with the extension and output mode they ran, not numbered -- which is why
# 33 fns become 53 cases: 29 unlooped invocations + 4 x 6 looped ones = 53 invocations,
# 53 cases. The generator asserts the loop count against the source before emitting and
# writes no line number for the loops: a figure that is neither gated nor pinned to an
# immutable ref is deleted (ruling 15's third answer), and the count is checked here
# rather than quoted there.
#
# U2 -- `[source]` is FILE-WIDE, and that is safe here. Every fixture
# (`app.js`, `app.jsx`, `app.ts`, `app.tsx`, `app_frozen.js`, `app_frozen.jsx`, `app_frozen.ts`, `app_frozen.tsx`, `main.js`, `main.jsx`, `main.ts`, `main.tsx`, `main_frozen.js`, `main_frozen.jsx`, `main_frozen.ts`, `main_frozen.tsx`, `smoke.test.js`, `smoke.test.jsx`, `smoke.test.ts`, `smoke.test.tsx`, `smoke_frozen.test.js`, `smoke_frozen.test.jsx`, `smoke_frozen.test.ts`, `smoke_frozen.test.tsx`)
# is written unconditionally by the source into a fresh temp dir: no fixture is written
# behind an `if`, and no case's point is the presence or absence of a file.
# Every command below names its entry explicitly on argv, so the unused siblings in
# a trial dir are inert.
# The `kali check` and `kali test` steps below were checked against the real binary
# with all 24 fixtures present in one directory: `payload.filesChecked` is still 1 and
# `payload.total` is still 1, so neither command discovers its siblings and the
# file-wide `[source]` table does not change what any case measures.
#
# U5 -- `[source]` KEY RENAMES. `[source]` is one flat file-wide namespace, and this
# source writes six different program texts across three filename families in different tests, so
# the keys are variant-suffixed:
#   * `app.<ext>` -> `app_frozen.<ext>` -- the FROZEN bundle/check program, which the plain build already claims `app.<ext>`
#   * `main.<ext>` -> `main_frozen.<ext>` -- the FROZEN harness run program
#   * `smoke.test.<ext>` -> `smoke_frozen.test.<ext>` -- the FROZEN harness test program; the `.test.` infix is kept so the entry still reads as a test file
# U5's safety condition holds for every rename: each of these filenames is passed to
# `kali` as a CLI argument only. None is referenced by string from inside any fixture
# body -- no dynamic-import call and no CommonJS require call names any of them, checked
# against every `[source]` value in this file -- so renaming does not rewrite the program
# under test (rule 9).
# Because `kali build --bundle` names its output directory after the input STEM, the
# renamed frozen entry moves the emitted bundle: those cases read
# `app_frozen/app_frozen.meta.json` and pass `entry = "app_frozen"` to the harness
# step, per U5's own requirement that the `file_json` path and the harness `entry`
# track the rename rather than staying hardcoded to `app`.
#
# RULING 7 -- DUPLICATE `[source]` BODIES ARE NOT HOISTED. Several entries below hold
# byte-identical program text. U13 would hoist them into `[constants]`; controller ruling 7
# declines that for `browser/` (a hoisted body makes `check_fixtures.py` go red on a correct
# file, and hoisting moves program text onto the surface `assertion_strings()` searches).
# The mandatory half of that ruling is honoured instead: the duplication is asserted
# MECHANICALLY in this file's generator, not eyeballed -- the generator compares the
# extracted fixture strings and raises if they are not byte-identical.
#
# RULE 8 / RULE 9 -- THE THREE FROZEN FIXTURES ARE CAPTURED, NEVER HAND-DERIVED. Each
# is built off its plain counterpart by a `source.replace(` call. The three
# REPLACEMENT literals -- not the needles --
# are `const object = Object.freeze(Object.fromEntries(` (:49), `const object = Object.freeze(Object.fromEntries(` (:56), `const object = Object.freeze(Object.fromEntries(` (:63);
# the third carries two leading spaces because the test fixture indents that line, and
# each needle beside them carries the same indentation as its replacement. The
# generator additionally asserts, mechanically, that each frozen capture is EXACTLY its
# plain capture with the `Object.freeze(` wrap applied -- so a capture taken before a
# source edit fails the generator rather than shipping a program that is no longer the
# program under test. The plain texts were captured through the same run rather than
# read out of the `.rs`, so that comparison has two measured sides.
#
# RULE 10 -- NOT NEEDED HERE, and that is checked rather than assumed. `expand.rs`'s
# `substitute()` hard-fails on any `${...}` it cannot resolve, so a fixture carrying a
# genuine JS template literal must declare `[constants] dollar = "$"` and spell the
# genuine `${` as `${dollar}{`. No `[source]` value and no step `body` in this file
# contains `${` at all -- the generator greps every emitted string and raises if one does
# -- so this file declares no `[constants]` table.
#
# RULE 13 -- transitive helper docs. Checked every fn in each call chain:
#   `kali_bin`, `browser_bundle_object_has_own_from_entries_source`,
#   `browser_bundle_object_has_own_frozen_from_entries_source`,
#   `browser_harness_object_has_own_from_entries_run_source`,
#   `browser_harness_object_has_own_frozen_from_entries_run_source`,
#   `browser_harness_object_has_own_from_entries_test_source`,
#   `browser_harness_object_has_own_frozen_from_entries_test_source`,
#   `assert_browser_bundle_object_has_own_from_entries_with_source`,
#   `assert_browser_check_object_has_own_from_entries_with_source`,
#   `assert_browser_harness_object_has_own_from_entries` -- none carries a `///` doc comment.
# The chain also reaches `kali_runtime_contract::browser_bundle_harness_script` and
# `::browser_harness_command_parts_for`, which DO carry one-line `///` docs. Those are
# NOT carried: in the migrated form this case file never calls them -- the
# `browser_bundle_harness` step kind means the case RUNNER does
# (crates/kali_case_runner/src/steps.rs) -- so their docs describe shared runner
# infrastructure documented in design spec 5.3, not what this case claims. That is
# controller ruling 6, which writes the exemption into the rule rather than leaving it
# to implementer discretion.
#
# ARGV ORDER is transcribed in the exact order each source helper's `Command` builder
# appends it, which differs between the three helper shapes and is not normalised here:
#   * check:   `check --api browser [--output json] <entry>` -- the `--output json` pair
#              is appended AFTER the subcommand and its flags.
#   * build:   `build --bundle --api browser [--output json] <entry>` -- likewise AFTER.
#   * run/test: `[--output json] <run|test> --api browser --max-threads 0
#              --max-spawned-processes 0 <entry>` -- the `--output json` pair is appended
#              BEFORE the subcommand.
# The source passes an absolute `dir.path().join(filename)` as the entry; the case runner
# passes the bare filename relative to the trial dir, matching every previously shipped
# `browser/` case file.
# The harness helper's env value is `KALI_BROWSER_BUNDLE_HARNESS_COMMAND`, read from the
# `kali_runtime_contract::BROWSER_HARNESS_COMMAND_ENV` constant the source passes by
# name rather than assumed. The check and build helpers set no env.
#
# THE `browser_bundle_harness` STEP is the migrated form of the source's own harness run.
# The source writes `kali_runtime_contract::browser_bundle_harness_script("app", false,
# <body>)` next to the emitted bundle directory and executes it under
# `browser_harness_command_parts_for`; the case runner does exactly that from the step's
# `entry` and `body` (crates/kali_case_runner/src/steps.rs). The prelude resolves the
# bundle relative to the harness script's own URL rather than to the process working
# directory, so the runner's cwd (the trial root) and the source's cwd (the bundle dir)
# produce the same resolution -- which is why every previously shipped `browser/` bundle
# case file spells it this way.
#
# ASSERTION SHAPE, mirrored from the source and nothing more. THREE HELPERS, THREE
# SHAPES:
#   * `assert_browser_check_object_has_own_from_entries_with_source`: `exit = "success"`; in
#     json mode schemaVersion/command/success/exitCode, `payload.filesChecked = 1`
#     `json["payload"]["filesChecked"]` (:198) and an empty `errors` array. It asserts NOTHING about
#     stdout in either mode, so neither does this file.
#   * `assert_browser_bundle_object_has_own_from_entries_with_source`:
#     `exit = "success"` on the build `output.status.success(),` (:97); in json mode the envelope and
#     an empty `errors` array `assert!(envelope["errors"]` (:112); `app*/app*.meta.json`'s apiSurface/
#     artifactKind in BOTH modes `assert_eq!(metadata["apiSurface"], "browser")` (:123); then the bundle harness, `exit =
#     "success"` with `stdout_contains` `stdout.contains("browser object hasOwn fromEntries ok")` (:160).
#   * `assert_browser_harness_object_has_own_from_entries`: `exit = "success"`; in text
#     mode `stdout_contains` `stdout.contains("browser object hasOwn fromEntries ok")` (:327) plus `"ok 1"` for the `test`
#     command only `stdout.contains("ok 1")` (:339); in json mode the envelope, an exact `json.stdout` pin
#     `let stdout = json["stdout"].as_str()` (:325), `json.stderr = ""` `assert_eq!(json["stderr"], "")` (:330) and an empty `errors`
#     array.
# The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed.
# On the JSON branch the same claim is taken against the string leaf json["stdout"], which has NO substring form in the case format (there is no json_contains key), so per controller ruling 3 it becomes an exact `json.stdout` pin -- and, per U9, only after the value was captured from the real kali binary rather than hand-computed.
# The live-captured value is 'browser object hasOwn fromEntries ok\n'. The generator
# re-captures it from the real binary and requires every capture to be byte-identical to
# every other AND to the embedded constant before a single pin is emitted, so the pin
# cannot drift from what the binary does without failing the generator -- CONDITIONAL ON
# THE BINARY BEING BUILT. `_pin` prints a loud skip and falls back to the embedded
# constant when the built `kali` is absent, so a generator run without it re-derives
# nothing here; the guarantee is only as strong as that precondition, and the sentence
# says so rather than overstating what the code does. NO CELL COUNT IS WRITTEN HERE: it
# would be a figure nothing re-resolves, and the capture set is a property of the
# generator rather than of this file's cases -- the cases that carry this pin are
# enumerable from the file itself.

[source]
"app.js" = """// kali-tree-shake: browserObjectHasOwnFromEntries
function browserObjectHasOwnFromEntries() {
  const object = Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
}
"""
"app_frozen.js" = """// kali-tree-shake: browserObjectHasOwnFromEntries
function browserObjectHasOwnFromEntries() {
  const object = Object.freeze(Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
}
"""
"main.js" = """function browserObjectHasOwnFromEntries() {
  const object = Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
}
browserObjectHasOwnFromEntries();
"""
"main_frozen.js" = """function browserObjectHasOwnFromEntries() {
  const object = Object.freeze(Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
}
browserObjectHasOwnFromEntries();
"""
"smoke.test.js" = """Kali.test('object hasOwn fromEntries', () => {
  const object = Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
});
"""
"smoke_frozen.test.js" = """Kali.test('object hasOwn fromEntries', () => {
  const object = Object.freeze(Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
});
"""
"app.ts" = """// kali-tree-shake: browserObjectHasOwnFromEntries
function browserObjectHasOwnFromEntries() {
  const object = Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
}
"""
"app_frozen.ts" = """// kali-tree-shake: browserObjectHasOwnFromEntries
function browserObjectHasOwnFromEntries() {
  const object = Object.freeze(Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
}
"""
"main.ts" = """function browserObjectHasOwnFromEntries() {
  const object = Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
}
browserObjectHasOwnFromEntries();
"""
"main_frozen.ts" = """function browserObjectHasOwnFromEntries() {
  const object = Object.freeze(Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
}
browserObjectHasOwnFromEntries();
"""
"smoke.test.ts" = """Kali.test('object hasOwn fromEntries', () => {
  const object = Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
});
"""
"smoke_frozen.test.ts" = """Kali.test('object hasOwn fromEntries', () => {
  const object = Object.freeze(Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
});
"""
"app.jsx" = """// kali-tree-shake: browserObjectHasOwnFromEntries
function browserObjectHasOwnFromEntries() {
  const object = Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
}
"""
"app_frozen.jsx" = """// kali-tree-shake: browserObjectHasOwnFromEntries
function browserObjectHasOwnFromEntries() {
  const object = Object.freeze(Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
}
"""
"main.jsx" = """function browserObjectHasOwnFromEntries() {
  const object = Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
}
browserObjectHasOwnFromEntries();
"""
"main_frozen.jsx" = """function browserObjectHasOwnFromEntries() {
  const object = Object.freeze(Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
}
browserObjectHasOwnFromEntries();
"""
"smoke.test.jsx" = """Kali.test('object hasOwn fromEntries', () => {
  const object = Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
});
"""
"smoke_frozen.test.jsx" = """Kali.test('object hasOwn fromEntries', () => {
  const object = Object.freeze(Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
});
"""
"app.tsx" = """// kali-tree-shake: browserObjectHasOwnFromEntries
function browserObjectHasOwnFromEntries() {
  const object = Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
}
"""
"app_frozen.tsx" = """// kali-tree-shake: browserObjectHasOwnFromEntries
function browserObjectHasOwnFromEntries() {
  const object = Object.freeze(Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
}
"""
"main.tsx" = """function browserObjectHasOwnFromEntries() {
  const object = Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
}
browserObjectHasOwnFromEntries();
"""
"main_frozen.tsx" = """function browserObjectHasOwnFromEntries() {
  const object = Object.freeze(Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
}
browserObjectHasOwnFromEntries();
"""
"smoke.test.tsx" = """Kali.test('object hasOwn fromEntries', () => {
  const object = Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
});
"""
"smoke_frozen.test.tsx" = """Kali.test('object hasOwn fromEntries', () => {
  const object = Object.freeze(Object.fromEntries([["a", 1], ["b", 2]]);
  const alias = object;
  const wrapped = (0, alias);
  const awaited = wrapped;
  const conditionalHasOwn = Object.freeze((true ? Object.hasOwn : Object.hasOwn));
  const conditionalHasOwnPropertyCall = Object.freeze((true ? Object.prototype.hasOwnProperty.call : Object.prototype.hasOwnProperty.call));
  if (
    !Object.hasOwn(wrapped, "a") ||
    !Object["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"]["hasOwn"](wrapped, "a") ||
    !globalThis.Object["hasOwn"](wrapped, "a") ||
    !globalThis["Object"].hasOwn(wrapped, "a") ||
    !Object.hasOwn(awaited, "a") ||
    !conditionalHasOwn(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(wrapped, "a") ||
    !Object.prototype.hasOwnProperty.call(awaited, "a") ||
    !conditionalHasOwnPropertyCall(wrapped, "a") ||
    !globalThis.Object.prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis.Object["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"].prototype.hasOwnProperty.call(wrapped, "a") ||
    !globalThis.Object["prototype"].hasOwnProperty.call(wrapped, "a") ||
    !globalThis["Object"]["prototype"]["hasOwnProperty"]["call"](wrapped, "a") ||
    !globalThis["Object"]["prototype"].hasOwnProperty["call"](wrapped, "a")
  ) {
    throw new Error('unexpected browser Object.hasOwn result');
  }
  console.log('browser object hasOwn fromEntries ok');
});
"""

[[case]]
name = "check_emits_frozen_object_has_own_from_entries_in_js_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `check_emits_frozen_object_has_own_from_entries_in_js_input`. `assert_browser_check_object_has_own_from_entries_with_source` writes the FROZEN Object.hasOwn-from-entries program to app_frozen.js and runs `kali check --api browser`. The program freezes an Object.fromEntries result and checks membership through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings. The check succeeds. This helper asserts the process succeeds and, in json mode, schemaVersion/command/success/exitCode, `payload.filesChecked = 1` `json["payload"]["filesChecked"]` (:198) and an empty `errors` array. It makes no stdout claim in either mode, so neither does this case -- adding one would be a rule-2 invention. The entry is U5-renamed from `app.<ext>` because the plain bundle program already claims that key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator."""
args = ["check", "--api", "browser", "app_frozen.js"]
exit = "success"

[[case]]
name = "json_check_emits_frozen_object_has_own_from_entries_in_js_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `json_check_emits_frozen_object_has_own_from_entries_in_js_input`. `assert_browser_check_object_has_own_from_entries_with_source` writes the FROZEN Object.hasOwn-from-entries program to app_frozen.js and runs `kali check --api browser` with `--output json`. The program freezes an Object.fromEntries result and checks membership through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings. The check succeeds. This helper asserts the process succeeds and, in json mode, schemaVersion/command/success/exitCode, `payload.filesChecked = 1` `json["payload"]["filesChecked"]` (:198) and an empty `errors` array. It makes no stdout claim in either mode, so neither does this case -- adding one would be a rule-2 invention. The entry is U5-renamed from `app.<ext>` because the plain bundle program already claims that key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator."""
args = ["check", "--api", "browser", "--output", "json", "app_frozen.js"]
exit = "success"
json = { schemaVersion = 1, command = "check", success = true, exitCode = 0, payload = { filesChecked = 1 }, errors = [] }

[[case]]
name = "check_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input_ts_text"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `check_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input`, the ts / text cell of its three-filename x two-output-mode loop (rule 5: six independent invocations become six named siblings). `assert_browser_check_object_has_own_from_entries_with_source` writes the FROZEN Object.hasOwn-from-entries program to app_frozen.ts and runs `kali check --api browser`. The program freezes an Object.fromEntries result and checks membership through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings. The check succeeds. This helper asserts the process succeeds and, in json mode, schemaVersion/command/success/exitCode, `payload.filesChecked = 1` `json["payload"]["filesChecked"]` (:198) and an empty `errors` array. It makes no stdout claim in either mode, so neither does this case -- adding one would be a rule-2 invention. The entry is U5-renamed from `app.<ext>` because the plain bundle program already claims that key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator."""
args = ["check", "--api", "browser", "app_frozen.ts"]
exit = "success"

[[case]]
name = "check_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input_ts_json"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `check_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input`, the ts / json cell of its three-filename x two-output-mode loop (rule 5: six independent invocations become six named siblings). `assert_browser_check_object_has_own_from_entries_with_source` writes the FROZEN Object.hasOwn-from-entries program to app_frozen.ts and runs `kali check --api browser` with `--output json`. The program freezes an Object.fromEntries result and checks membership through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings. The check succeeds. This helper asserts the process succeeds and, in json mode, schemaVersion/command/success/exitCode, `payload.filesChecked = 1` `json["payload"]["filesChecked"]` (:198) and an empty `errors` array. It makes no stdout claim in either mode, so neither does this case -- adding one would be a rule-2 invention. The entry is U5-renamed from `app.<ext>` because the plain bundle program already claims that key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator."""
args = ["check", "--api", "browser", "--output", "json", "app_frozen.ts"]
exit = "success"
json = { schemaVersion = 1, command = "check", success = true, exitCode = 0, payload = { filesChecked = 1 }, errors = [] }

[[case]]
name = "check_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input_jsx_text"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `check_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input`, the jsx / text cell of its three-filename x two-output-mode loop (rule 5: six independent invocations become six named siblings). `assert_browser_check_object_has_own_from_entries_with_source` writes the FROZEN Object.hasOwn-from-entries program to app_frozen.jsx and runs `kali check --api browser`. The program freezes an Object.fromEntries result and checks membership through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings. The check succeeds. This helper asserts the process succeeds and, in json mode, schemaVersion/command/success/exitCode, `payload.filesChecked = 1` `json["payload"]["filesChecked"]` (:198) and an empty `errors` array. It makes no stdout claim in either mode, so neither does this case -- adding one would be a rule-2 invention. The entry is U5-renamed from `app.<ext>` because the plain bundle program already claims that key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator."""
args = ["check", "--api", "browser", "app_frozen.jsx"]
exit = "success"

[[case]]
name = "check_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input_jsx_json"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `check_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input`, the jsx / json cell of its three-filename x two-output-mode loop (rule 5: six independent invocations become six named siblings). `assert_browser_check_object_has_own_from_entries_with_source` writes the FROZEN Object.hasOwn-from-entries program to app_frozen.jsx and runs `kali check --api browser` with `--output json`. The program freezes an Object.fromEntries result and checks membership through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings. The check succeeds. This helper asserts the process succeeds and, in json mode, schemaVersion/command/success/exitCode, `payload.filesChecked = 1` `json["payload"]["filesChecked"]` (:198) and an empty `errors` array. It makes no stdout claim in either mode, so neither does this case -- adding one would be a rule-2 invention. The entry is U5-renamed from `app.<ext>` because the plain bundle program already claims that key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator."""
args = ["check", "--api", "browser", "--output", "json", "app_frozen.jsx"]
exit = "success"
json = { schemaVersion = 1, command = "check", success = true, exitCode = 0, payload = { filesChecked = 1 }, errors = [] }

[[case]]
name = "check_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input_tsx_text"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `check_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input`, the tsx / text cell of its three-filename x two-output-mode loop (rule 5: six independent invocations become six named siblings). `assert_browser_check_object_has_own_from_entries_with_source` writes the FROZEN Object.hasOwn-from-entries program to app_frozen.tsx and runs `kali check --api browser`. The program freezes an Object.fromEntries result and checks membership through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings. The check succeeds. This helper asserts the process succeeds and, in json mode, schemaVersion/command/success/exitCode, `payload.filesChecked = 1` `json["payload"]["filesChecked"]` (:198) and an empty `errors` array. It makes no stdout claim in either mode, so neither does this case -- adding one would be a rule-2 invention. The entry is U5-renamed from `app.<ext>` because the plain bundle program already claims that key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator."""
args = ["check", "--api", "browser", "app_frozen.tsx"]
exit = "success"

[[case]]
name = "check_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input_tsx_json"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `check_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input`, the tsx / json cell of its three-filename x two-output-mode loop (rule 5: six independent invocations become six named siblings). `assert_browser_check_object_has_own_from_entries_with_source` writes the FROZEN Object.hasOwn-from-entries program to app_frozen.tsx and runs `kali check --api browser` with `--output json`. The program freezes an Object.fromEntries result and checks membership through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings. The check succeeds. This helper asserts the process succeeds and, in json mode, schemaVersion/command/success/exitCode, `payload.filesChecked = 1` `json["payload"]["filesChecked"]` (:198) and an empty `errors` array. It makes no stdout claim in either mode, so neither does this case -- adding one would be a rule-2 invention. The entry is U5-renamed from `app.<ext>` because the plain bundle program already claims that key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator."""
args = ["check", "--api", "browser", "--output", "json", "app_frozen.tsx"]
exit = "success"
json = { schemaVersion = 1, command = "check", success = true, exitCode = 0, payload = { filesChecked = 1 }, errors = [] }

[[case]]
name = "build_emits_object_has_own_from_entries_in_js_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `build_emits_object_has_own_from_entries_in_js_input`. `assert_browser_bundle_object_has_own_from_entries_with_source` writes the plain Object.hasOwn-from-entries program to app.js, builds it with `kali build --bundle --api browser`, asserts the emitted app/app.meta.json metadata `assert_eq!(metadata["apiSurface"], "browser")` (:123), then writes the browser-bundle harness and runs it under node. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings, including conditional and frozen callables, then prints its ok line. Both processes succeed `output.status.success(),` (:97), and the harness's stdout claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:160). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "app.js"]
exit = "success"

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.browserObjectHasOwnFromEntries();
"""
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "build_emits_object_has_own_from_entries_in_jsx_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `build_emits_object_has_own_from_entries_in_jsx_input`. `assert_browser_bundle_object_has_own_from_entries_with_source` writes the plain Object.hasOwn-from-entries program to app.jsx, builds it with `kali build --bundle --api browser`, asserts the emitted app/app.meta.json metadata `assert_eq!(metadata["apiSurface"], "browser")` (:123), then writes the browser-bundle harness and runs it under node. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings, including conditional and frozen callables, then prints its ok line. Both processes succeed `output.status.success(),` (:97), and the harness's stdout claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:160). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "app.jsx"]
exit = "success"

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.browserObjectHasOwnFromEntries();
"""
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "build_emits_object_has_own_from_entries_in_ts_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `build_emits_object_has_own_from_entries_in_ts_input`. `assert_browser_bundle_object_has_own_from_entries_with_source` writes the plain Object.hasOwn-from-entries program to app.ts, builds it with `kali build --bundle --api browser`, asserts the emitted app/app.meta.json metadata `assert_eq!(metadata["apiSurface"], "browser")` (:123), then writes the browser-bundle harness and runs it under node. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings, including conditional and frozen callables, then prints its ok line. Both processes succeed `output.status.success(),` (:97), and the harness's stdout claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:160). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "app.ts"]
exit = "success"

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.browserObjectHasOwnFromEntries();
"""
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "build_emits_object_has_own_from_entries_in_tsx_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `build_emits_object_has_own_from_entries_in_tsx_input`. `assert_browser_bundle_object_has_own_from_entries_with_source` writes the plain Object.hasOwn-from-entries program to app.tsx, builds it with `kali build --bundle --api browser`, asserts the emitted app/app.meta.json metadata `assert_eq!(metadata["apiSurface"], "browser")` (:123), then writes the browser-bundle harness and runs it under node. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings, including conditional and frozen callables, then prints its ok line. Both processes succeed `output.status.success(),` (:97), and the harness's stdout claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:160). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "app.tsx"]
exit = "success"

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.browserObjectHasOwnFromEntries();
"""
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "json_build_emits_object_has_own_from_entries_in_js_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `json_build_emits_object_has_own_from_entries_in_js_input`. `assert_browser_bundle_object_has_own_from_entries_with_source` writes the plain Object.hasOwn-from-entries program to app.js, builds it with `kali build --bundle --api browser` with `--output json`, asserts the emitted app/app.meta.json metadata `assert_eq!(metadata["apiSurface"], "browser")` (:123), then writes the browser-bundle harness and runs it under node. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings, including conditional and frozen callables, then prints its ok line. Both processes succeed `output.status.success(),` (:97), and the harness's stdout claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:160). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. This sibling additionally asserts the build JSON envelope -- schemaVersion/command/success/exitCode, payload artifactKind/bundleFormat, and the empty `errors` array `assert!(envelope["errors"]` (:112) -- rather than plain text; output shape is not a matrix axis because it changes the assertion shape, so it is a separate case. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "--output", "json", "app.js"]
exit = "success"
json = { schemaVersion = 1, command = "build", success = true, exitCode = 0, payload = { artifactKind = "bundle", bundleFormat = "esm" }, errors = [] }

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.browserObjectHasOwnFromEntries();
"""
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "json_build_emits_object_has_own_from_entries_in_jsx_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `json_build_emits_object_has_own_from_entries_in_jsx_input`. `assert_browser_bundle_object_has_own_from_entries_with_source` writes the plain Object.hasOwn-from-entries program to app.jsx, builds it with `kali build --bundle --api browser` with `--output json`, asserts the emitted app/app.meta.json metadata `assert_eq!(metadata["apiSurface"], "browser")` (:123), then writes the browser-bundle harness and runs it under node. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings, including conditional and frozen callables, then prints its ok line. Both processes succeed `output.status.success(),` (:97), and the harness's stdout claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:160). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. This sibling additionally asserts the build JSON envelope -- schemaVersion/command/success/exitCode, payload artifactKind/bundleFormat, and the empty `errors` array `assert!(envelope["errors"]` (:112) -- rather than plain text; output shape is not a matrix axis because it changes the assertion shape, so it is a separate case. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "--output", "json", "app.jsx"]
exit = "success"
json = { schemaVersion = 1, command = "build", success = true, exitCode = 0, payload = { artifactKind = "bundle", bundleFormat = "esm" }, errors = [] }

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.browserObjectHasOwnFromEntries();
"""
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "json_build_emits_object_has_own_from_entries_in_ts_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `json_build_emits_object_has_own_from_entries_in_ts_input`. `assert_browser_bundle_object_has_own_from_entries_with_source` writes the plain Object.hasOwn-from-entries program to app.ts, builds it with `kali build --bundle --api browser` with `--output json`, asserts the emitted app/app.meta.json metadata `assert_eq!(metadata["apiSurface"], "browser")` (:123), then writes the browser-bundle harness and runs it under node. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings, including conditional and frozen callables, then prints its ok line. Both processes succeed `output.status.success(),` (:97), and the harness's stdout claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:160). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. This sibling additionally asserts the build JSON envelope -- schemaVersion/command/success/exitCode, payload artifactKind/bundleFormat, and the empty `errors` array `assert!(envelope["errors"]` (:112) -- rather than plain text; output shape is not a matrix axis because it changes the assertion shape, so it is a separate case. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "--output", "json", "app.ts"]
exit = "success"
json = { schemaVersion = 1, command = "build", success = true, exitCode = 0, payload = { artifactKind = "bundle", bundleFormat = "esm" }, errors = [] }

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.browserObjectHasOwnFromEntries();
"""
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "json_build_emits_object_has_own_from_entries_in_tsx_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `json_build_emits_object_has_own_from_entries_in_tsx_input`. `assert_browser_bundle_object_has_own_from_entries_with_source` writes the plain Object.hasOwn-from-entries program to app.tsx, builds it with `kali build --bundle --api browser` with `--output json`, asserts the emitted app/app.meta.json metadata `assert_eq!(metadata["apiSurface"], "browser")` (:123), then writes the browser-bundle harness and runs it under node. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings, including conditional and frozen callables, then prints its ok line. Both processes succeed `output.status.success(),` (:97), and the harness's stdout claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:160). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. This sibling additionally asserts the build JSON envelope -- schemaVersion/command/success/exitCode, payload artifactKind/bundleFormat, and the empty `errors` array `assert!(envelope["errors"]` (:112) -- rather than plain text; output shape is not a matrix axis because it changes the assertion shape, so it is a separate case. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "--output", "json", "app.tsx"]
exit = "success"
json = { schemaVersion = 1, command = "build", success = true, exitCode = 0, payload = { artifactKind = "bundle", bundleFormat = "esm" }, errors = [] }

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.browserObjectHasOwnFromEntries();
"""
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "build_emits_frozen_object_has_own_from_entries_in_js_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `build_emits_frozen_object_has_own_from_entries_in_js_input`. `assert_browser_bundle_object_has_own_from_entries_with_source` writes the FROZEN Object.hasOwn-from-entries program to app_frozen.js, builds it with `kali build --bundle --api browser`, asserts the emitted app_frozen/app_frozen.meta.json metadata `assert_eq!(metadata["apiSurface"], "browser")` (:123), then writes the browser-bundle harness and runs it under node. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings, including conditional and frozen callables, then prints its ok line. Both processes succeed `output.status.success(),` (:97), and the harness's stdout claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:160). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The entry is U5-renamed from `app.<ext>` because the plain bundle program already claims that key; because `kali build --bundle` names its output directory after the input stem, the meta path and the harness `entry` track the rename rather than staying hardcoded to `app`. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "app_frozen.js"]
exit = "success"

[[case.step]]
kind = "file_json"
path = "app_frozen/app_frozen.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app_frozen"
body = """const mod = await import(bundleJs.href);
await mod.browserObjectHasOwnFromEntries();
"""
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "build_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input_ts_text"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `build_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input`, the ts / text cell of its three-filename x two-output-mode loop (rule 5). `assert_browser_bundle_object_has_own_from_entries_with_source` writes the FROZEN Object.hasOwn-from-entries program to app_frozen.ts, builds it with `kali build --bundle --api browser`, asserts the emitted app_frozen/app_frozen.meta.json metadata `assert_eq!(metadata["apiSurface"], "browser")` (:123), then writes the browser-bundle harness and runs it under node. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings, including conditional and frozen callables, then prints its ok line. Both processes succeed `output.status.success(),` (:97), and the harness's stdout claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:160). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The entry is U5-renamed from `app.<ext>` because the plain bundle program already claims that key; because `kali build --bundle` names its output directory after the input stem, the meta path and the harness `entry` track the rename rather than staying hardcoded to `app`. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "app_frozen.ts"]
exit = "success"

[[case.step]]
kind = "file_json"
path = "app_frozen/app_frozen.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app_frozen"
body = """const mod = await import(bundleJs.href);
await mod.browserObjectHasOwnFromEntries();
"""
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "build_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input_ts_json"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `build_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input`, the ts / json cell of its three-filename x two-output-mode loop (rule 5). `assert_browser_bundle_object_has_own_from_entries_with_source` writes the FROZEN Object.hasOwn-from-entries program to app_frozen.ts, builds it with `kali build --bundle --api browser` with `--output json`, asserts the emitted app_frozen/app_frozen.meta.json metadata `assert_eq!(metadata["apiSurface"], "browser")` (:123), then writes the browser-bundle harness and runs it under node. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings, including conditional and frozen callables, then prints its ok line. Both processes succeed `output.status.success(),` (:97), and the harness's stdout claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:160). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. This sibling additionally asserts the build JSON envelope -- schemaVersion/command/success/exitCode, payload artifactKind/bundleFormat, and the empty `errors` array `assert!(envelope["errors"]` (:112) -- rather than plain text; output shape is not a matrix axis because it changes the assertion shape, so it is a separate case. The entry is U5-renamed from `app.<ext>` because the plain bundle program already claims that key; because `kali build --bundle` names its output directory after the input stem, the meta path and the harness `entry` track the rename rather than staying hardcoded to `app`. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "--output", "json", "app_frozen.ts"]
exit = "success"
json = { schemaVersion = 1, command = "build", success = true, exitCode = 0, payload = { artifactKind = "bundle", bundleFormat = "esm" }, errors = [] }

[[case.step]]
kind = "file_json"
path = "app_frozen/app_frozen.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app_frozen"
body = """const mod = await import(bundleJs.href);
await mod.browserObjectHasOwnFromEntries();
"""
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "build_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input_jsx_text"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `build_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input`, the jsx / text cell of its three-filename x two-output-mode loop (rule 5). `assert_browser_bundle_object_has_own_from_entries_with_source` writes the FROZEN Object.hasOwn-from-entries program to app_frozen.jsx, builds it with `kali build --bundle --api browser`, asserts the emitted app_frozen/app_frozen.meta.json metadata `assert_eq!(metadata["apiSurface"], "browser")` (:123), then writes the browser-bundle harness and runs it under node. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings, including conditional and frozen callables, then prints its ok line. Both processes succeed `output.status.success(),` (:97), and the harness's stdout claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:160). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The entry is U5-renamed from `app.<ext>` because the plain bundle program already claims that key; because `kali build --bundle` names its output directory after the input stem, the meta path and the harness `entry` track the rename rather than staying hardcoded to `app`. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "app_frozen.jsx"]
exit = "success"

[[case.step]]
kind = "file_json"
path = "app_frozen/app_frozen.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app_frozen"
body = """const mod = await import(bundleJs.href);
await mod.browserObjectHasOwnFromEntries();
"""
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "build_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input_jsx_json"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `build_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input`, the jsx / json cell of its three-filename x two-output-mode loop (rule 5). `assert_browser_bundle_object_has_own_from_entries_with_source` writes the FROZEN Object.hasOwn-from-entries program to app_frozen.jsx, builds it with `kali build --bundle --api browser` with `--output json`, asserts the emitted app_frozen/app_frozen.meta.json metadata `assert_eq!(metadata["apiSurface"], "browser")` (:123), then writes the browser-bundle harness and runs it under node. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings, including conditional and frozen callables, then prints its ok line. Both processes succeed `output.status.success(),` (:97), and the harness's stdout claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:160). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. This sibling additionally asserts the build JSON envelope -- schemaVersion/command/success/exitCode, payload artifactKind/bundleFormat, and the empty `errors` array `assert!(envelope["errors"]` (:112) -- rather than plain text; output shape is not a matrix axis because it changes the assertion shape, so it is a separate case. The entry is U5-renamed from `app.<ext>` because the plain bundle program already claims that key; because `kali build --bundle` names its output directory after the input stem, the meta path and the harness `entry` track the rename rather than staying hardcoded to `app`. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "--output", "json", "app_frozen.jsx"]
exit = "success"
json = { schemaVersion = 1, command = "build", success = true, exitCode = 0, payload = { artifactKind = "bundle", bundleFormat = "esm" }, errors = [] }

[[case.step]]
kind = "file_json"
path = "app_frozen/app_frozen.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app_frozen"
body = """const mod = await import(bundleJs.href);
await mod.browserObjectHasOwnFromEntries();
"""
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "build_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input_tsx_text"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `build_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input`, the tsx / text cell of its three-filename x two-output-mode loop (rule 5). `assert_browser_bundle_object_has_own_from_entries_with_source` writes the FROZEN Object.hasOwn-from-entries program to app_frozen.tsx, builds it with `kali build --bundle --api browser`, asserts the emitted app_frozen/app_frozen.meta.json metadata `assert_eq!(metadata["apiSurface"], "browser")` (:123), then writes the browser-bundle harness and runs it under node. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings, including conditional and frozen callables, then prints its ok line. Both processes succeed `output.status.success(),` (:97), and the harness's stdout claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:160). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The entry is U5-renamed from `app.<ext>` because the plain bundle program already claims that key; because `kali build --bundle` names its output directory after the input stem, the meta path and the harness `entry` track the rename rather than staying hardcoded to `app`. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "app_frozen.tsx"]
exit = "success"

[[case.step]]
kind = "file_json"
path = "app_frozen/app_frozen.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app_frozen"
body = """const mod = await import(bundleJs.href);
await mod.browserObjectHasOwnFromEntries();
"""
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "build_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input_tsx_json"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `build_emits_frozen_object_has_own_from_entries_in_ts_jsx_tsx_input`, the tsx / json cell of its three-filename x two-output-mode loop (rule 5). `assert_browser_bundle_object_has_own_from_entries_with_source` writes the FROZEN Object.hasOwn-from-entries program to app_frozen.tsx, builds it with `kali build --bundle --api browser` with `--output json`, asserts the emitted app_frozen/app_frozen.meta.json metadata `assert_eq!(metadata["apiSurface"], "browser")` (:123), then writes the browser-bundle harness and runs it under node. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings, including conditional and frozen callables, then prints its ok line. Both processes succeed `output.status.success(),` (:97), and the harness's stdout claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:160). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. This sibling additionally asserts the build JSON envelope -- schemaVersion/command/success/exitCode, payload artifactKind/bundleFormat, and the empty `errors` array `assert!(envelope["errors"]` (:112) -- rather than plain text; output shape is not a matrix axis because it changes the assertion shape, so it is a separate case. The entry is U5-renamed from `app.<ext>` because the plain bundle program already claims that key; because `kali build --bundle` names its output directory after the input stem, the meta path and the harness `entry` track the rename rather than staying hardcoded to `app`. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "--output", "json", "app_frozen.tsx"]
exit = "success"
json = { schemaVersion = 1, command = "build", success = true, exitCode = 0, payload = { artifactKind = "bundle", bundleFormat = "esm" }, errors = [] }

[[case.step]]
kind = "file_json"
path = "app_frozen/app_frozen.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app_frozen"
body = """const mod = await import(bundleJs.href);
await mod.browserObjectHasOwnFromEntries();
"""
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "run_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_js_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `run_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_js_input`. `assert_browser_harness_object_has_own_from_entries` writes the plain Object.hasOwn-from-entries run program to main.js and asks `kali` to run it against the browser API surface with the browser harness backed by node. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. Its text-mode claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:327). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.js"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "run_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_jsx_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `run_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_jsx_input`. `assert_browser_harness_object_has_own_from_entries` writes the plain Object.hasOwn-from-entries run program to main.jsx and asks `kali` to run it against the browser API surface with the browser harness backed by node. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. Its text-mode claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:327). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.jsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "run_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `run_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_input`. `assert_browser_harness_object_has_own_from_entries` writes the plain Object.hasOwn-from-entries run program to main.ts and asks `kali` to run it against the browser API surface with the browser harness backed by node. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. Its text-mode claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:327). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.ts"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "run_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_tsx_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `run_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_tsx_input`. `assert_browser_harness_object_has_own_from_entries` writes the plain Object.hasOwn-from-entries run program to main.tsx and asks `kali` to run it against the browser API surface with the browser harness backed by node. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. Its text-mode claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:327). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.tsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "json_run_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_js_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `json_run_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_js_input`. `assert_browser_harness_object_has_own_from_entries` writes the plain Object.hasOwn-from-entries run program to main.js and asks `kali` to run it against the browser API surface with the browser harness backed by node, with `--output json`. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. In json mode the source reads json["stdout"] `let stdout = json["stdout"].as_str()` (:325) and asserts a substring match on it, plus `assert_eq!(json["stderr"], "")` (:330) and an empty `errors` array, and for `run` the envelope and payload exitCode / for `test` the payload total, passed and failed. On the JSON branch the same claim is taken against the string leaf json["stdout"], which has NO substring form in the case format (there is no json_contains key), so per controller ruling 3 it becomes an exact `json.stdout` pin -- and, per U9, only after the value was captured from the real kali binary rather than hand-computed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.js"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "run", success = true, exitCode = 0, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", exitCode = 0 }, stdout = "browser object hasOwn fromEntries ok\n", stderr = "", errors = [] }

[[case]]
name = "json_run_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_jsx_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `json_run_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_jsx_input`. `assert_browser_harness_object_has_own_from_entries` writes the plain Object.hasOwn-from-entries run program to main.jsx and asks `kali` to run it against the browser API surface with the browser harness backed by node, with `--output json`. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. In json mode the source reads json["stdout"] `let stdout = json["stdout"].as_str()` (:325) and asserts a substring match on it, plus `assert_eq!(json["stderr"], "")` (:330) and an empty `errors` array, and for `run` the envelope and payload exitCode / for `test` the payload total, passed and failed. On the JSON branch the same claim is taken against the string leaf json["stdout"], which has NO substring form in the case format (there is no json_contains key), so per controller ruling 3 it becomes an exact `json.stdout` pin -- and, per U9, only after the value was captured from the real kali binary rather than hand-computed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.jsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "run", success = true, exitCode = 0, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", exitCode = 0 }, stdout = "browser object hasOwn fromEntries ok\n", stderr = "", errors = [] }

[[case]]
name = "json_run_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `json_run_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_input`. `assert_browser_harness_object_has_own_from_entries` writes the plain Object.hasOwn-from-entries run program to main.ts and asks `kali` to run it against the browser API surface with the browser harness backed by node, with `--output json`. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. In json mode the source reads json["stdout"] `let stdout = json["stdout"].as_str()` (:325) and asserts a substring match on it, plus `assert_eq!(json["stderr"], "")` (:330) and an empty `errors` array, and for `run` the envelope and payload exitCode / for `test` the payload total, passed and failed. On the JSON branch the same claim is taken against the string leaf json["stdout"], which has NO substring form in the case format (there is no json_contains key), so per controller ruling 3 it becomes an exact `json.stdout` pin -- and, per U9, only after the value was captured from the real kali binary rather than hand-computed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.ts"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "run", success = true, exitCode = 0, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", exitCode = 0 }, stdout = "browser object hasOwn fromEntries ok\n", stderr = "", errors = [] }

[[case]]
name = "json_run_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_tsx_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `json_run_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_tsx_input`. `assert_browser_harness_object_has_own_from_entries` writes the plain Object.hasOwn-from-entries run program to main.tsx and asks `kali` to run it against the browser API surface with the browser harness backed by node, with `--output json`. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. In json mode the source reads json["stdout"] `let stdout = json["stdout"].as_str()` (:325) and asserts a substring match on it, plus `assert_eq!(json["stderr"], "")` (:330) and an empty `errors` array, and for `run` the envelope and payload exitCode / for `test` the payload total, passed and failed. On the JSON branch the same claim is taken against the string leaf json["stdout"], which has NO substring form in the case format (there is no json_contains key), so per controller ruling 3 it becomes an exact `json.stdout` pin -- and, per U9, only after the value was captured from the real kali binary rather than hand-computed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.tsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "run", success = true, exitCode = 0, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", exitCode = 0 }, stdout = "browser object hasOwn fromEntries ok\n", stderr = "", errors = [] }

[[case]]
name = "test_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_js_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `test_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_js_input`. `assert_browser_harness_object_has_own_from_entries` writes the plain Object.hasOwn-from-entries test program to smoke.test.js and asks `kali` to test it against the browser API surface with the browser harness backed by node. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. Its text-mode claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:327), plus `stdout.contains("ok 1")` (:339) for the `test` command. The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.js"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok", "ok 1"]

[[case]]
name = "test_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_jsx_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `test_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_jsx_input`. `assert_browser_harness_object_has_own_from_entries` writes the plain Object.hasOwn-from-entries test program to smoke.test.jsx and asks `kali` to test it against the browser API surface with the browser harness backed by node. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. Its text-mode claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:327), plus `stdout.contains("ok 1")` (:339) for the `test` command. The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.jsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok", "ok 1"]

[[case]]
name = "test_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `test_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_input`. `assert_browser_harness_object_has_own_from_entries` writes the plain Object.hasOwn-from-entries test program to smoke.test.ts and asks `kali` to test it against the browser API surface with the browser harness backed by node. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. Its text-mode claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:327), plus `stdout.contains("ok 1")` (:339) for the `test` command. The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.ts"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok", "ok 1"]

[[case]]
name = "test_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_tsx_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `test_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_tsx_input`. `assert_browser_harness_object_has_own_from_entries` writes the plain Object.hasOwn-from-entries test program to smoke.test.tsx and asks `kali` to test it against the browser API surface with the browser harness backed by node. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. Its text-mode claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:327), plus `stdout.contains("ok 1")` (:339) for the `test` command. The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.tsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok", "ok 1"]

[[case]]
name = "json_test_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_js_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `json_test_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_js_input`. `assert_browser_harness_object_has_own_from_entries` writes the plain Object.hasOwn-from-entries test program to smoke.test.js and asks `kali` to test it against the browser API surface with the browser harness backed by node, with `--output json`. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. In json mode the source reads json["stdout"] `let stdout = json["stdout"].as_str()` (:325) and asserts a substring match on it, plus `assert_eq!(json["stderr"], "")` (:330) and an empty `errors` array, and for `run` the envelope and payload exitCode / for `test` the payload total, passed and failed. On the JSON branch the same claim is taken against the string leaf json["stdout"], which has NO substring form in the case format (there is no json_contains key), so per controller ruling 3 it becomes an exact `json.stdout` pin -- and, per U9, only after the value was captured from the real kali binary rather than hand-computed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.js"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "test", success = true, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", total = 1, passed = 1, failed = 0 }, stdout = "browser object hasOwn fromEntries ok\n", stderr = "", errors = [] }

[[case]]
name = "json_test_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_jsx_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `json_test_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_jsx_input`. `assert_browser_harness_object_has_own_from_entries` writes the plain Object.hasOwn-from-entries test program to smoke.test.jsx and asks `kali` to test it against the browser API surface with the browser harness backed by node, with `--output json`. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. In json mode the source reads json["stdout"] `let stdout = json["stdout"].as_str()` (:325) and asserts a substring match on it, plus `assert_eq!(json["stderr"], "")` (:330) and an empty `errors` array, and for `run` the envelope and payload exitCode / for `test` the payload total, passed and failed. On the JSON branch the same claim is taken against the string leaf json["stdout"], which has NO substring form in the case format (there is no json_contains key), so per controller ruling 3 it becomes an exact `json.stdout` pin -- and, per U9, only after the value was captured from the real kali binary rather than hand-computed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.jsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "test", success = true, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", total = 1, passed = 1, failed = 0 }, stdout = "browser object hasOwn fromEntries ok\n", stderr = "", errors = [] }

[[case]]
name = "json_test_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `json_test_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_input`. `assert_browser_harness_object_has_own_from_entries` writes the plain Object.hasOwn-from-entries test program to smoke.test.ts and asks `kali` to test it against the browser API surface with the browser harness backed by node, with `--output json`. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. In json mode the source reads json["stdout"] `let stdout = json["stdout"].as_str()` (:325) and asserts a substring match on it, plus `assert_eq!(json["stderr"], "")` (:330) and an empty `errors` array, and for `run` the envelope and payload exitCode / for `test` the payload total, passed and failed. On the JSON branch the same claim is taken against the string leaf json["stdout"], which has NO substring form in the case format (there is no json_contains key), so per controller ruling 3 it becomes an exact `json.stdout` pin -- and, per U9, only after the value was captured from the real kali binary rather than hand-computed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.ts"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "test", success = true, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", total = 1, passed = 1, failed = 0 }, stdout = "browser object hasOwn fromEntries ok\n", stderr = "", errors = [] }

[[case]]
name = "json_test_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_tsx_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `json_test_supports_object_has_own_from_entries_when_browser_harness_is_configured_in_tsx_input`. `assert_browser_harness_object_has_own_from_entries` writes the plain Object.hasOwn-from-entries test program to smoke.test.tsx and asks `kali` to test it against the browser API surface with the browser harness backed by node, with `--output json`. The program checks membership in an Object.fromEntries(...) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. In json mode the source reads json["stdout"] `let stdout = json["stdout"].as_str()` (:325) and asserts a substring match on it, plus `assert_eq!(json["stderr"], "")` (:330) and an empty `errors` array, and for `run` the envelope and payload exitCode / for `test` the payload total, passed and failed. On the JSON branch the same claim is taken against the string leaf json["stdout"], which has NO substring form in the case format (there is no json_contains key), so per controller ruling 3 it becomes an exact `json.stdout` pin -- and, per U9, only after the value was captured from the real kali binary rather than hand-computed. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.tsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "test", success = true, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", total = 1, passed = 1, failed = 0 }, stdout = "browser object hasOwn fromEntries ok\n", stderr = "", errors = [] }

[[case]]
name = "run_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_js_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `run_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_js_input`. `assert_browser_harness_object_has_own_from_entries` writes the FROZEN Object.hasOwn-from-entries run program to main_frozen.js and asks `kali` to run it against the browser API surface with the browser harness backed by node. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. Its text-mode claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:327). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The entry is U5-renamed because the plain program already claims the unsuffixed key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_frozen.js"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "run_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input_ts_text"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `run_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input`, the ts / text cell of its three-filename x two-output-mode loop (rule 5). `assert_browser_harness_object_has_own_from_entries` writes the FROZEN Object.hasOwn-from-entries run program to main_frozen.ts and asks `kali` to run it against the browser API surface with the browser harness backed by node. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. Its text-mode claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:327). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The entry is U5-renamed because the plain program already claims the unsuffixed key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_frozen.ts"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "run_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input_ts_json"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `run_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input`, the ts / json cell of its three-filename x two-output-mode loop (rule 5). `assert_browser_harness_object_has_own_from_entries` writes the FROZEN Object.hasOwn-from-entries run program to main_frozen.ts and asks `kali` to run it against the browser API surface with the browser harness backed by node, with `--output json`. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. In json mode the source reads json["stdout"] `let stdout = json["stdout"].as_str()` (:325) and asserts a substring match on it, plus `assert_eq!(json["stderr"], "")` (:330) and an empty `errors` array, and for `run` the envelope and payload exitCode / for `test` the payload total, passed and failed. On the JSON branch the same claim is taken against the string leaf json["stdout"], which has NO substring form in the case format (there is no json_contains key), so per controller ruling 3 it becomes an exact `json.stdout` pin -- and, per U9, only after the value was captured from the real kali binary rather than hand-computed. The entry is U5-renamed because the plain program already claims the unsuffixed key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_frozen.ts"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "run", success = true, exitCode = 0, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", exitCode = 0 }, stdout = "browser object hasOwn fromEntries ok\n", stderr = "", errors = [] }

[[case]]
name = "run_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input_jsx_text"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `run_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input`, the jsx / text cell of its three-filename x two-output-mode loop (rule 5). `assert_browser_harness_object_has_own_from_entries` writes the FROZEN Object.hasOwn-from-entries run program to main_frozen.jsx and asks `kali` to run it against the browser API surface with the browser harness backed by node. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. Its text-mode claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:327). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The entry is U5-renamed because the plain program already claims the unsuffixed key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_frozen.jsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "run_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input_jsx_json"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `run_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input`, the jsx / json cell of its three-filename x two-output-mode loop (rule 5). `assert_browser_harness_object_has_own_from_entries` writes the FROZEN Object.hasOwn-from-entries run program to main_frozen.jsx and asks `kali` to run it against the browser API surface with the browser harness backed by node, with `--output json`. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. In json mode the source reads json["stdout"] `let stdout = json["stdout"].as_str()` (:325) and asserts a substring match on it, plus `assert_eq!(json["stderr"], "")` (:330) and an empty `errors` array, and for `run` the envelope and payload exitCode / for `test` the payload total, passed and failed. On the JSON branch the same claim is taken against the string leaf json["stdout"], which has NO substring form in the case format (there is no json_contains key), so per controller ruling 3 it becomes an exact `json.stdout` pin -- and, per U9, only after the value was captured from the real kali binary rather than hand-computed. The entry is U5-renamed because the plain program already claims the unsuffixed key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_frozen.jsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "run", success = true, exitCode = 0, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", exitCode = 0 }, stdout = "browser object hasOwn fromEntries ok\n", stderr = "", errors = [] }

[[case]]
name = "run_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input_tsx_text"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `run_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input`, the tsx / text cell of its three-filename x two-output-mode loop (rule 5). `assert_browser_harness_object_has_own_from_entries` writes the FROZEN Object.hasOwn-from-entries run program to main_frozen.tsx and asks `kali` to run it against the browser API surface with the browser harness backed by node. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. Its text-mode claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:327). The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The entry is U5-renamed because the plain program already claims the unsuffixed key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_frozen.tsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok"]

[[case]]
name = "run_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input_tsx_json"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `run_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input`, the tsx / json cell of its three-filename x two-output-mode loop (rule 5). `assert_browser_harness_object_has_own_from_entries` writes the FROZEN Object.hasOwn-from-entries run program to main_frozen.tsx and asks `kali` to run it against the browser API surface with the browser harness backed by node, with `--output json`. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. In json mode the source reads json["stdout"] `let stdout = json["stdout"].as_str()` (:325) and asserts a substring match on it, plus `assert_eq!(json["stderr"], "")` (:330) and an empty `errors` array, and for `run` the envelope and payload exitCode / for `test` the payload total, passed and failed. On the JSON branch the same claim is taken against the string leaf json["stdout"], which has NO substring form in the case format (there is no json_contains key), so per controller ruling 3 it becomes an exact `json.stdout` pin -- and, per U9, only after the value was captured from the real kali binary rather than hand-computed. The entry is U5-renamed because the plain program already claims the unsuffixed key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_frozen.tsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "run", success = true, exitCode = 0, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", exitCode = 0 }, stdout = "browser object hasOwn fromEntries ok\n", stderr = "", errors = [] }

[[case]]
name = "test_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_js_input"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `test_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_js_input`. `assert_browser_harness_object_has_own_from_entries` writes the FROZEN Object.hasOwn-from-entries test program to smoke_frozen.test.js and asks `kali` to test it against the browser API surface with the browser harness backed by node. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. Its text-mode claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:327), plus `stdout.contains("ok 1")` (:339) for the `test` command. The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The entry is U5-renamed because the plain program already claims the unsuffixed key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke_frozen.test.js"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok", "ok 1"]

[[case]]
name = "test_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input_ts_text"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `test_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input`, the ts / text cell of its three-filename x two-output-mode loop (rule 5). `assert_browser_harness_object_has_own_from_entries` writes the FROZEN Object.hasOwn-from-entries test program to smoke_frozen.test.ts and asks `kali` to test it against the browser API surface with the browser harness backed by node. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. Its text-mode claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:327), plus `stdout.contains("ok 1")` (:339) for the `test` command. The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The entry is U5-renamed because the plain program already claims the unsuffixed key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke_frozen.test.ts"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok", "ok 1"]

[[case]]
name = "test_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input_ts_json"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `test_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input`, the ts / json cell of its three-filename x two-output-mode loop (rule 5). `assert_browser_harness_object_has_own_from_entries` writes the FROZEN Object.hasOwn-from-entries test program to smoke_frozen.test.ts and asks `kali` to test it against the browser API surface with the browser harness backed by node, with `--output json`. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. In json mode the source reads json["stdout"] `let stdout = json["stdout"].as_str()` (:325) and asserts a substring match on it, plus `assert_eq!(json["stderr"], "")` (:330) and an empty `errors` array, and for `run` the envelope and payload exitCode / for `test` the payload total, passed and failed. On the JSON branch the same claim is taken against the string leaf json["stdout"], which has NO substring form in the case format (there is no json_contains key), so per controller ruling 3 it becomes an exact `json.stdout` pin -- and, per U9, only after the value was captured from the real kali binary rather than hand-computed. The entry is U5-renamed because the plain program already claims the unsuffixed key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke_frozen.test.ts"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "test", success = true, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", total = 1, passed = 1, failed = 0 }, stdout = "browser object hasOwn fromEntries ok\n", stderr = "", errors = [] }

[[case]]
name = "test_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input_jsx_text"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `test_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input`, the jsx / text cell of its three-filename x two-output-mode loop (rule 5). `assert_browser_harness_object_has_own_from_entries` writes the FROZEN Object.hasOwn-from-entries test program to smoke_frozen.test.jsx and asks `kali` to test it against the browser API surface with the browser harness backed by node. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. Its text-mode claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:327), plus `stdout.contains("ok 1")` (:339) for the `test` command. The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The entry is U5-renamed because the plain program already claims the unsuffixed key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke_frozen.test.jsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok", "ok 1"]

[[case]]
name = "test_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input_jsx_json"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `test_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input`, the jsx / json cell of its three-filename x two-output-mode loop (rule 5). `assert_browser_harness_object_has_own_from_entries` writes the FROZEN Object.hasOwn-from-entries test program to smoke_frozen.test.jsx and asks `kali` to test it against the browser API surface with the browser harness backed by node, with `--output json`. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. In json mode the source reads json["stdout"] `let stdout = json["stdout"].as_str()` (:325) and asserts a substring match on it, plus `assert_eq!(json["stderr"], "")` (:330) and an empty `errors` array, and for `run` the envelope and payload exitCode / for `test` the payload total, passed and failed. On the JSON branch the same claim is taken against the string leaf json["stdout"], which has NO substring form in the case format (there is no json_contains key), so per controller ruling 3 it becomes an exact `json.stdout` pin -- and, per U9, only after the value was captured from the real kali binary rather than hand-computed. The entry is U5-renamed because the plain program already claims the unsuffixed key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke_frozen.test.jsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "test", success = true, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", total = 1, passed = 1, failed = 0 }, stdout = "browser object hasOwn fromEntries ok\n", stderr = "", errors = [] }

[[case]]
name = "test_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input_tsx_text"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `test_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input`, the tsx / text cell of its three-filename x two-output-mode loop (rule 5). `assert_browser_harness_object_has_own_from_entries` writes the FROZEN Object.hasOwn-from-entries test program to smoke_frozen.test.tsx and asks `kali` to test it against the browser API surface with the browser harness backed by node. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. Its text-mode claim is `stdout.contains("browser object hasOwn fromEntries ok")` (:327), plus `stdout.contains("ok 1")` (:339) for the `test` command. The source spells this as a plain `.contains(...)` against raw stdout, so it is carried as stdout_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. The entry is U5-renamed because the plain program already claims the unsuffixed key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke_frozen.test.tsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["browser object hasOwn fromEntries ok", "ok 1"]

[[case]]
name = "test_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input_tsx_json"
rationale = """Migrated from browser_object_has_own_from_entries.rs, the source fn `test_supports_frozen_object_has_own_from_entries_when_browser_harness_is_configured_in_ts_jsx_tsx_input`, the tsx / json cell of its three-filename x two-output-mode loop (rule 5). `assert_browser_harness_object_has_own_from_entries` writes the FROZEN Object.hasOwn-from-entries test program to smoke_frozen.test.tsx and asks `kali` to test it against the browser API surface with the browser harness backed by node, with `--output json`. The program checks membership in an Object.freeze(Object.fromEntries(...)) result through eighteen Object.hasOwn and Object.prototype.hasOwnProperty.call spellings and prints its ok line. The process succeeds. In json mode the source reads json["stdout"] `let stdout = json["stdout"].as_str()` (:325) and asserts a substring match on it, plus `assert_eq!(json["stderr"], "")` (:330) and an empty `errors` array, and for `run` the envelope and payload exitCode / for `test` the payload total, passed and failed. On the JSON branch the same claim is taken against the string leaf json["stdout"], which has NO substring form in the case format (there is no json_contains key), so per controller ruling 3 it becomes an exact `json.stdout` pin -- and, per U9, only after the value was captured from the real kali binary rather than hand-computed. The entry is U5-renamed because the plain program already claims the unsuffixed key in this file-wide `[source]` table; the name is passed to `kali` on argv only and is referenced by no fixture body, checked mechanically in this file's generator. The fixture text is the byte-exact output of executing the real fixture builder (rules 8 and 9), never hand-derived."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke_frozen.test.tsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "test", success = true, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", total = 1, passed = 1, failed = 0 }, stdout = "browser object hasOwn fromEntries ok\n", stderr = "", errors = [] }
