# Migrated from tests/browser_for_await_array_iteration_harness_wrappers_js_input.rs.
#
# 72 #[test] fns, ALL individual calls (zero loops anywhere in this file --
# confirmed by reading every fn), each a single call to
# `assert_browser_harness_for_await_wrapper(command, filename, source,
# json_output)`. No [matrix]: the 5 body variants (as_const / satisfies /
# transparent_await / parenthesized_const_alias / const_alias_chain) are
# NOT uniform across the `ext` axis -- checked directly against the fn list,
# not assumed from the first few: `satisfies` is exercised in ONLY js and
# tsx (there is no `..._satisfies_wrapper..._ts_input` or `..._jsx_input`
# fn), while the other 4 variants each cover all of js/ts/jsx/tsx. A
# file-level [matrix] on `ext` would either invent an untested
# (satisfies, ts)/(satisfies, jsx) case (rule 2 violation, since `[matrix]`
# fans EVERY [[case]] in the file uniformly -- confirmed directly against
# `crates/kali_case_runner/src/expand.rs`, which loops every matrix cell
# over every case regardless of whether that case's own fields reference
# the axis) or require excluding a case from an axis the format has no way
# to express -- same shape as the pilot's file 1 (`math_atan2_bracketed_
# root`, no matrix, heterogeneous variants). 72 [[case]] entries here, one
# per source #[test] fn, 1:1.
#
# Every case's expected `stdout`/`json` was captured LIVE from the real
# `kali` binary (`tools/task-18-browser-pilot/kali_run.py`), not
# hand-computed: all 72 combinations produce byte-identical output (`1\n2\n`
# in text mode, plus `ok 1\n` for `test`; `json.stdout` is exactly `1\n2\n`
# for every case) regardless of variant/ext/command, since every fixture
# body's `for await` loop always yields the same two items (1, 2) --
# confirmed directly, not assumed from the first case.
#
# Source's own assertion is `output.contains("1")` / `output.contains("2")`
# (`assert_browser_for_await_array_iteration`) -- a genuine substring claim,
# not `assert_eq!`/`starts_with`, so it is mirrored here as `stdout_contains
# = ["1", "2"]` rather than weakened OR gratuitously strengthened to an
# exact match that source never claimed.
#
# `[source]` reuses the bare filename `main.<ext>` across all 5 variants in
# source (safe there -- each #[test] gets its own private tempdir), which
# the flat file-wide `[source]` table cannot hold five different bodies
# under one key. Disambiguated with a variant-suffix stem
# (`main_as_const.js`, `main_satisfies.tsx`, ...); the filename is only ever
# the bare CLI positional argument passed to `kali run`/`kali test` -- never
# read back inside any fixture body (no `import()`/`require()`/self-
# referencing string anywhere in any of the 5 source fns) -- so renaming it
# changes no claim, per the rule that only a filename read back inside a
# fixture body is unsafe to rename.
#
# No Rust comments exist in this source file (confirmed:
# `grep -n '^\s*//' browser_for_await_array_iteration_harness_wrappers_js_
# input.rs` has zero hits outside the JS `// kali-tree-shake: ...` fixture
# markers, which are program text kept verbatim inside `[source]` bodies
# below, not Rust doc comments), so there is no prose to move into
# `rationale`.
#
# Source does not assert `stderr` at all in the non-json branch (only
# `output.status.success()` and `output.status.code() == Some(0)`, both
# folded into `exit = "success"` here) -- no `stderr`/`stderr_contains`
# claim is added for those cases, since source makes none. The json branch
# DOES assert `json["stderr"] == ""` and `json["errors"].is_empty()`
# exactly, carried through as `stderr = ""` / `errors = []` inside `json`.
# Source's json branch never checks a top-level `json["exitCode"]` field in
# this file (unlike some sibling harness helpers elsewhere in this family)
# -- not added here since source makes no such claim.


[source]
"main_as_const.js" = """// kali-tree-shake: forAwaitArrayIterationAsConstWrapper
const value = 2;
for await (const item of ([1, (value)] as const)) {
  console.log(item);
}
"""
"main_as_const.ts" = """// kali-tree-shake: forAwaitArrayIterationAsConstWrapper
const value = 2;
for await (const item of ([1, (value)] as const)) {
  console.log(item);
}
"""
"main_as_const.jsx" = """// kali-tree-shake: forAwaitArrayIterationAsConstWrapper
const value = 2;
for await (const item of ([1, (value)] as const)) {
  console.log(item);
}
"""
"main_as_const.tsx" = """// kali-tree-shake: forAwaitArrayIterationAsConstWrapper
const value = 2;
for await (const item of ([1, (value)] as const)) {
  console.log(item);
}
"""
"main_satisfies.js" = """// kali-tree-shake: forAwaitArrayIterationSatisfiesWrapper
const value = 2;
for await (const item of ([1, (value)] satisfies readonly [1, 2])) {
  console.log(item);
}
"""
"main_satisfies.tsx" = """// kali-tree-shake: forAwaitArrayIterationSatisfiesWrapper
const value = 2;
for await (const item of ([1, (value)] satisfies readonly [1, 2])) {
  console.log(item);
}
"""
"main_transparent_await.js" = """// kali-tree-shake: forAwaitArrayIterationTransparentAwaitWrapper
for await (const item of await [1, 2]) {
  console.log(item);
}
"""
"main_transparent_await.ts" = """// kali-tree-shake: forAwaitArrayIterationTransparentAwaitWrapper
for await (const item of await [1, 2]) {
  console.log(item);
}
"""
"main_transparent_await.jsx" = """// kali-tree-shake: forAwaitArrayIterationTransparentAwaitWrapper
for await (const item of await [1, 2]) {
  console.log(item);
}
"""
"main_transparent_await.tsx" = """// kali-tree-shake: forAwaitArrayIterationTransparentAwaitWrapper
for await (const item of await [1, 2]) {
  console.log(item);
}
"""
"main_parenthesized_const_alias.js" = """// kali-tree-shake: forAwaitArrayIterationParenthesizedConstAliasWrapper
const value = 2;
const values = ([1, (value)]);
for await (const item of (values)) {
  console.log(item);
}
"""
"main_parenthesized_const_alias.ts" = """// kali-tree-shake: forAwaitArrayIterationParenthesizedConstAliasWrapper
const value = 2;
const values = ([1, (value)]);
for await (const item of (values)) {
  console.log(item);
}
"""
"main_parenthesized_const_alias.jsx" = """// kali-tree-shake: forAwaitArrayIterationParenthesizedConstAliasWrapper
const value = 2;
const values = ([1, (value)]);
for await (const item of (values)) {
  console.log(item);
}
"""
"main_parenthesized_const_alias.tsx" = """// kali-tree-shake: forAwaitArrayIterationParenthesizedConstAliasWrapper
const value = 2;
const values = ([1, (value)]);
for await (const item of (values)) {
  console.log(item);
}
"""
"main_const_alias_chain.js" = """// kali-tree-shake: forAwaitArrayIterationConstAliasChainWrapper
const value = 2;
const values = ([1, (value)]);
const alias = values;
for await (const item of alias) {
  console.log(item);
}
"""
"main_const_alias_chain.ts" = """// kali-tree-shake: forAwaitArrayIterationConstAliasChainWrapper
const value = 2;
const values = ([1, (value)]);
const alias = values;
for await (const item of alias) {
  console.log(item);
}
"""
"main_const_alias_chain.jsx" = """// kali-tree-shake: forAwaitArrayIterationConstAliasChainWrapper
const value = 2;
const values = ([1, (value)]);
const alias = values;
for await (const item of alias) {
  console.log(item);
}
"""
"main_const_alias_chain.tsx" = """// kali-tree-shake: forAwaitArrayIterationConstAliasChainWrapper
const value = 2;
const values = ([1, (value)]);
const alias = values;
for await (const item of alias) {
  console.log(item);
}
"""

[[case]]
name = "run_supports_for_await_array_iteration_lowering_with_as_const_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_as_const.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_run_supports_for_await_array_iteration_lowering_with_as_const_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_as_const.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "run", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "test_supports_for_await_array_iteration_lowering_with_as_const_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_as_const.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_test_supports_for_await_array_iteration_lowering_with_as_const_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_as_const.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "test", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "run_supports_for_await_array_iteration_lowering_with_as_const_wrapper_in_browser_api_surface_with_harness_ts_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_as_const.ts"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_run_supports_for_await_array_iteration_lowering_with_as_const_wrapper_in_browser_api_surface_with_harness_ts_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_as_const.ts"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "run", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "test_supports_for_await_array_iteration_lowering_with_as_const_wrapper_in_browser_api_surface_with_harness_ts_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_as_const.ts"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_test_supports_for_await_array_iteration_lowering_with_as_const_wrapper_in_browser_api_surface_with_harness_ts_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_as_const.ts"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "test", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "run_supports_for_await_array_iteration_lowering_with_as_const_wrapper_in_browser_api_surface_with_harness_jsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_as_const.jsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_run_supports_for_await_array_iteration_lowering_with_as_const_wrapper_in_browser_api_surface_with_harness_jsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_as_const.jsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "run", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "test_supports_for_await_array_iteration_lowering_with_as_const_wrapper_in_browser_api_surface_with_harness_jsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_as_const.jsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_test_supports_for_await_array_iteration_lowering_with_as_const_wrapper_in_browser_api_surface_with_harness_jsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_as_const.jsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "test", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "run_supports_for_await_array_iteration_lowering_with_as_const_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_as_const.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_run_supports_for_await_array_iteration_lowering_with_as_const_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_as_const.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "run", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "test_supports_for_await_array_iteration_lowering_with_as_const_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_as_const.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_test_supports_for_await_array_iteration_lowering_with_as_const_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_as_const.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "test", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "run_supports_for_await_array_iteration_lowering_with_satisfies_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_satisfies.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_run_supports_for_await_array_iteration_lowering_with_satisfies_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_satisfies.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "run", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "test_supports_for_await_array_iteration_lowering_with_satisfies_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_satisfies.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_test_supports_for_await_array_iteration_lowering_with_satisfies_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_satisfies.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "test", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "run_supports_for_await_array_iteration_lowering_with_satisfies_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_satisfies.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_run_supports_for_await_array_iteration_lowering_with_satisfies_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_satisfies.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "run", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "test_supports_for_await_array_iteration_lowering_with_satisfies_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_satisfies.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_test_supports_for_await_array_iteration_lowering_with_satisfies_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_satisfies.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "test", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "run_supports_for_await_array_iteration_lowering_with_transparent_await_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_transparent_await.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_run_supports_for_await_array_iteration_lowering_with_transparent_await_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_transparent_await.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "run", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "test_supports_for_await_array_iteration_lowering_with_transparent_await_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_transparent_await.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_test_supports_for_await_array_iteration_lowering_with_transparent_await_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_transparent_await.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "test", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "run_supports_for_await_array_iteration_lowering_with_transparent_await_wrapper_in_browser_api_surface_with_harness_ts_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_transparent_await.ts"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_run_supports_for_await_array_iteration_lowering_with_transparent_await_wrapper_in_browser_api_surface_with_harness_ts_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_transparent_await.ts"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "run", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "test_supports_for_await_array_iteration_lowering_with_transparent_await_wrapper_in_browser_api_surface_with_harness_ts_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_transparent_await.ts"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_test_supports_for_await_array_iteration_lowering_with_transparent_await_wrapper_in_browser_api_surface_with_harness_ts_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_transparent_await.ts"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "test", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "run_supports_for_await_array_iteration_lowering_with_transparent_await_wrapper_in_browser_api_surface_with_harness_jsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_transparent_await.jsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_run_supports_for_await_array_iteration_lowering_with_transparent_await_wrapper_in_browser_api_surface_with_harness_jsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_transparent_await.jsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "run", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "test_supports_for_await_array_iteration_lowering_with_transparent_await_wrapper_in_browser_api_surface_with_harness_jsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_transparent_await.jsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_test_supports_for_await_array_iteration_lowering_with_transparent_await_wrapper_in_browser_api_surface_with_harness_jsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_transparent_await.jsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "test", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "run_supports_for_await_array_iteration_lowering_with_transparent_await_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_transparent_await.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_run_supports_for_await_array_iteration_lowering_with_transparent_await_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_transparent_await.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "run", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "test_supports_for_await_array_iteration_lowering_with_transparent_await_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_transparent_await.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_test_supports_for_await_array_iteration_lowering_with_transparent_await_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_transparent_await.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "test", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "run_supports_for_await_array_iteration_lowering_with_parenthesized_const_alias_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_parenthesized_const_alias.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_run_supports_for_await_array_iteration_lowering_with_parenthesized_const_alias_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_parenthesized_const_alias.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "run", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "test_supports_for_await_array_iteration_lowering_with_parenthesized_const_alias_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_parenthesized_const_alias.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_test_supports_for_await_array_iteration_lowering_with_parenthesized_const_alias_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_parenthesized_const_alias.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "test", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "run_supports_for_await_array_iteration_lowering_with_parenthesized_const_alias_wrapper_in_browser_api_surface_with_harness_ts_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_parenthesized_const_alias.ts"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_run_supports_for_await_array_iteration_lowering_with_parenthesized_const_alias_wrapper_in_browser_api_surface_with_harness_ts_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_parenthesized_const_alias.ts"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "run", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "test_supports_for_await_array_iteration_lowering_with_parenthesized_const_alias_wrapper_in_browser_api_surface_with_harness_ts_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_parenthesized_const_alias.ts"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_test_supports_for_await_array_iteration_lowering_with_parenthesized_const_alias_wrapper_in_browser_api_surface_with_harness_ts_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_parenthesized_const_alias.ts"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "test", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "run_supports_for_await_array_iteration_lowering_with_parenthesized_const_alias_wrapper_in_browser_api_surface_with_harness_jsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_parenthesized_const_alias.jsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_run_supports_for_await_array_iteration_lowering_with_parenthesized_const_alias_wrapper_in_browser_api_surface_with_harness_jsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_parenthesized_const_alias.jsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "run", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "test_supports_for_await_array_iteration_lowering_with_parenthesized_const_alias_wrapper_in_browser_api_surface_with_harness_jsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_parenthesized_const_alias.jsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_test_supports_for_await_array_iteration_lowering_with_parenthesized_const_alias_wrapper_in_browser_api_surface_with_harness_jsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_parenthesized_const_alias.jsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "test", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "run_supports_for_await_array_iteration_lowering_with_parenthesized_const_alias_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_parenthesized_const_alias.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_run_supports_for_await_array_iteration_lowering_with_parenthesized_const_alias_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_parenthesized_const_alias.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "run", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "test_supports_for_await_array_iteration_lowering_with_parenthesized_const_alias_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_parenthesized_const_alias.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_test_supports_for_await_array_iteration_lowering_with_parenthesized_const_alias_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_parenthesized_const_alias.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "test", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "run_supports_for_await_array_iteration_lowering_with_const_alias_chain_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_const_alias_chain.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_run_supports_for_await_array_iteration_lowering_with_const_alias_chain_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_const_alias_chain.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "run", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "test_supports_for_await_array_iteration_lowering_with_const_alias_chain_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_const_alias_chain.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_test_supports_for_await_array_iteration_lowering_with_const_alias_chain_wrapper_in_browser_api_surface_with_harness_js_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_const_alias_chain.js"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "test", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "run_supports_for_await_array_iteration_lowering_with_const_alias_chain_wrapper_in_browser_api_surface_with_harness_ts_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_const_alias_chain.ts"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_run_supports_for_await_array_iteration_lowering_with_const_alias_chain_wrapper_in_browser_api_surface_with_harness_ts_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_const_alias_chain.ts"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "run", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "test_supports_for_await_array_iteration_lowering_with_const_alias_chain_wrapper_in_browser_api_surface_with_harness_ts_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_const_alias_chain.ts"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_test_supports_for_await_array_iteration_lowering_with_const_alias_chain_wrapper_in_browser_api_surface_with_harness_ts_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_const_alias_chain.ts"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "test", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "run_supports_for_await_array_iteration_lowering_with_const_alias_chain_wrapper_in_browser_api_surface_with_harness_jsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_const_alias_chain.jsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_run_supports_for_await_array_iteration_lowering_with_const_alias_chain_wrapper_in_browser_api_surface_with_harness_jsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_const_alias_chain.jsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "run", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "test_supports_for_await_array_iteration_lowering_with_const_alias_chain_wrapper_in_browser_api_surface_with_harness_jsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_const_alias_chain.jsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_test_supports_for_await_array_iteration_lowering_with_const_alias_chain_wrapper_in_browser_api_surface_with_harness_jsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_const_alias_chain.jsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "test", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "run_supports_for_await_array_iteration_lowering_with_const_alias_chain_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_const_alias_chain.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_run_supports_for_await_array_iteration_lowering_with_const_alias_chain_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_const_alias_chain.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "run", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }

[[case]]
name = "test_supports_for_await_array_iteration_lowering_with_const_alias_chain_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_const_alias_chain.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
stdout_contains = ["1", "2"]

[[case]]
name = "json_test_supports_for_await_array_iteration_lowering_with_const_alias_chain_wrapper_in_browser_api_surface_with_harness_tsx_input"
rationale = """Migrated from browser_for_await_array_iteration_harness_wrappers_js_input.rs. Every one of this file's 72 #[test] fns is an individual (zero-loop) call to `assert_browser_harness_for_await_wrapper(command, filename, source, json_output)`. No [matrix]: the `satisfies` variant is exercised in only js and tsx (not ts/jsx), so a file-level `ext` matrix would either invent an untested (satisfies, ts)/(satisfies, jsx) case or require excluding a case from an axis the format cannot express -- same shape as the pilot's file 1 (math_atan2_bracketed_root). Expected stdout/json captured live from the real kali binary: every (variant, ext, command, json_output) combination produces byte-identical output (`1\\n2\\n`, plus `ok 1\\n` for test) since every fixture's `for await` loop always yields items 1 then 2. Source's own assertion is `output.contains("1")` / `output.contains("2")` (a genuine substring claim, not assert_eq!/starts_with), mirrored here as `stdout_contains = ["1", "2"]` rather than weakened or gratuitously strengthened. `[source]` filenames are disambiguated with a variant-suffix stem (main_<variant>.<ext>) because source reuses the bare filename `main.<ext>` across all 5 variants and the flat `[source]` table is one file-wide namespace; the filename is only ever the bare CLI positional argument, never read back inside any fixture body, so renaming it changes no claim. Source does not assert stderr at all in the non-json branch (only exit success/code, folded into `exit = "success"`); the json branch's exact `json["stderr"] == ""` / `json["errors"].is_empty()` claims are carried through as `stderr = ""` / `errors = []` inside `json`. No Rust comments exist in this source file."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main_const_alias_chain.tsx"]
env = { "KALI_BROWSER_BUNDLE_HARNESS_COMMAND" = "node" }
exit = "success"
json = { "schemaVersion" = 1, "command" = "test", "success" = true, "payload" = { "hostContract" = "browser-requested", "runtimeBackend" = "browser-harness" }, "stdout" = "1\n2\n", "stderr" = "", "errors" = [] }
