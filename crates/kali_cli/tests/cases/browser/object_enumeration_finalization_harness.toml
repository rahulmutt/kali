# Migrated from tests/browser_object_enumeration_finalization_harness.rs.
#
# RULE 12 (carry every source comment verbatim): this source carries exactly ONE
# Rust comment block (:414), attached to its single assert helper, which
# every case below is produced by. It is carried into every rationale, and the
# text is COPIED out of the `.rs` by this file's generator, never retyped.
#
# RULE 7 / U1 -- MATRIX DECLINED, and why.
# 10 `#[test]` fns, 10 real helper invocations with every loop
# expanded.
# `json_output` IS NOT EXERCISED UNIFORMLY ACROSS THE EXTENSIONS. Both commands run
# over js/ts/jsx/tsx in TEXT mode, but the source declares a JSON test for `js` ONLY
# -- there is no json ts, jsx or tsx test here, for either command. A four-value
# `ext` axis over a json case would manufacture six runs the source never made (rule
# 2), and there is no per-case opt-out.
# Per U1 a `[matrix]` axis is FILE-WIDE -- `expand()` fans EVERY `[[case]]` by the full
# cross-product whether or not that case references the axis, and there is no per-case
# opt-out -- so an axis that any one case does not vary over would manufacture trials
# the source never ran (also a rule-2 invention). Rule 7's own remedy applies: drop
# `[matrix]` for the whole file and write named siblings.
# 10 invocations -> 10 named `[[case]]` entries, no `[matrix]`.
#
# RULE 6 -- 1:1 MAPPING. With no `[matrix]` there is no fold: every `[[case]]` below is
# exactly one real helper invocation of the source, named after the `#[test]` fn that made
# it. Two source fns are never folded into one case even when their invocations are
# literally identical, because the case is the only remaining trace of the fn.
#
# U2 -- `[source]` is FILE-WIDE, and that is safe here. Every fixture
# (`main.js`, `main.ts`, `main.jsx`, `main.tsx`, `smoke.test.js`, `smoke.test.ts`, `smoke.test.jsx`, `smoke.test.tsx`)
# is written unconditionally by the source into a fresh temp dir: no fixture is written
# behind an `if`, and no case's point is the presence or absence of a file.
# Every command below names its entry explicitly on argv, so the unused siblings in
# a trial dir are inert.
#
# RULING 7 -- DUPLICATE `[source]` BODIES ARE NOT HOISTED. Several entries below hold
# byte-identical program text. U13 would hoist them into `[constants]`; controller ruling 7
# declines that for `browser/` (a hoisted body makes `check_fixtures.py` go red on a correct
# file, and hoisting moves program text onto the surface `assertion_strings()` searches).
# The mandatory half of that ruling is honoured instead: the duplication is asserted
# MECHANICALLY in this file's generator, not eyeballed -- the generator compares the
# extracted fixture strings and raises if they are not byte-identical.
#
# MIGRATION NOTE (controller ruling 8) -- THE SOURCE FN NAMES ARE STALE.
# Every `#[test]` fn in this source is named for what it was once thought to prove, but the body asserts the command FAILS -- the source's only
# process assertion is `assert!(!output.status.success(), ...)`.
# The names are carried into the case names below unchanged and are NOT corrected:
# a fn name is not a comment so U7 does not literally apply, the `.rs` files are
# deleted wholesale after batch 8, and editing a source invalidates every audit run
# against its pre-trim blob. The discrepancy is recorded here so the case file
# preserves what the source actually did.
# Affected: all 10 `#[test]` fns.
#
# RULE 13 -- transitive helper docs. Checked every fn in each call chain:
#   `kali_bin`, `browser_object_enumeration_finalization_run_source`,
#   `browser_object_enumeration_finalization_test_source`,
#   `assert_browser_object_enumeration_finalization` -- none carries a `///` doc comment.
# The chain also reaches `kali_runtime_contract::browser_bundle_harness_script` and
# `::browser_harness_command_parts_for`, which DO carry one-line `///` docs. Those are
# NOT carried: in the migrated form this case file never calls them -- the
# `browser_bundle_harness` step kind means the case RUNNER does
# (crates/kali_case_runner/src/steps.rs) -- so their docs describe shared runner
# infrastructure documented in design spec 5.3, not what this case claims. That is
# controller ruling 6, which writes the exemption into the rule rather than leaving it
# to implementer discretion.
#
# ARGV ORDER is transcribed in the exact order the source's `Command` builder appends it,
# which differs between the two helper shapes and is not normalised here:
#   * build:   `build --bundle --api browser [--output json] <entry>` -- the `--output json`
#              pair is appended AFTER the subcommand and its flags.
#   * run/test: `[--output json] <run|test> --api browser [--max-threads 0
#              --max-spawned-processes 0] <entry>` -- the `--output json` pair is appended
#              BEFORE the subcommand.
# The source passes an absolute `dir.path().join(filename)` as the entry; the case runner
# passes the bare filename relative to the trial dir, matching every previously shipped
# `browser/` case file.
#
# ASSERTION SHAPE -- THIS TARGET ASSERTS A FAILURE, NOT A SUCCESS. The source's only
# process assertion is `assert!(!output.status.success(), ...)`, so the migrated step
# carries `exit = "failure"` and nothing else. That is the whole claim; adding a
# diagnostic-code or stdout claim the source never made would be a rule-2 invention,
# and `exit = "failure"` is exactly as strong as the assertion it replaces.
# The `!success` assertion is at :416. The argv carries `--max-threads 0`
# (`.arg("--max-threads")` :406) and `--max-spawned-processes 0` (:408), and the environment
# carries the variable named by
# `kali_runtime_contract::BROWSER_HARNESS_COMMAND_ENV` (:397), set to node.
# There is no json claim on either branch: the source parses no envelope at all, in
# either mode -- `--output json` changes only the argv here. That is also why this
# file's two json cases exist at all: they differ from their text siblings on argv
# alone, and dropping them would drop two real invocations.

[source]
"main.js" = """function assertSyncFinalization() {
  const values = { "b": 1, "a": 2 };
  let returnFinally = false;
  function returnProbe() {
    try {
      for (const key of Object.keys(values)) {
        return key;
      }
      throw new Error('unexpected empty Object.keys iteration');
    } finally {
      returnFinally = true;
    }
  }
  const returnValue = returnProbe();
  if (returnValue !== 'b' || !returnFinally) {
    throw new Error('unexpected Object.keys return/finally semantics');
  }

  let valuesReturnFinally = false;
  function valuesReturnProbe() {
    try {
      for (const value of Object.values(values)) {
        return value;
      }
      throw new Error('unexpected empty Object.values iteration');
    } finally {
      valuesReturnFinally = true;
    }
  }
  const valuesReturnValue = valuesReturnProbe();
  if (valuesReturnValue !== 1 || !valuesReturnFinally) {
    throw new Error('unexpected Object.values return/finally semantics');
  }

  let throwFinally = false;
  function throwProbe() {
    try {
      for (const entry of Object.entries(values)) {
        if (entry[0] === 'b') {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Object.entries iteration');
    } finally {
      throwFinally = true;
    }
  }
  let threw = false;
  try {
    throwProbe();
  } catch {
    threw = true;
  }
  if (!threw || !throwFinally) {
    throw new Error('unexpected Object.entries throw/finally semantics');
  }

  let valuesThrowFinally = false;
  function valuesThrowProbe() {
    try {
      for (const value of Object.values(values)) {
        if (value === 1) {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Object.values iteration');
    } finally {
      valuesThrowFinally = true;
    }
  }
  let valuesThrew = false;
  try {
    valuesThrowProbe();
  } catch {
    valuesThrew = true;
  }
  if (!valuesThrew || !valuesThrowFinally) {
    throw new Error('unexpected Object.values throw/finally semantics');
  }

  let valuesBreakFinally = false;
  let valuesBreakSeen = false;
  function valuesBreakProbe() {
    try {
      for (const value of Object.values(values)) {
        if (value === 1) {
          continue;
        }
        valuesBreakSeen = true;
        break;
      }
    } finally {
      valuesBreakFinally = true;
    }
  }
  valuesBreakProbe();
  if (!valuesBreakSeen || !valuesBreakFinally) {
    throw new Error('unexpected Object.values break/continue semantics');
  }

  let entriesBreakFinally = false;
  let entriesBreakSeen = false;
  function entriesBreakProbe() {
    try {
      for (const entry of Object.entries(values)) {
        if (entry[0] === 'b') {
          continue;
        }
        entriesBreakSeen = true;
        break;
      }
    } finally {
      entriesBreakFinally = true;
    }
  }
  entriesBreakProbe();
  if (!entriesBreakSeen || !entriesBreakFinally) {
    throw new Error('unexpected Object.entries break/continue semantics');
  }

  let reflectReturnFinally = false;
  function reflectReturnProbe() {
    try {
      for (const key of Reflect.ownKeys(values)) {
        return key;
      }
      throw new Error('unexpected empty Reflect.ownKeys iteration');
    } finally {
      reflectReturnFinally = true;
    }
  }
  const reflectReturnValue = reflectReturnProbe();
  if (reflectReturnValue !== 'b' || !reflectReturnFinally) {
    throw new Error('unexpected Reflect.ownKeys return/finally semantics');
  }

  let reflectThrowFinally = false;
  function reflectThrowProbe() {
    try {
      for (const key of Reflect.ownKeys(values)) {
        if (key === 'b') {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Reflect.ownKeys iteration');
    } finally {
      reflectThrowFinally = true;
    }
  }
  let reflectThrew = false;
  try {
    reflectThrowProbe();
  } catch {
    reflectThrew = true;
  }
  if (!reflectThrew || !reflectThrowFinally) {
    throw new Error('unexpected Reflect.ownKeys throw/finally semantics');
  }

  const asyncValues = { "b": 1, "a": 2 };
  let asyncFinallySeen = false;
  let asyncThrew = false;
  try {
    for await (const key of Object.keys(asyncValues)) {
      if (key === 'b') {
        throw new Error('boom');
      }
    }
    throw new Error('unexpected empty async Object.keys iteration');
  } catch {
    asyncThrew = true;
  } finally {
    asyncFinallySeen = true;
  }
  if (!asyncThrew || !asyncFinallySeen) {
    throw new Error('unexpected async Object.keys throw/finally semantics');
  }

  let asyncValuesFinallySeen = false;
  let asyncValuesThrew = false;
  try {
    for await (const value of Object.values(asyncValues)) {
      if (value === 1) {
        throw new Error('boom');
      }
    }
    throw new Error('unexpected empty async Object.values iteration');
  } catch {
    asyncValuesThrew = true;
  } finally {
    asyncValuesFinallySeen = true;
  }
  if (!asyncValuesThrew || !asyncValuesFinallySeen) {
    throw new Error('unexpected async Object.values throw/finally semantics');
  }

  let asyncValuesBreakFinally = false;
  let asyncValuesBreakSeen = false;
  try {
    for await (const value of Object.values(asyncValues)) {
      if (value === 1) {
        continue;
      }
      asyncValuesBreakSeen = true;
      break;
    }
  } finally {
    asyncValuesBreakFinally = true;
  }
  if (!asyncValuesBreakSeen || !asyncValuesBreakFinally) {
    throw new Error('unexpected async Object.values break/continue semantics');
  }

  let asyncEntriesBreakFinally = false;
  let asyncEntriesBreakSeen = false;
  try {
    for await (const entry of Object.entries(asyncValues)) {
      if (entry[0] === 'b') {
        continue;
      }
      asyncEntriesBreakSeen = true;
      break;
    }
  } finally {
    asyncEntriesBreakFinally = true;
  }
  if (!asyncEntriesBreakSeen || !asyncEntriesBreakFinally) {
    throw new Error('unexpected async Object.entries break/continue semantics');
  }

  let asyncEntriesFinallySeen = false;
  let asyncEntriesThrew = false;
  try {
    for await (const entry of Object.entries(asyncValues)) {
      if (entry[0] === 'b') {
        throw new Error('boom');
      }
    }
    throw new Error('unexpected empty async Object.entries iteration');
  } catch {
    asyncEntriesThrew = true;
  } finally {
    asyncEntriesFinallySeen = true;
  }
  if (!asyncEntriesThrew || !asyncEntriesFinallySeen) {
    throw new Error('unexpected async Object.entries throw/finally semantics');
  }

  let asyncReflectReturnFinallySeen = false;
  function asyncReflectReturnProbe() {
    try {
      for await (const key of Reflect.ownKeys(asyncValues)) {
        return key;
      }
      throw new Error('unexpected empty async Reflect.ownKeys iteration');
    } finally {
      asyncReflectReturnFinallySeen = true;
    }
  }
  const asyncReflectReturnValue = asyncReflectReturnProbe();
  if (asyncReflectReturnValue !== 'b' || !asyncReflectReturnFinallySeen) {
    throw new Error('unexpected async Reflect.ownKeys return/finally semantics');
  }

  let asyncReflectThrowFinallySeen = false;
  let asyncReflectThrew = false;
  try {
    for await (const key of Reflect.ownKeys(asyncValues)) {
      if (key === 'b') {
        throw new Error('boom');
      }
    }
    throw new Error('unexpected empty async Reflect.ownKeys iteration');
  } catch {
    asyncReflectThrew = true;
  } finally {
    asyncReflectThrowFinallySeen = true;
  }
  if (!asyncReflectThrew || !asyncReflectThrowFinallySeen) {
    throw new Error('unexpected async Reflect.ownKeys throw/finally semantics');
  }
}

assertSyncFinalization();
console.log('browser object enumeration finalization ok');
"""
"main.ts" = """function assertSyncFinalization() {
  const values = { "b": 1, "a": 2 };
  let returnFinally = false;
  function returnProbe() {
    try {
      for (const key of Object.keys(values)) {
        return key;
      }
      throw new Error('unexpected empty Object.keys iteration');
    } finally {
      returnFinally = true;
    }
  }
  const returnValue = returnProbe();
  if (returnValue !== 'b' || !returnFinally) {
    throw new Error('unexpected Object.keys return/finally semantics');
  }

  let valuesReturnFinally = false;
  function valuesReturnProbe() {
    try {
      for (const value of Object.values(values)) {
        return value;
      }
      throw new Error('unexpected empty Object.values iteration');
    } finally {
      valuesReturnFinally = true;
    }
  }
  const valuesReturnValue = valuesReturnProbe();
  if (valuesReturnValue !== 1 || !valuesReturnFinally) {
    throw new Error('unexpected Object.values return/finally semantics');
  }

  let throwFinally = false;
  function throwProbe() {
    try {
      for (const entry of Object.entries(values)) {
        if (entry[0] === 'b') {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Object.entries iteration');
    } finally {
      throwFinally = true;
    }
  }
  let threw = false;
  try {
    throwProbe();
  } catch {
    threw = true;
  }
  if (!threw || !throwFinally) {
    throw new Error('unexpected Object.entries throw/finally semantics');
  }

  let valuesThrowFinally = false;
  function valuesThrowProbe() {
    try {
      for (const value of Object.values(values)) {
        if (value === 1) {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Object.values iteration');
    } finally {
      valuesThrowFinally = true;
    }
  }
  let valuesThrew = false;
  try {
    valuesThrowProbe();
  } catch {
    valuesThrew = true;
  }
  if (!valuesThrew || !valuesThrowFinally) {
    throw new Error('unexpected Object.values throw/finally semantics');
  }

  let valuesBreakFinally = false;
  let valuesBreakSeen = false;
  function valuesBreakProbe() {
    try {
      for (const value of Object.values(values)) {
        if (value === 1) {
          continue;
        }
        valuesBreakSeen = true;
        break;
      }
    } finally {
      valuesBreakFinally = true;
    }
  }
  valuesBreakProbe();
  if (!valuesBreakSeen || !valuesBreakFinally) {
    throw new Error('unexpected Object.values break/continue semantics');
  }

  let entriesBreakFinally = false;
  let entriesBreakSeen = false;
  function entriesBreakProbe() {
    try {
      for (const entry of Object.entries(values)) {
        if (entry[0] === 'b') {
          continue;
        }
        entriesBreakSeen = true;
        break;
      }
    } finally {
      entriesBreakFinally = true;
    }
  }
  entriesBreakProbe();
  if (!entriesBreakSeen || !entriesBreakFinally) {
    throw new Error('unexpected Object.entries break/continue semantics');
  }

  let reflectReturnFinally = false;
  function reflectReturnProbe() {
    try {
      for (const key of Reflect.ownKeys(values)) {
        return key;
      }
      throw new Error('unexpected empty Reflect.ownKeys iteration');
    } finally {
      reflectReturnFinally = true;
    }
  }
  const reflectReturnValue = reflectReturnProbe();
  if (reflectReturnValue !== 'b' || !reflectReturnFinally) {
    throw new Error('unexpected Reflect.ownKeys return/finally semantics');
  }

  let reflectThrowFinally = false;
  function reflectThrowProbe() {
    try {
      for (const key of Reflect.ownKeys(values)) {
        if (key === 'b') {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Reflect.ownKeys iteration');
    } finally {
      reflectThrowFinally = true;
    }
  }
  let reflectThrew = false;
  try {
    reflectThrowProbe();
  } catch {
    reflectThrew = true;
  }
  if (!reflectThrew || !reflectThrowFinally) {
    throw new Error('unexpected Reflect.ownKeys throw/finally semantics');
  }

  const asyncValues = { "b": 1, "a": 2 };
  let asyncFinallySeen = false;
  let asyncThrew = false;
  try {
    for await (const key of Object.keys(asyncValues)) {
      if (key === 'b') {
        throw new Error('boom');
      }
    }
    throw new Error('unexpected empty async Object.keys iteration');
  } catch {
    asyncThrew = true;
  } finally {
    asyncFinallySeen = true;
  }
  if (!asyncThrew || !asyncFinallySeen) {
    throw new Error('unexpected async Object.keys throw/finally semantics');
  }

  let asyncValuesFinallySeen = false;
  let asyncValuesThrew = false;
  try {
    for await (const value of Object.values(asyncValues)) {
      if (value === 1) {
        throw new Error('boom');
      }
    }
    throw new Error('unexpected empty async Object.values iteration');
  } catch {
    asyncValuesThrew = true;
  } finally {
    asyncValuesFinallySeen = true;
  }
  if (!asyncValuesThrew || !asyncValuesFinallySeen) {
    throw new Error('unexpected async Object.values throw/finally semantics');
  }

  let asyncValuesBreakFinally = false;
  let asyncValuesBreakSeen = false;
  try {
    for await (const value of Object.values(asyncValues)) {
      if (value === 1) {
        continue;
      }
      asyncValuesBreakSeen = true;
      break;
    }
  } finally {
    asyncValuesBreakFinally = true;
  }
  if (!asyncValuesBreakSeen || !asyncValuesBreakFinally) {
    throw new Error('unexpected async Object.values break/continue semantics');
  }

  let asyncEntriesBreakFinally = false;
  let asyncEntriesBreakSeen = false;
  try {
    for await (const entry of Object.entries(asyncValues)) {
      if (entry[0] === 'b') {
        continue;
      }
      asyncEntriesBreakSeen = true;
      break;
    }
  } finally {
    asyncEntriesBreakFinally = true;
  }
  if (!asyncEntriesBreakSeen || !asyncEntriesBreakFinally) {
    throw new Error('unexpected async Object.entries break/continue semantics');
  }

  let asyncEntriesFinallySeen = false;
  let asyncEntriesThrew = false;
  try {
    for await (const entry of Object.entries(asyncValues)) {
      if (entry[0] === 'b') {
        throw new Error('boom');
      }
    }
    throw new Error('unexpected empty async Object.entries iteration');
  } catch {
    asyncEntriesThrew = true;
  } finally {
    asyncEntriesFinallySeen = true;
  }
  if (!asyncEntriesThrew || !asyncEntriesFinallySeen) {
    throw new Error('unexpected async Object.entries throw/finally semantics');
  }

  let asyncReflectReturnFinallySeen = false;
  function asyncReflectReturnProbe() {
    try {
      for await (const key of Reflect.ownKeys(asyncValues)) {
        return key;
      }
      throw new Error('unexpected empty async Reflect.ownKeys iteration');
    } finally {
      asyncReflectReturnFinallySeen = true;
    }
  }
  const asyncReflectReturnValue = asyncReflectReturnProbe();
  if (asyncReflectReturnValue !== 'b' || !asyncReflectReturnFinallySeen) {
    throw new Error('unexpected async Reflect.ownKeys return/finally semantics');
  }

  let asyncReflectThrowFinallySeen = false;
  let asyncReflectThrew = false;
  try {
    for await (const key of Reflect.ownKeys(asyncValues)) {
      if (key === 'b') {
        throw new Error('boom');
      }
    }
    throw new Error('unexpected empty async Reflect.ownKeys iteration');
  } catch {
    asyncReflectThrew = true;
  } finally {
    asyncReflectThrowFinallySeen = true;
  }
  if (!asyncReflectThrew || !asyncReflectThrowFinallySeen) {
    throw new Error('unexpected async Reflect.ownKeys throw/finally semantics');
  }
}

assertSyncFinalization();
console.log('browser object enumeration finalization ok');
"""
"main.jsx" = """function assertSyncFinalization() {
  const values = { "b": 1, "a": 2 };
  let returnFinally = false;
  function returnProbe() {
    try {
      for (const key of Object.keys(values)) {
        return key;
      }
      throw new Error('unexpected empty Object.keys iteration');
    } finally {
      returnFinally = true;
    }
  }
  const returnValue = returnProbe();
  if (returnValue !== 'b' || !returnFinally) {
    throw new Error('unexpected Object.keys return/finally semantics');
  }

  let valuesReturnFinally = false;
  function valuesReturnProbe() {
    try {
      for (const value of Object.values(values)) {
        return value;
      }
      throw new Error('unexpected empty Object.values iteration');
    } finally {
      valuesReturnFinally = true;
    }
  }
  const valuesReturnValue = valuesReturnProbe();
  if (valuesReturnValue !== 1 || !valuesReturnFinally) {
    throw new Error('unexpected Object.values return/finally semantics');
  }

  let throwFinally = false;
  function throwProbe() {
    try {
      for (const entry of Object.entries(values)) {
        if (entry[0] === 'b') {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Object.entries iteration');
    } finally {
      throwFinally = true;
    }
  }
  let threw = false;
  try {
    throwProbe();
  } catch {
    threw = true;
  }
  if (!threw || !throwFinally) {
    throw new Error('unexpected Object.entries throw/finally semantics');
  }

  let valuesThrowFinally = false;
  function valuesThrowProbe() {
    try {
      for (const value of Object.values(values)) {
        if (value === 1) {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Object.values iteration');
    } finally {
      valuesThrowFinally = true;
    }
  }
  let valuesThrew = false;
  try {
    valuesThrowProbe();
  } catch {
    valuesThrew = true;
  }
  if (!valuesThrew || !valuesThrowFinally) {
    throw new Error('unexpected Object.values throw/finally semantics');
  }

  let valuesBreakFinally = false;
  let valuesBreakSeen = false;
  function valuesBreakProbe() {
    try {
      for (const value of Object.values(values)) {
        if (value === 1) {
          continue;
        }
        valuesBreakSeen = true;
        break;
      }
    } finally {
      valuesBreakFinally = true;
    }
  }
  valuesBreakProbe();
  if (!valuesBreakSeen || !valuesBreakFinally) {
    throw new Error('unexpected Object.values break/continue semantics');
  }

  let entriesBreakFinally = false;
  let entriesBreakSeen = false;
  function entriesBreakProbe() {
    try {
      for (const entry of Object.entries(values)) {
        if (entry[0] === 'b') {
          continue;
        }
        entriesBreakSeen = true;
        break;
      }
    } finally {
      entriesBreakFinally = true;
    }
  }
  entriesBreakProbe();
  if (!entriesBreakSeen || !entriesBreakFinally) {
    throw new Error('unexpected Object.entries break/continue semantics');
  }

  let reflectReturnFinally = false;
  function reflectReturnProbe() {
    try {
      for (const key of Reflect.ownKeys(values)) {
        return key;
      }
      throw new Error('unexpected empty Reflect.ownKeys iteration');
    } finally {
      reflectReturnFinally = true;
    }
  }
  const reflectReturnValue = reflectReturnProbe();
  if (reflectReturnValue !== 'b' || !reflectReturnFinally) {
    throw new Error('unexpected Reflect.ownKeys return/finally semantics');
  }

  let reflectThrowFinally = false;
  function reflectThrowProbe() {
    try {
      for (const key of Reflect.ownKeys(values)) {
        if (key === 'b') {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Reflect.ownKeys iteration');
    } finally {
      reflectThrowFinally = true;
    }
  }
  let reflectThrew = false;
  try {
    reflectThrowProbe();
  } catch {
    reflectThrew = true;
  }
  if (!reflectThrew || !reflectThrowFinally) {
    throw new Error('unexpected Reflect.ownKeys throw/finally semantics');
  }

  const asyncValues = { "b": 1, "a": 2 };
  let asyncFinallySeen = false;
  let asyncThrew = false;
  try {
    for await (const key of Object.keys(asyncValues)) {
      if (key === 'b') {
        throw new Error('boom');
      }
    }
    throw new Error('unexpected empty async Object.keys iteration');
  } catch {
    asyncThrew = true;
  } finally {
    asyncFinallySeen = true;
  }
  if (!asyncThrew || !asyncFinallySeen) {
    throw new Error('unexpected async Object.keys throw/finally semantics');
  }

  let asyncValuesFinallySeen = false;
  let asyncValuesThrew = false;
  try {
    for await (const value of Object.values(asyncValues)) {
      if (value === 1) {
        throw new Error('boom');
      }
    }
    throw new Error('unexpected empty async Object.values iteration');
  } catch {
    asyncValuesThrew = true;
  } finally {
    asyncValuesFinallySeen = true;
  }
  if (!asyncValuesThrew || !asyncValuesFinallySeen) {
    throw new Error('unexpected async Object.values throw/finally semantics');
  }

  let asyncValuesBreakFinally = false;
  let asyncValuesBreakSeen = false;
  try {
    for await (const value of Object.values(asyncValues)) {
      if (value === 1) {
        continue;
      }
      asyncValuesBreakSeen = true;
      break;
    }
  } finally {
    asyncValuesBreakFinally = true;
  }
  if (!asyncValuesBreakSeen || !asyncValuesBreakFinally) {
    throw new Error('unexpected async Object.values break/continue semantics');
  }

  let asyncEntriesBreakFinally = false;
  let asyncEntriesBreakSeen = false;
  try {
    for await (const entry of Object.entries(asyncValues)) {
      if (entry[0] === 'b') {
        continue;
      }
      asyncEntriesBreakSeen = true;
      break;
    }
  } finally {
    asyncEntriesBreakFinally = true;
  }
  if (!asyncEntriesBreakSeen || !asyncEntriesBreakFinally) {
    throw new Error('unexpected async Object.entries break/continue semantics');
  }

  let asyncEntriesFinallySeen = false;
  let asyncEntriesThrew = false;
  try {
    for await (const entry of Object.entries(asyncValues)) {
      if (entry[0] === 'b') {
        throw new Error('boom');
      }
    }
    throw new Error('unexpected empty async Object.entries iteration');
  } catch {
    asyncEntriesThrew = true;
  } finally {
    asyncEntriesFinallySeen = true;
  }
  if (!asyncEntriesThrew || !asyncEntriesFinallySeen) {
    throw new Error('unexpected async Object.entries throw/finally semantics');
  }

  let asyncReflectReturnFinallySeen = false;
  function asyncReflectReturnProbe() {
    try {
      for await (const key of Reflect.ownKeys(asyncValues)) {
        return key;
      }
      throw new Error('unexpected empty async Reflect.ownKeys iteration');
    } finally {
      asyncReflectReturnFinallySeen = true;
    }
  }
  const asyncReflectReturnValue = asyncReflectReturnProbe();
  if (asyncReflectReturnValue !== 'b' || !asyncReflectReturnFinallySeen) {
    throw new Error('unexpected async Reflect.ownKeys return/finally semantics');
  }

  let asyncReflectThrowFinallySeen = false;
  let asyncReflectThrew = false;
  try {
    for await (const key of Reflect.ownKeys(asyncValues)) {
      if (key === 'b') {
        throw new Error('boom');
      }
    }
    throw new Error('unexpected empty async Reflect.ownKeys iteration');
  } catch {
    asyncReflectThrew = true;
  } finally {
    asyncReflectThrowFinallySeen = true;
  }
  if (!asyncReflectThrew || !asyncReflectThrowFinallySeen) {
    throw new Error('unexpected async Reflect.ownKeys throw/finally semantics');
  }
}

assertSyncFinalization();
console.log('browser object enumeration finalization ok');
"""
"main.tsx" = """function assertSyncFinalization() {
  const values = { "b": 1, "a": 2 };
  let returnFinally = false;
  function returnProbe() {
    try {
      for (const key of Object.keys(values)) {
        return key;
      }
      throw new Error('unexpected empty Object.keys iteration');
    } finally {
      returnFinally = true;
    }
  }
  const returnValue = returnProbe();
  if (returnValue !== 'b' || !returnFinally) {
    throw new Error('unexpected Object.keys return/finally semantics');
  }

  let valuesReturnFinally = false;
  function valuesReturnProbe() {
    try {
      for (const value of Object.values(values)) {
        return value;
      }
      throw new Error('unexpected empty Object.values iteration');
    } finally {
      valuesReturnFinally = true;
    }
  }
  const valuesReturnValue = valuesReturnProbe();
  if (valuesReturnValue !== 1 || !valuesReturnFinally) {
    throw new Error('unexpected Object.values return/finally semantics');
  }

  let throwFinally = false;
  function throwProbe() {
    try {
      for (const entry of Object.entries(values)) {
        if (entry[0] === 'b') {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Object.entries iteration');
    } finally {
      throwFinally = true;
    }
  }
  let threw = false;
  try {
    throwProbe();
  } catch {
    threw = true;
  }
  if (!threw || !throwFinally) {
    throw new Error('unexpected Object.entries throw/finally semantics');
  }

  let valuesThrowFinally = false;
  function valuesThrowProbe() {
    try {
      for (const value of Object.values(values)) {
        if (value === 1) {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Object.values iteration');
    } finally {
      valuesThrowFinally = true;
    }
  }
  let valuesThrew = false;
  try {
    valuesThrowProbe();
  } catch {
    valuesThrew = true;
  }
  if (!valuesThrew || !valuesThrowFinally) {
    throw new Error('unexpected Object.values throw/finally semantics');
  }

  let valuesBreakFinally = false;
  let valuesBreakSeen = false;
  function valuesBreakProbe() {
    try {
      for (const value of Object.values(values)) {
        if (value === 1) {
          continue;
        }
        valuesBreakSeen = true;
        break;
      }
    } finally {
      valuesBreakFinally = true;
    }
  }
  valuesBreakProbe();
  if (!valuesBreakSeen || !valuesBreakFinally) {
    throw new Error('unexpected Object.values break/continue semantics');
  }

  let entriesBreakFinally = false;
  let entriesBreakSeen = false;
  function entriesBreakProbe() {
    try {
      for (const entry of Object.entries(values)) {
        if (entry[0] === 'b') {
          continue;
        }
        entriesBreakSeen = true;
        break;
      }
    } finally {
      entriesBreakFinally = true;
    }
  }
  entriesBreakProbe();
  if (!entriesBreakSeen || !entriesBreakFinally) {
    throw new Error('unexpected Object.entries break/continue semantics');
  }

  let reflectReturnFinally = false;
  function reflectReturnProbe() {
    try {
      for (const key of Reflect.ownKeys(values)) {
        return key;
      }
      throw new Error('unexpected empty Reflect.ownKeys iteration');
    } finally {
      reflectReturnFinally = true;
    }
  }
  const reflectReturnValue = reflectReturnProbe();
  if (reflectReturnValue !== 'b' || !reflectReturnFinally) {
    throw new Error('unexpected Reflect.ownKeys return/finally semantics');
  }

  let reflectThrowFinally = false;
  function reflectThrowProbe() {
    try {
      for (const key of Reflect.ownKeys(values)) {
        if (key === 'b') {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Reflect.ownKeys iteration');
    } finally {
      reflectThrowFinally = true;
    }
  }
  let reflectThrew = false;
  try {
    reflectThrowProbe();
  } catch {
    reflectThrew = true;
  }
  if (!reflectThrew || !reflectThrowFinally) {
    throw new Error('unexpected Reflect.ownKeys throw/finally semantics');
  }

  const asyncValues = { "b": 1, "a": 2 };
  let asyncFinallySeen = false;
  let asyncThrew = false;
  try {
    for await (const key of Object.keys(asyncValues)) {
      if (key === 'b') {
        throw new Error('boom');
      }
    }
    throw new Error('unexpected empty async Object.keys iteration');
  } catch {
    asyncThrew = true;
  } finally {
    asyncFinallySeen = true;
  }
  if (!asyncThrew || !asyncFinallySeen) {
    throw new Error('unexpected async Object.keys throw/finally semantics');
  }

  let asyncValuesFinallySeen = false;
  let asyncValuesThrew = false;
  try {
    for await (const value of Object.values(asyncValues)) {
      if (value === 1) {
        throw new Error('boom');
      }
    }
    throw new Error('unexpected empty async Object.values iteration');
  } catch {
    asyncValuesThrew = true;
  } finally {
    asyncValuesFinallySeen = true;
  }
  if (!asyncValuesThrew || !asyncValuesFinallySeen) {
    throw new Error('unexpected async Object.values throw/finally semantics');
  }

  let asyncValuesBreakFinally = false;
  let asyncValuesBreakSeen = false;
  try {
    for await (const value of Object.values(asyncValues)) {
      if (value === 1) {
        continue;
      }
      asyncValuesBreakSeen = true;
      break;
    }
  } finally {
    asyncValuesBreakFinally = true;
  }
  if (!asyncValuesBreakSeen || !asyncValuesBreakFinally) {
    throw new Error('unexpected async Object.values break/continue semantics');
  }

  let asyncEntriesBreakFinally = false;
  let asyncEntriesBreakSeen = false;
  try {
    for await (const entry of Object.entries(asyncValues)) {
      if (entry[0] === 'b') {
        continue;
      }
      asyncEntriesBreakSeen = true;
      break;
    }
  } finally {
    asyncEntriesBreakFinally = true;
  }
  if (!asyncEntriesBreakSeen || !asyncEntriesBreakFinally) {
    throw new Error('unexpected async Object.entries break/continue semantics');
  }

  let asyncEntriesFinallySeen = false;
  let asyncEntriesThrew = false;
  try {
    for await (const entry of Object.entries(asyncValues)) {
      if (entry[0] === 'b') {
        throw new Error('boom');
      }
    }
    throw new Error('unexpected empty async Object.entries iteration');
  } catch {
    asyncEntriesThrew = true;
  } finally {
    asyncEntriesFinallySeen = true;
  }
  if (!asyncEntriesThrew || !asyncEntriesFinallySeen) {
    throw new Error('unexpected async Object.entries throw/finally semantics');
  }

  let asyncReflectReturnFinallySeen = false;
  function asyncReflectReturnProbe() {
    try {
      for await (const key of Reflect.ownKeys(asyncValues)) {
        return key;
      }
      throw new Error('unexpected empty async Reflect.ownKeys iteration');
    } finally {
      asyncReflectReturnFinallySeen = true;
    }
  }
  const asyncReflectReturnValue = asyncReflectReturnProbe();
  if (asyncReflectReturnValue !== 'b' || !asyncReflectReturnFinallySeen) {
    throw new Error('unexpected async Reflect.ownKeys return/finally semantics');
  }

  let asyncReflectThrowFinallySeen = false;
  let asyncReflectThrew = false;
  try {
    for await (const key of Reflect.ownKeys(asyncValues)) {
      if (key === 'b') {
        throw new Error('boom');
      }
    }
    throw new Error('unexpected empty async Reflect.ownKeys iteration');
  } catch {
    asyncReflectThrew = true;
  } finally {
    asyncReflectThrowFinallySeen = true;
  }
  if (!asyncReflectThrew || !asyncReflectThrowFinallySeen) {
    throw new Error('unexpected async Reflect.ownKeys throw/finally semantics');
  }
}

assertSyncFinalization();
console.log('browser object enumeration finalization ok');
"""
"smoke.test.js" = """function assertSyncFinalization() {
  const values = { "b": 1, "a": 2 };
  let returnFinally = false;
  function returnProbe() {
    try {
      for (const key of Object.keys(values)) {
        return key;
      }
      throw new Error('unexpected empty Object.keys iteration');
    } finally {
      returnFinally = true;
    }
  }
  const returnValue = returnProbe();
  if (returnValue !== 'b' || !returnFinally) {
    throw new Error('unexpected Object.keys return/finally semantics');
  }

  let throwFinally = false;
  function throwProbe() {
    try {
      for (const entry of Object.entries(values)) {
        if (entry[0] === 'b') {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Object.entries iteration');
    } finally {
      throwFinally = true;
    }
  }
  let threw = false;
  try {
    throwProbe();
  } catch {
    threw = true;
  }
  if (!threw || !throwFinally) {
    throw new Error('unexpected Object.entries throw/finally semantics');
  }

  let reflectReturnFinally = false;
  function reflectReturnProbe() {
    try {
      for (const key of Reflect.ownKeys(values)) {
        return key;
      }
      throw new Error('unexpected empty Reflect.ownKeys iteration');
    } finally {
      reflectReturnFinally = true;
    }
  }
  const reflectReturnValue = reflectReturnProbe();
  if (reflectReturnValue !== 'b' || !reflectReturnFinally) {
    throw new Error('unexpected Reflect.ownKeys return/finally semantics');
  }

  let reflectThrowFinally = false;
  function reflectThrowProbe() {
    try {
      for (const key of Reflect.ownKeys(values)) {
        if (key === 'b') {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Reflect.ownKeys iteration');
    } finally {
      reflectThrowFinally = true;
    }
  }
  let reflectThrew = false;
  try {
    reflectThrowProbe();
  } catch {
    reflectThrew = true;
  }
  if (!reflectThrew || !reflectThrowFinally) {
    throw new Error('unexpected Reflect.ownKeys throw/finally semantics');
  }
}

Kali.test('object enumeration finalization', () => {
  assertSyncFinalization();
});
"""
"smoke.test.ts" = """function assertSyncFinalization() {
  const values = { "b": 1, "a": 2 };
  let returnFinally = false;
  function returnProbe() {
    try {
      for (const key of Object.keys(values)) {
        return key;
      }
      throw new Error('unexpected empty Object.keys iteration');
    } finally {
      returnFinally = true;
    }
  }
  const returnValue = returnProbe();
  if (returnValue !== 'b' || !returnFinally) {
    throw new Error('unexpected Object.keys return/finally semantics');
  }

  let throwFinally = false;
  function throwProbe() {
    try {
      for (const entry of Object.entries(values)) {
        if (entry[0] === 'b') {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Object.entries iteration');
    } finally {
      throwFinally = true;
    }
  }
  let threw = false;
  try {
    throwProbe();
  } catch {
    threw = true;
  }
  if (!threw || !throwFinally) {
    throw new Error('unexpected Object.entries throw/finally semantics');
  }

  let reflectReturnFinally = false;
  function reflectReturnProbe() {
    try {
      for (const key of Reflect.ownKeys(values)) {
        return key;
      }
      throw new Error('unexpected empty Reflect.ownKeys iteration');
    } finally {
      reflectReturnFinally = true;
    }
  }
  const reflectReturnValue = reflectReturnProbe();
  if (reflectReturnValue !== 'b' || !reflectReturnFinally) {
    throw new Error('unexpected Reflect.ownKeys return/finally semantics');
  }

  let reflectThrowFinally = false;
  function reflectThrowProbe() {
    try {
      for (const key of Reflect.ownKeys(values)) {
        if (key === 'b') {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Reflect.ownKeys iteration');
    } finally {
      reflectThrowFinally = true;
    }
  }
  let reflectThrew = false;
  try {
    reflectThrowProbe();
  } catch {
    reflectThrew = true;
  }
  if (!reflectThrew || !reflectThrowFinally) {
    throw new Error('unexpected Reflect.ownKeys throw/finally semantics');
  }
}

Kali.test('object enumeration finalization', () => {
  assertSyncFinalization();
});
"""
"smoke.test.jsx" = """function assertSyncFinalization() {
  const values = { "b": 1, "a": 2 };
  let returnFinally = false;
  function returnProbe() {
    try {
      for (const key of Object.keys(values)) {
        return key;
      }
      throw new Error('unexpected empty Object.keys iteration');
    } finally {
      returnFinally = true;
    }
  }
  const returnValue = returnProbe();
  if (returnValue !== 'b' || !returnFinally) {
    throw new Error('unexpected Object.keys return/finally semantics');
  }

  let throwFinally = false;
  function throwProbe() {
    try {
      for (const entry of Object.entries(values)) {
        if (entry[0] === 'b') {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Object.entries iteration');
    } finally {
      throwFinally = true;
    }
  }
  let threw = false;
  try {
    throwProbe();
  } catch {
    threw = true;
  }
  if (!threw || !throwFinally) {
    throw new Error('unexpected Object.entries throw/finally semantics');
  }

  let reflectReturnFinally = false;
  function reflectReturnProbe() {
    try {
      for (const key of Reflect.ownKeys(values)) {
        return key;
      }
      throw new Error('unexpected empty Reflect.ownKeys iteration');
    } finally {
      reflectReturnFinally = true;
    }
  }
  const reflectReturnValue = reflectReturnProbe();
  if (reflectReturnValue !== 'b' || !reflectReturnFinally) {
    throw new Error('unexpected Reflect.ownKeys return/finally semantics');
  }

  let reflectThrowFinally = false;
  function reflectThrowProbe() {
    try {
      for (const key of Reflect.ownKeys(values)) {
        if (key === 'b') {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Reflect.ownKeys iteration');
    } finally {
      reflectThrowFinally = true;
    }
  }
  let reflectThrew = false;
  try {
    reflectThrowProbe();
  } catch {
    reflectThrew = true;
  }
  if (!reflectThrew || !reflectThrowFinally) {
    throw new Error('unexpected Reflect.ownKeys throw/finally semantics');
  }
}

Kali.test('object enumeration finalization', () => {
  assertSyncFinalization();
});
"""
"smoke.test.tsx" = """function assertSyncFinalization() {
  const values = { "b": 1, "a": 2 };
  let returnFinally = false;
  function returnProbe() {
    try {
      for (const key of Object.keys(values)) {
        return key;
      }
      throw new Error('unexpected empty Object.keys iteration');
    } finally {
      returnFinally = true;
    }
  }
  const returnValue = returnProbe();
  if (returnValue !== 'b' || !returnFinally) {
    throw new Error('unexpected Object.keys return/finally semantics');
  }

  let throwFinally = false;
  function throwProbe() {
    try {
      for (const entry of Object.entries(values)) {
        if (entry[0] === 'b') {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Object.entries iteration');
    } finally {
      throwFinally = true;
    }
  }
  let threw = false;
  try {
    throwProbe();
  } catch {
    threw = true;
  }
  if (!threw || !throwFinally) {
    throw new Error('unexpected Object.entries throw/finally semantics');
  }

  let reflectReturnFinally = false;
  function reflectReturnProbe() {
    try {
      for (const key of Reflect.ownKeys(values)) {
        return key;
      }
      throw new Error('unexpected empty Reflect.ownKeys iteration');
    } finally {
      reflectReturnFinally = true;
    }
  }
  const reflectReturnValue = reflectReturnProbe();
  if (reflectReturnValue !== 'b' || !reflectReturnFinally) {
    throw new Error('unexpected Reflect.ownKeys return/finally semantics');
  }

  let reflectThrowFinally = false;
  function reflectThrowProbe() {
    try {
      for (const key of Reflect.ownKeys(values)) {
        if (key === 'b') {
          throw new Error('boom');
        }
      }
      throw new Error('unexpected empty Reflect.ownKeys iteration');
    } finally {
      reflectThrowFinally = true;
    }
  }
  let reflectThrew = false;
  try {
    reflectThrowProbe();
  } catch {
    reflectThrew = true;
  }
  if (!reflectThrew || !reflectThrowFinally) {
    throw new Error('unexpected Reflect.ownKeys throw/finally semantics');
  }
}

Kali.test('object enumeration finalization', () => {
  assertSyncFinalization();
});
"""

[[case]]
name = "run_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_js_input"
rationale = """Migrated from browser_object_enumeration_finalization_harness.rs, `run_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_js_input` -- one source `#[test]` fn, one case (no `[matrix]` in this file; see the header). `assert_browser_object_enumeration_finalization` runs `kali run --api browser` with the browser harness backed by node and both concurrency limits pinned to 0, against a program that probes return/throw/break/continue finalization through Object.keys, Object.values, Object.entries and Reflect.ownKeys, synchronously and under `for await`, and asserts the command fails closed. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. MIGRATION NOTE (controller ruling 8): the source fn name says "supports", but the body asserts the command FAILS. The name is carried unchanged; see this file's header for why. The whole claim is that the process fails, so the migrated step carries `exit = "failure"` and nothing else."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.js"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "failure"

[[case]]
name = "run_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_ts_input"
rationale = """Migrated from browser_object_enumeration_finalization_harness.rs, `run_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_ts_input` -- one source `#[test]` fn, one case (no `[matrix]` in this file; see the header). `assert_browser_object_enumeration_finalization` runs `kali run --api browser` with the browser harness backed by node and both concurrency limits pinned to 0, against a program that probes return/throw/break/continue finalization through Object.keys, Object.values, Object.entries and Reflect.ownKeys, synchronously and under `for await`, and asserts the command fails closed. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. MIGRATION NOTE (controller ruling 8): the source fn name says "supports", but the body asserts the command FAILS. The name is carried unchanged; see this file's header for why. The whole claim is that the process fails, so the migrated step carries `exit = "failure"` and nothing else."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.ts"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "failure"

[[case]]
name = "run_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_jsx_input"
rationale = """Migrated from browser_object_enumeration_finalization_harness.rs, `run_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_jsx_input` -- one source `#[test]` fn, one case (no `[matrix]` in this file; see the header). `assert_browser_object_enumeration_finalization` runs `kali run --api browser` with the browser harness backed by node and both concurrency limits pinned to 0, against a program that probes return/throw/break/continue finalization through Object.keys, Object.values, Object.entries and Reflect.ownKeys, synchronously and under `for await`, and asserts the command fails closed. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. MIGRATION NOTE (controller ruling 8): the source fn name says "supports", but the body asserts the command FAILS. The name is carried unchanged; see this file's header for why. The whole claim is that the process fails, so the migrated step carries `exit = "failure"` and nothing else."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.jsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "failure"

[[case]]
name = "run_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_tsx_input"
rationale = """Migrated from browser_object_enumeration_finalization_harness.rs, `run_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_tsx_input` -- one source `#[test]` fn, one case (no `[matrix]` in this file; see the header). `assert_browser_object_enumeration_finalization` runs `kali run --api browser` with the browser harness backed by node and both concurrency limits pinned to 0, against a program that probes return/throw/break/continue finalization through Object.keys, Object.values, Object.entries and Reflect.ownKeys, synchronously and under `for await`, and asserts the command fails closed. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. MIGRATION NOTE (controller ruling 8): the source fn name says "supports", but the body asserts the command FAILS. The name is carried unchanged; see this file's header for why. The whole claim is that the process fails, so the migrated step carries `exit = "failure"` and nothing else."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.tsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "failure"

[[case]]
name = "test_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_js_input"
rationale = """Migrated from browser_object_enumeration_finalization_harness.rs, `test_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_js_input` -- one source `#[test]` fn, one case (no `[matrix]` in this file; see the header). `assert_browser_object_enumeration_finalization` runs `kali test --api browser` with the browser harness backed by node and both concurrency limits pinned to 0, against a program that probes return/throw/break/continue finalization through Object.keys, Object.values, Object.entries and Reflect.ownKeys, synchronously and under `for await`, and asserts the command fails closed. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. MIGRATION NOTE (controller ruling 8): the source fn name says "supports", but the body asserts the command FAILS. The name is carried unchanged; see this file's header for why. The whole claim is that the process fails, so the migrated step carries `exit = "failure"` and nothing else."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.js"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "failure"

[[case]]
name = "test_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_ts_input"
rationale = """Migrated from browser_object_enumeration_finalization_harness.rs, `test_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_ts_input` -- one source `#[test]` fn, one case (no `[matrix]` in this file; see the header). `assert_browser_object_enumeration_finalization` runs `kali test --api browser` with the browser harness backed by node and both concurrency limits pinned to 0, against a program that probes return/throw/break/continue finalization through Object.keys, Object.values, Object.entries and Reflect.ownKeys, synchronously and under `for await`, and asserts the command fails closed. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. MIGRATION NOTE (controller ruling 8): the source fn name says "supports", but the body asserts the command FAILS. The name is carried unchanged; see this file's header for why. The whole claim is that the process fails, so the migrated step carries `exit = "failure"` and nothing else."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.ts"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "failure"

[[case]]
name = "test_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_jsx_input"
rationale = """Migrated from browser_object_enumeration_finalization_harness.rs, `test_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_jsx_input` -- one source `#[test]` fn, one case (no `[matrix]` in this file; see the header). `assert_browser_object_enumeration_finalization` runs `kali test --api browser` with the browser harness backed by node and both concurrency limits pinned to 0, against a program that probes return/throw/break/continue finalization through Object.keys, Object.values, Object.entries and Reflect.ownKeys, synchronously and under `for await`, and asserts the command fails closed. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. MIGRATION NOTE (controller ruling 8): the source fn name says "supports", but the body asserts the command FAILS. The name is carried unchanged; see this file's header for why. The whole claim is that the process fails, so the migrated step carries `exit = "failure"` and nothing else."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.jsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "failure"

[[case]]
name = "test_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_tsx_input"
rationale = """Migrated from browser_object_enumeration_finalization_harness.rs, `test_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_tsx_input` -- one source `#[test]` fn, one case (no `[matrix]` in this file; see the header). `assert_browser_object_enumeration_finalization` runs `kali test --api browser` with the browser harness backed by node and both concurrency limits pinned to 0, against a program that probes return/throw/break/continue finalization through Object.keys, Object.values, Object.entries and Reflect.ownKeys, synchronously and under `for await`, and asserts the command fails closed. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. MIGRATION NOTE (controller ruling 8): the source fn name says "supports", but the body asserts the command FAILS. The name is carried unchanged; see this file's header for why. The whole claim is that the process fails, so the migrated step carries `exit = "failure"` and nothing else."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.tsx"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "failure"

[[case]]
name = "json_run_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_js_input"
rationale = """Migrated from browser_object_enumeration_finalization_harness.rs, `json_run_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_js_input` -- one source `#[test]` fn, one case (no `[matrix]` in this file; see the header). `assert_browser_object_enumeration_finalization` runs `kali run --api browser` with the browser harness backed by node and both concurrency limits pinned to 0, against a program that probes return/throw/break/continue finalization through Object.keys, Object.values, Object.entries and Reflect.ownKeys, synchronously and under `for await`, and asserts the command fails closed. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. MIGRATION NOTE (controller ruling 8): the source fn name says "supports", but the body asserts the command FAILS. The name is carried unchanged; see this file's header for why. The whole claim is that the process fails, so the migrated step carries `exit = "failure"` and nothing else."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.js"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "failure"

[[case]]
name = "json_test_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_js_input"
rationale = """Migrated from browser_object_enumeration_finalization_harness.rs, `json_test_supports_object_enumeration_finalization_when_browser_harness_is_configured_in_js_input` -- one source `#[test]` fn, one case (no `[matrix]` in this file; see the header). `assert_browser_object_enumeration_finalization` runs `kali test --api browser` with the browser harness backed by node and both concurrency limits pinned to 0, against a program that probes return/throw/break/continue finalization through Object.keys, Object.values, Object.entries and Reflect.ownKeys, synchronously and under `for await`, and asserts the command fails closed. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. MIGRATION NOTE (controller ruling 8): the source fn name says "supports", but the body asserts the command FAILS. The name is carried unchanged; see this file's header for why. The whole claim is that the process fails, so the migrated step carries `exit = "failure"` and nothing else."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.js"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "failure"
