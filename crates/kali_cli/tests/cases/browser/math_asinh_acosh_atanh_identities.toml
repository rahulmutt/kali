# Migrated from tests/browser_math_asinh_acosh_atanh_identities.rs.
#
# HISTORY -- this file is a RESTORED RETENTION. Batch 3 retained the whole .rs
# under a controller ruling that `.matches(...).count()` was a design-spec 5.11
# outlier and that no assertion key would be added for it, and deleted the case
# file it had shipped in `50061950a4`. The human partner REVERSED that ruling
# during the batch 4 interlude: `stdout_count`/`json_count` were added to the
# format (design spec 5.4, now twelve keys) specifically because this file's 24
# tests, and 12 other files like it, would otherwise have been retained
# hand-written against a 5.11 budget of ~8 targets for the entire crate. This
# case file is written FROM SCRATCH, not recovered from git: the deleted one
# predates the keys and encoded `count() >= 3` as a contiguous `"0\n0\n0\n"`
# substring, which asserts ADJACENCY the source never claimed (rule 2). The
# retention `//!` header is deleted from the .rs in the same commit -- retention
# prose in a fully migrated file is worse than none.
#
# RULE 12 (carry every source comment): `browser_math_asinh_acosh_atanh_identities.rs` contains no Rust
# comments -- the only `//` in the file is the `// kali-tree-shake:` marker
# inside a JS fixture body, which is program text and is carried verbatim into
# [source]. Nothing to move into `rationale`; comment_coverage.py is run with
# --allow-empty for this pair.
# (The deleted `//!` header was migration bookkeeping about the retention, not
# prose about the behaviour under test; it is superseded by this file's
# existence and is preserved in git history and in the batch 3/4 reports.)
#
# RULE 7 / U1 -- MATRIX ARITHMETIC, closes exactly. Enumerated mechanically with
# tools/task-18-browser-pilot/enumerate_invocations.py: 24 #[test] fns, 24
# invocations, no loops.
#   * `assert_browser_bundle_math_inverse_hyperbolic(filename, json_output)`
#     -- 8 = ext(js/ts/jsx/tsx) x json_output(false/true), full cross product.
#   * `assert_browser_harness_math_inverse_hyperbolic(command, filename,
#     source, json_output)` -- 16 = command(run/test) x ext(4) x
#     json_output(false/true), full cross product.
# `ext` is the one axis both helpers vary over uniformly and completely, so:
# 6 [[case]] x ext(4) = 24 trials = 24 #[test] fns. Per rule 6 the fold is
# stated here -- each [[case]] corresponds to 4 source fns, one per cell, and
# the assertion mapping stays 1:1 per trial. `command` and `json_output` are NOT
# axes: each changes the assertion SHAPE (json envelope vs text stdout;
# `exitCode` for run vs `total`/`passed`/`failed` for test), which design spec
# 5.6's closing note excludes from a matrix.
#
# U2 -- [source] is file-wide and that is safe here: all three fixtures are
# written unconditionally into a fresh temp dir, none is behind an `if`, and no
# case's point is a file's presence or absence. Each command names its entry on
# argv.
#
# THE COUNT CLAIM. Three sites, all the same bound: `:202` (bundle harness raw
# stdout), `:253` (`json["stdout"].as_str()`, inside `if json_output`), `:257`
# (raw stdout, else branch). Carried as `stdout_count` on the two raw surfaces
# and `json_count` on the JSON leaf. Live output is `0\n0\n0\n` -- exactly
# three occurrences, since Math.asinh(0), Math.acosh(1) and Math.atanh(0) all
# print 0 -- so the claim sits exactly on its `>= 3` boundary. It is still
# written `at_least = 3` and not `exact = 3`: the source states a lower bound,
# and pinning equality would assert something it never did (rule 2).
#
# RULE 13 -- transitive helper docs. Every fn in each call chain was checked for
# a `///` doc comment; none of this file's own helpers carries one. The chain
# reaches `kali_runtime_contract::browser_bundle_harness_script` and
# `::browser_harness_command_parts_for`, which do carry one-line `///` docs, but
# in the migrated form this case file never calls them -- the
# `browser_bundle_harness` step kind means the case RUNNER does
# (crates/kali_case_runner/src/steps.rs:70), so those docs describe shared runner
# infrastructure (design spec 5.3), not what this case claims. All 45 case files
# shipped before batch 4 do the same (measured: 0 of 45 carry either string).
# Raised in the batch 4 report as a standing question rather than decided here.
#
# ASSERTION SHAPE, mirrored and nothing more. This source asserts NO `errors`
# array anywhere -- neither on the build envelope (:150-159) nor on the harness
# envelope (:237-254) -- so no `errors = []` is written, unlike several siblings
# in this batch that do assert it. The harness json branch DOES assert
# `json["stderr"] == ""` (:254). The bundle harness step carries only the count
# claim (:202); there is no accompanying `.contains`. The source passes no
# `--max-threads`/`--max-spawned-processes`, so neither appears on argv.

[matrix]
ext = ["js", "ts", "jsx", "tsx"]

[source]
"app.${ext}" = """// kali-tree-shake: mathInverseHyperbolicIdentities
function mathInverseHyperbolicIdentities() {
  const zero = 0;
  const one = 1;
  console.log(Math.asinh(zero));
  console.log(Math.acosh(one));
  console.log(Math.atanh(zero));
}
"""
"main.${ext}" = """const zero = 0; const one = 1; console.log(Math.asinh(zero)); console.log(Math.acosh(one)); console.log(Math.atanh(zero));
"""
"smoke.test.${ext}" = """Kali.test('math inverse hyperbolic identities', () => {
  const zero = 0;
  const one = 1;
  console.log(Math.asinh(zero));
  console.log(Math.acosh(one));
  console.log(Math.atanh(zero));
});
"""

[[case]]
name = "build_emits_math_inverse_hyperbolic_identity_literals"
rationale = """Migrated from browser_math_asinh_acosh_atanh_identities.rs. `assert_browser_bundle_math_inverse_hyperbolic` builds a browser bundle (`kali build --bundle --api browser`), asserts the emitted `app/app.meta.json` metadata, then runs the bundle glue under the browser-bundle-harness contract. The bundled program calls Math.asinh(0), Math.acosh(1) and Math.atanh(0), each of which prints `0`. The source spells this claim as `.matches('0\\n').count() >= 3`, which the case format carries directly as `stdout_count` with `at_least = 3`. It is not weakened to a `*_contains` (one occurrence would satisfy that) and not strengthened to `exact` (the source states a lower bound, not an equality), per controller ruling 3 -- mirror the source. This file is the reason the count keys exist: batch 3 retained all 24 of its tests hand-written because the claim had no expressible form, and the human partner reversed that by adding the keys."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "app.${ext}"]
exit = "success"

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.mathInverseHyperbolicIdentities();
"""
exit = "success"
stdout_count = [{ needle = "0\n", at_least = 3 }]

[[case]]
name = "json_build_emits_math_inverse_hyperbolic_identity_literals"
rationale = """Migrated from browser_math_asinh_acosh_atanh_identities.rs. `assert_browser_bundle_math_inverse_hyperbolic` builds a browser bundle (`kali build --bundle --api browser`), asserts the emitted `app/app.meta.json` metadata, then runs the bundle glue under the browser-bundle-harness contract. The bundled program calls Math.asinh(0), Math.acosh(1) and Math.atanh(0), each of which prints `0`. The source spells this claim as `.matches('0\\n').count() >= 3`, which the case format carries directly as `stdout_count` with `at_least = 3`. It is not weakened to a `*_contains` (one occurrence would satisfy that) and not strengthened to `exact` (the source states a lower bound, not an equality), per controller ruling 3 -- mirror the source. This file is the reason the count keys exist: batch 3 retained all 24 of its tests hand-written because the claim had no expressible form, and the human partner reversed that by adding the keys. This sibling asserts the JSON build envelope (schemaVersion/command/success/exitCode and payload artifactKind/bundleFormat) instead of plain text. The source makes no `errors` claim on this envelope, so none is written."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "--output", "json", "app.${ext}"]
exit = "success"
json = { schemaVersion = 1, command = "build", success = true, exitCode = 0, payload = { artifactKind = "bundle", bundleFormat = "esm" } }

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.mathInverseHyperbolicIdentities();
"""
exit = "success"
stdout_count = [{ needle = "0\n", at_least = 3 }]

[[case]]
name = "run_supports_math_inverse_hyperbolic_identity_literals_when_browser_harness_is_configured"
rationale = """Migrated from browser_math_asinh_acosh_atanh_identities.rs. `assert_browser_harness_math_inverse_hyperbolic` runs `kali run --api browser` with the browser harness backed by `node`, against a program calling Math.asinh(0), Math.acosh(1) and Math.atanh(0) -- each printing `0`. The source spells this claim as `.matches('0\\n').count() >= 3`, which the case format carries directly as `stdout_count` with `at_least = 3`. It is not weakened to a `*_contains` (one occurrence would satisfy that) and not strengthened to `exact` (the source states a lower bound, not an equality), per controller ruling 3 -- mirror the source."""
args = ["run", "--api", "browser", "main.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_count = [{ needle = "0\n", at_least = 3 }]

[[case]]
name = "test_supports_math_inverse_hyperbolic_identity_literals_when_browser_harness_is_configured"
rationale = """Migrated from browser_math_asinh_acosh_atanh_identities.rs. `assert_browser_harness_math_inverse_hyperbolic` runs `kali test --api browser` with the browser harness backed by `node`, against a program calling Math.asinh(0), Math.acosh(1) and Math.atanh(0) -- each printing `0`. The source spells this claim as `.matches('0\\n').count() >= 3`, which the case format carries directly as `stdout_count` with `at_least = 3`. It is not weakened to a `*_contains` (one occurrence would satisfy that) and not strengthened to `exact` (the source states a lower bound, not an equality), per controller ruling 3 -- mirror the source."""
args = ["test", "--api", "browser", "smoke.test.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_count = [{ needle = "0\n", at_least = 3 }]

[[case]]
name = "json_run_supports_math_inverse_hyperbolic_identity_literals_when_browser_harness_is_configured"
rationale = """Migrated from browser_math_asinh_acosh_atanh_identities.rs. `assert_browser_harness_math_inverse_hyperbolic` runs `kali run --output json --api browser` with the browser harness backed by `node`, against a program calling Math.asinh(0), Math.acosh(1) and Math.atanh(0) -- each printing `0`. The same count claim is taken here against the JSON string leaf `json["stdout"]` rather than raw stdout (:253), which is why `json_count` exists alongside `stdout_count`. The source spells this claim as `.matches('0\\n').count() >= 3`, which the case format carries directly as `json_count` with `at_least = 3`. It is not weakened to a `*_contains` (one occurrence would satisfy that) and not strengthened to `exact` (the source states a lower bound, not an equality), per controller ruling 3 -- mirror the source. No equality pin is written for `json.stdout`: the source asserts only how many times `0\\n` occurs in it. `stderr` is asserted exactly empty (:254); the source makes no `errors` claim on this envelope, so none is written."""
args = ["--output", "json", "run", "--api", "browser", "main.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "run", success = true, exitCode = 0, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", exitCode = 0 }, stderr = "" }
json_count = [{ path = "stdout", needle = "0\n", at_least = 3 }]

[[case]]
name = "json_test_supports_math_inverse_hyperbolic_identity_literals_when_browser_harness_is_configured"
rationale = """Migrated from browser_math_asinh_acosh_atanh_identities.rs. `assert_browser_harness_math_inverse_hyperbolic` runs `kali test --output json --api browser` with the browser harness backed by `node`, against a program calling Math.asinh(0), Math.acosh(1) and Math.atanh(0) -- each printing `0`. The same count claim is taken here against the JSON string leaf `json["stdout"]` rather than raw stdout (:253), which is why `json_count` exists alongside `stdout_count`. The source spells this claim as `.matches('0\\n').count() >= 3`, which the case format carries directly as `json_count` with `at_least = 3`. It is not weakened to a `*_contains` (one occurrence would satisfy that) and not strengthened to `exact` (the source states a lower bound, not an equality), per controller ruling 3 -- mirror the source. No equality pin is written for `json.stdout`: the source asserts only how many times `0\\n` occurs in it. `stderr` is asserted exactly empty (:254); the source makes no `errors` claim on this envelope, so none is written."""
args = ["--output", "json", "test", "--api", "browser", "smoke.test.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "test", success = true, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", total = 1, passed = 1, failed = 0 }, stderr = "" }
json_count = [{ path = "stdout", needle = "0\n", at_least = 3 }]
