# Migrated from tests/browser_runtime_spawned_process_budget_js_input.rs -- the explicit-api half.
#   SOURCE REF: 28df9ba02962143a1fc1735e418b2f67caee8fc6
#
# RULE 12 / U6 -- SOURCE COMMENT PROSE. tests/browser_runtime_spawned_process_budget_js_input.rs carries NO
# Rust comment block at all -- neither a bare `//` block nor a `///` doc comment on
# any helper (the generator counts both and raises if either count changes), so
# there is no prose to move verbatim into a `rationale` here.
#
# RULE 7 / U1 -- MATRIX ARITHMETIC, and it closes exactly.
# 4 `#[test]` fns, 48 real helper invocations with every loop
# expanded:
#   * `assert_browser_requested_rejects_positive_spawned_process_budget` -- 16 invocation(s) = every real call of it in this half, with every `for` loop expanded
#   * `assert_explicit_browser_api_surface_accepts_zero_spawned_process_budget` -- 16 invocation(s) = every real call of it in this half, with every `for` loop expanded
#   * `assert_explicit_browser_api_surface_rejects_positive_spawned_process_budget` -- 16 invocation(s) = every real call of it in this half, with every `for` loop expanded
# `ext` is the ONE axis every case varies over uniformly, and every helper covers
# all 4 of its values, so a file-wide axis fans nothing the source never ran:
# 12 `[[case]]` entries x ext(4) = 48 trials = 48 invocations. Exact.
# `command` and `json_output` are NOT matrix axes, per rule 7 and design spec 5.6's own note:
# each changes the ASSERTION SHAPE rather than substituting a string.
# `json_output` switches between a text claim and a JSON-envelope claim.
# `command` switches the envelope's payload between `exitCode` (run) and
# `total`/`passed`/`failed` (test).
# Each is written as sibling `[[case]]` entries instead.
#
# RULE 6 -- 1:1 MAPPING, and the fold is stated here as the rule requires.
# Each `[[case]]` below corresponds to one `(command, json_output)` cell of the source's fns, fanned to the four extensions; the assertion mapping stays
# 1:1 per trial, so no claim is lost, but a failing trial's id reads its matrix cell
# rather than a source fn name.
#
# U2 -- `[source]` is FILE-WIDE, WHICH IS WHY THIS TARGET IS TWO CASE FILES.
# The source writes `kali.json` behind an `if with_browser_api_surface_manifest`.
# `expand.rs` clones the whole file-level `[source]` map into EVERY trial and a
# `[[case]]` cannot opt out of a fixture a sibling case needs, so one shared file would
# make the manifest unconditionally present. That is invisible to
# `audit-case-migration.py` (no literal is dropped) and invisible to `cargo test` (the
# trial still passes), so it is measured instead:
#   $ gen_batch8b.py --derive
#     with the flag under test present and NO manifest (what the source runs): the
#       case's pins hold;
#     with the manifest present and the flag REMOVED: the case's pins STILL hold.
#   So in a shared file the flag would be unverified -- the manifest would supply the
#   claim, no literal would be dropped (so the audit cannot see it) and the trial would
#   still pass (so `cargo test` cannot either).
# This file is the explicit-api half: 4 source `#[test]` fn(s) worth of
# invocations (48 of them). The other half is `runtime_spawned_process_budget_js_input_inherited_manifest`.
# The disarmed shape, stated so a reader can check the split is on the right line: the accepting cases pin `payload.hostContract = "browser-requested"`, which the manifest supplies on its own
# Every command below names its entry explicitly on argv, so the unused sibling
# fixtures in a trial dir are inert.
#
# RULE 13 -- transitive helper docs. This source's helper chain carries NO `///`
# doc comment on any helper any case reaches (checked mechanically by this file's
# generator, which raises if the count it accounts for is wrong), so there is none
# to carry. Controller ruling 6's runner-infrastructure exemption is separate and is
# not relied on here.
#
# ARGV ORDER. `args` reproduces the source's own argument order exactly, token for
# token, and the order is DERIVED from the source's `.arg(...)` chain by this file's
# generator rather than written down. The entry filename is last, as the source
# appends it last.
# `--output json` appears first in the json cases below, because that is where the source builds it.
#
# U5 -- `[source]` KEY RENAMES. `[source]` is one flat file-wide namespace, and this
# source writes two different program texts to the same filename in different tests, so
# the keys are variant-suffixed:
#   * `main.js` -> `main_run.js` -- the `run` leg writes its own program text
#   * `main.js` -> `main_test.js` -- the `test` leg writes its own program text
#   * `main.jsx` -> `main_run.jsx` -- the `run` leg writes its own program text
#   * `main.jsx` -> `main_test.jsx` -- the `test` leg writes its own program text
#   * `main.ts` -> `main_run.ts` -- the `run` leg writes its own program text
#   * `main.ts` -> `main_test.ts` -- the `test` leg writes its own program text
#   * `main.tsx` -> `main_run.tsx` -- the `run` leg writes its own program text
#   * `main.tsx` -> `main_test.tsx` -- the `test` leg writes its own program text
# U5's safety condition holds for every rename: each of these filenames is passed to
# `kali` as a CLI argument only. None is referenced by string from inside any fixture
# body -- no dynamic-import call and no CommonJS require call names any of them, checked
# against every `[source]` value in this file -- so renaming does not rewrite the program
# under test (rule 9).
#
# ASSERTION SHAPE. Every key below is derived from the source's own asserts, resolved
# for this case's `(command, json_output)` binding by this file's generator, which
# RAISES on a branch condition it cannot evaluate rather than letting both branches
# contribute. Text cases keep `stdout_contains`/`stderr_contains` (ruling 3: a plain
# `.contains` against a field that HAS a substring form is not strengthened). JSON
# cases pin the envelope paths the source asserts; where the source spells
# `.contains(...)` against a JSON string leaf, which has no substring form in this
# format, ruling 3 requires an exact pin, and the generator refuses to emit one that
# does not contain the source's own needle -- so the strengthening is verified.
# `assert!(!errors.is_empty())` is carried by the `errors.0.*` pins, which a missing
# path hard-fails on; the generator raises if a file asserts non-emptiness with no
# `errors.0` pin to carry it.
#
# RULING 7 -- DUPLICATE `[source]` BODIES ARE NOT HOISTED. Several entries below hold
# byte-identical program text. U13 would hoist them into `[constants]`; controller ruling 7
# declines that for `browser/` (a hoisted body makes `check_fixtures.py` go red on a correct
# file, and hoisting moves program text onto the surface `assertion_strings()` searches).
# The mandatory half of that ruling is honoured instead: the duplication is asserted
# MECHANICALLY in this file's generator, not eyeballed -- the generator compares the
# extracted fixture strings and raises if they are not byte-identical.
#
# No count of the wider `browser/` corpus appears anywhere in this file (ruling 16): a
# family-wide population count has no gateable home inside a case file, and the
# remaining batches would falsify it by construction.
#
# EXTRA-CLAIM DECLARATIONS (U14's `extra` direction).
# check_extra_claims.py compares this file's claim strings against the source's and fails
# on any that appear nowhere in the .rs. The entries below are the deliberate exceptions;
# a genuinely new one will not be on this list and will fail the gate.
# EXTRA-OK: 'selected resource budget(s) ["resources.maxSpawnedProcesses"] are unavailable in this phase' -- live-captured exact `json.errors.0.message` pin; the source asserts `.contains` on a JSON leaf, which has no substring form, so ruling 3 requires an exact pin captured from the real binary
# EXTRA-OK: 'browser spawned process budget ok\n' -- live-captured exact `json.stdout` pin; source asserts `.contains` on a JSON leaf, which has no substring form, so ruling 3 requires an exact pin captured from the real binary
# EXTRA-OK: 'main_run.js' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'main_test.js' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'main_run.jsx' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'main_test.jsx' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'main_run.ts' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'main_test.ts' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'main_run.tsx' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test
# EXTRA-OK: 'main_test.tsx' -- U5-renamed `[source]` entry filename; passed to `kali` on argv only and referenced by no fixture body (checked mechanically in this file's generator), so the rename cannot rewrite the program under test

[matrix]
ext = ["js", "ts", "jsx", "tsx"]

[source]
"main_run.${ext}" = """console.log('browser spawned process budget ok');
"""
"smoke.test.${ext}" = """Kali.test('browser spawned process budget', () => {
  console.log('browser spawned process budget ok');
});
"""
"main_test.${ext}" = """Kali.test('browser spawned process budget', () => {
  console.log('browser spawned process budget ok');
});
"""

[[case]]
name = "run_rejects_positive_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs__text"
rationale = """Migrated from browser_runtime_spawned_process_budget_js_input.rs. This `[[case]]` is one `(command, json_output)` cell of the source, matrix-fanned by `ext(4)`, so it stands for `run_rejects_positive_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` -- one trial per real invocation, with the assertion mapping staying one-to-one per trial (rule 6's sanctioned rule-7 fold, stated here as the rule requires). The program under test is a one-line `console.log` for `run` and a single `Kali.test` registration for `test` (rule 9: copied out of the source). It routes through `assert_browser_requested_rejects_positive_spawned_process_budget(` (:172); that helper loops `for json_output in [false, true]`, so each call site is two real invocations. The source spells this as a plain `.contains(...)` against raw stdout/stderr, so it is carried as stdout_contains/stderr_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. U2 -- this target is TWO case files split on `kali.json` presence; this is the explicit-api half and `runtime_spawned_process_budget_js_input_inherited_manifest` is the other. The split is measured, not assumed (see the file header). MIGRATION NOTE (controller ruling 8): the source fn `assert_browser_requested_rejects_positive_spawned_process_budget` has a name that misdescribes its own body -- its name says browser-requested (spelled without backticks here: U8's gate resolves every backticked lower-case identifier against this source's fn list, and that is a prose fragment of a fn name, not a fn), but unlike its accepting twin it writes NO `kali.json` and passes NO `--api browser`, so no browser API surface is requested at all; the E5506 it asserts is the spawned-process budget rejection, which fires on the default surface too. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did. MIGRATION NOTE (controller ruling 8): the source fn `run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` has a name that misdescribes its own body -- it hands `kali test` a `main.<ext>` rather than a `smoke.test.<ext>`, unlike every other `test` leg in this source; the argv is carried faithfully and the `[source]` key is U5-renamed because the two legs write different programs to that one filename. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did."""
args = ["run", "--max-spawned-processes", "1", "main_run.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = 5
stderr_contains = ["E5506", "resources.maxSpawnedProcesses", "--max-spawned-processes"]

[[case]]
name = "run_rejects_positive_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs__json"
rationale = """Migrated from browser_runtime_spawned_process_budget_js_input.rs. This `[[case]]` is one `(command, json_output)` cell of the source, matrix-fanned by `ext(4)`, so it stands for `run_rejects_positive_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` -- one trial per real invocation, with the assertion mapping staying one-to-one per trial (rule 6's sanctioned rule-7 fold, stated here as the rule requires). The program under test is a one-line `console.log` for `run` and a single `Kali.test` registration for `test` (rule 9: copied out of the source). It routes through `assert_browser_requested_rejects_positive_spawned_process_budget(` (:172); that helper loops `for json_output in [false, true]`, so each call site is two real invocations. On the JSON branch the source spells its claim as `.contains(...)` against the string leaf `json.errors.0.message`, which has NO substring form in this case format (the twelve keys of design spec 5.4 include no json-substring key), so per controller ruling 3 it becomes an exact pin -- and, per U9, only after the value was captured from the real `kali` binary rather than hand-computed. The generator refuses to emit a pin that does not contain the source's own needle, so the strengthening is verified rather than asserted. U2 -- this target is TWO case files split on `kali.json` presence; this is the explicit-api half and `runtime_spawned_process_budget_js_input_inherited_manifest` is the other. The split is measured, not assumed (see the file header). MIGRATION NOTE (controller ruling 8): the source fn `assert_browser_requested_rejects_positive_spawned_process_budget` has a name that misdescribes its own body -- its name says browser-requested (spelled without backticks here: U8's gate resolves every backticked lower-case identifier against this source's fn list, and that is a prose fragment of a fn name, not a fn), but unlike its accepting twin it writes NO `kali.json` and passes NO `--api browser`, so no browser API surface is requested at all; the E5506 it asserts is the spawned-process budget rejection, which fires on the default surface too. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did. MIGRATION NOTE (controller ruling 8): the source fn `run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` has a name that misdescribes its own body -- it hands `kali test` a `main.<ext>` rather than a `smoke.test.<ext>`, unlike every other `test` leg in this source; the argv is carried faithfully and the `[source]` key is U5-renamed because the two legs write different programs to that one filename. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did."""
args = ["--output", "json", "run", "--max-spawned-processes", "1", "main_run.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = 5
json.schemaVersion = 1
json.command = "run"
json.success = false
json.exitCode = 5
json.errors.0.code = "E5506"
json.errors.0.context.origin = "cli"
json.errors.0.context.flag = "--max-spawned-processes"
json.errors.0.context.requestedValue = "1"
json.errors.0.context.effectiveValue = "1"
json.errors.0.message = "selected resource budget(s) [\"resources.maxSpawnedProcesses\"] are unavailable in this phase"

[[case]]
name = "test_rejects_positive_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs__text"
rationale = """Migrated from browser_runtime_spawned_process_budget_js_input.rs. This `[[case]]` is one `(command, json_output)` cell of the source, matrix-fanned by `ext(4)`, so it stands for `test_rejects_positive_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` -- one trial per real invocation, with the assertion mapping staying one-to-one per trial (rule 6's sanctioned rule-7 fold, stated here as the rule requires). The program under test is a one-line `console.log` for `run` and a single `Kali.test` registration for `test` (rule 9: copied out of the source). It routes through `assert_browser_requested_rejects_positive_spawned_process_budget(` (:172); that helper loops `for json_output in [false, true]`, so each call site is two real invocations. The source spells this as a plain `.contains(...)` against raw stdout/stderr, so it is carried as stdout_contains/stderr_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. U2 -- this target is TWO case files split on `kali.json` presence; this is the explicit-api half and `runtime_spawned_process_budget_js_input_inherited_manifest` is the other. The split is measured, not assumed (see the file header). MIGRATION NOTE (controller ruling 8): the source fn `assert_browser_requested_rejects_positive_spawned_process_budget` has a name that misdescribes its own body -- its name says browser-requested (spelled without backticks here: U8's gate resolves every backticked lower-case identifier against this source's fn list, and that is a prose fragment of a fn name, not a fn), but unlike its accepting twin it writes NO `kali.json` and passes NO `--api browser`, so no browser API surface is requested at all; the E5506 it asserts is the spawned-process budget rejection, which fires on the default surface too. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did. MIGRATION NOTE (controller ruling 8): the source fn `run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` has a name that misdescribes its own body -- it hands `kali test` a `main.<ext>` rather than a `smoke.test.<ext>`, unlike every other `test` leg in this source; the argv is carried faithfully and the `[source]` key is U5-renamed because the two legs write different programs to that one filename. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did."""
args = ["test", "--max-spawned-processes", "1", "smoke.test.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = 5
stderr_contains = ["E5506", "resources.maxSpawnedProcesses", "--max-spawned-processes"]

[[case]]
name = "test_rejects_positive_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs__json"
rationale = """Migrated from browser_runtime_spawned_process_budget_js_input.rs. This `[[case]]` is one `(command, json_output)` cell of the source, matrix-fanned by `ext(4)`, so it stands for `test_rejects_positive_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` -- one trial per real invocation, with the assertion mapping staying one-to-one per trial (rule 6's sanctioned rule-7 fold, stated here as the rule requires). The program under test is a one-line `console.log` for `run` and a single `Kali.test` registration for `test` (rule 9: copied out of the source). It routes through `assert_browser_requested_rejects_positive_spawned_process_budget(` (:172); that helper loops `for json_output in [false, true]`, so each call site is two real invocations. On the JSON branch the source spells its claim as `.contains(...)` against the string leaf `json.errors.0.message`, which has NO substring form in this case format (the twelve keys of design spec 5.4 include no json-substring key), so per controller ruling 3 it becomes an exact pin -- and, per U9, only after the value was captured from the real `kali` binary rather than hand-computed. The generator refuses to emit a pin that does not contain the source's own needle, so the strengthening is verified rather than asserted. U2 -- this target is TWO case files split on `kali.json` presence; this is the explicit-api half and `runtime_spawned_process_budget_js_input_inherited_manifest` is the other. The split is measured, not assumed (see the file header). MIGRATION NOTE (controller ruling 8): the source fn `assert_browser_requested_rejects_positive_spawned_process_budget` has a name that misdescribes its own body -- its name says browser-requested (spelled without backticks here: U8's gate resolves every backticked lower-case identifier against this source's fn list, and that is a prose fragment of a fn name, not a fn), but unlike its accepting twin it writes NO `kali.json` and passes NO `--api browser`, so no browser API surface is requested at all; the E5506 it asserts is the spawned-process budget rejection, which fires on the default surface too. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did. MIGRATION NOTE (controller ruling 8): the source fn `run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` has a name that misdescribes its own body -- it hands `kali test` a `main.<ext>` rather than a `smoke.test.<ext>`, unlike every other `test` leg in this source; the argv is carried faithfully and the `[source]` key is U5-renamed because the two legs write different programs to that one filename. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did."""
args = ["--output", "json", "test", "--max-spawned-processes", "1", "smoke.test.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = 5
json.schemaVersion = 1
json.command = "test"
json.success = false
json.exitCode = 5
json.errors.0.code = "E5506"
json.errors.0.context.origin = "cli"
json.errors.0.context.flag = "--max-spawned-processes"
json.errors.0.context.requestedValue = "1"
json.errors.0.context.effectiveValue = "1"
json.errors.0.message = "selected resource budget(s) [\"resources.maxSpawnedProcesses\"] are unavailable in this phase"

[[case]]
name = "run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs__run__text"
rationale = """Migrated from browser_runtime_spawned_process_budget_js_input.rs. This `[[case]]` is one `(command, json_output)` cell of the source, matrix-fanned by `ext(4)`, so it stands for `run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` -- one trial per real invocation, with the assertion mapping staying one-to-one per trial (rule 6's sanctioned rule-7 fold, stated here as the rule requires). The program under test is a one-line `console.log` for `run` and a single `Kali.test` registration for `test` (rule 9: copied out of the source). It routes through `assert_explicit_browser_api_surface_accepts_zero_spawned_process_budget(` (:100); that helper loops `for json_output in [false, true]`, so each call site is two real invocations. The source spells this as a plain `.contains(...)` against raw stdout/stderr, so it is carried as stdout_contains/stderr_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. U2 -- this target is TWO case files split on `kali.json` presence; this is the explicit-api half and `runtime_spawned_process_budget_js_input_inherited_manifest` is the other. The split is measured, not assumed (see the file header). MIGRATION NOTE (controller ruling 8): the source fn `assert_browser_requested_rejects_positive_spawned_process_budget` has a name that misdescribes its own body -- its name says browser-requested (spelled without backticks here: U8's gate resolves every backticked lower-case identifier against this source's fn list, and that is a prose fragment of a fn name, not a fn), but unlike its accepting twin it writes NO `kali.json` and passes NO `--api browser`, so no browser API surface is requested at all; the E5506 it asserts is the spawned-process budget rejection, which fires on the default surface too. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did. MIGRATION NOTE (controller ruling 8): the source fn `run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` has a name that misdescribes its own body -- it hands `kali test` a `main.<ext>` rather than a `smoke.test.<ext>`, unlike every other `test` leg in this source; the argv is carried faithfully and the `[source]` key is U5-renamed because the two legs write different programs to that one filename. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did."""
args = ["run", "--api", "browser", "--max-spawned-processes", "0", "main_run.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["browser spawned process budget ok"]

[[case]]
name = "run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs__run__json"
rationale = """Migrated from browser_runtime_spawned_process_budget_js_input.rs. This `[[case]]` is one `(command, json_output)` cell of the source, matrix-fanned by `ext(4)`, so it stands for `run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` -- one trial per real invocation, with the assertion mapping staying one-to-one per trial (rule 6's sanctioned rule-7 fold, stated here as the rule requires). The program under test is a one-line `console.log` for `run` and a single `Kali.test` registration for `test` (rule 9: copied out of the source). It routes through `assert_explicit_browser_api_surface_accepts_zero_spawned_process_budget(` (:100); that helper loops `for json_output in [false, true]`, so each call site is two real invocations. On the JSON branch the source spells its claim as `.contains(...)` against the string leaf `json.stdout`, which has NO substring form in this case format (the twelve keys of design spec 5.4 include no json-substring key), so per controller ruling 3 it becomes an exact pin -- and, per U9, only after the value was captured from the real `kali` binary rather than hand-computed. The generator refuses to emit a pin that does not contain the source's own needle, so the strengthening is verified rather than asserted. U2 -- this target is TWO case files split on `kali.json` presence; this is the explicit-api half and `runtime_spawned_process_budget_js_input_inherited_manifest` is the other. The split is measured, not assumed (see the file header). MIGRATION NOTE (controller ruling 8): the source fn `assert_browser_requested_rejects_positive_spawned_process_budget` has a name that misdescribes its own body -- its name says browser-requested (spelled without backticks here: U8's gate resolves every backticked lower-case identifier against this source's fn list, and that is a prose fragment of a fn name, not a fn), but unlike its accepting twin it writes NO `kali.json` and passes NO `--api browser`, so no browser API surface is requested at all; the E5506 it asserts is the spawned-process budget rejection, which fires on the default surface too. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did. MIGRATION NOTE (controller ruling 8): the source fn `run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` has a name that misdescribes its own body -- it hands `kali test` a `main.<ext>` rather than a `smoke.test.<ext>`, unlike every other `test` leg in this source; the argv is carried faithfully and the `[source]` key is U5-renamed because the two legs write different programs to that one filename. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did."""
args = ["--output", "json", "run", "--api", "browser", "--max-spawned-processes", "0", "main_run.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json.schemaVersion = 1
json.command = "run"
json.success = true
json.payload.hostContract = "browser-requested"
json.payload.runtimeBackend = "browser-harness"
json.exitCode = 0
json.payload.exitCode = 0
json.stderr = ""
json.payload.threadTopology.totalInstances = 0
json.payload.threadTopology.terminatedInstances = 0
json.payload.threadTopology.liveInstances = []
json.errors = []
json.stdout = "browser spawned process budget ok\n"

[[case]]
name = "run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs__test__text"
rationale = """Migrated from browser_runtime_spawned_process_budget_js_input.rs. This `[[case]]` is one `(command, json_output)` cell of the source, matrix-fanned by `ext(4)`, so it stands for `run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` -- one trial per real invocation, with the assertion mapping staying one-to-one per trial (rule 6's sanctioned rule-7 fold, stated here as the rule requires). The program under test is a one-line `console.log` for `run` and a single `Kali.test` registration for `test` (rule 9: copied out of the source). It routes through `assert_explicit_browser_api_surface_accepts_zero_spawned_process_budget(` (:100); that helper loops `for json_output in [false, true]`, so each call site is two real invocations. The source spells this as a plain `.contains(...)` against raw stdout/stderr, so it is carried as stdout_contains/stderr_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. U2 -- this target is TWO case files split on `kali.json` presence; this is the explicit-api half and `runtime_spawned_process_budget_js_input_inherited_manifest` is the other. The split is measured, not assumed (see the file header). MIGRATION NOTE (controller ruling 8): the source fn `assert_browser_requested_rejects_positive_spawned_process_budget` has a name that misdescribes its own body -- its name says browser-requested (spelled without backticks here: U8's gate resolves every backticked lower-case identifier against this source's fn list, and that is a prose fragment of a fn name, not a fn), but unlike its accepting twin it writes NO `kali.json` and passes NO `--api browser`, so no browser API surface is requested at all; the E5506 it asserts is the spawned-process budget rejection, which fires on the default surface too. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did. MIGRATION NOTE (controller ruling 8): the source fn `run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` has a name that misdescribes its own body -- it hands `kali test` a `main.<ext>` rather than a `smoke.test.<ext>`, unlike every other `test` leg in this source; the argv is carried faithfully and the `[source]` key is U5-renamed because the two legs write different programs to that one filename. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did."""
args = ["test", "--api", "browser", "--max-spawned-processes", "0", "main_test.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["browser spawned process budget ok", "ok 1"]

[[case]]
name = "run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs__test__json"
rationale = """Migrated from browser_runtime_spawned_process_budget_js_input.rs. This `[[case]]` is one `(command, json_output)` cell of the source, matrix-fanned by `ext(4)`, so it stands for `run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` -- one trial per real invocation, with the assertion mapping staying one-to-one per trial (rule 6's sanctioned rule-7 fold, stated here as the rule requires). The program under test is a one-line `console.log` for `run` and a single `Kali.test` registration for `test` (rule 9: copied out of the source). It routes through `assert_explicit_browser_api_surface_accepts_zero_spawned_process_budget(` (:100); that helper loops `for json_output in [false, true]`, so each call site is two real invocations. On the JSON branch the source spells its claim as `.contains(...)` against the string leaf `json.stdout`, which has NO substring form in this case format (the twelve keys of design spec 5.4 include no json-substring key), so per controller ruling 3 it becomes an exact pin -- and, per U9, only after the value was captured from the real `kali` binary rather than hand-computed. The generator refuses to emit a pin that does not contain the source's own needle, so the strengthening is verified rather than asserted. U2 -- this target is TWO case files split on `kali.json` presence; this is the explicit-api half and `runtime_spawned_process_budget_js_input_inherited_manifest` is the other. The split is measured, not assumed (see the file header). MIGRATION NOTE (controller ruling 8): the source fn `assert_browser_requested_rejects_positive_spawned_process_budget` has a name that misdescribes its own body -- its name says browser-requested (spelled without backticks here: U8's gate resolves every backticked lower-case identifier against this source's fn list, and that is a prose fragment of a fn name, not a fn), but unlike its accepting twin it writes NO `kali.json` and passes NO `--api browser`, so no browser API surface is requested at all; the E5506 it asserts is the spawned-process budget rejection, which fires on the default surface too. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did. MIGRATION NOTE (controller ruling 8): the source fn `run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` has a name that misdescribes its own body -- it hands `kali test` a `main.<ext>` rather than a `smoke.test.<ext>`, unlike every other `test` leg in this source; the argv is carried faithfully and the `[source]` key is U5-renamed because the two legs write different programs to that one filename. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did."""
args = ["--output", "json", "test", "--api", "browser", "--max-spawned-processes", "0", "main_test.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json.schemaVersion = 1
json.command = "test"
json.success = true
json.payload.hostContract = "browser-requested"
json.payload.runtimeBackend = "browser-harness"
json.payload.total = 1
json.payload.passed = 1
json.payload.failed = 0
json.payload.skipped = 0
json.stderr = ""
json.payload.threadTopology.totalInstances = 0
json.payload.threadTopology.terminatedInstances = 0
json.payload.threadTopology.liveInstances = []
json.errors = []
json.stdout = "browser spawned process budget ok\n"

[[case]]
name = "run_and_test_reject_positive_spawned_process_budget_with_explicit_browser_api_surface_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs__run__text"
rationale = """Migrated from browser_runtime_spawned_process_budget_js_input.rs. This `[[case]]` is one `(command, json_output)` cell of the source, matrix-fanned by `ext(4)`, so it stands for `run_and_test_reject_positive_spawned_process_budget_with_explicit_browser_api_surface_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` -- one trial per real invocation, with the assertion mapping staying one-to-one per trial (rule 6's sanctioned rule-7 fold, stated here as the rule requires). The program under test is a one-line `console.log` for `run` and a single `Kali.test` registration for `test` (rule 9: copied out of the source). It routes through `assert_explicit_browser_api_surface_rejects_positive_spawned_process_budget(` (:239); that helper loops `for json_output in [false, true]`, so each call site is two real invocations. The source spells this as a plain `.contains(...)` against raw stdout/stderr, so it is carried as stdout_contains/stderr_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. U2 -- this target is TWO case files split on `kali.json` presence; this is the explicit-api half and `runtime_spawned_process_budget_js_input_inherited_manifest` is the other. The split is measured, not assumed (see the file header). MIGRATION NOTE (controller ruling 8): the source fn `assert_browser_requested_rejects_positive_spawned_process_budget` has a name that misdescribes its own body -- its name says browser-requested (spelled without backticks here: U8's gate resolves every backticked lower-case identifier against this source's fn list, and that is a prose fragment of a fn name, not a fn), but unlike its accepting twin it writes NO `kali.json` and passes NO `--api browser`, so no browser API surface is requested at all; the E5506 it asserts is the spawned-process budget rejection, which fires on the default surface too. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did. MIGRATION NOTE (controller ruling 8): the source fn `run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` has a name that misdescribes its own body -- it hands `kali test` a `main.<ext>` rather than a `smoke.test.<ext>`, unlike every other `test` leg in this source; the argv is carried faithfully and the `[source]` key is U5-renamed because the two legs write different programs to that one filename. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did."""
args = ["run", "--api", "browser", "--max-spawned-processes", "1", "main_run.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = 5
stderr_contains = ["E5506", "resources.maxSpawnedProcesses", "--max-spawned-processes"]

[[case]]
name = "run_and_test_reject_positive_spawned_process_budget_with_explicit_browser_api_surface_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs__run__json"
rationale = """Migrated from browser_runtime_spawned_process_budget_js_input.rs. This `[[case]]` is one `(command, json_output)` cell of the source, matrix-fanned by `ext(4)`, so it stands for `run_and_test_reject_positive_spawned_process_budget_with_explicit_browser_api_surface_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` -- one trial per real invocation, with the assertion mapping staying one-to-one per trial (rule 6's sanctioned rule-7 fold, stated here as the rule requires). The program under test is a one-line `console.log` for `run` and a single `Kali.test` registration for `test` (rule 9: copied out of the source). It routes through `assert_explicit_browser_api_surface_rejects_positive_spawned_process_budget(` (:239); that helper loops `for json_output in [false, true]`, so each call site is two real invocations. On the JSON branch the source spells its claim as `.contains(...)` against the string leaf `json.errors.0.message`, which has NO substring form in this case format (the twelve keys of design spec 5.4 include no json-substring key), so per controller ruling 3 it becomes an exact pin -- and, per U9, only after the value was captured from the real `kali` binary rather than hand-computed. The generator refuses to emit a pin that does not contain the source's own needle, so the strengthening is verified rather than asserted. U2 -- this target is TWO case files split on `kali.json` presence; this is the explicit-api half and `runtime_spawned_process_budget_js_input_inherited_manifest` is the other. The split is measured, not assumed (see the file header). MIGRATION NOTE (controller ruling 8): the source fn `assert_browser_requested_rejects_positive_spawned_process_budget` has a name that misdescribes its own body -- its name says browser-requested (spelled without backticks here: U8's gate resolves every backticked lower-case identifier against this source's fn list, and that is a prose fragment of a fn name, not a fn), but unlike its accepting twin it writes NO `kali.json` and passes NO `--api browser`, so no browser API surface is requested at all; the E5506 it asserts is the spawned-process budget rejection, which fires on the default surface too. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did. MIGRATION NOTE (controller ruling 8): the source fn `run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` has a name that misdescribes its own body -- it hands `kali test` a `main.<ext>` rather than a `smoke.test.<ext>`, unlike every other `test` leg in this source; the argv is carried faithfully and the `[source]` key is U5-renamed because the two legs write different programs to that one filename. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did."""
args = ["--output", "json", "run", "--api", "browser", "--max-spawned-processes", "1", "main_run.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = 5
json.schemaVersion = 1
json.command = "run"
json.success = false
json.exitCode = 5
json.errors.0.code = "E5506"
json.errors.0.context.origin = "cli"
json.errors.0.context.flag = "--max-spawned-processes"
json.errors.0.context.requestedValue = "1"
json.errors.0.context.effectiveValue = "1"
json.errors.0.message = "selected resource budget(s) [\"resources.maxSpawnedProcesses\"] are unavailable in this phase"

[[case]]
name = "run_and_test_reject_positive_spawned_process_budget_with_explicit_browser_api_surface_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs__test__text"
rationale = """Migrated from browser_runtime_spawned_process_budget_js_input.rs. This `[[case]]` is one `(command, json_output)` cell of the source, matrix-fanned by `ext(4)`, so it stands for `run_and_test_reject_positive_spawned_process_budget_with_explicit_browser_api_surface_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` -- one trial per real invocation, with the assertion mapping staying one-to-one per trial (rule 6's sanctioned rule-7 fold, stated here as the rule requires). The program under test is a one-line `console.log` for `run` and a single `Kali.test` registration for `test` (rule 9: copied out of the source). It routes through `assert_explicit_browser_api_surface_rejects_positive_spawned_process_budget(` (:239); that helper loops `for json_output in [false, true]`, so each call site is two real invocations. The source spells this as a plain `.contains(...)` against raw stdout/stderr, so it is carried as stdout_contains/stderr_contains and NOT strengthened to an exact pin -- controller ruling 3, mirror the source: a plain `.contains` against a field that HAS a substring form keeps the substring form even though the exact output was observed. U2 -- this target is TWO case files split on `kali.json` presence; this is the explicit-api half and `runtime_spawned_process_budget_js_input_inherited_manifest` is the other. The split is measured, not assumed (see the file header). MIGRATION NOTE (controller ruling 8): the source fn `assert_browser_requested_rejects_positive_spawned_process_budget` has a name that misdescribes its own body -- its name says browser-requested (spelled without backticks here: U8's gate resolves every backticked lower-case identifier against this source's fn list, and that is a prose fragment of a fn name, not a fn), but unlike its accepting twin it writes NO `kali.json` and passes NO `--api browser`, so no browser API surface is requested at all; the E5506 it asserts is the spawned-process budget rejection, which fires on the default surface too. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did. MIGRATION NOTE (controller ruling 8): the source fn `run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` has a name that misdescribes its own body -- it hands `kali test` a `main.<ext>` rather than a `smoke.test.<ext>`, unlike every other `test` leg in this source; the argv is carried faithfully and the `[source]` key is U5-renamed because the two legs write different programs to that one filename. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did."""
args = ["test", "--api", "browser", "--max-spawned-processes", "1", "main_test.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = 5
stderr_contains = ["E5506", "resources.maxSpawnedProcesses", "--max-spawned-processes"]

[[case]]
name = "run_and_test_reject_positive_spawned_process_budget_with_explicit_browser_api_surface_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs__test__json"
rationale = """Migrated from browser_runtime_spawned_process_budget_js_input.rs. This `[[case]]` is one `(command, json_output)` cell of the source, matrix-fanned by `ext(4)`, so it stands for `run_and_test_reject_positive_spawned_process_budget_with_explicit_browser_api_surface_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` -- one trial per real invocation, with the assertion mapping staying one-to-one per trial (rule 6's sanctioned rule-7 fold, stated here as the rule requires). The program under test is a one-line `console.log` for `run` and a single `Kali.test` registration for `test` (rule 9: copied out of the source). It routes through `assert_explicit_browser_api_surface_rejects_positive_spawned_process_budget(` (:239); that helper loops `for json_output in [false, true]`, so each call site is two real invocations. On the JSON branch the source spells its claim as `.contains(...)` against the string leaf `json.errors.0.message`, which has NO substring form in this case format (the twelve keys of design spec 5.4 include no json-substring key), so per controller ruling 3 it becomes an exact pin -- and, per U9, only after the value was captured from the real `kali` binary rather than hand-computed. The generator refuses to emit a pin that does not contain the source's own needle, so the strengthening is verified rather than asserted. U2 -- this target is TWO case files split on `kali.json` presence; this is the explicit-api half and `runtime_spawned_process_budget_js_input_inherited_manifest` is the other. The split is measured, not assumed (see the file header). MIGRATION NOTE (controller ruling 8): the source fn `assert_browser_requested_rejects_positive_spawned_process_budget` has a name that misdescribes its own body -- its name says browser-requested (spelled without backticks here: U8's gate resolves every backticked lower-case identifier against this source's fn list, and that is a prose fragment of a fn name, not a fn), but unlike its accepting twin it writes NO `kali.json` and passes NO `--api browser`, so no browser API surface is requested at all; the E5506 it asserts is the spawned-process budget rejection, which fires on the default surface too. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did. MIGRATION NOTE (controller ruling 8): the source fn `run_and_test_support_explicit_browser_api_surface_with_zero_spawned_process_budget_when_browser_harness_is_configured_in_js_ts_jsx_and_tsx_inputs` has a name that misdescribes its own body -- it hands `kali test` a `main.<ext>` rather than a `smoke.test.<ext>`, unlike every other `test` leg in this source; the argv is carried faithfully and the `[source]` key is U5-renamed because the two legs write different programs to that one filename. The source is NOT corrected: a fn name is not a comment so U7 does not literally apply, the `.rs` files are deleted wholesale after batch 8, and editing a source invalidates every audit run against its pre-trim blob. The discrepancy is recorded here so the case file preserves what the source actually did."""
args = ["--output", "json", "test", "--api", "browser", "--max-spawned-processes", "1", "main_test.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = 5
json.schemaVersion = 1
json.command = "test"
json.success = false
json.exitCode = 5
json.errors.0.code = "E5506"
json.errors.0.context.origin = "cli"
json.errors.0.context.flag = "--max-spawned-processes"
json.errors.0.context.requestedValue = "1"
json.errors.0.context.effectiveValue = "1"
json.errors.0.message = "selected resource budget(s) [\"resources.maxSpawnedProcesses\"] are unavailable in this phase"
