# Migrated from tests/browser_math_hypot_empty_identity.rs.
#
# RULE 12 (carry every source comment): `browser_math_hypot_empty_identity.rs` contains NO Rust
# comments. Checked with `grep -nE '//'`: the single hit is the
# `// kali-tree-shake:` marker on the first line of the bundle fixture's
# `r##"..."##` body, which is program text under test and is carried verbatim
# into [source] below, not Rust prose. There is nothing to move into a
# `rationale`, and `comment_coverage.py` is therefore run with --allow-empty for
# this pair (its batch-3 zero-line floor would otherwise report a vacuous
# green).
#
# RULE 7 / U1 -- MATRIX ARITHMETIC, and it closes exactly. Enumerated with
# tools/task-18-browser-pilot/enumerate_invocations.py: 24 #[test] fns, 24 real
# invocations, no loops anywhere in the file (every #[test] fn is a single
# unlooped helper call, verified by reading both helper bodies -- neither
# contains a `for`).
#   * `assert_browser_bundle_global_this_math_hypot_empty_identity(filename,
#     json_output)` -- 8 = ext(js/ts/jsx/tsx) x json_output(false/true), a full
#     cross product.
#   * `assert_browser_harness_global_this_math_hypot_empty_identity(command,
#     filename, source, json_output)` -- 16 = command(run/test) x ext(4) x
#     json_output(false/true), also a full cross product.
# `ext` is the one axis both helpers vary over uniformly and completely:
# 6 [[case]] x ext(4) = 24 trials = 24 #[test] fns. Exact. Per rule 6 the fold is
# stated here -- each [[case]] corresponds to 4 source fns, one per cell, and the
# assertion mapping stays 1:1 per trial. `command` and `json_output` are NOT
# axes: each changes the assertion SHAPE (JSON envelope vs text stdout;
# `exitCode` for run vs `total`/`passed`/`failed` for test), which design spec
# 5.6's closing note excludes from a matrix.
#
# U2 -- [source] is file-wide, and that is safe here. Every fixture is written
# unconditionally into a fresh temp dir by the source, none sits behind an `if`,
# and no case's point is a file's presence or absence. Each command names its
# entry explicitly on argv, so the sibling fixtures in a trial dir are inert;
# verified against the real binary that a `test` run in a directory containing
# all four `smoke.test.<ext>` files still reports total/passed/failed = 1/1/0 for
# the one it was given, and that a `build --bundle` of `app.<ext>` in a directory
# containing all four `app.*` still emits `app/app.meta.json`.
#
# RULE 13 (transitive helper docs): `grep -c '///' browser_math_hypot_empty_identity.rs` returns 0 --
# no fn in any call chain of this file carries a doc comment, so there is none to
# carry. The chain does reach `kali_runtime_contract`'s
# `browser_bundle_harness_script` and `browser_harness_command_parts_for`, which
# carry one-line `///` docs, but the migrated case file never calls them: the
# `browser_bundle_harness` step kind means the case RUNNER does, so those docs
# describe shared runner infrastructure (design spec 5.3) rather than what this
# case claims. Same disposition as the already-shipped `math_log2_log10.toml`
# header, flagged there as a standing question rather than decided per file.
#
# ASSERTION SHAPE, mirrored from the source and nothing more.
# Bundle helper: `exit = "success"` on the build (:65) and on the harness process
# (:121); in json mode schemaVersion/command/success/exitCode and payload
# artifactKind/bundleFormat (:74-80) PLUS `errors = []` (:81-84) -- this file DOES
# assert the empty errors array on its build envelope, unlike
# `math_imul_omitted_operands`, so it is written here and not there; the emitted
# `app/app.meta.json` metadata (:92-93), read outside the `if json_output` and so
# asserted in both modes; then the bundle-harness `stdout_contains` (:128).
# Harness helper: `exit = "success"` (:160); non-json `stdout_contains` (:192);
# json mode schemaVersion/command/success/payload(hostContract, runtimeBackend)
# (:168-172), `exitCode` plus `payload.exitCode` for run (:174-175) or
# payload total/passed/failed for test (:177-179), the exact `json.stdout` pin
# resolving :181-187, `stderr = ""` (:188) and `errors = []` (:189) -- this file
# asserts BOTH, which several siblings in this batch do not.
# The source DOES pass `--max-threads 0 --max-spawned-processes 0` on the harness
# argv (:151-155), so both flags appear; the build argv has neither.
# The stdout claim at every site is a plain contiguous
# `.contains("0\n0\n0\n0\n0\n")` -- it is NOT a `.matches(...).count()`, so no
# `stdout_count`/`json_count` key appears in this file, and the contiguous
# five-line needle is carried exactly as written rather than split into five
# `"0\n"` needles (that would be a different, weaker claim about adjacency).
#
# The `json` branch's stdout claim is `json["stdout"].as_str().contains(...)`.
# That leaf has no substring form: design spec 5.4's twelve keys include no
# JSON-substring key at all (only exact `json` paths, `json_null` and
# `json_count`). So per controller ruling 3 -- a plain `.contains` against a
# field with NO substring form becomes an exact pin -- it is written as an exact
# `json.stdout` pin, and per U9 the pinned value was captured by running the real
# `kali` binary with `node` as the browser harness backend, never hand-computed. Captured value for both `run` and `test` and all four extensions:
# "0\n0\n0\n0\n0\n" -- identical in all 8 combinations, which is what makes a
# single pin valid under the `ext` axis.

[matrix]
ext = ["js", "ts", "jsx", "tsx"]

[source]
"app.${ext}" = """// kali-tree-shake: globalThisMathHypotEmptyIdentity
function globalThisMathHypotEmptyIdentity() {
  console.log(Math.hypot());
  console.log(globalThis.Math.hypot());
  console.log(globalThis.Math["hypot"]());
  console.log(globalThis["Math"].hypot());
  console.log(globalThis["Math"]["hypot"]());
  return [
    Math.hypot(),
    globalThis.Math.hypot(),
    globalThis.Math["hypot"](),
    globalThis["Math"].hypot(),
    globalThis["Math"]["hypot"](),
  ];
}
"""
"main.${ext}" = """console.log(Math.hypot()); console.log(globalThis.Math.hypot()); console.log(globalThis.Math["hypot"]()); console.log(globalThis["Math"].hypot()); console.log(globalThis["Math"]["hypot"]());
"""
"smoke.test.${ext}" = """Kali.test('globalThis math hypot empty identity', () => {
  console.log(Math.hypot());
  console.log(globalThis.Math.hypot());
  console.log(globalThis.Math["hypot"]());
  console.log(globalThis["Math"].hypot());
  console.log(globalThis["Math"]["hypot"]());
});
"""

[[case]]
name = "build_emits_global_this_math_hypot_empty_identity"
rationale = """Migrated from browser_math_hypot_empty_identity.rs. `assert_browser_bundle_global_this_math_hypot_empty_identity` builds a browser bundle (`kali build --bundle --api browser`), asserts the emitted `app/app.meta.json` metadata, then runs the bundle glue under the browser-bundle-harness contract. The bundled program calls `Math.hypot()` with no arguments through five spellings of the Math root (bare, dotted globalThis, and three bracketed forms); the empty-argument identity makes each print `0`, so the harness stdout carries five consecutive zero lines. The source asserts that as one contiguous `stdout.contains("0\\n0\\n0\\n0\\n0\\n")` (:128), carried verbatim as a single `stdout_contains` needle -- not weakened to five separate `"0\\n"` needles, which would drop the adjacency the source claims, and not strengthened to an exact `stdout` pin, which the source never writes (controller ruling 3: mirror the source)."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "app.${ext}"]
exit = "success"

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.globalThisMathHypotEmptyIdentity();
"""
exit = "success"
stdout_contains = ["0\n0\n0\n0\n0\n"]

[[case]]
name = "json_build_emits_global_this_math_hypot_empty_identity"
rationale = """Migrated from browser_math_hypot_empty_identity.rs. `assert_browser_bundle_global_this_math_hypot_empty_identity` builds a browser bundle (`kali build --bundle --api browser`), asserts the emitted `app/app.meta.json` metadata, then runs the bundle glue under the browser-bundle-harness contract. The bundled program calls `Math.hypot()` with no arguments through five spellings of the Math root (bare, dotted globalThis, and three bracketed forms); the empty-argument identity makes each print `0`, so the harness stdout carries five consecutive zero lines. The source asserts that as one contiguous `stdout.contains("0\\n0\\n0\\n0\\n0\\n")` (:128), carried verbatim as a single `stdout_contains` needle -- not weakened to five separate `"0\\n"` needles, which would drop the adjacency the source claims, and not strengthened to an exact `stdout` pin, which the source never writes (controller ruling 3: mirror the source). This sibling asserts the JSON build envelope (schemaVersion/command/success/exitCode, payload artifactKind and bundleFormat, and an empty `errors` array, :74-84) instead of plain text. Output shape is a sibling case rather than a matrix axis because it changes the assertion shape (design spec 5.6)."""

[[case.step]]
args = ["build", "--bundle", "--api", "browser", "--output", "json", "app.${ext}"]
exit = "success"
json = { schemaVersion = 1, command = "build", success = true, exitCode = 0, payload = { artifactKind = "bundle", bundleFormat = "esm" }, errors = [] }

[[case.step]]
kind = "file_json"
path = "app/app.meta.json"
fields = { apiSurface = "browser", artifactKind = "bundle" }

[[case.step]]
kind = "browser_bundle_harness"
entry = "app"
body = """const mod = await import(bundleJs.href);
await mod.globalThisMathHypotEmptyIdentity();
"""
exit = "success"
stdout_contains = ["0\n0\n0\n0\n0\n"]

[[case]]
name = "run_supports_global_this_math_hypot_empty_identity_when_browser_harness_is_configured"
rationale = """Migrated from browser_math_hypot_empty_identity.rs. `assert_browser_harness_global_this_math_hypot_empty_identity` runs `kali run --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program calling `Math.hypot()` with no arguments through five spellings of the Math root, each printing `0`. This is the non-json branch (:190-193): the only output claim is `stdout.contains("0\\n0\\n0\\n0\\n0\\n")`, carried as `stdout_contains` at the same strength."""
args = ["run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["0\n0\n0\n0\n0\n"]

[[case]]
name = "test_supports_global_this_math_hypot_empty_identity_when_browser_harness_is_configured"
rationale = """Migrated from browser_math_hypot_empty_identity.rs. `assert_browser_harness_global_this_math_hypot_empty_identity` runs `kali test --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program calling `Math.hypot()` with no arguments through five spellings of the Math root, each printing `0`. This is the non-json branch (:190-193): the only output claim is `stdout.contains("0\\n0\\n0\\n0\\n0\\n")`, carried as `stdout_contains` at the same strength."""
args = ["test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
stdout_contains = ["0\n0\n0\n0\n0\n"]

[[case]]
name = "run_supports_global_this_math_hypot_empty_identity_when_browser_harness_is_configured_in_json"
rationale = """Migrated from browser_math_hypot_empty_identity.rs. `assert_browser_harness_global_this_math_hypot_empty_identity` runs `kali --output json run --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program calling `Math.hypot()` with no arguments through five spellings of the Math root, each printing `0`. This is the json branch (:166-189). The `json` branch's stdout claim is `json["stdout"].as_str().contains(...)`. That leaf has no substring form: design spec 5.4's twelve keys include no JSON-substring key at all (only exact `json` paths, `json_null` and `json_count`). So per controller ruling 3 -- a plain `.contains` against a field with NO substring form becomes an exact pin -- it is written as an exact `json.stdout` pin, and per U9 the pinned value was captured by running the real `kali` binary with `node` as the browser harness backend, never hand-computed. The captured value is "0\\n0\\n0\\n0\\n0\\n", identical for both commands and all four extensions. `stderr` is asserted exactly empty (:188) and `errors` exactly empty (:189); both are real source assertions in this file, unlike its expm1/log1p siblings which assert `stderr` only."""
args = ["--output", "json", "run", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "main.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "run", success = true, exitCode = 0, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", exitCode = 0 }, stdout = "0\n0\n0\n0\n0\n", stderr = "", errors = [] }

[[case]]
name = "test_supports_global_this_math_hypot_empty_identity_when_browser_harness_is_configured_in_json"
rationale = """Migrated from browser_math_hypot_empty_identity.rs. `assert_browser_harness_global_this_math_hypot_empty_identity` runs `kali --output json test --api browser --max-threads 0 --max-spawned-processes 0` with the browser harness backed by `node`, against a program calling `Math.hypot()` with no arguments through five spellings of the Math root, each printing `0`. This is the json branch (:166-189). The `json` branch's stdout claim is `json["stdout"].as_str().contains(...)`. That leaf has no substring form: design spec 5.4's twelve keys include no JSON-substring key at all (only exact `json` paths, `json_null` and `json_count`). So per controller ruling 3 -- a plain `.contains` against a field with NO substring form becomes an exact pin -- it is written as an exact `json.stdout` pin, and per U9 the pinned value was captured by running the real `kali` binary with `node` as the browser harness backend, never hand-computed. The captured value is "0\\n0\\n0\\n0\\n0\\n", identical for both commands and all four extensions. `stderr` is asserted exactly empty (:188) and `errors` exactly empty (:189); both are real source assertions in this file, unlike its expm1/log1p siblings which assert `stderr` only."""
args = ["--output", "json", "test", "--api", "browser", "--max-threads", "0", "--max-spawned-processes", "0", "smoke.test.${ext}"]
env = { KALI_BROWSER_BUNDLE_HARNESS_COMMAND = "node" }
exit = "success"
json = { schemaVersion = 1, command = "test", success = true, payload = { hostContract = "browser-requested", runtimeBackend = "browser-harness", total = 1, passed = 1, failed = 0 }, stdout = "0\n0\n0\n0\n0\n", stderr = "", errors = [] }
