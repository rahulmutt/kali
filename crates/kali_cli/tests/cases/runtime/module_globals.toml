# Migrated from tests/runtime_module_globals.rs.
#   SOURCE REF: 020b5c33707052455d68dc44cac72a12e961f6ae
#
# Module-scope `var` promoted to a persistent mutable wasm global: reads, writes, compound assignment, float use sites and lexical shadowing, with module-scope OBJECTS still fail-closed.
#
# MATRIX DECLINED. 8 source helper invocation(s) == 8 case(s) x 1 -- no axis every case varies over uniformly (rule 7, U1). Every `#[test]` fn in this source runs a DIFFERENT program with a different expectation; there is no extension, command or output-mode axis to fan them over, and inventing one would produce combinations the source never exercised (rule 2). The identity is asserted inside `gen_task19_batch3.arithmetic`, so this sentence cannot state arithmetic that does not close.
#
# THE SHAPE THIS FILE WAS MIGRATED FROM, AND WHAT DERIVED ITS CLAIMS. The source declares one helper -- `fn run_source(src: &str) -> std::process::Output` -- which writes a single program to `main.ts` under a fresh temp directory and runs `kali run <path>` on it. Every `#[test]` fn in the file calls it EXACTLY ONCE and constructs no other command, so one `#[test]` fn is one `[[case]]` is one trial (rule 6, one-to-one) and rule 5's split never applies. ("one-to-one", not the arithmetic spelling: `citation_sweep.sh`'s `CITE` reads a backticked construct followed by a colon-digit as a line citation, and batch 2 had to reword the same phrase for the same reason.)
#
# The claim set of every case below is DERIVED from the source by `tools/migration/t19b3_extract.py`, not listed by hand, and its shape table is CLOSED: an `assert!`/`assert_eq!` it does not model raises rather than being skipped. That direction is the point -- a forward extractor that silently skips what it does not understand turns a DROPPED claim into a green run. The modelled shapes are exactly:
#
#   assert!(out.status.success(), ..)              -> exit = "success"
#   assert!(!out.status.success(), ..)             -> exit = "failure"
#   assert_eq!(String::from_utf8_lossy(&out.stdout), <str>)  -> exact `stdout`
#   assert!(out.stdout.is_empty(), ..)            -> exact `stdout = ""`
#   assert!(<stderr>.contains(<str>), ..)          -> `stderr_contains`
#   assert!(<stderr>.contains(A) || <stderr>.contains(B), ..)
#                                                  -> rule 11 / ruling 17
#
# An `assert!`'s second and later arguments are its PANIC MESSAGE, which the program under test never sees; they are not claims and are not migrated. Reproduce the derivation with:
#
#   python3 tools/migration/t19b3_extract.py runtime_module_globals
#
# ASSERTION STRENGTH -- RULING 3, MIRROR THE SOURCE, CLAUSE BY CLAUSE.
#
#   * an exact `assert_eq!` on the WHOLE of stdout is an exact source assertion, so it becomes an exact `stdout` pin (clause 1). The pinned text is COPIED out of the source's own literal by `lexer.find_string_literals`; it is not a live capture and this header does not claim it is. Batch 2 shipped a header saying "captured from the real binary, never computed" about values that were in fact copied out of the source, and had to correct it -- the distinction is recorded here so it is not lost again.
#   * `assert!(out.stdout.is_empty())` is an exact assertion about the whole of stdout, so it becomes `stdout = ""`. That is the same exact-stdout discipline used everywhere else on a taken, verified path, not a rule-2 invention.
#   * a plain `.contains` against stderr stays `stderr_contains` (clause 3): the field has a substring form, and it is NOT strengthened to an exact pin just because the exact output was observed.
#   * a test whose only assertion is `!out.status.success()` pins `exit = "failure"` and NOTHING about either stream. The real binary emits a diagnostic on every one of them, and pinning it would be rule 2's exact prohibition: never add an assertion the source did not make merely because it is true.
#
# REALISM IS PROVEN SEPARATELY FROM FIDELITY (U9). Every trial in this file runs against the real `kali` binary in the suite; that proves the case matches what the binary does today. It does not prove nothing was dropped -- only the source-vs-TOML direction does that, which is what the derived extraction and `check_extra_claims.py` are for.
#
# `[source]` KEYS ARE VARIANT-SUFFIXED (U5), ALL 8 OF THEM. The source wrote every one of these programs to `main.ts`, under a fresh temp directory per `#[test]`. `[source]` here is one FILE-WIDE namespace that `expand()` clones into every trial, so 8 programs cannot share one key -- the last body written would win and every other case would silently run the wrong program. Each case's program is keyed by the source `#[test]` fn that wrote it, and that name is what the case passes as argv, so the mapping from trial to source fn survives the deletion of `runtime_module_globals.rs`. Every renamed key is declared `# EXTRA-OK:` below: it is a fixture FILENAME surfaced as an argv token, not a claim about behaviour. No program in this file references a sibling filename by string, which is the check U5 actually asks for and which `gen_task19_batch3.check_no_fixture_names_referenced` runs over every fixture body rather than over argv alone.
#
# U2 CHECK, RUN RATHER THAN ASSUMED. `[source]` is one flat FILE-WIDE map that `expand()` clones into every trial, so every sibling case's program is present in this case's trial directory too. That is inert here, and the controls were run rather than reasoned about:
#
#   * every case names its own program explicitly on argv, exactly as the source did (`kali run <fn-name>.ts`), so a sibling `.ts` in the same directory is never read. Control: `kali run a.ts` in a directory also holding a `b.ts` that fails to compile still prints a.ts's output, rc=0; the same command naming `b.ts` fails, rc=1. The discriminator is argv, and argv is what these cases carry.
#   * `kali.json` IS auto-discovered as a manifest and would NOT be inert. No fixture in this file is named `kali.json` -- every key is a source `#[test]` fn name with a `.ts` suffix -- and gen_task19_batch3's check_no_manifest_named_fixture asserts it rather than leaving it to inspection. (Tool function names are written unbackticked in this header on purpose: `check_rationale_fn_names.py` reads a backticked fn-shaped token as a citation into the SOURCE, and a generator's own function is not one.)
#
# No fixture here is one whose mere PRESENCE or ABSENCE is a case's point, which is the shape U2 exists to stop: every case in this family is a single program run by name.
#
# RULE 12 ATTRIBUTION, DERIVED FROM THE SOURCE'S OWN LAYOUT (U6). Every comment in the source is carried, and which rationale it lands in is decided by position rather than per-comment judgement -- `t19b3_extract.prose` implements the split and this generator reads it:
#
#   * a comment paragraph (or `///` doc block) INSIDE a `#[test]`'s body, or directly abutting it, goes into THAT case's rationale and no other. U6: over-attribution is forbidden even though it turns `comment_coverage.py` green. The inside-the-body arm is not decoration: an abutting-only predicate filed this batch's in-body comments as file-wide, which is under-attribution, and the shipped predicate checks it first.
#   * a SECTION BANNER -- a paragraph the source itself brackets with `----` divider lines, or a single line wrapped in them (`---- Finding 2: ... ----`) -- goes into the rationale of every case in its section, the section running from the banner to the next banner or to end of file. That is rule 12's own sentence for prose attached to a section, applied mechanically to the source's own delimiters instead of re-deciding per test which of the following fns a banner "really" means.
#   * FILE-WIDE prose -- the `//!` module doc and the comments attached to `run_source`/`kali_bin` -- is carried in this `#` header, which is the one place rule 12 puts prose that every case reaches equally.
#
# TRAILING comments (U16) are attributed by line to whichever `#[test]` body encloses them. `comment_coverage.py` was blind to that whole shape until Task 19 batch 2 closed it.
#
# FILE-WIDE SOURCE PROSE, CARRIED VERBATIM (rule 12). Read out of `runtime_module_globals.rs` by `t19b3_extract.prose`, never retyped:
#
#   Per-process AtomicU64 counter keeps the temp slug unique even when two
#   sources share a length (repo convention; avoids the macOS CI temp-slug
#   collision flake).
#
# RULING 6 EXEMPTION, APPLIED TO THE HARNESS HELPER'S OWN PROSE. The comments carried in the paragraph above describe how the Rust harness kept its own temp directories from colliding (a pid + atomic-counter slug). The migrated case depends on nothing that helper computed: the case runner gives every trial its own directory, so the helper's job is now the runner's job -- which is ruling 6's own test for the exemption. Carried verbatim here so nothing is dropped (rule 12), and deliberately NOT replicated into every rationale, because attributing harness mechanics to every case is the over-attribution U6 forbids.
#
# EXTRA-OK: 'module_var_written_and_read_from_function.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'module_var_read_only_from_function.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'module_var_lcg_float_division.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'module_var_compound_assign_from_function.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'module_object_reassigned_never_member_accessed_is_rejected.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'local_var_shadowing_module_global_does_not_clobber.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'param_shadowing_module_global_reads_param.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'module_object_mutation_from_function_still_rejected.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: '0.3746499199817101\n0.729023776863283\n' -- a deliberate exact pin whose value the SOURCE COMPUTES in Rust (`lines.push_str(&format!("{}\n", v))` over an LCG loop), so it exists as no literal in the `.rs` and `check_extra_claims.py` cannot find it there. Obtained by EXECUTING the source's own block (rule 8) and cross-checked against the real binary running this fixture
#
# CONSEQUENCE FOR THE GATES -- `comment_coverage.py` IS EXPECTED-RED (rc=1) ON THIS PAIR [classes: per-case, file-wide]. The checker asks whether every source comment line appears in EVERY case's rationale, and it reports two different things that way:
#
#   * PER-CASE ATTRIBUTION (`from N/M cases` lines). A comment attached to one `#[test]` fn belongs in the rationale of the case that fn produced and nowhere else, and a SECTION banner belongs in the rationales of its own section and nowhere else. U6 says so and calls copying all of a file's comments into all of its cases "over-attribution ... forbidden, even though it turns the checker green".
#   * FILE-WIDE PROSE IN THE HEADER (`from ALL N cases` lines). The source's `//!` module doc, the prose attached to `run_source`/`kali_bin` carried under ruling 6's exemption, and the `----` divider ornament that `comment_coverage.py` counts as a comment line whenever it shares a paragraph with text, all describe the whole file rather than any one case. Rule 12 puts them in this `#` header -- which `comment_coverage.py` deliberately does not read as coverage. Every such line is prose that IS carried, in the one place the rule says it belongs.
#
# U6 anticipates the first exactly: "on such a file the checker's false `missing` must be documented in the header instead." That is what this paragraph is. THE CLASS LIST ABOVE IS GATED, not asserted: `gen_task19_batch3.check_gate_declarations` re-runs the checker and requires the classes its output actually contains to match the ones named here. Reproduce with:
#
#   python3 tools/task-18-browser-pilot/comment_coverage.py \
#     crates/kali_cli/tests/runtime_module_globals.rs \
#     crates/kali_cli/tests/cases/runtime/module_globals.toml
#

[source]
"module_var_written_and_read_from_function.ts" = """var g = 42;
function bump(){ g = g + 1; return g; }
console.log(bump());
console.log(bump());
"""
"module_var_read_only_from_function.ts" = """var base = 100;
function get(){ return base + 1; }
console.log(get());
console.log(base);
"""
"module_var_lcg_float_division.ts" = """var s = 42;
function r(){ s = (s * 3877 + 29573) % 139968; return (1.0 * s) / 139968; }
console.log(r());
console.log(r());
"""
"module_var_compound_assign_from_function.ts" = """var acc = 0;
function add(n){ acc += n; return acc; }
console.log(add(5));
console.log(add(7));
"""
"module_object_reassigned_never_member_accessed_is_rejected.ts" = """var o = { x: 1 };
function f(){ o = { x: 2 }; return 0; }
f();
console.log(o);
"""
"local_var_shadowing_module_global_does_not_clobber.ts" = """var g = 10;
function f(){ var g = 5; return g + 1; }
console.log(f());
console.log(g);
"""
"param_shadowing_module_global_reads_param.ts" = """var n = 5;
function id(n){ return n; }
console.log(id(7));
console.log(n);
"""
"module_object_mutation_from_function_still_rejected.ts" = """var o = { x: 1 };
function f(){ o.x = 2; return o.x; }
console.log(f());
"""

[[case]]
name = "module_var_written_and_read_from_function"
rationale = """Migrated from runtime_module_globals.rs, source `#[test]` fn `module_var_written_and_read_from_function` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `out.status.success()`, pinned as `exit = "success"`. Its `assert_eq!` on the whole of stdout is an exact source assertion, so it becomes an exact `stdout` pin (ruling 3 clause 1); the value is COPIED out of that literal, not retyped and not captured.

The exact Spec 4a LCG shape: a module-scope `var` mutated (READ + WRITE)
from inside a function across calls. Pre-fix this hit E5506 (read) + E8001
(`=` write); now the persistent mutable global carries state across calls."""
args = ["run", "module_var_written_and_read_from_function.ts"]
exit = "success"
stdout = "43\n44\n"

[[case]]
name = "module_var_read_only_from_function"
rationale = """Migrated from runtime_module_globals.rs, source `#[test]` fn `module_var_read_only_from_function` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `out.status.success()`, pinned as `exit = "success"`. Its `assert_eq!` on the whole of stdout is an exact source assertion, so it becomes an exact `stdout` pin (ruling 3 clause 1); the value is COPIED out of that literal, not retyped and not captured.

Read-only module var from a function (no write), plus a module-scope read."""
args = ["run", "module_var_read_only_from_function.ts"]
exit = "success"
stdout = "101\n100\n"

[[case]]
name = "module_var_lcg_float_division"
rationale = """Migrated from runtime_module_globals.rs, source `#[test]` fn `module_var_lcg_float_division` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `out.status.success()`, pinned as `exit = "success"`. Its expected stdout is COMPUTED in Rust by the source itself, so it exists as no literal: the pin was obtained by executing the source's own block (rule 8) and cross-checked against the real binary running this very fixture.

The capstone's LCG with the float-division use site: an i64 module global is
mutated by integer arithmetic (`% 139968`) then read in a float context
(`(1.0 * s) / 139968`) — the repr system must convert the i64 GlobalGet to
f64 at the use site.

node -e on the same source:
  s = (42*3877+29573)%139968 = 192407 % 139968 = 52439 -> 52439/139968
  s = (52439*3877+29573)%139968 = ... -> matches node

Compute the reference locally to avoid a hardcoded transcription error."""
args = ["run", "module_var_lcg_float_division.ts"]
exit = "success"
stdout = "0.3746499199817101\n0.729023776863283\n"

[[case]]
name = "module_var_compound_assign_from_function"
rationale = """Migrated from runtime_module_globals.rs, source `#[test]` fn `module_var_compound_assign_from_function` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `out.status.success()`, pinned as `exit = "success"`. Its `assert_eq!` on the whole of stdout is an exact source assertion, so it becomes an exact `stdout` pin (ruling 3 clause 1); the value is COPIED out of that literal, not retyped and not captured.

Compound assignment (`+=`) to a module global from a function decomposes to
GlobalGet/op/GlobalSet."""
args = ["run", "module_var_compound_assign_from_function.ts"]
exit = "success"
stdout = "5\n12\n"

[[case]]
name = "module_object_reassigned_never_member_accessed_is_rejected"
rationale = """Migrated from runtime_module_globals.rs, source `#[test]` fn `module_object_reassigned_never_member_accessed_is_rejected` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `!out.status.success()`, pinned as `exit = "failure"`. It makes no claim about either stream, so none is written: the trial does emit a diagnostic, and pinning it would be rule 2's exact prohibition against adding an assertion merely because it is true. Its `stderr.contains('E5506')` is a plain `.contains` against a field that HAS a substring form, so it stays a `stderr_contains` rather than being strengthened to an exact pin (ruling 3 clause 3).

FIX 1 fail-closed pin: a module OBJECT reassigned from a function but NEVER
member-accessed has a DEFAULT-`I64` repr (its shape is never proven), so a
naive "I64 minus member-bases" heuristic would promote it and store the
object handle in an i64 global (a memory-corruption-class fail-open, printed
`0` exit 0 pre-fix). The positive-scalar promotion proof requires the init
(`{x:1}`) to be numeric — it is not — so the binding is left unpromoted and
the assignment stays fail-closed (E5506)."""
args = ["run", "module_object_reassigned_never_member_accessed_is_rejected.ts"]
exit = "failure"
stderr_contains = ["E5506"]

[[case]]
name = "local_var_shadowing_module_global_does_not_clobber"
rationale = """Migrated from runtime_module_globals.rs, source `#[test]` fn `local_var_shadowing_module_global_does_not_clobber` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `out.status.success()`, pinned as `exit = "success"`. Its `assert_eq!` on the whole of stdout is an exact source assertion, so it becomes an exact `stdout` pin (ruling 3 clause 1); the value is COPIED out of that literal, not retyped and not captured.

FIX 2: a local `var g` inside a function that shadows a promoted module
global `g` must write its OWN local slot (JS lexical scoping), NOT clobber
the module global. Pre-fix the declarator-init `GlobalSet` fired for the
local too, so the module `g` became 5 (node: 10)."""
args = ["run", "local_var_shadowing_module_global_does_not_clobber.ts"]
exit = "success"
stdout = "6\n10\n"

[[case]]
name = "param_shadowing_module_global_reads_param"
rationale = """Migrated from runtime_module_globals.rs, source `#[test]` fn `param_shadowing_module_global_reads_param` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `out.status.success()`, pinned as `exit = "success"`. Its `assert_eq!` on the whole of stdout is an exact source assertion, so it becomes an exact `stdout` pin (ruling 3 clause 1); the value is COPIED out of that literal, not retyped and not captured.

FIX 3: a param `n` that shadows a promoted module global `n` must read the
PARAM, not the global. Pre-fix the read `GlobalGet` branch sat ahead of the
param lookup, so `id(7)` returned the global 5 (node: 7)."""
args = ["run", "param_shadowing_module_global_reads_param.ts"]
exit = "success"
stdout = "7\n5\n"

[[case]]
name = "module_object_mutation_from_function_still_rejected"
rationale = """Migrated from runtime_module_globals.rs, source `#[test]` fn `module_object_mutation_from_function_still_rejected` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `!out.status.success()`, pinned as `exit = "failure"`. It makes no claim about either stream, so none is written: the trial does emit a diagnostic, and pinning it would be rule 2's exact prohibition against adding an assertion merely because it is true. Its `stderr.contains('E5506')` is a plain `.contains` against a field that HAS a substring form, so it stays a `stderr_contains` rather than being strengthened to an exact pin (ruling 3 clause 3).

SCOPE fail-closed pin: a mutable module-scope OBJECT mutated from a function
is a persistent heap root the GC-less region reclamation does not model.
It MUST stay rejected (E5506) — never silently lowered to a mutable global."""
args = ["run", "module_object_mutation_from_function_still_rejected.ts"]
exit = "failure"
stderr_contains = ["E5506"]
