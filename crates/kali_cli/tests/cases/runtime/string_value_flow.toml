# Migrated from tests/runtime_string_value_flow.rs.
#   SOURCE REF: 020b5c33707052455d68dc44cac72a12e961f6ae
#
# Runtime string VALUE flow: concatenation, accumulation, `+=` and content equality run, while truthiness, ordering and consumed mixed returns reject rather than compare raw handles.
#
# MATRIX DECLINED. 12 source helper invocation(s) == 12 case(s) x 1 -- no axis every case varies over uniformly (rule 7, U1). Every `#[test]` fn in this source runs a DIFFERENT program with a different expectation; there is no extension, command or output-mode axis to fan them over, and inventing one would produce combinations the source never exercised (rule 2). The identity is asserted inside `gen_task19_batch3.arithmetic`, so this sentence cannot state arithmetic that does not close.
#
# THE SHAPE THIS FILE WAS MIGRATED FROM, AND WHAT DERIVED ITS CLAIMS. The source declares one helper -- `fn run_source(src: &str) -> std::process::Output` -- which writes a single program to `main.ts` under a fresh temp directory and runs `kali run <path>` on it. Every `#[test]` fn in the file calls it EXACTLY ONCE and constructs no other command, so one `#[test]` fn is one `[[case]]` is one trial (rule 6, one-to-one) and rule 5's split never applies. ("one-to-one", not the arithmetic spelling: `citation_sweep.sh`'s `CITE` reads a backticked construct followed by a colon-digit as a line citation, and batch 2 had to reword the same phrase for the same reason.)
#
# The claim set of every case below is DERIVED from the source by `tools/migration/t19b3_extract.py`, not listed by hand, and its shape table is CLOSED: an `assert!`/`assert_eq!` it does not model raises rather than being skipped. That direction is the point -- a forward extractor that silently skips what it does not understand turns a DROPPED claim into a green run. The modelled shapes are exactly:
#
#   assert!(out.status.success(), ..)              -> exit = "success"
#   assert!(!out.status.success(), ..)             -> exit = "failure"
#   assert_eq!(String::from_utf8_lossy(&out.stdout), <str>)  -> exact `stdout`
#   assert!(out.stdout.is_empty(), ..)            -> exact `stdout = ""`
#   assert!(<stderr>.contains(<str>), ..)          -> `stderr_contains`
#   assert!(<stderr>.contains(A) || <stderr>.contains(B), ..)
#                                                  -> rule 11 / ruling 17
#
# An `assert!`'s second and later arguments are its PANIC MESSAGE, which the program under test never sees; they are not claims and are not migrated. Reproduce the derivation with:
#
#   python3 tools/migration/t19b3_extract.py runtime_string_value_flow
#
# ASSERTION STRENGTH -- RULING 3, MIRROR THE SOURCE, CLAUSE BY CLAUSE.
#
#   * an exact `assert_eq!` on the WHOLE of stdout is an exact source assertion, so it becomes an exact `stdout` pin (clause 1). The pinned text is COPIED out of the source's own literal by `lexer.find_string_literals`; it is not a live capture and this header does not claim it is. Batch 2 shipped a header saying "captured from the real binary, never computed" about values that were in fact copied out of the source, and had to correct it -- the distinction is recorded here so it is not lost again.
#   * `assert!(out.stdout.is_empty())` is an exact assertion about the whole of stdout, so it becomes `stdout = ""`. That is the same exact-stdout discipline used everywhere else on a taken, verified path, not a rule-2 invention.
#   * a plain `.contains` against stderr stays `stderr_contains` (clause 3): the field has a substring form, and it is NOT strengthened to an exact pin just because the exact output was observed.
#   * a test whose only assertion is `!out.status.success()` pins `exit = "failure"` and NOTHING about either stream. The real binary emits a diagnostic on every one of them, and pinning it would be rule 2's exact prohibition: never add an assertion the source did not make merely because it is true.
#
# REALISM IS PROVEN SEPARATELY FROM FIDELITY (U9). Every trial in this file runs against the real `kali` binary in the suite; that proves the case matches what the binary does today. It does not prove nothing was dropped -- only the source-vs-TOML direction does that, which is what the derived extraction and `check_extra_claims.py` are for.
#
# `[source]` KEYS ARE VARIANT-SUFFIXED (U5), ALL 12 OF THEM. The source wrote every one of these programs to `main.ts`, under a fresh temp directory per `#[test]`. `[source]` here is one FILE-WIDE namespace that `expand()` clones into every trial, so 12 programs cannot share one key -- the last body written would win and every other case would silently run the wrong program. Each case's program is keyed by the source `#[test]` fn that wrote it, and that name is what the case passes as argv, so the mapping from trial to source fn survives the deletion of `runtime_string_value_flow.rs`. Every renamed key is declared `# EXTRA-OK:` below: it is a fixture FILENAME surfaced as an argv token, not a claim about behaviour. No program in this file references a sibling filename by string, which is the check U5 actually asks for and which `gen_task19_batch3.check_no_fixture_names_referenced` runs over every fixture body rather than over argv alone.
#
# U2 CHECK, RUN RATHER THAN ASSUMED. `[source]` is one flat FILE-WIDE map that `expand()` clones into every trial, so every sibling case's program is present in this case's trial directory too. That is inert here, and the controls were run rather than reasoned about:
#
#   * every case names its own program explicitly on argv, exactly as the source did (`kali run <fn-name>.ts`), so a sibling `.ts` in the same directory is never read. Control: `kali run a.ts` in a directory also holding a `b.ts` that fails to compile still prints a.ts's output, rc=0; the same command naming `b.ts` fails, rc=1. The discriminator is argv, and argv is what these cases carry.
#   * `kali.json` IS auto-discovered as a manifest and would NOT be inert. No fixture in this file is named `kali.json` -- every key is a source `#[test]` fn name with a `.ts` suffix -- and gen_task19_batch3's check_no_manifest_named_fixture asserts it rather than leaving it to inspection. (Tool function names are written unbackticked in this header on purpose: `check_rationale_fn_names.py` reads a backticked fn-shaped token as a citation into the SOURCE, and a generator's own function is not one.)
#
# No fixture here is one whose mere PRESENCE or ABSENCE is a case's point, which is the shape U2 exists to stop: every case in this family is a single program run by name.
#
# RULE 12 ATTRIBUTION, DERIVED FROM THE SOURCE'S OWN LAYOUT (U6). Every comment in the source is carried, and which rationale it lands in is decided by position rather than per-comment judgement -- `t19b3_extract.prose` implements the split and this generator reads it:
#
#   * a comment paragraph (or `///` doc block) INSIDE a `#[test]`'s body, or directly abutting it, goes into THAT case's rationale and no other. U6: over-attribution is forbidden even though it turns `comment_coverage.py` green. The inside-the-body arm is not decoration: an abutting-only predicate filed this batch's in-body comments as file-wide, which is under-attribution, and the shipped predicate checks it first.
#   * a SECTION BANNER -- a paragraph the source itself brackets with `----` divider lines, or a single line wrapped in them (`---- Finding 2: ... ----`) -- goes into the rationale of every case in its section, the section running from the banner to the next banner or to end of file. That is rule 12's own sentence for prose attached to a section, applied mechanically to the source's own delimiters instead of re-deciding per test which of the following fns a banner "really" means.
#   * FILE-WIDE prose -- the `//!` module doc and the comments attached to `run_source`/`kali_bin` -- is carried in this `#` header, which is the one place rule 12 puts prose that every case reaches equally.
#
# TRAILING comments (U16) are attributed by line to whichever `#[test]` body encloses them. `comment_coverage.py` was blind to that whole shape until Task 19 batch 2 closed it.
#
# FILE-WIDE SOURCE PROSE, CARRIED VERBATIM (rule 12). Read out of `runtime_string_value_flow.rs` by `t19b3_extract.prose`, never retyped:
#
#   A per-process AtomicU64 counter makes the slug unique even when two
#   sources share a length (sharing a length previously collided the dir and
#   caused macOS CI temp-slug flakes — repo convention is a counter).
#
# RULING 6 EXEMPTION, APPLIED TO THE HARNESS HELPER'S OWN PROSE. The comments carried in the paragraph above describe how the Rust harness kept its own temp directories from colliding (a pid + atomic-counter slug). The migrated case depends on nothing that helper computed: the case runner gives every trial its own directory, so the helper's job is now the runner's job -- which is ruling 6's own test for the exemption. Carried verbatim here so nothing is dropped (rule 12), and deliberately NOT replicated into every rationale, because attributing harness mechanics to every case is the over-attribution U6 forbids.
#
# EXTRA-OK: 'string_variable_concat_prints.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'string_param_return_roundtrip_prints.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'string_accumulation_loop_prints.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'string_then_number_binding_is_rejected.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'consumed_mixed_return_used_as_string_is_rejected.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'concat_result_equality_compares_content.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'concat_result_truthiness_is_rejected.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'concat_result_relational_is_rejected.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'interned_literal_equality_is_preserved.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'mixed_literal_int_and_string_element_store_is_rejected.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'string_add_assign_concatenates.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'consumed_mixed_return_used_as_number_is_rejected.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
#
# CONSEQUENCE FOR THE GATES -- `comment_coverage.py` IS EXPECTED-RED (rc=1) ON THIS PAIR [classes: per-case, file-wide]. The checker asks whether every source comment line appears in EVERY case's rationale, and it reports two different things that way:
#
#   * PER-CASE ATTRIBUTION (`from N/M cases` lines). A comment attached to one `#[test]` fn belongs in the rationale of the case that fn produced and nowhere else, and a SECTION banner belongs in the rationales of its own section and nowhere else. U6 says so and calls copying all of a file's comments into all of its cases "over-attribution ... forbidden, even though it turns the checker green".
#   * FILE-WIDE PROSE IN THE HEADER (`from ALL N cases` lines). The source's `//!` module doc, the prose attached to `run_source`/`kali_bin` carried under ruling 6's exemption, and the `----` divider ornament that `comment_coverage.py` counts as a comment line whenever it shares a paragraph with text, all describe the whole file rather than any one case. Rule 12 puts them in this `#` header -- which `comment_coverage.py` deliberately does not read as coverage. Every such line is prose that IS carried, in the one place the rule says it belongs.
#
# U6 anticipates the first exactly: "on such a file the checker's false `missing` must be documented in the header instead." That is what this paragraph is. THE CLASS LIST ABOVE IS GATED, not asserted: `gen_task19_batch3.check_gate_declarations` re-runs the checker and requires the classes its output actually contains to match the ones named here. Reproduce with:
#
#   python3 tools/task-18-browser-pilot/comment_coverage.py \
#     crates/kali_cli/tests/runtime_string_value_flow.rs \
#     crates/kali_cli/tests/cases/runtime/string_value_flow.toml
#
# CONSEQUENCE FOR THE GATES -- `check_rationale_fn_names.py` (U8) IS EXPECTED-RED (rc=1) ON THIS PAIR, AND IT CANNOT BE MADE GREEN WITHOUT BREAKING RULE 12. The gate requires every backticked fn-shaped identifier in this file to resolve against the source `.rs`'s own fn list. The ones it cannot resolve here are carried VERBATIM out of the source's own comments -- rule 12 requires the carry, U7 forbids rewording it -- and they are: `element_read_captor_is_not_string`.
#
# REMEDY: a CLI-family entry in the gate's own known-identifier allowlist. These name items in other crates, in the compiler under test, or in the Rust standard library, which this file's source never declares, so no amount of correct prose resolves them. The pilot flagged the same gap on `nullish/assign_reject.toml` and `misc/growable_array_fail_closed.toml`, and batch 2 on four more.
#
# THE NAMES AND THEIR CLASSIFICATION ARE DERIVED, not asserted: `gen_task19_batch3.u8_reason` re-runs the gate, reads back the identifiers it could not explain, and decides for each whether the source DEFINES a JavaScript function of that name inside its own fixture programs. Reproduce with:
#
#   python3 tools/task-18-browser-pilot/check_rationale_fn_names.py \
#     crates/kali_cli/tests/runtime_string_value_flow.rs \
#     crates/kali_cli/tests/cases/runtime/string_value_flow.toml
#

[source]
"string_variable_concat_prints.ts" = """let x = "GG";
x = x + "CC";
console.log(x);
"""
"string_param_return_roundtrip_prints.ts" = """function f(s) { return s + "!"; }
console.log(f("hi"));
"""
"string_accumulation_loop_prints.ts" = """let a = "";
for (let i = 0; i < 3; i = i + 1) { a = a + "y"; }
console.log(a);
"""
"string_then_number_binding_is_rejected.ts" = """let x = "a";
x = 5;
console.log(x);
"""
"consumed_mixed_return_used_as_string_is_rejected.ts" = """function g(v, k) { if (k > 0) return "yes"; return v; }
let r = g(99, 0);
console.log(r + "?");
"""
"concat_result_equality_compares_content.ts" = """let a = "x";
let b = a + "y";
console.log(b == "xy");
"""
"concat_result_truthiness_is_rejected.ts" = """let a = "";
let b = a + "";
if (b) { console.log(1); } else { console.log(0); }
"""
"concat_result_relational_is_rejected.ts" = """let a = "x";
let b = a + "y";
console.log(b < "b");
"""
"interned_literal_equality_is_preserved.ts" = """let s = "hi";
console.log(s == "hi");
"""
"mixed_literal_int_and_string_element_store_is_rejected.ts" = """let a = [1];
let s = a[0];
a[0] = "x";
console.log(s + "!");
"""
"string_add_assign_concatenates.ts" = """let a = "x";
a += "y";
console.log(a + "!");
"""
"consumed_mixed_return_used_as_number_is_rejected.ts" = """function g(v, k) { if (k > 0) return "yes"; return v; }
let r = g(42, 0);
console.log(r + 1);
"""

[[case]]
name = "string_variable_concat_prints"
rationale = """Migrated from runtime_string_value_flow.rs, source `#[test]` fn `string_variable_concat_prints` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `out.status.success()`, pinned as `exit = "success"`. Its `assert_eq!` on the whole of stdout is an exact source assertion, so it becomes an exact `stdout` pin (ruling 3 clause 1); the value is COPIED out of that literal, not retyped and not captured."""
args = ["run", "string_variable_concat_prints.ts"]
exit = "success"
stdout = "GGCC\n"

[[case]]
name = "string_param_return_roundtrip_prints"
rationale = """Migrated from runtime_string_value_flow.rs, source `#[test]` fn `string_param_return_roundtrip_prints` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `out.status.success()`, pinned as `exit = "success"`. Its `assert_eq!` on the whole of stdout is an exact source assertion, so it becomes an exact `stdout` pin (ruling 3 clause 1); the value is COPIED out of that literal, not retyped and not captured."""
args = ["run", "string_param_return_roundtrip_prints.ts"]
exit = "success"
stdout = "hi!\n"

[[case]]
name = "string_accumulation_loop_prints"
rationale = """Migrated from runtime_string_value_flow.rs, source `#[test]` fn `string_accumulation_loop_prints` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `out.status.success()`, pinned as `exit = "success"`. Its `assert_eq!` on the whole of stdout is an exact source assertion, so it becomes an exact `stdout` pin (ruling 3 clause 1); the value is COPIED out of that literal, not retyped and not captured."""
args = ["run", "string_accumulation_loop_prints.ts"]
exit = "success"
stdout = "yyy\n"

[[case]]
name = "string_then_number_binding_is_rejected"
rationale = """Migrated from runtime_string_value_flow.rs, source `#[test]` fn `string_then_number_binding_is_rejected` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `!out.status.success()`, pinned as `exit = "failure"`. It makes no claim about either stream, so none is written: the trial does emit a diagnostic, and pinning it would be rule 2's exact prohibition against adding an assertion merely because it is true.

A binding used as both string and number must fail to compile (fail-closed),
not silently miscompile."""
args = ["run", "string_then_number_binding_is_rejected.ts"]
exit = "failure"

[[case]]
name = "consumed_mixed_return_used_as_string_is_rejected"
rationale = """Migrated from runtime_string_value_flow.rs, source `#[test]` fn `consumed_mixed_return_used_as_string_is_rejected` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `!out.status.success()`, pinned as `exit = "failure"`. It makes no claim about either stream, so none is written: the trial does emit a diagnostic, and pinning it would be rule 2's exact prohibition against adding an assertion merely because it is true.

Finding 1: a mixed return (string branch + plain/int branch) is downgraded
to I64, but string-reachability still flows through the call-result into a
capturing scalar, which would classify `Repr::String` over the runtime int
— codegen would then read the raw int as a string handle (base printed
`99?`, the miscompile printed `?`). Consumed mixed returns must FAIL CLOSED."""
args = ["run", "consumed_mixed_return_used_as_string_is_rejected.ts"]
exit = "failure"

[[case]]
name = "concat_result_equality_compares_content"
rationale = """Migrated from runtime_string_value_flow.rs, source `#[test]` fn `concat_result_equality_compares_content` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `out.status.success()`, pinned as `exit = "success"`. Its `assert_eq!` on the whole of stdout is an exact source assertion, so it becomes an exact `stdout` pin (ruling 3 clause 1); the value is COPIED out of that literal, not retyped and not captured.

---- Final-review Finding 1: proven-string values in non-`+` consumers ----

RE-PIN (throw-fallout Stage 1): `b == "xy"` on a FRESH runtime concat
handle is now CONTENT equality via `__streq` (node: true). The old pin
asserted the E3200 fail-closed reject that Stage 1 lifted. kali prints
the comparison as `1` (same pre-existing boolean-print lane
`interned_literal_equality_is_preserved` pins)."""
args = ["run", "concat_result_equality_compares_content.ts"]
exit = "success"
stdout = "1\n"

[[case]]
name = "concat_result_truthiness_is_rejected"
rationale = """Migrated from runtime_string_value_flow.rs, source `#[test]` fn `concat_result_truthiness_is_rejected` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `!out.status.success()`, pinned as `exit = "failure"`. It makes no claim about either stream, so none is written: the trial does emit a diagnostic, and pinning it would be rule 2's exact prohibition against adding an assertion merely because it is true.

---- Final-review Finding 1: proven-string values in non-`+` consumers ----

Empty-string falsiness is lost: a concat handle is always non-zero, so
`if (b)` for an empty concat took the wrong branch on head. Reject."""
args = ["run", "concat_result_truthiness_is_rejected.ts"]
exit = "failure"

[[case]]
name = "concat_result_relational_is_rejected"
rationale = """Migrated from runtime_string_value_flow.rs, source `#[test]` fn `concat_result_relational_is_rejected` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `!out.status.success()`, pinned as `exit = "failure"`. It makes no claim about either stream, so none is written: the trial does emit a diagnostic, and pinning it would be rule 2's exact prohibition against adding an assertion merely because it is true.

---- Final-review Finding 1: proven-string values in non-`+` consumers ----

Ordering compares raw handles, not string content. Reject."""
args = ["run", "concat_result_relational_is_rejected.ts"]
exit = "failure"

[[case]]
name = "interned_literal_equality_is_preserved"
rationale = """Migrated from runtime_string_value_flow.rs, source `#[test]` fn `interned_literal_equality_is_preserved` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `out.status.success()`, pinned as `exit = "success"`. Its `assert_eq!` on the whole of stdout is an exact source assertion, so it becomes an exact `stdout` pin (ruling 3 clause 1); the value is COPIED out of that literal, not retyped and not captured.

---- Final-review Finding 1: proven-string values in non-`+` consumers ----

A literal-rooted string variable is an INTERNED handle: identity equality
== value equality, so `s == "hi"` is correct on the base compiler and
must stay correct here (only runtime-concat handles are rejected)."""
args = ["run", "interned_literal_equality_is_preserved.ts"]
exit = "success"
stdout = "1\n"

[[case]]
name = "mixed_literal_int_and_string_element_store_is_rejected"
rationale = """Migrated from runtime_string_value_flow.rs, source `#[test]` fn `mixed_literal_int_and_string_element_store_is_rejected` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `!out.status.success()`, pinned as `exit = "failure"`. It makes no claim about either stream, so none is written: the trial does emit a diagnostic, and pinning it would be rule 2's exact prohibition against adding an assertion merely because it is true. Its `stderr.contains('elements of `a`')` is a plain `.contains` against a field that HAS a substring form, so it stays a `stderr_contains` rather than being strengthened to an exact pin (ruling 3 clause 3).

---- Final-review Finding 2: array-element read must not leak the string axis ----

Superseded by Spec 3 (runtime `Array.prototype.join`): `element_read_captor_is_not_string`
pinned this exact shape (`let a = [1]; let s = a[0]; a[0] = "x";`) compiling
successfully and printing the base compiler's silently-wrong `0!`, because
element reads were float-only (excluded from the string axis) and mixed
string/number element STORES were never checked. Spec 3 lifts the read-edge
exclusion and adds a store-source conflict check, so this array's elements
(an int literal store from `[1]` and a string store from `a[0] = "x"`) are
now correctly diagnosed as a shape conflict instead of silently reading back
an unrelated int — a compile error is strictly better than the old `0!`."""
args = ["run", "mixed_literal_int_and_string_element_store_is_rejected.ts"]
exit = "failure"
stderr_contains = ["elements of `a`"]

[[case]]
name = "string_add_assign_concatenates"
rationale = """Migrated from runtime_string_value_flow.rs, source `#[test]` fn `string_add_assign_concatenates` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `out.status.success()`, pinned as `exit = "success"`. Its `assert_eq!` on the whole of stdout is an exact source assertion, so it becomes an exact `stdout` pin (ruling 3 clause 1); the value is COPIED out of that literal, not retyped and not captured.

---- Final-review Finding 3: string `+=` lowers through string concat ----

`a += "y"` on a string target must concatenate (base miscompiled it; head
vouched a non-handle). Correct output is `xy!`."""
args = ["run", "string_add_assign_concatenates.ts"]
exit = "success"
stdout = "xy!\n"

[[case]]
name = "consumed_mixed_return_used_as_number_is_rejected"
rationale = """Migrated from runtime_string_value_flow.rs, source `#[test]` fn `consumed_mixed_return_used_as_number_is_rejected` -- one `[[case]]` per source `#[test]` fn (rule 6), and the `[source]` key is that fn's name so the mapping survives the source's deletion.

The source asserts `!out.status.success()`, pinned as `exit = "failure"`. It makes no claim about either stream, so none is written: the trial does emit a diagnostic, and pinning it would be rule 2's exact prohibition against adding an assertion merely because it is true.

---- Final-review Finding 3: string `+=` lowers through string concat ----

Same defect on the numeric-consumer side (base printed `43`, the miscompile
printed `1`): the capturing scalar is still poisoned to `Repr::String`, so
the program must be rejected rather than silently miscompiled."""
args = ["run", "consumed_mixed_return_used_as_number_is_rejected.ts"]
exit = "failure"
