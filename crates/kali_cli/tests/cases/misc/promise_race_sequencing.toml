# GENERATED by tools/migration/gen_task19_batch2.py -- do not edit this file by hand. Edit
# the generator and re-run it with --write; its default mode re-renders every file it owns
# and fails on any byte difference.
#
# Migrated from tests/promise_race_sequencing.rs.
#   SOURCE REF: b7f2ed5d5644fff14b03b5266fcc7f26b13ac925
#
# `Promise.race` sequencing fails closed and loud: the fixture's own self-check throws and
# the run exits nonzero.
#
# MATRIX DECLARED. 4 source helper invocation(s) == 2 case(s) x ext(2) == 2 x 2. Four
# `#[test]` fns == two helper shapes (`run`, `test`) x two input extensions, and every
# case varies over the extension uniformly, so `ext` is a real axis (rule 7, U1). The
# `[source]` keys carry the axis too, so a `js` trial materialises only the `js` fixture.
#
# U13 -- WHAT THE MATRIX BOUGHT. The source writes the same program text under `main.js`
# and `main.ts` (and the same test program under `smoke.test.js` and `smoke.test.ts`).
# Written as four named siblings that would be a duplicated `[source]` body and a U13
# hoist question; declaring `ext` removes the duplication instead of hoisting around it,
# which is strictly better than either horn of ruling 7's dilemma.
#
# U2 CHECK, RUN RATHER THAN ASSUMED. `[source]` is one flat FILE-WIDE map that `expand()`
# clones into every trial, so a fixture a sibling case needs is present in this case's
# trial too. Measured, with a known positive first, because a green that was never made
# red is not evidence:
#
#   * a sandbox policy file present in the working directory but NOT named on argv is inert. Control: the same deny-everything policy PASSED as `--sandbox` turns `console.log(1)` into `error[E9007]: inferred effect 'Console.Write' is not permitted by the active policy`, rc=5; the same three files present as `policy.json` / `tiny.policy.json` / `kali.policy.json` and not passed leave `kali run m.ts` printing `1`, rc=0.
#   * a sibling `*.test.*` fixture is inert BECAUSE every case names its program explicitly on argv, exactly as the source did. Control: a bare `kali test` in a directory holding `main.test.js` and `other.test.js` discovers both and prints `ok 2`; `kali test main.test.js` in that same directory prints `ok 1`.
#   * `kali.json` IS auto-discovered as a manifest and would not be inert. No fixture in this batch is named `kali.json`; recorded because the next batch's author cannot tell from the tree that the name is special.
#
# No fixture here is one whose mere PRESENCE or ABSENCE is a case's point, which is the
# shape U2 exists to stop.
#
# ASSERTION SHAPE. The helper asserts ONLY `!output.status.success()`. No stdout or stderr
# claim is written, because the source makes none -- the trap text these trials do emit is
# true and unasserted, and pinning it would be a rule-2 invention.
#
# CONSEQUENCE FOR THE GATES -- `audit-case-migration.py` IS EXPECTED-RED (rc=3) ON THIS
# PAIR. rc=3 is AUDIT INAPPLICABLE, not a dropped claim: the audit ran to completion over
# this pair and demanded ZERO claims of it, so it decided nothing either way. The source's
# ONLY assertion is `assert!(!output.status.success(), ...)` -- an exit-status claim
# carrying no output literal -- and everything else in the `.rs` is fixture program text,
# which the audit excludes from its search by construction, so there is nothing for a
# literal-coverage tool to find. Stated here because AUDIT OK over zero demanded claims
# and AUDIT OK over a covered pair are otherwise indistinguishable, and only one of them
# is evidence. What DOES cover this pair is the case runner itself: every trial below pins
# `exit = "failure"`, which is exactly the claim the source made, and `check_fixtures.py`
# pins the argv and fixture correspondence. THE DECLARATION IS GATED, not asserted: this
# generator's `--check` re-runs the audit over the pinned source and fails unless the rc
# it returns is the rc declared here.
#

[matrix]
ext = ["js", "ts"]

[source]
"main.${ext}" = """async function promiseRaceSmoke() {
  const winner = await Promise.race([Promise.resolve(1), Promise.resolve(2)]);
  if (winner !== 1) {
    throw new Error('unexpected Promise.race sequencing');
  }
}

async function main() {
  await promiseRaceSmoke();
  console.log('promise race ok');
}

main();
"""
"smoke.test.${ext}" = """async function promiseRaceSmoke() {
  const winner = await Promise.race([Promise.resolve(1), Promise.resolve(2)]);
  if (winner !== 1) {
    throw new Error('unexpected Promise.race sequencing');
  }
}

Kali.test('promise race', () => promiseRaceSmoke());
"""

[[case]]
name = "run_supports_promise_race_in_js_and_ts_input"
rationale = """Migrated from promise_race_sequencing.rs. Honest re-pin (PR #16 rev2): kali fails closed/loud here;
see docs/superpowers/followups/pr16-honest-repin-inventory.md.

Source `#[test]` fns: run_supports_promise_race_in_js_input, run_supports_promise_race_in_ts_input -- folded into one `[[case]]` by the file-wide `ext` axis under rule 7, named here so the fn names survive the sources' deletion (rule 6's reason, not just its letter). `kali run` over the `Promise.race` fixture must exit nonzero. The source asserts nothing about either stream, so nothing about either stream is pinned (rule 2). This case is fanned over `ext` by the file-wide `[matrix]`, reproducing the source's `js` and `ts` invocations 1:1."""
args = ["run", "main.${ext}"]
exit = "failure"

[[case]]
name = "test_supports_promise_race_in_js_and_ts_input"
rationale = """Migrated from promise_race_sequencing.rs. Honest re-pin (PR #16 rev2): kali fails closed/loud here;
see docs/superpowers/followups/pr16-honest-repin-inventory.md.

Source `#[test]` fns: test_supports_promise_race_in_js_input, test_supports_promise_race_in_ts_input -- folded into one `[[case]]` by the file-wide `ext` axis under rule 7, named here so the fn names survive the sources' deletion (rule 6's reason, not just its letter). `kali test` over the `Promise.race` fixture must exit nonzero. The source asserts nothing about either stream, so nothing about either stream is pinned (rule 2). This case is fanned over `ext` by the file-wide `[matrix]`, reproducing the source's `js` and `ts` invocations 1:1."""
args = ["test", "smoke.test.${ext}"]
exit = "failure"
