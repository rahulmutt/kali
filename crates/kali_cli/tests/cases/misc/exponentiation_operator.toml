# Migrated from tests/exponentiation_operator.rs.
#   SOURCE REF: b7f2ed5d5644fff14b03b5266fcc7f26b13ac925
#
# `**` lowers to exponentiation at runtime: `console.log(2 ** 3)` prints 8.
#
# MATRIX DECLINED. 1 source helper invocation(s) == 1 case(s) x 1 -- no axis every case
# varies over uniformly (rule 7, U1). The single `#[test]` fn is a single `kali run`
# invocation.
#
# ASSERTION SHAPE. The source asserts `output.status.success()` and
# `stdout.contains("8")`. `stdout` has a substring form, so the `.contains` stays a
# `stdout_contains` rather than being strengthened to the exact `"8\n"` this trial was
# observed to emit -- ruling 3, clause 3: mirror the source, and do not strengthen because
# you observed the exact output.
#
# CONSEQUENCE FOR THE GATES -- `comment_coverage.py` IS EXPECTED-RED (rc=2) ON THIS PAIR,
# AND THE RED IS THE GATE WORKING. The source carries no comment lines at all, so the
# checker's ruling-5 zero-line floor fires: without it a pair with nothing to check
# reports a vacuous green, which is the dangerous direction. Stated explicitly because *no
# prose* and *prose missed* are otherwise indistinguishable to a later reader. Reproduce
# with:
#
#   python3 tools/task-18-browser-pilot/comment_coverage.py \
#     crates/kali_cli/tests/exponentiation_operator.rs \
#     crates/kali_cli/tests/cases/misc/exponentiation_operator.toml
#

[source]
"main.js" = """console.log(2 ** 3);
"""

[[case]]
name = "run_supports_exponentiation_operator_in_js_input"
rationale = """Migrated from exponentiation_operator.rs. `2 ** 3` must evaluate to 8 at runtime. The source carries no comments, so rules 12 and 13 have nothing to carry into this rationale -- stated rather than left to be inferred from silence."""
args = ["run", "main.js"]
exit = "success"
stdout_contains = ["8"]
