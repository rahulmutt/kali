# Migrated from tests/set_iteration_runtime.rs.
#   SOURCE REF: 7f57e0ed87ac9eaa0d05de88816f1fb1fdf6ef15
#
# MATRIX DECLINED. 8 source helper invocation(s) == 8 case(s) x 1. No axis every case in this file varies over uniformly (rule 7, U1), so `[matrix]` is dropped for the WHOLE file and the invocations are written as named siblings -- rule 5's split, one `[[case]]` per real helper invocation. The predicate that decides this is gen_task19_batch4.axis_for and it is mechanical; the arithmetic is asserted by gen_task19_batch4.arithmetic, so this header cannot state arithmetic that does not close.
#
# THE SHAPE THIS FILE WAS MIGRATED FROM, AND WHAT DERIVED ITS CLAIMS. set_iteration_runtime.rs is a MULTI-BUILDER, MULTI-MODE source: fixture-builder fns returning `&'static str`, assert-helpers that build one `Command` each, and `#[test]` fns that call them once or loop over a list of filenames. Batch 3's one-helper shape table does not fit it, so the claim set of every case below is derived by tools/migration/t19b4_extract.py -- a small INTERPRETER over a closed statement and expression language, not a pattern match over one call. Reproduce with:
#
#   python3 tools/migration/t19b4_extract.py set_iteration_runtime
#
# THE TABLE IS CLOSED OVER CLAIMS, NOT OVER `assert*!` MACROS. Enumerating assertion macros says nothing about a claim written as control flow, carried by an `.expect()`, or made outside any macro; a source using one would migrate silently short. t19b4_extract.residual_claims blanks every assert span, every permitted whole statement and every permitted call, then REFUSES on what is left. Every construct outside the statement/expression language raises as well, so an unmodelled source shape breaks the generator instead of quietly under-claiming. An `assert!`'s second and later arguments are its PANIC MESSAGE, which the program under test never sees; they are not claims and are not migrated.
#
# ASSERTION STRENGTH -- RULING 3, MIRROR THE SOURCE. An exact `assert_eq!` on the whole of a stream becomes an exact pin; an is_empty() assertion on a stream becomes an exact `""` pin, which is the same exact-stream discipline used elsewhere and not a rule-2 invention; a plain `.contains` against a stream stays a `*_contains` and is NOT strengthened because the exact output was observed. Pinned text is COPIED out of the source's own literal by lexer.find_string_literals -- it is not a live capture and this header does not claim it is.
#
# REALISM IS PROVEN SEPARATELY FROM FIDELITY (U9). Every trial in this file runs against the real `kali` binary in the suite, which proves the case matches what the binary does today. It does not prove nothing was dropped -- only the source-vs-TOML direction does that, which is what the derived extraction and check_extra_claims.py are for.
#
# `[source]` KEYS ARE VARIANT-SUFFIXED (U5). `[source]` is one FILE-WIDE namespace that expand() clones into every trial, and this source writes many different programs to the SAME filename, so the programs cannot share a key -- the last body written would win and every other case would silently run the wrong program. Each key is named after the source `#[test]` fn that wrote the program, so the mapping from trial to source fn survives the deletion of the `.rs`. The WHOLE suffix chain is preserved (`.test.js`, not `.js`), because `kali test` dispatches on the `.test.` infix and U5 permits renaming an argv filename, not renaming one whose shape the tool reads. No program in this file references a sibling filename by string, which is the check U5 actually asks for and which gen_task19_batch4.check_no_fixture_names_referenced runs over every fixture body rather than over argv alone.
#
# U2 CHECK, RUN RATHER THAN ASSUMED. Every sibling case's program is present in this case's trial directory. That is inert here and the controls were run: every case names its own program explicitly on argv, exactly as the source did, so a sibling is never read; and `kali.json` IS auto-discovered as a manifest and would not be inert, which gen_task19_batch4.check_no_manifest_named_fixture asserts rather than leaving to inspection. (Tool function names are written unbackticked on purpose: check_rationale_fn_names.py reads a backticked fn-shaped token as a citation into the SOURCE, and a generator's own function is not one.)
#
# RULE 12 ATTRIBUTION, DERIVED FROM THE SOURCE'S OWN LAYOUT (U6). Every comment in the source is carried, and which rationale it lands in is decided by POSITION rather than per-comment judgement: a paragraph inside a `#[test]` body, or directly abutting it, goes into that case's rationale and no other; a paragraph attached to a helper goes into the rationale of every case that helper's call path reaches, which the evaluator RECORDS rather than guesses; everything else is file-wide and is carried in this `#` header. Over-attribution is forbidden even though it turns comment_coverage.py green (U6). Trailing comments (U16) are attributed by line to whichever body encloses them.
#
# U13, BOTH HALVES, MEASURED RATHER THAN EYEBALLED. Over this file's 8 `[source]` bodies: **12 byte-identical pair(s)** and a **longest shared leading-line prefix of 2 line(s)**. The hoist into `[constants]` is DECLINED (ruling 7), and the reason is measured rather than inherited: check_fixtures.py's toml_program_texts reads `[source]` values VERBATIM and never resolves a `${NAME}` reference, so a hoisted body makes the rule-9 fixture gate report UNMATCHED on a correct file. Ruling 7 states that for `browser/`; the property is the tool's, not the family's. The identity is asserted mechanically by this generator's u13_measure rather than eyeballed, which is the half of ruling 7 that is mandatory.
#
# FILE-WIDE SOURCE PROSE: THERE IS NONE. Stated explicitly, because no prose and prose missed are otherwise indistinguishable -- the same reason ruling 5 gave comment_coverage.py a zero-line floor.
#
# EXTRA-OK: 'run_supports_set_constructor_iteration_in_js_input.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_set_constructor_iteration_in_jsx_and_tsx_input__main_jsx.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_set_constructor_iteration_in_jsx_and_tsx_input__main_tsx.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_set_constructor_iteration_in_ts_input.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_set_constructor_iteration_in_js_input.test.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_set_constructor_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_set_constructor_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_set_constructor_iteration_in_ts_input.test.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
#
# CONSEQUENCE FOR THE GATES -- `comment_coverage.py` IS EXPECTED-RED (rc=1) ON THIS PAIR [classes: per-case]. The checker asks whether every source comment line appears in EVERY case's rationale, and reports two different things that way. PER-CASE ATTRIBUTION (`from N/M cases`): a comment attached to one `#[test]` fn belongs in that case's rationale and nowhere else, and U6 calls copying all of a file's comments into all of its cases over-attribution that is forbidden even though it turns the checker green. FILE-WIDE PROSE IN THE HEADER (`from ALL N`): prose describing the whole file goes in this `#` header, which the checker deliberately does not read as coverage. U6 anticipates the first exactly: on such a file the checker's false MISSING report is documented in the header. THE CLASS LIST IS GATED, not asserted -- gen_task19_batch4's declaration check re-runs the checker and requires the classes its output contains to match the ones named here.
#
#   python3 scripts/audit-case-migration.py crates/kali_cli/tests/set_iteration_runtime.rs \
#     crates/kali_cli/tests/cases/misc/set_iteration_runtime.toml
#

[source]
"run_supports_set_constructor_iteration_in_js_input.js" = """
const values = [1, 2, 1]; const setAlias = Set; const wrappedSetAlias = (setAlias); const frozenSet = Object.freeze(Set); const frozenGlobalThisSet = Object.freeze(globalThis.Set); const frozenGlobalThisBracketedSet = Object.freeze(globalThis["Set"]); const frozenSingleBracketedSet = Object.freeze(globalThis['Set']); const frozenParenthesizedSet = Object.freeze((Set)); const frozenParenthesizedGlobalThisSet = Object.freeze((globalThis.Set)); const frozenParenthesizedGlobalThisBracketedSet = Object.freeze((globalThis["Set"])); const frozenParenthesizedSingleBracketedSet = Object.freeze((globalThis['Set'])); for (const value of new Set(values)) { console.log(value); } for (const value of new setAlias(values)) { console.log(value); } for (const value of new globalThis['Set'](values)) { console.log(value); } for await (const value of new (wrappedSetAlias)(values)) { console.log(value); } const nullishValues = []; for (const value of new (null ?? Set)(values)) { nullishValues.push(value); } const logicalOrValues = []; for (const value of new (false || Set)(values)) { logicalOrValues.push(value); } if (nullishValues.length !== 2 || nullishValues[0] !== 1 || nullishValues[1] !== 2) { throw new Error('unexpected nullish Set constructor iteration semantics'); } if (logicalOrValues.length !== 2 || logicalOrValues[0] !== 1 || logicalOrValues[1] !== 2) { throw new Error('unexpected logical-or Set constructor iteration semantics'); } const frozenValues = Object.freeze(values); for (const value of new Set(frozenValues)) { console.log(value); } for (const value of new (frozenSet)(values)) { console.log('frozen set constructor iteration ok'); } for (const value of new (frozenGlobalThisSet)(values)) { console.log('frozen globalThis.Set constructor iteration ok'); } for (const value of new (frozenGlobalThisBracketedSet)(values)) { console.log('frozen globalThis["Set"] constructor iteration ok'); } for (const value of new (frozenSingleBracketedSet)(values)) { console.log('frozen globalThis['Set'] constructor iteration ok'); } for (const value of new (frozenParenthesizedSet)(values)) { console.log('frozen parenthesized Set constructor iteration ok'); } for (const value of new (frozenParenthesizedGlobalThisSet)(values)) { console.log('frozen parenthesized globalThis.Set constructor iteration ok'); } for (const value of new (frozenParenthesizedGlobalThisBracketedSet)(values)) { console.log('frozen parenthesized globalThis["Set"] constructor iteration ok'); } for (const value of new (frozenParenthesizedSingleBracketedSet)(values)) { console.log('frozen parenthesized globalThis['Set'] constructor iteration ok'); }
"""
"run_supports_set_constructor_iteration_in_jsx_and_tsx_input__main_jsx.jsx" = """
const values = [1, 2, 1]; const setAlias = Set; const wrappedSetAlias = (setAlias); const frozenSet = Object.freeze(Set); const frozenGlobalThisSet = Object.freeze(globalThis.Set); const frozenGlobalThisBracketedSet = Object.freeze(globalThis["Set"]); const frozenSingleBracketedSet = Object.freeze(globalThis['Set']); const frozenParenthesizedSet = Object.freeze((Set)); const frozenParenthesizedGlobalThisSet = Object.freeze((globalThis.Set)); const frozenParenthesizedGlobalThisBracketedSet = Object.freeze((globalThis["Set"])); const frozenParenthesizedSingleBracketedSet = Object.freeze((globalThis['Set'])); for (const value of new Set(values)) { console.log(value); } for (const value of new setAlias(values)) { console.log(value); } for (const value of new globalThis['Set'](values)) { console.log(value); } for await (const value of new (wrappedSetAlias)(values)) { console.log(value); } const nullishValues = []; for (const value of new (null ?? Set)(values)) { nullishValues.push(value); } const logicalOrValues = []; for (const value of new (false || Set)(values)) { logicalOrValues.push(value); } if (nullishValues.length !== 2 || nullishValues[0] !== 1 || nullishValues[1] !== 2) { throw new Error('unexpected nullish Set constructor iteration semantics'); } if (logicalOrValues.length !== 2 || logicalOrValues[0] !== 1 || logicalOrValues[1] !== 2) { throw new Error('unexpected logical-or Set constructor iteration semantics'); } const frozenValues = Object.freeze(values); for (const value of new Set(frozenValues)) { console.log(value); } for (const value of new (frozenSet)(values)) { console.log('frozen set constructor iteration ok'); } for (const value of new (frozenGlobalThisSet)(values)) { console.log('frozen globalThis.Set constructor iteration ok'); } for (const value of new (frozenGlobalThisBracketedSet)(values)) { console.log('frozen globalThis["Set"] constructor iteration ok'); } for (const value of new (frozenSingleBracketedSet)(values)) { console.log('frozen globalThis['Set'] constructor iteration ok'); } for (const value of new (frozenParenthesizedSet)(values)) { console.log('frozen parenthesized Set constructor iteration ok'); } for (const value of new (frozenParenthesizedGlobalThisSet)(values)) { console.log('frozen parenthesized globalThis.Set constructor iteration ok'); } for (const value of new (frozenParenthesizedGlobalThisBracketedSet)(values)) { console.log('frozen parenthesized globalThis["Set"] constructor iteration ok'); } for (const value of new (frozenParenthesizedSingleBracketedSet)(values)) { console.log('frozen parenthesized globalThis['Set'] constructor iteration ok'); }
"""
"run_supports_set_constructor_iteration_in_jsx_and_tsx_input__main_tsx.tsx" = """
const values = [1, 2, 1]; const setAlias = Set; const wrappedSetAlias = (setAlias); const frozenSet = Object.freeze(Set); const frozenGlobalThisSet = Object.freeze(globalThis.Set); const frozenGlobalThisBracketedSet = Object.freeze(globalThis["Set"]); const frozenSingleBracketedSet = Object.freeze(globalThis['Set']); const frozenParenthesizedSet = Object.freeze((Set)); const frozenParenthesizedGlobalThisSet = Object.freeze((globalThis.Set)); const frozenParenthesizedGlobalThisBracketedSet = Object.freeze((globalThis["Set"])); const frozenParenthesizedSingleBracketedSet = Object.freeze((globalThis['Set'])); for (const value of new Set(values)) { console.log(value); } for (const value of new setAlias(values)) { console.log(value); } for (const value of new globalThis['Set'](values)) { console.log(value); } for await (const value of new (wrappedSetAlias)(values)) { console.log(value); } const nullishValues = []; for (const value of new (null ?? Set)(values)) { nullishValues.push(value); } const logicalOrValues = []; for (const value of new (false || Set)(values)) { logicalOrValues.push(value); } if (nullishValues.length !== 2 || nullishValues[0] !== 1 || nullishValues[1] !== 2) { throw new Error('unexpected nullish Set constructor iteration semantics'); } if (logicalOrValues.length !== 2 || logicalOrValues[0] !== 1 || logicalOrValues[1] !== 2) { throw new Error('unexpected logical-or Set constructor iteration semantics'); } const frozenValues = Object.freeze(values); for (const value of new Set(frozenValues)) { console.log(value); } for (const value of new (frozenSet)(values)) { console.log('frozen set constructor iteration ok'); } for (const value of new (frozenGlobalThisSet)(values)) { console.log('frozen globalThis.Set constructor iteration ok'); } for (const value of new (frozenGlobalThisBracketedSet)(values)) { console.log('frozen globalThis["Set"] constructor iteration ok'); } for (const value of new (frozenSingleBracketedSet)(values)) { console.log('frozen globalThis['Set'] constructor iteration ok'); } for (const value of new (frozenParenthesizedSet)(values)) { console.log('frozen parenthesized Set constructor iteration ok'); } for (const value of new (frozenParenthesizedGlobalThisSet)(values)) { console.log('frozen parenthesized globalThis.Set constructor iteration ok'); } for (const value of new (frozenParenthesizedGlobalThisBracketedSet)(values)) { console.log('frozen parenthesized globalThis["Set"] constructor iteration ok'); } for (const value of new (frozenParenthesizedSingleBracketedSet)(values)) { console.log('frozen parenthesized globalThis['Set'] constructor iteration ok'); }
"""
"run_supports_set_constructor_iteration_in_ts_input.ts" = """
const values = [1, 2, 1]; const setAlias = Set; const wrappedSetAlias = (setAlias); const frozenSet = Object.freeze(Set); const frozenGlobalThisSet = Object.freeze(globalThis.Set); const frozenGlobalThisBracketedSet = Object.freeze(globalThis["Set"]); const frozenSingleBracketedSet = Object.freeze(globalThis['Set']); const frozenParenthesizedSet = Object.freeze((Set)); const frozenParenthesizedGlobalThisSet = Object.freeze((globalThis.Set)); const frozenParenthesizedGlobalThisBracketedSet = Object.freeze((globalThis["Set"])); const frozenParenthesizedSingleBracketedSet = Object.freeze((globalThis['Set'])); for (const value of new Set(values)) { console.log(value); } for (const value of new setAlias(values)) { console.log(value); } for (const value of new globalThis['Set'](values)) { console.log(value); } for await (const value of new (wrappedSetAlias)(values)) { console.log(value); } const nullishValues = []; for (const value of new (null ?? Set)(values)) { nullishValues.push(value); } const logicalOrValues = []; for (const value of new (false || Set)(values)) { logicalOrValues.push(value); } if (nullishValues.length !== 2 || nullishValues[0] !== 1 || nullishValues[1] !== 2) { throw new Error('unexpected nullish Set constructor iteration semantics'); } if (logicalOrValues.length !== 2 || logicalOrValues[0] !== 1 || logicalOrValues[1] !== 2) { throw new Error('unexpected logical-or Set constructor iteration semantics'); } const frozenValues = Object.freeze(values); for (const value of new Set(frozenValues)) { console.log(value); } for (const value of new (frozenSet)(values)) { console.log('frozen set constructor iteration ok'); } for (const value of new (frozenGlobalThisSet)(values)) { console.log('frozen globalThis.Set constructor iteration ok'); } for (const value of new (frozenGlobalThisBracketedSet)(values)) { console.log('frozen globalThis["Set"] constructor iteration ok'); } for (const value of new (frozenSingleBracketedSet)(values)) { console.log('frozen globalThis['Set'] constructor iteration ok'); } for (const value of new (frozenParenthesizedSet)(values)) { console.log('frozen parenthesized Set constructor iteration ok'); } for (const value of new (frozenParenthesizedGlobalThisSet)(values)) { console.log('frozen parenthesized globalThis.Set constructor iteration ok'); } for (const value of new (frozenParenthesizedGlobalThisBracketedSet)(values)) { console.log('frozen parenthesized globalThis["Set"] constructor iteration ok'); } for (const value of new (frozenParenthesizedSingleBracketedSet)(values)) { console.log('frozen parenthesized globalThis['Set'] constructor iteration ok'); }
"""
"test_supports_set_constructor_iteration_in_js_input.test.js" = """
Kali.test('set constructor iteration', () => { const values = [1, 2, 1]; const setAlias = Set; const wrappedSetAlias = (setAlias); const frozenSet = Object.freeze(Set); const frozenGlobalThisSet = Object.freeze(globalThis.Set); const frozenGlobalThisBracketedSet = Object.freeze(globalThis["Set"]); const frozenSingleBracketedSet = Object.freeze(globalThis['Set']); const frozenParenthesizedSet = Object.freeze((Set)); const frozenParenthesizedGlobalThisSet = Object.freeze((globalThis.Set)); const frozenParenthesizedGlobalThisBracketedSet = Object.freeze((globalThis["Set"])); const frozenParenthesizedSingleBracketedSet = Object.freeze((globalThis['Set'])); for (const value of new Set(values)) { console.log(value); } for (const value of new setAlias(values)) { console.log(value); } for await (const value of new (wrappedSetAlias)(values)) { console.log(value); } const nullishValues = []; for (const value of new (null ?? Set)(values)) { nullishValues.push(value); } const logicalOrValues = []; for (const value of new (false || Set)(values)) { logicalOrValues.push(value); } if (nullishValues.length !== 2 || nullishValues[0] !== 1 || nullishValues[1] !== 2) { throw new Error('unexpected nullish Set constructor iteration semantics'); } if (logicalOrValues.length !== 2 || logicalOrValues[0] !== 1 || logicalOrValues[1] !== 2) { throw new Error('unexpected logical-or Set constructor iteration semantics'); } const frozenValues = Object.freeze(values); for (const value of new Set(frozenValues)) { console.log(value); } for (const value of new (frozenSet)(values)) { console.log('frozen set constructor iteration ok'); } for (const value of new (frozenGlobalThisSet)(values)) { console.log('frozen globalThis.Set constructor iteration ok'); } for (const value of new (frozenGlobalThisBracketedSet)(values)) { console.log('frozen globalThis["Set"] constructor iteration ok'); } for (const value of new (frozenSingleBracketedSet)(values)) { console.log('frozen globalThis[\\'Set\\'] constructor iteration ok'); } for (const value of new (frozenParenthesizedSet)(values)) { console.log('frozen parenthesized Set constructor iteration ok'); } for (const value of new (frozenParenthesizedGlobalThisSet)(values)) { console.log('frozen parenthesized globalThis.Set constructor iteration ok'); } for (const value of new (frozenParenthesizedGlobalThisBracketedSet)(values)) { console.log('frozen parenthesized globalThis["Set"] constructor iteration ok'); } for (const value of new (frozenParenthesizedSingleBracketedSet)(values)) { console.log('frozen parenthesized globalThis[\\'Set\\'] constructor iteration ok'); } });
"""
"test_supports_set_constructor_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx" = """
Kali.test('set constructor iteration', () => { const values = [1, 2, 1]; const setAlias = Set; const wrappedSetAlias = (setAlias); const frozenSet = Object.freeze(Set); const frozenGlobalThisSet = Object.freeze(globalThis.Set); const frozenGlobalThisBracketedSet = Object.freeze(globalThis["Set"]); const frozenSingleBracketedSet = Object.freeze(globalThis['Set']); const frozenParenthesizedSet = Object.freeze((Set)); const frozenParenthesizedGlobalThisSet = Object.freeze((globalThis.Set)); const frozenParenthesizedGlobalThisBracketedSet = Object.freeze((globalThis["Set"])); const frozenParenthesizedSingleBracketedSet = Object.freeze((globalThis['Set'])); for (const value of new Set(values)) { console.log(value); } for (const value of new setAlias(values)) { console.log(value); } for await (const value of new (wrappedSetAlias)(values)) { console.log(value); } const nullishValues = []; for (const value of new (null ?? Set)(values)) { nullishValues.push(value); } const logicalOrValues = []; for (const value of new (false || Set)(values)) { logicalOrValues.push(value); } if (nullishValues.length !== 2 || nullishValues[0] !== 1 || nullishValues[1] !== 2) { throw new Error('unexpected nullish Set constructor iteration semantics'); } if (logicalOrValues.length !== 2 || logicalOrValues[0] !== 1 || logicalOrValues[1] !== 2) { throw new Error('unexpected logical-or Set constructor iteration semantics'); } const frozenValues = Object.freeze(values); for (const value of new Set(frozenValues)) { console.log(value); } for (const value of new (frozenSet)(values)) { console.log('frozen set constructor iteration ok'); } for (const value of new (frozenGlobalThisSet)(values)) { console.log('frozen globalThis.Set constructor iteration ok'); } for (const value of new (frozenGlobalThisBracketedSet)(values)) { console.log('frozen globalThis["Set"] constructor iteration ok'); } for (const value of new (frozenSingleBracketedSet)(values)) { console.log('frozen globalThis[\\'Set\\'] constructor iteration ok'); } for (const value of new (frozenParenthesizedSet)(values)) { console.log('frozen parenthesized Set constructor iteration ok'); } for (const value of new (frozenParenthesizedGlobalThisSet)(values)) { console.log('frozen parenthesized globalThis.Set constructor iteration ok'); } for (const value of new (frozenParenthesizedGlobalThisBracketedSet)(values)) { console.log('frozen parenthesized globalThis["Set"] constructor iteration ok'); } for (const value of new (frozenParenthesizedSingleBracketedSet)(values)) { console.log('frozen parenthesized globalThis[\\'Set\\'] constructor iteration ok'); } });
"""
"test_supports_set_constructor_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx" = """
Kali.test('set constructor iteration', () => { const values = [1, 2, 1]; const setAlias = Set; const wrappedSetAlias = (setAlias); const frozenSet = Object.freeze(Set); const frozenGlobalThisSet = Object.freeze(globalThis.Set); const frozenGlobalThisBracketedSet = Object.freeze(globalThis["Set"]); const frozenSingleBracketedSet = Object.freeze(globalThis['Set']); const frozenParenthesizedSet = Object.freeze((Set)); const frozenParenthesizedGlobalThisSet = Object.freeze((globalThis.Set)); const frozenParenthesizedGlobalThisBracketedSet = Object.freeze((globalThis["Set"])); const frozenParenthesizedSingleBracketedSet = Object.freeze((globalThis['Set'])); for (const value of new Set(values)) { console.log(value); } for (const value of new setAlias(values)) { console.log(value); } for await (const value of new (wrappedSetAlias)(values)) { console.log(value); } const nullishValues = []; for (const value of new (null ?? Set)(values)) { nullishValues.push(value); } const logicalOrValues = []; for (const value of new (false || Set)(values)) { logicalOrValues.push(value); } if (nullishValues.length !== 2 || nullishValues[0] !== 1 || nullishValues[1] !== 2) { throw new Error('unexpected nullish Set constructor iteration semantics'); } if (logicalOrValues.length !== 2 || logicalOrValues[0] !== 1 || logicalOrValues[1] !== 2) { throw new Error('unexpected logical-or Set constructor iteration semantics'); } const frozenValues = Object.freeze(values); for (const value of new Set(frozenValues)) { console.log(value); } for (const value of new (frozenSet)(values)) { console.log('frozen set constructor iteration ok'); } for (const value of new (frozenGlobalThisSet)(values)) { console.log('frozen globalThis.Set constructor iteration ok'); } for (const value of new (frozenGlobalThisBracketedSet)(values)) { console.log('frozen globalThis["Set"] constructor iteration ok'); } for (const value of new (frozenSingleBracketedSet)(values)) { console.log('frozen globalThis[\\'Set\\'] constructor iteration ok'); } for (const value of new (frozenParenthesizedSet)(values)) { console.log('frozen parenthesized Set constructor iteration ok'); } for (const value of new (frozenParenthesizedGlobalThisSet)(values)) { console.log('frozen parenthesized globalThis.Set constructor iteration ok'); } for (const value of new (frozenParenthesizedGlobalThisBracketedSet)(values)) { console.log('frozen parenthesized globalThis["Set"] constructor iteration ok'); } for (const value of new (frozenParenthesizedSingleBracketedSet)(values)) { console.log('frozen parenthesized globalThis[\\'Set\\'] constructor iteration ok'); } });
"""
"test_supports_set_constructor_iteration_in_ts_input.test.ts" = """
Kali.test('set constructor iteration', () => { const values = [1, 2, 1]; const setAlias = Set; const wrappedSetAlias = (setAlias); const frozenSet = Object.freeze(Set); const frozenGlobalThisSet = Object.freeze(globalThis.Set); const frozenGlobalThisBracketedSet = Object.freeze(globalThis["Set"]); const frozenSingleBracketedSet = Object.freeze(globalThis['Set']); const frozenParenthesizedSet = Object.freeze((Set)); const frozenParenthesizedGlobalThisSet = Object.freeze((globalThis.Set)); const frozenParenthesizedGlobalThisBracketedSet = Object.freeze((globalThis["Set"])); const frozenParenthesizedSingleBracketedSet = Object.freeze((globalThis['Set'])); for (const value of new Set(values)) { console.log(value); } for (const value of new setAlias(values)) { console.log(value); } for await (const value of new (wrappedSetAlias)(values)) { console.log(value); } const nullishValues = []; for (const value of new (null ?? Set)(values)) { nullishValues.push(value); } const logicalOrValues = []; for (const value of new (false || Set)(values)) { logicalOrValues.push(value); } if (nullishValues.length !== 2 || nullishValues[0] !== 1 || nullishValues[1] !== 2) { throw new Error('unexpected nullish Set constructor iteration semantics'); } if (logicalOrValues.length !== 2 || logicalOrValues[0] !== 1 || logicalOrValues[1] !== 2) { throw new Error('unexpected logical-or Set constructor iteration semantics'); } const frozenValues = Object.freeze(values); for (const value of new Set(frozenValues)) { console.log(value); } for (const value of new (frozenSet)(values)) { console.log('frozen set constructor iteration ok'); } for (const value of new (frozenGlobalThisSet)(values)) { console.log('frozen globalThis.Set constructor iteration ok'); } for (const value of new (frozenGlobalThisBracketedSet)(values)) { console.log('frozen globalThis["Set"] constructor iteration ok'); } for (const value of new (frozenSingleBracketedSet)(values)) { console.log('frozen globalThis[\\'Set\\'] constructor iteration ok'); } for (const value of new (frozenParenthesizedSet)(values)) { console.log('frozen parenthesized Set constructor iteration ok'); } for (const value of new (frozenParenthesizedGlobalThisSet)(values)) { console.log('frozen parenthesized globalThis.Set constructor iteration ok'); } for (const value of new (frozenParenthesizedGlobalThisBracketedSet)(values)) { console.log('frozen parenthesized globalThis["Set"] constructor iteration ok'); } for (const value of new (frozenParenthesizedSingleBracketedSet)(values)) { console.log('frozen parenthesized globalThis[\\'Set\\'] constructor iteration ok'); } });
"""

[[case]]
name = "run_supports_set_constructor_iteration_in_js_input"
rationale = "Migrated 1:1 from `set_iteration_runtime.rs`'s `#[test] fn run_supports_set_constructor_iteration_in_js_input`. The source asserts the command fails closed. It asserts that stderr CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3). RULE 11 -- the source's assertion is CROSS-STREAM and the format has no such surface, so it was resolved against the real binary and pinned to the stream that carries it. The source's own sentence, carried verbatim: `assert!( stderr.contains(\"E4000\") || stdout.contains(\"E4000\"), \"stdout: {stdout}\\nstderr: {stderr}\" )`. Observed on every cell: the needle appears on stderr and not on the other stream, so pinning stderr is a verified strengthening (every run satisfying the new assertion satisfies the old). This is a PRESENCE claim; rule 2 forbids the same narrowing of an absence claim. Batch-local variant (PR #16 rev2, batch 7): `assert_set_iteration` above is shared with the `test_supports_set_constructor_iteration_in_*` fns below, which are out of this batch and currently green, so the shared helper is left untouched. These 3 in-batch `run_supports_*` members all fail closed/loud: the fixture's own self-check throws (`Uncaught Error: unexpected nullish Set constructor iteration semantics`) and lowers to a wasm `error[E4000]` runtime trap, nonzero exit. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md."
args = ["run", "run_supports_set_constructor_iteration_in_js_input.js"]
exit = "failure"
stderr_contains = ["E4000"]

[[case]]
name = "run_supports_set_constructor_iteration_in_ts_input"
rationale = "Migrated 1:1 from `set_iteration_runtime.rs`'s `#[test] fn run_supports_set_constructor_iteration_in_ts_input`. The source asserts the command fails closed. It asserts that stderr CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3). RULE 11 -- the source's assertion is CROSS-STREAM and the format has no such surface, so it was resolved against the real binary and pinned to the stream that carries it. The source's own sentence, carried verbatim: `assert!( stderr.contains(\"E4000\") || stdout.contains(\"E4000\"), \"stdout: {stdout}\\nstderr: {stderr}\" )`. Observed on every cell: the needle appears on stderr and not on the other stream, so pinning stderr is a verified strengthening (every run satisfying the new assertion satisfies the old). This is a PRESENCE claim; rule 2 forbids the same narrowing of an absence claim. Batch-local variant (PR #16 rev2, batch 7): `assert_set_iteration` above is shared with the `test_supports_set_constructor_iteration_in_*` fns below, which are out of this batch and currently green, so the shared helper is left untouched. These 3 in-batch `run_supports_*` members all fail closed/loud: the fixture's own self-check throws (`Uncaught Error: unexpected nullish Set constructor iteration semantics`) and lowers to a wasm `error[E4000]` runtime trap, nonzero exit. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md."
args = ["run", "run_supports_set_constructor_iteration_in_ts_input.ts"]
exit = "failure"
stderr_contains = ["E4000"]

[[case]]
name = "test_supports_set_constructor_iteration_in_js_input"
rationale = "Migrated 1:1 from `set_iteration_runtime.rs`'s `#[test] fn test_supports_set_constructor_iteration_in_js_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_set_constructor_iteration_in_js_input.test.js"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_set_constructor_iteration_in_ts_input"
rationale = "Migrated 1:1 from `set_iteration_runtime.rs`'s `#[test] fn test_supports_set_constructor_iteration_in_ts_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_set_constructor_iteration_in_ts_input.test.ts"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "run_supports_set_constructor_iteration_in_jsx_and_tsx_input__main_jsx"
rationale = "Migrated 1:1 from `set_iteration_runtime.rs`'s `#[test] fn run_supports_set_constructor_iteration_in_jsx_and_tsx_input`. The source asserts the command fails closed. It asserts that stderr CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3). RULE 11 -- the source's assertion is CROSS-STREAM and the format has no such surface, so it was resolved against the real binary and pinned to the stream that carries it. The source's own sentence, carried verbatim: `assert!( stderr.contains(\"E4000\") || stdout.contains(\"E4000\"), \"stdout: {stdout}\\nstderr: {stderr}\" )`. Observed on every cell: the needle appears on stderr and not on the other stream, so pinning stderr is a verified strengthening (every run satisfying the new assertion satisfies the old). This is a PRESENCE claim; rule 2 forbids the same narrowing of an absence claim. Batch-local variant (PR #16 rev2, batch 7): `assert_set_iteration` above is shared with the `test_supports_set_constructor_iteration_in_*` fns below, which are out of this batch and currently green, so the shared helper is left untouched. These 3 in-batch `run_supports_*` members all fail closed/loud: the fixture's own self-check throws (`Uncaught Error: unexpected nullish Set constructor iteration semantics`) and lowers to a wasm `error[E4000]` runtime trap, nonzero exit. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md."
args = ["run", "run_supports_set_constructor_iteration_in_jsx_and_tsx_input__main_jsx.jsx"]
exit = "failure"
stderr_contains = ["E4000"]

[[case]]
name = "run_supports_set_constructor_iteration_in_jsx_and_tsx_input__main_tsx"
rationale = "Migrated 1:1 from `set_iteration_runtime.rs`'s `#[test] fn run_supports_set_constructor_iteration_in_jsx_and_tsx_input`. The source asserts the command fails closed. It asserts that stderr CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3). RULE 11 -- the source's assertion is CROSS-STREAM and the format has no such surface, so it was resolved against the real binary and pinned to the stream that carries it. The source's own sentence, carried verbatim: `assert!( stderr.contains(\"E4000\") || stdout.contains(\"E4000\"), \"stdout: {stdout}\\nstderr: {stderr}\" )`. Observed on every cell: the needle appears on stderr and not on the other stream, so pinning stderr is a verified strengthening (every run satisfying the new assertion satisfies the old). This is a PRESENCE claim; rule 2 forbids the same narrowing of an absence claim. Batch-local variant (PR #16 rev2, batch 7): `assert_set_iteration` above is shared with the `test_supports_set_constructor_iteration_in_*` fns below, which are out of this batch and currently green, so the shared helper is left untouched. These 3 in-batch `run_supports_*` members all fail closed/loud: the fixture's own self-check throws (`Uncaught Error: unexpected nullish Set constructor iteration semantics`) and lowers to a wasm `error[E4000]` runtime trap, nonzero exit. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md."
args = ["run", "run_supports_set_constructor_iteration_in_jsx_and_tsx_input__main_tsx.tsx"]
exit = "failure"
stderr_contains = ["E4000"]

[[case]]
name = "test_supports_set_constructor_iteration_in_jsx_and_tsx_input__smoke_test_jsx"
rationale = "Migrated 1:1 from `set_iteration_runtime.rs`'s `#[test] fn test_supports_set_constructor_iteration_in_jsx_and_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_set_constructor_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_set_constructor_iteration_in_jsx_and_tsx_input__smoke_test_tsx"
rationale = "Migrated 1:1 from `set_iteration_runtime.rs`'s `#[test] fn test_supports_set_constructor_iteration_in_jsx_and_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_set_constructor_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx"]
exit = "success"
stdout_contains = ["ok 1"]
