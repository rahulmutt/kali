# Migrated from tests/for_of_object_keys_iteration.rs.
#   SOURCE REF: 7f57e0ed87ac9eaa0d05de88816f1fb1fdf6ef15
#
# MATRIX DECLINED. 108 source helper invocation(s) == 108 case(s) x 1. No axis every case in this file varies over uniformly (rule 7, U1), so `[matrix]` is dropped for the WHOLE file and the invocations are written as named siblings -- rule 5's split, one `[[case]]` per real helper invocation. The predicate that decides this is gen_task19_batch4.axis_for and it is mechanical; the arithmetic is asserted by gen_task19_batch4.arithmetic, so this header cannot state arithmetic that does not close.
#
# THE SHAPE THIS FILE WAS MIGRATED FROM, AND WHAT DERIVED ITS CLAIMS. for_of_object_keys_iteration.rs is a MULTI-BUILDER, MULTI-MODE source: fixture-builder fns returning `&'static str`, assert-helpers that build one `Command` each, and `#[test]` fns that call them once or loop over a list of filenames. Batch 3's one-helper shape table does not fit it, so the claim set of every case below is derived by tools/migration/t19b4_extract.py -- a small INTERPRETER over a closed statement and expression language, not a pattern match over one call. Reproduce with:
#
#   python3 tools/migration/t19b4_extract.py for_of_object_keys_iteration
#
# THE TABLE IS CLOSED OVER CLAIMS, NOT OVER `assert*!` MACROS. Enumerating assertion macros says nothing about a claim written as control flow, carried by an `.expect()`, or made outside any macro; a source using one would migrate silently short. t19b4_extract.residual_claims blanks every assert span, every permitted whole statement and every permitted call, then REFUSES on what is left. Every construct outside the statement/expression language raises as well, so an unmodelled source shape breaks the generator instead of quietly under-claiming. An `assert!`'s second and later arguments are its PANIC MESSAGE, which the program under test never sees; they are not claims and are not migrated.
#
# ASSERTION STRENGTH -- RULING 3, MIRROR THE SOURCE. An exact `assert_eq!` on the whole of a stream becomes an exact pin; an is_empty() assertion on a stream becomes an exact `""` pin, which is the same exact-stream discipline used elsewhere and not a rule-2 invention; a plain `.contains` against a stream stays a `*_contains` and is NOT strengthened because the exact output was observed. Pinned text is COPIED out of the source's own literal by lexer.find_string_literals -- it is not a live capture and this header does not claim it is.
#
# REALISM IS PROVEN SEPARATELY FROM FIDELITY (U9). Every trial in this file runs against the real `kali` binary in the suite, which proves the case matches what the binary does today. It does not prove nothing was dropped -- only the source-vs-TOML direction does that, which is what the derived extraction and check_extra_claims.py are for.
#
# `[source]` KEYS ARE VARIANT-SUFFIXED (U5). `[source]` is one FILE-WIDE namespace that expand() clones into every trial, and this source writes many different programs to the SAME filename, so the programs cannot share a key -- the last body written would win and every other case would silently run the wrong program. Each key is named after the source `#[test]` fn that wrote the program, so the mapping from trial to source fn survives the deletion of the `.rs`. The WHOLE suffix chain is preserved (`.test.js`, not `.js`), because `kali test` dispatches on the `.test.` infix and U5 permits renaming an argv filename, not renaming one whose shape the tool reads. No program in this file references a sibling filename by string, which is the check U5 actually asks for and which gen_task19_batch4.check_no_fixture_names_referenced runs over every fixture body rather than over argv alone.
#
# U2 CHECK, RUN RATHER THAN ASSUMED. Every sibling case's program is present in this case's trial directory. That is inert here and the controls were run: every case names its own program explicitly on argv, exactly as the source did, so a sibling is never read; and `kali.json` IS auto-discovered as a manifest and would not be inert, which gen_task19_batch4.check_no_manifest_named_fixture asserts rather than leaving to inspection. (Tool function names are written unbackticked on purpose: check_rationale_fn_names.py reads a backticked fn-shaped token as a citation into the SOURCE, and a generator's own function is not one.)
#
# RULE 12 ATTRIBUTION, DERIVED FROM THE SOURCE'S OWN LAYOUT (U6). Every comment in the source is carried, and which rationale it lands in is decided by POSITION rather than per-comment judgement: a paragraph inside a `#[test]` body, or directly abutting it, goes into that case's rationale and no other; a paragraph attached to a helper goes into the rationale of every case that helper's call path reaches, which the evaluator RECORDS rather than guesses; everything else is file-wide and is carried in this `#` header. Over-attribution is forbidden even though it turns comment_coverage.py green (U6). Trailing comments (U16) are attributed by line to whichever body encloses them.
#
# U13, BOTH HALVES, MEASURED RATHER THAN EYEBALLED. Over this file's 108 `[source]` bodies: **174 byte-identical pair(s)** and a **longest shared leading-line prefix of 31 line(s)**. The hoist into `[constants]` is DECLINED (ruling 7), and the reason is measured rather than inherited: check_fixtures.py's toml_program_texts reads `[source]` values VERBATIM and never resolves a `${NAME}` reference, so a hoisted body makes the rule-9 fixture gate report UNMATCHED on a correct file. Ruling 7 states that for `browser/`; the property is the tool's, not the family's. The identity is asserted mechanically by this generator's u13_measure rather than eyeballed, which is the half of ruling 7 that is mandatory.
#
# FILE-WIDE SOURCE PROSE: THERE IS NONE. Stated explicitly, because no prose and prose missed are otherwise indistinguishable -- the same reason ruling 5 gave comment_coverage.py a zero-line floor.
#
# EXTRA-OK: 'run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__main_js.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__main_jsx.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__main_ts.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__main_tsx.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_direct_object_keys_iteration_in_jsx_and_tsx_input__main_jsx.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_direct_object_keys_iteration_in_jsx_and_tsx_input__main_tsx.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__main_js.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__main_jsx.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__main_ts.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__main_tsx.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__main_js.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__main_jsx.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__main_ts.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__main_tsx.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__main_js.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__main_jsx.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__main_ts.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__main_tsx.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_global_object_keys_iteration_in_js_input.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_global_object_keys_iteration_in_jsx_and_tsx_input__main_jsx.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_global_object_keys_iteration_in_jsx_and_tsx_input__main_tsx.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__main_jsx.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__main_ts.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__main_tsx.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_entries_from_entries_iteration_in_js_input.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input__main_jsx.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input__main_tsx.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_entries_from_entries_iteration_in_ts_input.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__main_js.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__main_jsx.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__main_ts.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__main_tsx.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_keys_from_entries_iteration_in_js_input.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input__main_jsx.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input__main_tsx.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_keys_from_entries_iteration_in_ts_input.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_keys_iteration_in_js_input.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_keys_iteration_in_jsx_and_tsx_input__main_jsx.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_keys_iteration_in_jsx_and_tsx_input__main_tsx.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_keys_iteration_in_ts_input.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_keys_iteration_with_direct_literal_object_in_js_input.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_keys_iteration_with_direct_literal_object_in_ts_input.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__main_js.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__main_jsx.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__main_ts.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__main_tsx.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_values_from_entries_iteration_in_js_input.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input__main_jsx.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input__main_tsx.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_values_from_entries_iteration_in_ts_input.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_values_iteration_in_js_input.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_values_iteration_in_jsx_and_tsx_input__main_jsx.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_values_iteration_in_jsx_and_tsx_input__main_tsx.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'run_supports_object_values_iteration_in_ts_input.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__smoke_test_js.test.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__smoke_test_jsx.test.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__smoke_test_ts.test.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__smoke_test_tsx.test.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_direct_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_direct_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__smoke_test_js.test.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx.test.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts.test.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx.test.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_js.test.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx.test.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts.test.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx.test.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__smoke_test_js.test.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx.test.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts.test.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx.test.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_global_object_keys_iteration_in_js_input.test.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_global_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_global_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__smoke_test_jsx.test.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__smoke_test_ts.test.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__smoke_test_tsx.test.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_entries_from_entries_iteration_in_js_input.test.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_entries_from_entries_iteration_in_ts_input.test.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__smoke_test_js.test.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx.test.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts.test.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx.test.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_keys_from_entries_iteration_in_js_input.test.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_keys_from_entries_iteration_in_ts_input.test.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_keys_iteration_in_js_input.test.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_keys_iteration_in_ts_input.test.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_keys_iteration_with_direct_literal_object_in_js_input.test.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_keys_iteration_with_direct_literal_object_in_ts_input.test.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_js.test.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx.test.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts.test.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx.test.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_values_from_entries_iteration_in_js_input.test.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_values_from_entries_iteration_in_ts_input.test.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_values_iteration_in_js_input.test.js' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_values_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_values_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
# EXTRA-OK: 'test_supports_object_values_iteration_in_ts_input.test.ts' -- a U5 variant-suffixed `[source]` key surfaced as an argv token; it is a fixture FILENAME named after the source `#[test]` fn that wrote the program, not a claim about behaviour
#
# CONSEQUENCE FOR THE GATES -- `comment_coverage.py` IS EXPECTED-RED (rc=1) ON THIS PAIR [classes: per-case]. The checker asks whether every source comment line appears in EVERY case's rationale, and reports two different things that way. PER-CASE ATTRIBUTION (`from N/M cases`): a comment attached to one `#[test]` fn belongs in that case's rationale and nowhere else, and U6 calls copying all of a file's comments into all of its cases over-attribution that is forbidden even though it turns the checker green. FILE-WIDE PROSE IN THE HEADER (`from ALL N`): prose describing the whole file goes in this `#` header, which the checker deliberately does not read as coverage. U6 anticipates the first exactly: on such a file the checker's false `missing` is documented in the header. THE CLASS LIST IS GATED, not asserted -- gen_task19_batch4's declaration check re-runs the checker and requires the classes its output contains to match the ones named here.
#
#   python3 scripts/audit-case-migration.py crates/kali_cli/tests/for_of_object_keys_iteration.rs \
#     crates/kali_cli/tests/cases/misc/for_of_object_keys_iteration.toml
#

[source]
"run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__main_js.js" = """
async function main() {
  if (!Object.is(await 1, await 1)) {
    throw new Error('unexpected await-wrapped Object.is semantics');
  }
  if (!Number.isSafeInteger(await 1)) {
    throw new Error('unexpected await-wrapped Number.isSafeInteger semantics');
  }
  if (!Object.hasOwn((0, { a: 1 }), 'a')) {
    throw new Error('unexpected sequence-wrapped Object.hasOwn semantics');
  }
  const keys = Object.keys(await { a: 1 });
  if (keys.length !== 1 || keys[0] !== 'a') {
    throw new Error('unexpected await-wrapped Object.keys semantics');
  }
  const sequencedKeys = Object.keys((0, { a: 1 }));
  if (sequencedKeys.length !== 1 || sequencedKeys[0] !== 'a') {
    throw new Error('unexpected sequence-wrapped Object.keys semantics');
  }
  const ownKeys = Reflect.ownKeys(await Object.freeze({ b: 1, a: 2 }));
  if (ownKeys.length !== 2 || ownKeys[0] !== 'b' || ownKeys[1] !== 'a') {
    throw new Error('unexpected await-wrapped Reflect.ownKeys semantics');
  }
  const sequencedOwnKeys = Reflect.ownKeys((0, Object.freeze({ b: 1, a: 2 })));
  if (sequencedOwnKeys.length !== 2 || sequencedOwnKeys[0] !== 'b' || sequencedOwnKeys[1] !== 'a') {
    throw new Error('unexpected sequence-wrapped Reflect.ownKeys semantics');
  }
}
main();
"""
"run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__main_jsx.jsx" = """
async function main() {
  if (!Object.is(await 1, await 1)) {
    throw new Error('unexpected await-wrapped Object.is semantics');
  }
  if (!Number.isSafeInteger(await 1)) {
    throw new Error('unexpected await-wrapped Number.isSafeInteger semantics');
  }
  if (!Object.hasOwn((0, { a: 1 }), 'a')) {
    throw new Error('unexpected sequence-wrapped Object.hasOwn semantics');
  }
  const keys = Object.keys(await { a: 1 });
  if (keys.length !== 1 || keys[0] !== 'a') {
    throw new Error('unexpected await-wrapped Object.keys semantics');
  }
  const sequencedKeys = Object.keys((0, { a: 1 }));
  if (sequencedKeys.length !== 1 || sequencedKeys[0] !== 'a') {
    throw new Error('unexpected sequence-wrapped Object.keys semantics');
  }
  const ownKeys = Reflect.ownKeys(await Object.freeze({ b: 1, a: 2 }));
  if (ownKeys.length !== 2 || ownKeys[0] !== 'b' || ownKeys[1] !== 'a') {
    throw new Error('unexpected await-wrapped Reflect.ownKeys semantics');
  }
  const sequencedOwnKeys = Reflect.ownKeys((0, Object.freeze({ b: 1, a: 2 })));
  if (sequencedOwnKeys.length !== 2 || sequencedOwnKeys[0] !== 'b' || sequencedOwnKeys[1] !== 'a') {
    throw new Error('unexpected sequence-wrapped Reflect.ownKeys semantics');
  }
}
main();
"""
"run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__main_ts.ts" = """
async function main() {
  if (!Object.is(await 1, await 1)) {
    throw new Error('unexpected await-wrapped Object.is semantics');
  }
  if (!Number.isSafeInteger(await 1)) {
    throw new Error('unexpected await-wrapped Number.isSafeInteger semantics');
  }
  if (!Object.hasOwn((0, { a: 1 }), 'a')) {
    throw new Error('unexpected sequence-wrapped Object.hasOwn semantics');
  }
  const keys = Object.keys(await { a: 1 });
  if (keys.length !== 1 || keys[0] !== 'a') {
    throw new Error('unexpected await-wrapped Object.keys semantics');
  }
  const sequencedKeys = Object.keys((0, { a: 1 }));
  if (sequencedKeys.length !== 1 || sequencedKeys[0] !== 'a') {
    throw new Error('unexpected sequence-wrapped Object.keys semantics');
  }
  const ownKeys = Reflect.ownKeys(await Object.freeze({ b: 1, a: 2 }));
  if (ownKeys.length !== 2 || ownKeys[0] !== 'b' || ownKeys[1] !== 'a') {
    throw new Error('unexpected await-wrapped Reflect.ownKeys semantics');
  }
  const sequencedOwnKeys = Reflect.ownKeys((0, Object.freeze({ b: 1, a: 2 })));
  if (sequencedOwnKeys.length !== 2 || sequencedOwnKeys[0] !== 'b' || sequencedOwnKeys[1] !== 'a') {
    throw new Error('unexpected sequence-wrapped Reflect.ownKeys semantics');
  }
}
main();
"""
"run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__main_tsx.tsx" = """
async function main() {
  if (!Object.is(await 1, await 1)) {
    throw new Error('unexpected await-wrapped Object.is semantics');
  }
  if (!Number.isSafeInteger(await 1)) {
    throw new Error('unexpected await-wrapped Number.isSafeInteger semantics');
  }
  if (!Object.hasOwn((0, { a: 1 }), 'a')) {
    throw new Error('unexpected sequence-wrapped Object.hasOwn semantics');
  }
  const keys = Object.keys(await { a: 1 });
  if (keys.length !== 1 || keys[0] !== 'a') {
    throw new Error('unexpected await-wrapped Object.keys semantics');
  }
  const sequencedKeys = Object.keys((0, { a: 1 }));
  if (sequencedKeys.length !== 1 || sequencedKeys[0] !== 'a') {
    throw new Error('unexpected sequence-wrapped Object.keys semantics');
  }
  const ownKeys = Reflect.ownKeys(await Object.freeze({ b: 1, a: 2 }));
  if (ownKeys.length !== 2 || ownKeys[0] !== 'b' || ownKeys[1] !== 'a') {
    throw new Error('unexpected await-wrapped Reflect.ownKeys semantics');
  }
  const sequencedOwnKeys = Reflect.ownKeys((0, Object.freeze({ b: 1, a: 2 })));
  if (sequencedOwnKeys.length !== 2 || sequencedOwnKeys[0] !== 'b' || sequencedOwnKeys[1] !== 'a') {
    throw new Error('unexpected sequence-wrapped Reflect.ownKeys semantics');
  }
}
main();
"""
"run_supports_direct_object_keys_iteration_in_jsx_and_tsx_input__main_jsx.jsx" = """
const keys = [];
for (const key of Object.keys({ "b": 1, "a": 2 })) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
  throw new Error('unexpected Object.keys iteration semantics');
}
"""
"run_supports_direct_object_keys_iteration_in_jsx_and_tsx_input__main_tsx.tsx" = """
const keys = [];
for (const key of Object.keys({ "b": 1, "a": 2 })) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
  throw new Error('unexpected Object.keys iteration semantics');
}
"""
"run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__main_js.js" = """
const seen = [];
for (const entry of Object.entries(Object.freeze({ "b": 1, "a": 2 }))) {
  seen.push(entry[0]);
  seen.push(entry[1]);
}
if (seen.length !== 4 || seen[0] !== 'b' || seen[1] !== 1 || seen[2] !== 'a' || seen[3] !== 2) {
  throw new Error('unexpected frozen Object.entries iteration semantics');
}
"""
"run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__main_jsx.jsx" = """
const seen = [];
for (const entry of Object.entries(Object.freeze({ "b": 1, "a": 2 }))) {
  seen.push(entry[0]);
  seen.push(entry[1]);
}
if (seen.length !== 4 || seen[0] !== 'b' || seen[1] !== 1 || seen[2] !== 'a' || seen[3] !== 2) {
  throw new Error('unexpected frozen Object.entries iteration semantics');
}
"""
"run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__main_ts.ts" = """
const seen = [];
for (const entry of Object.entries(Object.freeze({ "b": 1, "a": 2 }))) {
  seen.push(entry[0]);
  seen.push(entry[1]);
}
if (seen.length !== 4 || seen[0] !== 'b' || seen[1] !== 1 || seen[2] !== 'a' || seen[3] !== 2) {
  throw new Error('unexpected frozen Object.entries iteration semantics');
}
"""
"run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__main_tsx.tsx" = """
const seen = [];
for (const entry of Object.entries(Object.freeze({ "b": 1, "a": 2 }))) {
  seen.push(entry[0]);
  seen.push(entry[1]);
}
if (seen.length !== 4 || seen[0] !== 'b' || seen[1] !== 1 || seen[2] !== 'a' || seen[3] !== 2) {
  throw new Error('unexpected frozen Object.entries iteration semantics');
}
"""
"run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__main_js.js" = """
const keys = [];
for (const key of Object.keys(Object.freeze({ a: 1, b: 2 }))) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'a' || keys[1] !== 'b') {
  throw new Error('unexpected frozen object enumeration semantics');
}
"""
"run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__main_jsx.jsx" = """
const keys = [];
for (const key of Object.keys(Object.freeze({ a: 1, b: 2 }))) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'a' || keys[1] !== 'b') {
  throw new Error('unexpected frozen object enumeration semantics');
}
"""
"run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__main_ts.ts" = """
const keys = [];
for (const key of Object.keys(Object.freeze({ a: 1, b: 2 }))) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'a' || keys[1] !== 'b') {
  throw new Error('unexpected frozen object enumeration semantics');
}
"""
"run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__main_tsx.tsx" = """
const keys = [];
for (const key of Object.keys(Object.freeze({ a: 1, b: 2 }))) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'a' || keys[1] !== 'b') {
  throw new Error('unexpected frozen object enumeration semantics');
}
"""
"run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__main_js.js" = """
const seen = [];
for (const value of Object.values(Object.freeze({ "b": 1, "a": 2 }))) {
  seen.push(value);
}
if (seen.length !== 2 || seen[0] !== 1 || seen[1] !== 2) {
  throw new Error('unexpected frozen Object.values iteration semantics');
}
"""
"run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__main_jsx.jsx" = """
const seen = [];
for (const value of Object.values(Object.freeze({ "b": 1, "a": 2 }))) {
  seen.push(value);
}
if (seen.length !== 2 || seen[0] !== 1 || seen[1] !== 2) {
  throw new Error('unexpected frozen Object.values iteration semantics');
}
"""
"run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__main_ts.ts" = """
const seen = [];
for (const value of Object.values(Object.freeze({ "b": 1, "a": 2 }))) {
  seen.push(value);
}
if (seen.length !== 2 || seen[0] !== 1 || seen[1] !== 2) {
  throw new Error('unexpected frozen Object.values iteration semantics');
}
"""
"run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__main_tsx.tsx" = """
const seen = [];
for (const value of Object.values(Object.freeze({ "b": 1, "a": 2 }))) {
  seen.push(value);
}
if (seen.length !== 2 || seen[0] !== 1 || seen[1] !== 2) {
  throw new Error('unexpected frozen Object.values iteration semantics');
}
"""
"run_supports_global_object_keys_iteration_in_js_input.js" = """
const values = { "b": 1, "a": 2 };
const alias = values;
const keys = [];
for (const key of globalThis.Object.keys(alias)) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
  throw new Error('unexpected Object.keys iteration semantics');
}
"""
"run_supports_global_object_keys_iteration_in_jsx_and_tsx_input__main_jsx.jsx" = """
const values = { "b": 1, "a": 2 };
const alias = values;
const keys = [];
for (const key of globalThis.Object.keys(alias)) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
  throw new Error('unexpected Object.keys iteration semantics');
}
"""
"run_supports_global_object_keys_iteration_in_jsx_and_tsx_input__main_tsx.tsx" = """
const values = { "b": 1, "a": 2 };
const alias = values;
const keys = [];
for (const key of globalThis.Object.keys(alias)) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
  throw new Error('unexpected Object.keys iteration semantics');
}
"""
"run_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__main_jsx.jsx" = """
const values = { "b": 1, "a": 2 };
const alias = values;
const keys = [];
for (const key of globalThis.Object.keys(alias)) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
  throw new Error('unexpected Object.keys iteration semantics');
}
"""
"run_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__main_ts.ts" = """
const values = { "b": 1, "a": 2 };
const alias = values;
const keys = [];
for (const key of globalThis.Object.keys(alias)) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
  throw new Error('unexpected Object.keys iteration semantics');
}
"""
"run_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__main_tsx.tsx" = """
const values = { "b": 1, "a": 2 };
const alias = values;
const keys = [];
for (const key of globalThis.Object.keys(alias)) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
  throw new Error('unexpected Object.keys iteration semantics');
}
"""
"run_supports_object_entries_from_entries_iteration_in_js_input.js" = """
const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
const seen = [];
for (const entry of Object.entries(fromEntries)) {
  seen.push(entry[0]);
  seen.push(entry[1]);
}
if (seen.length !== 4 || seen[0] !== 'b' || seen[1] !== 3 || seen[2] !== 'a' || seen[3] !== 2) {
  throw new Error('unexpected Object.entries(Object.fromEntries(...)) iteration semantics');
}
"""
"run_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input__main_jsx.jsx" = """
const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
const seen = [];
for (const entry of Object.entries(fromEntries)) {
  seen.push(entry[0]);
  seen.push(entry[1]);
}
if (seen.length !== 4 || seen[0] !== 'b' || seen[1] !== 3 || seen[2] !== 'a' || seen[3] !== 2) {
  throw new Error('unexpected Object.entries(Object.fromEntries(...)) iteration semantics');
}
"""
"run_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input__main_tsx.tsx" = """
const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
const seen = [];
for (const entry of Object.entries(fromEntries)) {
  seen.push(entry[0]);
  seen.push(entry[1]);
}
if (seen.length !== 4 || seen[0] !== 'b' || seen[1] !== 3 || seen[2] !== 'a' || seen[3] !== 2) {
  throw new Error('unexpected Object.entries(Object.fromEntries(...)) iteration semantics');
}
"""
"run_supports_object_entries_from_entries_iteration_in_ts_input.ts" = """
const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
const seen = [];
for (const entry of Object.entries(fromEntries)) {
  seen.push(entry[0]);
  seen.push(entry[1]);
}
if (seen.length !== 4 || seen[0] !== 'b' || seen[1] !== 3 || seen[2] !== 'a' || seen[3] !== 2) {
  throw new Error('unexpected Object.entries(Object.fromEntries(...)) iteration semantics');
}
"""
"run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__main_js.js" = """
const values = { "b": 1, "a": 2 };
const alias = values;
const keys = [];
for (const key of Object.keys(alias)) {
  if (key === 'b') {
    continue;
  }
  keys.push(key);
  break;
}
if (keys.length !== 1 || keys[0] !== 'a') {
  throw new Error('unexpected Object.keys break/continue iteration semantics');
}
"""
"run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__main_jsx.jsx" = """
const values = { "b": 1, "a": 2 };
const alias = values;
const keys = [];
for (const key of Object.keys(alias)) {
  if (key === 'b') {
    continue;
  }
  keys.push(key);
  break;
}
if (keys.length !== 1 || keys[0] !== 'a') {
  throw new Error('unexpected Object.keys break/continue iteration semantics');
}
"""
"run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__main_ts.ts" = """
const values = { "b": 1, "a": 2 };
const alias = values;
const keys = [];
for (const key of Object.keys(alias)) {
  if (key === 'b') {
    continue;
  }
  keys.push(key);
  break;
}
if (keys.length !== 1 || keys[0] !== 'a') {
  throw new Error('unexpected Object.keys break/continue iteration semantics');
}
"""
"run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__main_tsx.tsx" = """
const values = { "b": 1, "a": 2 };
const alias = values;
const keys = [];
for (const key of Object.keys(alias)) {
  if (key === 'b') {
    continue;
  }
  keys.push(key);
  break;
}
if (keys.length !== 1 || keys[0] !== 'a') {
  throw new Error('unexpected Object.keys break/continue iteration semantics');
}
"""
"run_supports_object_keys_from_entries_iteration_in_js_input.js" = """
const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
const keys = [];
for (const key of Object.keys(fromEntries)) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
  throw new Error('unexpected Object.keys(Object.fromEntries(...)) iteration semantics');
}
"""
"run_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input__main_jsx.jsx" = """
const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
const keys = [];
for (const key of Object.keys(fromEntries)) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
  throw new Error('unexpected Object.keys(Object.fromEntries(...)) iteration semantics');
}
"""
"run_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input__main_tsx.tsx" = """
const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
const keys = [];
for (const key of Object.keys(fromEntries)) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
  throw new Error('unexpected Object.keys(Object.fromEntries(...)) iteration semantics');
}
"""
"run_supports_object_keys_from_entries_iteration_in_ts_input.ts" = """
const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
const keys = [];
for (const key of Object.keys(fromEntries)) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
  throw new Error('unexpected Object.keys(Object.fromEntries(...)) iteration semantics');
}
"""
"run_supports_object_keys_iteration_in_js_input.js" = """
const values = { "b": 1, "a": 2 };
const alias = values;
const keys = [];
for (const key of Object.keys(alias)) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
  throw new Error('unexpected Object.keys iteration semantics');
}
"""
"run_supports_object_keys_iteration_in_jsx_and_tsx_input__main_jsx.jsx" = """
const values = { "b": 1, "a": 2 };
const alias = values;
const keys = [];
for (const key of Object.keys(alias)) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
  throw new Error('unexpected Object.keys iteration semantics');
}
"""
"run_supports_object_keys_iteration_in_jsx_and_tsx_input__main_tsx.tsx" = """
const values = { "b": 1, "a": 2 };
const alias = values;
const keys = [];
for (const key of Object.keys(alias)) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
  throw new Error('unexpected Object.keys iteration semantics');
}
"""
"run_supports_object_keys_iteration_in_ts_input.ts" = """
const values = { "b": 1, "a": 2 };
const alias = values;
const keys = [];
for (const key of Object.keys(alias)) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
  throw new Error('unexpected Object.keys iteration semantics');
}
"""
"run_supports_object_keys_iteration_with_direct_literal_object_in_js_input.js" = """
const keys = [];
for (const key of Object.keys({ "b": 1, "a": 2 })) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
  throw new Error('unexpected Object.keys iteration semantics');
}
"""
"run_supports_object_keys_iteration_with_direct_literal_object_in_ts_input.ts" = """
const keys = [];
for (const key of Object.keys({ "b": 1, "a": 2 })) {
  keys.push(key);
}
if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
  throw new Error('unexpected Object.keys iteration semantics');
}
"""
"run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__main_js.js" = """
const seen = [];
for (const value of 'ab') {
  seen.push(value);
}
if (seen.length !== 2 || seen[0] !== 'a' || seen[1] !== 'b') {
  throw new Error('unexpected string iteration semantics');
}
"""
"run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__main_jsx.jsx" = """
const seen = [];
for (const value of 'ab') {
  seen.push(value);
}
if (seen.length !== 2 || seen[0] !== 'a' || seen[1] !== 'b') {
  throw new Error('unexpected string iteration semantics');
}
"""
"run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__main_ts.ts" = """
const seen = [];
for (const value of 'ab') {
  seen.push(value);
}
if (seen.length !== 2 || seen[0] !== 'a' || seen[1] !== 'b') {
  throw new Error('unexpected string iteration semantics');
}
"""
"run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__main_tsx.tsx" = """
const seen = [];
for (const value of 'ab') {
  seen.push(value);
}
if (seen.length !== 2 || seen[0] !== 'a' || seen[1] !== 'b') {
  throw new Error('unexpected string iteration semantics');
}
"""
"run_supports_object_values_from_entries_iteration_in_js_input.js" = """
const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
const seen = [];
for (const value of Object.values(fromEntries)) {
  seen.push(value);
}
if (seen.length !== 2 || seen[0] !== 3 || seen[1] !== 2) {
  throw new Error('unexpected Object.values(Object.fromEntries(...)) iteration semantics');
}
"""
"run_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input__main_jsx.jsx" = """
const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
const seen = [];
for (const value of Object.values(fromEntries)) {
  seen.push(value);
}
if (seen.length !== 2 || seen[0] !== 3 || seen[1] !== 2) {
  throw new Error('unexpected Object.values(Object.fromEntries(...)) iteration semantics');
}
"""
"run_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input__main_tsx.tsx" = """
const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
const seen = [];
for (const value of Object.values(fromEntries)) {
  seen.push(value);
}
if (seen.length !== 2 || seen[0] !== 3 || seen[1] !== 2) {
  throw new Error('unexpected Object.values(Object.fromEntries(...)) iteration semantics');
}
"""
"run_supports_object_values_from_entries_iteration_in_ts_input.ts" = """
const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
const seen = [];
for (const value of Object.values(fromEntries)) {
  seen.push(value);
}
if (seen.length !== 2 || seen[0] !== 3 || seen[1] !== 2) {
  throw new Error('unexpected Object.values(Object.fromEntries(...)) iteration semantics');
}
"""
"run_supports_object_values_iteration_in_js_input.js" = """
const values = { "b": 1, "a": 2 };
const alias = values;
const seen = [];
for (const value of Object.values(alias)) {
  seen.push(value);
}
if (seen.length !== 2 || seen[0] !== 1 || seen[1] !== 2) {
  throw new Error('unexpected Object.values iteration semantics');
}
"""
"run_supports_object_values_iteration_in_jsx_and_tsx_input__main_jsx.jsx" = """
const values = { "b": 1, "a": 2 };
const alias = values;
const seen = [];
for (const value of Object.values(alias)) {
  seen.push(value);
}
if (seen.length !== 2 || seen[0] !== 1 || seen[1] !== 2) {
  throw new Error('unexpected Object.values iteration semantics');
}
"""
"run_supports_object_values_iteration_in_jsx_and_tsx_input__main_tsx.tsx" = """
const values = { "b": 1, "a": 2 };
const alias = values;
const seen = [];
for (const value of Object.values(alias)) {
  seen.push(value);
}
if (seen.length !== 2 || seen[0] !== 1 || seen[1] !== 2) {
  throw new Error('unexpected Object.values iteration semantics');
}
"""
"run_supports_object_values_iteration_in_ts_input.ts" = """
const values = { "b": 1, "a": 2 };
const alias = values;
const seen = [];
for (const value of Object.values(alias)) {
  seen.push(value);
}
if (seen.length !== 2 || seen[0] !== 1 || seen[1] !== 2) {
  throw new Error('unexpected Object.values iteration semantics');
}
"""
"test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__smoke_test_js.test.js" = """
Kali.test('await-wrapped static object helpers', () => {
  async function main() {
    if (!Object.is(await 1, await 1)) {
      throw new Error('unexpected await-wrapped Object.is semantics');
    }
    if (!Number.isSafeInteger(await 1)) {
      throw new Error('unexpected await-wrapped Number.isSafeInteger semantics');
    }
    if (!Object.hasOwn((0, { a: 1 }), 'a')) {
      throw new Error('unexpected sequence-wrapped Object.hasOwn semantics');
    }
    const keys = Object.keys(await { a: 1 });
    if (keys.length !== 1 || keys[0] !== 'a') {
      throw new Error('unexpected await-wrapped Object.keys semantics');
    }
    const sequencedKeys = Object.keys((0, { a: 1 }));
    if (sequencedKeys.length !== 1 || sequencedKeys[0] !== 'a') {
      throw new Error('unexpected sequence-wrapped Object.keys semantics');
    }
    const ownKeys = Reflect.ownKeys(await Object.freeze({ b: 1, a: 2 }));
    if (ownKeys.length !== 2 || ownKeys[0] !== 'b' || ownKeys[1] !== 'a') {
      throw new Error('unexpected await-wrapped Reflect.ownKeys semantics');
    }
    const sequencedOwnKeys = Reflect.ownKeys((0, Object.freeze({ b: 1, a: 2 })));
    if (sequencedOwnKeys.length !== 2 || sequencedOwnKeys[0] !== 'b' || sequencedOwnKeys[1] !== 'a') {
      throw new Error('unexpected sequence-wrapped Reflect.ownKeys semantics');
    }
  }
  return main();
});
"""
"test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__smoke_test_jsx.test.jsx" = """
Kali.test('await-wrapped static object helpers', () => {
  async function main() {
    if (!Object.is(await 1, await 1)) {
      throw new Error('unexpected await-wrapped Object.is semantics');
    }
    if (!Number.isSafeInteger(await 1)) {
      throw new Error('unexpected await-wrapped Number.isSafeInteger semantics');
    }
    if (!Object.hasOwn((0, { a: 1 }), 'a')) {
      throw new Error('unexpected sequence-wrapped Object.hasOwn semantics');
    }
    const keys = Object.keys(await { a: 1 });
    if (keys.length !== 1 || keys[0] !== 'a') {
      throw new Error('unexpected await-wrapped Object.keys semantics');
    }
    const sequencedKeys = Object.keys((0, { a: 1 }));
    if (sequencedKeys.length !== 1 || sequencedKeys[0] !== 'a') {
      throw new Error('unexpected sequence-wrapped Object.keys semantics');
    }
    const ownKeys = Reflect.ownKeys(await Object.freeze({ b: 1, a: 2 }));
    if (ownKeys.length !== 2 || ownKeys[0] !== 'b' || ownKeys[1] !== 'a') {
      throw new Error('unexpected await-wrapped Reflect.ownKeys semantics');
    }
    const sequencedOwnKeys = Reflect.ownKeys((0, Object.freeze({ b: 1, a: 2 })));
    if (sequencedOwnKeys.length !== 2 || sequencedOwnKeys[0] !== 'b' || sequencedOwnKeys[1] !== 'a') {
      throw new Error('unexpected sequence-wrapped Reflect.ownKeys semantics');
    }
  }
  return main();
});
"""
"test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__smoke_test_ts.test.ts" = """
Kali.test('await-wrapped static object helpers', () => {
  async function main() {
    if (!Object.is(await 1, await 1)) {
      throw new Error('unexpected await-wrapped Object.is semantics');
    }
    if (!Number.isSafeInteger(await 1)) {
      throw new Error('unexpected await-wrapped Number.isSafeInteger semantics');
    }
    if (!Object.hasOwn((0, { a: 1 }), 'a')) {
      throw new Error('unexpected sequence-wrapped Object.hasOwn semantics');
    }
    const keys = Object.keys(await { a: 1 });
    if (keys.length !== 1 || keys[0] !== 'a') {
      throw new Error('unexpected await-wrapped Object.keys semantics');
    }
    const sequencedKeys = Object.keys((0, { a: 1 }));
    if (sequencedKeys.length !== 1 || sequencedKeys[0] !== 'a') {
      throw new Error('unexpected sequence-wrapped Object.keys semantics');
    }
    const ownKeys = Reflect.ownKeys(await Object.freeze({ b: 1, a: 2 }));
    if (ownKeys.length !== 2 || ownKeys[0] !== 'b' || ownKeys[1] !== 'a') {
      throw new Error('unexpected await-wrapped Reflect.ownKeys semantics');
    }
    const sequencedOwnKeys = Reflect.ownKeys((0, Object.freeze({ b: 1, a: 2 })));
    if (sequencedOwnKeys.length !== 2 || sequencedOwnKeys[0] !== 'b' || sequencedOwnKeys[1] !== 'a') {
      throw new Error('unexpected sequence-wrapped Reflect.ownKeys semantics');
    }
  }
  return main();
});
"""
"test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__smoke_test_tsx.test.tsx" = """
Kali.test('await-wrapped static object helpers', () => {
  async function main() {
    if (!Object.is(await 1, await 1)) {
      throw new Error('unexpected await-wrapped Object.is semantics');
    }
    if (!Number.isSafeInteger(await 1)) {
      throw new Error('unexpected await-wrapped Number.isSafeInteger semantics');
    }
    if (!Object.hasOwn((0, { a: 1 }), 'a')) {
      throw new Error('unexpected sequence-wrapped Object.hasOwn semantics');
    }
    const keys = Object.keys(await { a: 1 });
    if (keys.length !== 1 || keys[0] !== 'a') {
      throw new Error('unexpected await-wrapped Object.keys semantics');
    }
    const sequencedKeys = Object.keys((0, { a: 1 }));
    if (sequencedKeys.length !== 1 || sequencedKeys[0] !== 'a') {
      throw new Error('unexpected sequence-wrapped Object.keys semantics');
    }
    const ownKeys = Reflect.ownKeys(await Object.freeze({ b: 1, a: 2 }));
    if (ownKeys.length !== 2 || ownKeys[0] !== 'b' || ownKeys[1] !== 'a') {
      throw new Error('unexpected await-wrapped Reflect.ownKeys semantics');
    }
    const sequencedOwnKeys = Reflect.ownKeys((0, Object.freeze({ b: 1, a: 2 })));
    if (sequencedOwnKeys.length !== 2 || sequencedOwnKeys[0] !== 'b' || sequencedOwnKeys[1] !== 'a') {
      throw new Error('unexpected sequence-wrapped Reflect.ownKeys semantics');
    }
  }
  return main();
});
"""
"test_supports_direct_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx" = """
Kali.test('object keys iteration', () => {
  const keys = [];
  for (const key of Object.keys({ "b": 1, "a": 2 })) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
    throw new Error('unexpected Object.keys iteration semantics');
  }
});
"""
"test_supports_direct_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx" = """
Kali.test('object keys iteration', () => {
  const keys = [];
  for (const key of Object.keys({ "b": 1, "a": 2 })) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
    throw new Error('unexpected Object.keys iteration semantics');
  }
});
"""
"test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__smoke_test_js.test.js" = """
Kali.test('frozen object entries iteration', () => {
  const seen = [];
  for (const entry of Object.entries(Object.freeze({ "b": 1, "a": 2 }))) {
    seen.push(entry[0]);
    seen.push(entry[1]);
  }
  if (seen.length !== 4 || seen[0] !== 'b' || seen[1] !== 1 || seen[2] !== 'a' || seen[3] !== 2) {
    throw new Error('unexpected frozen Object.entries iteration semantics');
  }
});
"""
"test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx.test.jsx" = """
Kali.test('frozen object entries iteration', () => {
  const seen = [];
  for (const entry of Object.entries(Object.freeze({ "b": 1, "a": 2 }))) {
    seen.push(entry[0]);
    seen.push(entry[1]);
  }
  if (seen.length !== 4 || seen[0] !== 'b' || seen[1] !== 1 || seen[2] !== 'a' || seen[3] !== 2) {
    throw new Error('unexpected frozen Object.entries iteration semantics');
  }
});
"""
"test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts.test.ts" = """
Kali.test('frozen object entries iteration', () => {
  const seen = [];
  for (const entry of Object.entries(Object.freeze({ "b": 1, "a": 2 }))) {
    seen.push(entry[0]);
    seen.push(entry[1]);
  }
  if (seen.length !== 4 || seen[0] !== 'b' || seen[1] !== 1 || seen[2] !== 'a' || seen[3] !== 2) {
    throw new Error('unexpected frozen Object.entries iteration semantics');
  }
});
"""
"test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx.test.tsx" = """
Kali.test('frozen object entries iteration', () => {
  const seen = [];
  for (const entry of Object.entries(Object.freeze({ "b": 1, "a": 2 }))) {
    seen.push(entry[0]);
    seen.push(entry[1]);
  }
  if (seen.length !== 4 || seen[0] !== 'b' || seen[1] !== 1 || seen[2] !== 'a' || seen[3] !== 2) {
    throw new Error('unexpected frozen Object.entries iteration semantics');
  }
});
"""
"test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_js.test.js" = """
Kali.test('frozen object enumeration', () => {
  const keys = [];
  for (const key of Object.keys(Object.freeze({ a: 1, b: 2 }))) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'a' || keys[1] !== 'b') {
    throw new Error('unexpected frozen object enumeration semantics');
  }
});
"""
"test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx.test.jsx" = """
Kali.test('frozen object enumeration', () => {
  const keys = [];
  for (const key of Object.keys(Object.freeze({ a: 1, b: 2 }))) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'a' || keys[1] !== 'b') {
    throw new Error('unexpected frozen object enumeration semantics');
  }
});
"""
"test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts.test.ts" = """
Kali.test('frozen object enumeration', () => {
  const keys = [];
  for (const key of Object.keys(Object.freeze({ a: 1, b: 2 }))) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'a' || keys[1] !== 'b') {
    throw new Error('unexpected frozen object enumeration semantics');
  }
});
"""
"test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx.test.tsx" = """
Kali.test('frozen object enumeration', () => {
  const keys = [];
  for (const key of Object.keys(Object.freeze({ a: 1, b: 2 }))) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'a' || keys[1] !== 'b') {
    throw new Error('unexpected frozen object enumeration semantics');
  }
});
"""
"test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__smoke_test_js.test.js" = """
Kali.test('frozen object values iteration', () => {
  const seen = [];
  for (const value of Object.values(Object.freeze({ "b": 1, "a": 2 }))) {
    seen.push(value);
  }
  if (seen.length !== 2 || seen[0] !== 1 || seen[1] !== 2) {
    throw new Error('unexpected frozen Object.values iteration semantics');
  }
});
"""
"test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx.test.jsx" = """
Kali.test('frozen object values iteration', () => {
  const seen = [];
  for (const value of Object.values(Object.freeze({ "b": 1, "a": 2 }))) {
    seen.push(value);
  }
  if (seen.length !== 2 || seen[0] !== 1 || seen[1] !== 2) {
    throw new Error('unexpected frozen Object.values iteration semantics');
  }
});
"""
"test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts.test.ts" = """
Kali.test('frozen object values iteration', () => {
  const seen = [];
  for (const value of Object.values(Object.freeze({ "b": 1, "a": 2 }))) {
    seen.push(value);
  }
  if (seen.length !== 2 || seen[0] !== 1 || seen[1] !== 2) {
    throw new Error('unexpected frozen Object.values iteration semantics');
  }
});
"""
"test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx.test.tsx" = """
Kali.test('frozen object values iteration', () => {
  const seen = [];
  for (const value of Object.values(Object.freeze({ "b": 1, "a": 2 }))) {
    seen.push(value);
  }
  if (seen.length !== 2 || seen[0] !== 1 || seen[1] !== 2) {
    throw new Error('unexpected frozen Object.values iteration semantics');
  }
});
"""
"test_supports_global_object_keys_iteration_in_js_input.test.js" = """
Kali.test('object keys iteration', () => {
  const values = { "b": 1, "a": 2 };
  const alias = values;
  const keys = [];
  for (const key of globalThis.Object.keys(alias)) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
    throw new Error('unexpected Object.keys iteration semantics');
  }
});
"""
"test_supports_global_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx" = """
Kali.test('object keys iteration', () => {
  const values = { "b": 1, "a": 2 };
  const alias = values;
  const keys = [];
  for (const key of globalThis.Object.keys(alias)) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
    throw new Error('unexpected Object.keys iteration semantics');
  }
});
"""
"test_supports_global_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx" = """
Kali.test('object keys iteration', () => {
  const values = { "b": 1, "a": 2 };
  const alias = values;
  const keys = [];
  for (const key of globalThis.Object.keys(alias)) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
    throw new Error('unexpected Object.keys iteration semantics');
  }
});
"""
"test_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__smoke_test_jsx.test.jsx" = """
Kali.test('object keys iteration', () => {
  const values = { "b": 1, "a": 2 };
  const alias = values;
  const keys = [];
  for (const key of globalThis.Object.keys(alias)) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
    throw new Error('unexpected Object.keys iteration semantics');
  }
});
"""
"test_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__smoke_test_ts.test.ts" = """
Kali.test('object keys iteration', () => {
  const values = { "b": 1, "a": 2 };
  const alias = values;
  const keys = [];
  for (const key of globalThis.Object.keys(alias)) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
    throw new Error('unexpected Object.keys iteration semantics');
  }
});
"""
"test_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__smoke_test_tsx.test.tsx" = """
Kali.test('object keys iteration', () => {
  const values = { "b": 1, "a": 2 };
  const alias = values;
  const keys = [];
  for (const key of globalThis.Object.keys(alias)) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
    throw new Error('unexpected Object.keys iteration semantics');
  }
});
"""
"test_supports_object_entries_from_entries_iteration_in_js_input.test.js" = """
Kali.test('object entries fromEntries iteration', () => {
  const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
  const seen = [];
  for (const entry of Object.entries(fromEntries)) {
    seen.push(entry[0]);
    seen.push(entry[1]);
  }
  if (seen.length !== 4 || seen[0] !== 'b' || seen[1] !== 3 || seen[2] !== 'a' || seen[3] !== 2) {
    throw new Error('unexpected Object.entries(Object.fromEntries(...)) iteration semantics');
  }
});
"""
"test_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx" = """
Kali.test('object entries fromEntries iteration', () => {
  const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
  const seen = [];
  for (const entry of Object.entries(fromEntries)) {
    seen.push(entry[0]);
    seen.push(entry[1]);
  }
  if (seen.length !== 4 || seen[0] !== 'b' || seen[1] !== 3 || seen[2] !== 'a' || seen[3] !== 2) {
    throw new Error('unexpected Object.entries(Object.fromEntries(...)) iteration semantics');
  }
});
"""
"test_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx" = """
Kali.test('object entries fromEntries iteration', () => {
  const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
  const seen = [];
  for (const entry of Object.entries(fromEntries)) {
    seen.push(entry[0]);
    seen.push(entry[1]);
  }
  if (seen.length !== 4 || seen[0] !== 'b' || seen[1] !== 3 || seen[2] !== 'a' || seen[3] !== 2) {
    throw new Error('unexpected Object.entries(Object.fromEntries(...)) iteration semantics');
  }
});
"""
"test_supports_object_entries_from_entries_iteration_in_ts_input.test.ts" = """
Kali.test('object entries fromEntries iteration', () => {
  const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
  const seen = [];
  for (const entry of Object.entries(fromEntries)) {
    seen.push(entry[0]);
    seen.push(entry[1]);
  }
  if (seen.length !== 4 || seen[0] !== 'b' || seen[1] !== 3 || seen[2] !== 'a' || seen[3] !== 2) {
    throw new Error('unexpected Object.entries(Object.fromEntries(...)) iteration semantics');
  }
});
"""
"test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__smoke_test_js.test.js" = """
Kali.test('object keys break/continue iteration', () => {
  const values = { "b": 1, "a": 2 };
  const alias = values;
  const keys = [];
  for (const key of Object.keys(alias)) {
    if (key === 'b') {
      continue;
    }
    keys.push(key);
    break;
  }
  if (keys.length !== 1 || keys[0] !== 'a') {
    throw new Error('unexpected Object.keys break/continue iteration semantics');
  }
});
"""
"test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx.test.jsx" = """
Kali.test('object keys break/continue iteration', () => {
  const values = { "b": 1, "a": 2 };
  const alias = values;
  const keys = [];
  for (const key of Object.keys(alias)) {
    if (key === 'b') {
      continue;
    }
    keys.push(key);
    break;
  }
  if (keys.length !== 1 || keys[0] !== 'a') {
    throw new Error('unexpected Object.keys break/continue iteration semantics');
  }
});
"""
"test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts.test.ts" = """
Kali.test('object keys break/continue iteration', () => {
  const values = { "b": 1, "a": 2 };
  const alias = values;
  const keys = [];
  for (const key of Object.keys(alias)) {
    if (key === 'b') {
      continue;
    }
    keys.push(key);
    break;
  }
  if (keys.length !== 1 || keys[0] !== 'a') {
    throw new Error('unexpected Object.keys break/continue iteration semantics');
  }
});
"""
"test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx.test.tsx" = """
Kali.test('object keys break/continue iteration', () => {
  const values = { "b": 1, "a": 2 };
  const alias = values;
  const keys = [];
  for (const key of Object.keys(alias)) {
    if (key === 'b') {
      continue;
    }
    keys.push(key);
    break;
  }
  if (keys.length !== 1 || keys[0] !== 'a') {
    throw new Error('unexpected Object.keys break/continue iteration semantics');
  }
});
"""
"test_supports_object_keys_from_entries_iteration_in_js_input.test.js" = """
Kali.test('object keys fromEntries iteration', () => {
  const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
  const keys = [];
  for (const key of Object.keys(fromEntries)) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
    throw new Error('unexpected Object.keys(Object.fromEntries(...)) iteration semantics');
  }
});
"""
"test_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx" = """
Kali.test('object keys fromEntries iteration', () => {
  const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
  const keys = [];
  for (const key of Object.keys(fromEntries)) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
    throw new Error('unexpected Object.keys(Object.fromEntries(...)) iteration semantics');
  }
});
"""
"test_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx" = """
Kali.test('object keys fromEntries iteration', () => {
  const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
  const keys = [];
  for (const key of Object.keys(fromEntries)) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
    throw new Error('unexpected Object.keys(Object.fromEntries(...)) iteration semantics');
  }
});
"""
"test_supports_object_keys_from_entries_iteration_in_ts_input.test.ts" = """
Kali.test('object keys fromEntries iteration', () => {
  const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
  const keys = [];
  for (const key of Object.keys(fromEntries)) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
    throw new Error('unexpected Object.keys(Object.fromEntries(...)) iteration semantics');
  }
});
"""
"test_supports_object_keys_iteration_in_js_input.test.js" = """
Kali.test('object keys iteration', () => {
  const values = { "b": 1, "a": 2 };
  const alias = values;
  const keys = [];
  for (const key of Object.keys(alias)) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
    throw new Error('unexpected Object.keys iteration semantics');
  }
});
"""
"test_supports_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx" = """
Kali.test('object keys iteration', () => {
  const values = { "b": 1, "a": 2 };
  const alias = values;
  const keys = [];
  for (const key of Object.keys(alias)) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
    throw new Error('unexpected Object.keys iteration semantics');
  }
});
"""
"test_supports_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx" = """
Kali.test('object keys iteration', () => {
  const values = { "b": 1, "a": 2 };
  const alias = values;
  const keys = [];
  for (const key of Object.keys(alias)) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
    throw new Error('unexpected Object.keys iteration semantics');
  }
});
"""
"test_supports_object_keys_iteration_in_ts_input.test.ts" = """
Kali.test('object keys iteration', () => {
  const values = { "b": 1, "a": 2 };
  const alias = values;
  const keys = [];
  for (const key of Object.keys(alias)) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
    throw new Error('unexpected Object.keys iteration semantics');
  }
});
"""
"test_supports_object_keys_iteration_with_direct_literal_object_in_js_input.test.js" = """
Kali.test('object keys iteration', () => {
  const keys = [];
  for (const key of Object.keys({ "b": 1, "a": 2 })) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
    throw new Error('unexpected Object.keys iteration semantics');
  }
});
"""
"test_supports_object_keys_iteration_with_direct_literal_object_in_ts_input.test.ts" = """
Kali.test('object keys iteration', () => {
  const keys = [];
  for (const key of Object.keys({ "b": 1, "a": 2 })) {
    keys.push(key);
  }
  if (keys.length !== 2 || keys[0] !== 'b' || keys[1] !== 'a') {
    throw new Error('unexpected Object.keys iteration semantics');
  }
});
"""
"test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_js.test.js" = """
Kali.test('string iteration', () => {
  const seen = [];
  for (const value of 'ab') {
    seen.push(value);
  }
  if (seen.length !== 2 || seen[0] !== 'a' || seen[1] !== 'b') {
    throw new Error('unexpected string iteration semantics');
  }
});
"""
"test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx.test.jsx" = """
Kali.test('string iteration', () => {
  const seen = [];
  for (const value of 'ab') {
    seen.push(value);
  }
  if (seen.length !== 2 || seen[0] !== 'a' || seen[1] !== 'b') {
    throw new Error('unexpected string iteration semantics');
  }
});
"""
"test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts.test.ts" = """
Kali.test('string iteration', () => {
  const seen = [];
  for (const value of 'ab') {
    seen.push(value);
  }
  if (seen.length !== 2 || seen[0] !== 'a' || seen[1] !== 'b') {
    throw new Error('unexpected string iteration semantics');
  }
});
"""
"test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx.test.tsx" = """
Kali.test('string iteration', () => {
  const seen = [];
  for (const value of 'ab') {
    seen.push(value);
  }
  if (seen.length !== 2 || seen[0] !== 'a' || seen[1] !== 'b') {
    throw new Error('unexpected string iteration semantics');
  }
});
"""
"test_supports_object_values_from_entries_iteration_in_js_input.test.js" = """
Kali.test('object values fromEntries iteration', () => {
  const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
  const seen = [];
  for (const value of Object.values(fromEntries)) {
    seen.push(value);
  }
  if (seen.length !== 2 || seen[0] !== 3 || seen[1] !== 2) {
    throw new Error('unexpected Object.values(Object.fromEntries(...)) iteration semantics');
  }
});
"""
"test_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx" = """
Kali.test('object values fromEntries iteration', () => {
  const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
  const seen = [];
  for (const value of Object.values(fromEntries)) {
    seen.push(value);
  }
  if (seen.length !== 2 || seen[0] !== 3 || seen[1] !== 2) {
    throw new Error('unexpected Object.values(Object.fromEntries(...)) iteration semantics');
  }
});
"""
"test_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx" = """
Kali.test('object values fromEntries iteration', () => {
  const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
  const seen = [];
  for (const value of Object.values(fromEntries)) {
    seen.push(value);
  }
  if (seen.length !== 2 || seen[0] !== 3 || seen[1] !== 2) {
    throw new Error('unexpected Object.values(Object.fromEntries(...)) iteration semantics');
  }
});
"""
"test_supports_object_values_from_entries_iteration_in_ts_input.test.ts" = """
Kali.test('object values fromEntries iteration', () => {
  const fromEntries = Object.fromEntries([["b", 1], ["a", 2], ["b", 3]]);
  const seen = [];
  for (const value of Object.values(fromEntries)) {
    seen.push(value);
  }
  if (seen.length !== 2 || seen[0] !== 3 || seen[1] !== 2) {
    throw new Error('unexpected Object.values(Object.fromEntries(...)) iteration semantics');
  }
});
"""
"test_supports_object_values_iteration_in_js_input.test.js" = """
Kali.test('object values iteration', () => {
  const values = { "b": 1, "a": 2 };
  const alias = values;
  const seen = [];
  for (const value of Object.values(alias)) {
    seen.push(value);
  }
  if (seen.length !== 2 || seen[0] !== 1 || seen[1] !== 2) {
    throw new Error('unexpected Object.values iteration semantics');
  }
});
"""
"test_supports_object_values_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx" = """
Kali.test('object values iteration', () => {
  const values = { "b": 1, "a": 2 };
  const alias = values;
  const seen = [];
  for (const value of Object.values(alias)) {
    seen.push(value);
  }
  if (seen.length !== 2 || seen[0] !== 1 || seen[1] !== 2) {
    throw new Error('unexpected Object.values iteration semantics');
  }
});
"""
"test_supports_object_values_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx" = """
Kali.test('object values iteration', () => {
  const values = { "b": 1, "a": 2 };
  const alias = values;
  const seen = [];
  for (const value of Object.values(alias)) {
    seen.push(value);
  }
  if (seen.length !== 2 || seen[0] !== 1 || seen[1] !== 2) {
    throw new Error('unexpected Object.values iteration semantics');
  }
});
"""
"test_supports_object_values_iteration_in_ts_input.test.ts" = """
Kali.test('object values iteration', () => {
  const values = { "b": 1, "a": 2 };
  const alias = values;
  const seen = [];
  for (const value of Object.values(alias)) {
    seen.push(value);
  }
  if (seen.length !== 2 || seen[0] !== 1 || seen[1] !== 2) {
    throw new Error('unexpected Object.values iteration semantics');
  }
});
"""

[[case]]
name = "run_supports_object_keys_iteration_in_js_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_keys_iteration_in_js_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_keys_iteration_in_js_input.js"]
exit = "failure"

[[case]]
name = "run_supports_object_keys_iteration_in_ts_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_keys_iteration_in_ts_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_keys_iteration_in_ts_input.ts"]
exit = "failure"

[[case]]
name = "test_supports_object_keys_iteration_in_js_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_keys_iteration_in_js_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_keys_iteration_in_js_input.test.js"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_object_keys_iteration_in_ts_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_keys_iteration_in_ts_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_keys_iteration_in_ts_input.test.ts"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "run_supports_object_keys_iteration_with_direct_literal_object_in_js_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_keys_iteration_with_direct_literal_object_in_js_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_keys_iteration_with_direct_literal_object_in_js_input.js"]
exit = "failure"

[[case]]
name = "run_supports_object_keys_iteration_with_direct_literal_object_in_ts_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_keys_iteration_with_direct_literal_object_in_ts_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_keys_iteration_with_direct_literal_object_in_ts_input.ts"]
exit = "failure"

[[case]]
name = "test_supports_object_keys_iteration_with_direct_literal_object_in_js_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_keys_iteration_with_direct_literal_object_in_js_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_keys_iteration_with_direct_literal_object_in_js_input.test.js"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_object_keys_iteration_with_direct_literal_object_in_ts_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_keys_iteration_with_direct_literal_object_in_ts_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_keys_iteration_with_direct_literal_object_in_ts_input.test.ts"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "run_supports_global_object_keys_iteration_in_js_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_global_object_keys_iteration_in_js_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_global_object_keys_iteration_in_js_input.js"]
exit = "failure"

[[case]]
name = "run_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__main_ts"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_global_object_keys_iteration_in_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__main_ts.ts"]
exit = "failure"

[[case]]
name = "run_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__main_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_global_object_keys_iteration_in_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__main_jsx.jsx"]
exit = "failure"

[[case]]
name = "run_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__main_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_global_object_keys_iteration_in_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__main_tsx.tsx"]
exit = "failure"

[[case]]
name = "test_supports_global_object_keys_iteration_in_js_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_global_object_keys_iteration_in_js_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_global_object_keys_iteration_in_js_input.test.js"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__smoke_test_ts"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_global_object_keys_iteration_in_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__smoke_test_ts.test.ts"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__smoke_test_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_global_object_keys_iteration_in_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__smoke_test_jsx.test.jsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__smoke_test_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_global_object_keys_iteration_in_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_global_object_keys_iteration_in_ts_jsx_tsx_input__smoke_test_tsx.test.tsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "run_supports_object_values_iteration_in_js_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_values_iteration_in_js_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_values_iteration_in_js_input.js"]
exit = "failure"

[[case]]
name = "run_supports_object_values_iteration_in_ts_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_values_iteration_in_ts_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_values_iteration_in_ts_input.ts"]
exit = "failure"

[[case]]
name = "test_supports_object_values_iteration_in_js_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_values_iteration_in_js_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_values_iteration_in_js_input.test.js"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_object_values_iteration_in_ts_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_values_iteration_in_ts_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_values_iteration_in_ts_input.test.ts"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "run_supports_object_keys_from_entries_iteration_in_js_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_keys_from_entries_iteration_in_js_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_keys_from_entries_iteration_in_js_input.js"]
exit = "failure"

[[case]]
name = "run_supports_object_keys_from_entries_iteration_in_ts_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_keys_from_entries_iteration_in_ts_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_keys_from_entries_iteration_in_ts_input.ts"]
exit = "failure"

[[case]]
name = "test_supports_object_keys_from_entries_iteration_in_js_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_keys_from_entries_iteration_in_js_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_keys_from_entries_iteration_in_js_input.test.js"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_object_keys_from_entries_iteration_in_ts_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_keys_from_entries_iteration_in_ts_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_keys_from_entries_iteration_in_ts_input.test.ts"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "run_supports_object_values_from_entries_iteration_in_js_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_values_from_entries_iteration_in_js_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_values_from_entries_iteration_in_js_input.js"]
exit = "failure"

[[case]]
name = "run_supports_object_values_from_entries_iteration_in_ts_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_values_from_entries_iteration_in_ts_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_values_from_entries_iteration_in_ts_input.ts"]
exit = "failure"

[[case]]
name = "test_supports_object_values_from_entries_iteration_in_js_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_values_from_entries_iteration_in_js_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_values_from_entries_iteration_in_js_input.test.js"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_object_values_from_entries_iteration_in_ts_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_values_from_entries_iteration_in_ts_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_values_from_entries_iteration_in_ts_input.test.ts"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "run_supports_object_entries_from_entries_iteration_in_js_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_entries_from_entries_iteration_in_js_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_entries_from_entries_iteration_in_js_input.js"]
exit = "failure"

[[case]]
name = "run_supports_object_entries_from_entries_iteration_in_ts_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_entries_from_entries_iteration_in_ts_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_entries_from_entries_iteration_in_ts_input.ts"]
exit = "failure"

[[case]]
name = "test_supports_object_entries_from_entries_iteration_in_js_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_entries_from_entries_iteration_in_js_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["test", "test_supports_object_entries_from_entries_iteration_in_js_input.test.js"]
exit = "failure"

[[case]]
name = "test_supports_object_entries_from_entries_iteration_in_ts_input"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_entries_from_entries_iteration_in_ts_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["test", "test_supports_object_entries_from_entries_iteration_in_ts_input.test.ts"]
exit = "failure"

[[case]]
name = "run_supports_object_keys_iteration_in_jsx_and_tsx_input__main_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_keys_iteration_in_jsx_and_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_keys_iteration_in_jsx_and_tsx_input__main_jsx.jsx"]
exit = "failure"

[[case]]
name = "run_supports_object_keys_iteration_in_jsx_and_tsx_input__main_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_keys_iteration_in_jsx_and_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_keys_iteration_in_jsx_and_tsx_input__main_tsx.tsx"]
exit = "failure"

[[case]]
name = "test_supports_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_keys_iteration_in_jsx_and_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_keys_iteration_in_jsx_and_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "run_supports_direct_object_keys_iteration_in_jsx_and_tsx_input__main_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_direct_object_keys_iteration_in_jsx_and_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_direct_object_keys_iteration_in_jsx_and_tsx_input__main_jsx.jsx"]
exit = "failure"

[[case]]
name = "run_supports_direct_object_keys_iteration_in_jsx_and_tsx_input__main_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_direct_object_keys_iteration_in_jsx_and_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_direct_object_keys_iteration_in_jsx_and_tsx_input__main_tsx.tsx"]
exit = "failure"

[[case]]
name = "test_supports_direct_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_direct_object_keys_iteration_in_jsx_and_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_direct_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_direct_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_direct_object_keys_iteration_in_jsx_and_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_direct_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "run_supports_global_object_keys_iteration_in_jsx_and_tsx_input__main_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_global_object_keys_iteration_in_jsx_and_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_global_object_keys_iteration_in_jsx_and_tsx_input__main_jsx.jsx"]
exit = "failure"

[[case]]
name = "run_supports_global_object_keys_iteration_in_jsx_and_tsx_input__main_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_global_object_keys_iteration_in_jsx_and_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_global_object_keys_iteration_in_jsx_and_tsx_input__main_tsx.tsx"]
exit = "failure"

[[case]]
name = "test_supports_global_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_global_object_keys_iteration_in_jsx_and_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_global_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_global_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_global_object_keys_iteration_in_jsx_and_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_global_object_keys_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "run_supports_object_values_iteration_in_jsx_and_tsx_input__main_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_values_iteration_in_jsx_and_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_values_iteration_in_jsx_and_tsx_input__main_jsx.jsx"]
exit = "failure"

[[case]]
name = "run_supports_object_values_iteration_in_jsx_and_tsx_input__main_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_values_iteration_in_jsx_and_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_values_iteration_in_jsx_and_tsx_input__main_tsx.tsx"]
exit = "failure"

[[case]]
name = "test_supports_object_values_iteration_in_jsx_and_tsx_input__smoke_test_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_values_iteration_in_jsx_and_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_values_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_object_values_iteration_in_jsx_and_tsx_input__smoke_test_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_values_iteration_in_jsx_and_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_values_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "run_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input__main_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input__main_jsx.jsx"]
exit = "failure"

[[case]]
name = "run_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input__main_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input__main_tsx.tsx"]
exit = "failure"

[[case]]
name = "test_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_keys_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "run_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input__main_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input__main_jsx.jsx"]
exit = "failure"

[[case]]
name = "run_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input__main_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input__main_tsx.tsx"]
exit = "failure"

[[case]]
name = "test_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_values_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "run_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input__main_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input__main_jsx.jsx"]
exit = "failure"

[[case]]
name = "run_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input__main_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input__main_tsx.tsx"]
exit = "failure"

[[case]]
name = "test_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["test", "test_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_jsx.test.jsx"]
exit = "failure"

[[case]]
name = "test_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["test", "test_supports_object_entries_from_entries_iteration_in_jsx_and_tsx_input__smoke_test_tsx.test.tsx"]
exit = "failure"

[[case]]
name = "run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__main_js"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__main_js.js"]
exit = "failure"

[[case]]
name = "run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__main_ts"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__main_ts.ts"]
exit = "failure"

[[case]]
name = "run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__main_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__main_jsx.jsx"]
exit = "failure"

[[case]]
name = "run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__main_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__main_tsx.tsx"]
exit = "failure"

[[case]]
name = "test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__smoke_test_js"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["test", "test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__smoke_test_js.test.js"]
exit = "failure"

[[case]]
name = "test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["test", "test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts.test.ts"]
exit = "failure"

[[case]]
name = "test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["test", "test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx.test.jsx"]
exit = "failure"

[[case]]
name = "test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["test", "test_supports_frozen_object_entries_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx.test.tsx"]
exit = "failure"

[[case]]
name = "run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__main_js"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__main_js.js"]
exit = "failure"

[[case]]
name = "run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__main_ts"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__main_ts.ts"]
exit = "failure"

[[case]]
name = "run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__main_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__main_jsx.jsx"]
exit = "failure"

[[case]]
name = "run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__main_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__main_tsx.tsx"]
exit = "failure"

[[case]]
name = "test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_js"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_js.test.js"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts.test.ts"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx.test.jsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_frozen_object_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx.test.tsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__main_js"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__main_js.js"]
exit = "failure"

[[case]]
name = "run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__main_ts"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__main_ts.ts"]
exit = "failure"

[[case]]
name = "run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__main_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__main_jsx.jsx"]
exit = "failure"

[[case]]
name = "run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__main_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__main_tsx.tsx"]
exit = "failure"

[[case]]
name = "test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__smoke_test_js"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__smoke_test_js.test.js"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts.test.ts"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx.test.jsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_frozen_object_values_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx.test.tsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__main_js"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__main_js.js"]
exit = "failure"

[[case]]
name = "run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__main_ts"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__main_ts.ts"]
exit = "failure"

[[case]]
name = "run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__main_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__main_jsx.jsx"]
exit = "failure"

[[case]]
name = "run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__main_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__main_tsx.tsx"]
exit = "failure"

[[case]]
name = "test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_js"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_js.test.js"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts.test.ts"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx.test.jsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_string_enumeration_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx.test.tsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__main_js"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__main_js.js"]
exit = "failure"

[[case]]
name = "run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__main_ts"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__main_ts.ts"]
exit = "failure"

[[case]]
name = "run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__main_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__main_jsx.jsx"]
exit = "failure"

[[case]]
name = "run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__main_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__main_tsx.tsx"]
exit = "failure"

[[case]]
name = "test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__smoke_test_js"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["test", "test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__smoke_test_js.test.js"]
exit = "failure"

[[case]]
name = "test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__smoke_test_ts"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["test", "test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__smoke_test_ts.test.ts"]
exit = "failure"

[[case]]
name = "test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__smoke_test_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["test", "test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__smoke_test_jsx.test.jsx"]
exit = "failure"

[[case]]
name = "test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__smoke_test_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["test", "test_supports_await_wrapped_static_helper_inputs_in_js_ts_jsx_tsx_input__smoke_test_tsx.test.tsx"]
exit = "failure"

[[case]]
name = "run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__main_js"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__main_js.js"]
exit = "failure"

[[case]]
name = "run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__main_ts"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__main_ts.ts"]
exit = "failure"

[[case]]
name = "run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__main_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__main_jsx.jsx"]
exit = "failure"

[[case]]
name = "run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__main_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command fails closed. It makes NO claim about either stream, so none is written: rule 2 forbids adding an assertion the source did not make merely because it is true. Honest re-pin (PR #16 rev2): kali fails closed/loud here; see docs/superpowers/followups/pr16-honest-repin-inventory.md. Not routed through assert_object_keys_iteration: that helper also serves out-of-batch `test_*` callers that are still green, so it is left untouched."
args = ["run", "run_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__main_tsx.tsx"]
exit = "failure"

[[case]]
name = "test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__smoke_test_js"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__smoke_test_js.test.js"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__smoke_test_ts.test.ts"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__smoke_test_jsx.test.jsx"]
exit = "success"
stdout_contains = ["ok 1"]

[[case]]
name = "test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx"
rationale = "Migrated 1:1 from `for_of_object_keys_iteration.rs`'s `#[test] fn test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input`. The source asserts the command succeeds. It asserts that stdout CONTAINS the pinned needle; the source's claim is a plain `.contains` against a field that has a substring form, so it stays a `*_contains` and is NOT strengthened to an exact pin (ruling 3, clause 3)."
args = ["test", "test_supports_object_keys_break_continue_iteration_in_js_ts_jsx_tsx_input__smoke_test_tsx.test.tsx"]
exit = "success"
stdout_contains = ["ok 1"]
