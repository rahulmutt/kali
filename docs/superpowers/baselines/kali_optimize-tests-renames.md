No renames — the post-refactor basename set is identical to the pre-refactor set apart from the one deliberately-added `kali_test_support_fixtures_round_trip_files` fixture test (the user-approved kali_codegen-style deviation, exercising the new `kali_test_support` dev-dependency). Counts: before=117 (110 tests.rs + 7 profile::tests), after=118 (+1 fixture). The 110 split-out tests and 7 profile tests kept identical names; co-location changed only their `--list` module-path prefixes, which is why the proof strips prefixes and compares basenames.
